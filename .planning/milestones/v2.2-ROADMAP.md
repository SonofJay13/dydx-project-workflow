# Milestone v2.2: Stage 1 Kickoff + Stage 4 Fnspec Split

**Status:** ✅ SHIPPED 2026-05-11
**Phases:** 7-8
**Total Plans:** 7 (4 + 3)
**Requirements:** 21/21 satisfied (5 STG1 + 3 STG2 + 2 STG3 + 6 STG4 + 5 ROUTE)
**Branch:** `main` (PR #1 merged ahead of milestone close)
**Audit:** [`v2.2-MILESTONE-AUDIT.md`](./v2.2-MILESTONE-AUDIT.md) — final verdict `passed` (was `tech_debt` at audit time; 3 hygiene items closed inline pre-archive — see TD Closure Footer in the audit)

## Overview

CHANGE-01 bundle 2 of N per `.planning/CHANGELIST.md` Phase 3. Two phases bundled — Phase 7 ships the missing Stage 1 Kickoff skill plus the upstream-consumer wiring updates to Stage 2 (`discovery-intake` MODIFIED) and Stage 3 (`generate-sow` MODIFIED); Phase 8 splits `generate-functional-spec` into Stage 4a `generate-fnspec-platform` + Stage 4b `generate-fnspec-integration` with the `delivery: native-ai | api` routing key, cross-spec consistency check (owned by 4b, runs FIRST before fnspec write), Stage 5 scope-gate forward-compat interface, and the v2.1 TD-2 stage-skill enum / platform-ziflow routing-claim reconciliation (resolved inline as path-(a): `ziflow` ADDED to the 4-enum `pipefy | wrike | ziflow | other`). All requirements derive verbatim from locked design contracts in DESIGN.md (DESIGN-17/18/19/20/21).

Phase numbering CONTINUES from v2.1 — no `--reset-phase-numbers`. Phase 7 lands BEFORE Phase 8 (Phase 8 reads approved discovery + approved SOW produced by Phase 7's MODIFIED upstream stages).

## Phases

### Phase 7: Stage 1 Kickoff + Discovery/SOW upstream wiring

**Goal**: A reviewer can run a kickoff-to-SOW pipeline end-to-end against a sample CR — produce an approved kickoff artefact, see the downstream stages route correctly on either `discovery-ready` or `draft-sow` branch, and end with an approved single-scope SOW carrying the canonical 4-state lifecycle.

**Depends on**: Phase 6 (platform skills + canonical references must exist; STG2/STG3 stage-skill repointing carries forward from FOUND-05/06)

**Requirements**: STG1-01, STG1-02, STG1-03, STG1-04, STG1-05, STG2-01, STG2-02, STG2-03, STG3-01, STG3-02 (10/10 satisfied)

**Success Criteria** (all TRUE at phase close):
  1. A reviewer can invoke `kickoff-capture` against meeting notes / Miro paste / Field Notes (`processed_at IS NULL` triage default) and receive a `01_kickoff_v<N>.md` artefact carrying a single `kickoff_branch: discovery-ready | draft-sow` enum field, with `[unknown — needs human classification]` markers visible inline wherever auto-classification confidence is low.
  2. When `kickoff_branch: discovery-ready` ships, `discovery-intake` consumes the kickoff (`based_on_kickoff:` MANDATORY in frontmatter) and emits `02_discovery_v<N>.md`; raw-notes entry path returns an explicit RETIRED error rather than silently accepting input.
  3. When `kickoff_branch: draft-sow` ships, `discovery-intake` emits the verbatim hand-off `Stage 2 SKIPPED — kickoff branch = draft-sow` and writes no `02_discovery_v<N>.md` artefact; `generate-sow` then reads the kickoff directly via `based_on_kickoff:`.
  4. `generate-sow` writes `03_sow_v<N>.md` carrying the canonical 4-state lifecycle `draft → client_review → approved → archived` (sole skill retaining `client_review` per DESIGN-08 survey + AUDIT-01.2); single SOW covers BOTH platform AND integration scope (no Stage 3 split — that lives in Stage 4 per DESIGN-20).
  5. Phase 7 structure-check exits 0 — kickoff skill directory + `references/` shape present; canonical-reference pointers at `safety-rules.md` / `stage-numbering.md` / `frontmatter-scheme.md` / `glossary.md` resolve; `based_on_kickoff:` field appears on every test `02_*` artefact; SOW status-lifecycle enum matches the 4-state canonical exactly.

**Plans**: 4 plans (07-03 synthesis split to 07-04 per cross-AI review C2 fix)

- [x] **07-01-PLAN** — NEW `kickoff-capture/` skill (SKILL.md + 3 references/ files: kickoff-template.md / auto-classify-rubric.md / capture-paths.md) + `phase7-structure-check.sh` with `--section <kickoff|discovery|sow|all>` dispatch
- [x] **07-02-PLAN** — MODIFIED `discovery-intake/` (consume kickoff as sole upstream, RETIRE raw-notes entry path, verbatim skip-message on draft-sow branch)
- [x] **07-03-PLAN** — MODIFIED `generate-sow/` (4-state lifecycle `draft → client_review → approved → archived` per AUDIT-01.2 + DESIGN-08; dual-scope H2 split — Platform + Integration sections in single SOW per DESIGN-19 / D-75); surgical edits only; synthesis split to 07-04 per cross-AI review C2
- [x] **07-04-PLAN** — Synthesis: R-02 glossary fix (DESIGN.md alignment to authoritative `kickoff_branch` spelling), 3 fixture CR artefacts, manual e2e smoke against both routing branches, `--all` phase gate, REQUIREMENTS.md 10-row trace flip

**Notable execution detail**: Wave 2 ran 07-02 ‖ 07-03 in parallel on disjoint skill-file scopes (discovery-intake/ vs generate-sow/ directories); structure-check `--section` flag enabled per-skill self-verification before phase-wide synthesis.

### Phase 8: Stage 4 Fnspec Split + ROUTE (incl. TD-2 inline)

**Goal**: A reviewer can produce a Stage 4a platform fnspec AND a Stage 4b integration fnspec from approved discovery+SOW, see every requirement row carry the `delivery: native-ai | api` routing key, watch the consistency check halt on synthetic conflicts, and confirm the routing key survives forward into a v0.3.0-style smoke read — and the v2.1 TD-2 stage-skill enum vs platform-ziflow routing-claim is resolved with a single documented outcome.

**Depends on**: Phase 7 (Stage 4a reads approved `02_discovery_v*` + approved `03_sow_v*`; Stage 4b reads same plus Stage 4a output). Also depends on Phase 6 (platform skills' `references/native-ai-inventory.md` is the classifier input for `delivery:` row tagging).

**Requirements**: STG4-01, STG4-02, STG4-03, STG4-04, STG4-05, STG4-06, ROUTE-01, ROUTE-02, ROUTE-03, ROUTE-04, ROUTE-05 (11/11 satisfied)

**Success Criteria** (all TRUE at phase close):
  1. A reviewer can invoke `generate-fnspec-platform` (Stage 4a) and receive `04a_fnspec-platform_v<N>.md` with every requirement row carrying `delivery: native-ai | api` (canonical enum order, never reversed); HIGH/MEDIUM-confidence rows from the loaded platform skill's `native-ai-inventory.md` suggest `native-ai`, LOW/`[OPEN]` rows default to `api`, and human reviewer overrides per row are preserved on re-run.
  2. A reviewer can invoke `generate-fnspec-integration` (Stage 4b) — the consistency check runs FIRST (before fnspec write); synthetic conflicts (a conflicting `delivery:` tag across 4a/4b, a dangling integration touchpoint ref, an orphan API endpoint) halt the skill and emit `04b_consistency_check_v<N>.md` listing the specific failure rows. On clean checks, `04b_fnspec-integration_v<N>.md` writes successfully.
  3. The v0.3.0 `generate-functional-spec/` skill is RETIRED — directory removed; legacy filename artefacts (`02_functional-spec_v*.md` or `04_functional-spec_v*.md`) still parse via DESIGN-08 lenient-read; templates / READMEs / changelogs reference 4a + 4b in place of `generate-functional-spec`.
  4. All three Stage 5 scope-gate branches resolve cleanly from 4a/4b frontmatter alone (no downstream Stage 5 skill needed in v2.2): (a) full path — 4b exists with `delivery: api` rows; (b) skip-with-addendum — no 4b but 4a carries `## Platform-API Addendum` H2 + `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only`; (c) skip-entirely — no 4b AND no `delivery: api` rows anywhere → no addendum, no frontmatter change. Either-spec-skip (4a-only or 4b-only) is independently selectable per project.
  5. TD-2 carryover RESOLVED with a single documented outcome — D-78 path (a) selected: `ziflow` ADDED to the stage-skill `platform:` enum across all consuming stage skills (10 files, 11 lines edited in 08-03); `platform-ziflow/SKILL.md:14` aligned to the new 4-enum; outcome recorded in DESIGN-20 sub-decision (D-78) + `dydx-delivery/references/glossary.md` routing-key entry.
  6. Forward-compat smoke check passes — `delivery:` field survives at the canonical position on every requirement row through a `based_on_*` chain read into 3 fixture-output artefacts (4a + 4b + downstream stub).

**Plans**: 3 plans, strict W1 → W2 → W3 sequence per D-80 (4b reads 4a output; closeout reads both)

- [x] **08-01-PLAN** — NEW `generate-fnspec-platform/` (Stage 4a) — SKILL.md + 3 references/ files: `fnspec-platform-template.md`, `auto-classify-rubric.md` (D-81 + STG4-05 classifier rubric), `addendum-template.md` (D-79 Platform-API Addendum body skeleton). Wave 1; STG4-01, STG4-04, STG4-05, ROUTE-03. Assertions P1-P8 PASS.
- [x] **08-02-PLAN** — NEW `generate-fnspec-integration/` (Stage 4b) — SKILL.md + 3 references/ files: `fnspec-integration-template.md`, `consistency-rules.md` (D-84 three checks + halt-on-failure), `either-spec-skip-paths.md` (STG4-06 topologies + D-85). Wave 2 (depends on 08-01); STG4-02, STG4-04, ROUTE-01, ROUTE-02, ROUTE-03. Assertions I1-I8 PASS, 0 deviations.
- [x] **08-03-PLAN** — Cross-cutting closeout (RETIRE `generate-functional-spec/` + ROUTE-04 D-78 enum rollout + ROUTE-05 smoke). Wave 3 (depends on 08-01 + 08-02); `autonomous: false` (human-verify checkpoint mid-execution); STG4-03, STG4-06, ROUTE-04, ROUTE-05. T1-T5 atomic pre-checkpoint + T-smoke + T-gate `--all` PASS exit 0 + T-flips landed.

**Notable execution detail**: 08-03 ran in two halves around a human-verify checkpoint. Pre-checkpoint atomically delivered the phase8-structure-check.sh 5-runner harness (P/I/E/S/X assertions with locked-literal grep on E2), the D-78 4-enum rollout (11 lines across 10 files), the R-01 glossary lines 47+66 + DESIGN-20 D-78 sub-decision, the STG4-03 retirement of `generate-functional-spec/` + STG4-06 4a cross-ref + README split, and 3 fixture-output artefacts for ROUTE-05. Post-checkpoint delivered T-smoke 5-step walkthrough against T5 fixtures (documentation-mode smoke per fixture stubs — no live skill invocation), T-gate `--all` PASS, T-flips (REQUIREMENTS.md 11 rows + ROADMAP.md Phase 8 row + STATE.md).

---

## Milestone Summary

**Decimal Phases:**
- None. Phase numbering ran clean integers (7 → 8); no urgent insertions needed.

**Key Decisions:**

| Decision | Rationale | Outcome |
|---|---|---|
| Phase 7 bundles STG1 + STG2 + STG3 (10 reqs) in one phase | STG2/STG3 are downstream consumers of the kickoff artefact contract (`based_on_kickoff:` MANDATORY; `kickoff_branch:` routes) — landing them with STG1 in the same phase means the pipeline lights up end-to-end through Stage 3 for both routing branches before Phase 8 starts | ✓ Good — e2e smoke against both branches green at Phase 7 close; Phase 8 had a working upstream to read from |
| Phase 8 bundles STG4 + ROUTE (11 reqs) per D-63 per-platform-atomic-slicing precedent | STG4 split (4a/4b) + ROUTE consistency check + Stage 5 scope-gate forward-compat + TD-2 inline + delivery routing-key propagation are all tightly coupled to the DESIGN-20 contract; ship as one atomic phase | ✓ Good — phase8-structure-check.sh `--all` PASS at close; 0 cross-coupling defects surfaced |
| TD-2 (ROUTE-04) resolved INLINE in Phase 8 as path-(a) — ADD `ziflow` to stage-skill `platform:` enum (D-78) | Path-(a) preserves Ziflow as a primary platform routing key consistent with `platform-ziflow/SKILL.md:14` claim; alternative path-(b) (document Ziflow as integration-only) would have forced re-litigating the v2.1 platform-skill catalogue | ✓ Good — D-78 4-enum (`pipefy \| wrike \| ziflow \| other`) rolled out across 10 files / 11 lines in 08-03; no residue of 3-enum left in `dydx-delivery/` |
| Wave 3 (08-03) `autonomous: false` with human-verify checkpoint mid-execution | Cross-cutting closeout edits 10 disjoint files + retires a skill directory + lands fixture artefacts — too many concurrent surfaces for unattended execution; reviewer wanted gate before T-gate run | ✓ Good — orchestrator paused at checkpoint; reviewer approved; T-gate ran cleanly post-approval |
| Cross-spec consistency check OWNED by Stage 4b (D-84) — runs FIRST before fnspec write | Single-owner avoids 4a-vs-4b double-write race + halts on conflict before emitting an inconsistent artefact; D-84 names the three checks: no conflicting delivery: tags / no dangling refs / no orphan API endpoints | ✓ Good — landed in 08-02 with halt-on-failure → `04b_consistency_check_v<N>.md` emission |
| Stage 5 scope-gate FORWARD-COMPAT only in v2.2 (DESIGN-21) — 4a/4b emit the necessary frontmatter (`has_platform_api_addendum:`, `tech_spec_scope:`) and the `## Platform-API Addendum` H2; actual Stage 5 consumption ships in v2.3 | Avoids premature Stage 5 work; ROUTE-03 success criteria fully testable from frontmatter alone in v2.2 | ✓ Good — 3 fixture-output artefacts in 08-03 prove scope-gate branches resolve cleanly from frontmatter |
| `delivery: native-ai \| api` canonical enum order LOCKED — never reversed (D-81 + STG4-04) | Reverse order would invert downstream consumer assumptions in Stages 5/6/7b/10; lock the canonical and grep-gate against the reverse | ✓ Good — anti-pattern scan returned 0 matches of reversed `api \| native-ai` across all dydx-delivery/ skills shipped in v2.2 |

**Issues Resolved:**

- **v2.1 TD-2 closed** — Stage-skill `platform:` enum (`pipefy \| wrike \| other`) was inconsistent with `platform-ziflow/SKILL.md:14` routing-key claim; resolved as D-78 path-(a) — `ziflow` ADDED to the 4-enum across all consuming stage skills (10 files / 11 lines in 08-03).
- **R-02 DESIGN.md glossary drift** — DESIGN.md glossary entry for `kickoff_branch` had legacy `discovery-via` / `kickoff-direct` spellings; aligned to authoritative `discovery-ready` / `draft-sow` in 07-04 (b038995).
- **R-01 glossary lines 47+66** — Both legacy spellings cleaned in `dydx-delivery/references/glossary.md`; routing-key entry + DESIGN-20 D-78 sub-decision landed inline (c6d05dc).
- **STG4-03 retirement of v0.3.0 `generate-functional-spec/`** — Directory removed; templates / READMEs / changelogs repointed to 4a + 4b; lenient-read mode in DESIGN-08 preserves backward compat for legacy `_v*.md` filenames.

**Issues Deferred:**

- **Stage 5 / Stage 6 / Stage 7a/7b / Stage 8 build** — Sequenced for v2.3-v2.6 per CHANGELIST.md Phase 4..8; v2.2 only emits the forward-compat interface (ROUTE-03 + ROUTE-05).
- **Native-AI ingestion paths** — Locked OUT-OF-SCOPE through v2.6 per UAT-6.1; Stage 10 paste-bundle path lands in v2.5.
- **Nyquist VALIDATION.md flag normalization** — Phase 7 + Phase 8 VALIDATION.md docs have `nyquist_compliant: false` (Phase 7) / absent frontmatter (Phase 8); structure-checks both run exit 0 with full PASS so actual coverage is green. Optional `/gsd-validate-phase 7` + `/gsd-validate-phase 8` retroactive normalization available if clean Nyquist signaling needed for archive; not blocking close.

**Technical Debt Incurred:**

- **None blocking.** The 3 hygiene items surfaced in v2.2-MILESTONE-AUDIT.md (TD-v2.2-01 kickoff-capture missing `platform:` frontmatter / TD-v2.2-02 `when-to-open-claude-code.md:27` retired-skill name in ASCII pipeline / TD-v2.2-03 plugin manifest stale "Seven skills" prose) were CLOSED INLINE before milestone archive in commit `e9c69cf` (chore(v2.2): close TD-v2.2-01..03 hygiene items inline). Audit verdict flipped `tech_debt` → `passed`.

---

## Carryover into v2.3

Key v2.2 decisions that lock baseline for v2.3 planning:

- **D-78 path (a)** — `ziflow` as routing-key value is now canonical baseline; any new platform-skill must follow `pipefy | wrike | ziflow | other` precedent (extend to 5-enum only via a new D-XX decision).
- **D-79** — Stage 4a authors `## Platform-API Addendum` inline on platform-only-with-API-rows topology; Stage 5 v2.3 will consume `has_platform_api_addendum:` + `tech_spec_scope:` frontmatter.
- **D-84** — Stage 4b owns three consistency checks (run FIRST before write); Stage 5 v2.3 will inherit similar "consistency-first" discipline for its scope-gate decisions.
- **D-85** — Either-spec-skip with verbatim em-dash skip-emit; Stage 5 v2.3 will face the analogous "either-spec-skip" topology on its three scope-gate paths (full / addendum-only / skip-entirely).

---

*For current project status, see [.planning/ROADMAP.md](../ROADMAP.md)*
*Milestone archived: 2026-05-11 via `/gsd-complete-milestone v2.2`*
