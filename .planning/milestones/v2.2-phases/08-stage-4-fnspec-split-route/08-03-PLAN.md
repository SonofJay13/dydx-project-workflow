---
plan: 08-03
phase: 8
slug: stage-4-fnspec-split-route
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - dydx-delivery/skills/discovery-intake/SKILL.md
  - dydx-delivery/skills/discovery-intake/references/intake-template.md
  - dydx-delivery/skills/generate-build-prompt/SKILL.md
  - dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md
  - dydx-delivery/skills/generate-technical-spec/SKILL.md
  - dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md
  - dydx-delivery/skills/generate-test-plan/SKILL.md
  - dydx-delivery/skills/generate-test-plan/references/test-plan-template.md
  - dydx-delivery/skills/execute-tests/references/results-template.md
  - dydx-delivery/README.md
  - dydx-delivery/references/glossary.md
  - .planning/DESIGN.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
files_created:
  - .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh
  - .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04a_fnspec-platform_v1.md
  - .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_fnspec-integration_v1.md
  - .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_consistency_check_v1.md
files_deleted:
  - dydx-delivery/skills/generate-functional-spec/SKILL.md
  - dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md
autonomous: false
requirements: [STG4-03, STG4-06, ROUTE-04, ROUTE-05, STG4-01, STG4-02, STG4-04, STG4-05, ROUTE-01, ROUTE-02, ROUTE-03]
must_haves:
  truths:
    - "v0.3.0 `dydx-delivery/skills/generate-functional-spec/` directory does NOT exist after this plan (STG4-03; assertion E5)"
    - "D-78 4-enum (`pipefy | wrike | ziflow | other`) present in `platform:` enum line in all 11 D-78 touch-list files (5 stage SKILL.md files + 5 stage templates + dydx-delivery/README.md) per ROUTE-04 (assertion E1)"
    - "`platform-ziflow/SKILL.md:14` retains its routing-key claim per D-78 path (a) (assertion E2)"
    - "`dydx-delivery/references/glossary.md` routing-key entry enumerates `pipefy | wrike | ziflow | other` per D-78 + ROUTE-04 (assertion E3)"
    - "`dydx-delivery/references/glossary.md` `**kickoff_branch**` entry (line 66) uses `discovery-ready | draft-sow` per R-01 Phase 7 carry-forward fix AND `**based_on_discovery**` entry (line 47) drops the `(discovery-via branch)` parenthetical so the whole-file grep for `discovery-via` returns empty (NOT the stale `kickoff-direct | discovery-via` spelling anywhere in the file; assertion E4)"
    - "Three topologies (4a-only / 4b-only / both) documented in BOTH 4a + 4b SKILL.md bodies via the 08-02 `either-spec-skip-paths.md` cross-reference (assertion E6 — STG4-06)"
    - "`phase8-structure-check.sh --section <4a|4b|route|smoke|all>` exists with `--section` partitioning + cross-section assertions X1-X4 per D-77/D-86 (RESEARCH §2.3)"
    - "Forward-compat smoke section asserts S1-S6 all PASS via synthetic Stage 5/6/7b/10 consumer-stub functions reading 4a + 4b fixtures (ROUTE-05 + D-86)"
    - "3 fixture-output artefacts captured at `fixtures/output/` per Phase 7 07-04 manual e2e smoke pattern: `sample-cr-04a_fnspec-platform_v1.md` + `sample-cr-04b_fnspec-integration_v1.md` + `sample-cr-04b_consistency_check_v1.md`"
    - "`bash phase8-structure-check.sh --all` exits 0 with `ALL ASSERTIONS PASSED` (8 P + 8 I + 6 E + 6 S + 4 X = ~32 PASS lines)"
    - "All 11 Phase 8 REQUIREMENTS.md `## Traceability` table rows (STG4-01..06 + ROUTE-01..05) flip `Pending → Satisfied` AFTER `--all` exits 0 (per Phase 7 C5 ordering precedent — never before)"
    - "`.planning/DESIGN.md` §DESIGN-20 carries a sub-decision note recording the D-78 TD-2 resolution outcome (ziflow added to stage-skill `platform:` enum; `platform-ziflow/SKILL.md:14` routing-key claim retained)"
  artifacts:
    - path: ".planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh"
      provides: "Phase 8 structure-check harness with `--section <4a|4b|route|smoke|all>` dispatch (D-77 / D-86)"
      contains: "Shebang + comment-vs-code policy banner + `set -euo pipefail` + fail/pass helpers + arg parser (--section, --all, --help) + 5 runner functions (run_4a_section / run_4b_section / run_route_section / run_smoke_section / run_cross_section) + 4 synthetic consumer-stub functions for smoke (read_frontmatter / iterate_delivery_rows / synth_stage5_stub / synth_stage6_stub / synth_stage7b_stub / synth_stage10_stub) + dispatch case + ALL ASSERTIONS PASSED footer; P1-P8 + I1-I8 + E1-E6 + S1-S6 + X1-X4 assertion blocks lifted from RESEARCH §2.3 partition table"
    - path: ".planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04a_fnspec-platform_v1.md"
      provides: "Manual e2e smoke fixture — Stage 4a output produced against a sample-CR upstream"
      contains: "4a frontmatter (D-78 4-enum) + `delivery:` row markup on every sec 4 / 5 / 8 row + (one variant) `## Platform-API Addendum` H2 body with `has_platform_api_addendum: true` per D-79"
    - path: ".planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_fnspec-integration_v1.md"
      provides: "Manual e2e smoke fixture — Stage 4b output produced after consistency-check pass"
      contains: "4b frontmatter (D-78 4-enum + `based_on_fnspec_platform:` chain) + integration touchpoints + API-endpoints section + `delivery:` row markup on every row"
    - path: ".planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_consistency_check_v1.md"
      provides: "Manual e2e smoke fixture — Stage 4b halt-path output produced after a synthetic conflict injection"
      contains: "Halt-path frontmatter (`artefact_type: consistency_check_failure` + `status: halt`) + failure-rows table demonstrating ROUTE-01 check (a) conflict per consistency-rules.md template"
    - path: "dydx-delivery/references/glossary.md"
      provides: "Routing-key entry updated for D-78 4-enum + R-01 carry-forward `kickoff_branch` + `based_on_discovery` spelling fixes"
      contains: "Routing-key entry enumerates `pipefy | wrike | ziflow | other` with one-line definitions per platform; `**kickoff_branch**` entry (line 66) uses `discovery-ready | draft-sow` (no longer `kickoff-direct | discovery-via`); `**based_on_discovery**` entry (line 47) drops the stale `(discovery-via branch)` parenthetical so whole-file `discovery-via` substring is absent"
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-20 sub-decision note recording D-78 outcome"
      contains: "Sub-decision: TD-2 RESOLVED — `ziflow` added to stage-skill `platform:` enum per D-78 path (a); `platform-ziflow/SKILL.md:14` routing-key claim retained; canonical 4-enum is `pipefy | wrike | ziflow | other`"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table reflecting Phase 8 satisfaction"
      contains: "All 11 v2.2 Phase 8 requirement rows (STG4-01..06 + ROUTE-01..05) marked Satisfied / [x]"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 close flips — checkbox + plan-list + Progress table row"
      contains: "Phase 8 entry `- [ ]` → `- [x]`; three plan-list checkboxes (08-01 / 08-02 / 08-03) flipped; Progress table row updated to `Complete ✓ SHIPPED` with completed-date populated"
  key_links:
    - from: ".planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh"
      to: "8 dydx-delivery files modified by this plan + 6 dydx-delivery files authored by 08-01 + 08-02 + 3 fixture-output files authored here"
      via: "`grep -qF` literal-string + file-existence + `! grep` absence + synthetic consumer-stub function assertions"
      pattern: "(run_4a_section|run_4b_section|run_route_section|run_smoke_section|run_cross_section)"
    - from: "phase8-structure-check.sh --all exit 0"
      to: "REQUIREMENTS.md trace flips (all 11 rows Pending → Satisfied)"
      via: "Phase 7 C5 ordering — `--all` PASS is precondition for trace flips; trace flips DO NOT happen if `--all` fails"
      pattern: "ALL ASSERTIONS PASSED"
    - from: "phase8-structure-check.sh --section smoke synth_stage{5,6,7b,10}_stub functions"
      to: "fixtures/output/sample-cr-04a_*.md + fixtures/output/sample-cr-04b_*.md"
      via: "sed/awk/grep against frontmatter + iterate `delivery:` rows; assert canonical `native-ai | api` order survives the forward read chain per ROUTE-05"
      pattern: "delivery: (native-ai|api) \\[[^]]+\\]"
threat_model: {}
---

<plan_origin>
**This plan IS the cross-cutting closeout for Phase 8.** Mirrors Phase 7 07-04-PLAN.md pattern (synthesis split per cross-AI review C2 precedent) — bundles retirement + D-78 enum rollout + structure-check authoring + smoke fixtures + REQUIREMENTS flips + DESIGN-20 sub-decision in a single plan.

**R-03 LOC watch.** RESEARCH §6 R-03 flags this plan may exceed Phase 7 D-69 ~400 LOC heuristic (estimate ~350-500 LOC). Planner reviewed structure: tasks are MOSTLY mechanical (11 enum edits + 1 directory delete + 3 fixtures + 1 bash script + 1 glossary edit + 1 DESIGN.md note + 11 REQUIREMENTS flips), with the bash script being the only authored-from-scratch artefact (~250-320 LOC per RESEARCH §2.1). At plan-write time the planner judged single-plan viability is acceptable because: (a) most tasks are mechanical greps; (b) the bash script lifts directly from phase7-structure-check.sh (207 lines on disk); (c) splitting would add a 08-04 wave with NO substantive isolation benefit. If at execute time the plan content materially exceeds 400 LOC across all tasks (excluding the bash script body), executor MAY propose a split per Phase 7 07-04 precedent and re-open this plan with the orchestrator. Otherwise proceed.

**autonomous: false** — Task T-smoke (manual e2e smoke walkthrough against sample-CR fixtures) requires reviewer interaction per Phase 7 07-04-T-smoke precedent.
</plan_origin>

<objective>
Phase 8 cross-cutting closeout — STG4-03 retirement of v0.3.0 `generate-functional-spec/`, STG4-06 three-topology documentation cross-reference, ROUTE-04 D-78 enum rollout (11 mechanical edits) + glossary updates (routing-key entry + R-01 carry-forward `kickoff_branch` fix on line 66 + R-01 carry-forward `based_on_discovery` parenthetical fix on line 47) + DESIGN-20 sub-decision note, ROUTE-05 forward-compat smoke harness via `phase8-structure-check.sh --section smoke` + 3 fixture-output artefacts (manual e2e smoke per Phase 7 07-04 pattern), then `--all` final gate + REQUIREMENTS.md trace flips (all 11 rows) + ROADMAP.md plan-list + Progress-table flips after `--all` exits 0.

Task ordering enforces the Phase 7 C5 precedent: structure-check `--all` MUST exit 0 before REQUIREMENTS.md trace + ROADMAP.md is updated. If `--all` fails, trace + roadmap flips DO NOT happen.

Output: 1 new directory deleted, 14 modified files (10 D-78 enum touch-list + glossary + DESIGN.md + REQUIREMENTS.md + ROADMAP.md), 4 new files (1 bash script + 3 fixture-output artefacts). One successful `--all` run; 11 REQUIREMENTS.md trace flips; ROADMAP.md Phase 8 close flips; manual e2e smoke attestation captured in 08-03-SUMMARY.md.

Wave 3 — depends_on: [08-01, 08-02]. Runs after both 4a + 4b skill files ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DESIGN.md
@.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md
@.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md
@.planning/phases/08-stage-4-fnspec-split-route/08-VALIDATION.md
@.planning/phases/08-stage-4-fnspec-split-route/08-01-SUMMARY.md
@.planning/phases/08-stage-4-fnspec-split-route/08-02-SUMMARY.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-PLAN.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-SUMMARY.md
@dydx-delivery/skills/generate-fnspec-platform/SKILL.md
@dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md
@dydx-delivery/skills/generate-fnspec-integration/SKILL.md
@dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md
@dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md
@dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md
@dydx-delivery/references/glossary.md
@dydx-delivery/references/frontmatter-scheme.md
@dydx-delivery/skills/platform-ziflow/SKILL.md
@dydx-delivery/README.md

<interfaces>
**D-78 enum touch-list (mechanical — 11 EDIT lines + 5 ANCHOR lines verified by RESEARCH §2.3):**

| # | File | Line | Before | After |
|---|------|------|--------|-------|
| 1 | dydx-delivery/skills/discovery-intake/SKILL.md | 114 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 2 | dydx-delivery/skills/discovery-intake/references/intake-template.md | 3 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 3 | dydx-delivery/skills/discovery-intake/references/intake-template.md | 45 | prose `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 4 | dydx-delivery/skills/generate-build-prompt/SKILL.md | 114 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 5 | dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md | 3 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 6 | dydx-delivery/skills/generate-technical-spec/SKILL.md | 101 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 7 | dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md | 3 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 8 | dydx-delivery/skills/generate-test-plan/SKILL.md | 96 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 9 | dydx-delivery/skills/generate-test-plan/references/test-plan-template.md | 4 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 10 | dydx-delivery/skills/execute-tests/references/results-template.md | 4 | `<pipefy | wrike | other>` | `<pipefy | wrike | ziflow | other>` |
| 11 | dydx-delivery/README.md | 62 | prose `<platform>/  # pipefy | wrike` | `<platform>/  # pipefy | wrike | ziflow` |

**Anchors (already 4-enum; no edit needed — assertion E1 checks all 11 EDIT + 5 ANCHOR are now 4-enum):**
- `dydx-delivery/skills/generate-sow/SKILL.md:86` (Phase 7 STG3-02 work)
- `dydx-delivery/skills/generate-sow/references/sow-template.md:3` (Phase 7)
- `dydx-delivery/references/frontmatter-scheme.md:41` (canonical reference)
- `dydx-delivery/references/glossary.md:71` (`**platform**` entry — already 4-enum; ROUTE-04 adds ROUTING-KEY entry separately)
- `dydx-delivery/skills/platform-ziflow/SKILL.md:5+14` (path (a) retention — assertion E2)

**STG4-03 retirement target:** `dydx-delivery/skills/generate-functional-spec/` entire directory (2 files: SKILL.md + references/functional-spec-template.md). E5 asserts directory does NOT exist.

**Phase 7 R-02 carry-forward (R-01 in this plan):** `dydx-delivery/references/glossary.md` STILL contains TWO occurrences of the stale spelling `discovery-via`:
- Line 47 — `**based_on_discovery**` entry contains the parenthetical `(discovery-via branch)` verbatim. Fix: remove the `(discovery-via branch)` parenthetical entirely; `**based_on_discovery**` is unambiguous without it. After fix, line 47 reads (one acceptable form): `**based_on_discovery** — Frontmatter field on `03_sow_*` artefacts pointing at the upstream `02_discovery_v<N>.md` per DESIGN-19.`
- Line 66 — `**kickoff_branch**` entry STILL uses `kickoff-direct (skip Stage 2; feed Stage 3 directly) or discovery-via (feed Stage 2 first)`. Fix: replace the enum values + their parenthetical descriptions with the authoritative spelling `discovery-ready` (Stage 2 runs against this kickoff) `| draft-sow` (Stage 2 SKIPPED; Stage 3 reads kickoff directly via `based_on_kickoff:`).

Phase 7's structure-check K4 only checked the kickoff-template, not the glossary; drift survived in BOTH locations. ROUTE-04 already touches glossary.md (routing-key entry update) — bundle BOTH spelling fixes (line 47 + line 66) into the same edit task. Authoritative spelling for `kickoff_branch` enum values: `discovery-ready | draft-sow`. Assertion E4 (whole-file `! grep -qF 'discovery-via' glossary.md` + `! grep -qF 'kickoff-direct' glossary.md`) catches BOTH occurrences — fix MUST address both lines, otherwise E4 fails at T-gate.

**E1-E6 assertion contract (route section per RESEARCH §2.3):**
- E1 — D-78 4-enum (`ziflow` present) in `platform:` enum line in all 11 modified files (EDIT) + 5 anchors verified.
- E2 — `dydx-delivery/skills/platform-ziflow/SKILL.md:14` retains routing-key claim. **Locked grep string** (verified against file content 2026-05-11): `grep -qF "is the routing key" dydx-delivery/skills/platform-ziflow/SKILL.md` — the literal `is the routing key` appears only on line 14 of platform-ziflow/SKILL.md and is the canonical routing-key-claim sentence. This is the assertion contract; the script author MUST use this exact grep string (NOT a soft `grep -qF 'routing'` which false-passes on line 41's "Stage 4a delivery routing").
- E3 — `glossary.md` routing-key entry enumerates `pipefy | wrike | ziflow | other`.
- E4 — Whole-file `glossary.md` contains NEITHER `kickoff-direct` NOR `discovery-via` AND contains BOTH `discovery-ready` AND `draft-sow` (R-01 carry-forward fix — line 47 `based_on_discovery` AND line 66 `kickoff_branch` must BOTH be fixed; assertion is whole-file substring absence, NOT line-anchored).
- E5 — `dydx-delivery/skills/generate-functional-spec/` directory does NOT exist.
- E6 — Three topologies (4a-only / 4b-only / both) documented in BOTH 4a + 4b SKILL.md bodies (STG4-06).

**S1-S6 assertion contract (smoke section per RESEARCH §2.3 + D-86):**
- S1 — `fixtures/output/sample-cr-04a_fnspec-platform_v1.md` exists and parses YAML frontmatter.
- S2 — `fixtures/output/sample-cr-04b_fnspec-integration_v1.md` exists and parses.
- S3 — Synthetic Stage 5 stub reads `delivery:` field from every row + re-emits at same column position; no rows stripped + no enum reorder (canonical `native-ai | api` survives).
- S4 — Synthetic Stage 6 stub re-emits row count matches 4a + 4b combined.
- S5 — Synthetic Stage 7b stub iterates rows + asserts canonical enum order on every row (no reversed `api | native-ai`).
- S6 — Synthetic Stage 10 stub reads frontmatter `based_on_*` chain end-to-end + asserts no chain break.

**X1-X4 cross-section assertion contract (per RESEARCH §2.3):**
- X1 — `platform:` enum spelling consistent across every skill (all 4-enum after D-78 rollout, including 4 NEW skill files).
- X2 — `delivery:` markup token grep-greppable from BOTH 4a + 4b template files (cross-skill consistency).
- X3 — `based_on_fnspec_platform:` cited in 4b SKILL.md AND `based_on_*` chain documented in canonical `frontmatter-scheme.md`.
- X4 — `phase8-structure-check.sh` itself passes `bash -n` (syntax check) — meta-assert.

**Verbatim D-85 skip-emit string (assertion I5 carry-forward from 08-02):**
```
Stage 4b SKIPPED — no integration work in scope
```
(em-dash unicode verbatim)

**Final phase gate command:**
```
bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --all
```
Expected: 8 P + 8 I + 6 E + 6 S + 4 X = ~32 PASS lines followed by `ALL ASSERTIONS PASSED` + exit 0.
</interfaces>
</context>

<tasks>

<task id="08-03-T1" type="auto">
  <name>Task 1: Ship phase8-structure-check.sh with --section <4a|4b|route|smoke|all> dispatch + 5 runners + synthetic consumer-stub functions (D-77 + D-86)</name>
  <files>.planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh</files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh` (207 lines, full — direct-lift skeleton)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.3 partition table (P1-P8 + I1-I8 + E1-E6 + S1-S6 + X1-X4 assertion bodies verbatim — copy into runners)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.3 lines 366-405 (synthetic consumer-stub conceptual shape — `read_frontmatter` / `iterate_delivery_rows` / `synth_stage5_stub` etc.)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-VALIDATION.md` §1 (Phase Requirements → Verification Map; assertion IDs verbatim)
    - 08-01-SUMMARY.md + 08-02-SUMMARY.md (concrete `platform:` value chosen in templates + final D-85 verbatim string locked + handoff-message strings locked)
    - `dydx-delivery/skills/platform-ziflow/SKILL.md` line 14 (verify the literal `is the routing key` is still present on line 14 before locking the E2 grep string)
  </read_first>
  <action>
    Lift `phase7-structure-check.sh` skeleton wholesale (shebang + comment-vs-code banner + `set -euo pipefail` + `fail()`/`pass()` helpers + arg parser + dispatch case + exit conventions). Adapt per D-77 + D-86 with 5 runners instead of 4 + smoke section's synthetic consumer-stub functions.

    **Banner + USAGE adaptation:**
    1. Retitle banner: `phase8-structure-check.sh — structural verifier for Phase 8 (Stage 4 fnspec split + ROUTE + TD-2 closeout) deliverables`.
    2. Comment-vs-code policy carries forward verbatim (`grep -qF` for literals, `grep -qE` for patterns, no `-i` per phase7 R-01).
    3. USAGE block lists `4a|4b|route|smoke|all` (5 modes vs phase7's 3+all).
    4. Arg parser adds `--section` + `--all` + `--help` (same as phase7) — no `--quick` no-op (drop).

    **5 runner functions:**

    `run_4a_section()` — lift P1-P8 assertions from RESEARCH §2.3 verbatim:
    - P1: `[ -f dydx-delivery/skills/generate-fnspec-platform/SKILL.md ]` else fail.
    - P2: `for ref in fnspec-platform-template.md auto-classify-rubric.md addendum-template.md; do [ -f "dydx-delivery/skills/generate-fnspec-platform/references/$ref" ]; done`.
    - P3: `for canonical in safety-rules.md stage-numbering.md frontmatter-scheme.md glossary.md; do grep -qF "dydx-delivery/references/$canonical" dydx-delivery/skills/generate-fnspec-platform/SKILL.md; done`.
    - P4: `grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md`.
    - P5: `grep -qF 'native-ai | api' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && ! grep -qF 'api | native-ai' dydx-delivery/skills/generate-fnspec-platform/SKILL.md`.
    - P6: `grep -qF 'references/auto-classify-rubric.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md`.
    - P7: `grep -qF 'references/addendum-template.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md`.
    - P8: `grep -qF 'has_platform_api_addendum' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'tech_spec_scope' dydx-delivery/skills/generate-fnspec-platform/SKILL.md`.

    `run_4b_section()` — lift I1-I8 assertions:
    - I1: `[ -f dydx-delivery/skills/generate-fnspec-integration/SKILL.md ]`.
    - I2: 3 references files (`fnspec-integration-template.md`, `consistency-rules.md`, `either-spec-skip-paths.md`).
    - I3: 4 canonical-pointer paths in 4b SKILL.md.
    - I4: `grep -qF 'based_on_fnspec_platform' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF 'based_on_fnspec_platform' dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md`.
    - I5: `grep -qF 'Stage 4b SKIPPED — no integration work in scope'` in 4b SKILL.md OR `either-spec-skip-paths.md`.
    - I6: `grep -qF 'Check (a)' && grep -qF 'Check (b)' && grep -qF 'Check (c)'` in `consistency-rules.md`.
    - I7: `grep -qF '04b_consistency_check_v' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF '04b_consistency_check_v' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md`.
    - I8: `for d in D-78 D-79 D-82 D-84 D-85; do grep -qF "$d" dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF "$d" dydx-delivery/skills/generate-fnspec-integration/SKILL.md; done`.

    `run_route_section()` — lift E1-E6 assertions:
    - E1: For each of the 11 EDIT files in §2.3 touch-list + 5 ANCHOR files: `grep -qF 'pipefy | wrike | ziflow | other' <file>` succeeds. Use a bash array of file paths to keep the runner readable.
    - E2: **Locked literal-anchor grep** — `grep -qF "is the routing key" dydx-delivery/skills/platform-ziflow/SKILL.md`. The literal `is the routing key` occurs only on line 14 (the routing-key-claim sentence: `artefact frontmatter \`platform: ziflow\` is the routing key.`). This grep string is the contract — DO NOT substitute a soft `grep -qF 'routing'` (false-passes on line 41's "Stage 4a delivery routing"). DO NOT defer the grep string to executor authoring. If line 14 content drifts between plan-write date (2026-05-11) and execute time, executor flags + plan is re-opened to re-lock the literal.
    - E3: `grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/references/glossary.md` succeeds in the routing-key entry block (anchored grep using a routing-key-context pattern; planner picks the anchor pattern based on the final glossary line authored in T3 — e.g., `grep -B 5 -A 5 'routing-key' glossary.md | grep -qF 'pipefy | wrike | ziflow | other'`).
    - E4: **Whole-file absence + presence assertions for R-01 carry-forward (BOTH glossary lines 47 AND 66 must be clean):**
      ```bash
      ! grep -qF 'kickoff-direct' dydx-delivery/references/glossary.md \
        && ! grep -qF 'discovery-via' dydx-delivery/references/glossary.md \
        && grep -qF 'discovery-ready' dydx-delivery/references/glossary.md \
        && grep -qF 'draft-sow' dydx-delivery/references/glossary.md
      ```
      `! grep -qF 'discovery-via'` is whole-file; it catches BOTH the stale line 66 `kickoff_branch` enum value AND the stale line 47 `(discovery-via branch)` parenthetical inside the `**based_on_discovery**` entry. Both occurrences MUST be removed by T3 (b); fixing only one fails E4.
    - E5: `[ ! -d dydx-delivery/skills/generate-functional-spec ]` else fail (STG4-03).
    - E6: `grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md` (4a SKILL.md must reference the 4b skip-paths file for the cross-reference per STG4-06; planner edits 4a SKILL.md in T4 below to add this reference if not already present) AND `grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-integration/SKILL.md`. Plus: `for topo in '4a-only' '4b-only' 'both'; do grep -qiF "$topo" dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md; done` (three topologies named).

    `run_smoke_section()` — author the 4 synthetic consumer-stub helper functions (per RESEARCH §2.3 lines 370-402) BEFORE the assertions:
    ```bash
    read_frontmatter() {
      sed -n '/^---$/,/^---$/p' "$1" | sed '1d;$d'
    }

    iterate_delivery_rows() {
      grep -oE 'delivery: (native-ai|api) \[[^]]+\]' "$1"
    }

    synth_stage5_stub() {
      local fixture="$1"
      local row_count
      row_count=$(iterate_delivery_rows "$fixture" | wc -l)
      [ "$row_count" -gt 0 ] || fail "S3: synth_stage5_stub found 0 delivery rows in $fixture"
      if iterate_delivery_rows "$fixture" | grep -qE 'delivery: api \| native-ai'; then
        fail "S3: reversed enum 'api | native-ai' found in $fixture — STG4-04 canonical order violation"
      fi
      echo "  synth_stage5_stub: re-emitted $row_count delivery rows in canonical order from $fixture"
    }

    synth_stage6_stub() {
      local fa="$1"; local fb="$2"
      local count_4a count_4b combined
      count_4a=$(iterate_delivery_rows "$fa" | wc -l)
      count_4b=$(iterate_delivery_rows "$fb" | wc -l)
      combined=$((count_4a + count_4b))
      [ "$combined" -gt 0 ] || fail "S4: synth_stage6_stub: 4a + 4b row counts sum to 0"
      echo "  synth_stage6_stub: 4a=$count_4a + 4b=$count_4b = combined=$combined rows"
    }

    synth_stage7b_stub() {
      local fixture="$1"
      iterate_delivery_rows "$fixture" | while IFS= read -r row; do
        echo "$row" | grep -qE 'delivery: (native-ai|api) \[' \
          || fail "S5: synth_stage7b_stub: malformed delivery row in $fixture: $row"
      done
      echo "  synth_stage7b_stub: every row in $fixture matches canonical delivery: (native-ai|api) [...] pattern"
    }

    synth_stage10_stub() {
      local fixture="$1"
      local based_on
      based_on=$(read_frontmatter "$fixture" | grep -E '^based_on_' || true)
      [ -n "$based_on" ] || fail "S6: synth_stage10_stub: no based_on_* fields in $fixture frontmatter"
      echo "  synth_stage10_stub: based_on_* chain present in $fixture:"
      echo "$based_on" | sed 's/^/    /'
    }
    ```

    Then `run_smoke_section()` body:
    - S1: `[ -f .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04a_fnspec-platform_v1.md ]` AND read_frontmatter on it produces non-empty output (parses YAML).
    - S2: `[ -f .../sample-cr-04b_fnspec-integration_v1.md ]` AND read_frontmatter on it produces non-empty output.
    - S3: `synth_stage5_stub <4a>` AND `synth_stage5_stub <4b>` both pass.
    - S4: `synth_stage6_stub <4a> <4b>` passes.
    - S5: `synth_stage7b_stub <4a>` AND `synth_stage7b_stub <4b>` both pass.
    - S6: `synth_stage10_stub <4a>` AND `synth_stage10_stub <4b>` both pass.

    `run_cross_section()` — X1-X4 assertions:
    - X1: For each `platform:` declarer skill (the 5 modified SKILL.md files + 2 new 4a/4b SKILL.md files + generate-sow SKILL.md anchor): `grep -qF 'pipefy | wrike | ziflow | other' <file>` succeeds.
    - X2: `grep -qF 'delivery:' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md && grep -qF 'delivery:' dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md`.
    - X3: `grep -qF 'based_on_fnspec_platform' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qE 'based_on_' dydx-delivery/references/frontmatter-scheme.md`.
    - X4: `bash -n .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh` (meta-assert; syntax-only check; runs as a sub-shell to avoid recursion).

    **Dispatch case:** `4a|4b|route|smoke|all|""` — `all`/`""` runs all 5 runners; unknown value fails with `unknown --section value: $SECTION (use 4a|4b|route|smoke|all)`. Cross-section runner X1-X4 fires ONLY under `all`/`""` per phase7 D-77 precedent.

    **Final lines:** `echo "ALL ASSERTIONS PASSED"` + `exit 0`.

    `chmod +x` on commit if shell supports it (gracefully fall through on Windows Git Bash where chmod is a no-op).

    **Important:** Author the script BEFORE Tasks T2-T6 modify any files. Why: Tasks T2-T6 produce the input artefacts the script asserts against; if the script ships AFTER edits, executor cannot verify per-task progress with `--section <name>` calls. With the script in place at T1, executor can run `--section <name>` after each downstream task and catch regressions immediately. (Phase 7 07-01 precedent — ship the script first.)
  </action>
  <verify>
    <automated>bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --help && bash -n .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh`
    - `bash phase8-structure-check.sh --help` exits 0 and prints USAGE listing `4a|4b|route|smoke|all`.
    - `bash -n phase8-structure-check.sh` exits 0 (syntax-clean).
    - `grep -qF 'run_4a_section()' phase8-structure-check.sh` succeeds.
    - `grep -qF 'run_4b_section()' phase8-structure-check.sh` succeeds.
    - `grep -qF 'run_route_section()' phase8-structure-check.sh` succeeds.
    - `grep -qF 'run_smoke_section()' phase8-structure-check.sh` succeeds.
    - `grep -qF 'run_cross_section()' phase8-structure-check.sh` succeeds.
    - `grep -qF 'synth_stage5_stub' phase8-structure-check.sh` succeeds.
    - `grep -qF 'synth_stage6_stub' phase8-structure-check.sh` succeeds.
    - `grep -qF 'synth_stage7b_stub' phase8-structure-check.sh` succeeds.
    - `grep -qF 'synth_stage10_stub' phase8-structure-check.sh` succeeds.
    - `grep -qF 'set -euo pipefail' phase8-structure-check.sh` succeeds.
    - `grep -qF 'ALL ASSERTIONS PASSED' phase8-structure-check.sh` succeeds.
    - `grep -qF 'is the routing key' phase8-structure-check.sh` succeeds (locked E2 literal-anchor grep string is present in the script body, not a soft `grep -qF 'routing'`).
    - Note: at this point `--all` will FAIL (T2-T6 not yet shipped) — expected; full PASS after T-gate.
  </acceptance_criteria>
  <done>Script lifted from phase7 skeleton; 5 runners present + synthetic consumer-stub helpers; 5-mode dispatch wired; usage/help works; syntax clean. E2 literal-anchor grep string locked to `is the routing key`. Downstream tasks can self-verify with `--section <name>` after each edit.</done>
</task>

<task id="08-03-T2" type="auto">
  <name>Task 2: D-78 enum rollout — 11 mechanical edits across 5 stage SKILL.md files + 5 templates + README.md (ROUTE-04)</name>
  <files>dydx-delivery/skills/discovery-intake/SKILL.md, dydx-delivery/skills/discovery-intake/references/intake-template.md, dydx-delivery/skills/generate-build-prompt/SKILL.md, dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md, dydx-delivery/skills/generate-technical-spec/SKILL.md, dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md, dydx-delivery/skills/generate-test-plan/SKILL.md, dydx-delivery/skills/generate-test-plan/references/test-plan-template.md, dydx-delivery/skills/execute-tests/references/results-template.md, dydx-delivery/README.md</files>
  <read_first>
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.3 line-numbered touch-list (11 EDIT rows verbatim — re-read at execute time in case line numbers drifted since 2026-05-11 research date)
    - Each of the 10 files above (at the line number per the touch-list) — verify current line content matches the BEFORE value before edit
    - `dydx-delivery/skills/generate-sow/SKILL.md:86` + `dydx-delivery/skills/generate-sow/references/sow-template.md:3` (5-anchor cross-check — these already carry 4-enum; do NOT re-edit)
  </read_first>
  <action>
    Surgical mechanical edits — 11 lines across 10 files. Pattern: replace `<pipefy | wrike | other>` with `<pipefy | wrike | ziflow | other>` per the §2.3 touch-list.

    **Step 1 — Pre-flight grep.** Before edit, verify each touch-list file STILL carries the 3-enum form at the line predicted by RESEARCH (line numbers may have drifted slightly). Run:
    ```bash
    for f in \
      dydx-delivery/skills/discovery-intake/SKILL.md \
      dydx-delivery/skills/discovery-intake/references/intake-template.md \
      dydx-delivery/skills/generate-build-prompt/SKILL.md \
      dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md \
      dydx-delivery/skills/generate-technical-spec/SKILL.md \
      dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md \
      dydx-delivery/skills/generate-test-plan/SKILL.md \
      dydx-delivery/skills/generate-test-plan/references/test-plan-template.md \
      dydx-delivery/skills/execute-tests/references/results-template.md; do
        grep -nF 'pipefy | wrike | other' "$f" || echo "  WARN: 3-enum already absent in $f (already 4-enum OR text drifted)";
    done
    ```
    Expected: each file emits at least one line matching `pipefy | wrike | other` (the prediction from RESEARCH is correct). If any file is already 4-enum OR the predicted line drifted, planner inspects manually and decides per-file.

    **Step 2 — Per-file edit.** For each file in the touch-list, replace `pipefy | wrike | other` with `pipefy | wrike | ziflow | other`. Use surgical edits — do NOT rewrite the whole file. Preserve surrounding text verbatim.

    Files 1-9 (skill + template files): each has 1 occurrence to edit (intake-template.md has 2 — lines 3 + 45 per touch-list rows 2+3).

    File 10 — `dydx-delivery/README.md:62` — this is a prose form (NOT a YAML enum). Touch-list says:
    BEFORE: `│   └── <platform>/                       # pipefy | wrike`
    AFTER:  `│   └── <platform>/                       # pipefy | wrike | ziflow`
    (Prose form has only 2 platform names not 3+other; D-78 path (a) adds `ziflow` to the prose enumeration. Verify against the file at execute time — the prose may have evolved since RESEARCH date.)

    **Step 3 — Post-edit verification.** Run the same grep loop as Step 1 with the new spelling:
    ```bash
    for f in <same 10 files>; do
      grep -qF 'pipefy | wrike | ziflow | other' "$f" || echo "  FAIL: 4-enum missing in $f after edit";
    done
    ```
    Plus negative check: no file in the touch-list should still contain the bare 3-enum `pipefy | wrike | other` (the substring `pipefy | wrike | other` is now obsolete; only `pipefy | wrike | ziflow | other` should match). Run:
    ```bash
    for f in <same 10 files>; do
      if grep -qF 'pipefy | wrike | other' "$f"; then echo "FAIL: 3-enum still present in $f"; fi;
    done
    ```
    (NOTE: this negative check matches the substring; since `pipefy | wrike | other` is a strict prefix of `pipefy | wrike | ziflow | other` is FALSE — the new spelling has `ziflow |` in the middle, not at end — so `grep -qF 'pipefy | wrike | other'` will NOT spuriously match the new spelling. Safe.)

    **Step 4 — Self-verify with phase8-structure-check.sh `--section route`.** Run:
    ```bash
    bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --section route 2>&1 | grep -E '(PASS|FAIL) E1'
    ```
    Expected: `PASS E1` (4-enum present in all 11 EDIT files + 5 ANCHOR files). E2-E6 still expected to fail since T3-T6 haven't shipped yet — acceptable.

    **Anchor cross-check** (no edit needed, just verify the anchors stayed correct after Phase 7):
    ```bash
    for f in \
      dydx-delivery/skills/generate-sow/SKILL.md \
      dydx-delivery/skills/generate-sow/references/sow-template.md \
      dydx-delivery/references/frontmatter-scheme.md \
      dydx-delivery/references/glossary.md \
      dydx-delivery/skills/platform-ziflow/SKILL.md; do
        grep -qF 'pipefy | wrike | ziflow | other' "$f" || grep -qF 'pipefy | wrike | ziflow' "$f" || echo "  ANCHOR FAIL: $f drifted from 4-enum";
    done
    ```
    (The `||` second-grep handles `platform-ziflow/SKILL.md` which carries the routing-key claim as prose, NOT a literal 4-enum frontmatter line — adapt the grep per file if needed at execute time.)
  </action>
  <verify>
    <automated>for f in dydx-delivery/skills/discovery-intake/SKILL.md dydx-delivery/skills/discovery-intake/references/intake-template.md dydx-delivery/skills/generate-build-prompt/SKILL.md dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md dydx-delivery/skills/generate-technical-spec/SKILL.md dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md dydx-delivery/skills/generate-test-plan/SKILL.md dydx-delivery/skills/generate-test-plan/references/test-plan-template.md dydx-delivery/skills/execute-tests/references/results-template.md; do grep -qF 'pipefy | wrike | ziflow | other' "$f" || { echo "FAIL: $f"; exit 1; }; done && grep -qF 'pipefy | wrike | ziflow' dydx-delivery/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All 9 YAML-enum files now carry `pipefy | wrike | ziflow | other` literal.
    - `dydx-delivery/README.md` now carries `pipefy | wrike | ziflow` (prose form).
    - No file in the touch-list still carries the bare 3-enum `pipefy | wrike | other`.
    - 5 anchor files unchanged from Phase 7 state (verify via `git diff` — only the 10 EDIT files in the touch-list should show changes; anchors unchanged).
    - `bash phase8-structure-check.sh --section route 2>&1 | grep 'PASS E1'` succeeds.
  </acceptance_criteria>
  <done>D-78 enum rolled out across 10 files (11 line edits). ROUTE-04 main payload landed. E1 assertion passes; E2-E6 still pending downstream tasks.</done>
</task>

<task id="08-03-T3" type="auto">
  <name>Task 3: Glossary updates — routing-key entry 4-enum + R-01 carry-forward `discovery-via` removal on BOTH line 47 (based_on_discovery) AND line 66 (kickoff_branch) + DESIGN-20 sub-decision note</name>
  <files>dydx-delivery/references/glossary.md, .planning/DESIGN.md</files>
  <read_first>
    - `dydx-delivery/references/glossary.md` lines 40-80 (covers BOTH occurrences of the stale `discovery-via` spelling: line 47 `**based_on_discovery**` entry's `(discovery-via branch)` parenthetical AND line 66 `**kickoff_branch**` entry's enum values; ALSO covers existing `**platform**` entry around line 71 used by E3 anchor grep)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §6 R-01 (Phase 7 R-02 carry-forward fix — verbatim outlier-spelling description)
    - `.planning/DESIGN.md` §DESIGN-20 section (current text — locate insertion point for sub-decision note)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-PLAN.md` §T1 (Phase 7 surgical-edit precedent for glossary alignment)
  </read_first>
  <action>
    FOUR surgical edits in this task: (a) glossary routing-key entry update, (b1) glossary line 47 `**based_on_discovery**` parenthetical fix, (b2) glossary line 66 `**kickoff_branch**` enum-value fix, (c) DESIGN.md DESIGN-20 sub-decision note.

    **(a) Glossary routing-key entry — D-78 + ROUTE-04 — UPDATE.**
    Current glossary line 71 (`**platform**` entry) already carries the 4-enum `(pipefy | wrike | ziflow | other)` per RESEARCH §2.3 anchor A4. But ROUTE-04 also requires a ROUTING-KEY entry (separate from the `**platform**` entry) that enumerates the 4 values with one-line per-platform definitions. Locate the existing routing-key entry in glossary.md (search for `routing-key` or `routing key` literal); if it does NOT exist, ADD one in the appropriate alphabetical position; if it exists, UPDATE it to:
    ```markdown
    - **routing-key (`platform:`)** — `pipefy | wrike | ziflow | other`. The `platform:` frontmatter value drives stage-skill routing across Stages 2/3/4a/4b/5/6/7b/10. One-line per platform:
      - **pipefy** — Pipefy GraphQL platform; primary workflow-tool platform tier 1.
      - **wrike** — Wrike work-management platform; primary workflow-tool platform tier 1.
      - **ziflow** — Ziflow proof-review platform; primary proof-review platform tier 1 (D-78 path (a) — added 2026-05-11 in Phase 8).
      - **other** — Fallback for client-supplied platforms not in the v2.x platform-skill catalogue.
    ```
    Insert in the appropriate place in the glossary (alphabetical or near the `**platform**` entry). Preserve surrounding glossary entries untouched.

    **(b1) Glossary line 47 `**based_on_discovery**` entry — R-01 carry-forward fix part 1 — UPDATE.**
    Current glossary line 47 contains the stale spelling `discovery-via` inside a parenthetical. Verbatim current content (verified 2026-05-11):
    ```
    **based_on_discovery** — Frontmatter field on `03_sow_*` artefacts (discovery-via branch) pointing at the upstream `02_discovery_v<N>.md` per DESIGN-19.
    ```
    The `(discovery-via branch)` parenthetical is the stale spelling — `**based_on_discovery**` is unambiguous WITHOUT the parenthetical (the entry name itself names the discovery linkage). Remove the parenthetical entirely.

    BEFORE: `**based_on_discovery** — Frontmatter field on `03_sow_*` artefacts (discovery-via branch) pointing at the upstream `02_discovery_v<N>.md` per DESIGN-19.`
    AFTER: `**based_on_discovery** — Frontmatter field on `03_sow_*` artefacts pointing at the upstream `02_discovery_v<N>.md` per DESIGN-19.`

    Single-line surgical edit. Drop the ` (discovery-via branch)` substring (leading space + parenthetical) verbatim.

    **(b2) Glossary line 66 `**kickoff_branch**` entry — R-01 carry-forward fix part 2 — UPDATE.**
    Current glossary line 66 STILL carries the stale spelling `kickoff-direct | discovery-via` per RESEARCH §6 R-01. Verbatim current content (verified 2026-05-11):
    ```
    **kickoff_branch** — Routing key on `01_kickoff_v<N>.md` selecting the downstream path: `kickoff-direct` (skip Stage 2; feed Stage 3 directly) or `discovery-via` (feed Stage 2 first) per DESIGN-17 / DESIGN-19.
    ```
    Edit to the authoritative spelling per Phase 7 v2.2 lock + REQUIREMENTS.md STG1-02 + ROADMAP Phase 7 success criterion #1:

    BEFORE: `**kickoff_branch** — Routing key on `01_kickoff_v<N>.md` selecting the downstream path: `kickoff-direct` (skip Stage 2; feed Stage 3 directly) or `discovery-via` (feed Stage 2 first) per DESIGN-17 / DESIGN-19.`
    AFTER: `**kickoff_branch** — Enum on `01_kickoff_v<N>.md` frontmatter. `discovery-ready` (Stage 2 runs against this kickoff) | `draft-sow` (Stage 2 SKIPPED; Stage 3 reads kickoff directly via `based_on_kickoff:`). Lock: Phase 7 STG1-02 / DESIGN-17 line 562.`

    Surgical 1-2 line edit. Preserve glossary structure.

    **(b) Whole-file post-edit sanity grep — CRITICAL.** After edits (b1) AND (b2) are applied, run the E4 whole-file assertion locally to catch any third occurrence of the stale spellings:
    ```bash
    ! grep -qF 'discovery-via' dydx-delivery/references/glossary.md \
      && ! grep -qF 'kickoff-direct' dydx-delivery/references/glossary.md \
      && echo "  PASS local: no stale spellings remain in glossary.md" \
      || { echo "  FAIL local: stale spelling still present — re-grep with line numbers"; grep -nF 'discovery-via\|kickoff-direct' dydx-delivery/references/glossary.md; }
    ```
    If the local grep finds a third occurrence, executor inspects + fixes that line too before proceeding. The E4 assertion at T-gate is whole-file, NOT line-anchored — every occurrence MUST go.

    **(c) DESIGN.md DESIGN-20 sub-decision note — D-78 outcome capture.**
    Per CONTEXT.md §decisions D-78 + RESEARCH §6 R-05: the D-78 TD-2 resolution outcome must be captured as a sub-decision under DESIGN-20 (Stage 4 split contract). Locate DESIGN-20 in `.planning/DESIGN.md`; at the END of the DESIGN-20 section body (before the next DESIGN-NN section starts), add a sub-decision paragraph:

    ```markdown
    **Sub-decision (Phase 8 / 2026-05-11) — D-78 TD-2 resolution.** The stage-skill `platform:` enum is finalised as `pipefy | wrike | ziflow | other` (path (a) per CONTEXT.md D-78). Ziflow is wired through as a routing-key value across all stage-skill consumers per the 11-file ROUTE-04 rollout in 08-03-PLAN.md. `platform-ziflow/SKILL.md:14` retains its routing-key claim. Path (b) — documenting Ziflow as integration-only — was rejected because it would preclude Ziflow-primary engagements without a structural reason. See `dydx-delivery/references/glossary.md` routing-key entry for the canonical 4-enum definition.
    ```

    Surgical insertion — do NOT rewrite the DESIGN-20 body.

    **Self-verify** with `--section route 2>&1 | grep -E 'PASS E3|PASS E4'`. Expected: PASS E3 (glossary routing-key entry 4-enum) + PASS E4 (whole-file `discovery-via` AND `kickoff-direct` both absent; `discovery-ready` AND `draft-sow` both present).
  </action>
  <verify>
    <automated>grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/references/glossary.md && grep -qF 'discovery-ready' dydx-delivery/references/glossary.md && grep -qF 'draft-sow' dydx-delivery/references/glossary.md && ! grep -qF 'kickoff-direct' dydx-delivery/references/glossary.md && ! grep -qF 'discovery-via' dydx-delivery/references/glossary.md && grep -qF 'D-78' .planning/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/references/glossary.md` succeeds (routing-key entry 4-enum).
    - `grep -qF 'discovery-ready' glossary.md` AND `grep -qF 'draft-sow' glossary.md` succeed (R-01 carry-forward — authoritative spelling on line 66).
    - `! grep -qF 'kickoff-direct' glossary.md` (whole-file absence — line 66 fixed).
    - `! grep -qF 'discovery-via' glossary.md` (whole-file absence — BOTH line 47 `(discovery-via branch)` parenthetical AND line 66 enum value cleaned).
    - `grep -qF '**based_on_discovery** — Frontmatter field on \`03_sow_*\` artefacts pointing' glossary.md` succeeds (or analogous post-edit form of line 47 without the parenthetical).
    - DESIGN.md contains the D-78 sub-decision text (anchor: `grep -qF 'D-78 TD-2 resolution' .planning/DESIGN.md` succeeds; or `grep -qF 'ziflow' .planning/DESIGN.md` returns at least the new occurrence).
    - `bash phase8-structure-check.sh --section route 2>&1 | grep -cE 'PASS E[34]'` returns 2 (E3 + E4 both PASS).
  </acceptance_criteria>
  <done>Glossary routing-key entry + `**based_on_discovery**` line 47 + `**kickoff_branch**` line 66 all aligned with authoritative spellings; DESIGN-20 sub-decision note recorded. Whole-file `discovery-via` AND `kickoff-direct` substrings absent. E3 + E4 PASS.</done>
</task>

<task id="08-03-T4" type="auto">
  <name>Task 4: STG4-03 retirement — delete generate-functional-spec/ directory + STG4-06 three-topology cross-reference + grep README.md for legacy references</name>
  <files>dydx-delivery/skills/generate-functional-spec/SKILL.md, dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md, dydx-delivery/README.md, dydx-delivery/skills/generate-fnspec-platform/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-functional-spec/SKILL.md` (113 lines — record the retiring-skill body shape in 08-03-SUMMARY for posterity before deletion)
    - `dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md` (165 lines — record analogous for posterity)
    - `dydx-delivery/README.md` (full — grep for `generate-functional-spec` references and Stage 4 pipeline diagram entries per RESEARCH §6 R-04)
    - `dydx-delivery/skills/generate-fnspec-platform/SKILL.md` (08-01 product — must reference `either-spec-skip-paths.md` to satisfy E6 STG4-06 cross-reference; check whether 08-01 T1 already included this reference)
  </read_first>
  <action>
    Four sub-tasks: (a) grep README.md for legacy `generate-functional-spec` references and update to `generate-fnspec-platform` / `generate-fnspec-integration`; (b) delete the retiring skill directory; (c) ensure 4a SKILL.md cross-references 4b's `either-spec-skip-paths.md` to satisfy E6; (d) self-verify.

    **(a) README.md sweep.** Run:
    ```bash
    grep -nF 'generate-functional-spec' dydx-delivery/README.md
    ```
    For each match: replace `generate-functional-spec` references with the 4a + 4b split as appropriate. Common patterns to expect (per RESEARCH §6 R-04):
    - Pipeline diagram entry — replace single `generate-functional-spec` with `generate-fnspec-platform (4a) + generate-fnspec-integration (4b)`.
    - Skill table — replace the row entry with two rows (4a and 4b).
    - Stage 4 description prose — replace with the split contract per DESIGN-20.

    Also grep the rest of `dydx-delivery/` (excluding the soon-to-be-deleted `generate-functional-spec/` directory):
    ```bash
    grep -rln 'generate-functional-spec' dydx-delivery/ --exclude-dir=generate-functional-spec
    ```
    Expected results: README.md (handled above) + possibly `generate-technical-spec/SKILL.md` (v0.3.0 read path — if the techspec skill mentions reading `04_functional-spec_v*.md` per DESIGN-08 lenient mode, that mention may need updating to also mention 4a/4b artefacts). Leave DESIGN-08 lenient-mode references intact (they describe what the techspec READS from disk; legacy filenames remain readable).

    For each non-README file with a hit, planner inspects the context at execute time and decides per-file whether the reference is (i) a legitimate DESIGN-08 lenient-read reference (leave alone) or (ii) a stale pipeline reference (update to 4a/4b split).

    **(b) Directory delete.**
    ```bash
    rm -rf dydx-delivery/skills/generate-functional-spec/
    ```
    Verify:
    ```bash
    [ ! -d dydx-delivery/skills/generate-functional-spec ] && echo "PASS: directory removed"
    ```
    Plus record in 08-03-SUMMARY.md (T-summary task) the file inventory pre-delete: 2 files total (`SKILL.md` 113 lines + `references/functional-spec-template.md` 165 lines).

    **(c) STG4-06 cross-reference in 4a SKILL.md.**
    Check whether 08-01 T1's authored 4a SKILL.md already cross-references the 4b skip-paths file:
    ```bash
    grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md \
      && echo "PASS: 4a SKILL.md already references 4b skip-paths file" \
      || echo "  NEEDS EDIT: add cross-reference in 4a SKILL.md"
    ```

    If MISSING (08-01 T1 did not include the cross-reference): add a single sentence in the 4a SKILL.md `## Key decisions` block D-85 entry, pointing at the 4b file:
    > See `dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md` for the three-topology rules (4a-only / 4b-only / both) and the verbatim D-85 skip-emit string.

    This makes assertion E6 reachable from BOTH 4a + 4b SKILL.md bodies via grep against the relative reference path `either-spec-skip-paths.md`.

    If ALREADY PRESENT (08-01 T1 included it): no edit needed.

    **(d) Self-verify** with `--section route 2>&1 | grep -cE 'PASS E[56]'`. Expected: 2 (E5 + E6 PASS).
  </action>
  <verify>
    <automated>[ ! -d dydx-delivery/skills/generate-functional-spec ] && ! grep -rl 'generate-functional-spec' dydx-delivery/README.md | grep -v 'generate-functional-spec/' && grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-integration/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `[ ! -d dydx-delivery/skills/generate-functional-spec ]` — directory removed (E5 anchor).
    - `dydx-delivery/README.md` does NOT carry stale `generate-functional-spec` references at the pipeline-diagram + skill-table positions (manual inspection at execute time; legitimate DESIGN-08 lenient-mode references in other files may remain).
    - `grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md` succeeds (4a half of E6).
    - `grep -qF 'either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-integration/SKILL.md` succeeds (4b half of E6 — already authored in 08-02 T1).
    - `bash phase8-structure-check.sh --section route 2>&1 | grep -cE 'PASS E[1-6]'` returns 6 (all 6 E-assertions PASS after T2+T3+T4).
  </acceptance_criteria>
  <done>v0.3.0 generate-functional-spec/ directory retired; README.md updated to 4a/4b split; 4a SKILL.md cross-references 4b skip-paths file. E5 + E6 PASS. `--section route` now fully green (E1-E6 all PASS).</done>
</task>

<task id="08-03-T5" type="auto">
  <name>Task 5: Author 3 fixture-output artefacts for the smoke section (4a + 4b clean + 4b consistency-check halt-path)</name>
  <files>.planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04a_fnspec-platform_v1.md, .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_fnspec-integration_v1.md, .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_consistency_check_v1.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md` (08-01 product — template for 04a fixture content)
    - `dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md` (08-01 product — body skeleton if the 4a fixture includes the addendum H2 variant)
    - `dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md` (08-02 product — template for 04b clean fixture)
    - `dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md` (08-02 product — failure-row template for halt-path fixture)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/` (Phase 7 sample-CR inputs — optionally reuse as upstream for narrative grounding per RESEARCH §6 R-02; can reference paths instead of copying content)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-SUMMARY.md` (Phase 7 fixture pattern — `fixture: true` + `fixture_for:` frontmatter convention)
  </read_first>
  <action>
    Author 3 fixture-output artefacts under `.planning/phases/08-stage-4-fnspec-split-route/fixtures/output/`. These fixtures serve a dual purpose: (a) smoke-section S1-S6 input (the synthetic consumer stubs read these), and (b) manual e2e smoke record per Phase 7 07-04 pattern (T-smoke captures these as evidence).

    **Create directory first:**
    ```bash
    mkdir -p .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/
    ```

    **Fixture 1 — `sample-cr-04a_fnspec-platform_v1.md` (~80-120 lines).**
    Synthetic sample-CR scenario (e.g., VodafoneZiggo Pipefy ticket-routing phase add, per D-65 client list). Author per `fnspec-platform-template.md`:
    - Frontmatter: `client: VodafoneZiggo` (or `Up & Up Group` per D-65 alternative), `project: <plausible CR name>`, `frontmatter_version: 2`, `platform: pipefy`, `based_on_discovery: 02_discovery_v1.md`, `based_on_sow: 03_sow_v1.md`, `status: approved`, `approved_by: planner@dydx-delivery`, `approved_at: 2026-05-11`, `generated_at: 2026-05-11`. Add `fixture: true` + `fixture_for: phase-8-e2e-smoke` per Phase 7 fixture convention.
    - Body: 10 H2 sections per template; sections 4 (Business rules) / 5 (Field reqs) / 8 (Acceptance criteria) populated with at least 5 rows total carrying D-82 `delivery:` markup. MUST include a MIX of `delivery: native-ai [HIGH, ...]` + `delivery: native-ai [MEDIUM, ...]` + `delivery: api [LOW → default api, ...]` + at least one `delivery: api [reviewer-override: api]` (D-82 preservation pattern). NEVER reversed enum order.
    - Optional addendum: include the `## Platform-API Addendum` H2 body (one or two rows in API surface inventory) AND set `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only` in frontmatter. This exercises D-79 + ROUTE-03 on a topology variant. **NOTE:** the smoke stubs do not require the addendum to be present — they just iterate `delivery:` rows. Adding it tests S6 (chain end-to-end) without making the fixture fragile.

    **Fixture 2 — `sample-cr-04b_fnspec-integration_v1.md` (~80-120 lines).**
    Same sample-CR scenario as Fixture 1, integration-side. Author per `fnspec-integration-template.md`:
    - Frontmatter: same client/project; `platform: pipefy`; `based_on_discovery: 02_discovery_v1.md`; `based_on_sow: 03_sow_v1.md`; `based_on_fnspec_platform: 04a_fnspec-platform_v1.md` (chain link to Fixture 1 — critical for S6); `status: approved`; approved_by/at; `fixture: true` + `fixture_for: phase-8-e2e-smoke`.
    - Body: 10 H2 sections; section 4 (Integration touchpoints) populated with at least 3 rows; section 5a (API endpoints) with at least 2 rows; section 8 (AC) with at least 2 rows. Every row carries D-82 markup. `Cites 4a req` column MUST cite a req ID that EXISTS in Fixture 1 (ROUTE-01 check (b) compliance — the fixture is a clean-check pass, not a halt-path).

    **Fixture 3 — `sample-cr-04b_consistency_check_v1.md` (~30-50 lines).**
    Halt-path artefact showing what the failure-report looks like. Author per `consistency-rules.md` failure-report template:
    - Frontmatter: `client: <same>`, `project: <same>`, `frontmatter_version: 2`, `artefact_type: consistency_check_failure`, `based_on_fnspec_platform: 04a_fnspec-platform_v1.md`, `based_on_attempted_fnspec_integration: N/A-pre-write`, `checks_run: 3`, `checks_failed: 1`, `status: halt`, `generated_at: 2026-05-11`, `fixture: true`, `fixture_for: phase-8-e2e-smoke`.
    - Body: failure-row table with ONE synthetic conflict — e.g., Check (a) Conflicting delivery tags: 4a row REQ-3 carries `delivery: native-ai [HIGH, src: platform-pipefy]`; an attempted 4b row REQ-3 carries `delivery: api [reviewer-override: api]`. Suggested resolution: reviewer decides authoritative routing + aligns both rows.
    - The fixture demonstrates the halt-path AS DOCUMENTED — does NOT exercise the runtime skill (this is a documentation fixture, not a live test).

    **Self-verify** with `--section smoke`. Expected: S1-S6 all PASS now that the fixtures exist.
    ```bash
    bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --section smoke
    ```
    Expected stdout: 6 `PASS:` lines (S1-S6) + `ALL ASSERTIONS PASSED`.

    **Discretion:**
    - Planner may choose a different sample-CR scenario (e.g., a Wrike or Ziflow primary platform variant); only requirement is the fixture must be plausible and exercise the D-82 markup canonically.
    - Whether to include the addendum on Fixture 1 — planner's call; recommended YES to exercise S6 chain check.
    - Fixture sizes can vary ±20 lines; templates ARE the contract, not exact line counts.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04a_fnspec-platform_v1.md && test -f .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_fnspec-integration_v1.md && test -f .planning/phases/08-stage-4-fnspec-split-route/fixtures/output/sample-cr-04b_consistency_check_v1.md && bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --section smoke
</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixture files exist at the paths in `files_created`.
    - `grep -qF 'fixture: true' sample-cr-04a_fnspec-platform_v1.md` succeeds (Phase 7 fixture convention).
    - `grep -qF 'delivery: native-ai' sample-cr-04a_fnspec-platform_v1.md` succeeds (D-82 markup present).
    - `grep -qF 'delivery: api' sample-cr-04a_fnspec-platform_v1.md` succeeds.
    - `grep -qF 'based_on_fnspec_platform: 04a_fnspec-platform_v1.md' sample-cr-04b_fnspec-integration_v1.md` succeeds (chain link for S6).
    - `grep -qF 'artefact_type: consistency_check_failure' sample-cr-04b_consistency_check_v1.md` succeeds.
    - `grep -qF 'status: halt' sample-cr-04b_consistency_check_v1.md` succeeds.
    - `bash phase8-structure-check.sh --section smoke` exits 0 with 6 PASS lines (S1-S6).
  </acceptance_criteria>
  <done>3 fixture-output artefacts authored; S1-S6 smoke section passes. Forward-compat smoke (ROUTE-05 + D-86) verified.</done>
</task>

<task id="08-03-T-smoke" type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 (T-smoke): Manual e2e smoke against sample-CR upstream (4a clean run + 4b clean run + 4b halt run + 4b skip run) — capture frontmatter snippets + skip-message + halt-artefact path in 08-03-SUMMARY.md</name>
  <what-built>
    Phase 8 ships 2 NEW skills (generate-fnspec-platform 4a + generate-fnspec-integration 4b) + 1 RETIRED skill (generate-functional-spec) + 1 structure-check script + 3 fixture-output artefacts. The structure-check harness validates static structural properties; it does NOT exercise the skills end-to-end. This checkpoint runs the skills against sample-CR upstream (reused from Phase 7 fixtures OR authored fresh) to validate actual pipeline behaviour.

    **Lift Phase 7 07-04-T-smoke pattern verbatim — strengthened acceptance per cross-AI review C4:** concrete invocation commands per skill+fixture, concrete artefact output paths, frontmatter-field snippet captures, explicit workspace path convention.
  </what-built>
  <how-to-verify>
    **Workspace path convention:** all e2e smoke artefacts are written to `.planning/phases/08-stage-4-fnspec-split-route/fixtures/output/`. This directory already exists from T5 (Fixture 1-3 live here).

    **Upstream-input convention (per R-02):** reuse Phase 7's `fixtures/output/` (containing approved `01_kickoff_v*`, `02_discovery_v*`, `03_sow_v*` from 07-04 e2e smoke) as Phase 8's upstream. If Phase 7 outputs lack the `## Platform Scope` + `## Integration Scope` H2 content needed to exercise the three topologies, reviewer authors minimal additional fixture-input artefacts under `.planning/phases/08-stage-4-fnspec-split-route/fixtures/input/`.

    Reviewer executes the smoke sequence below. **Each step lists invocation command, expected artefact path, and frontmatter fields to capture in 08-03-SUMMARY.md.**

    **Step 1 — 4a clean run (typical case — both topology, no addendum)**
    - Invocation: invoke `generate-fnspec-platform` against an upstream `02_discovery_v*.md` + `03_sow_v*.md` pair where `## Integration Scope` H2 has content (both topology).
    - Expected output path: `.planning/phases/08-stage-4-fnspec-split-route/fixtures/output/04a_fnspec-platform_v1.md` (or `_v2.md` if T5's `_v1.md` exists; reviewer chooses version suffix to avoid overwrite — alternatively rename T5's fixture before run).
    - Capture for SUMMARY: (a) `platform:` value (expected: `pipefy` per default), (b) `based_on_discovery:` + `based_on_sow:` chain values, (c) `has_platform_api_addendum:` value (expected: `false` on the both topology), (d) one example `delivery:` row from sec 4 / 5 / 8 demonstrating canonical markup.

    **Step 2 — 4a clean run (4a-only topology with API rows — addendum variant)**
    - Invocation: invoke `generate-fnspec-platform` against an upstream where `## Integration Scope` H2 is empty / "n/a" AND the platform scope has rows that classify to `delivery: api`.
    - Expected output: a fresh 4a artefact carrying `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only` AND a `## Platform-API Addendum` H2 body inline.
    - Capture for SUMMARY: (a) `has_platform_api_addendum: true` line verbatim, (b) `tech_spec_scope: platform-api-addendum-only` line verbatim, (c) the H2 anchor `## Platform-API Addendum` literal, (d) one example row from the API surface inventory table demonstrating D-82 markup on an api row.

    **Step 3 — 4b clean run (passes consistency-check)**
    - Invocation: invoke `generate-fnspec-integration` against the Step 1 4a artefact + the same discovery + SOW. SOW Integration Scope has content; 4b inputs are coherent with 4a (no synthetic conflict).
    - Expected behaviour: consistency check passes (all 3 checks green); `04b_fnspec-integration_v1.md` written to fixtures/output/.
    - Capture for SUMMARY: (a) `based_on_fnspec_platform:` value (chain link to Step 1 4a), (b) `04b_consistency_check_v*.md` ABSENT (clean check leaves no audit footprint per D-84), (c) one example `delivery:` row from integration touchpoints or API endpoints table.

    **Step 4 — 4b halt run (injected conflict, halt-path)**
    - Invocation: invoke `generate-fnspec-integration` against the same upstream BUT inject a synthetic conflict — e.g., edit the 4a copy in upstream to carry `delivery: native-ai [HIGH, src: platform-pipefy]` on REQ-3, then prepare a 4b draft (or use the skill's interactive prompt) to attempt `delivery: api` on the same REQ-3.
    - Expected behaviour: consistency check (a) fails; `04b_consistency_check_v<N>.md` written; HALT before any `04b_fnspec-integration_v*.md` write.
    - Capture for SUMMARY: (a) `04b_consistency_check_v<N>.md` path written, (b) `artefact_type: consistency_check_failure` line verbatim, (c) `status: halt` line verbatim, (d) the failure-row table excerpt showing the synthetic conflict.
    - Cleanup: revert the synthetic conflict in the 4a copy after this step OR keep the halt-path artefact alongside the clean Step 3 output as evidence.

    **Step 5 — 4b skip run (either-spec-skip topology — D-85)**
    - Invocation: invoke `generate-fnspec-integration` against an upstream where SOW `## Integration Scope` H2 is empty / "n/a" OR no 4a exists OR explicit reviewer signal "integration out of scope".
    - Expected behaviour: skill emits the verbatim string `Stage 4b SKIPPED — no integration work in scope` to stdout AND writes NO `04b_*` artefact.
    - Capture for SUMMARY: (a) the exact stdout line containing the verbatim skip-emit string (em-dash unicode verbatim), (b) confirmation via `ls fixtures/output/04b_*` that no NEW 4b file was produced on this run (rename or remove Step 3 + Step 4 artefacts before running Step 5 to isolate, or run in a separate `fixtures/output-step5/` subdirectory).

    **What must be recorded in 08-03-SUMMARY.md (per Phase 7 C4 pattern):**
    1. The 5 actual artefact paths produced (Step 1, 2, 3 produce 4a + 4a-with-addendum + 4b; Step 4 produces consistency_check; Step 5 produces nothing) — record paths + per-path file size.
    2. Frontmatter-field captures listed under each step above.
    3. Verbatim skip-emit string observed in Step 5 stdout (must match `Stage 4b SKIPPED — no integration work in scope` exactly).
    4. Verbatim halt-artefact frontmatter from Step 4 (artefact_type + status + checks_failed values).
    5. Confirmation that Step 1 / Step 2 / Step 3 artefacts all carry D-82 markup canonical enum order (no reversed `api | native-ai` anywhere).
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark the e2e smoke pass, or describe any failures (which step, which fixture, what was wrong) for a follow-up fix task.

    The acceptance gate is that `08-03-SUMMARY.md` MUST exist by the end of this plan AND contain all 5 artefact path captures (or absence confirmations) AND all frontmatter-field snippets listed above.
  </resume-signal>
  <acceptance_criteria>
    - Reviewer types "approved" or equivalent confirmation.
    - All 4 invoked-runs (Steps 1, 2, 3, 4) produce expected artefacts; Step 5 produces no artefact (skip path).
    - Verbatim skip-emit string `Stage 4b SKIPPED — no integration work in scope` observed exactly on Step 5 stdout.
    - Step 4 produces `04b_consistency_check_v<N>.md` AND does NOT produce a `04b_fnspec-integration_v*.md` on the halt branch.
    - All artefacts in Step 1/2/3 carry canonical `delivery: native-ai | api` enum order; no reversed enum.
    - 08-03-SUMMARY.md exists AND contains 5 step captures + verbatim strings + frontmatter snippets.
  </acceptance_criteria>
  <done>Manual e2e smoke passes with C4-strengthened acceptance — concrete paths + frontmatter snippets + skip-message verbatim + halt-artefact frontmatter all captured in 08-03-SUMMARY.md. Phase 8 e2e behaviour validated end-to-end against real upstream.</done>
</task>

<task id="08-03-T-gate" type="auto">
  <name>Task 7 (T-gate): Final phase gate — bash phase8-structure-check.sh --all (P1-P8 + I1-I8 + E1-E6 + S1-S6 + X1-X4 all PASS)</name>
  <files></files>
  <read_first>
    - `.planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh` (shipped in T1)
    - All Phase 8 deliverables on disk (08-01 + 08-02 outputs + 08-03 T1-T5 outputs + T-smoke outputs)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-PLAN.md` T-gate (C5 ordering precedent — `--all` PASS BEFORE trace flips)
  </read_first>
  <action>
    Run the full Phase 8 structure-check gate. This is the phase-level final gate — all 5 runners (4a / 4b / route / smoke / cross) must emit PASS for every assertion.

    **C5 ordering (Phase 7 precedent):** This task MUST exit 0 BEFORE the next task (T-flips) updates REQUIREMENTS.md traceability + ROADMAP.md. If `--all` fails, trace + roadmap flips DO NOT happen. The plan pauses, the failing source file is fixed, `--all` is re-run, and only on exit 0 does the executor proceed to T-flips.

    Command:
    ```bash
    bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --all
    ```
    (or no flag — same behaviour per dispatch case.)

    Expected output: every P1-P8 + I1-I8 + E1-E6 + S1-S6 + X1-X4 assertion emits `PASS:` followed by `ALL ASSERTIONS PASSED` and exit 0. Expected PASS-line count: 8 + 8 + 6 + 6 + 4 = 32.

    If ANY assertion fails:
    - P-series fail → 08-01 deliverable issue; check generate-fnspec-platform/ files.
    - I-series fail → 08-02 deliverable issue; check generate-fnspec-integration/ files.
    - I8 fail → cross-plan two-place declaration regression; check BOTH 4a + 4b SKILL.md key-decisions sections.
    - E1 fail → D-78 enum rollout T2 issue; check the 10-file touch-list edits.
    - E2 fail → platform-ziflow/SKILL.md routing-key claim drifted (the literal `is the routing key` no longer appears on line 14); restore the line per D-78 path (a).
    - E3 fail → glossary routing-key entry edit regression; check T3 (a) edit.
    - E4 fail → glossary STILL carries `discovery-via` OR `kickoff-direct` substring somewhere; check T3 (b1) line 47 `**based_on_discovery**` parenthetical fix AND T3 (b2) line 66 `**kickoff_branch**` enum-value fix — BOTH must be applied or whole-file E4 grep fails.
    - E5 fail → generate-functional-spec/ directory still exists; check T4 (b) deletion.
    - E6 fail → 4a or 4b SKILL.md missing `either-spec-skip-paths.md` cross-reference; check T4 (c).
    - S-series fail → fixture-output artefact issue; check T5 (rebuild fixtures if needed).
    - X-series fail → cross-skill consistency regression; trace to specific cross-section assertion.

    Executor fixes failing source file (NOT the script — the script is the contract) then re-runs `--all` until exit 0.
  </action>
  <verify>
    <automated>bash .planning/phases/08-stage-4-fnspec-split-route/scripts/phase8-structure-check.sh --all</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0.
    - stdout contains `ALL ASSERTIONS PASSED`.
    - stdout contains 32 `PASS:` lines covering P1-P8 + I1-I8 + E1-E6 + S1-S6 + X1-X4 (or close to 32 — exact count depends on planner's S* assertion granularity).
    - This task MUST complete with exit 0 BEFORE T-flips runs.
  </acceptance_criteria>
  <done>`--all` exits 0 with ALL ASSERTIONS PASSED. All Phase 8 structural requirements verified at the assertion level. T-flips unblocked.</done>
</task>

<task id="08-03-T-flips" type="auto">
  <name>Task 8 (T-flips): REQUIREMENTS.md + ROADMAP.md trace flips — all 11 Phase 8 rows Pending → Satisfied + Phase 8 checkbox + plan list + Progress table row (post-T-gate per C5 ordering)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` § Traceability table (lines 118-145; identify all 11 Phase 8 rows)
    - `.planning/ROADMAP.md` Phase 8 entry + plan list + Progress table row
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-PLAN.md` T-flips (Phase 7 precedent for this exact task shape — 10-row flip)
    - `.planning/milestones/v2.1-RETROSPECTIVE.md` (auto-flip REQUIREMENTS pattern locked in v2.1)
  </read_first>
  <action>
    Surgical edits to TWO files — `.planning/REQUIREMENTS.md` § Traceability table (11 row flips + checkbox flips) AND `.planning/ROADMAP.md` (Phase 8 close flips: 1 phase checkbox + 3 plan checkboxes + Progress table row update). Per C5 ordering, this task runs ONLY after T-gate exits 0; if T-gate did NOT exit 0, this task DOES NOT execute and the plan is paused.

    **Edit 1 — REQUIREMENTS.md Traceability rows (per REQUIREMENTS.md lines 130-140):**
    | Requirement | Phase | BEFORE | AFTER |
    |---|---|---|---|
    | STG4-01 | Phase 8 | Pending | Satisfied |
    | STG4-02 | Phase 8 | Pending | Satisfied |
    | STG4-03 | Phase 8 | Pending | Satisfied |
    | STG4-04 | Phase 8 | Pending | Satisfied |
    | STG4-05 | Phase 8 | Pending | Satisfied |
    | STG4-06 | Phase 8 | Pending | Satisfied |
    | ROUTE-01 | Phase 8 | Pending | Satisfied |
    | ROUTE-02 | Phase 8 | Pending | Satisfied |
    | ROUTE-03 | Phase 8 | Pending | Satisfied |
    | ROUTE-04 | Phase 8 | Pending | Satisfied |
    | ROUTE-05 | Phase 8 | Pending | Satisfied |

    **Edit 2 — REQUIREMENTS.md body section checkboxes (lines 51-66):**
    Update each checkbox `- [ ]` to `- [x]` on the corresponding REQUIREMENTS.md `### Stage 4 Fnspec Split (STG4)` + `### Routing key + Cross-spec consistency check + TD-2 (ROUTE)` sections — same pattern as Phase 7 retrospective flipped STG1/STG2/STG3 rows.

    **Edit 3 — ROADMAP.md Phase 8 entry + plan list + Progress table** (declared in `files_modified` frontmatter):
    - Phase 8 entry: `- [ ] **Phase 8: Stage 4 Fnspec Split + ROUTE...**` → `- [x] **Phase 8: Stage 4 Fnspec Split + ROUTE...**`
    - Plan list (under Phase 8 details):
      - `- [ ] 08-01-PLAN.md` → `- [x] 08-01-PLAN.md`
      - `- [ ] 08-02-PLAN.md` → `- [x] 08-02-PLAN.md`
      - `- [ ] 08-03-PLAN.md` → `- [x] 08-03-PLAN.md`
    - Progress table row for Phase 8: status `Plans authored` → `Complete ✓ SHIPPED`; completed-date column populated with 2026-05-11 (or actual execute-time date).

    Surgical 15-16 line edits total across 2 files (.planning/REQUIREMENTS.md + .planning/ROADMAP.md). Preserve all surrounding content untouched.
  </action>
  <verify>
    <automated>grep -c '| STG4-0[1-6] | Phase 8 | Satisfied |' .planning/REQUIREMENTS.md | grep -qE '^6$' && grep -c '| ROUTE-0[1-5] | Phase 8 | Satisfied |' .planning/REQUIREMENTS.md | grep -qE '^5$' && ! grep -qE '\| (STG4-0[1-6]|ROUTE-0[1-5]) \| Phase 8 \| Pending \|' .planning/REQUIREMENTS.md && grep -qF '- [x] **Phase 8:' .planning/ROADMAP.md && grep -qF '- [x] 08-01-PLAN.md' .planning/ROADMAP.md && grep -qF '- [x] 08-02-PLAN.md' .planning/ROADMAP.md && grep -qF '- [x] 08-03-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All 11 Phase 8 rows in REQUIREMENTS.md § Traceability now show `Satisfied` (6 STG4 + 5 ROUTE).
    - No Phase 8 row still shows `Pending`.
    - All 11 corresponding checkboxes in REQUIREMENTS.md body sections (`### Stage 4 Fnspec Split` + `### Routing key + Cross-spec consistency check + TD-2`) flipped `- [ ]` → `- [x]`.
    - ROADMAP.md Phase 8 entry flipped `- [ ]` → `- [x]`; plan list checkboxes (08-01 / 08-02 / 08-03) all flipped; Progress table row updated to `Complete ✓ SHIPPED` with completed-date populated.
    - `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md` shows minimal-delta surgical edits (no surrounding-prose churn).
  </acceptance_criteria>
  <done>REQUIREMENTS.md trace + ROADMAP.md plan list + Progress table all reflect Phase 8 completion. Phase 8 closeout complete. v2.2 milestone shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer → manual e2e smoke (T-smoke) | Reviewer invokes 4a + 4b skills against synthetic sample-CR fixtures. Inputs are reviewer-authored. Trust posture: low risk (smoke test only). |
| phase8-structure-check.sh → filesystem read | Script reads many `dydx-delivery/` and `.planning/` files via grep/sed/awk. No writes. Trust = internal. |
| fixture-output artefacts → smoke section synthetic stubs | Fixtures are author-locked at T5; future regressions would surface as S1-S6 failures. Trust = internal fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | D-78 enum rollout regression (e.g., a future edit reverts one of the 10 touch-list files to 3-enum) | mitigate | E1 + X1 assertions iterate every D-78 touch-list file and assert 4-enum present. Any single-file revert fails-loud at structure-check `--all`. R-01 carry-forward fix demonstrates this defence works across phases. |
| T-08-12 | Tampering | R-01 (Phase 7 R-02) glossary `discovery-via` / `kickoff-direct` spelling regresses after edit | mitigate | E4 asserts whole-file `! grep -qF 'discovery-via'` AND `! grep -qF 'kickoff-direct'` AND `grep -qF 'discovery-ready'` AND `grep -qF 'draft-sow'` on glossary.md. Re-introducing either stale spelling on ANY line of the file fails E4. This iteration explicitly covers BOTH line 47 (`**based_on_discovery**` parenthetical) AND line 66 (`**kickoff_branch**` enum) — Phase 7 K4 only checked the kickoff-template; Phase 8 E4 closes BOTH glossary gaps via whole-file substring absence. |
| T-08-13 | Tampering | generate-functional-spec/ directory accidentally recreated (e.g., via `git revert` of T4) | mitigate | E5 asserts `[ ! -d ]` on the retiring directory. Any restoration fails-loud. STG4-03 retirement is structural — directory existence is the test. |
| T-08-14 | Tampering | REQUIREMENTS.md trace / ROADMAP.md flipped to Satisfied while `--all` failed | mitigate | C5 ordering enforced: T-flips runs AFTER T-gate exits 0. T-gate failure pauses the plan; T-flips does not execute. Reviewer can verify by checking T-gate exit code in 08-03-SUMMARY.md before accepting the plan close. |
| T-08-15 | Spoofing | Synthetic consumer-stub functions in smoke section produce false positive (i.e., assertion passes but real downstream would fail) | accept | Stubs are deliberately thin per D-86 — they assert "delivery field survives at canonical position", not "consumer behaves correctly". Real v2.3 Stage 5 consumer implementation will surface any consumption-side bugs; v2.2's smoke section's job is forward-compat interface only per ROUTE-05 wording. |
| T-08-16 | Information disclosure | Fixture-output artefacts under `.planning/phases/08-.../fixtures/output/` leak client names | accept | Fixtures use VodafoneZiggo / Up & Up Group per D-65 (approved client names); no real PII / commercial detail. Fixture frontmatter `fixture: true` + `fixture_for: phase-8-e2e-smoke` flags non-production status. |
| T-08-17 | Tampering | E2 grep string drifts to a soft substring match (e.g., `grep -qF 'routing'`) that false-passes on unrelated occurrences | mitigate | E2 grep is locked to the literal `is the routing key` per the routing-key-claim sentence on platform-ziflow/SKILL.md line 14. Acceptance criterion for T1 asserts `grep -qF 'is the routing key' phase8-structure-check.sh` — if the script body contains a softer grep, T1 fails-loud at script-author time. |

ASVS L1 alignment: doc-only edits + bash script authoring + REQUIREMENTS.md / ROADMAP.md trace updates; primary threats are tampering of structural assertions (mitigated via E1-E6 + S1-S6 grep gates with locked literal anchors) and ordering violations (mitigated via T-gate/T-flips sequence per Phase 7 C5).
</threat_model>

<verification>
- **Plan-local (T-gate):** `bash phase8-structure-check.sh --all` exits 0 with ALL ASSERTIONS PASSED.
- **Manual (T-smoke):** Reviewer captures 5 step outcomes + verbatim strings + frontmatter snippets in 08-03-SUMMARY.md.
- **Cross-section X4 meta-assert:** Script itself passes `bash -n` (syntax-clean meta-assert under `--all` only).
- **Trace flips ordering (C5):** T-gate exit 0 is the precondition for T-flips. T-flips does NOT execute if T-gate failed.
- **R-01 carry-forward verification:** E4 explicitly asserts the glossary spelling fix on whole-file basis — verifies cross-phase fix landed across BOTH stale occurrences (line 47 `(discovery-via branch)` parenthetical inside `**based_on_discovery**` AND line 66 `**kickoff_branch**` enum values). Phase 7 attempted the fix in DESIGN.md only via 07-04 T1; Phase 8 T3 (b1) + T3 (b2) close BOTH parallel glossary gaps that Phase 7 K4 missed.
- **E2 literal-anchor verification:** Grep string is the locked literal `is the routing key`, not a soft `routing` substring. T1 acceptance criterion asserts the locked string is present in phase8-structure-check.sh body.
- **Manual:** Spot-check 08-01-SUMMARY.md + 08-02-SUMMARY.md to confirm upstream plans completed cleanly (P1-P8 + I1-I8 PASS recorded) before T-gate runs. If either summary is missing or shows non-PASS state, do NOT run T-gate — re-open the upstream plan instead.
</verification>

<success_criteria>
- 2 files deleted (`generate-functional-spec/SKILL.md` + `functional-spec-template.md`).
- 14 files modified (10 D-78 enum touch-list + glossary + DESIGN.md + REQUIREMENTS.md + ROADMAP.md). All declared in `files_modified` frontmatter.
- 4 files created (1 bash script + 3 fixture-output artefacts).
- `bash phase8-structure-check.sh --all` exits 0.
- 32 PASS lines (8 P + 8 I + 6 E + 6 S + 4 X) — exact count may differ slightly based on planner's S* granularity at script-write time.
- E2 assertion uses the locked literal `is the routing key` (NOT a soft `routing` substring).
- E4 assertion uses whole-file `! grep -qF 'discovery-via'` AND `! grep -qF 'kickoff-direct'` (catches BOTH line 47 `based_on_discovery` parenthetical AND line 66 `kickoff_branch` enum values).
- REQUIREMENTS.md § Traceability table shows all 11 Phase 8 rows Satisfied.
- ROADMAP.md Phase 8 entry checked off + plan-list checkboxes flipped + Progress table row updated.
- DESIGN.md DESIGN-20 carries the D-78 sub-decision note.
- 08-03-SUMMARY.md exists with T-smoke captures + T-gate exit code + T-flips edit summary.
- v2.2 milestone shippable: Phase 7 ✓ + Phase 8 ✓.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stage-4-fnspec-split-route/08-03-SUMMARY.md` recording:
- Files deleted (2) + files modified (14) + files created (4)
- T-gate exit code (0 expected) + PASS-line count (~32 expected: 8 P + 8 I + 6 E + 6 S + 4 X — record actual)
- T-smoke step-by-step captures per the T-smoke checklist (5 artefact paths + frontmatter snippets + verbatim skip-emit string + verbatim halt-artefact frontmatter)
- T-flips git diff summary (REQUIREMENTS.md + ROADMAP.md edits — minimal-delta surgical confirmed)
- D-78 enum touch-list completion: 11 EDIT lines applied + 5 ANCHOR files verified unchanged
- R-01 carry-forward fix landed: glossary line 47 `**based_on_discovery**` parenthetical removed (no `discovery-via` substring anywhere in file) AND glossary line 66 `**kickoff_branch**` entry now reads `discovery-ready | draft-sow` (NOT `kickoff-direct | discovery-via`)
- E2 locked literal-anchor grep `is the routing key` present in phase8-structure-check.sh body (not a soft `routing` substring)
- DESIGN.md DESIGN-20 sub-decision note text recorded (anchor for v2.3 to read)
- Any planner-time decision to split 08-03 to 08-04 (record rationale + new wave structure) — NOT recommended if T-gate ran cleanly
- v2.2 milestone-shippable confirmation

</output>
