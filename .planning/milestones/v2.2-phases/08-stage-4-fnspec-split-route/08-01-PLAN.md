---
plan: 08-01
phase: 8
slug: stage-4-fnspec-split-route
wave: 1
depends_on: []
files_modified: []
files_created:
  - dydx-delivery/skills/generate-fnspec-platform/SKILL.md
  - dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md
  - dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md
  - dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md
files_deleted: []
autonomous: true
requirements: [STG4-01, STG4-04, STG4-05, ROUTE-03]
must_haves:
  truths:
    - "Reviewer can invoke `generate-fnspec-platform` against approved `02_discovery_v*` + approved `03_sow_v*` + per-platform `references/native-ai-inventory.md` and receive `04a_fnspec-platform_v<N>.md` per DESIGN-02 (STG4-01)"
    - "Every business-rule / field-req / acceptance-criteria row in the 4a template carries `delivery: native-ai | api` markup in canonical enum order (never reversed) — STG4-04 + D-82 (assertion P5)"
    - "HIGH/MEDIUM-confidence rows from the loaded platform's `native-ai-inventory.md` suggest `delivery: native-ai`; LOW/`[OPEN]` rows default to `delivery: api` per D-81 + STG4-05 (auto-classify-rubric.md authored)"
    - "Rows carrying `[reviewer-override:]` token are NOT re-classified on re-run (D-82 + D-81 re-run preservation rule)"
    - "On platform-only-with-API-rows topology (no 4b in scope AND 4a has `delivery: api` rows), 4a authors the `## Platform-API Addendum` H2 body INLINE per D-79 + ROUTE-03 + emits frontmatter `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only` (assertion P8)"
    - "SKILL.md cites all 4 canonical references (safety-rules / stage-numbering / frontmatter-scheme / glossary) per FOUND-04 + D-59 pattern (assertion P3)"
    - "3 `references/` files exist per D-83: `fnspec-platform-template.md` + `auto-classify-rubric.md` + `addendum-template.md` (assertion P2)"
    - "`platform:` enum in 4a SKILL.md + template carries the 4-value form `pipefy | wrike | ziflow | other` per D-78 (baked in from authorship; assertion P4 / X1)"
    - "SKILL.md `## Key decisions` section names D-78 (TD-2 / ziflow) + D-79 (addendum carrier) + D-82 (row markup) + D-84 (consistency check ownership) + D-85 (skip mechanics) — two-place declaration per T-02-06-02 / ROUTE-01 (assertion I8 contribution)"
    - "`auto-classify-rubric.md` referenced from SKILL.md (assertion P6); `addendum-template.md` referenced from SKILL.md (assertion P7)"
  artifacts:
    - path: "dydx-delivery/skills/generate-fnspec-platform/SKILL.md"
      provides: "Stage 4a skill body — invocation triggers, inputs, output, How-to-run steps, addendum-branch logic, write-and-hand-off, canonical-reference pointers"
      contains: "4 canonical-pointer sentences (safety-rules / stage-numbering / frontmatter-scheme / glossary), `platform: pipefy | wrike | ziflow | other` enum, verbatim handoff message, references/auto-classify-rubric.md + references/addendum-template.md + references/fnspec-platform-template.md paths, `## Key decisions` section naming D-78/D-79/D-82/D-84/D-85, D-82 row markup format documented, 5-step or 7-step How-to-run with explicit addendum-branch step (D-79)"
    - path: "dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md"
      provides: "Artefact body template for `04a_fnspec-platform_v<N>.md`"
      contains: "Frontmatter block carrying `platform: pipefy | wrike | ziflow | other` + `based_on_discovery:` + `based_on_sow:` + `based_on_kickoff:` (conditional) + canonical 4-state status lifecycle + `has_platform_api_addendum:` + `tech_spec_scope:` (conditional, D-79) + `approved_by` + `approved_at` + 10 H2 sections (Scope summary / User roles / User journeys / Business rules / Field reqs / State model / Edge cases / Acceptance criteria / Open questions / Out-of-scope) with `Delivery` column carrying D-82 markup pattern in sections 4 / 5 / 8"
    - path: "dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md"
      provides: "D-81 + STG4-05 classifier rubric (HIGH/MED → native-ai; LOW/[OPEN] → api; reviewer-override preserved on re-run)"
      contains: "Explicit triggers (5 rules tied to native-ai-inventory.md confidence levels) + escalation table mapping HIGH/MEDIUM/LOW/[OPEN] × suggest-native-ai / default-api / preserve-reviewer-override + operational principle backstop + re-run preservation rule citing `[reviewer-override:]` token + 'How this rubric is consumed' footer citing SKILL.md Step N"
    - path: "dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md"
      provides: "D-79 `## Platform-API Addendum` H2 body skeleton (error-paths discipline for API portions only)"
      contains: "`## Platform-API Addendum` H2 + sub-sections (API surface inventory / Error paths / Retry + idempotency rules / Observability hooks) with D-82 row markup pattern on every API-row line; planner-authored body shape within D-79 contract"
  key_links:
    - from: "dydx-delivery/skills/generate-fnspec-platform/SKILL.md"
      to: "dydx-delivery/references/{safety-rules,stage-numbering,frontmatter-scheme,glossary}.md"
      via: "4 inline canonical-pointer sentences (FOUND-04 / D-59 pattern)"
      pattern: "dydx-delivery/references/.*\\.md"
    - from: "dydx-delivery/skills/generate-fnspec-platform/SKILL.md"
      to: "dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md"
      via: "Step body reference (P6 assertion)"
      pattern: "references/auto-classify-rubric.md"
    - from: "dydx-delivery/skills/generate-fnspec-platform/SKILL.md"
      to: "dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md"
      via: "Step body reference inside addendum-branch step (P7 assertion + D-79)"
      pattern: "references/addendum-template.md"
    - from: "dydx-delivery/skills/generate-fnspec-platform/SKILL.md"
      to: "dydx-delivery/skills/platform-{pipefy,wrike,ziflow}/references/native-ai-inventory.md"
      via: "Step 2 — load active platform skill's native-AI inventory based on the SOW's `platform:` value (per DESIGN-14/15/16)"
      pattern: "platform-(pipefy|wrike|ziflow)/references/native-ai-inventory.md"
threat_model: {}
---

<objective>
Author Stage 4a `generate-fnspec-platform/` skill end-to-end — SKILL.md body + 3 `references/` files (fnspec-platform-template / auto-classify-rubric / addendum-template) — locking the 4a frontmatter contract (D-78 4-enum baked in, D-82 per-row `delivery:` markup, D-79 addendum branch + frontmatter), the D-81 / STG4-05 classifier rubric, and the two-place key-decisions declaration (D-78/D-79/D-82/D-84/D-85) per T-02-06-02 / ROUTE-01 mitigation.

Purpose: Stand up the routing-key author tier. Stage 4b (08-02) reads the approved 4a output as upstream — 4a's `delivery:` row markup is the input that 4b's consistency check verifies against. Lock the contract now so 08-02 has a stable upstream to write against.

Output: 4 new files (1 SKILL.md + 3 references/). 08-02 lights up immediately after this plan ships.

Wave 1 — no upstream plan dependency. `autonomous: true`.
</objective>

<plan_warning>
**CRITICAL — phase8-structure-check.sh does NOT exist yet during 08-01.**

The Phase 8 structure-check shell script ships in 08-03 (per D-86 / RESEARCH §2.3 partition table). During 08-01 execution, there is NO automated `--section 4a` gate to run. Plan 08-01's verification posture is therefore:

1. **Per-task `<verify>` commands** below are direct `grep -qF` / `grep -qE` / `test -f` assertions against the files this plan authors. These are the proxy assertions for what 08-03 will later wire into `run_4a_section()` in `phase8-structure-check.sh`.

2. **Final plan gate** is a sequence of greps run at plan close — every assertion P1-P8 from `08-VALIDATION.md` §1 is reachable via a direct grep against the 4 files authored here.

3. The full structure-check `--section 4a` PASS-line gate runs at the END of 08-03 (and again under `--all`) once `phase8-structure-check.sh` is on disk. 08-03 self-fails if 4a-section assertions fail at that point — which would force a 08-01 retro-fix. To avoid that, this plan's task-level greps anticipate every P1-P8 assertion verbatim.

**The P1-P8 assertion authorship contract authored here MUST match what 08-03 wires into the script.** Cross-check the assertion strings in this plan against the partition table in `08-VALIDATION.md` §1 + `08-RESEARCH.md` §2.3 — they must be identical.
</plan_warning>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md
@.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md
@.planning/phases/08-stage-4-fnspec-split-route/08-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@dydx-delivery/skills/generate-functional-spec/SKILL.md
@dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md
@dydx-delivery/skills/kickoff-capture/SKILL.md
@dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md
@dydx-delivery/skills/discovery-intake/SKILL.md
@dydx-delivery/skills/generate-sow/SKILL.md
@dydx-delivery/skills/generate-sow/references/sow-template.md
@dydx-delivery/skills/platform-pipefy/SKILL.md
@dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md
@dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md
@dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md
@dydx-delivery/references/frontmatter-scheme.md
@dydx-delivery/references/glossary.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-01-PLAN.md

<interfaces>
<!-- Verbatim strings the future structure-check (ships in 08-03) will assert via `grep -qF`. Every executor edit MUST preserve these literally. -->

**4a artefact frontmatter contract (DESIGN-20 + D-78 + D-79 + DESIGN-01 + DESIGN-06):**
```yaml
---
client: <CLIENT_NAME>
project: <PROJECT_NAME>
frontmatter_version: 2
platform: <pipefy | wrike | ziflow | other>           # D-78 — 4-enum baked in
based_on_discovery: 02_discovery_v<N>.md              # MANDATORY when discovery exists
based_on_sow: 03_sow_v<N>.md                          # MANDATORY (single SOW path per STG3-02)
based_on_kickoff: 01_kickoff_v<N>.md                  # CONDITIONAL — only when discovery skipped (kickoff_branch: draft-sow path)
status: draft                                         # canonical 4-state: draft → client_review → approved → archived
has_platform_api_addendum: <true | false>             # D-79 — present only when 4b is OUT of scope AND 4a has any delivery: api rows
tech_spec_scope: platform-api-addendum-only           # D-79 — present only when has_platform_api_addendum: true
approved_by: <name-or-handle>                         # MANDATORY on status: approved writes (DESIGN-06)
approved_at: <ISO date>                               # MANDATORY on status: approved writes (DESIGN-06)
generated_at: <ISO date>
---
```

**D-82 per-row markup contract (verbatim canonical enum order — STG4-04 lock; NEVER reversed):**
```
delivery: native-ai [HIGH, src: platform-pipefy]
delivery: native-ai [MEDIUM, src: platform-wrike]
delivery: api [LOW → default api, src: platform-ziflow]
delivery: api [reviewer-override: api]
delivery: native-ai [reviewer-override: native-ai]
```
- Canonical enum order is `native-ai | api` — NEVER `api | native-ai` (STG4-04 lock).
- Unicode arrow `→` verbatim (NOT `->`).
- `[reviewer-override:]` literal token = re-run preservation trigger.

**Canonical 4-state status lifecycle (DESIGN-01 + DESIGN-08 + Phase 7 D-75 carry-forward):**
```
draft → client_review → approved → archived
```
(unicode arrows; NEVER `->`)

**Canonical-pointer sentence pattern (FOUND-04 / D-59 — lift from `kickoff-capture/SKILL.md` canonical-pointer block):**
```markdown
> **<topic>:** <one-line context>. See `dydx-delivery/references/<file>.md` for the canonical <noun>.
```

**Verbatim handoff message (planner-authored within D-79 + STG4 contracts; lock at write time):**
```
Awaiting status: approved on 04a_fnspec-platform_v<N>.md. Stage 4b (generate-fnspec-integration) reads this artefact when integration scope exists. If integration is out-of-scope AND this 4a carries delivery: api rows, this artefact's `## Platform-API Addendum` H2 body is the v2.3 Stage 5 input via has_platform_api_addendum: true + tech_spec_scope: platform-api-addendum-only frontmatter.
```
(Planner polishes; final string MUST be locked verbatim before execute since it forms a structure-check anchor.)

**Two-place key-decisions block (T-02-06-02 / ROUTE-01 mitigation; 4a half — 4b emits the matching block in 08-02):**
```markdown
## Key decisions

- **D-78 — TD-2 resolution.** Stage-skill `platform:` enum is `pipefy | wrike | ziflow | other`. Ziflow is wired through as a routing-key value per `platform-ziflow/SKILL.md`.
- **D-79 — Platform-API Addendum carrier (ROUTE-03).** When no 4b is in scope AND this 4a has any `delivery: api` rows, this skill authors the FULL `## Platform-API Addendum` H2 body inline + emits `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only`. v2.3 Stage 5 reads these frontmatter fields and consumes the addendum body verbatim.
- **D-82 — Per-row routing markup.** Every business-rule / field-req / acceptance-criteria row carries `delivery: native-ai | api [<confidence>, src: <platform>]` in canonical enum order. `[reviewer-override:]` token is the re-run preservation trigger.
- **D-84 — Consistency check ownership.** Stage 4b OWNS the three cross-spec consistency checks; this 4a does NOT run them. 4a's contract is to emit clean, classified rows that 4b can verify.
- **D-85 — Either-spec-skip mechanics.** Stage 4a runs normally regardless of 4b topology; there is no "skip 4a" path. 4b detects either-spec-skip via SOW H2s + presence/absence of this 4a output.
```

**Anticipated P1-P8 assertion gate (08-03 wires these into `run_4a_section()`; planner authors against this contract):**
- P1 — `dydx-delivery/skills/generate-fnspec-platform/SKILL.md` exists.
- P2 — 3 references/ files exist (`fnspec-platform-template.md`, `auto-classify-rubric.md`, `addendum-template.md`).
- P3 — 4 canonical-pointer paths present in SKILL.md (safety-rules / stage-numbering / frontmatter-scheme / glossary).
- P4 — `platform:` enum carries `pipefy | wrike | ziflow | other` in SKILL.md AND template.
- P5 — `delivery: native-ai | api` canonical order documented in SKILL.md body (NEVER `api | native-ai`).
- P6 — `references/auto-classify-rubric.md` referenced from SKILL.md.
- P7 — `references/addendum-template.md` referenced from SKILL.md.
- P8 — `has_platform_api_addendum` + `tech_spec_scope` frontmatter fields documented in SKILL.md / template.
</interfaces>
</context>

<tasks>

<task id="08-01-T1" type="auto">
  <name>Task 1: Author generate-fnspec-platform/SKILL.md (canonical pointers + Inputs/Output + 5-7 How-to-run steps + D-79 addendum branch + Key decisions block + handoff)</name>
  <files>dydx-delivery/skills/generate-fnspec-platform/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-functional-spec/SKILL.md` (113 lines, full — RETIRING skill; lift Step shape: Step 1 Locate upstream / Step 2 Check for existing fnspec / Step 3 Draft body / Step 4 Senior-level challenge / Step 5 Write + handoff)
    - `dydx-delivery/skills/kickoff-capture/SKILL.md` (Phase 7 product — canonical-pointer block exemplar lines 1-30 + How-to-run / Inputs / Output section structure; lift the 4-pointer block pattern verbatim)
    - `dydx-delivery/skills/discovery-intake/SKILL.md:23-29` (Step 1 — Locate upstream artefact triage; lift start-at-any-point pattern with three options)
    - `.planning/codebase/CONVENTIONS.md` § "SKILL.md body structure" (body-shape canon)
    - `.planning/DESIGN.md` §DESIGN-20 + §DESIGN-21 (Stage 4 split contract + Stage 5 scope-gate forward-compat)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions (D-78 / D-79 / D-81 / D-82 verbatim wording)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.1 (Step body sketch lines 188-196 — full 7-step lift target)
    - `dydx-delivery/skills/platform-pipefy/SKILL.md` + `platform-wrike/SKILL.md` + `platform-ziflow/SKILL.md` (loaded-platform-skill reference for Step 2 `native-ai-inventory.md` load pattern)
    - `dydx-delivery/references/frontmatter-scheme.md` (canonical underscore-snake-case keys + `based_on_*` chain convention)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-platform/SKILL.md` per CONVENTIONS.md SKILL.md body-shape canon.

    **Skill frontmatter** (top of file, between `---` delimiters):
    ```yaml
    name: generate-fnspec-platform
    description: Generate the Stage 4a platform functional spec. Use when the user says "generate fnspec-platform for X", "draft 4a", "platform fnspec", or asks for a functional spec scoped to platform configuration after discovery + SOW are approved. Reads approved 02_discovery_v* + approved 03_sow_v* + the loaded platform skill's references/native-ai-inventory.md. Writes 04a_fnspec-platform_v<N>.md with every requirement row carrying the delivery: native-ai | api routing key. On the platform-only-with-API-rows topology, also authors the ## Platform-API Addendum H2 body inline per ROUTE-03.
    ```

    **Body sections in this order** (per CONVENTIONS.md):

    1. `# generate-fnspec-platform` — H1 matching directory name.

    2. **One-paragraph purpose statement.** Reference "Stage 4a of the dydx-delivery pipeline"; name the `delivery: native-ai | api` routing key (in canonical enum order); name the 4 input artefacts (approved discovery / approved SOW / per-platform `native-ai-inventory.md` / approved kickoff on draft-sow path); name the output artefact `04a_fnspec-platform_v<N>.md`; reference the D-79 conditional addendum-body authorship in one sentence.

    3. **4 canonical-pointer block quotes** (lift verbatim shape from `kickoff-capture/SKILL.md`; adapt topic-line wording to 4a's role — P3 asserts each substring `dydx-delivery/references/<file>.md`):
       ```markdown
       > **Hard rules:** Sandbox-only operations. Refuses destructive actions on production tenants. Approval gate is non-negotiable per DESIGN-06. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.

       > **Stage numbering:** This skill writes `04a_fnspec-platform_v<N>.md` per the canonical file-prefix scheme — Stage 4a in the dydx-delivery pipeline. See `dydx-delivery/references/stage-numbering.md` for the canonical Stage 4a placement + old→new mapping table.

       > **Frontmatter scheme:** This skill emits `platform: <pipefy | wrike | ziflow | other>` + `based_on_discovery:` + `based_on_sow:` + (conditional) `based_on_kickoff:` + `has_platform_api_addendum:` + `tech_spec_scope:` per the canonical underscore-snake-case convention. See `dydx-delivery/references/frontmatter-scheme.md` for the canonical scheme + lenient-mode contract.

       > **Glossary:** `delivery: native-ai | api`, `platform: pipefy | wrike | ziflow | other`, `[reviewer-override:]`, and `has_platform_api_addendum` are canonical terms defined in `dydx-delivery/references/glossary.md`.
       ```
       All 4 paths must appear literally (P3). The Frontmatter and Glossary blocks BOTH name the 4-enum form `pipefy | wrike | ziflow | other` (helps satisfy P4) AND the canonical enum order `native-ai | api` (helps satisfy P5).

    4. `## Inputs` — bulleted list per STG4-01 + DESIGN-20 + DESIGN-14/15/16:
       - Approved `02_discovery_v<N>.md` (status: approved enforced) — normal path.
       - Approved `03_sow_v<N>.md` (status: approved enforced) — MANDATORY (single-SOW path per STG3-02).
       - Approved `01_kickoff_v<N>.md` — CONDITIONAL substitute for discovery on the `kickoff_branch: draft-sow` path (Phase 7 D-74 carry-forward).
       - Loaded platform skill's `references/native-ai-inventory.md` — the classifier input. Loaded based on the SOW's `platform:` value. Path: `dydx-delivery/skills/platform-<platform>/references/native-ai-inventory.md` where `<platform>` is one of `pipefy | wrike | ziflow | other` (D-78 expanded enum).

    5. `## Output` — `<Client> Brain/<Project>/04a_fnspec-platform_v<N>.md` per DESIGN-02 file-prefix scheme. Frontmatter contract per the `<interfaces>` block above. Also (conditionally) writes the `## Platform-API Addendum` H2 inline within the same artefact when D-79 conditions hold.

    6. `## How to run` — author 7 numbered steps lifting from RESEARCH.md §2.1 lines 188-196:
       - `### Step 1 — Locate approved upstream artefacts` — find highest-version approved `02_discovery_v*.md` + approved `03_sow_v*.md`. If discovery is absent (draft-sow path), use approved `01_kickoff_v*.md` directly per `based_on_kickoff:`. Start-at-any-point triage on missing inputs (three options: paste / walk-through / cancel — lift from `discovery-intake/SKILL.md:23-29`).
       - `### Step 2 — Load platform skill's native-AI inventory` — read `dydx-delivery/skills/platform-<platform>/references/native-ai-inventory.md` matching the SOW's `platform:` value. Cache HIGH / MEDIUM / LOW / `[OPEN]` confidence rows for Step 3 classifier (DESIGN-14/15/16).
       - `### Step 3 — Check for existing 4a artefact + re-run policy` — if `04a_fnspec-platform_v*.md` exists, ask reviewer revise / extend / fresh; on revise, scan existing rows for `[reviewer-override:]` tokens and PRESERVE them (do not re-classify) per D-81 + D-82.
       - `### Step 4 — Draft the platform fnspec body` — walk SOW `## Platform Scope` deliverable-by-deliverable; produce sections 1-10 per `references/fnspec-platform-template.md`. For every row in sections 4 (Business rules) / 5 (Field-level requirements) / 8 (Acceptance criteria), apply the D-81 rubric → emit `delivery: native-ai | api [<confidence>, src: <platform>]` markup per D-82. **Quote the canonical enum order `native-ai | api` verbatim in the step body — NEVER reversed (helps P5).** Reference `references/auto-classify-rubric.md` by relative path (P6 asserts this substring).
       - `### Step 5 — Scope-gate branch decision (ROUTE-03 / D-79)` — determine 4b topology from SOW `## Integration Scope` + presence/absence of 4a `delivery: api` rows:
         - **If no-4b-in-scope AND 4a has any `delivery: api` rows** → set `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only` in frontmatter AND author the `## Platform-API Addendum` H2 body inline using `references/addendum-template.md` (full error-paths discipline per D-79). Reference `references/addendum-template.md` by relative path (P7 asserts this substring).
         - **Else** → omit both frontmatter fields and the addendum H2.
         Step body MUST name both branches explicitly. Quote `has_platform_api_addendum` AND `tech_spec_scope` literally so P8 catches them.
       - `### Step 6 — Senior-level challenge` — pressure-test per the lifted functional-spec checks (every business rule numbered + unambiguous; every SOW platform deliverable has at least one user journey + AC block; edge cases concrete not generic; no platform-implementation details leaked; addendum body (when present) carries explicit error-path rules per row).
       - `### Step 7 — Write and hand off` — write to `<Client> Brain/<Project>/04a_fnspec-platform_v<N>.md`. Emit the verbatim handoff message from `<interfaces>` block above.

    7. `## Key decisions` — author the two-place key-decisions block from the `<interfaces>` section verbatim (T-02-06-02 / ROUTE-01 mitigation — naming D-78 / D-79 / D-82 / D-84 / D-85). This block contributes to assertion I8 in 08-02 (08-02's structure-check checks BOTH 4a and 4b SKILL.md key-decisions sections cite the same decisions).

    8. `## What this skill does not do`:
       - Does NOT run the cross-spec consistency checks — those are owned by Stage 4b (D-84).
       - Does NOT auto-progress to Stage 4b — approval gate non-negotiable per DESIGN-06.
       - Does NOT re-classify rows carrying `[reviewer-override:]` token on re-run (D-81 + D-82 preservation rule).
       - Does NOT consume the addendum body downstream — v2.3 Stage 5 reads `has_platform_api_addendum:` + `tech_spec_scope:` frontmatter to decide whether to skip writing `05_techspec_v<N>.md` (forward-compat interface only per ROUTE-03 + ROUTE-05).
       - Does NOT call any Coda / native-AI ingestion APIs — DESIGN-14/15/16 native-ai-inventory is read-only file content; native-AI consumption ships in v2.5+ (Stage 10) per ROUTE-05.

    9. `## Quality bar` — bulleted "good looks like":
       - Every row in sections 4 / 5 / 8 carries the D-82 `delivery:` markup (no row missing the routing key).
       - Canonical enum order `native-ai | api` (never reversed) — STG4-04 lock.
       - `[reviewer-override:]` tokens are preserved verbatim across re-runs.
       - Addendum body (when present) per D-79 contract — error-paths discipline matching a full tech spec, API portions only.
       - All 4 canonical references resolve.
       - Handoff message emitted verbatim.

    Verbatim D-82 markup example MUST appear at least once in the SKILL.md body — e.g., quote `delivery: native-ai [HIGH, src: platform-pipefy]` inside Step 4 prose so a future reader sees the exact format. This helps satisfy P5 (canonical order documented).
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'dydx-delivery/references/safety-rules.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'dydx-delivery/references/stage-numbering.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'dydx-delivery/references/frontmatter-scheme.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'dydx-delivery/references/glossary.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'references/auto-classify-rubric.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'references/addendum-template.md' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'native-ai | api' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'has_platform_api_addendum' dydx-delivery/skills/generate-fnspec-platform/SKILL.md && grep -qF 'tech_spec_scope' dydx-delivery/skills/generate-fnspec-platform/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `dydx-delivery/skills/generate-fnspec-platform/SKILL.md` (P1 anchor)
    - `grep -qF 'dydx-delivery/references/safety-rules.md' SKILL.md` succeeds (P3a)
    - `grep -qF 'dydx-delivery/references/stage-numbering.md' SKILL.md` succeeds (P3b)
    - `grep -qF 'dydx-delivery/references/frontmatter-scheme.md' SKILL.md` succeeds (P3c)
    - `grep -qF 'dydx-delivery/references/glossary.md' SKILL.md` succeeds (P3d)
    - `grep -qF 'pipefy | wrike | ziflow | other' SKILL.md` succeeds (P4 — 4-enum present in SKILL.md)
    - `grep -qF 'native-ai | api' SKILL.md` succeeds (P5 — canonical order)
    - `! grep -qF 'api | native-ai' SKILL.md` (no reversed enum anywhere — STG4-04 lock)
    - `grep -qF 'references/auto-classify-rubric.md' SKILL.md` succeeds (P6)
    - `grep -qF 'references/addendum-template.md' SKILL.md` succeeds (P7)
    - `grep -qF 'has_platform_api_addendum' SKILL.md` succeeds (P8a)
    - `grep -qF 'tech_spec_scope' SKILL.md` succeeds (P8b)
    - `grep -qF '[reviewer-override:' SKILL.md` succeeds (D-82 re-run preservation token documented)
    - `grep -qF 'delivery: native-ai [HIGH, src: platform-' SKILL.md` succeeds (D-82 markup example present)
    - `grep -cE '^### Step [1-7]' SKILL.md` returns >= 5 (5+ numbered steps; 7 is the RESEARCH-derived target)
    - `grep -qF '## Key decisions' SKILL.md` succeeds (two-place declaration anchor)
    - SKILL.md key-decisions section names D-78 AND D-79 AND D-82 AND D-84 AND D-85 (each as a literal substring) — `for d in D-78 D-79 D-82 D-84 D-85; do grep -qF "$d" SKILL.md || fail $d; done` returns clean.
  </acceptance_criteria>
  <done>SKILL.md exists with 4 canonical pointers + 4-enum baked in + canonical `native-ai | api` order documented + addendum branch step + 5-7 numbered steps + Key decisions block naming D-78/D-79/D-82/D-84/D-85 + handoff message. P1, P3, P4, P5, P6, P7, P8 reachable via grep.</done>
</task>

<task id="08-01-T2" type="auto">
  <name>Task 2: Author references/fnspec-platform-template.md (artefact frontmatter contract + 10 H2 sections + Delivery column with D-82 markup in sections 4/5/8)</name>
  <files>dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md` (165 lines, full — direct-lift body shape; 10-section spine)
    - `dydx-delivery/skills/generate-sow/references/sow-template.md` (Phase 7 D-75 — `## Platform Scope` / `## Integration Scope` H2 shape; cross-check for downstream consistency)
    - `dydx-delivery/skills/kickoff-capture/references/kickoff-template.md` (Phase 7 D-71 — frontmatter shape precedent)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-78 / D-79 / D-82
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.1 + §3.1 (frontmatter contract + per-row markup contract)
    - `dydx-delivery/references/frontmatter-scheme.md` (canonical 4-enum reference at line 41)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md`.

    **Pre-frontmatter documentation block** (above the opening `---`):
    ```markdown
    <!--
      04a_fnspec-platform template — Stage 4a output skeleton.

      delivery: row markup (D-82) — canonical enum order is `native-ai | api`, NEVER reversed (STG4-04 lock).
      Suffix variants:
        - delivery: native-ai [HIGH, src: platform-pipefy]
        - delivery: native-ai [MEDIUM, src: platform-wrike]
        - delivery: api [LOW → default api, src: platform-ziflow]
        - delivery: api [reviewer-override: api]              # re-run preservation — NOT re-classified
        - delivery: native-ai [reviewer-override: native-ai]  # re-run preservation — NOT re-classified

      has_platform_api_addendum + tech_spec_scope (D-79) — emit ONLY when no 4b is in scope AND this 4a has any `delivery: api` rows.
      Otherwise omit both frontmatter fields AND the ## Platform-API Addendum H2 section below.
    -->
    ```

    **Frontmatter block** (concrete enum value in `platform:` line — `pipefy` as the planner-default; reviewer switches at write time. Anchored P4 + P8 assertions match the literal `pipefy | wrike | ziflow | other` token AND `has_platform_api_addendum` / `tech_spec_scope` tokens.):
    ```yaml
    ---
    client: <CLIENT_NAME>
    project: <PROJECT_NAME>
    frontmatter_version: 2
    platform: pipefy                                # Valid values: pipefy | wrike | ziflow | other (D-78 — switch at write time)
    based_on_discovery: 02_discovery_v<N>.md         # MANDATORY when discovery exists (normal path)
    based_on_sow: 03_sow_v<N>.md                     # MANDATORY (single SOW path per STG3-02)
    based_on_kickoff: <N/A or 01_kickoff_v<N>.md>    # CONDITIONAL — present only on the draft-sow path (Stage 2 skipped)
    status: draft                                    # canonical 4-state: draft → client_review → approved → archived
    has_platform_api_addendum: false                 # D-79 — set to true ONLY when no 4b is in scope AND this 4a has any delivery: api rows
    tech_spec_scope: <omit or platform-api-addendum-only>  # D-79 — present ONLY when has_platform_api_addendum: true
    approved_by: <name-or-handle>                    # MANDATORY on status: approved writes (DESIGN-06)
    approved_at: <ISO date>                          # MANDATORY on status: approved writes
    generated_at: <ISO date>
    ---
    ```
    The literal token `pipefy | wrike | ziflow | other` appears in the inline comment so a future grep against the template catches it (P4). The literal tokens `has_platform_api_addendum` and `tech_spec_scope` appear in the frontmatter as field names (P8).

    **Body opening** (adapt from functional-spec-template.md):
    ```markdown
    # Stage 4a Platform Functional Spec — <CLIENT_NAME> · <PROJECT_NAME>

    > Stage 4a of the dydx-delivery pipeline. Captures the platform-side functional contract from approved discovery + approved SOW + the loaded platform skill's native-AI inventory. Every requirement row carries the `delivery: native-ai | api` routing key per D-82 — canonical enum order, never reversed (STG4-04). Stage 4b consumes this artefact as upstream for the integration fnspec + the three cross-spec consistency checks.
    ```

    **10 H2 sections in this order** (lift body shape from functional-spec-template.md):
    1. `## 1. Scope summary` — one-paragraph summary of what's in scope from the SOW's `## Platform Scope` section.
    2. `## 2. User roles` — primary actors + secondary stakeholders touching the platform.
    3. `## 3. User journeys` — main happy-path flows; numbered list.
    4. `## 4. Business rules` — **table with `ID | Rule | Source | Delivery` columns**. Every row's `Delivery` column carries a D-82 markup string (e.g., `delivery: native-ai [HIGH, src: platform-pipefy]`). Include at least 3 placeholder rows demonstrating different confidence levels (HIGH / MEDIUM / LOW) and one row carrying `[reviewer-override:]` to show the preservation pattern.
    5. `## 5. Field-level requirements` — **table with `Field | Type | Required | Source | Delivery` columns**. Same D-82 markup pattern on every row.
    6. `## 6. State model` — workflow states + transitions; not row-tagged (no Delivery column needed).
    7. `## 7. Edge cases` — concrete (not generic) edge cases enumerated.
    8. `## 8. Acceptance criteria` — **table with `ID | AC | Validates rule(s) | Delivery` columns**. Same D-82 markup pattern.
    9. `## 9. Open questions` — explicit unresolved items.
    10. `## 10. Out-of-scope clarifications` — explicit non-goals.

    **Conditional appendix — `## Platform-API Addendum` H2 (D-79)** — placed AFTER section 10. Include a placeholder block with an HTML comment explaining when to emit it:
    ```markdown
    <!--
      D-79 — emit this H2 ONLY when has_platform_api_addendum: true (i.e., no 4b is in scope AND this 4a has any delivery: api rows).
      Body content is authored from `references/addendum-template.md`.
      v2.3 Stage 5 reads this body verbatim and skips writing 05_techspec_v<N>.md when tech_spec_scope: platform-api-addendum-only.
    -->

    ## Platform-API Addendum

    *(emit only when has_platform_api_addendum: true — see references/addendum-template.md for the body skeleton)*
    ```

    **Handoff footer:** lift verbatim from SKILL.md `<interfaces>` handoff message.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md && grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md && grep -qF 'native-ai | api' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md && grep -qF 'has_platform_api_addendum' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md && grep -qF 'tech_spec_scope' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md && [ $(grep -cE '^## [0-9]+\.' dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md) -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the target path (P2 contribution).
    - `grep -qF 'pipefy | wrike | ziflow | other' fnspec-platform-template.md` succeeds (P4 — 4-enum present in template).
    - `grep -qF 'native-ai | api' fnspec-platform-template.md` succeeds (P5 — canonical order in template).
    - `! grep -qF 'api | native-ai' fnspec-platform-template.md` (no reversed enum anywhere).
    - `grep -qF 'has_platform_api_addendum' fnspec-platform-template.md` succeeds (P8a in template).
    - `grep -qF 'tech_spec_scope' fnspec-platform-template.md` succeeds (P8b in template).
    - `grep -cE '^## [0-9]+\.' fnspec-platform-template.md` returns >= 10 (10 numbered H2 sections present).
    - `grep -qF '## Platform-API Addendum' fnspec-platform-template.md` succeeds (D-79 addendum H2 carrier present).
    - `grep -qF 'delivery: native-ai [HIGH, src: platform-' fnspec-platform-template.md` succeeds (D-82 example row present in body — typically in section 4 or 5 table).
    - `grep -qF '[reviewer-override:' fnspec-platform-template.md` succeeds (D-82 re-run preservation pattern present in an example row).
    - `grep -qF 'draft → client_review → approved → archived' fnspec-platform-template.md` succeeds (canonical 4-state lifecycle documented in pre-frontmatter or frontmatter comment).
  </acceptance_criteria>
  <done>Template exists with 10 H2 sections + D-78 4-enum + D-82 row markup pattern in sections 4/5/8 + D-79 addendum H2 carrier + canonical 4-state lifecycle.</done>
</task>

<task id="08-01-T3" type="auto">
  <name>Task 3: Author references/auto-classify-rubric.md (D-81 + STG4-05 — 5 triggers + escalation table + reviewer-override re-run preservation rule)</name>
  <files>dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md</files>
  <read_first>
    - `dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md` (44 lines, full — Phase 7 D-73 direct template; 5-trigger + escalation table + operational principle shape)
    - `dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md` + `platform-wrike/references/native-ai-inventory.md` + `platform-ziflow/references/native-ai-inventory.md` (HIGH / MEDIUM / LOW / `[OPEN]` confidence levels — the classifier input shape per DESIGN-14/15/16)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-81 + D-82
    - `.planning/REQUIREMENTS.md` STG4-05 (verbatim rubric authority)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md`. Body shape lifts from `kickoff-capture/references/auto-classify-rubric.md`; replace kickoff triggers with 4a-specific classifier rules per D-81 + STG4-05.

    **Body structure:**

    1. **H1 + one-paragraph purpose statement** — explain when to emit `delivery: native-ai` vs `delivery: api`; reference the loaded platform skill's `references/native-ai-inventory.md` as the classifier input per DESIGN-14/15/16; cite the consumer (`generate-fnspec-platform/SKILL.md` Step 4).

    2. `## Explicit triggers` — 5 numbered rules (planner authors threshold wording within D-81's rule shape; D-81 + STG4-05 are the verbatim authority):
       1. **Row matches a HIGH-confidence entry in the loaded platform's `native-ai-inventory.md`** → suggest `delivery: native-ai [HIGH, src: platform-<platform>]`.
       2. **Row matches a MEDIUM-confidence entry** → suggest `delivery: native-ai [MEDIUM, src: platform-<platform>]`. Planner discretion on MEDIUM threshold (e.g., "≥2 distinct verified MCP probes" or "≥3" — pick one and document; default `≥2`).
       3. **Row matches a LOW-confidence entry** → default to `delivery: api [LOW → default api, src: platform-<platform>]`. Default-to-api avoids optimistic native-AI claims when only copy-paste works (per D-81 + STG4-05 explicit rationale).
       4. **Row matches an `[OPEN]`-flagged entry** in the inventory → default to `delivery: api [LOW → default api, src: platform-<platform>]`. Treat `[OPEN]` as LOW per D-81.
       5. **Row does NOT appear in the inventory** → default to `delivery: api` with no `src:` reference. Inventory absence == evidence absence; do not assume native-AI applies.

    3. `## Input-signal → outcome escalation` — markdown table mapping inventory confidence level × outcome:
       | Inventory level | Outcome |
       |---|---|
       | HIGH | `delivery: native-ai [HIGH, src: platform-<platform>]` |
       | MEDIUM | `delivery: native-ai [MEDIUM, src: platform-<platform>]` |
       | LOW | `delivery: api [LOW → default api, src: platform-<platform>]` |
       | `[OPEN]` | `delivery: api [LOW → default api, src: platform-<platform>]` |
       | absent | `delivery: api` (no `src:` reference) |

    4. `## Reviewer-override mechanics (D-82)` — explain the override pattern:
       - Reviewer edits a row to read `delivery: native-ai [reviewer-override: native-ai]` or `delivery: api [reviewer-override: api]`.
       - The `[reviewer-override:]` token verbatim is the re-run preservation trigger.
       - On re-run, the skill MUST scan existing rows for `[reviewer-override:]` and PRESERVE them — do NOT re-apply the rubric to overridden rows.
       - Reference: SKILL.md Step 3 (re-run policy) + Step 4 (classifier draft pass).

    5. `## Re-run preservation rule` — explicit one-sentence rule per D-81 + D-82:
       > Rows carrying the `[reviewer-override:]` token are NEVER re-classified on re-run. The reviewer's choice is final.

    6. `## Operational principle (backstop)` — defensive backstop when rubric output is ambiguous:
       > When the inventory entry confidence is borderline OR the reviewer has not pre-flagged the row, default to `delivery: api`. Native-AI claims should be defensible against the loaded platform's verified MCP probes; optimistic native-AI claims downstream of LOW confidence inventory rows produce false routing decisions in Stage 5 / 6 / 7b / 10.

    7. `## How this rubric is consumed` — short paragraph noting that `generate-fnspec-platform/SKILL.md` Step 4 references this file by relative path (P6 assertion); structure-check P5 asserts the `delivery: native-ai | api` canonical enum order is documented in SKILL.md body (not here). Note that this rubric is invoked once per row in sections 4 / 5 / 8 of the platform fnspec.

    Discretion: planner may adjust the MEDIUM trigger threshold (e.g., "≥2 distinct verified MCP probes" → "≥3") if rationale exists; default is `≥2` (matches the Phase 6 native-ai-inventory HIGH/MEDIUM/LOW canon).
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md && grep -qF 'native-ai-inventory.md' dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md && grep -qF '[reviewer-override:' dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md && grep -qF 'delivery: native-ai' dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md && grep -qF 'delivery: api' dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the target path (P2 contribution + P6 reachable).
    - grep -qF native-ai-inventory.md in auto-classify-rubric.md succeeds (classifier input documented).
    - grep -qF "delivery: native-ai" in auto-classify-rubric.md succeeds (native-ai outcome row documented).
    - grep -qF "delivery: api" in auto-classify-rubric.md succeeds (api outcome row documented).
    - grep -qF "[reviewer-override:" in auto-classify-rubric.md succeeds (D-82 re-run preservation token documented in the override-mechanics section).
    - grep -qiF "Explicit triggers" in auto-classify-rubric.md succeeds.
    - grep -qiF "Operational principle" in auto-classify-rubric.md succeeds (backstop present).
    - grep -qiF "How this rubric is consumed" in auto-classify-rubric.md succeeds (consumer reference present).
    - grep -c numbered triggers in auto-classify-rubric.md returns >= 5 (5 numbered triggers present).
  </acceptance_criteria>
  <done>Rubric file authored per D-81 + STG4-05 — 5 triggers + escalation table + reviewer-override mechanics + re-run preservation rule + operational-principle backstop. P6 reachable via SKILL.md cross-reference (authored in T1).</done>
</task>

<task id="08-01-T4" type="auto">
  <name>Task 4: Author references/addendum-template.md (D-79 — Platform-API Addendum H2 body skeleton with error-paths discipline for API portions)</name>
  <files>dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md` (165 lines — body-shape baseline)
    - `dydx-delivery/skills/platform-pipefy/SKILL.md` + `platform-wrike/SKILL.md` + `platform-ziflow/SKILL.md` (API + rate limit + retry + idempotency conventions per Phase 6)
    - `.planning/DESIGN.md` §DESIGN-21 (Stage 5 scope-gate contract — addendum is v2.3 Stage 5 input on platform-only-with-API-rows topology)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-79 (verbatim authoring requirement)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.1 (sub-section enumeration: API surface inventory / Error paths / Retry + idempotency / Observability hooks)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md`. This is the D-79 body skeleton — `generate-fnspec-platform/SKILL.md` Step 5 authors the inline `## Platform-API Addendum` H2 body using this skeleton when D-79 conditions hold (no 4b in scope AND 4a has any `delivery: api` rows).

    **Pre-body documentation block** (HTML comment at top):
    Explain that this skeleton is appended INSIDE `04a_fnspec-platform_v<N>.md` when (a) no 4b is in scope AND (b) this 4a has any `delivery: api` rows. When emitted, 4a frontmatter ALSO carries `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only`. v2.3 Stage 5 reads these frontmatter fields, skips writing `05_techspec_v<N>.md`, and consumes the addendum body verbatim. Per D-79: error-paths discipline matches a full tech spec (API portions only). Every row continues to carry the D-82 `delivery:` markup (canonical enum order `native-ai | api`, never reversed).

    **Body H2 + sub-sections** (planner-authored within D-79 contract):

    1. `## Platform-API Addendum` — top heading (matches the H2 in the fnspec body where this is inlined).

       Opening paragraph: explain this addendum exists because (a) no Stage 4b integration fnspec is in scope, AND (b) this Stage 4a carries `delivery: api` requirement rows that need full tech-spec discipline. v2.3 Stage 5 consumes this body verbatim per the `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only` frontmatter contract (DESIGN-21 + ROUTE-03).

    2. `### API surface inventory` — **table with columns `Endpoint | Method | Auth | Rate limit | Delivery`**:
       - Every row enumerates a specific API surface touched by the platform-side requirements.
       - `Delivery` column carries the D-82 markup verbatim — typically all `delivery: api [...]` here (this addendum exists BECAUSE 4a has api rows; native-ai rows belong in the main fnspec body, not the addendum).
       - Auth column documents the platform's auth surface (Pipefy: GraphQL OAuth bearer; Wrike: OAuth2; Ziflow: API key).
       - Rate-limit column documents per-platform limits (Pipefy: paginate_all per Phase 6; Wrike: TBD; Ziflow: 4 req / 10s per Q05 decided).

    3. `### Error paths` — **table with columns `HTTP class | Expected behaviour | Per-platform error mapping | Reviewer-facing message`**:
       - Enumerate 4xx (validation, auth, rate-limit) + 5xx (transient, persistent) error classes.
       - Per-platform error mapping documents how the loaded platform's API surface signals each error class.
       - Reviewer-facing message documents what gets surfaced inline in the 4a artefact when the error path fires.
       - This section is the D-79 "error-paths discipline" — MUST be concrete (not "handle errors gracefully" placeholder prose) per D-79 wording.

    4. `### Retry + idempotency rules` — Bulleted list:
       - Which API operations are idempotent (safe to retry on transient failure)?
       - Which are NOT idempotent (require explicit dedupe key)?
       - Default retry policy (exponential backoff, max attempts, per-platform deviation).
       - Idempotency-key field name + format (per platform).

    5. `### Observability hooks` — Bulleted list:
       - Where the platform-side API operation logs (request ID, trace ID, response status).
       - What gets surfaced into the artefact's audit trail (timestamps, response codes, error excerpts).
       - Reviewer-visible vs internal-only observations.

    **Footer note** — italic line: "This addendum is the v2.3 Stage 5 input on the platform-only-with-API-rows topology (DESIGN-21 + ROUTE-03 + D-79). Stage 5 v2.3 consumes the body verbatim and does not re-author. Every row in the API surface inventory table carries D-82 markup so the routing key survives forward into Stage 5 / 6 / 7b / 10 per ROUTE-05."

    Discretion: planner authors the exact prose body within the section shape above. Concrete API examples may reference one or more of the v2.1 platforms (Pipefy / Wrike / Ziflow) by name for grounding, but the skeleton must remain platform-agnostic enough to apply to any of the 4 D-78 enum values (including `other`).
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md && grep -qF '## Platform-API Addendum' dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md && grep -qF 'has_platform_api_addendum: true' dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md && grep -qF 'tech_spec_scope: platform-api-addendum-only' dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md && grep -qF 'delivery: api' dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the target path (P2 contribution + P7 reachable).
    - grep -qF "## Platform-API Addendum" in addendum-template.md succeeds (D-79 H2 carrier present).
    - grep -qF "has_platform_api_addendum: true" in addendum-template.md succeeds (P8 cross-reference — addendum body documents when to emit).
    - grep -qF "tech_spec_scope: platform-api-addendum-only" in addendum-template.md succeeds (P8 cross-reference).
    - grep -qF "delivery: api" in addendum-template.md succeeds (D-82 markup present in API surface inventory table).
    - grep -qiE "Error paths" in addendum-template.md succeeds (D-79 error-paths discipline section present).
    - grep -qiE "(Retry|idempoten)" in addendum-template.md succeeds (retry + idempotency section present).
    - grep -qiE "Observability" in addendum-template.md succeeds (observability hooks section present).
    - grep -qiE "API surface inventory" in addendum-template.md succeeds.
  </acceptance_criteria>
  <done>Addendum-template authored per D-79 + DESIGN-21 — `## Platform-API Addendum` H2 + 4 sub-sections + D-82 markup on API rows + emit-when-conditions documented. P7 reachable via SKILL.md cross-reference (authored in T1).</done>
</task>

<task id="08-01-T5" type="auto">
  <name>Task 5: Plan-local 4a-section gate — run the P1-P8 proxy assertions as a bash one-liner</name>
  <files></files>
  <read_first>
    - All 4 files authored in T1-T4
    - `.planning/phases/08-stage-4-fnspec-split-route/08-VALIDATION.md` §1 (P1-P8 assertion contract)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.3 partition table (P1-P8 verbatim)
  </read_first>
  <action>
    Run the P1-P8 proxy assertions as a bash one-liner against the 4 files authored in T1-T4. This is the plan-local gate — `phase8-structure-check.sh --section 4a` does not exist yet (ships in 08-03), so this one-liner anticipates the structure-check assertion block verbatim.

    Run this exact bash command (single shell invocation; on first failure, fix the source file and re-run — the assertion strings ARE the contract):

    set -euo pipefail
    SK=dydx-delivery/skills/generate-fnspec-platform/SKILL.md
    TP=dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md
    RU=dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md
    AD=dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md
    test -f "$SK" && echo "PASS P1: SKILL.md exists" || { echo "FAIL P1"; exit 1; }
    for f in "$TP" "$RU" "$AD"; do test -f "$f" || { echo "FAIL P2: $f missing"; exit 1; }; done; echo "PASS P2: 3 references/ files exist"
    for c in safety-rules.md stage-numbering.md frontmatter-scheme.md glossary.md; do grep -qF "dydx-delivery/references/$c" "$SK" || { echo "FAIL P3: $c not in SKILL.md"; exit 1; }; done; echo "PASS P3: 4 canonical pointers present"
    grep -qF 'pipefy | wrike | ziflow | other' "$SK" && grep -qF 'pipefy | wrike | ziflow | other' "$TP" && echo "PASS P4" || { echo "FAIL P4"; exit 1; }
    grep -qF 'native-ai | api' "$SK" && ! grep -qF 'api | native-ai' "$SK" && echo "PASS P5" || { echo "FAIL P5"; exit 1; }
    grep -qF 'references/auto-classify-rubric.md' "$SK" && echo "PASS P6" || { echo "FAIL P6"; exit 1; }
    grep -qF 'references/addendum-template.md' "$SK" && echo "PASS P7" || { echo "FAIL P7"; exit 1; }
    grep -qF 'has_platform_api_addendum' "$SK" && grep -qF 'tech_spec_scope' "$SK" && echo "PASS P8" || { echo "FAIL P8"; exit 1; }
    echo "ALL P1-P8 ASSERTIONS PASSED — 4a section ready for 08-03 wire-up"

    Expected output: 8 PASS lines (P1-P8) followed by "ALL P1-P8 ASSERTIONS PASSED — 4a section ready for 08-03 wire-up". Exit code 0.

    If any assertion fails, the executor fixes the source file (NOT the assertion — the assertion strings are the contract that 08-03 will wire into `phase8-structure-check.sh run_4a_section()`).

    **NOTE:** This gate does NOT cover assertion I8 (two-place key-decisions cross-reference) — that is 08-02's responsibility to assert (since I8 requires BOTH 4a + 4b SKILL.md key-decisions sections to exist). T1 above authored the 4a half of the cross-reference; 08-02 authors the 4b half + asserts the cross-reference at its plan close.
  </action>
  <verify>
    <automated>set -euo pipefail; SK=dydx-delivery/skills/generate-fnspec-platform/SKILL.md; TP=dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md; RU=dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md; AD=dydx-delivery/skills/generate-fnspec-platform/references/addendum-template.md; test -f "$SK" && test -f "$TP" && test -f "$RU" && test -f "$AD" && for c in safety-rules.md stage-numbering.md frontmatter-scheme.md glossary.md; do grep -qF "dydx-delivery/references/$c" "$SK"; done && grep -qF 'pipefy | wrike | ziflow | other' "$SK" && grep -qF 'pipefy | wrike | ziflow | other' "$TP" && grep -qF 'native-ai | api' "$SK" && ! grep -qF 'api | native-ai' "$SK" && grep -qF 'references/auto-classify-rubric.md' "$SK" && grep -qF 'references/addendum-template.md' "$SK" && grep -qF 'has_platform_api_addendum' "$SK" && grep -qF 'tech_spec_scope' "$SK"</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0 on the one-liner verify command.
    - All 8 PASS lines (P1-P8) emitted to stdout when run interactively.
    - Files produced: 4 new files at the `files_created` paths.
  </acceptance_criteria>
  <done>P1-P8 proxy gate passes — 4a section is ready for 08-03 to wire `run_4a_section()` against. 08-02 can begin Wave 2 with a stable 4a upstream contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer → generate-fnspec-platform skill | Reviewer invokes skill against approved discovery + approved SOW. Inputs are reviewer-approved artefacts, not external attacker input. Trust posture: low risk. |
| loaded platform skill's `native-ai-inventory.md` → classifier | DESIGN-14/15/16 inventory file is plugin-internal (Phase 6 product). Trust = internal canonical reference. |
| 4a write-path → filesystem | Skill writes `04a_fnspec-platform_v<N>.md` to `<Client> Brain/<Project>/`. Path components come from frontmatter supplied by reviewer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | fnspec-platform-template.md `platform:` enum value drift (e.g., `pipefy | wrike | other` regression after D-78) | mitigate | P4 assertion (08-03 wires into `phase8-structure-check.sh run_4a_section()`) asserts the literal token `pipefy | wrike | ziflow | other` in SKILL.md AND template. Drift fails-loud at structure-check. T5 one-liner asserts the same contract before 08-02 can begin. |
| T-08-02 | Tampering | D-82 row markup enum order regression (e.g., `delivery: api | native-ai` after a future edit) | mitigate | P5 asserts canonical `native-ai | api` order AND absence of `api | native-ai` in SKILL.md (STG4-04 lock). The `! grep -qF 'api | native-ai'` complementary check catches reversed-enum regressions. |
| T-08-03 | Tampering | `[reviewer-override:]` token drift breaks re-run preservation | mitigate | D-82 token documented verbatim in SKILL.md + template + rubric (3 places). T1+T2+T3 acceptance criteria each grep-assert the literal token. |
| T-08-04 | Tampering | D-79 frontmatter field rename (e.g., `has_platform_api_addendum` → other spelling) | mitigate | P8 asserts both literal field names in SKILL.md AND template. v2.3 Stage 5 will consume these frontmatter fields; rename breaks the forward-compat chain. |
| T-08-05 | Information disclosure | Reviewer-supplied `client:` / `project:` path traversal | accept | Reviewer is the operator; no untrusted writer. Future `validate-frontmatter` hook (v2.6 / SURF-01..03) will sanitise path components; deferred per scope-lock (same posture as Phase 7 T-07-03). |

ASVS L1 alignment: doc-only skill authoring with no execution surface; primary threats are tampering of canonical strings (mitigated via P1-P8 grep gates) and routing-key regression (mitigated via complementary `! grep` checks against reversed enum). No native-AI ingestion API in scope per UAT-6.1.
</threat_model>

<verification>
- **Plan-local (T5):** The P1-P8 one-liner above exits 0 with all 8 PASS lines.
- **Cross-plan deferred:** I8 (two-place key-decisions cross-reference between 4a + 4b SKILL.md) is 08-02's responsibility. T1 here authored the 4a half (D-78 / D-79 / D-82 / D-84 / D-85 named in `## Key decisions`); 08-02's plan-close gate asserts both halves cross-reference correctly.
- **Cross-plan deferred:** P1-P8 wired into `phase8-structure-check.sh run_4a_section()` during 08-03. Assertion strings in T5 above MUST match what 08-03 wires verbatim.
- **Manual:** Spot-check that all 4 canonical-pointer paths in SKILL.md actually exist on disk at `dydx-delivery/references/{safety-rules,stage-numbering,frontmatter-scheme,glossary}.md`.
- **Manual:** Read SKILL.md Key decisions block end-to-end; confirm D-78/D-79/D-82/D-84/D-85 are each named with a one-line description matching CONTEXT.md `<decisions>` section.
- **Forbidden:** Do NOT run `bash phase8-structure-check.sh --section 4a` in 08-01 — the script does not exist yet. T5's one-liner is the plan-local gate.
</verification>

<success_criteria>
- 4 new files exist at the paths in `files_created`.
- T5 one-liner exits 0 with 8 PASS lines (P1-P8).
- STG4-01 + STG4-04 (4a half) + STG4-05 + ROUTE-03 (4a addendum body authorship) requirement bodies are addressable via the P1-P8 assertion contract (per `08-VALIDATION.md` §1 mapping).
- 08-02 can begin Wave 2 with a stable 4a frontmatter contract + `04a_fnspec-platform_v*.md` upstream artefact shape locked.
- `## Key decisions` section authored in 4a SKILL.md naming D-78/D-79/D-82/D-84/D-85 — the 4a half of the T-02-06-02 / ROUTE-01 two-place cross-reference; 08-02 authors the matching 4b half.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stage-4-fnspec-split-route/08-01-SUMMARY.md` recording:
- Files created (4)
- T5 one-liner exit code (0 expected) + count of PASS lines (8 expected: P1-P8)
- Concrete `platform:` enum value chosen in `fnspec-platform-template.md` (default `pipefy` — reviewer switches at write time)
- D-82 markup example row(s) used in template sections 4 / 5 / 8 (record the literal text used)
- D-79 addendum-template body subsection structure chosen (API surface inventory / Error paths / Retry + idempotency / Observability hooks — confirm all 4 present)
- D-81 MEDIUM trigger threshold chosen in `auto-classify-rubric.md` (default `>=2 distinct verified MCP probes`; record actual choice if adjusted)
- Verbatim handoff-message string locked (so 08-02 + 08-03 structure-check can grep against it)
- Any deviation from planner-default trigger wording or addendum sub-section enumeration
</output>
