---
plan: 08-02
phase: 8
slug: stage-4-fnspec-split-route
wave: 2
depends_on: [08-01]
files_modified: []
files_created:
  - dydx-delivery/skills/generate-fnspec-integration/SKILL.md
  - dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md
  - dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md
  - dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md
files_deleted: []
autonomous: true
requirements: [STG4-02, STG4-04, ROUTE-01, ROUTE-02, ROUTE-03]
must_haves:
  truths:
    - "Reviewer can invoke `generate-fnspec-integration` against approved `02_discovery_v*` + approved `03_sow_v*` + approved `04a_fnspec-platform_v*` and receive `04b_fnspec-integration_v<N>.md` per DESIGN-02 (STG4-02; assertion I1)"
    - "Three consistency checks run FIRST before any 4b write, per D-84 + ROUTE-01: (a) no requirement ID with conflicting `delivery:` tags across 4a and 4b; (b) every 4b integration touchpoint cites a 4a requirement ID (no dangling refs); (c) no orphan API endpoints in 4b (every endpoint maps to a requirement) — assertion I6"
    - "On consistency-check failure, Stage 4b emits `04b_consistency_check_v<N>.md` listing failure rows + HALTS before any `04b_fnspec-integration_v*.md` write per ROUTE-02 + D-84 (assertion I7)"
    - "Two-place key-decisions cross-reference: BOTH 4a + 4b SKILL.md `## Key decisions` sections name D-78 / D-79 / D-82 / D-84 / D-85 per T-02-06-02 / ROUTE-01 (assertion I8)"
    - "Either-spec-skip topology detected via (a) presence/absence of `04a_fnspec-platform_v*.md`, (b) SOW `## Platform Scope` / `## Integration Scope` H2s, (c) explicit reviewer signal at invocation; when out-of-scope, emits verbatim `Stage 4b SKIPPED — no integration work in scope` to stdout and writes NO artefact per D-85 (assertion I5)"
    - "Every integration-touchpoint / API-endpoint / acceptance-criteria row in the 4b template carries `delivery: native-ai | api` markup in canonical enum order per STG4-04 + D-82"
    - "SKILL.md cites all 4 canonical references (safety-rules / stage-numbering / frontmatter-scheme / glossary) per FOUND-04 + D-59 (assertion I3)"
    - "3 `references/` files exist per D-83: `fnspec-integration-template.md` + `consistency-rules.md` + `either-spec-skip-paths.md` (assertion I2)"
    - "`based_on_fnspec_platform:` frontmatter field documented in 4b SKILL.md + template (assertion I4 + ROUTE-03 4b read-side)"
    - "`platform:` enum in 4b SKILL.md + template carries the 4-value form `pipefy | wrike | ziflow | other` per D-78 (baked in from authorship)"
  artifacts:
    - path: "dydx-delivery/skills/generate-fnspec-integration/SKILL.md"
      provides: "Stage 4b skill body — invocation triggers, inputs, output, How-to-run steps (with consistency-check FIRST + halt-path + skip-path), Key decisions, What-it-does-not-do, Quality bar"
      contains: "4 canonical-pointer sentences, `platform: pipefy | wrike | ziflow | other`, `based_on_fnspec_platform:` frontmatter contract, verbatim D-85 skip-emit string `Stage 4b SKIPPED — no integration work in scope`, references to all 3 references/ files, `## Key decisions` section naming D-78/D-79/D-82/D-84/D-85 (T-02-06-02 mitigation half), 7-9 numbered How-to-run steps with Step 2 either-spec-skip detection AND Step 3 consistency checks FIRST (D-84 halt-on-failure protocol), `04b_consistency_check_v<N>.md` halt-path output contract documented"
    - path: "dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md"
      provides: "Artefact body template for `04b_fnspec-integration_v<N>.md`"
      contains: "Frontmatter block: `platform: pipefy | wrike | ziflow | other` + `based_on_discovery:` + `based_on_sow:` + `based_on_fnspec_platform:` (conditional — present when 4a exists; absent on 4b-only topology) + canonical 4-state status lifecycle + `approved_by` + `approved_at` + 10 H2 sections with integration-touchpoints table + dedicated API-endpoints sub-section per ROUTE-01 check (c) + `Delivery` column carrying D-82 markup pattern on every row"
    - path: "dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md"
      provides: "D-84 + ROUTE-01 — three consistency checks + halt-on-failure protocol + `04b_consistency_check_v<N>.md` failure-report template"
      contains: "Ownership statement (Stage 4b runs FIRST per D-84); three checks ordered (a/b/c with concrete trigger logic); halt-on-failure protocol (emit `04b_consistency_check_v<N>.md` + exit before fnspec write); failure-report frontmatter contract (`artefact_type: consistency_check_failure` + `checks_run: 3` + `checks_failed: <N>` + `status: halt`); failure-row table template (`Check ID | Failure type | Row reference | Detail | Suggested resolution`); reviewer-retry mechanics; two-place key-decisions cross-reference paragraph"
    - path: "dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md"
      provides: "STG4-06 + D-85 — three-topology rules + verbatim skip-emit string + topology detection logic"
      contains: "STG4-06 three-topology table (4a-only / 4b-only / both — input detection signals + behavioural contract per topology); D-85 verbatim skip-emit string locked: `Stage 4b SKIPPED — no integration work in scope`; D-75 SOW signal documentation (`## Platform Scope` / `## Integration Scope` H2s read for topology hints); reviewer-override path when SOW signal is ambiguous; forward-compat note (4a-only → Stage 5 v2.3 reads addendum per D-79; 4b-only → Stage 5 reads only 4b; both → Stage 5 reads chain)"
  key_links:
    - from: "dydx-delivery/skills/generate-fnspec-integration/SKILL.md"
      to: "dydx-delivery/references/{safety-rules,stage-numbering,frontmatter-scheme,glossary}.md"
      via: "4 inline canonical-pointer sentences (FOUND-04 / D-59)"
      pattern: "dydx-delivery/references/.*\\.md"
    - from: "dydx-delivery/skills/generate-fnspec-integration/SKILL.md"
      to: "dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md"
      via: "Step 3 body reference; consistency-check ownership (D-84)"
      pattern: "references/consistency-rules.md"
    - from: "dydx-delivery/skills/generate-fnspec-integration/SKILL.md"
      to: "dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md"
      via: "Step 2 body reference; either-spec-skip detection (D-85 + STG4-06)"
      pattern: "references/either-spec-skip-paths.md"
    - from: "dydx-delivery/skills/generate-fnspec-integration/SKILL.md"
      to: "dydx-delivery/skills/generate-fnspec-platform/SKILL.md"
      via: "Two-place key-decisions cross-reference (D-78/D-79/D-82/D-84/D-85 named in BOTH SKILL.md Key decisions sections per T-02-06-02)"
      pattern: "## Key decisions"
    - from: "dydx-delivery/skills/generate-fnspec-integration/SKILL.md"
      to: "dydx-delivery/skills/generate-fnspec-platform/ output (04a_fnspec-platform_v*.md)"
      via: "Step 1 — Locate approved upstream artefacts; 4a is one of the 3 inputs (alongside discovery + SOW)"
      pattern: "04a_fnspec-platform_v"
threat_model: {}
---

<objective>
Author Stage 4b `generate-fnspec-integration/` skill end-to-end — SKILL.md body + 3 `references/` files (fnspec-integration-template / consistency-rules / either-spec-skip-paths) — locking the 4b frontmatter contract, the three D-84 consistency checks (run FIRST before any write), the D-85 either-spec-skip mechanics with verbatim skip-emit string, and the 4b half of the T-02-06-02 / ROUTE-01 two-place key-decisions cross-reference.

Purpose: Close the routing-key consumer tier. Stage 4b reads 4a's `delivery:` row markup as input; runs three consistency checks before any 4b write; halts on conflict with explicit `04b_consistency_check_v<N>.md` failure report; skips with verbatim stdout string + no file on either-spec-skip topology. Locks the contract 08-03 wires into `phase8-structure-check.sh run_4b_section()` (I1-I8 assertions).

Output: 4 new files (1 SKILL.md + 3 references/). 08-03 closeout begins immediately after this plan ships.

Wave 2 — depends_on: [08-01]. 4b reads approved 4a output; cannot start in parallel with 08-01.
</objective>

<plan_warning>
**CRITICAL — phase8-structure-check.sh does NOT exist yet during 08-02.**

The Phase 8 structure-check shell script ships in 08-03 (per D-86 / RESEARCH §2.3 partition table). During 08-02 execution, there is NO automated `--section 4b` gate to run. Plan 08-02's verification posture mirrors 08-01:

1. **Per-task `<verify>` commands** below are direct `grep -qF` / `grep -qE` / `test -f` assertions against the files this plan authors.

2. **Final plan gate (T5)** is a sequence of greps run at plan close — every assertion I1-I8 from `08-VALIDATION.md` §1 reachable via direct grep against the 4 files authored here PLUS the 4a SKILL.md authored in 08-01 (for the I8 two-place cross-reference check).

3. The full structure-check `--section 4b` PASS-line gate runs at the END of 08-03 once `phase8-structure-check.sh` is on disk. Anticipate every I1-I8 assertion verbatim here.

**The I1-I8 assertion authorship contract authored here MUST match what 08-03 wires into `run_4b_section()`.** Cross-check the assertion strings in this plan against the partition table in `08-VALIDATION.md` §1 + `08-RESEARCH.md` §2.3.

**Plus — 08-02 has a unique cross-plan dependency check:** I8 (two-place key-decisions) checks BOTH 4a SKILL.md (authored in 08-01) AND 4b SKILL.md (authored here). T5 below scans BOTH files; if 08-01's `## Key decisions` block is missing any of D-78/D-79/D-82/D-84/D-85, this plan flags it as an 08-01 regression rather than fixing 4b. The fix lives upstream.
</plan_warning>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md
@.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md
@.planning/phases/08-stage-4-fnspec-split-route/08-VALIDATION.md
@.planning/phases/08-stage-4-fnspec-split-route/08-01-PLAN.md
@.planning/phases/08-stage-4-fnspec-split-route/08-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@dydx-delivery/skills/generate-fnspec-platform/SKILL.md
@dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md
@dydx-delivery/skills/generate-fnspec-platform/references/auto-classify-rubric.md
@dydx-delivery/skills/generate-functional-spec/SKILL.md
@dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md
@dydx-delivery/skills/discovery-intake/SKILL.md
@dydx-delivery/skills/generate-sow/SKILL.md
@dydx-delivery/skills/generate-sow/references/sow-template.md
@dydx-delivery/skills/kickoff-capture/SKILL.md
@dydx-delivery/references/frontmatter-scheme.md
@dydx-delivery/references/glossary.md

<interfaces>
<!-- Verbatim strings the future structure-check (ships in 08-03) will assert via `grep -qF`. Every executor edit MUST preserve these literally. -->

**4b artefact frontmatter contract (DESIGN-20 + D-78 + DESIGN-01 + DESIGN-06):**
```yaml
---
client: <CLIENT_NAME>
project: <PROJECT_NAME>
frontmatter_version: 2
platform: <pipefy | wrike | ziflow | other>             # D-78 — 4-enum baked in
based_on_discovery: 02_discovery_v<N>.md                 # MANDATORY when discovery exists
based_on_sow: 03_sow_v<N>.md                             # MANDATORY (single SOW path)
based_on_fnspec_platform: 04a_fnspec-platform_v<N>.md    # CONDITIONAL — present when 4a exists ('both' topology); absent on 4b-only topology
status: draft                                            # canonical 4-state: draft → client_review → approved → archived
approved_by: <name-or-handle>                            # MANDATORY on approved
approved_at: <ISO date>                                  # MANDATORY on approved
generated_at: <ISO date>
---
```

**4b consistency-check halt-path frontmatter contract (ROUTE-02 + D-84):**
```yaml
---
client: <CLIENT_NAME>
project: <PROJECT_NAME>
frontmatter_version: 2
artefact_type: consistency_check_failure
based_on_fnspec_platform: 04a_fnspec-platform_v<N>.md
based_on_attempted_fnspec_integration: <intended-04b-filename-or-N/A-if-pre-write>
checks_run: 3
checks_failed: <N>
status: halt
generated_at: <ISO date>
---
```
Body lists failure rows in a table: `Check ID | Failure type | Row reference | Detail | Suggested resolution`.

**D-85 verbatim skip-emit string (locked at plan-write time per CONTEXT.md Discretion + RESEARCH A5):**
```
Stage 4b SKIPPED — no integration work in scope
```
- Em-dash `—` verbatim (NOT hyphen `-`).
- Emitted to stdout ONLY; no `04b_*` artefact written.
- I5 assertion grep-asserts this exact string in SKILL.md OR `either-spec-skip-paths.md`.

**Three consistency checks (ROUTE-01 + D-84) — must appear named in both SKILL.md AND consistency-rules.md:**
- **Check (a)** — no requirement ID with conflicting `delivery:` tags across 4a and 4b.
- **Check (b)** — every 4b integration touchpoint cites a referenced platform requirement ID from 4a (no dangling refs).
- **Check (c)** — no orphan API endpoints in 4b (every endpoint maps to a requirement).

**Two-place key-decisions block (T-02-06-02 / ROUTE-01 mitigation; 4b half — must MATCH the 4a block in `generate-fnspec-platform/SKILL.md`):**
```markdown
## Key decisions

- **D-78 — TD-2 resolution.** Stage-skill `platform:` enum is `pipefy | wrike | ziflow | other`. Ziflow is wired through as a routing-key value per `platform-ziflow/SKILL.md`.
- **D-79 — Platform-API Addendum carrier (ROUTE-03).** Stage 4a owns the addendum body authorship; this 4b reads `has_platform_api_addendum:` + `tech_spec_scope:` from 4a frontmatter on the read-side (no write-side responsibility here).
- **D-82 — Per-row routing markup.** Every integration-touchpoint / API-endpoint / acceptance-criteria row carries `delivery: native-ai | api [<confidence>, src: <platform>]` in canonical enum order. 4b does NOT re-classify rows — reads 4a's per-row tags as-is per D-81 closing clause.
- **D-84 — Consistency check ownership.** This 4b OWNS the three cross-spec consistency checks; runs them FIRST before any fnspec write. On any failure → emit `04b_consistency_check_v<N>.md` + HALT.
- **D-85 — Either-spec-skip mechanics.** This 4b detects either-spec-skip via SOW H2s + presence/absence of 4a output. When out-of-scope → emit verbatim `Stage 4b SKIPPED — no integration work in scope` to stdout, write NO artefact, exit.
```

**Verbatim handoff message (planner-authored within STG4 contracts; lock at write time):**
```
Awaiting status: approved on 04b_fnspec-integration_v<N>.md. Stage 5 v2.3 reads both 4a + 4b via the based_on_* chain (based_on_discovery + based_on_sow + based_on_fnspec_platform).
```

**Anticipated I1-I8 assertion gate (08-03 wires these into `run_4b_section()`; planner authors against this contract):**
- I1 — `dydx-delivery/skills/generate-fnspec-integration/SKILL.md` exists.
- I2 — 3 references/ files exist (`fnspec-integration-template.md`, `consistency-rules.md`, `either-spec-skip-paths.md`).
- I3 — 4 canonical-pointer paths present in 4b SKILL.md.
- I4 — `based_on_fnspec_platform:` documented in 4b SKILL.md / template.
- I5 — Verbatim skip-emit string `Stage 4b SKIPPED — no integration work in scope` present in SKILL.md OR `either-spec-skip-paths.md`.
- I6 — 3 consistency checks named in `consistency-rules.md`.
- I7 — Halt-on-failure + `04b_consistency_check_v<N>.md` failure-artefact contract documented in SKILL.md AND consistency-rules.md.
- I8 — Two-place key-decisions cross-reference: BOTH 4a SKILL.md + 4b SKILL.md `## Key decisions` sections name D-78 AND D-79 AND D-82 AND D-84 AND D-85.
</interfaces>
</context>

<tasks>

<task id="08-02-T1" type="auto">
  <name>Task 1: Author generate-fnspec-integration/SKILL.md (canonical pointers + Inputs/Output + 7-9 How-to-run steps with consistency-check FIRST + halt-path + skip-path + Key decisions block + handoff)</name>
  <files>dydx-delivery/skills/generate-fnspec-integration/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-fnspec-platform/SKILL.md` (08-01 product — direct sibling template for canonical-pointer block + How-to-run shape + Key decisions block; LIFT the 4-pointer block verbatim shape + adapt Inputs)
    - `dydx-delivery/skills/discovery-intake/SKILL.md` skip-emit body (Phase 7 D-74 — direct template for D-85 verbatim skip-emit pattern: stdout-only + no file written + exit)
    - `dydx-delivery/skills/generate-functional-spec/SKILL.md` (113 lines — RETIRING; Step shape baseline for sections 4-6)
    - `dydx-delivery/skills/discovery-intake/SKILL.md:23-29` (Step 1 — Locate upstream artefact triage; lift start-at-any-point pattern)
    - `.planning/codebase/CONVENTIONS.md` § "SKILL.md body structure"
    - `.planning/DESIGN.md` §DESIGN-20 (Stage 4b consistency-check contract) + §DESIGN-21 (Stage 5 scope-gate)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-78 / D-82 / D-84 / D-85
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.2 (Step body sketch lines 273-281 — full 7-step lift target)
    - `dydx-delivery/references/frontmatter-scheme.md` (canonical `based_on_*` chain convention)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-integration/SKILL.md` per CONVENTIONS.md SKILL.md body-shape canon. Use 4a SKILL.md (08-01 product) as the direct sibling template — same shape, adapted for integration scope + consistency-check ownership.

    **Skill frontmatter** (top of file, between `---` delimiters):
    ```yaml
    name: generate-fnspec-integration
    description: Generate the Stage 4b integration functional spec. Use when the user says "generate fnspec-integration for X", "draft 4b", "integration fnspec", or asks for a functional spec scoped to system-to-system integrations after discovery + SOW + 4a are approved. Reads approved 02_discovery_v* + approved 03_sow_v* + approved 04a_fnspec-platform_v*. Runs three cross-spec consistency checks FIRST before any write; halts with 04b_consistency_check_v<N>.md on failure. On either-spec-skip topology, emits the verbatim skip-emit string to stdout and writes no artefact. Writes 04b_fnspec-integration_v<N>.md on clean check + integration-in-scope path with every row carrying the delivery: native-ai | api routing key.
    ```

    **Body sections in this order** (per CONVENTIONS.md):

    1. `# generate-fnspec-integration` — H1 matching directory name.

    2. **One-paragraph purpose statement.** Reference "Stage 4b of the dydx-delivery pipeline"; name the three responsibilities (consistency-check ownership / either-spec-skip detection / integration fnspec authorship); name the output artefact `04b_fnspec-integration_v<N>.md` plus halt-path artefact `04b_consistency_check_v<N>.md`; name the verbatim D-85 skip-emit string `Stage 4b SKIPPED — no integration work in scope`.

    3. **4 canonical-pointer block quotes** (lift verbatim shape from 4a SKILL.md; adapt topic-line wording for 4b's role — I3 asserts each substring `dydx-delivery/references/<file>.md`):
       - Hard rules pointer → safety-rules.md
       - Stage numbering pointer → stage-numbering.md (note: writes `04b_fnspec-integration_v<N>.md` AND conditionally `04b_consistency_check_v<N>.md`)
       - Frontmatter scheme pointer → frontmatter-scheme.md (emits `platform: <pipefy | wrike | ziflow | other>` + `based_on_discovery:` + `based_on_sow:` + `based_on_fnspec_platform:` (conditional))
       - Glossary pointer → glossary.md (canonical terms: `delivery: native-ai | api`, `platform: pipefy | wrike | ziflow | other`, `based_on_fnspec_platform`, `Stage 4b SKIPPED — no integration work in scope`)
       Each pointer MUST appear as the literal substring `dydx-delivery/references/<file>.md`. The Frontmatter pointer block names `based_on_fnspec_platform` literally (helps satisfy I4).

    4. `## Inputs` — bulleted list per STG4-02 + DESIGN-20:
       - Approved `02_discovery_v<N>.md` (status: approved enforced).
       - Approved `03_sow_v<N>.md` (status: approved enforced) — MANDATORY.
       - Approved `04a_fnspec-platform_v<N>.md` — CONDITIONAL (present on 'both' topology; absent on 4b-only topology). When present, drives the consistency-check input.
       - Explicit reviewer signal at invocation when SOW H2 topology signal is ambiguous (D-85 fallback).

    5. `## Output` — primary: `<Client> Brain/<Project>/04b_fnspec-integration_v<N>.md` per DESIGN-02 (on clean consistency-check + integration-in-scope path). Halt-path: `<Client> Brain/<Project>/04b_consistency_check_v<N>.md` per ROUTE-02 + D-84 (frontmatter contract per `<interfaces>` above). Skip-path: stdout-only verbatim `Stage 4b SKIPPED — no integration work in scope`; NO artefact written per D-85.

    6. `## How to run` — author 7 numbered steps lifting from RESEARCH.md §2.2 lines 273-281:
       - `### Step 1 — Locate approved upstream artefacts` — find highest-version approved `02_discovery_v*` + approved `03_sow_v*` + approved `04a_fnspec-platform_v*.md` if present. Start-at-any-point triage on missing inputs.
       - `### Step 2 — Either-spec-skip detection (D-85 + STG4-06)` — combine signals: (a) presence/absence of `04a_*`; (b) SOW `## Platform Scope` + `## Integration Scope` H2 content (Phase 7 D-75 signal); (c) explicit reviewer signal at invocation. If determined out-of-scope → emit the verbatim string `Stage 4b SKIPPED — no integration work in scope` to stdout, write NO artefact, exit. Topology rules cited from `references/either-spec-skip-paths.md` (relative path).
       - `### Step 3 — Run consistency checks FIRST (D-84 + ROUTE-01, before any write)` — three checks against approved 4a output + draft 4b plan. **MUST name all three checks (a/b/c) inline in the step body** so I6 catches them in SKILL.md too:
         - (a) no requirement ID with conflicting `delivery:` tags across 4a and 4b.
         - (b) every 4b integration touchpoint cites a referenced platform requirement ID from 4a (no dangling refs).
         - (c) no orphan API endpoints in 4b (every endpoint maps to a requirement).
         On any failure → write `04b_consistency_check_v<N>.md` (frontmatter per `<interfaces>` halt-path contract; body lists failure rows in `Check ID | Failure type | Row reference | Detail | Suggested resolution` table), emit halt message, exit. NO 4b fnspec written. Quote the halt artefact filename literally (helps I7). Reference `references/consistency-rules.md` by relative path (I7 contributes).
       - `### Step 4 — Check for existing 4b artefact + re-run policy` — if `04b_fnspec-integration_v*.md` exists, ask reviewer revise / extend / fresh; preserve `[reviewer-override:]` tokens per D-82.
       - `### Step 5 — Draft the integration fnspec body` — walk SOW `## Integration Scope` touchpoint-by-touchpoint; produce sections 1-10 per `references/fnspec-integration-template.md`. Apply D-82 row markup using 4a's classifications as input (no independent classifier — Stage 4b reads 4a tags as-is per D-81 closing clause). Quote `delivery: native-ai | api` verbatim (canonical order; helps cross-section X2).
       - `### Step 6 — Senior-level challenge` — cross-check integration touchpoints against discovery's data-flow narrative; verify every API endpoint has explicit error-path rules; surface any orphan touchpoints not in the SOW.
       - `### Step 7 — Write and hand off` — write to `<Client> Brain/<Project>/04b_fnspec-integration_v<N>.md`. Emit the verbatim handoff message from `<interfaces>` block.

    7. `## Key decisions` — author the two-place key-decisions block from the `<interfaces>` section verbatim (T-02-06-02 / ROUTE-01 mitigation — naming D-78 / D-79 / D-82 / D-84 / D-85). This block PLUS 4a's matching block (authored in 08-01 T1) together satisfy assertion I8.

    8. `## What this skill does not do`:
       - Does NOT re-classify rows — reads 4a's per-row `delivery:` tags as-is per D-81 closing clause.
       - Does NOT author the Platform-API Addendum body — that is 4a's responsibility per D-79.
       - Does NOT auto-progress to Stage 5 — approval gate non-negotiable per DESIGN-06.
       - Does NOT write `04b_fnspec-integration_v*.md` when consistency checks fail — halt-path emits `04b_consistency_check_v*.md` ONLY (D-84 halt-on-failure).
       - Does NOT write any `04b_*` artefact on either-spec-skip topology — stdout skip-emit only (D-85).

    9. `## Quality bar` — bulleted "good looks like":
       - Three consistency checks run FIRST before any fnspec write (D-84).
       - Every row in integration-touchpoints / API-endpoints / acceptance-criteria tables carries D-82 `delivery:` markup (canonical order).
       - Halt-path artefact `04b_consistency_check_v<N>.md` lists every failure row with a Suggested resolution column (no generic "fix it" prose).
       - Skip-path emits the verbatim D-85 string + writes NO file.
       - 4-canonical-references all resolve.
       - Handoff message emitted verbatim on clean path.

    Quote the D-85 verbatim skip-emit string at least once in the body (Step 2 explicit; also fine to repeat in Quality bar) so I5 grep-asserts it from SKILL.md directly.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-integration/SKILL.md && for c in safety-rules.md stage-numbering.md frontmatter-scheme.md glossary.md; do grep -qF "dydx-delivery/references/$c" dydx-delivery/skills/generate-fnspec-integration/SKILL.md; done && grep -qF 'references/consistency-rules.md' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF 'references/either-spec-skip-paths.md' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF 'native-ai | api' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF 'based_on_fnspec_platform' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF 'Stage 4b SKIPPED — no integration work in scope' dydx-delivery/skills/generate-fnspec-integration/SKILL.md && grep -qF '04b_consistency_check_v' dydx-delivery/skills/generate-fnspec-integration/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists (I1 anchor).
    - 4 canonical pointers present (I3) — grep -qF on each of the 4 substrings.
    - `references/consistency-rules.md` referenced (Step 3 reference; I6/I7 contribution).
    - `references/either-spec-skip-paths.md` referenced (Step 2 reference; I5 contribution).
    - `references/fnspec-integration-template.md` referenced (Step 5 reference).
    - `pipefy | wrike | ziflow | other` present (D-78 4-enum).
    - `native-ai | api` present in canonical order; `! grep -qF 'api | native-ai'` (no reversed enum).
    - `based_on_fnspec_platform` literal present (I4 in SKILL.md).
    - Verbatim skip-emit `Stage 4b SKIPPED — no integration work in scope` present (I5 in SKILL.md).
    - `04b_consistency_check_v` literal present (I7 — halt artefact filename documented).
    - All 3 check letters named (`Check (a)` / `Check (b)` / `Check (c)` or `(a)` / `(b)` / `(c)` in a Step 3 list) — I6 contribution from SKILL.md.
    - `## Key decisions` section names D-78 AND D-79 AND D-82 AND D-84 AND D-85 (each as literal substring) — I8 4b half.
    - `grep -cE '^### Step [1-9]' SKILL.md` returns >= 7 (7 numbered steps target).
  </acceptance_criteria>
  <done>4b SKILL.md authored with canonical pointers + 4-enum + canonical `native-ai | api` order + consistency-check Step 3 (3 checks named) + halt-path filename + verbatim skip-emit string + Key decisions block naming 5 D-IDs + handoff. I1, I3, I4, I5, I7 reachable via grep from this file alone; I6 reachable from this file (3 checks named); I8 reachable when combined with 4a SKILL.md (08-01 T1 product).</done>
</task>

<task id="08-02-T2" type="auto">
  <name>Task 2: Author references/fnspec-integration-template.md (frontmatter contract + 10 H2 sections + Integration touchpoints + dedicated API-endpoints sub-section per ROUTE-01 check (c) + D-82 markup)</name>
  <files>dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-fnspec-platform/references/fnspec-platform-template.md` (08-01 product — direct sibling template; LIFT 10-section spine + frontmatter shape + D-82 markup convention)
    - `dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md` (165 lines — body shape baseline)
    - `dydx-delivery/skills/generate-sow/references/sow-template.md` (Phase 7 D-75 — `## Integration Scope` H2 shape)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-78 / D-82
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.2 + §3.2 (frontmatter contract + per-row markup contract)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md`. Direct sibling of `fnspec-platform-template.md` — same 10-section spine, re-aimed at integrations.

    **Pre-frontmatter documentation block** (HTML comment at top): Explain that this is the 04b_fnspec-integration template. Document the D-82 row markup convention verbatim (suffix variants + canonical enum order + `[reviewer-override:]` token + unicode arrow). Document that `based_on_fnspec_platform:` is CONDITIONAL — present when 4a exists ('both' topology), absent on 4b-only topology.

    **Frontmatter block** (concrete enum value in `platform:` line — `pipefy` as planner-default; reviewer switches at write time):
    ```yaml
    ---
    client: <CLIENT_NAME>
    project: <PROJECT_NAME>
    frontmatter_version: 2
    platform: pipefy                                 # Valid values: pipefy | wrike | ziflow | other (D-78)
    based_on_discovery: 02_discovery_v<N>.md          # MANDATORY when discovery exists
    based_on_sow: 03_sow_v<N>.md                      # MANDATORY (single SOW path per STG3-02)
    based_on_fnspec_platform: 04a_fnspec-platform_v<N>.md  # CONDITIONAL — present on 'both' topology; absent on 4b-only
    status: draft                                     # canonical 4-state: draft → client_review → approved → archived
    approved_by: <name-or-handle>                     # MANDATORY on approved
    approved_at: <ISO date>                           # MANDATORY on approved
    generated_at: <ISO date>
    ---
    ```
    The literal token `pipefy | wrike | ziflow | other` appears in the inline comment (D-78 4-enum). The literal `based_on_fnspec_platform` field name appears in the frontmatter (I4 in template).

    **Body opening:**
    ```markdown
    # Stage 4b Integration Functional Spec — <CLIENT_NAME> · <PROJECT_NAME>

    > Stage 4b of the dydx-delivery pipeline. Captures the integration-side functional contract from approved discovery + approved SOW + (when 'both' topology) approved Stage 4a platform fnspec. Every requirement row carries the `delivery: native-ai | api` routing key per D-82 — canonical enum order, never reversed (STG4-04). Stage 4b reads 4a's per-row tags as-is (no re-classification); the three cross-spec consistency checks (D-84 + ROUTE-01) run FIRST before any write.
    ```

    **10 H2 sections** (re-aimed for integration scope):
    1. `## 1. Scope summary` — one-paragraph summary from SOW `## Integration Scope` section.
    2. `## 2. System participants` — primary system + integrated systems (external SaaS / APIs / paste-fed surfaces).
    3. `## 3. Data-flow journeys` — system-to-system flows; numbered list (NOT user journeys — integration-specific).
    4. `## 4. Integration touchpoints` — **table with `Touchpoint ID | Description | Source system | Target system | Cites 4a req | Delivery` columns**. The `Cites 4a req` column is the ROUTE-01 check (b) input — every row MUST cite a 4a requirement ID by name (when 4a exists). `Delivery` column carries D-82 markup.
    5. `## 5. Integration business rules` — **table with `ID | Rule | Source | Delivery` columns**. Same D-82 markup pattern.
    6. `## 5a. API endpoints` — **dedicated sub-section per ROUTE-01 check (c) — table with `Endpoint ID | URL | Method | Auth | Cites 4a req | Delivery`**. Every endpoint MUST map to a 4a requirement ID (no orphans). `Delivery` column carries D-82 markup.
    7. `## 6. State model` — workflow state transitions across system boundaries; not row-tagged.
    8. `## 7. Edge cases` — concrete edge cases on the integration path (network failure, partial-write, idempotency violation).
    9. `## 8. Acceptance criteria` — **table with `ID | AC | Validates touchpoint(s)/endpoint(s) | Delivery` columns**. Same D-82 markup.
    10. `## 9. Open questions` — explicit unresolved items.
    11. `## 10. Out-of-scope clarifications` — explicit non-goals.

    Include at least 3 placeholder rows in section 4 (touchpoints) + 2 rows in section 5a (endpoints) demonstrating different confidence levels (HIGH / MEDIUM / LOW) and one row carrying `[reviewer-override:]` (D-82 preservation pattern).

    **Handoff footer:** lift verbatim from SKILL.md `<interfaces>` handoff message.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md && grep -qF 'pipefy | wrike | ziflow | other' dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md && grep -qF 'native-ai | api' dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md && grep -qF 'based_on_fnspec_platform' dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md && grep -qE '^## [0-9]+\.' dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists (I2 contribution).
    - `grep -qF 'pipefy | wrike | ziflow | other'` succeeds (D-78 in template).
    - `grep -qF 'native-ai | api'` succeeds (canonical order in template).
    - `! grep -qF 'api | native-ai'` (no reversed enum).
    - `grep -qF 'based_on_fnspec_platform'` succeeds (I4 in template).
    - `grep -cE '^## [0-9]+\.' template` returns >= 10 (10 numbered H2 sections — section 5a may be `## 5a.` which the regex also catches if `[0-9]+` is broadened, but the 10 sections 1-10 must each be present).
    - `grep -qE '^## 5a\.' template` OR equivalent — dedicated API endpoints sub-section present (ROUTE-01 check (c) anchor).
    - `grep -qF 'Integration touchpoints' template` succeeds (section 4 title).
    - `grep -qF 'API endpoints' template` succeeds (section 5a title).
    - `grep -qF 'Cites 4a req' template` succeeds (ROUTE-01 check (b) column header documented).
    - `grep -qF 'delivery: native-ai [HIGH, src:' template` succeeds (D-82 example row).
    - `grep -qF '[reviewer-override:' template` succeeds (D-82 preservation pattern).
    - `grep -qF 'draft → client_review → approved → archived' template` succeeds (canonical 4-state lifecycle documented).
  </acceptance_criteria>
  <done>4b template authored with 10 H2 sections + dedicated `## 5a. API endpoints` sub-section + `Cites 4a req` column on touchpoints + endpoints (ROUTE-01 b + c) + D-82 row markup + D-78 4-enum + canonical 4-state lifecycle.</done>
</task>

<task id="08-02-T3" type="auto">
  <name>Task 3: Author references/consistency-rules.md (D-84 + ROUTE-01 — three checks + halt-on-failure protocol + 04b_consistency_check_v<N>.md failure-report template + two-place cross-reference paragraph)</name>
  <files>dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md</files>
  <read_first>
    - `dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md` (Phase 7 D-73 — structural shape for ordered-list rule reference)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-84 (verbatim authority)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.2 + §3.3 (consistency-rules.md content + halt-path frontmatter contract)
    - `.planning/REQUIREMENTS.md` ROUTE-01 + ROUTE-02 (verbatim authority on three checks + halt-on-failure)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md`.

    **Body structure:**

    1. **H1 + one-paragraph purpose statement.** Explain that this file documents the three cross-spec consistency checks owned by Stage 4b per D-84 + ROUTE-01. State explicit ownership: "These checks run FIRST in `generate-fnspec-integration/SKILL.md` Step 3, before any `04b_fnspec-integration_v<N>.md` write. On any failure, the skill writes `04b_consistency_check_v<N>.md` listing the failure rows and HALTS — no fnspec is produced until the reviewer resolves the failure and re-invokes Stage 4b."

    2. `## The three checks (D-84 + ROUTE-01)`:
       - **Check (a) — Conflicting delivery tags across 4a and 4b.** For every requirement ID present in BOTH 4a and 4b: the `delivery:` tag MUST match. Conflict examples: 4a row `REQ-12: delivery: native-ai [HIGH, src: platform-pipefy]` paired with 4b row `REQ-12: delivery: api [reviewer-override: api]` — mismatch on the routing key for the same requirement. Trigger logic: parse all rows in 4a sections 4/5/8 + 4b sections 4/5/5a/8; collect (req_id, delivery_value) pairs; flag any req_id appearing twice with non-identical delivery_value.
       - **Check (b) — Dangling 4b integration touchpoints.** Every row in 4b section 4 (Integration touchpoints) MUST cite a referenced platform requirement ID from 4a in its `Cites 4a req` column. Dangling = `Cites 4a req` column is empty OR cites a req_id that does NOT exist in 4a. Trigger logic: parse 4b section 4; for each row's cited req_id, assert it appears in 4a sections 4/5/8.
       - **Check (c) — Orphan API endpoints in 4b.** Every row in 4b section 5a (API endpoints) MUST map to a 4a requirement ID in its `Cites 4a req` column. Orphan = endpoint with no req mapping. Trigger logic: parse 4b section 5a; for each row's cited req_id, assert it appears in 4a sections 4/5/8.

    3. `## Halt-on-failure protocol (D-84 + ROUTE-02)`:
       - On ANY failure (any of the three checks fails on any row): write `04b_consistency_check_v<N>.md` to `<Client> Brain/<Project>/`; emit halt message to stdout; EXIT WITHOUT writing `04b_fnspec-integration_v<N>.md`.
       - Versioning: `04b_consistency_check_v1.md` on first failure; if reviewer resolves and re-invokes and a NEW failure surfaces, write `04b_consistency_check_v2.md` etc.
       - On clean re-run (no failures): write `04b_fnspec-integration_v<N>.md` normally; do NOT write a fresh consistency-check artefact (clean checks leave no audit footprint per D-84).

    4. `## 04b_consistency_check_v<N>.md frontmatter contract`:
       ```yaml
       ---
       client: <CLIENT_NAME>
       project: <PROJECT_NAME>
       frontmatter_version: 2
       artefact_type: consistency_check_failure
       based_on_fnspec_platform: 04a_fnspec-platform_v<N>.md
       based_on_attempted_fnspec_integration: <intended-04b-filename-or-N/A-if-pre-write>
       checks_run: 3
       checks_failed: <N>
       status: halt
       generated_at: <ISO date>
       ---
       ```

    5. `## Failure-report body template`:
       ```markdown
       # Stage 4b Consistency Check Failure — <CLIENT> · <PROJECT>

       Three consistency checks ran. <N> failed. No `04b_fnspec-integration_v<M>.md` was written.

       ## Failure rows

       | Check ID | Failure type | Row reference | Detail | Suggested resolution |
       |----------|--------------|---------------|--------|----------------------|
       | (a)      | Conflicting delivery tags | REQ-12 in 4a row L23 + 4b row L45 | 4a: native-ai [HIGH, src: platform-pipefy]; 4b: api [reviewer-override: api] | Reviewer decides authoritative routing for REQ-12; align both rows + re-invoke 4b. |
       | (b)      | Dangling 4b touchpoint | 4b Section 4 row 3 (Touchpoint TP-07) | Cites 4a req `REQ-99` which does NOT exist in 4a | Edit TP-07 in 4b to cite an existing 4a req OR add REQ-99 to 4a + re-approve 4a first. |
       | (c)      | Orphan API endpoint | 4b Section 5a row 2 (Endpoint EP-04) | Cites 4a req column is empty | Map EP-04 to a 4a req OR remove EP-04 from 4b scope. |

       ## Retry mechanics

       Reviewer resolves each failure row above (edit 4a OR 4b as appropriate), then re-invokes Stage 4b. If checks pass on retry, `04b_fnspec-integration_v<N>.md` is written. If new failures surface, a new `04b_consistency_check_v<N+1>.md` is written.
       ```

    6. `## Reviewer-retry mechanics`: explicit one-paragraph workflow: failure resolution may live in either 4a (re-approve 4a after edit) OR 4b (edit 4b draft + re-invoke). Reviewer judgement.

    7. `## Two-place key-decisions cross-reference (T-02-06-02 mitigation)`:
       > Per ROUTE-01 mitigation T-02-06-02, the consistency-check ownership AND the routing-key contract are declared in BOTH `generate-fnspec-platform/SKILL.md` (4a) AND `generate-fnspec-integration/SKILL.md` (4b) `## Key decisions` sections — naming D-78 (TD-2 ziflow enum) + D-79 (addendum carrier) + D-82 (per-row markup) + D-84 (consistency check ownership) + D-85 (skip mechanics). Cross-reference avoids single-source drift. The 4b structure-check assertion I8 verifies BOTH halves are present.

    8. `## How this rubric is consumed`: short paragraph noting that `generate-fnspec-integration/SKILL.md` Step 3 references this file by relative path; structure-check I6 + I7 grep-assert the three check letters + the halt-on-failure contract are documented in this file AND in SKILL.md.

    Discretion: planner authors the specific failure-row template wording above; only the halt-on-failure contract + three checks are locked per CONTEXT.md Discretion.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md && grep -qF 'Check (a)' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md && grep -qF 'Check (b)' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md && grep -qF 'Check (c)' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md && grep -qF '04b_consistency_check_v' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md && grep -qF 'artefact_type: consistency_check_failure' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md && grep -qF 'status: halt' dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists (I2 contribution + I6 / I7 anchor).
    - All three checks named: `grep -qF 'Check (a)'` + `'Check (b)'` + `'Check (c)'` (I6).
    - `04b_consistency_check_v` literal present (I7 — halt artefact filename).
    - `artefact_type: consistency_check_failure` literal present (halt-path frontmatter contract).
    - `status: halt` literal present.
    - `Failure rows` literal present (failure-report body template).
    - Two-place key-decisions cross-reference paragraph names D-78 AND D-79 AND D-82 AND D-84 AND D-85.
    - Halt-on-failure protocol documented (`HALT` or `exit` or `before any` literal in body).
  </acceptance_criteria>
  <done>Consistency rules file authored with 3 checks + halt-on-failure protocol + `04b_consistency_check_v<N>.md` frontmatter contract + failure-row table template + two-place cross-reference paragraph. I6 + I7 reachable from this file.</done>
</task>

<task id="08-02-T4" type="auto">
  <name>Task 4: Author references/either-spec-skip-paths.md (STG4-06 three-topology rules + D-85 verbatim skip-emit string + topology detection logic)</name>
  <files>dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md</files>
  <read_first>
    - `dydx-delivery/skills/discovery-intake/SKILL.md` (Phase 7 D-74 skip-emit body — direct precedent for D-85 stdout-only + no-file-written pattern)
    - `dydx-delivery/skills/generate-sow/references/sow-template.md` (Phase 7 D-75 — `## Platform Scope` / `## Integration Scope` H2 shape; topology signal source)
    - `.planning/REQUIREMENTS.md` STG4-06 (verbatim authority on three topologies)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-CONTEXT.md` §decisions D-85 (verbatim authority)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.2 (either-spec-skip-paths.md content)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md`.

    **Body structure:**

    1. **H1 + one-paragraph purpose statement.** Explain that this file documents the STG4-06 three valid topologies (4a-only / 4b-only / both) + the D-85 either-spec-skip detection mechanics for Stage 4b. Cited from `generate-fnspec-integration/SKILL.md` Step 2.

    2. `## Three topologies (STG4-06)`:
       - **Topology 1: both (typical case).** 4a is in scope AND 4b is in scope. `04a_fnspec-platform_v*.md` exists AND SOW `## Integration Scope` H2 has content. Stage 4b runs normally — read 4a, run consistency checks, write 4b.
       - **Topology 2: 4a-only (no integration scope).** 4a is in scope, 4b is OUT of scope. `04a_fnspec-platform_v*.md` exists; SOW `## Integration Scope` H2 is empty / "n/a" / absent. Stage 4b emits verbatim skip + exits. On platform-only-with-API-rows path, 4a authors the `## Platform-API Addendum` H2 body inline per D-79 (ROUTE-03 forward-compat).
       - **Topology 3: 4b-only (no platform configuration scope).** 4a is OUT of scope, 4b is in scope. `04a_fnspec-platform_v*.md` does NOT exist; SOW `## Integration Scope` H2 has content but `## Platform Scope` H2 is empty / "n/a" / absent. Stage 4b runs normally BUT consistency check (b) and (c) skip because there is no 4a to cite into; only check (a) runs (vacuously — no 4a to conflict with). `based_on_fnspec_platform:` frontmatter field is ABSENT from the 4b output.

    3. `## Topology detection signals (D-85)`:
       Three combined signals, in priority order:
       - **Signal 1 (highest priority): Explicit reviewer signal at invocation.** Reviewer can declare topology at skill invocation ("integration is out of scope" / "no platform fnspec for this engagement"). Overrides everything below.
       - **Signal 2: SOW H2 content.** Read `## Platform Scope` + `## Integration Scope` H2s in the highest-version approved `03_sow_v*.md`. Empty / "n/a" / absent body under either H2 == that side is out of scope. (Phase 7 D-75 locked these H2s — both H2s ALWAYS present in SOW; emptiness is the signal.)
       - **Signal 3: Presence/absence of 4a artefact.** If `04a_fnspec-platform_v*.md` does NOT exist in the project folder at Stage 4b invocation, 4a is out of scope (Topology 3 — 4b-only).

    4. `## D-85 verbatim skip-emit string (locked)`:
       > When Stage 4b is determined out of scope (Topology 2 / 4a-only), emit the verbatim string to stdout:
       >
       > ```
       > Stage 4b SKIPPED — no integration work in scope
       > ```
       >
       > Em-dash `—` verbatim (NOT hyphen). Stdout ONLY — NO `04b_*` artefact written, NO halt artefact written, NO consistency check run.

    5. `## Reviewer-override path for ambiguous SOW signal`:
       When SOW H2 content is ambiguous (e.g., `## Integration Scope` contains only "TBD" or a single placeholder sentence), Stage 4b PROMPTS the reviewer: "SOW Integration Scope is ambiguous — is integration in scope (y/N)?" Reviewer answer = explicit signal 1 above. Stage 4b proceeds based on that signal.

    6. `## Forward-compat note (ROUTE-03 + ROUTE-05)`:
       - **Topology 1 (both):** Stage 5 v2.3 reads BOTH 4a + 4b via the `based_on_*` chain.
       - **Topology 2 (4a-only with API rows):** Stage 5 v2.3 reads 4a's `## Platform-API Addendum` H2 body verbatim per `has_platform_api_addendum: true` + `tech_spec_scope: platform-api-addendum-only` (D-79).
       - **Topology 2 (4a-only with NO API rows):** Stage 5 v2.3 skips entirely — no tech spec written, no addendum, no frontmatter change (ROADMAP Phase 8 success criterion #4 branch c).
       - **Topology 3 (4b-only):** Stage 5 v2.3 reads only 4b (no 4a chain).

    7. `## How this rubric is consumed`: short paragraph noting that `generate-fnspec-integration/SKILL.md` Step 2 references this file by relative path; structure-check I5 grep-asserts the verbatim D-85 skip-emit string in SKILL.md OR in this file.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md && grep -qF 'Stage 4b SKIPPED — no integration work in scope' dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md && grep -qiF '4a-only' dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md && grep -qiF '4b-only' dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md && grep -qiF 'both' dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists (I2 contribution).
    - Verbatim D-85 string `Stage 4b SKIPPED — no integration work in scope` present (I5 anchor; em-dash unicode verbatim).
    - All three topology names present: `4a-only`, `4b-only`, `both` (STG4-06).
    - `## Platform Scope` AND `## Integration Scope` SOW signal references present (Phase 7 D-75 cited).
    - Reviewer-override path documented.
    - `has_platform_api_addendum: true` reference present (forward-compat note — Topology 2 with API rows).
    - `tech_spec_scope: platform-api-addendum-only` reference present.
  </acceptance_criteria>
  <done>Either-spec-skip-paths file authored with three topologies + topology detection logic + verbatim D-85 string + forward-compat note. I5 reachable from this file (and also from SKILL.md per T1).</done>
</task>

<task id="08-02-T5" type="auto">
  <name>Task 5: Plan-local 4b-section gate — run the I1-I8 proxy assertions as a bash one-liner (includes cross-plan I8 check against 4a SKILL.md authored in 08-01)</name>
  <files></files>
  <read_first>
    - All 4 files authored in T1-T4
    - `dydx-delivery/skills/generate-fnspec-platform/SKILL.md` (08-01 product — I8 cross-plan check)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-VALIDATION.md` §1 (I1-I8 assertion contract)
    - `.planning/phases/08-stage-4-fnspec-split-route/08-RESEARCH.md` §2.3 partition table (I1-I8 verbatim)
  </read_first>
  <action>
    Run the I1-I8 proxy assertions as a bash one-liner against the 4 files authored in T1-T4 PLUS the 08-01-product 4a SKILL.md (for I8 cross-plan check). This is the plan-local gate — `phase8-structure-check.sh --section 4b` does not exist yet (ships in 08-03).

    Run this exact bash command (single shell invocation; on first failure, fix the source file and re-run — assertion strings ARE the contract):

    set -euo pipefail
    SK=dydx-delivery/skills/generate-fnspec-integration/SKILL.md
    TP=dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md
    CR=dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md
    SP=dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md
    SK4A=dydx-delivery/skills/generate-fnspec-platform/SKILL.md
    test -f "$SK" && echo "PASS I1" || { echo "FAIL I1"; exit 1; }
    for f in "$TP" "$CR" "$SP"; do test -f "$f" || { echo "FAIL I2: $f missing"; exit 1; }; done; echo "PASS I2"
    for c in safety-rules.md stage-numbering.md frontmatter-scheme.md glossary.md; do grep -qF "dydx-delivery/references/$c" "$SK" || { echo "FAIL I3: $c"; exit 1; }; done; echo "PASS I3"
    grep -qF 'based_on_fnspec_platform' "$SK" && grep -qF 'based_on_fnspec_platform' "$TP" && echo "PASS I4" || { echo "FAIL I4"; exit 1; }
    grep -qF 'Stage 4b SKIPPED — no integration work in scope' "$SK" || grep -qF 'Stage 4b SKIPPED — no integration work in scope' "$SP" && echo "PASS I5" || { echo "FAIL I5"; exit 1; }
    grep -qF 'Check (a)' "$CR" && grep -qF 'Check (b)' "$CR" && grep -qF 'Check (c)' "$CR" && echo "PASS I6" || { echo "FAIL I6"; exit 1; }
    grep -qF '04b_consistency_check_v' "$SK" && grep -qF '04b_consistency_check_v' "$CR" && echo "PASS I7" || { echo "FAIL I7"; exit 1; }
    for d in D-78 D-79 D-82 D-84 D-85; do
      grep -qF "$d" "$SK4A" || { echo "FAIL I8: $d missing from 4a SKILL.md (08-01 regression)"; exit 1; }
      grep -qF "$d" "$SK" || { echo "FAIL I8: $d missing from 4b SKILL.md (08-02 regression)"; exit 1; }
    done; echo "PASS I8"
    grep -qF 'pipefy | wrike | ziflow | other' "$SK" && grep -qF 'pipefy | wrike | ziflow | other' "$TP" && echo "PASS D-78-baked-in" || { echo "FAIL D-78"; exit 1; }
    grep -qF 'native-ai | api' "$SK" && ! grep -qF 'api | native-ai' "$SK" && echo "PASS canonical-enum-order" || { echo "FAIL canonical-enum-order"; exit 1; }
    echo "ALL I1-I8 ASSERTIONS PASSED — 4b section ready for 08-03 wire-up"

    Expected output: 8 PASS lines (I1-I8) + 2 supplementary PASS lines (D-78-baked-in + canonical-enum-order) followed by the all-passed message. Exit code 0.

    If I8 fails with "08-01 regression", do NOT fix 4b. The fix lives upstream in 08-01 — re-open 08-01 SUMMARY and add the missing D-ID to `generate-fnspec-platform/SKILL.md` `## Key decisions` section, then re-run this gate.

    If I8 fails with "08-02 regression", fix 4b SKILL.md `## Key decisions` here.
  </action>
  <verify>
    <automated>set -euo pipefail; SK=dydx-delivery/skills/generate-fnspec-integration/SKILL.md; TP=dydx-delivery/skills/generate-fnspec-integration/references/fnspec-integration-template.md; CR=dydx-delivery/skills/generate-fnspec-integration/references/consistency-rules.md; SP=dydx-delivery/skills/generate-fnspec-integration/references/either-spec-skip-paths.md; SK4A=dydx-delivery/skills/generate-fnspec-platform/SKILL.md; test -f "$SK" && test -f "$TP" && test -f "$CR" && test -f "$SP" && test -f "$SK4A" && for c in safety-rules.md stage-numbering.md frontmatter-scheme.md glossary.md; do grep -qF "dydx-delivery/references/$c" "$SK"; done && grep -qF 'based_on_fnspec_platform' "$SK" && grep -qF 'based_on_fnspec_platform' "$TP" && { grep -qF 'Stage 4b SKIPPED — no integration work in scope' "$SK" || grep -qF 'Stage 4b SKIPPED — no integration work in scope' "$SP"; } && grep -qF 'Check (a)' "$CR" && grep -qF 'Check (b)' "$CR" && grep -qF 'Check (c)' "$CR" && grep -qF '04b_consistency_check_v' "$SK" && grep -qF '04b_consistency_check_v' "$CR" && for d in D-78 D-79 D-82 D-84 D-85; do grep -qF "$d" "$SK4A" && grep -qF "$d" "$SK"; done && grep -qF 'pipefy | wrike | ziflow | other' "$SK" && grep -qF 'native-ai | api' "$SK" && ! grep -qF 'api | native-ai' "$SK"</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0 on the one-liner verify command.
    - All 8 I-assertions emit PASS when run interactively (I1-I8).
    - Cross-plan I8 check: BOTH 4a SKILL.md AND 4b SKILL.md name all 5 D-IDs (D-78/D-79/D-82/D-84/D-85). If 4a is missing any, this is an 08-01 regression — fix upstream.
    - Files produced: 4 new files at the `files_created` paths (plus 4a SKILL.md from 08-01 must remain present).
    - Supplementary PASS lines on D-78-baked-in (4-enum present in SKILL.md + template) + canonical-enum-order (no reversed `api | native-ai`).
  </acceptance_criteria>
  <done>I1-I8 proxy gate passes — 4b section is ready for 08-03 to wire `run_4b_section()` against. 08-03 closeout begins immediately after with both 4a + 4b SKILL.md + all 6 references files on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer → generate-fnspec-integration skill | Reviewer invokes skill against approved discovery + approved SOW + approved 4a. Inputs are reviewer-approved artefacts; trust posture: low risk. |
| 4a artefact → 4b consistency check | 4a is the canonical routing-key source; 4b reads 4a tags as-is per D-81 closing clause. Trust = internal (sibling skill output). |
| 4b write-path → filesystem | Skill writes `04b_fnspec-integration_v<N>.md` OR `04b_consistency_check_v<N>.md` to `<Client> Brain/<Project>/`. Reviewer-supplied path components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Consistency check silently skipped (skill writes 4b without running checks) | mitigate | I6 + I7 assert all three checks named in SKILL.md AND consistency-rules.md AND halt-on-failure protocol documented. Step 3 in SKILL.md states "FIRST before any write" verbatim — any code path that writes 4b without invoking the rules file violates the documented contract. Reviewer can grep the SKILL.md body to verify the contract pre-execute. |
| T-08-07 | Tampering | D-85 verbatim skip-emit string drift (e.g., hyphen instead of em-dash) | mitigate | I5 asserts the literal string `Stage 4b SKIPPED — no integration work in scope` (with unicode em-dash) in SKILL.md OR `either-spec-skip-paths.md`. Drift fails-loud at structure-check. Em-dash is locked per CONTEXT.md `<specifics>` line 178. |
| T-08-08 | Tampering | I8 cross-plan two-place declaration regression (e.g., 4a SKILL.md edits remove a D-ID) | mitigate | T5 one-liner above grep-asserts ALL 5 D-IDs (D-78/D-79/D-82/D-84/D-85) in BOTH 4a + 4b SKILL.md. T5 distinguishes 08-01 regression vs 08-02 regression by checking each file separately. Upstream fix path documented in the action body. |
| T-08-09 | Information disclosure | Halt-path artefact `04b_consistency_check_v<N>.md` leaks platform internal details | accept | Failure-row table contains only req IDs + delivery tags + reviewer-facing suggestions; no platform credentials / API keys / tenant data. Same posture as 4a artefact (T-08-05). |
| T-08-10 | Denial of service | Pathological consistency-check input (e.g., 10K rows in 4a) | accept | v2.2 fnspec scale bounded by SOW size (typically < 100 reqs per CR); pathological scale is not a v2.2 concern. Future scaling concern deferred. |

ASVS L1 alignment: doc-only skill authoring; primary threats are silent-skip of consistency checks (mitigated via I6+I7 grep gates) and verbatim string drift (mitigated via I5 grep gate with unicode-em-dash verbatim).
</threat_model>

<verification>
- **Plan-local (T5):** I1-I8 one-liner exits 0 with all 8 I-PASS lines + 2 supplementary PASS lines.
- **Cross-plan I8 check:** T5 explicitly checks BOTH 4a SKILL.md AND 4b SKILL.md `## Key decisions` sections for D-78/D-79/D-82/D-84/D-85 — fails with explicit "08-01 regression" message if 4a is missing any D-ID. Fix lives upstream in that case.
- **Cross-plan deferred:** I1-I8 wired into `phase8-structure-check.sh run_4b_section()` during 08-03. Assertion strings here MUST match what 08-03 wires.
- **Manual:** Spot-check the verbatim D-85 skip-emit string in `either-spec-skip-paths.md` uses unicode em-dash (`—` U+2014, NOT `--` or `-`). The em-dash is locked per CONTEXT.md `<specifics>` line 178 and the structure-check anchor.
- **Forbidden:** Do NOT run `bash phase8-structure-check.sh --section 4b` — script does not exist yet. T5 is the plan-local gate.
</verification>

<success_criteria>
- 4 new files exist at the paths in `files_created`.
- T5 one-liner exits 0 with all I1-I8 assertions PASS.
- STG4-02 + STG4-04 (4b half) + ROUTE-01 + ROUTE-02 + ROUTE-03 (4b read-side) requirement bodies are addressable via I1-I8 assertion contract.
- 08-03 closeout begins with all 6 4a/4b skill files on disk + the I8 two-place cross-reference verified across both SKILL.md key-decisions sections.
- Both 4a + 4b SKILL.md `## Key decisions` sections name all 5 D-IDs (D-78/D-79/D-82/D-84/D-85) — T-02-06-02 / ROUTE-01 mitigation complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-stage-4-fnspec-split-route/08-02-SUMMARY.md` recording:
- Files created (4)
- T5 one-liner exit code (0 expected) + count of PASS lines (10 expected: I1-I8 + D-78-baked-in + canonical-enum-order)
- Concrete `platform:` enum value chosen in `fnspec-integration-template.md` (default `pipefy`; record actual)
- D-82 markup example row(s) used in template sections 4 / 5 / 5a / 8
- Final verbatim D-85 skip-emit string locked (default `Stage 4b SKIPPED — no integration work in scope`; record if planner adjusted phrasing)
- Failure-row template column headers chosen in `consistency-rules.md` (default `Check ID | Failure type | Row reference | Detail | Suggested resolution`)
- Confirmation that 4a SKILL.md `## Key decisions` section (08-01 product) names all 5 D-IDs (D-78/D-79/D-82/D-84/D-85) — if missing, record the regression + fix
- Verbatim handoff-message string locked
- Any deviation from planner-default trigger wording or failure-report template
</output>
