---
plan: 07-01
phase: 7
slug: stage-1-kickoff-discovery-sow-upstream-wiring
wave: 1
depends_on: []
files_modified: []
files_created:
  - dydx-delivery/skills/kickoff-capture/SKILL.md
  - dydx-delivery/skills/kickoff-capture/references/kickoff-template.md
  - dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md
  - dydx-delivery/skills/kickoff-capture/references/capture-paths.md
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh
autonomous: true
requirements: [STG1-01, STG1-02, STG1-03, STG1-04, STG1-05]
must_haves:
  truths:
    - "Reviewer can run `kickoff-capture` against meeting-notes / Miro / Field Notes paste and receive a `01_kickoff_v<N>.md` artefact"
    - "Artefact frontmatter carries a single `kickoff_branch:` field set to a concrete enum value (`discovery-ready` OR `draft-sow`) — both values documented in the kickoff-capture skill files"
    - "Auto-classification emits `[unknown — needs human classification]` markers where confidence is low"
    - "Field Notes triage default filter is `processed_at IS NULL`; kickoff never auto-merges entries (MOD-8 prevention)"
    - "Three capture paths documented: meeting-notes paste / Miro paste / Field Notes paste — no raw-notes-direct-to-discovery shortcut"
    - "`bash phase7-structure-check.sh --section kickoff` exits 0"
  artifacts:
    - path: "dydx-delivery/skills/kickoff-capture/SKILL.md"
      provides: "Stage 1 skill body — invocation triggers, inputs, output, How-to-run steps, write-and-hand-off, canonical-reference pointers"
      contains: "4 canonical-pointer sentences (safety-rules / stage-numbering / frontmatter-scheme / glossary), verbatim handoff message, references/auto-classify-rubric.md path"
    - path: "dydx-delivery/skills/kickoff-capture/references/kickoff-template.md"
      provides: "Artefact body template for `01_kickoff_v<N>.md`"
      contains: "Frontmatter with concrete `kickoff_branch: discovery-ready` value + enum-documenting comment listing both valid values + 8 H2 categories in STG1-04 order + `[unknown — needs human classification]` marker convention"
    - path: "dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md"
      provides: "Auto-classification confidence rubric (D-73)"
      contains: "5 explicit triggers + escalation table + operational principle backstop"
    - path: "dydx-delivery/skills/kickoff-capture/references/capture-paths.md"
      provides: "3 capture-path paste protocols (STG1-05 + D-72)"
      contains: "meeting-notes paste / Miro paste fallback per DESIGN-07 / Field Notes paste with `processed_at IS NULL` + MOD-8 prevention"
    - path: ".planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh"
      provides: "Phase 7 structure-check harness with `--section kickoff|discovery|sow|all` dispatch (D-77)"
      contains: "K1-K7 (incl. K4 concrete value + K4b enum-documented assertions) + D1-D3 + S1-S2 + X1-X2 assertion blocks; exits 0 on `--section kickoff` when 07-01 ships"
  key_links:
    - from: "dydx-delivery/skills/kickoff-capture/SKILL.md"
      to: "dydx-delivery/references/{safety-rules,stage-numbering,frontmatter-scheme,glossary}.md"
      via: "4 inline canonical-pointer sentences (FOUND-04 / D-59 pattern)"
      pattern: "dydx-delivery/references/.*\\.md"
    - from: "dydx-delivery/skills/kickoff-capture/SKILL.md"
      to: "dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md"
      via: "Step body reference (per K7 assertion)"
      pattern: "references/auto-classify-rubric.md"
    - from: "scripts/phase7-structure-check.sh"
      to: "dydx-delivery/skills/kickoff-capture/SKILL.md + 3 references/ files"
      via: "`grep -qF` literal-string + file-existence assertions"
      pattern: "run_kickoff_section"
---

<objective>
Author Stage 1 `kickoff-capture/` skill end-to-end — SKILL.md body + 3 `references/` files (kickoff-template, auto-classify-rubric, capture-paths) — and ship `scripts/phase7-structure-check.sh` with `--section <kickoff|discovery|sow|all>` partitioning per D-77.

Purpose: Lock the kickoff frontmatter contract (concrete `kickoff_branch:` value + enum documented in body + `[unknown — needs human classification]` marker + 8-category H2 list) before 07-02/07-03 light up against it. Ship the partitioned structure-check so 07-02 + 07-03 can self-verify their own section while the other skill is in flight.

Output: 5 new files (4 skill files + 1 script). `--section kickoff` green at plan end; `--section discovery` and `--section sow` will fail until 07-02/07-03 ship (expected).
</objective>

<plan_warning>
**CRITICAL — `--all` is FORBIDDEN inside 07-01.**

The script ships with cross-section assertions (D1-D3 for discovery, S1-S2 for sow, X1-X2 for cross-skill consistency) that reference files NOT yet on disk during 07-01 (`discovery-intake/SKILL.md` post-edit + `generate-sow/SKILL.md` + `sow-template.md` post-edit). Those assertions will FAIL during 07-01 — that is correct and expected.

**The only green gate for 07-01 is `bash phase7-structure-check.sh --section kickoff`.**

Do NOT run `--all` or `--section discovery|sow` during 07-01 execution except as an expected-failure diagnostic. Wave 2 (07-02 + 07-03) and Wave 3 (07-04) own the `--section discovery|sow|all` gates respectively.
</plan_warning>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@dydx-delivery/skills/discovery-intake/SKILL.md
@dydx-delivery/skills/discovery-intake/references/intake-template.md
@dydx-delivery/skills/platform-pipefy/SKILL.md
@dydx-delivery/references/frontmatter-scheme.md
@.planning/milestones/v2.1-phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh

<interfaces>
<!-- Verbatim strings the structure-check asserts via `grep -qF` — every executor edit must preserve these literally. -->

Kickoff frontmatter fields (DESIGN-17 line 562 lift — note: `kickoff_branch:` value below is the CONCRETE enum value the template will carry per C1 fix; the enum-documenting comment lists both):
```yaml
---
client: <CLIENT_NAME>
project: <PROJECT_NAME>
frontmatter_version: 2
kickoff_branch: discovery-ready    # Valid values: discovery-ready | draft-sow — switch to draft-sow when Stage 2 is to be skipped
field_notes_processed_count: <N>
status: draft
---
```

Verbatim marker (STG1-04 + DESIGN-17 line 566):
```
[unknown — needs human classification]
```

Verbatim Field Notes filter (DESIGN-09 / STG1-03):
```
processed_at IS NULL
```

8 H2 categories in STG1-04 order (verbatim, this order):
1. ## 1. System
2. ## 2. Users
3. ## 3. Triggers
4. ## 4. Data
5. ## 5. Rules
6. ## 6. Integrations
7. ## 7. Exceptions
8. ## 8. Failure points

Verbatim handoff message (DESIGN-17 line 574):
```
Awaiting status: approved write to 01_kickoff_v<N>.md. Branch routing on kickoff_branch: value (discovery-ready -> Stage 2; draft-sow -> SKIP Stage 2 -> Stage 3).
```

Canonical-pointer sentence pattern (FOUND-04 / D-59 — lift from platform-pipefy/SKILL.md:27):
```markdown
> **<topic>:** <one-line context>. See `dydx-delivery/references/<file>.md` for the canonical <noun>.
```

Bash assertion bodies (lift directly from RESEARCH §3 code block) — K1, K2, K3, K4, K4b (NEW per C1 fix), K5, K6, K7, D1, D2, D3, S1, S2, X1, X2.
</interfaces>
</context>

<tasks>

<task id="07-01-T1" type="auto">
  <name>Task 1: Ship phase7-structure-check.sh with --section dispatch + K4/K4b split (Wave 0-equivalent)</name>
  <files>.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh</files>
  <read_first>
    - `.planning/milestones/v2.1-phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` (281-line direct-lift skeleton — read in full)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §3 (concrete D-77 assertion bodies for K1-K7 / D1-D3 / S1-S2 / X1-X2 — copy verbatim, then add K4b per the action below)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §5 (bash script skeleton lift pattern + 4-mode dispatch deviation from phase6)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md` (Dimension 8 sensor model — note: K4 has been split into K4 + K4b per cross-AI review C1 fix)
  </read_first>
  <action>
    Lift `phase6-structure-check.sh` skeleton wholesale (shebang + comment-vs-code banner + `set -euo pipefail` line 41 + `fail()`/`pass()` helpers lines 43-44 + arg parser lines 46-68 + dispatch case lines 271-277 + exit conventions lines 279-280). Adapt per D-77 with the C1 K4/K4b split:

    1. Retitle banner: `phase7-structure-check.sh — structural verifier for Phase 7 (Stage 1 kickoff + Stage 2/3 upstream wiring) deliverables`.
    2. Update USAGE block to list `kickoff|discovery|sow|all`.
    3. Add `--all` arg parser branch alongside `--section` (Phase 7 deviation: 4 dispatch modes vs phase6's 3).
    4. Replace phase6 platform runners with 4 new runners — `run_kickoff_section()`, `run_discovery_section()`, `run_sow_section()`, `run_cross_section()` — bodies copied verbatim from 07-RESEARCH.md §3 code block (assertions K1-K7, D1-D3, S1-S2, X1-X2) PLUS the new K4b assertion described in step 7 below.
    5. Dispatch case: `kickoff|discovery|sow|all|""` per RESEARCH §3 — `all`/`""` runs all 4 runners; unknown value fails with `unknown --section value: $SECTION (use kickoff|discovery|sow|all)`.
    6. Use `grep -qF` (literal, case-sensitive) for verbatim strings; `grep -qE` (extended regex) for enum patterns. NO `-i` (Git Bash GNU grep 3.0 SIGABRTs on `-i` against multibyte em-dash / arrow chars per RESEARCH §11 + PATTERNS §5).
    7. **K4 / K4b split (per C1 fix — cross-AI review).** The original K4 anchored-regex assertion was internally inconsistent with the planned template literal. Replace with TWO assertions:
       - **K4 — concrete enum value asserted in template.** Assert `^kickoff_branch: (discovery-ready|draft-sow)$` matches at least one line of `kickoff-capture/references/kickoff-template.md` via `grep -qE '^kickoff_branch: (discovery-ready|draft-sow)$' …`. The template MUST write a single concrete enum value (per T3 below, `discovery-ready` is the planner default; reviewer switches to `draft-sow` at write time).
       - **K4b — both enum values documented in kickoff-capture skill files.** Assert that the literal strings `discovery-ready` AND `draft-sow` both appear at least once SOMEWHERE in the union of `kickoff-capture/SKILL.md`, `kickoff-capture/references/kickoff-template.md`, `kickoff-capture/references/auto-classify-rubric.md`, and `kickoff-capture/references/capture-paths.md`. Use two separate `grep -qF` calls (one per value); cat all four files together and pipe into each grep. This guarantees the enum is documented somewhere in the skill — independent of which concrete value the template carries.

       Bash skeleton for K4b (planner-suggested form):
       ```bash
       local kickoff_files=(
         "dydx-delivery/skills/kickoff-capture/SKILL.md"
         "dydx-delivery/skills/kickoff-capture/references/kickoff-template.md"
         "dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md"
         "dydx-delivery/skills/kickoff-capture/references/capture-paths.md"
       )
       if cat "${kickoff_files[@]}" 2>/dev/null | grep -qF 'discovery-ready' \
          && cat "${kickoff_files[@]}" 2>/dev/null | grep -qF 'draft-sow'; then
         pass "K4b: both enum values (discovery-ready, draft-sow) documented in kickoff-capture skill files"
       else
         fail "K4b: missing one or both enum values (discovery-ready / draft-sow) in kickoff-capture skill files"
       fi
       ```

    8. Verbatim strings asserted (cite each in the assertion fail message):
       - K4: `^kickoff_branch: (discovery-ready|draft-sow)$` (anchored regex) in `kickoff-capture/references/kickoff-template.md` — matches a CONCRETE value line, not a placeholder.
       - K4b: both `discovery-ready` AND `draft-sow` literals present somewhere across the 4 kickoff-capture skill files (enum-documented assertion).
       - K5: `[unknown — needs human classification]` in `kickoff-capture/SKILL.md`
       - K6: `processed_at IS NULL` in `kickoff-capture/references/capture-paths.md`
       - D3: `Stage 2 SKIPPED — kickoff branch = draft-sow` in `discovery-intake/SKILL.md`
       - S1: `draft → client_review → approved → archived` in `generate-sow/SKILL.md` OR `generate-sow/references/sow-template.md`
       - S2: `^## Platform Scope` AND `^## Integration Scope` (anchored regex) in `generate-sow/references/sow-template.md`
    9. Cross-section runner X1: assert `kickoff_branch` literal in all 3 of `kickoff-capture/references/kickoff-template.md`, `discovery-intake/SKILL.md`, `generate-sow/SKILL.md`. X2: assert `based_on_kickoff` in both `discovery-intake/SKILL.md` and `generate-sow/SKILL.md`.
    10. NO awk-between-headings OPEN-Q row-flip extraction (phase6 lines 252-264) — RESEARCH §1 confirms zero STG-prefixed rows; section is vacuous in Phase 7.
    11. Final lines: `echo "ALL ASSERTIONS PASSED"` + `exit 0`.

    Make script executable on commit if shell supports it (chmod +x). Cross-section runner only fires under `all|""`.
  </action>
  <verify>
    <automated>bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --help</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh`
    - `bash phase7-structure-check.sh --help` exits 0 and prints USAGE listing `kickoff|discovery|sow|all`
    - `grep -qF 'run_kickoff_section()' phase7-structure-check.sh` succeeds
    - `grep -qF 'run_discovery_section()' phase7-structure-check.sh` succeeds
    - `grep -qF 'run_sow_section()' phase7-structure-check.sh` succeeds
    - `grep -qF 'run_cross_section()' phase7-structure-check.sh` succeeds
    - `grep -qF 'set -euo pipefail' phase7-structure-check.sh` succeeds
    - `grep -qF 'ALL ASSERTIONS PASSED' phase7-structure-check.sh` succeeds
    - `grep -qF 'K4b:' phase7-structure-check.sh` succeeds (C1 K4b assertion present in script)
    - `grep -qF "grep -qE '^kickoff_branch: (discovery-ready|draft-sow)" phase7-structure-check.sh` OR equivalent literal — confirms K4 asserts the concrete-value regex (not the placeholder form)
    - At this point `--section kickoff` will FAIL (kickoff skill not yet authored) — expected; passes after T2-T5.
  </acceptance_criteria>
  <done>Script lifted from phase6 skeleton; 4 runners present; 4-mode dispatch wired; usage/help works; K4 + K4b split present per C1 fix; verbatim assertion strings embedded literally.</done>
</task>

<task id="07-01-T2" type="auto">
  <name>Task 2: Author kickoff-capture/SKILL.md (body + 4 canonical-pointer sentences + handoff)</name>
  <files>dydx-delivery/skills/kickoff-capture/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/discovery-intake/SKILL.md` (149 lines — primary body-shape analog; lift Inputs/Output/How-to-run/What-it-does-not-do/Quality-bar section structure)
    - `dydx-delivery/skills/platform-pipefy/SKILL.md` lines 25-27 (canonical-pointer sentence exemplar — FOUND-04 / D-59 pattern)
    - `.planning/codebase/CONVENTIONS.md` § "SKILL.md body structure" lines 185-201 (body-shape canon)
    - `.planning/DESIGN.md` lines 547-585 (DESIGN-17 — frontmatter shape + handoff verbatim + key decisions)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §6 (suggested 4-block canonical-pointer draft for direct lift) + §2 DESIGN-17 lift map
    - `dydx-delivery/references/frontmatter-scheme.md` (canonical `based_on_*` underscore-snake-case convention)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/kickoff-capture/SKILL.md` following CONVENTIONS.md SKILL.md body shape exactly.

    **Frontmatter** (top of file, between `---` delimiters):
    ```yaml
    name: kickoff-capture
    description: Capture a kickoff for a new engagement or change request. Use when the user says "kickoff for X", "start kickoff", "capture meeting notes for X", "run kickoff", or pastes raw meeting notes / Miro / Field Notes content needing structuring. Stage 1 of the dydx-delivery pipeline — produces 01_kickoff_v<N>.md with a kickoff_branch routing field.
    ```
    (Adapt from discovery-intake/SKILL.md lines 1-4 pattern; replace name + description.)

    **Body sections in this order** (per CONVENTIONS.md § SKILL.md body structure):

    1. `# kickoff-capture` — H1 matching directory name.
    2. One-paragraph purpose statement explicitly referencing "Stage 1 of the dydx-delivery pipeline" and naming the `kickoff_branch:` routing field. MUST mention both enum values (`discovery-ready` and `draft-sow`) by name in this paragraph so K4b is satisfied even if the template carries only one concrete value.
    3. **4 canonical-pointer block quotes** (lift verbatim shape from RESEARCH §6 draft block — STG1-01 requirement; stronger than v2.1 precedent):
       ```markdown
       > **Hard rules:** Sandbox-only operations. Refuses destructive actions on production tenants and on the Field Notes Coda table. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.

       > **Stage numbering:** This skill writes `01_kickoff_v<N>.md` per the canonical file-prefix scheme. See `dydx-delivery/references/stage-numbering.md` for the canonical Stage 1 placement + old→new mapping table.

       > **Frontmatter scheme:** This skill emits `kickoff_branch:` (enum: `discovery-ready | draft-sow`) + `based_on_*` fields per the canonical underscore-snake-case convention. See `dydx-delivery/references/frontmatter-scheme.md` for the canonical scheme + lenient-mode contract.

       > **Glossary:** `kickoff_branch:` (enum: `discovery-ready | draft-sow`) and `[unknown — needs human classification]` are canonical terms defined in `dydx-delivery/references/glossary.md`.
       ```
       All 4 paths must appear literally (K3 asserts each substring). Both `discovery-ready` and `draft-sow` appear in this section (helps satisfy K4b).
    4. `## Inputs` — bulleted list per STG1-05: meeting-notes paste (freeform); Miro paste fallback per DESIGN-07 / AUDIT-08; Field Notes Coda paste with `processed_at IS NULL` triage (D-72 paste-only). No MCP / no API calls. Reference `references/capture-paths.md` for protocol detail.
    5. `## Output` — `<Client> Brain/<Project>/01_kickoff_v<N>.md` per DESIGN-02 file-prefix scheme + DESIGN-17 line 561.
    6. `## How to run` with these numbered steps:
       - `### Step 1 — Locate or establish capture entry point` (start-at-any-point triage: which of paste/Miro/Field Notes is the entry; if Field Notes, default to `processed_at IS NULL` filter — quote verbatim).
       - `### Step 2 — Classify captured inputs into 8 sections` (system / users / triggers / data / rules / integrations / exceptions / failure-points — verbatim list, verbatim order; describe the `[unknown — needs human classification]` marker convention by quoting the literal string K5 asserts; reference `references/auto-classify-rubric.md` by relative path — K7 asserts this substring).
       - `### Step 3 — Senior-level challenge` (per CONVENTIONS.md late-step convention — name gaps, push back on TBDs).
       - `### Step 4 — Classify kickoff_branch` (decide `discovery-ready` vs `draft-sow` per DESIGN-17 lines 563-565; document the routing decision visibly inline in the artefact's `## Routing` block). This step MUST name both enum values explicitly (helps satisfy K4b).
       - `### Step 5 — Write and hand off` — emit the artefact at the output path with the DESIGN-17 frontmatter block (lift verbatim from RESEARCH §2 DESIGN-17 table — set `kickoff_branch:` to the concrete enum value chosen at Step 4, not a placeholder); then emit the verbatim handoff: `Awaiting status: approved write to 01_kickoff_v<N>.md. Branch routing on kickoff_branch: value (discovery-ready -> Stage 2; draft-sow -> SKIP Stage 2 -> Stage 3).` (DESIGN-17 line 574 verbatim).
    7. `## What this skill does not do`:
       - Does NOT call any Coda / Miro MCP or API (paste-only per D-72; future MCP captured in Deferred Ideas v2.6 / SURF-01..03).
       - Does NOT auto-merge Field Notes entries into the client brain — quotes each note + asks human keep/drop/edit-and-keep (MOD-8 prevention per DESIGN-09).
       - Does NOT auto-progress to Stage 2/3 — approval gate non-negotiable per DESIGN-06.
       - Does NOT accept raw-notes input bypassing the kickoff — kickoff is the sole upstream for `discovery-intake` (STG2-01 retired the raw-notes path).
    8. `## Quality bar` — bulleted "good looks like": every `[unknown — needs human classification]` marker is intentional + defensible; `kickoff_branch:` value documented in `## Routing` block; Field Notes rows traceable to their source row via `field_notes_processed_count:` count match; canonical references resolve.
  </action>
  <verify>
    <automated>bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --section kickoff 2>&1 | grep -E "(K1|K3|K5|K7)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `dydx-delivery/skills/kickoff-capture/SKILL.md`
    - `grep -qF '[unknown — needs human classification]' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K5)
    - `grep -qF 'dydx-delivery/references/safety-rules.md' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K3a)
    - `grep -qF 'dydx-delivery/references/stage-numbering.md' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K3b)
    - `grep -qF 'dydx-delivery/references/frontmatter-scheme.md' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K3c)
    - `grep -qF 'dydx-delivery/references/glossary.md' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K3d)
    - `grep -qF 'references/auto-classify-rubric.md' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K7)
    - `grep -qF 'Awaiting status: approved write to 01_kickoff_v<N>.md.' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (DESIGN-17 line 574 verbatim handoff)
    - `grep -qF 'discovery-ready' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K4b — both enums named in SKILL.md body)
    - `grep -qF 'draft-sow' dydx-delivery/skills/kickoff-capture/SKILL.md` succeeds (K4b)
    - `grep -cE '^### Step [1-5]' dydx-delivery/skills/kickoff-capture/SKILL.md` returns >= 5 (5 numbered steps)
  </acceptance_criteria>
  <done>SKILL.md exists with H1, frontmatter block, 4 canonical pointers inline (each Frontmatter/Glossary pointer documents enum inline), 5-step How-to-run, verbatim DESIGN-17 handoff, both enum values named, and rubric file reference — K1/K3/K5/K7 assertions pass and K4b contribution present.</done>
</task>

<task id="07-01-T3" type="auto">
  <name>Task 3: Author references/kickoff-template.md (concrete kickoff_branch value + enum-documenting comment + 8 H2 categories)</name>
  <files>dydx-delivery/skills/kickoff-capture/references/kickoff-template.md</files>
  <read_first>
    - `dydx-delivery/skills/discovery-intake/references/intake-template.md` (157 lines — frontmatter shape + body opening pattern; cross-check the 9-section discovery layout to confirm 8-section kickoff layout deviates per STG1-04 / R-04)
    - `.planning/DESIGN.md` lines 547-585 (DESIGN-17 — frontmatter fields line 562, marker convention line 566, 8-category list line 581)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §2 (DESIGN-17 lift map) + §11 R-02 (do NOT lift glossary `kickoff-direct | discovery-via` outlier; use `discovery-ready | draft-sow`)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §2 (template body shape; 8 H2 sections in STG1-04 order; Routing block)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/kickoff-capture/references/kickoff-template.md`.

    **C1 fix — concrete kickoff_branch value + enum-documenting comment.** The K4 structure-check asserts `^kickoff_branch: (discovery-ready|draft-sow)$` (anchored regex; matches a CONCRETE enum value line — NOT a placeholder like `<discovery-ready | draft-sow>` or a pipe-list literal). The template therefore carries a concrete enum value (default `discovery-ready` — the more common case per success criterion #1). To document the alternative, add an HTML comment AND a "Valid values:" line above the frontmatter block so reviewers know to switch to `draft-sow` when emitting a Stage-2-skipping kickoff.

    **Pre-frontmatter documentation block** (above the opening `---`):
    ```markdown
    <!--
      kickoff_branch enum values:
        - discovery-ready : Stage 2 (discovery-intake) will run against this kickoff. Default for engagements with material unknowns.
        - draft-sow       : Stage 2 will be SKIPPED. Use when the engagement is well-enough understood to draft a SOW directly from the kickoff.
      Reviewer: edit the `kickoff_branch:` line below to one of the two values at write time.
    -->

    > **Valid values for `kickoff_branch:`** — `discovery-ready` (Stage 2 runs) | `draft-sow` (Stage 2 skipped). Edit the frontmatter line below to one of these two values at write time.
    ```

    **Frontmatter block** (concrete enum value — `discovery-ready` as the planner-default per C1 fix; reviewer switches to `draft-sow` at write time when applicable):
    ```yaml
    ---
    client: <CLIENT_NAME>
    project: <PROJECT_NAME>
    frontmatter_version: 2
    kickoff_branch: discovery-ready
    field_notes_processed_count: <N>
    status: draft
    based_on_kickoff: <upstream-CR-id-or-N/A-for-fresh-engagement>
    ---
    ```
    K4 asserts `^kickoff_branch: (discovery-ready|draft-sow)$` — the line `kickoff_branch: discovery-ready` matches this regex via the alternation. The pre-frontmatter comment + Valid-values quote documents both enum values, satisfying K4b.

    **Body opening** — adapt from intake-template.md lines 11-13:
    ```markdown
    # Kickoff — <CLIENT_NAME> · <ENGAGEMENT_NAME>

    > Stage 1 of the dydx-delivery pipeline. Captures the operational reality of an engagement / change request so downstream stages (discovery / SOW / fnspec / tech-spec) have a real foundation. Mark every unknown explicitly using the `[unknown — needs human classification]` marker convention — do not guess.
    ```
    The verbatim marker string is embedded once here so reviewers see it before they start filling sections.

    **8 H2 sections in STG1-04 order** (verbatim, this order — RESEARCH §11 R-04 confirms the 9-vs-8 mismatch with discovery is INTENTIONAL per STG2-03; do NOT add "Business outcome" to align with discovery):
    1. `## 1. System` — what's being built/changed; current state vs target state.
    2. `## 2. Users` — who the workflow serves; primary actors + secondary stakeholders.
    3. `## 3. Triggers` — what kicks off the workflow; manual vs scheduled vs event-driven.
    4. `## 4. Data` — inputs the workflow consumes; outputs it produces; persistence boundaries.
    5. `## 5. Rules` — business logic; conditional branches; approval gates.
    6. `## 6. Integrations` — external systems involved (platforms / SaaS / APIs / paste-fed surfaces).
    7. `## 7. Exceptions` — non-happy paths; error states; reviewer overrides.
    8. `## 8. Failure points` — known fragility; integration boundaries; observability gaps.

    Each section body mirrors intake-template.md lines 17-26 shape (mix of prose prompts, table for enumerated lists when relevant, italic placeholders). Each section MUST display the `[unknown — needs human classification]` marker convention inline at least once (e.g., a placeholder row in a table or an explicit italic prompt) — reinforces STG1-04.

    **`## Routing` block** (before `## Sign-off`) — author from STG1-02 + DESIGN-17 lines 563-565:
    ```markdown
    ## Routing

    **kickoff_branch:** (set in frontmatter — choose one)
    - `discovery-ready` — discovery is needed before SOW; routes to Stage 2 (`discovery-intake`).
    - `draft-sow` — engagement is well-enough understood to skip discovery; routes directly to Stage 3 (`generate-sow`). Stage 2 will emit `Stage 2 SKIPPED — kickoff branch = draft-sow` and write no `02_discovery_v<N>.md` artefact.

    **Rationale:** <one-paragraph explanation of why this branch fits this engagement>
    ```

    **`## Sign-off` block** (provenance + approval gate placeholder) — lift shape from intake-template.md's analogous section.
  </action>
  <verify>
    <automated>grep -qE '^kickoff_branch: (discovery-ready|draft-sow)$' dydx-delivery/skills/kickoff-capture/references/kickoff-template.md && grep -cE '^## [1-8]\.' dydx-delivery/skills/kickoff-capture/references/kickoff-template.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `dydx-delivery/skills/kickoff-capture/references/kickoff-template.md`
    - `grep -qE '^kickoff_branch: (discovery-ready|draft-sow)$' kickoff-template.md` succeeds (K4 — anchored regex matches the concrete-value line)
    - `grep -qF 'discovery-ready' kickoff-template.md` succeeds (K4b — first enum value documented)
    - `grep -qF 'draft-sow' kickoff-template.md` succeeds (K4b — second enum value documented in the pre-frontmatter comment / Valid-values line / Routing block)
    - `grep -c '^## [1-8]\.' kickoff-template.md` returns 8 (8 numbered H2 categories present)
    - `grep -qF 'System' kickoff-template.md` AND `grep -qF 'Failure points' kickoff-template.md` succeed (first + last category names from STG1-04)
    - `grep -qF '[unknown — needs human classification]' kickoff-template.md` succeeds (marker convention embedded inline)
    - `grep -qF '## Routing' kickoff-template.md` succeeds
    - File does NOT contain string `kickoff-direct` or `discovery-via` (verifies R-02 — glossary outlier spelling not lifted)
    - File contains the pre-frontmatter "Valid values" documentation line (verifiable via `grep -qF 'Valid values for' kickoff-template.md`)
  </acceptance_criteria>
  <done>Template exists with concrete `kickoff_branch: discovery-ready` value + enum-documenting comment + Valid-values line + 8 H2 categories in STG1-04 order + marker inline + Routing block — K4 + K4b both pass via this file alone; R-02 outlier spelling avoided.</done>
</task>

<task id="07-01-T4" type="auto">
  <name>Task 4: Author references/auto-classify-rubric.md (D-73 triggers + escalation table + operational principle)</name>
  <files>dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md</files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md` D-73 (5 explicit triggers + escalation + operational principle — verbatim source)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §3 (no-analog — body shape guidance from D-73)
    - `.planning/REQUIREMENTS.md` STG1-04 (marker convention authority)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md`. No upstream analog (first confidence rubric in the plugin per PATTERNS §3); author body from D-73 + STG1-04.

    **Body structure** (follow PATTERNS §3 shape verbatim):

    1. H1 + one-paragraph purpose statement explaining when to emit the `[unknown — needs human classification]` marker.

    2. `## Explicit triggers` — 5 numbered triggers from D-73 (lift verbatim wording):
       1. **Category not named verbatim** in any source input (meeting notes / Miro paste / Field Notes paste).
       2. **< 2 distinct source mentions** — would require synthesising from a single witness.
       3. **Source contains explicit `TBD` / `?` / `unclear` / `come back to this`** text against this category.
       4. **Source mentions cross conflicting alternatives** without resolution.
       5. **Reviewer pre-flagged TBD** in the kickoff template input.

    3. `## Input-signal → outcome escalation` — markdown table mapping each trigger to one of three outcomes:
       | Trigger | Section keeps marker | Section partially fills + retains marker | Section proceeds without marker |
       | Trigger 1 (not named) | X | | |
       | Trigger 2 (<2 mentions) | | X | |
       | Trigger 3 (explicit TBD text) | X | | |
       | Trigger 4 (conflicting alts) | X | | |
       | Trigger 5 (reviewer-pre-flagged) | X | | |

    4. `## Operational principle` (final defensive backstop):
       > Mark unknown when you'd hesitate to defend the classification to a reviewer.

       This rule supersedes the explicit triggers above when reviewer judgement says the section is under-evidenced.

    5. `## How this rubric is consumed` — short paragraph noting that `kickoff-capture/SKILL.md` Step 2 references this file by relative path (K7 assertion); structure-check K5 asserts the marker convention is documented in SKILL.md (not here). Note that the rubric is invoked regardless of `kickoff_branch:` value (the rubric runs at Step 2; the branch decision happens at Step 4).

    Discretion: planner may adjust the trigger threshold ("<2 distinct source mentions" → "<3") if rationale exists; default is the D-73 verbatim wording.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md && grep -c '^[0-9]\.' dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `dydx-delivery/skills/kickoff-capture/references/auto-classify-rubric.md`
    - `grep -qF 'Explicit triggers' auto-classify-rubric.md` succeeds
    - `grep -qF 'Operational principle' auto-classify-rubric.md` succeeds
    - `grep -qF '[unknown — needs human classification]' auto-classify-rubric.md` succeeds (marker convention referenced)
    - `grep -c '^[0-9]\. \*\*' auto-classify-rubric.md` returns >= 5 (5 numbered triggers)
    - `grep -qF 'Mark unknown when you' auto-classify-rubric.md` succeeds (operational principle present)
  </acceptance_criteria>
  <done>Rubric file authored from D-73 spec with 5 triggers + escalation table + operational principle backstop.</done>
</task>

<task id="07-01-T5" type="auto">
  <name>Task 5: Author references/capture-paths.md (3 paste protocols + processed_at IS NULL + MOD-8 prevention)</name>
  <files>dydx-delivery/skills/kickoff-capture/references/capture-paths.md</files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md` D-72 (Field Notes Coda doc-only paste protocol — verbatim)
    - `.planning/REQUIREMENTS.md` STG1-05 (3 capture paths) + STG1-03 (`processed_at IS NULL` filter + MOD-8 prevention)
    - `.planning/DESIGN.md` (DESIGN-07 + AUDIT-08 — Miro paste fallback shape; DESIGN-09 — Field Notes directional boundary + MOD-8 prevention rule)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §4 (no-analog body shape guidance from STG1-05 + D-72)
  </read_first>
  <action>
    Author NEW `dydx-delivery/skills/kickoff-capture/references/capture-paths.md`. No upstream analog; author body per PATTERNS §4 shape.

    **Body structure:**

    1. H1 + one-paragraph intro: "Three paths feed kickoff-capture. All are paste-only per UAT-3.5 / UAT-6.1 (no MCP, no API calls in v2.2). Future Coda MCP integration is captured as a Deferred Idea (v2.6 / SURF-01..03 candidate). Each path produces a kickoff with a `kickoff_branch:` value of either `discovery-ready` or `draft-sow` chosen at Step 4."

    2. `## Path 1 — Meeting notes paste (freeform)`
       - Protocol: reviewer pastes raw meeting notes, transcript snippet, brief, or email thread into the kickoff invocation.
       - Triage: kickoff classifies content into the 8 STG1-04 sections per `references/auto-classify-rubric.md`.
       - No format requirements; freeform text is the contract.

    3. `## Path 2 — Miro paste fallback`
       - Protocol per DESIGN-07 + AUDIT-08: image paste from Miro board + reviewer-supplied text describing the workflow shown. No "render whole board" assumption — kickoff cannot crawl Miro.
       - Triage: reviewer narrates the diagram in prose; kickoff classifies the narrated content.
       - Explicit limit: kickoff does NOT OCR images; the narration is the canonical source.

    4. `## Path 3 — Field Notes (Coda) paste`
       - The Field Notes Coda table is a **read-only input queue**. Kickoff never auto-merges entries (MOD-8 prevention per DESIGN-09).
       - **Triage filter (default):** `processed_at IS NULL` (verbatim — K6 asserts this exact string).
       - **Protocol:**
         1. Reviewer queries Coda manually (web app + filter `processed_at IS NULL`).
         2. Reviewer pastes rows into the kickoff session.
         3. For each row, kickoff quotes the note + asks human: **keep / drop / edit-and-keep** (MOD-8 prevention).
         4. On keep/edit-and-keep, kickoff classifies the row content into the 8 STG1-04 sections; on drop, kickoff records the rejection inline in the artefact (audit trail).
         5. `field_notes_processed_count:` frontmatter field tracks how many rows were processed (NOT how many were kept — every row touched counts).

    5. `## Deferred — Coda MCP integration`
       - Captured under v2.6 / SURF-01..03 evaluation. Will replace the manual paste step. Out of scope per UAT-3.5 through v2.6.
  </action>
  <verify>
    <automated>grep -qF 'processed_at IS NULL' dydx-delivery/skills/kickoff-capture/references/capture-paths.md && grep -cE '^## Path [1-3]' dydx-delivery/skills/kickoff-capture/references/capture-paths.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `dydx-delivery/skills/kickoff-capture/references/capture-paths.md`
    - `grep -qF 'processed_at IS NULL' capture-paths.md` succeeds (K6 verbatim filter assertion)
    - `grep -c '^## Path [1-3]' capture-paths.md` returns 3 (3 capture paths)
    - `grep -qF 'Meeting notes paste' capture-paths.md` succeeds (Path 1)
    - `grep -qF 'Miro paste' capture-paths.md` succeeds (Path 2)
    - `grep -qF 'Field Notes' capture-paths.md` succeeds (Path 3)
    - `grep -qiE '(keep|drop|edit-and-keep)' capture-paths.md` succeeds (MOD-8 protocol documented)
    - `grep -qF 'read-only input queue' capture-paths.md` succeeds (DESIGN-09 directional boundary documented)
  </acceptance_criteria>
  <done>3-path protocol file authored with verbatim `processed_at IS NULL` filter wording and MOD-8 prevention spelled out.</done>
</task>

<task id="07-01-T6" type="auto">
  <name>Task 6: Run phase7-structure-check.sh --section kickoff (final plan gate)</name>
  <files></files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh` (the script authored in T1)
    - All 4 files authored in T2-T5
  </read_first>
  <action>
    Run the kickoff-section gate. Resolve any failed asserts surfaced by the script before marking the plan done.

    Command:
    ```bash
    bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --section kickoff
    ```

    Expected output: every K1-K7 assertion + K4b assertion emits `PASS:` followed by `ALL ASSERTIONS PASSED` and `exit 0`. Total kickoff-section PASS lines expected: 8 (K1, K2, K3, K4, K4b, K5, K6, K7).

    If any K-assertion fails, the executor fixes the source file (NOT the script — the script is the contract).

    **WARNING (per `<plan_warning>` block above and per cross-AI review C6 finding):** `--all` and `--section discovery|sow` are FORBIDDEN inside 07-01 except as expected-failure diagnostics. Cross-section asserts (X1+X2) and other sections (D1-D3, S1-S2) reference files that 07-02 + 07-03 + 07-04 ship — they will fail during 07-01 and that is correct. Do NOT attempt to "fix" them by editing the script; the script is the contract.
  </action>
  <verify>
    <automated>bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --section kickoff</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0
    - stdout contains `ALL ASSERTIONS PASSED`
    - stdout contains 8 `PASS:` lines covering K1, K2, K3, K4, K4b, K5, K6, K7
  </acceptance_criteria>
  <done>`--section kickoff` exits 0 with 8 PASS lines (K1-K7 + K4b) — STG1-01..05 verified at the structural level. Plan 07-01 complete; downstream 07-02 and 07-03 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer → kickoff-capture skill | Reviewer pastes meeting notes / Miro narration / Field Notes rows into the skill invocation. Input is reviewer-controlled (not external attacker); trust posture is low risk. |
| Coda → reviewer → kickoff | Field Notes paste path. Coda content originates from project team members. Trust = internal. |
| kickoff write-path → filesystem | Skill writes `01_kickoff_v<N>.md` to `<Client> Brain/<Project>/`. Path components come from frontmatter (`client:`, `project:`) supplied by reviewer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | kickoff-template.md frontmatter `kickoff_branch:` enum value drift | mitigate | K4 structure-check asserts `^kickoff_branch: (discovery-ready|draft-sow)$` (anchored regex, concrete enum value). K4b additionally asserts both enum values appear somewhere across the 4 kickoff-capture skill files (enum-documented assertion). Both K4 and K4b together catch (a) wrong enum value in template, (b) enum spelling drift, and (c) missing documentation of the alternative branch. Downstream consumers (07-02, 07-03, 07-04) also branch on this value; bad values fail-loud at runtime. |
| T-07-02 | Information disclosure | Field Notes paste containing PII | accept | Reviewer-driven paste; PII handling is governed by client engagement policy. Out of plugin scope per UAT-3.5 (paste-only, no automated sync). |
| T-07-03 | Spoofing / Repudiation | Path traversal via reviewer-supplied `client:` / `project:` frontmatter values | accept | Reviewer is the operator; no untrusted writer. Future `validate-frontmatter` hook (v2.6 / SURF-01..03) will sanitise path components; deferred per scope-lock. |
| T-07-04 | Tampering | phase7-structure-check.sh assertion strings drift from source-of-truth | mitigate | Assertion strings are the single source of truth (D-77). Any drift between SKILL.md text and structure-check `grep -qF` literal will fail-loud at next gate. R-01/R-02 risks (skip-message + glossary enum) explicitly resolved per RESEARCH §11. C1 fix splits K4 into K4 + K4b to close the previous regex/template inconsistency. |
| T-07-05 | Denial of service | Field Notes paste of large batch (>1000 rows) | accept | Manual paste throughput naturally bounded by reviewer; no scaling concern in v2.2. |

ASVS L1 alignment: doc-only paste protocols with no execution surface; primary threats are tampering of canonical strings (mitigated via K1-K7 + K4b grep gates) and accidental PII paste (accepted per scope-lock).
</threat_model>

<verification>
- Plan-local: `bash phase7-structure-check.sh --section kickoff` exits 0 (K1-K7 + K4b all green — 8 PASS lines).
- Cross-section: `--section discovery` and `--section sow` are EXPECTED to fail at end of 07-01 (07-02 and 07-03 not yet shipped) — do NOT regress this expectation. `--all` is FORBIDDEN in 07-01 except as expected-failure diagnostic (per C6 / `<plan_warning>`).
- Manual: spot-check that all 4 canonical-pointer paths in SKILL.md actually exist on disk at `dydx-delivery/references/{safety-rules,stage-numbering,frontmatter-scheme,glossary}.md` (Phase 5 / FOUND-01..04 output — should already exist).
- Manual: read `kickoff-template.md` Routing block end-to-end; confirm both `discovery-ready` and `draft-sow` branches are explained in plain English.
</verification>

<success_criteria>
- 5 new files exist at the paths in `files_created`.
- `bash phase7-structure-check.sh --section kickoff` exits 0 with `ALL ASSERTIONS PASSED` and 8 PASS lines (K1-K7 + K4b).
- STG1-01..05 requirement bodies are addressable via structure-check assertions (K1-K7 + K4b ↔ STG1-01..05 mapping in 07-VALIDATION.md).
- 07-02 and 07-03 can begin in Wave 2 with a stable kickoff frontmatter contract.
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-01-SUMMARY.md` recording:
- Files created (5)
- Final `--section kickoff` exit code (0 expected) + count of PASS lines (8 expected)
- Concrete `kickoff_branch:` value chosen in `kickoff-template.md` (default `discovery-ready` per C1 fix)
- Any deviations from D-70 / D-73 / D-72 author-from-spec defaults
- Any threshold adjustments to auto-classify-rubric.md trigger wording
</output>
</content>
</invoke>