---
plan: 07-04
phase: 7
slug: stage-1-kickoff-discovery-sow-upstream-wiring
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - .planning/DESIGN.md
  - .planning/REQUIREMENTS.md
files_created:
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-meeting-notes.md
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-miro-paste.md
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-field-notes.md
autonomous: false
requirements: [STG1-01, STG1-02, STG1-03, STG1-04, STG1-05, STG2-01, STG2-02, STG2-03, STG3-01, STG3-02]
must_haves:
  truths:
    - "DESIGN.md glossary line ~1482 fixed to discovery-ready | draft-sow (R-02 — glossary correction only, no design contract change)"
    - "Sample-CR fixtures exist (3 files) for manual e2e smoke"
    - "Manual e2e smoke completes against fixtures with concrete invocation commands, artefact paths, and frontmatter snippet captures recorded in 07-04-SUMMARY.md"
    - "bash phase7-structure-check.sh --all exits 0 (K1-K7 + K4b + D1-D3 + S1-S2 + X1-X2 all PASS) — runs BEFORE REQUIREMENTS.md trace flips per C5 ordering fix"
    - "REQUIREMENTS.md traceability table flipped — all 10 v2.2 Phase 7 requirement rows show Pending → Satisfied (flipped ONLY after --all exits 0 per C5)"
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "Glossary entry aligned with 4-source authoritative spelling (R-02 closed)"
      contains: "kickoff_branch enum as discovery-ready | draft-sow at line ~1482; no kickoff-direct or discovery-via tokens remaining"
    - path: ".planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/"
      provides: "Sample-CR fixtures for manual e2e smoke"
      contains: "3 files: meeting-notes (~30 lines) + Miro paste (~15 lines) + Field Notes (~20 lines, processed_at IS NULL rows)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table reflecting Phase 7 satisfaction"
      contains: "All 10 v2.2 Phase 7 requirement rows (STG1-01..05 + STG2-01..03 + STG3-01..02) marked Satisfied / [x]"
  key_links:
    - from: "manual e2e smoke (T-smoke)"
      to: "07-04-SUMMARY.md"
      via: "reviewer records concrete artefact paths + frontmatter snippets + absence-of-02-on-draft-sow confirmation"
      pattern: "01_kickoff_v|02_discovery_v|03_sow_v|based_on_kickoff|based_on_discovery|kickoff_branch"
    - from: "phase7-structure-check.sh --all (T-gate)"
      to: "REQUIREMENTS.md trace flips (T-flips)"
      via: "T-gate exit 0 is the precondition for T-flips per C5 ordering"
      pattern: "ALL ASSERTIONS PASSED"
---

<plan_origin>
**This plan was created by splitting synthesis out of the original 07-03-PLAN.md per cross-AI review C2 finding.** Codex identified that 07-03's `--all` structure-check gate (T7 in the old plan) had an undeclared dependency on 07-02 completion — in Wave 2 parallel execution, 07-03's final gate could run before 07-02's D1-D3 assertions were satisfied, causing non-deterministic failure. Codex also flagged (C5) that the REQUIREMENTS.md trace flips (T6 in the old plan) ran BEFORE the `--all` gate, risking false "Satisfied" state on assertion failure.

D-69 explicitly authorizes the split: "synthesis SPLITS to 07-04-PLAN.md if 07-03 exceeds ~400 LOC OR creates ordering problems". C2 is the latter. Codex suggested option B by name.

This plan therefore carries:
- R-02 DESIGN.md glossary fix (C7 — labelled "correction only, no contract change")
- Sample-CR fixture authorship (3 files)
- Manual e2e smoke (C4 — strengthened acceptance with concrete commands, paths, frontmatter snippets)
- `phase7-structure-check.sh --all` final gate (C5 — runs BEFORE trace flips)
- REQUIREMENTS.md 10-row trace flips (C5 — runs AFTER --all exits 0)

Wave 3 / `depends_on: [07-02, 07-03]` serialises this after both Wave 2 plans land.
</plan_origin>

<objective>
Phase 7 synthesis — closes R-02 / R-05 cleanups, lands sample-CR fixtures, runs the manual e2e smoke against the now-shipped 3 skills, runs the full phase structure-check, then flips REQUIREMENTS.md traceability.

Task ordering enforces the C5 fix: structure-check `--all` MUST exit 0 before REQUIREMENTS.md trace is updated. If `--all` fails, trace flips do NOT happen.

Output: 1 modified DESIGN.md + 3 new fixture files + 1 modified REQUIREMENTS.md + 1 successful `--all` run + 1 manual e2e smoke attestation captured in SUMMARY.

Wave 3 — runs after 07-02 + 07-03 ship. `autonomous: false` because T-smoke requires reviewer interaction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DESIGN.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-01-SUMMARY.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-02-SUMMARY.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-03-SUMMARY.md
@dydx-delivery/skills/kickoff-capture/SKILL.md
@dydx-delivery/skills/discovery-intake/SKILL.md
@dydx-delivery/skills/generate-sow/SKILL.md

<interfaces>
**R-02 glossary fix target (DESIGN.md line ~1482) — C7: glossary correction only, no design contract change.**
- BEFORE: `kickoff-direct | discovery-via`
- AFTER: `discovery-ready | draft-sow`

**Manual e2e smoke artefact path convention (per C4 fix and RESEARCH §10b non-binding recommendation):**
- Default workspace: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/`
- Rationale: co-located with input fixtures; doesn't pollute a real `<Client> Brain/<Project>/`. Reviewer may override to a designated test brain but the default MUST be the co-located fixtures/output path.

**Manual e2e smoke artefact filename convention:**
- Kickoff outputs: `01_kickoff_v1.md`
- Discovery outputs (discovery-ready branch ONLY): `02_discovery_v1.md`
- SOW outputs: `03_sow_v1.md`
- Filename version suffix: `_v1` for first run; `_v2` etc. on retries.

**Frontmatter snippet capture (per C4):**
- Kickoff `01_kickoff_v1.md`: capture the entire frontmatter block; verify it contains `kickoff_branch:` with one of (`discovery-ready` | `draft-sow`).
- Discovery `02_discovery_v1.md` (when present): capture `based_on_kickoff:` field value (must reference the corresponding kickoff).
- SOW `03_sow_v1.md`: capture either `based_on_discovery:` (on discovery-ready branch) or `based_on_kickoff:` (on draft-sow branch). Never both.

**Structure-check final gate command:**
```
bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --all
```
Expected: 15 PASS lines total (K1, K2, K3, K4, K4b, K5, K6, K7 = 8 kickoff; D1, D2, D3 = 3 discovery; S1, S2 = 2 sow; X1, X2 = 2 cross) followed by `ALL ASSERTIONS PASSED` and exit 0.
</interfaces>
</context>

<tasks>

<task id="07-04-T1" type="auto">
  <name>Task 1: R-02 glossary fix — DESIGN.md line ~1482 kickoff_branch enum alignment (correction only, no contract change)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - `.planning/DESIGN.md` lines 1475-1490 (glossary entry context around line 1482)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §11 R-02 (4-vs-1 source vote, authoritative spelling)
  </read_first>
  <action>
    **Scope: glossary correction only — no design contract change.** Per cross-AI review C7 finding: DESIGN.md glossary line ~1482 carries a stale enum (`kickoff-direct | discovery-via`) that contradicts 4 authoritative sources:
    - REQUIREMENTS.md STG1-02 (`discovery-ready | draft-sow`)
    - ROADMAP.md Phase 7 success criterion #1 (`discovery-ready | draft-sow`)
    - 07-CONTEXT.md D-69 + D-77 table (`discovery-ready | draft-sow`)
    - DESIGN.md body at line 562 / DESIGN-17 (`discovery-ready | draft-sow`)
    - `dydx-delivery/references/frontmatter-scheme.md` (`discovery-ready | draft-sow`)

    This is a **typo correction in a glossary annotation, NOT a change to a locked DESIGN row.** The locked DESIGN-17 contract at line 562 already uses the authoritative spelling. Aligning the glossary closes R-02 without re-litigating any design decision.

    Surgical one-line edit to `.planning/DESIGN.md` glossary entry around line 1482. Locate the `kickoff_branch:` enum definition; replace the outlier spelling with the authoritative spelling.

    BEFORE (line ~1482): `kickoff-direct | discovery-via`
    AFTER: `discovery-ready | draft-sow`

    Surrounding context is preserved untouched. This is a 1-token swap — the glossary entry's description, definition, or other prose stays as-is.

    If the line number has drifted from RESEARCH §11's note of ~1482, search for `kickoff-direct` or `discovery-via` literal strings in DESIGN.md and fix every occurrence — there should be only ONE occurrence (the glossary entry). Confirm via `grep -c "kickoff-direct" .planning/DESIGN.md` returns 1 before edit (single drift instance) and 0 after edit.
  </action>
  <verify>
    <automated>! grep -qF "kickoff-direct" .planning/DESIGN.md && ! grep -qF "discovery-via" .planning/DESIGN.md && grep -qF "discovery-ready" .planning/DESIGN.md && grep -qF "draft-sow" .planning/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kickoff-direct" .planning/DESIGN.md` returns 0 (R-02 — outlier removed)
    - `grep -c "discovery-via" .planning/DESIGN.md` returns 0 (R-02 — outlier removed)
    - `grep -qF "discovery-ready" .planning/DESIGN.md` succeeds (authoritative spelling present)
    - `grep -qF "draft-sow" .planning/DESIGN.md` succeeds (authoritative spelling present)
    - `git diff --stat .planning/DESIGN.md` shows a single-file change with minimal line delta (1-token swap; no surrounding-prose churn)
  </acceptance_criteria>
  <done>DESIGN.md glossary entry aligned with the 4-source authoritative spelling. R-02 resolved. No design contract changed — annotation correction only per C7.</done>
</task>

<task id="07-04-T2" type="auto">
  <name>Task 2: Author 3 sample-CR fixtures for manual e2e smoke</name>
  <files>
    .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-meeting-notes.md,
    .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-miro-paste.md,
    .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-field-notes.md
  </files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §10b (sample-CR fixture proposal — line counts + content shape)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md` (Manual-Only Verifications row for e2e smoke)
    - `.planning/REQUIREMENTS.md` STG1-05 (three capture paths)
  </read_first>
  <action>
    Author 3 minimal fixture files under `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/` for the manual e2e smoke task (T-smoke). Each fixture represents one of the three STG1-05 capture paths. Content is illustrative — a small synthetic change request with enough operational signal to populate a kickoff artefact's 8 categories.

    **Fixture 1 — `sample-cr-meeting-notes.md` (~30 lines):**
    Freeform meeting-notes paste. Synthetic scenario: e.g., "VodafoneZiggo wants a new Pipefy phase for ticket auto-routing based on customer tier" or similar plausible CR for one of the established clients (Up & Up Group or VodafoneZiggo per D-65). Include enough signal to populate at least 5 of the 8 kickoff categories (system / users / triggers / data / rules); leave some categories under-evidenced so the `[unknown — needs human classification]` marker has reason to fire (validates STG1-04 rubric).

    **Fixture 2 — `sample-cr-miro-paste.md` (~15 lines per DESIGN-07 / AUDIT-08 shape):**
    Text describing a workflow diagram pasted from Miro. Use markdown to represent the diagram shape (boxes + arrows in prose, e.g., "Box: Ticket Created → Box: Triage Phase → [Decision: tier?] → Box: Studio Lead Queue / Director Queue"). No actual image — narration is the canonical source per DESIGN-07.

    **Fixture 3 — `sample-cr-field-notes.md` (~20 lines, 2-3 mock Field Notes rows):**
    Mock Field Notes Coda paste — 2 or 3 rows in markdown table form, each with `processed_at: NULL` (verbatim — matches the K6 triage filter wording). Each row captures a single operational observation (e.g., "Ticket 12345 stuck in Triage for 3 days — no SLA on Triage Phase"). Include at least one row that should be DROPPED by the reviewer (MOD-8 prevention validation — the kickoff session asks keep/drop/edit-and-keep).

    All three fixtures begin with a YAML front-matter block declaring `fixture: true` + `fixture_for: phase-7-e2e-smoke` so the e2e smoke task in T-smoke can confirm fixtures are not mistaken for production CRs.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-meeting-notes.md && test -f .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-miro-paste.md && test -f .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-field-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixture files exist at the paths in `files_created`
    - `grep -qF "fixture_for: phase-7-e2e-smoke" sample-cr-meeting-notes.md` succeeds (and same for the other two fixtures)
    - `grep -qF "processed_at" sample-cr-field-notes.md` succeeds (Field Notes shape — verbatim filter column referenced)
    - `wc -l sample-cr-meeting-notes.md` returns approximately 25-40 lines (target ~30)
    - `wc -l sample-cr-miro-paste.md` returns approximately 12-20 lines (target ~15)
    - `wc -l sample-cr-field-notes.md` returns approximately 15-25 lines (target ~20)
  </acceptance_criteria>
  <done>3 fixtures authored; ready for manual e2e smoke in T-smoke.</done>
</task>

<task id="07-04-T-smoke" type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (T-smoke): Manual e2e smoke against sample-CR fixtures (kickoff → discovery → sow) with concrete commands + path + frontmatter capture</name>
  <what-built>
    Phase 7 ships 3 skills (kickoff-capture NEW; discovery-intake MODIFIED; generate-sow MODIFIED) plus structure-check harness plus 3 sample-CR fixtures. The structure-check harness validates static structural properties of the skill bodies; it does NOT exercise the skills end-to-end. This checkpoint runs the skills against the fixtures from T2 to validate the actual pipeline behaviour.

    **Per cross-AI review C4 fix:** this checkpoint has been strengthened with (1) exact invocation commands per skill+fixture, (2) exact artefact output paths recorded in 07-04-SUMMARY.md, (3) frontmatter-field snippet captures, and (4) explicit workspace path convention (`.planning/phases/07-.../fixtures/output/`).
  </what-built>
  <how-to-verify>
    **Workspace path convention (per C4):** all e2e smoke artefacts are written to `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/`. Create this directory first if it doesn't exist:
    ```bash
    mkdir -p .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/
    ```
    Rationale: co-located with input fixtures; doesn't pollute a real `<Client> Brain/<Project>/`. Reviewer may override to a designated test brain but the default for this checkpoint is the co-located fixtures/output path.

    Reviewer executes the following smoke sequence. **Each numbered step includes the EXACT invocation command, the EXPECTED artefact output path, and the FRONTMATTER FIELDS to capture in 07-04-SUMMARY.md.**

    **Step 1 — Kickoff (meeting-notes path, discovery-ready branch)**
    - Invocation: invoke the `kickoff-capture` skill against `fixtures/sample-cr-meeting-notes.md`. The exact runner command depends on the project's standard skill-invocation pattern (e.g., `claude --skill kickoff-capture --input ...` or the plugin's local equivalent — check `dydx-delivery/skills/kickoff-capture/SKILL.md` "How to run" section and `dydx-delivery/AGENTS.md` / project README for the runner convention).
    - Expected output path: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/01_kickoff_v1.md`
    - Choose `kickoff_branch: discovery-ready` based on the fixture's content depth (there is material discovery work needed).
    - Capture for SUMMARY: the `kickoff_branch:` line from this artefact's frontmatter (expected: `kickoff_branch: discovery-ready`).
    - Mark `status: approved` (manual approval gate per DESIGN-06).

    **Step 2 — Discovery (discovery-ready branch)**
    - Invocation: invoke `discovery-intake` skill against the approved `01_kickoff_v1.md` from Step 1.
    - Expected output path: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/02_discovery_v1.md`
    - Capture for SUMMARY: the `based_on_kickoff:` line from this artefact's frontmatter (expected: `based_on_kickoff: 01_kickoff_v1.md`).
    - Mark `status: approved`.

    **Step 3 — Kickoff (Miro path, draft-sow branch)**
    - Invocation: invoke `kickoff-capture` against `fixtures/sample-cr-miro-paste.md`. Choose `kickoff_branch: draft-sow` this time (engagement is well-enough understood from the Miro flow).
    - Expected output path: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/01_kickoff_v2.md` (second kickoff in the same workspace; suffix `_v2`).
    - Capture for SUMMARY: the `kickoff_branch:` line from this artefact's frontmatter (expected: `kickoff_branch: draft-sow`).
    - Mark `status: approved`.

    **Step 4 — Discovery skip (draft-sow branch)**
    - Invocation: invoke `discovery-intake` against the approved `01_kickoff_v2.md` from Step 3.
    - Expected behaviour: skill emits the verbatim message `Stage 2 SKIPPED — kickoff branch = draft-sow` to stdout / handoff log AND writes NO `02_discovery_v<N>.md` artefact (per D-74).
    - Verify absence: `ls .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/02_discovery_v*` for kickoff_v2 must show no NEW file produced (the only `02_discovery_v1.md` present is from Step 2's discovery-ready run). One way to confirm: rename `02_discovery_v1.md` to `02_discovery_v1.md.from-step2` before running Step 4, then confirm no new `02_discovery_v*.md` appears.
    - Capture for SUMMARY: explicit confirmation that no `02_discovery_v*.md` file was produced on this branch + the exact stdout / handoff log line containing the verbatim skip-message.

    **Step 5 — SOW from discovery-ready branch**
    - Invocation: invoke `generate-sow` against the approved `02_discovery_v1.md` from Step 2.
    - Expected output path: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/03_sow_v1.md`
    - Capture for SUMMARY: the `based_on_discovery:` line from this artefact's frontmatter (expected: `based_on_discovery: 02_discovery_v1.md`). Also confirm both `## Platform Scope` and `## Integration Scope` H2 sections are present (per STG3-02 + D-75).

    **Step 6 — SOW from draft-sow branch**
    - Invocation: invoke `generate-sow` against the approved `01_kickoff_v2.md` from Step 3 (no intervening discovery).
    - Expected output path: `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/output/03_sow_v2.md`
    - Capture for SUMMARY: the `based_on_kickoff:` line from this artefact's frontmatter (expected: `based_on_kickoff: 01_kickoff_v2.md`). Confirm both `## Platform Scope` and `## Integration Scope` H2 sections are present.

    **Step 7 (optional bonus) — Field Notes path validates MOD-8 prevention**
    - Invocation: invoke `kickoff-capture` against `fixtures/sample-cr-field-notes.md`.
    - Validate the MOD-8 prevention prompt asks `keep / drop / edit-and-keep` for each Field Notes row; mark the one row designated as "should be dropped" with `drop`; observe the artefact records the rejection inline (audit trail per DESIGN-09).
    - Capture for SUMMARY: confirmation that the keep/drop/edit-and-keep prompt fired for each row + the drop decision was recorded inline.

    **What must be recorded in 07-04-SUMMARY.md (per C4):**
    1. The 4 actual artefact paths produced (Steps 1, 2, 5, 6) and the absence-of-artefact confirmation for Step 4.
    2. Three frontmatter-field captures: (a) `kickoff_branch:` value on each of the two kickoffs, (b) `based_on_kickoff:` value on the discovery artefact AND on the draft-sow SOW artefact, (c) `based_on_discovery:` value on the discovery-ready SOW artefact.
    3. The verbatim skip-message line observed in Step 4's stdout / log (must match `Stage 2 SKIPPED — kickoff branch = draft-sow` exactly).
    4. Confirmation that both SOW artefacts contain `## Platform Scope` AND `## Integration Scope` H2 sections (even if one is empty / "no integration scope" placeholder).
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark the e2e smoke pass, or describe any failures (which step, which fixture, what was wrong) for a follow-up fix task.

    The acceptance gate for this checkpoint is that `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-SUMMARY.md` MUST exist by the end of this plan AND contain all 4 artefact paths AND all 3 frontmatter-field captures listed above.
  </resume-signal>
  <acceptance_criteria>
    - Reviewer types "approved" or equivalent confirmation
    - All 3 fixture-driven smoke runs produce expected artefacts (or expected skip behaviour) at the expected paths under `.planning/phases/07-.../fixtures/output/`
    - Verbatim skip-message string `Stage 2 SKIPPED — kickoff branch = draft-sow` observed exactly on the draft-sow path
    - No `02_discovery_v*.md` artefact produced for the draft-sow fixture (kickoff_v2)
    - Both SOW artefacts contain `## Platform Scope` AND `## Integration Scope` H2 sections (even if one is empty / "no integration scope" placeholder)
    - `07-04-SUMMARY.md` exists AND contains: 4 artefact paths (kickoff_v1, discovery_v1, sow_v1 on discovery-ready, sow_v2 on draft-sow), absence confirmation for discovery_v2, frontmatter field captures (kickoff_branch on both kickoffs, based_on_kickoff on discovery_v1 and sow_v2, based_on_discovery on sow_v1)
  </acceptance_criteria>
  <done>Manual e2e smoke passes with C4-strengthened acceptance — concrete paths + frontmatter snippets + skip-message verbatim + absence-of-02 confirmation all captured in 07-04-SUMMARY.md.</done>
</task>

<task id="07-04-T-gate" type="auto">
  <name>Task 4 (T-gate): Final phase gate — bash phase7-structure-check.sh --all (K1-K7 + K4b + D1-D3 + S1-S2 + X1-X2 all PASS)</name>
  <files></files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh` (shipped in 07-01)
    - All Phase 7 deliverables on disk (07-01 + 07-02 + 07-03 outputs + 07-04 T1 + T2 + T-smoke outputs)
  </read_first>
  <action>
    Run the full Phase 7 structure-check gate. This is the phase-level final gate — all 4 runners (kickoff / discovery / sow / cross) must emit PASS for every assertion.

    **C5 fix — ordering:** This task MUST exit 0 BEFORE the next task (T-flips) updates REQUIREMENTS.md traceability. If `--all` fails, trace flips DO NOT happen. The plan is paused, the failing source file is fixed, `--all` is re-run, and only on exit 0 does the executor proceed to T-flips.

    Command:
    ```
    bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --all
    ```
    (or without the `--all` flag — same behaviour per dispatch case.)

    Expected output: every K1-K7, K4b, D1-D3, S1-S2, X1-X2 assertion emits `PASS:` followed by `ALL ASSERTIONS PASSED` and exit 0. Expected PASS-line count: 15.

    If ANY assertion fails:
    - K-series fail → 07-01 deliverable issue; check kickoff-capture skill body and references.
    - K4b fail → enum values not documented across the 4 kickoff-capture files; fix SKILL.md or kickoff-template.md.
    - D-series fail → 07-02 deliverable issue; check discovery-intake/SKILL.md.
    - S-series fail → 07-03 T1/T2 issue; check generate-sow/SKILL.md or sow-template.md.
    - X-series fail → cross-skill consistency issue; check that `kickoff_branch:` appears in all 3 skill files AND `based_on_kickoff:` appears in both discovery-intake and generate-sow SKILL.md.

    Fix the source file (NEVER the script — the script is the contract). Re-run until exit 0. **Do NOT proceed to T-flips until this task exits 0.**
  </action>
  <verify>
    <automated>bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --all</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0
    - stdout contains `ALL ASSERTIONS PASSED`
    - stdout contains PASS lines covering K1, K2, K3, K4, K4b, K5, K6, K7, D1, D2, D3, S1, S2, X1, X2 (15 assertions total — 8 kickoff incl. K4b + 3 discovery + 2 sow + 2 cross)
  </acceptance_criteria>
  <done>Final phase gate green. C5 precondition for T-flips satisfied. Phase 7 ready for REQUIREMENTS.md trace flip.</done>
</task>

<task id="07-04-T-flips" type="auto">
  <name>Task 5 (T-flips): Flip 10 REQUIREMENTS.md traceability checkboxes (STG1-01..05 + STG2-01..03 + STG3-01..02) — runs AFTER T-gate exits 0 per C5</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 117-145 (Traceability table — current state shows all 10 v2.2 Phase 7 rows as Pending)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md` (Requirement → Assertion Mapping table)
    - The T-gate output above (must show exit 0 and `ALL ASSERTIONS PASSED`) — this task's precondition per C5
  </read_first>
  <action>
    **C5 precondition check first:** verify the prior T-gate task exited 0. Do not run any of the edits below unless `bash phase7-structure-check.sh --all` exited 0 in the immediately prior task. If T-gate failed, this task is BLOCKED — fix the source file flagged by the failing assertion and re-run T-gate before touching REQUIREMENTS.md.

    Update `.planning/REQUIREMENTS.md` Traceability table — flip 10 v2.2 Phase 7 requirement rows from `Pending` to `Satisfied` (or whatever status token the table convention uses — match the prior milestone's token if present).

    Rows to flip:
    1. STG1-01 — Phase 7 — Pending → Satisfied
    2. STG1-02 — Phase 7 — Pending → Satisfied
    3. STG1-03 — Phase 7 — Pending → Satisfied
    4. STG1-04 — Phase 7 — Pending → Satisfied
    5. STG1-05 — Phase 7 — Pending → Satisfied
    6. STG2-01 — Phase 7 — Pending → Satisfied
    7. STG2-02 — Phase 7 — Pending → Satisfied
    8. STG2-03 — Phase 7 — Pending → Satisfied
    9. STG3-01 — Phase 7 — Pending → Satisfied
    10. STG3-02 — Phase 7 — Pending → Satisfied

    Also flip the 10 requirement-line checkboxes earlier in REQUIREMENTS.md (lines 26-46 — the `- [ ] **STG1-01**: ...` entries) from `[ ]` to `[x]`.

    DO NOT flip any Phase 8 rows (STG4, ROUTE) — those are out of scope for Phase 7.

    Update the "Coverage" footer at lines 142-145 if the status token-change reflects in any cumulative counter (recommend leaving the count text alone — Phase 7 didn't change the requirement count, only the satisfaction status).
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*STG" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*STG[123]-" .planning/REQUIREMENTS.md` returns 10 (5 STG1 + 3 STG2 + 2 STG3 boxes checked)
    - `grep -c "^- \[ \] \*\*STG[123]-" .planning/REQUIREMENTS.md` returns 0 (no Phase 7 requirement still unchecked)
    - `grep -c "^- \[ \] \*\*STG4-" .planning/REQUIREMENTS.md` returns 6 (Phase 8 STG4 rows untouched)
    - `grep -c "^- \[ \] \*\*ROUTE-" .planning/REQUIREMENTS.md` returns 5 (Phase 8 ROUTE rows untouched)
    - The Traceability table at the bottom shows all 10 STG1/STG2/STG3 rows in a satisfied status (literal token chosen to match prior-milestone convention — `Satisfied` or `Complete` or `Satisfied ✓`)
  </acceptance_criteria>
  <done>REQUIREMENTS.md trace table reflects Phase 7 satisfaction; Phase 8 rows untouched. C5 ordering preserved (T-gate exit 0 was the precondition).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer → e2e smoke (T-smoke) | Reviewer-driven manual invocation against synthetic fixtures (low trust risk; not external attacker). |
| DESIGN.md / REQUIREMENTS.md write-path | Planning artefacts edited by reviewer/executor; version-controlled via git. |
| T-gate → T-flips ordering | C5 precondition: T-flips MUST NOT run until T-gate exit 0 captured. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | DESIGN.md glossary edit (T1) accidentally introduces secondary drift | mitigate | T1 acceptance criteria explicitly assert `grep -c "kickoff-direct"` returns 0 AND both authoritative tokens are present. C7 fix labels this as glossary correction only — no design contract change. Edit is single-token swap, minimal scope. |
| T-07-14 | Information disclosure | Sample-CR fixtures (T2) accidentally contain real client PII | accept | Fixtures use established clients per D-65 (VodafoneZiggo / Up & Up Group) with synthetic scenarios; reviewer responsibility to keep content illustrative. `fixture_for: phase-7-e2e-smoke` marker distinguishes from production CRs. |
| T-07-15 | Repudiation | E2e smoke (T-smoke) skipped or partially completed; manual gate falsely signed off | mitigate | C4 fix strengthens acceptance: concrete invocation commands per skill+fixture, concrete artefact paths, frontmatter snippet captures recorded in 07-04-SUMMARY.md, absence-of-02-on-draft-sow confirmation. Plan is autonomous=false so the checkpoint cannot be auto-passed by an executor agent. SUMMARY existence + content is acceptance-gated. |
| T-07-16 | Denial of service | Structure-check `--all` runs slow due to many `grep -qF` calls | accept | Total assertion count is 15; estimated runtime <1s per 07-VALIDATION.md. No DoS surface. |
| T-07-17 | Elevation of privilege | REQUIREMENTS.md trace flip applied to Phase 8 rows by accident | mitigate | T-flips acceptance explicitly asserts `grep -c "^- \[ \] \*\*STG4-"` returns 6 AND `grep -c "^- \[ \] \*\*ROUTE-"` returns 5 — Phase 8 rows must remain unchecked. |
| T-07-18 | Tampering / Repudiation | REQUIREMENTS.md flipped to Satisfied while `--all` actually failed (false-positive satisfaction) | mitigate | C5 fix enforces ordering: T-gate (exit 0) is the precondition for T-flips. T-flips action explicitly checks T-gate output before proceeding. If T-gate fails, T-flips is BLOCKED. |

ASVS L1 alignment: doc-only changes + manual gate (T-smoke) + structure-check enforcement across 15 assertions + C5 ordering. Threat surface remains small; primary mitigations are grep-gate verifications of canonical strings + ordered-task precondition enforcement.
</threat_model>

<verification>
- Plan-local (T1): DESIGN.md glossary fix verified via grep — zero `kickoff-direct` / `discovery-via` tokens remaining.
- Plan-local (T2): 3 fixture files exist with `fixture_for: phase-7-e2e-smoke` markers.
- Plan-local (T-smoke): reviewer attestation + 07-04-SUMMARY.md with C4-required captures (4 paths, absence confirmation, 3 frontmatter captures, verbatim skip-message).
- Plan-local (T-gate): `bash phase7-structure-check.sh --all` exits 0 with 15 PASS lines.
- Plan-local (T-flips): runs ONLY after T-gate exits 0 per C5. REQUIREMENTS.md trace flipped for 10 Phase 7 rows; Phase 8 untouched.
- R-02 closed: DESIGN.md glossary aligned to authoritative spelling.
- C2 ordering closed: this plan owns the `--all` gate; depends_on covers both 07-02 and 07-03.
- C4 closed: T-smoke acceptance has concrete commands + paths + frontmatter captures.
- C5 closed: T-gate precedes T-flips with explicit precondition.
- C7 closed: T1 labelled glossary correction only.
</verification>

<success_criteria>
- `.planning/DESIGN.md` glossary entry aligned (T1 — R-02 closed, C7 labelling applied).
- 3 sample-CR fixtures exist at `.planning/phases/07-.../fixtures/` (T2).
- Manual e2e smoke (T-smoke) signed off by reviewer; 07-04-SUMMARY.md contains C4-required captures.
- `bash phase7-structure-check.sh --all` exits 0 with `ALL ASSERTIONS PASSED` (T-gate — 15 PASS lines).
- REQUIREMENTS.md trace table shows all 10 v2.2 Phase 7 requirement rows satisfied (T-flips — runs AFTER T-gate per C5).
- Phase 7 ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-04-SUMMARY.md` recording:
- R-02 glossary fix outcome (DESIGN.md line ~1482 — record the actual line number found at edit time + before/after token diff)
- 3 fixture file paths + final line counts
- E2e smoke outcome (T-smoke — reviewer attestation summary + C4-required captures):
  - 4 artefact paths (kickoff_v1, discovery_v1, sow_v1 on discovery-ready, sow_v2 on draft-sow) + absence-of-discovery_v2 confirmation
  - 3 frontmatter snippet captures (kickoff_branch on both kickoffs; based_on_kickoff on discovery_v1 and sow_v2; based_on_discovery on sow_v1)
  - Verbatim skip-message line observed in Step 4 stdout
  - Confirmation that both SOW artefacts contain `## Platform Scope` AND `## Integration Scope` H2 sections
  - Any deviations recorded
- Final `bash phase7-structure-check.sh --all` exit code (0 expected) + PASS-line count (15 expected) — recorded BEFORE the trace flips per C5
- REQUIREMENTS.md trace flip count (10) + confirmation Phase 8 rows untouched
- Note: synthesis was split from 07-03 to this plan per cross-AI review C2 finding
</output>
</content>
</invoke>