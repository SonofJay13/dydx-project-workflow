---
phase: 01-audit
plan: 07
type: execute
wave: 7
depends_on: ["01-audit/06"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-07]
tags: [audit, cosmetic-issues, design-only, v2.1-deferred]

must_haves:
  truths:
    - "AUDIT-07 catalogues every cosmetic-but-client-visible issue: README truncation, residual 'test sheet' wording, pipeline-step count mismatch in root README, missing LICENSE, owner-email mismatch, [NEW] homepage asymmetry."
    - "Every cosmetic-fix bullet contains the canonical v2.1 sentinel phrase per D-16 (per-bullet, not just at section header) — case-insensitive ERE 'scheduled for v2\\.1 (foundations )?build' matches both lowercase and capitalized variants."
    - "Each cosmetic finding cited in compact `file:line` form per D-14."
    - "Each finding tagged [COSMETIC] per D-15; [NEW] tag on homepage asymmetry per D-10."
    - "Each fix references the v2.1 FOUND-* requirement that closes it (FOUND-04 for manifest/license/email; FOUND-07 for cosmetic CONCERNS)."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-07 section populated with 6 cosmetic-fix subsections; structural-check assertion #3 (the v2.1 phrase, case-insensitive ERE) now passes."
      contains: "## AUDIT-07: Cosmetic-but-Client-Visible Issues; ### 7.1 README truncation; ### 7.2 'test sheet' residual; ### 7.3 Pipeline-step count mismatch; ### 7.4 Missing LICENSE; ### 7.5 Owner-email mismatch; ### 7.6 [NEW] Homepage asymmetry; Scheduled for v2.1 Foundations build; FOUND-04; FOUND-07"
  key_links:
    - from: "AUDIT-07 cosmetic citations"
      to: "v0.3.0 surfaces (READMEs + manifests)"
      via: "compact file:line citations"
      pattern: "`(README|dydx-delivery/README|\\.claude-plugin/marketplace|dydx-delivery/\\.claude-plugin/plugin)\\.(md|json):[0-9]+"
    - from: "AUDIT-07 v2.1 phrase per bullet"
      to: "audit-structure-check.sh assertion #3 (case-insensitive ERE)"
      via: "case-insensitive ERE: 'scheduled for v2\\.1 (foundations )?build'"
      pattern: "scheduled for v2\\.1 (foundations )?build"
---

<objective>
Populate AUDIT-07 with 6 cosmetic-but-client-visible issue subsections — each citing the issue with `file:line`, tagging severity [COSMETIC], and carrying the canonical D-16 sentinel phrase **"Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone."** per-bullet. Every cosmetic fix references the v2.1 FOUND-* requirement that closes it.

**Purpose:** AUDIT-07 satisfies ROADMAP success criterion 5 (cosmetic-but-client-visible issues; per-bullet v2.1 scheduling). RESEARCH.md §8 already enumerates each issue with `file:line` and FOUND-* mapping — this plan transcribes them. Per CONTEXT.md `<specifics>`: "Cosmetic-fix list is the most likely place for misuse — someone in v2.1 might lift a bullet without the constraint. Carry the constraint per-bullet, not just at the section header."

**Output:** AUDIT-07 section of `.planning/AUDIT.md` populated; 6 H3 cosmetic-fix subsections; structural-check assertion #3 (case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'`) now passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- AUDIT-07 input data is FULLY pre-built in RESEARCH.md §8 (lines 416-456). -->
<!-- The 6 cosmetic-fix subsections + their FOUND-* mapping are pre-decided. -->

The 6 cosmetic-fix subsections (per RESEARCH.md §8):

| Subsection anchor | Topic | RESEARCH.md source | Citation | FOUND-* |
|-------------------|-------|---------------------|----------|---------|
| `### 7.1 README truncation (plugin-level)` | Plugin README ends mid-sentence at line 126 | RESEARCH.md §8.1 | `dydx-delivery/README.md:126` | FOUND-07 |
| `### 7.2 Residual "test sheet" wording` | Root README still says "test sheet"; plugin renamed in 0.3.0 | RESEARCH.md §8.2 | `README.md:9`; `dydx-delivery/README.md:126` | FOUND-07 |
| `### 7.3 Pipeline-step count mismatch (root README)` | Root README says 5 steps; manifests describe 7 skills | RESEARCH.md §8.3 | `README.md:9`; `marketplace.json:15`; `plugin.json:4`; `dydx-delivery/README.md:33-41` | FOUND-07 |
| `### 7.4 Missing LICENSE file` | plugin.json declares Proprietary; no LICENSE file | RESEARCH.md §8.4 | `dydx-delivery/.claude-plugin/plugin.json:10`; `.claude-plugin/marketplace.json` (no license field) | FOUND-04 |
| `### 7.5 Owner-email mismatch with stated org` | Both manifests use jasonmichaelb@gmail.com; org named "dYdX Digital" | RESEARCH.md §8.5 | `.claude-plugin/marketplace.json:5`; `dydx-delivery/.claude-plugin/plugin.json:7`; `.claude-plugin/marketplace.json:4`; `dydx-delivery/.claude-plugin/plugin.json:4` | FOUND-04 |
| `### 7.6 [NEW] Homepage asymmetry` | plugin.json has homepage; marketplace.json does not | RESEARCH.md §8.6 (NEW finding) | `dydx-delivery/.claude-plugin/plugin.json:9`; `.planning/codebase/CONCERNS.md:197-199` | FOUND-04 |

Per D-16 (locked): The canonical sentinel phrase **"Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone."** must appear PER-BULLET, not just at the section header. PATTERNS.md §"`v2.1` constraint phrase" demonstrates the canonical wording: `**Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` (bold-wrapped, sentence-final, references the closing FOUND-*).

**Canonical phrase + gate alignment:** Plan 07 writes the **uppercase variant** `**Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` per-bullet. The structural-check script (Plan 01-01, assertion #3) and the acceptance criteria below match this with the **case-insensitive ERE** `'scheduled for v2\.1 (foundations )?build'`. This pattern accepts BOTH the lowercase variant (`scheduled for v2.1 build`) and the uppercase Foundations variant (`Scheduled for v2.1 Foundations build`). Future audit revisions may drop "Foundations" without re-revising the gate.

Per D-10: subsection 7.6 carries the [NEW] tag (homepage asymmetry was flagged in CONCERNS.md but not formally categorized; AUDIT.md elevates it to [NEW] cosmetic finding).

Compact citation form per D-14: `` `dydx-delivery/.claude-plugin/plugin.json:9` ``.

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-07 section opener + 6 cosmetic-fix subsections — each with per-bullet v2.1 sentinel phrase</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §8 (lines 416-456) — 6 cosmetic items pre-cited with FOUND-* mapping
    - .planning/codebase/CONCERNS.md (lines 52-55, 80-92, 191-199, 218-222) — original cosmetic findings
    - .planning/REQUIREMENTS.md AUDIT-07 (line 24); FOUND-04 (manifest sync); FOUND-07 (cosmetic cleanup)
    - .planning/phases/01-audit/01-CONTEXT.md D-10 ([NEW] tag), D-13, D-14, D-15, D-16 (per-bullet v2.1 phrase)
    - .planning/phases/01-audit/01-PATTERNS.md §"`v2.1` constraint phrase" (per D-16) — locked wording variants
    - .planning/phases/01-audit/01-CONTEXT.md `<specifics>` (lines 130-133) — rationale for per-bullet (not just header) phrasing
    - .planning/phases/01-audit/scripts/audit-structure-check.sh assertion #3 — confirms the gate uses case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'`
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-07: Cosmetic-but-Client-Visible Issues` in `.planning/AUDIT.md`.

    **Section opener** (1 paragraph, 2-3 sentences): "Catalogues every cosmetic-but-client-visible issue in v0.3.0 — README truncation, naming residuals, missing LICENSE, owner-email mismatch, manifest asymmetries. **Each fix below carries the per-bullet sentinel 'Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.' so a v2.1 implementer cannot lift one bullet out of context (per D-16).** This audit catalogues the issues; v2.1 Foundations build executes the fixes (FOUND-04, FOUND-07)."

    Then write 6 H3 subsections per `<interfaces>`. Each subsection has this exact shape:

    ```markdown
    ### 7.N <Topic>

    - **Citation:** `<path>:<line>` — <quoted snippet from source>
    - **Context:** <one-sentence explanation of why this matters / what's client-visible>.
    - **Severity:** **[COSMETIC]** — client-visible (<one-line: which surface clients see>).
    - **Fix:** <concrete fix description>. **Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**

    ---
    ```

    For subsection 7.6, prefix the topic with `[NEW]` and add a `**[NEW]**` tag in addition to `**[COSMETIC]**`.

    **Canonical sentinel wording** (per D-16, write VERBATIM — uppercase "Scheduled" and uppercase "Foundations"; the structural-check gate's case-insensitive ERE accepts both this uppercase variant and the lowercase variant `scheduled for v2.1 build`, so this canonical form satisfies the gate while keeping the audit reader-facing prose clean):

    ```
    **Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**
    ```

    Substitute `FOUND-NN` with `FOUND-07` (subsections 7.1, 7.2, 7.3) or `FOUND-04` (subsections 7.4, 7.5, 7.6).

    **Per-subsection content** (transcribe from RESEARCH.md §8):

    **§ 7.1 README truncation (plugin-level)**
    - Citation: `` `dydx-delivery/README.md:126` `` — Changelog entry for `0.3.0` reads: *"The bot-run terminal stage (`execute-tests`) now c"* and the file terminates mid-sentence. No closing punctuation, no further entries.
    - Context: Plugin README is the install-time README; truncation is visible to anyone reading the plugin's marketplace listing.
    - Severity: **[COSMETIC]** — client-visible (plugin README).
    - Fix: Complete the truncated sentence in the changelog entry. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**

    **§ 7.2 Residual "test sheet" wording**
    - Citation: `` `README.md:9` `` — Root README still says "discovery → SOW → functional spec → technical spec → **test sheet** → execution".
    - Context: Plugin README changelog (`dydx-delivery/README.md:126`) records the 0.3.0 rename `generate-test-sheet` → `generate-test-plan`; root README never followed the rename.
    - Severity: **[COSMETIC]** — client-visible (root marketplace README).
    - Fix: Change "test sheet" to "test plan" at `` `README.md:9` ``. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**

    **§ 7.3 Pipeline-step count mismatch (root README)**
    - Citation: `` `README.md:9` `` says 5 pipeline steps; `` `.claude-plugin/marketplace.json:15` `` and `` `dydx-delivery/.claude-plugin/plugin.json:4` `` describe **seven skills**; `` `dydx-delivery/README.md:33-41` `` lists seven.
    - Context: Two-vs-three sources of truth disagree on pipeline length; new readers see contradictory information.
    - Severity: **[COSMETIC]** — client-visible (root README).
    - Fix: Update `` `README.md:9` `` to reflect the 7-skill v0.3.0 pipeline (or v2's 13-skill pipeline post-rebuild). **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**

    **§ 7.4 Missing LICENSE file**
    - Citation: `` `dydx-delivery/.claude-plugin/plugin.json:10` `` declares `"license": "Proprietary"`; no `LICENSE` or `LICENSE.md` file exists at repo root or plugin folder; `.claude-plugin/marketplace.json` has no `license` field.
    - Context: Provenance gap — `Proprietary` declaration is unbacked by an actual licence text; clients viewing the marketplace see a license field that doesn't resolve to a document.
    - Severity: **[COSMETIC]** — client-visible (provenance).
    - Fix: Add `LICENSE` file at repo root (or `dydx-delivery/LICENSE`) with proprietary licence terms; add matching `license` field to `marketplace.json`. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.**

    **§ 7.5 Owner-email mismatch with stated org**
    - Citation: `` `.claude-plugin/marketplace.json:5` `` (`owner.email`) and `` `dydx-delivery/.claude-plugin/plugin.json:7` `` (`author.email`) both list `jasonmichaelb@gmail.com`; README + marketplace metadata describe the team as "dYdX Digital" (e.g. `` `.claude-plugin/marketplace.json:4` ``, `` `dydx-delivery/.claude-plugin/plugin.json:4` ``); no `@dydx.digital` or org domain appears in the manifests.
    - Context: Provenance — clients viewing the marketplace see a personal Gmail; mismatches the stated organisational identity.
    - Severity: **[COSMETIC]** — client-visible (provenance).
    - Fix: Change to an org-domain address. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.** **Cross-ref:** MIN-6 PITFALL.

    **§ 7.6 [NEW] Homepage asymmetry**
    - Citation: `` `dydx-delivery/.claude-plugin/plugin.json:9` `` has `"homepage": "https://github.com/SonofJay13/dydx-project-workflow"`; `.claude-plugin/marketplace.json` has no `homepage` field. (`` `.planning/codebase/CONCERNS.md:197-199` ``).
    - Context: Asymmetric metadata — the marketplace listing omits a link the plugin manifest provides.
    - Severity: **[NEW]** **[COSMETIC]** — client-visible (marketplace listing). [NEW] tag because CONCERNS.md flagged the asymmetry but didn't formally categorize it; AUDIT.md elevates to cosmetic-client-visible.
    - Fix: Add matching `homepage` field to `marketplace.json`. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.**

    Each subsection ends with `---` separator.

    **CRITICAL ASSERTION:** The canonical sentinel substring `Scheduled for v2.1 Foundations build` (uppercase S, "Foundations" present) must appear at least 6 times (one per subsection). The structural-check script at Plan 01-01 uses **case-insensitive ERE** `'scheduled for v2\.1 (foundations )?build'` so the gate accepts both the uppercase Foundations variant (what this plan writes) and a lowercase / no-Foundations variant (acceptable for future revisions). Per-bullet enforcement (D-16) is verified by counting matches inside the AUDIT-07 section, NOT just at the header.

    **Implementing decisions:** D-13 (cosmetic items NOT in this milestone), D-14 (compact citations), D-15 ([COSMETIC] severity), D-16 (per-bullet v2.1 phrase), D-10 ([NEW] tag on 7.6).
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### 7\.[1-6] ' .planning/AUDIT.md)" = "6" ] &amp;&amp; [ "$(awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -ciE 'scheduled for v2\.1 (foundations )?build')" -ge "6" ] &amp;&amp; [ "$(awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -c '\*\*\[COSMETIC\]\*\*')" -ge "6" ] &amp;&amp; awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -q '\*\*\[NEW\]\*\*' &amp;&amp; awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -q 'FOUND-04' &amp;&amp; awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -q 'FOUND-07' &amp;&amp; echo COSMETIC_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 6 H3 subsections under AUDIT-07: `grep -cE '^### 7\.[1-6] ' .planning/AUDIT.md` returns `6`
    - Case-insensitive count of cosmetic-fix bullets containing the v2.1 sentinel — at least 6 matches inside AUDIT-07 (one per subsection per D-16): `awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -ciE 'scheduled for v2\.1 (foundations )?build'` >= `6`. The case-insensitive ERE matches both the canonical uppercase variant Plan 07 writes (`Scheduled for v2.1 Foundations build`) and the lowercase / no-Foundations variant the structural-check script's assertion #3 also accepts; future audit revisions may drop "Foundations" without re-revising this gate.
    - At least 6 [COSMETIC] tags: `awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -c '\*\*\[COSMETIC\]\*\*'` >= `6`
    - [NEW] tag on subsection 7.6: `awk '/^### 7\.6 /,/^---$/' .planning/AUDIT.md | grep -q '\*\*\[NEW\]\*\*'` succeeds
    - FOUND-04 referenced (subsections 7.4, 7.5, 7.6): `awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -c 'FOUND-04'` >= `3`
    - FOUND-07 referenced (subsections 7.1, 7.2, 7.3): `awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -c 'FOUND-07'` >= `3`
    - All key citation paths present: `awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -cE '\`(README\.md:9|dydx-delivery/README\.md:126|dydx-delivery/\.claude-plugin/plugin\.json:(4|7|9|10)|\.claude-plugin/marketplace\.json:[0-9]+)'` >= `6`
    - Structural-check assertion #3 now passes (case-insensitive ERE matches the canonical sentinel anywhere in AUDIT.md): `grep -qiE 'scheduled for v2\.1 (foundations )?build' .planning/AUDIT.md` succeeds
    - The v2.1 sentinel phrase does NOT leak outside AUDIT-07 (per PATTERNS.md "Forbidden anywhere else") — case-insensitive ERE leakage check matches the same pattern as the gate: `awk '/^## AUDIT-01:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -ciE 'scheduled for v2\.1 (foundations )?build'` returns `0`
    - No design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-07:/,/^## AUDIT-08:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`
  </acceptance_criteria>
  <done>
    AUDIT-07 has 6 cosmetic-fix subsections; per-bullet v2.1 sentinel present 6+ times (case-insensitive count); structural-check assertion #3 passes; sentinel does not leak into other AUDIT-* sections.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH.md §8 → AUDIT-07 prose | Executor transcribes 6 pre-built findings; each has FOUND-* mapping locked. |
| D-16 per-bullet phrase | The phrase MUST appear per-bullet, not just at section header — otherwise a v2.1 implementer can lift one bullet out of context and execute the fix in this design milestone (which is forbidden). |
| Canonical sentinel ↔ structural-check gate alignment | Plan 07 writes the uppercase variant `Scheduled for v2.1 Foundations build`; the structural-check script uses case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'` so both variants pass. Diverging the literal phrase from what the script's regex matches would defeat D-16 — both are now aligned and documented in `<interfaces>`. |
| `scheduled for v2.1 build` phrase scope | The sentinel must NOT appear outside AUDIT-07. PATTERNS.md "Forbidden anywhere else" makes this explicit — sentinel leaking into AUDIT-01..06/08 triggers reviewer concern that a cosmetic finding leaked sideways. Leakage check uses the same case-insensitive ERE as the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Tampering | v2.1 sentinel only at section header (not per-bullet) — defeats D-16 | mitigate | Acceptance criterion mandates **case-insensitive ERE count** `grep -ciE 'scheduled for v2\.1 (foundations )?build'` >= `6` (one per subsection). Manual review confirms each subsection has the sentinel in its bullet body. The case-insensitive ERE accepts both `Scheduled for v2.1 Foundations build` (canonical uppercase) and `scheduled for v2.1 build` (lowercase no-Foundations) so the gate is robust to capitalization drift. |
| T-01-07-02 | Repudiation | Wrong FOUND-* mapping (e.g. claiming FOUND-07 closes LICENSE when FOUND-04 does) | mitigate | RESEARCH.md §8 (lines 416-456) is canonical; executor reads it; acceptance criteria check FOUND-04 appears >= 3 times and FOUND-07 >= 3 times (matches §8 split). |
| T-01-07-03 | Information Disclosure | Audit prematurely "executes" a cosmetic fix (modifies README or manifest) | mitigate | Plan's `files_modified` is `.planning/AUDIT.md` only. Phase-level success criterion checks `git status -- README.md dydx-delivery/` is empty. |
| T-01-07-04 | Tampering | v2.1 sentinel leaks into other AUDIT-* sections | mitigate | Acceptance criterion runs `awk '/^## AUDIT-01:/,/^## AUDIT-07:/' ... | grep -ciE 'scheduled for v2\.1 (foundations )?build'` and demands `0`. Same case-insensitive ERE as the gate. |
| T-01-07-05 | Repudiation | Plan 07's literal phrase + Plan 01's grep pattern drift apart (the original BLOCKER) | mitigate | Both plans now reference the same canonical case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'` documented in this plan's `<interfaces>` and Plan 01-01's script header comment. The pattern accepts both capitalization variants so the audit can use uppercase Foundations prose while the gate stays robust. |
</threat_model>

<verification>
End of Plan 07 success-state:
1. AUDIT-07 has 6 H3 subsections with the four required mini-bullets each (Citation / Context / Severity / Fix).
2. The canonical sentinel `Scheduled for v2.1 Foundations build` appears >= 6 times in AUDIT-07 (case-insensitive ERE count, per D-16).
3. The sentinel appears 0 times outside AUDIT-07 (PATTERNS.md "Forbidden anywhere else"; case-insensitive leakage check).
4. Structural-check assertion #3 (case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'`) NOW passes.
5. FOUND-04 referenced >= 3 times; FOUND-07 referenced >= 3 times.
6. [NEW] tag on subsection 7.6.
7. No design-move language (canonical D-13 ERE returns 0); no premature execution.
8. 8-H2-section count holds.
</verification>

<success_criteria>
- [ ] AUDIT-07 has 6 cosmetic-fix subsections, each carrying the per-bullet canonical v2.1 sentinel
- [ ] Structural-check assertion #3 (case-insensitive ERE) now passes; assertion #4 (absorption claim) still fails (Plan 09 closes that)
- [ ] [COSMETIC] tag count >= 6
- [ ] [NEW] tag on subsection 7.6
- [ ] FOUND-04 + FOUND-07 referenced
- [ ] No content under `dydx-delivery/`, `README.md`, or any manifest modified
- [ ] v2.1 sentinel does not leak into other AUDIT-* sections (case-insensitive leakage check)
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-07-SUMMARY.md` documenting:
- Subsection count, per-bullet v2.1-sentinel count (case-insensitive)
- Confirmation that structural-check assertion #3 (case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'`) now passes
- Confirmation that the sentinel is contained to AUDIT-07
- Pointer: "Plan 08 (Wave 8) populates AUDIT-08 next"
</output>
