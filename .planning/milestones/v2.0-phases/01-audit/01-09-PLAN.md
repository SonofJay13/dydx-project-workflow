---
phase: 01-audit
plan: 09
type: execute
wave: 9
depends_on: ["01-audit/08"]
files_modified:
  - .planning/AUDIT.md
  - .planning/phases/01-audit/01-AUDIT-COVERAGE.md
autonomous: true
requirements: [AUDIT-02]
tags: [audit, synthesis, concerns-absorption, appendix-b, executive-summary, design-only]

must_haves:
  truths:
    - "AUDIT-02 section confirms AUDIT.md is a verified superset of CONCERNS.md — explicit auditor statement that no CONCERNS.md entry was dropped silently."
    - "Appendix B (CONCERNS.md → AUDIT.md trace) maps every CONCERNS.md H2 section to its absorbing AUDIT-* destination. Executor counts H2 anchors live in CONCERNS.md and substitutes the actual count into the absorption claim and trace summary."
    - "Appendix A (Glossary) defines AUDIT terminology (BLOCKING / STRUCTURAL / COSMETIC / NEW / DESIGN-* / CRIT-* / MOD-* / MIN-*)."
    - "Executive Summary table shows status / count of findings per AUDIT-* requirement."
    - "How-to-read preamble paragraph orients first-time readers."
    - "Reviewer coverage doc (01-AUDIT-COVERAGE.md) maps each ROADMAP success criterion 1–5 to AUDIT.md sections."
    - "Structural-check script exits 0 (all 8 assertions pass) at end of this plan — including assertion #7 which requires Appendix B to have >= 14 data rows."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "Final AUDIT.md — preamble + executive summary + AUDIT-02 absorption claim + Appendix A glossary + Appendix B trace"
      contains: "How to read this audit; Executive Summary; ## AUDIT-02:; verified superset; All CONCERNS.md sections absorbed; ## Appendix A:; ## Appendix B:"
    - path: ".planning/phases/01-audit/01-AUDIT-COVERAGE.md"
      provides: "Reviewer punch-list mapping ROADMAP success criteria 1–5 to AUDIT.md sections"
      contains: "Success criterion 1; Success criterion 2; Success criterion 3; Success criterion 4; Success criterion 5; Reviewer attests"
  key_links:
    - from: "Appendix B trace"
      to: "Every CONCERNS.md H2 section + AUDIT-* destinations"
      via: "row-per-CONCERNS-section table (column-1 = numeric index)"
      pattern: "Version string mismatches.*AUDIT-06"
    - from: "Coverage doc"
      to: "ROADMAP success criteria 1-5 + AUDIT.md sections"
      via: "checklist trace"
      pattern: "Success criterion [1-5]"
---

<objective>
Final synthesis wave — close out AUDIT.md with the AUDIT-02 absorption claim, Appendix B trace mapping every CONCERNS.md section to its AUDIT-* destination, Appendix A glossary, executive summary table, how-to-read preamble, and a reviewer coverage doc that walks success criteria 1–5. Run the structural-check script at the end and confirm exit 0 (all 8 assertions pass — including the new Appendix B row-count assertion).

**Purpose:** AUDIT-02 satisfies ROADMAP success criterion 2 (verified superset of CONCERNS.md, no entry dropped silently). Appendix B is the formal trace; the executive summary and preamble give readers entry points. Coverage doc gives the human reviewer their punch-list during the approval gate.

**Output:** AUDIT.md fully populated; coverage doc created; structural-check passes (assertion #4 — absorption claim — and assertion #7 — Appendix B >= 14 data rows — finally land here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/phases/01-audit/01-VALIDATION.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- The CONCERNS.md → AUDIT.md absorption mapping is FULLY pre-built in RESEARCH.md §2 (lines 75-97). -->
<!-- N CONCERNS.md sections × AUDIT-* destination is the pre-decided trace; executor counts H2 anchors live to confirm N. -->

CONCERNS.md sections to trace (per RESEARCH.md §2 + CONCERNS.md actual section count):

| # | CONCERNS.md section | Lines | Entries | Primary AUDIT-* destination | Cross-ref | Severity |
|---|---------------------|-------|---------|----------------------------|-----------|----------|
| 1 | Version string mismatches across manifests and docs | `CONCERNS.md:9-24` | 5 entries | AUDIT-06 | AUDIT-07 | [STRUCTURAL] + [COSMETIC] |
| 2 | References to skills/files that do not exist in the repo | `CONCERNS.md:28-48` | 4 categories | AUDIT-04 | AUDIT-01 | [STRUCTURAL] |
| 3 | Truncated / cut-off content | `CONCERNS.md:52-55` | 1 | AUDIT-07 | — | [COSMETIC] |
| 4 | Pipeline stage numbering inconsistencies | `CONCERNS.md:59-76` | Two-scheme conflict | AUDIT-01 brittleness; AUDIT-05 | DESIGN-02 | [STRUCTURAL] |
| 5 | Naming inconsistency: "test plan" vs "test sheet" | `CONCERNS.md:80-84` | 1 | AUDIT-07 | AUDIT-06 | [COSMETIC] |
| 6 | Pipeline-step count mismatch in root README | `CONCERNS.md:88-92` | 1 | AUDIT-07 | AUDIT-06 | [COSMETIC] |
| 7 | Empty / missing scaffold directories | `CONCERNS.md:96-105` | 3 | AUDIT-04 | AUDIT-01 | [STRUCTURAL] |
| 8 | Cross-references between docs | `CONCERNS.md:109-117` | NEGATIVE FINDING | AUDIT-04 §4.6 (verified clean) | — | (negative — clean) |
| 9 | Duplicated content across skills | `CONCERNS.md:121-150` | 4 categories | AUDIT-05 | DESIGN-03; MOD-16 | [STRUCTURAL] |
| 10 | TODO / FIXME / HACK / XXX markers | `CONCERNS.md:154-162` | NEGATIVE FINDING | AUDIT-01 negative-finding subsection or AUDIT-07 | — | (negative — clean) |
| 11 | Frontmatter / template structural inconsistencies | `CONCERNS.md:166-187` | 3 sub-categories | AUDIT-01 brittleness per skill | DESIGN-01; CRIT-6 | [STRUCTURAL] |
| 12 | Identifiers / contact details | `CONCERNS.md:191-199` | 2 | AUDIT-07 (email); AUDIT-04 (homepage one-way) → AUDIT-07 | MIN-6 | [COSMETIC] / [STRUCTURAL] |
| 13 | Build artefact path references in spec text | `CONCERNS.md:203-214` | 3 | AUDIT-01 brittleness for `generate-build-prompt`; AUDIT-04 | DESIGN-12 | [STRUCTURAL] |
| 14 | License field | `CONCERNS.md:218-222` | 1 | AUDIT-07 | — | [COSMETIC] |
| 15 | Versioning convention vs current state | `CONCERNS.md:226-230` | 1 | AUDIT-06 | — | [COSMETIC] |

Total CONCERNS.md sections: **15** (per CONCERNS.md actual H2 count; RESEARCH.md §2 says "14" but that excludes the standalone "Versioning convention vs current state" section at line 226 — Plan 09 must verify by re-counting H2 anchors in CONCERNS.md and absorbing all of them).

**Appendix B row format that aligns with structural-check assertion #7:** Plan 09 writes the trace table with column-1 = numeric index (`| 1 |` ... `| 15 |`). The structural-check script's assertion #7 (Plan 01-01) counts data rows via `awk '/^## Appendix B:/,0' "$AUDIT_FILE" | grep -cE '^\| [^|]+ \|'` — this regex tolerates ANY non-empty first column. The numeric-index format Plan 09 uses (column-1 always digits + space) matches this regex robustly.

Note: The structural-check script's assertion #4 looks for the literal phrase pattern matching `verified superset of` OR `all .* (entries|sections) absorbed`. Plan 09's AUDIT-02 section MUST contain wording like "All N CONCERNS.md sections absorbed" where the executor substitutes the actual H2 count for `N`. The locked artefact text is the literal phrase **"All CONCERNS.md sections absorbed; zero entries dropped silently."** which Task 1 writes verbatim — this is what `must_haves.contains` checks for.

Glossary terms (Appendix A) to define:
- BLOCKING / STRUCTURAL / COSMETIC (severity tags per D-15)
- [NEW] (per D-10)
- DESIGN-* (Phase 2 Design requirements)
- CRIT-/MOD-/MIN- (PITFALLS.md severity prefixes)
- AUDIT-* (this audit's requirements)
- FOUND-* (v2.1 Foundations build requirements)
- canonical (single-source-of-truth surface)
- file:line (compact citation form per D-14)
- SoT (source of truth)

Coverage doc shape (per VALIDATION.md §"Wave 0 Requirements" line 62-63):
- One H2 per ROADMAP success criterion (1–5)
- Each section quotes the criterion + bullet list of `- [ ] AUDIT.md §<section> — <what to look for>`
- Trailing `**Reviewer attests:** ☐ All 5 criteria met` checkbox

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CONCERNS.md section count + write AUDIT-02 absorption claim section</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file — count `^## ` H2 anchors and record actual section count)
    - .planning/phases/01-audit/01-RESEARCH.md §2 (lines 75-97) — pre-built absorption map
    - .planning/REQUIREMENTS.md AUDIT-02 (line 19) — exact requirement
    - .planning/phases/01-audit/01-CONTEXT.md D-08, D-09, D-10
  </read_first>
  <action>
    **Step 1 — Count CONCERNS.md H2 sections.** Run `grep -cE '^## ' .planning/codebase/CONCERNS.md` and capture the exact count. RESEARCH.md says 14; the executor's count is canonical for the absorption claim. If the count is N, the AUDIT-02 absorption statement must include the literal sentinel phrase **`All CONCERNS.md sections absorbed; zero entries dropped silently.`** verbatim AND the running text `All N CONCERNS.md sections absorbed` where `N` = the actual integer count (so reviewers see the number).

    **Step 2 — Replace the placeholder body under `## AUDIT-02: CONCERNS.md absorption (verified superset)` in `.planning/AUDIT.md`.**

    Section opener (1 paragraph, 3-4 sentences): "AUDIT.md is a **verified superset** of `.planning/codebase/CONCERNS.md` (audit dated 2026-05-09). Every CONCERNS.md entry has been rewritten into the appropriate AUDIT-* section per D-08, with the original `file:line` citation preserved. Net-new findings discovered during this audit pass carry the `[NEW]` tag per D-10. **All N CONCERNS.md sections absorbed; zero entries dropped silently.** [where N = actual H2 count from Step 1; for example if N=15, write 'All 15 CONCERNS.md sections absorbed; zero entries dropped silently.']"

    Then on a separate line directly below, also write the canonical sentinel exactly as: **`**All CONCERNS.md sections absorbed; zero entries dropped silently.**`** (bold-wrapped, no count token). This canonical sentinel is what `must_haves.contains` checks for as a literal substring, regardless of whatever count the running prose carries — so the gate is robust to the live re-count.

    Below the opener + sentinel, write a brief absorption summary table showing the high-level mapping (one row per CONCERNS.md section → AUDIT-* destination — this is the SHORT version; Appendix B carries the detailed trace). Use compact form:

    ```
    | CONCERNS.md section | Primary destination |
    |---|---|
    | Version string mismatches | AUDIT-06 |
    | References to skills/files that do not exist | AUDIT-04 |
    | Truncated / cut-off content | AUDIT-07 |
    | Pipeline stage numbering inconsistencies | AUDIT-01; AUDIT-05 |
    | "test plan" vs "test sheet" | AUDIT-07 |
    | Pipeline-step count mismatch | AUDIT-07 |
    | Empty / missing scaffold directories | AUDIT-04 |
    | Cross-references between docs (negative) | AUDIT-04 §4.6 |
    | Duplicated content across skills | AUDIT-05 |
    | TODO / FIXME / HACK / XXX markers (negative) | AUDIT-01 / AUDIT-07 (clean) |
    | Frontmatter / template structural inconsistencies | AUDIT-01 (per skill) |
    | Identifiers / contact details | AUDIT-07 (email); AUDIT-04 / AUDIT-07 (homepage) |
    | Build artefact path references in spec text | AUDIT-01 (build-prompt); AUDIT-04 |
    | License field | AUDIT-07 |
    | Versioning convention vs current state | AUDIT-06 |
    ```

    Then close with: "**See Appendix B for the full row-per-section trace** including line ranges, entry counts, severity tags, and cross-references."

    Then `---` separator.

    **CRITICAL:** The AUDIT-02 body MUST contain BOTH the literal phrase `verified superset of` AND the canonical sentinel `**All CONCERNS.md sections absorbed; zero entries dropped silently.**` AND the running-prose form `All N CONCERNS.md sections absorbed` (with N substituted live). The structural-check script's assertion #4 uses ERE `verified superset of|all .* (entries|sections) absorbed` (case-insensitive) — both phrasings satisfy it.

    **Implementing decisions:** D-08 (CONCERNS absorption), D-09 (Appendix B reference), D-10 ([NEW] policy mention).
  </action>
  <verify>
    <automated>awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -q 'verified superset of' &amp;&amp; awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -q 'All CONCERNS.md sections absorbed; zero entries dropped silently' &amp;&amp; awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -qiE 'all [0-9]+ .* (entries|sections) absorbed' &amp;&amp; [ "$(awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -cE '^\| ')" -ge "10" ] &amp;&amp; awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -q 'Appendix B' &amp;&amp; echo AUDIT_02_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-02 contains literal phrase `verified superset of`: `awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -q 'verified superset of'` succeeds
    - AUDIT-02 contains the canonical sentinel substring (literal, no count token): `awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -q 'All CONCERNS.md sections absorbed; zero entries dropped silently'` succeeds — this is what `must_haves.contains` checks for as a literal substring.
    - AUDIT-02 also contains running-prose form with live count: `awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -qiE 'all [0-9]+ .* (entries|sections) absorbed'` succeeds (executor substitutes actual integer).
    - Absorption summary table has at least 10 rows: `awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -cE '^\| '` >= `10`
    - AUDIT-02 references Appendix B: `awk '/^## AUDIT-02:/,/^## AUDIT-03:/' .planning/AUDIT.md | grep -q 'Appendix B'` succeeds
    - Structural-check assertion #4 (CONCERNS absorption claim) now passes: `grep -qiE 'verified superset of|all .* (entries|sections) absorbed' .planning/AUDIT.md` succeeds
  </acceptance_criteria>
  <done>
    AUDIT-02 absorption claim landed; canonical sentinel literal present; structural-check assertion #4 now passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Appendix B (full CONCERNS.md → AUDIT.md trace) + Appendix A (Glossary)</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file)
    - .planning/phases/01-audit/01-RESEARCH.md §2 (lines 75-97) — full absorption map
    - .planning/phases/01-audit/01-PATTERNS.md §"Trailing-section pattern: appendices and trace tables" (lines 192-208)
    - .planning/phases/01-audit/01-CONTEXT.md D-09 (Appendix B trace)
    - .planning/phases/01-audit/scripts/audit-structure-check.sh assertion #7 — confirms the row-count gate uses regex `^\| [^|]+ \|` against rows in the Appendix B section
  </read_first>
  <action>
    **Step 1 — Replace the placeholder body under `## Appendix A: Glossary` in `.planning/AUDIT.md`.**

    Open with one short paragraph: "Quick-reference for tags, IDs, and conventions used in this audit. Reviewer can scan this if a tag or ID is unfamiliar."

    Then a 2-column table:

    ```
    | Term | Meaning |
    |---|---|
    | **[BLOCKING]** | Severity tag — contract is broken; downstream skills cannot load (D-15) |
    | **[STRUCTURAL]** | Severity tag — architectural/schema/scaffold/consistency debt (D-15) |
    | **[COSMETIC]** | Severity tag — client-visible polish issue, scheduled for v2.1 build (D-15, D-16) |
    | **[NEW]** | Net-new finding beyond CONCERNS.md (D-10); precedes severity tag |
    | DESIGN-NN | Phase 2 Design requirement (`.planning/REQUIREMENTS.md` §"Design") |
    | AUDIT-NN | This audit's requirement (`.planning/REQUIREMENTS.md` §"Audit") |
    | CHANGE-NN | Phase 3 Change list requirement |
    | OPEN-NN | Phase 4 Open Questions requirement |
    | CRIT-/MOD-/MIN- | PITFALLS.md severity prefixes (`.planning/research/PITFALLS.md`) |
    | FOUND-NN | v2.1 Foundations build requirement (deferred — milestone-future) |
    | canonical | The single-source-of-truth surface for a piece of content |
    | `file:line` | Compact citation form per D-14: backtick-wrapped path + line/range with ASCII hyphen |
    | SoT | Source of truth |
    | MCP | Model Context Protocol — Claude Code's connector mechanism |
    | v0.3.0 | Current shipped plugin version (per `dydx-delivery/.claude-plugin/plugin.json:3`) |
    | v2.0 | Design-only milestone (this audit + design + change list + open questions) |
    | v2.1 | First v2 build milestone (Foundations) — deferred per `.planning/REQUIREMENTS.md` |
    ```

    Then `---` separator.

    **Step 2 — Replace the placeholder body under `## Appendix B: CONCERNS.md → AUDIT.md trace` in `.planning/AUDIT.md`.**

    Per D-09 + PATTERNS.md "Trailing-section pattern", this is the formal trace table.

    Open with one paragraph: "Row-per-CONCERNS.md-section trace. Confirms every CONCERNS.md section absorbed into a primary AUDIT-* destination per D-08. Net-new findings tagged [NEW] in their respective AUDIT-* sections per D-10."

    Then the trace table — exact column shape per RESEARCH.md §2 (column 1 = numeric index, which aligns with the structural-check script's row-counting regex `^\| [^|]+ \|` from assertion #7):

    ```
    | # | CONCERNS.md section | Lines | Entries | Primary AUDIT-* destination | Cross-ref | Severity |
    |---|---|---|---|---|---|---|
    ```

    Then 14-15 data rows — one per CONCERNS.md H2 section. Use the table in `<interfaces>` as the source. Each row's "Primary AUDIT-* destination" lists the section where the entry was absorbed; "Cross-ref" lists secondary sections; "Severity" uses bracketed tags. **Each row's first cell MUST be a numeric index (`| 1 |`, `| 2 |`, ..., `| N |`) — this is what assertion #7's row-count regex matches.**

    After the table, add a closing summary line per D-09: "**Total CONCERNS.md sections:** N. **All N sections absorbed.** Net-new findings beyond CONCERNS.md scope are tagged `[NEW]` in-line within their respective AUDIT-* sections (subsection 4.6 verified-clean cross-refs; subsection 5.5 [NEW] Stage-N label collision; subsection 7.6 [NEW] Homepage asymmetry; AUDIT-08 Slack [NEW] row)."

    Use the actual section count from Task 1. If executor counted 15 H2 sections in CONCERNS.md, write "All 15 sections absorbed". The data table must have exactly N data rows where N = the live count. **Minimum row count for the structural-check gate to pass is 14 (assertion #7 in Plan 01-01).** If the live count is less than 14, STOP and reconcile with CONCERNS.md — the absorption claim is broken.

    Then `---` separator.

    **Implementing decisions:** D-08 (absorption), D-09 (Appendix B trace), D-10 ([NEW] policy summary).
  </action>
  <verify>
    <automated>awk '/^## Appendix A:/,/^## Appendix B:/' .planning/AUDIT.md | grep -q 'BLOCKING' &amp;&amp; awk '/^## Appendix A:/,/^## Appendix B:/' .planning/AUDIT.md | grep -q 'STRUCTURAL' &amp;&amp; awk '/^## Appendix A:/,/^## Appendix B:/' .planning/AUDIT.md | grep -q 'COSMETIC' &amp;&amp; awk '/^## Appendix B:/,/^\*Audit produced/' .planning/AUDIT.md | grep -F '| # | CONCERNS.md section | Lines | Entries | Primary AUDIT-* destination | Cross-ref | Severity |' &amp;&amp; [ "$(awk '/^## Appendix B:/,/^\*Audit produced/' .planning/AUDIT.md | grep -cE '^\| [0-9]+ \|')" -ge "14" ] &amp;&amp; echo APPENDICES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Appendix A glossary present with all severity-tag definitions: `awk '/^## Appendix A:/,/^## Appendix B:/' .planning/AUDIT.md | grep -cE 'BLOCKING|STRUCTURAL|COSMETIC|\[NEW\]|DESIGN-|CRIT-|file:line'` >= `7`
    - Appendix B trace table header present: `awk '/^## Appendix B:/,/^\*Audit produced/' .planning/AUDIT.md | grep -F '| # | CONCERNS.md section | Lines | Entries | Primary AUDIT-* destination | Cross-ref | Severity |'` succeeds
    - Appendix B has at least 14 numeric-index data rows (one per CONCERNS.md section): `awk '/^## Appendix B:/,/^\*Audit produced/' .planning/AUDIT.md | grep -cE '^\| [0-9]+ \|'` >= `14`
    - Structural-check assertion #7 passes against the same row format (regex `^\| [^|]+ \|`): `awk '/^## Appendix B:/,0' .planning/AUDIT.md | grep -cE '^\| [^|]+ \|'` >= `14`
    - Closing summary line includes "All N sections absorbed": `awk '/^## Appendix B:/,/^\*Audit produced/' .planning/AUDIT.md | grep -qiE 'All [0-9]+ sections absorbed'` succeeds
    - All 8 AUDIT-* destinations referenced in Appendix B (every section of the audit gets at least one CONCERNS.md row pointing at it OR is acknowledged as NEW-finding-only): `awk '/^## Appendix B:/,/^\*Audit produced/' .planning/AUDIT.md | grep -cE 'AUDIT-0[1-8]'` >= `10`
    - Net-new findings list at end of Appendix B mentions subsections 4.6, 5.5, 7.6, plus AUDIT-08 Slack row.
  </acceptance_criteria>
  <done>
    Appendix A + Appendix B populated; full trace table present (>= 14 numeric-indexed rows); closing summary line confirms zero silent drops; assertion #7 row-count gate now satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write executive summary table + how-to-read preamble</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/AUDIT.md (full current state — review what each AUDIT-* section landed)
    - .planning/phases/01-audit/01-CONTEXT.md D-03 (preamble + executive summary opener)
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 1 — Frontmatter / preamble convention"
  </read_first>
  <action>
    **Step 1 — Replace the placeholder body under `## How to read this audit` in `.planning/AUDIT.md`.**

    Per PATTERNS.md Pattern 1 + D-03, write a single-paragraph blockquote-style preamble (3-5 sentences). Use the CONCERNS.md analog voice ("X is the case", not "X should change"):

    ```markdown
    ## How to read this audit

    > Citation-grounded inventory of the v0.3.0 dydx-delivery plugin. **What this is:** observation-led catalogue of skills, connectors, missing artefacts, duplicated content, version mismatches, cosmetic issues, and live MCP wiring as of 2026-05-09. **What this is not:** a v2 design document, a fix list, or a v2.1 build plan — every "what's missing for v2" callout points at a DESIGN-* requirement in `.planning/REQUIREMENTS.md` without proposing the design move (per D-13). Cosmetic issues in AUDIT-07 are explicitly **scheduled for v2.1 Foundations build, NOT this milestone** (per D-16). Severity is tagged inline with `[BLOCKING] / [STRUCTURAL] / [COSMETIC]`; net-new findings beyond CONCERNS.md carry `[NEW]`. **Reading order:** scan the Executive Summary below for status-per-requirement, then jump to any AUDIT-0N section. Appendix B traces every CONCERNS.md entry to its AUDIT-* destination so reviewers can verify nothing was dropped silently.
    ```

    Then `---` separator.

    **Step 2 — Replace the placeholder body under `## Executive Summary` with a one-table summary** (per D-03 — one row per AUDIT-* req with status / count of findings / link to section).

    ```markdown
    ## Executive Summary

    Snapshot of the audit at completion. One row per AUDIT-* requirement with finding count and severity breakdown.

    | Requirement | Section | Findings | Severity breakdown | Status |
    |---|---|---|---|---|
    | AUDIT-01 | [Per-Skill Inventory](#audit-01-per-skill-inventory) | 7 skills × 3-7 brittleness items | Mostly [STRUCTURAL]; 3 [BLOCKING] (platform skill refs in 3 SKILL.md files) | ✓ |
    | AUDIT-02 | [CONCERNS.md absorption](#audit-02-concernsmd-absorption-verified-superset) | All N CONCERNS.md sections absorbed | (verified superset claim) | ✓ |
    | AUDIT-03 | [Per-Stage Connector Dependencies](#audit-03-per-stage-connector-dependencies) | 7 stages × 9 connectors; mostly artefact-driven | 1 REQUIRED (execute-tests platform APIs); 1 graceful (discovery-intake Miro) | ✓ |
    | AUDIT-04 | [Referenced-but-Missing Artefacts](#audit-04-referenced-but-missing-artefacts) | 5 missing categories + 1 verified-clean | 1 [BLOCKING] (platform skills); 4 [STRUCTURAL] | ✓ |
    | AUDIT-05 | [Duplicated Content Blocks](#audit-05-duplicated-content-blocks) | 4 confirmed dups + 1 [NEW] label collision | All [STRUCTURAL] | ✓ |
    | AUDIT-06 | [Version-String Mismatches](#audit-06-version-string-mismatches) | 8 version-bearing locations; 2.0.0 synced target | [STRUCTURAL] | ✓ |
    | AUDIT-07 | [Cosmetic-but-Client-Visible Issues](#audit-07-cosmetic-but-client-visible-issues) | 5 confirmed + 1 [NEW] homepage asymmetry | All [COSMETIC]; scheduled for v2.1 Foundations build | ✓ |
    | AUDIT-08 | [Live MCP Wiring Probe](#audit-08-live-mcp-wiring-probe) | 5 wired (working) + 1 [NEW] Slack (unauth) + 4 deferred | (probe is observational) | ✓ |

    *N = actual CONCERNS.md section count from Appendix B trace.*
    ```

    Replace `N` in the AUDIT-02 row with the actual count from Task 1. Markdown anchor links (e.g. `#audit-01-per-skill-inventory`) follow standard markdown anchor rules (lowercase, hyphen-separated, no special chars).

    Then `---` separator.

    **Implementing decisions:** D-03 (preamble + executive summary), D-13 (observation-led framing).
  </action>
  <verify>
    <automated>awk '/^## How to read this audit$/,/^---$/' .planning/AUDIT.md | grep -q 'observation-led catalogue' &amp;&amp; awk '/^## Executive Summary$/,/^---$/' .planning/AUDIT.md | grep -F '| Requirement | Section | Findings | Severity breakdown | Status |' &amp;&amp; [ "$(awk '/^## Executive Summary$/,/^---$/' .planning/AUDIT.md | grep -cE '^\| AUDIT-0[1-8] \|')" = "8" ] &amp;&amp; echo SUMMARY_OK</automated>
  </verify>
  <acceptance_criteria>
    - Preamble paragraph present under "How to read this audit": `awk '/^## How to read this audit$/,/^---$/' .planning/AUDIT.md | grep -q 'observation-led'` succeeds
    - Executive Summary table present with the 5-column header: `awk '/^## Executive Summary$/,/^---$/' .planning/AUDIT.md | grep -F '| Requirement | Section | Findings | Severity breakdown | Status |'` succeeds
    - All 8 AUDIT-* rows in executive summary: `awk '/^## Executive Summary$/,/^---$/' .planning/AUDIT.md | grep -cE '^\| AUDIT-0[1-8] \|'` returns `8`
    - "scheduled for v2.1" referenced in AUDIT-07 row (executive-summary recap of D-16, case-insensitive): `awk '/^## Executive Summary$/,/^---$/' .planning/AUDIT.md | grep -qiE 'scheduled for v2\.1'` succeeds
  </acceptance_criteria>
  <done>
    Executive summary + how-to-read preamble landed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create reviewer coverage doc 01-AUDIT-COVERAGE.md</name>
  <files>.planning/phases/01-audit/01-AUDIT-COVERAGE.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 1: Audit" §"Success Criteria" (5 criteria)
    - .planning/phases/01-audit/01-VALIDATION.md §"Wave 0 Requirements" (line 62-63 — coverage doc spec)
    - .planning/phases/01-audit/01-PATTERNS.md §"`.planning/phases/01-audit/01-AUDIT-COVERAGE.md` (optional)" — pattern guidance
  </read_first>
  <action>
    Create `.planning/phases/01-audit/01-AUDIT-COVERAGE.md` — single-page reviewer punch-list mapping ROADMAP success criteria 1–5 to AUDIT.md sections.

    File structure:

    ```markdown
    # Phase 1 Audit — Reviewer Coverage Checklist

    **Created:** 2026-05-09
    **Companion to:** `.planning/AUDIT.md`
    **Purpose:** Reviewer punch-list for the Phase 1 approval gate. Each ROADMAP success criterion (1–5) is mapped to the AUDIT.md section(s) that satisfy it. Reviewer ticks each box after spot-checking the cited section.

    ---

    ## Success criterion 1 — Per-skill inventory complete

    > "AUDIT.md exists at .planning/AUDIT.md and catalogues every v0.3.0 skill (`discovery-intake`, `generate-sow`, `generate-functional-spec`, `generate-technical-spec`, `generate-test-plan`, `generate-build-prompt`, `execute-tests`) with purpose, inputs, outputs, hand-off contract, dependencies, observed brittleness, and what's missing for v2 — readable as a self-contained reference without consulting the v0.3.0 source files."

    - [ ] AUDIT.md §AUDIT-01 — open with the 7-row skill matrix (one row per skill)
    - [ ] AUDIT.md §AUDIT-01 — 7 H3 subsections (`### 1.1` through `### 1.7`) with Hand-off contract / Observed brittleness / What's missing for v2 mini-headers
    - [ ] AUDIT.md §AUDIT-01 — every brittleness bullet cited in `file:line` form
    - [ ] AUDIT.md §AUDIT-01 — every "What's missing for v2" points at a DESIGN-* requirement (no design move proposed)

    ---

    ## Success criterion 2 — Verified superset of CONCERNS.md

    > "AUDIT.md is a verified superset of `.planning/codebase/CONCERNS.md` (every existing entry absorbed) and adds any new structural issues found during the audit pass; auditor confirms in the document that no CONCERNS.md entry was dropped silently."

    - [ ] AUDIT.md §AUDIT-02 — explicit "verified superset of" / canonical sentinel `**All CONCERNS.md sections absorbed; zero entries dropped silently.**` / running-prose form `all N sections absorbed`
    - [ ] AUDIT.md §Appendix B — row-per-CONCERNS.md-section trace table; one numeric-indexed row per actual `^## ` heading in CONCERNS.md (>= 14 rows for assertion #7 to pass)
    - [ ] AUDIT.md §Appendix B closing summary — "All N sections absorbed" + named [NEW] findings (4.6 / 5.5 / 7.6 / Slack in 8)
    - [ ] **Reviewer spot-check:** Pick 3 CONCERNS.md entries; confirm each is rewritten in its AUDIT-* destination with original `file:line` preserved

    ---

    ## Success criterion 3 — Per-stage connector deps + live MCP probe

    > "AUDIT.md contains a per-stage connector dependency table...marking each connector required vs graceful-degradation per stage, AND a live-wiring probe table recording present-and-working / present-but-broken / missing for each MCP in this workspace with version pins where present."

    - [ ] AUDIT.md §AUDIT-03 — 7-stage × 9-connector dependency matrix with `REQUIRED` / `optional (graceful)` / `(none)` / `(referenced in artefact only)` cells
    - [ ] AUDIT.md §AUDIT-03 — research-derived fallback hint sub-table with explicit DESIGN-07 pointer
    - [ ] AUDIT.md §AUDIT-08 — 5-row live MCP probe table with status / server identity / endpoint / probe call / result / version pin
    - [ ] AUDIT.md §AUDIT-08 — Slack [NEW] row + 4-row verification-deferred sub-table
    - [ ] AUDIT.md §AUDIT-08 — probe-time timestamp recorded
    - [ ] **Reviewer spot-check:** Pick 3 probe rows; re-run the documented probe call against the live MCP; confirm status matches

    ---

    ## Success criterion 4 — Missing artefacts + duplicated content catalogued

    > "AUDIT.md catalogues every referenced-but-missing artefact (`platform-pipefy`/`platform-wrike` skills, `/refine-<skill>` slash commands, workspace `hub.md`, client-folder `.env.example`) with exact citations from v0.3.0 docs, AND every duplicated content block...with a canonical-source recommendation per duplicate."

    - [ ] AUDIT.md §AUDIT-04 — 5 missing-artefact subsections (4.1 platform skills; 4.2 /refine-<skill>; 4.3 hub.md; 4.4 .env.example; 4.5 commands/agents/hooks scaffold) + 4.6 verified-clean
    - [ ] AUDIT.md §AUDIT-04 §4.1 — [BLOCKING] tag (3 downstream skills depend on platform-pipefy/wrike)
    - [ ] AUDIT.md §AUDIT-05 — 4 duplicate-content categories (5.1 hard-rules; 5.2 start-at-any-point; 5.3 Cowork-vs-Claude-Code; 5.4 pipeline diagram) + 5.5 [NEW] Stage-6 label collision
    - [ ] AUDIT.md §AUDIT-05 — every duplicate has a `**Canonical source:**` line
    - [ ] AUDIT.md §AUDIT-05 §5.2 — all 6 start-at-any-point copies cited
    - [ ] AUDIT.md §AUDIT-05 §5.1 — `safety-rules.md` named as canonical for hard-rules

    ---

    ## Success criterion 5 — Version mismatches + cosmetic issues

    > "AUDIT.md lists every version-string mismatch across `plugin.json`, `marketplace.json`, root README, plugin README and hardcoded version strings (recommending `2.0.0` as the synced target) AND flags every cosmetic-but-client-visible issue...with each cosmetic fix scheduled for v2.1 build, NOT this design milestone."

    - [ ] AUDIT.md §AUDIT-06 — 8-row version-string-mismatch table
    - [ ] AUDIT.md §AUDIT-06 — `2.0.0` recommended as synced target (per D-17)
    - [ ] AUDIT.md §AUDIT-06 — FOUND-04 referenced as v2.1 build target
    - [ ] AUDIT.md §AUDIT-07 — 6 cosmetic-fix subsections (7.1 README truncation; 7.2 "test sheet" residual; 7.3 pipeline-step count mismatch; 7.4 LICENSE; 7.5 owner-email; 7.6 [NEW] homepage asymmetry)
    - [ ] AUDIT.md §AUDIT-07 — **every** cosmetic-fix bullet contains the canonical sentinel "Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone." per D-16 (case-insensitive ERE `'scheduled for v2\.1 (foundations )?build'` matches >= 6 times in AUDIT-07)
    - [ ] AUDIT.md §AUDIT-07 — the v2.1 sentinel does NOT appear outside AUDIT-07 (case-insensitive leakage check returns 0)

    ---

    ## Reviewer attestation

    - [ ] All 5 ROADMAP success criteria met
    - [ ] Manual spot-check of citations resolves (5 random `file:line` entries)
    - [ ] Manual spot-check of MCP probe results matches a re-probe (3 random rows)
    - [ ] No CONCERNS.md entry dropped silently (Appendix B trace verified exhaustive)
    - [ ] Approve Phase 1 → Phase 2 (Design) may begin

    **Reviewer attests:** ☐ All criteria met
    **Reviewer name:** _____________
    **Date:** _____________
    ```

    **Implementing decisions:** VALIDATION.md §"Wave 0 Requirements" (coverage doc); ROADMAP success criteria 1-5.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-audit/01-AUDIT-COVERAGE.md &amp;&amp; [ "$(grep -cE '^## Success criterion [1-5]' .planning/phases/01-audit/01-AUDIT-COVERAGE.md)" = "5" ] &amp;&amp; grep -q 'Reviewer attests' .planning/phases/01-audit/01-AUDIT-COVERAGE.md &amp;&amp; grep -qE '\[ \] AUDIT.md' .planning/phases/01-audit/01-AUDIT-COVERAGE.md &amp;&amp; echo COVERAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/01-audit/01-AUDIT-COVERAGE.md`
    - 5 H2 sections (one per ROADMAP success criterion): `grep -cE '^## Success criterion [1-5]' .planning/phases/01-audit/01-AUDIT-COVERAGE.md` returns `5`
    - Reviewer attestation block present: `grep -q 'Reviewer attests' .planning/phases/01-audit/01-AUDIT-COVERAGE.md` succeeds
    - Each section contains `[ ] AUDIT.md` checklist items: `grep -cE '\[ \] AUDIT.md' .planning/phases/01-audit/01-AUDIT-COVERAGE.md` >= `15`
    - At least 3 reviewer spot-check items: `grep -c 'Reviewer spot-check' .planning/phases/01-audit/01-AUDIT-COVERAGE.md` >= `2`
  </acceptance_criteria>
  <done>
    Coverage doc created.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run structural-check script and confirm exit 0 (all 8 assertions pass)</name>
  <files>(no files modified — verification-only)</files>
  <read_first>
    - .planning/AUDIT.md (final state after Tasks 1–3)
    - .planning/phases/01-audit/scripts/audit-structure-check.sh
  </read_first>
  <action>
    Run the structural-check script and confirm exit 0:

    ```
    bash .planning/phases/01-audit/scripts/audit-structure-check.sh; echo "EXIT_CODE=$?"
    ```

    Expected: exit code = `0`. The script's stdout reads `OK: all structural checks passed`.

    If the script exits non-zero, READ THE STDERR MESSAGE — it will name the failing assertion. Common failure modes after Plan 09:
    - Assertion #1 (file exists): impossible at this stage — file exists
    - Assertion #2 (8 sections): if a section header was accidentally renamed/removed during Plan 09's edits
    - Assertion #3 (case-insensitive ERE `scheduled for v2\.1 (foundations )?build`): if Plan 07's v2.1 sentinel was overwritten or the canonical wording drifted
    - Assertion #4 (CONCERNS absorption): if Plan 09 Task 1's "verified superset" / "absorbed" wording is missing
    - Assertion #5 (`## Executive Summary` header): if Plan 09 Task 3's heading drift broke the anchor
    - Assertion #6 (`## Appendix B:` header): if Plan 09 Task 2's heading drift broke the anchor
    - Assertion #7 (Appendix B >= 14 numeric-indexed data rows): if Plan 09 Task 2 wrote fewer than 14 rows or used a row format that doesn't match the regex `^\| [^|]+ \|`
    - Assertion #8 (`2.0.0`): if Plan 06's recommendation was overwritten

    Fix any failure by editing AUDIT.md to restore the missing phrase / structure / row count. Re-run until exit 0.

    **DO NOT** skip this task. Plan 09's gate is the structural-check passing — if the script fails, AUDIT.md is incomplete and Phase 1 cannot approve.
  </action>
  <verify>
    <automated>bash .planning/phases/01-audit/scripts/audit-structure-check.sh; CODE=$?; echo "EXIT_CODE=$CODE"; [ "$CODE" = "0" ] &amp;&amp; echo PHASE_1_GATE_PASSES</automated>
  </verify>
  <acceptance_criteria>
    - `bash .planning/phases/01-audit/scripts/audit-structure-check.sh` exits with code `0`.
    - stdout contains `OK: all structural checks passed`.
    - All 8 assertions in the script pass.
  </acceptance_criteria>
  <done>
    Phase 1 structural gate passes; AUDIT.md is structurally complete; ready for human approval gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 09 → Plans 02-08 outputs | Plan 09 is the **last** plan; if any earlier plan's output got corrupted (section overwritten, citation drift, severity tag dropped), Plan 09's structural-check is the gate that catches it. |
| CONCERNS.md actual section count → AUDIT-02 absorption claim | Hardcoded "14" or "15" risks drift if CONCERNS.md changes; Task 1 mandates a live re-grep for the count, AND writes the canonical no-count sentinel `**All CONCERNS.md sections absorbed; zero entries dropped silently.**` so `must_haves.contains` is robust. |
| Coverage doc claims → AUDIT.md actual content | If coverage doc says "AUDIT.md §AUDIT-01 has 7 H3 subsections" but the AUDIT.md only has 5, reviewer punch-list lies. |
| Appendix B row format ↔ structural-check assertion #7 regex | Plan 01-01's script counts data rows via `^\| [^|]+ \|`. Plan 09 writes rows whose first cell is a numeric index — both this format and the gate's tolerant regex agree, and the check passes for any row count >= 14. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Tampering | Silent CONCERNS.md drop — a CONCERNS.md section is missing from Appendix B trace | mitigate | Task 1 mandates re-counting H2 anchors in CONCERNS.md; Task 2 acceptance criterion checks Appendix B has >= 14 numbered rows AND the structural-check assertion #7 enforces the same minimum independently. Reviewer spot-check (Manual-Only Verification) re-confirms by random sampling. |
| T-01-09-02 | Repudiation | "Verified superset" claim made without actual verification | mitigate | Task 1 ties the claim to a literal section count AND writes the canonical no-count sentinel substring. Task 2's Appendix B is the trace; Coverage doc Step 2 reviewer spot-check is the manual safety net. |
| T-01-09-03 | Tampering | Structural-check script accidentally bypassed (e.g. assertion regex weakened) | mitigate | Plan 01's script is read-only at this stage; Plan 09 doesn't modify the script, only AUDIT.md. Task 5 runs the canonical script verbatim. |
| T-01-09-04 | Information Disclosure | Coverage doc claims success-criteria met that aren't | mitigate | Coverage doc structure makes it a checklist for the human reviewer (not an auto-pass) — checkboxes are unchecked when the file is created; reviewer ticks them. |
| T-01-09-05 | Denial of Service | Plan 09 fails halfway through, leaving AUDIT.md inconsistent | mitigate | Tasks are ordered so each successive task only ADDS content; if Task 5 fails, the developer can re-run earlier tasks idempotently. |
| T-01-09-06 | Tampering | `must_haves.contains` literal substring drifts from what the executor writes | mitigate | The canonical sentinel `All CONCERNS.md sections absorbed; zero entries dropped silently` is locked: Task 1's `<action>` mandates writing that exact substring on its own line, AND `must_haves.contains` references that exact substring. The running-prose form with the live count `N` is in addition to the canonical sentinel, not in place of it. |
</threat_model>

<verification>
End of Plan 09 + end of Phase 1 success-state:
1. `bash .planning/phases/01-audit/scripts/audit-structure-check.sh` exits `0`. stdout: `OK: all structural checks passed`.
2. AUDIT-02 contains "verified superset of" + canonical sentinel `All CONCERNS.md sections absorbed; zero entries dropped silently` + running-prose `All N sections absorbed`.
3. Appendix B trace has >= 14 numeric-indexed data rows.
4. Appendix A glossary defines BLOCKING/STRUCTURAL/COSMETIC/[NEW]/DESIGN-/CRIT- etc.
5. Executive Summary has 8 AUDIT-* rows.
6. How-to-read preamble paragraph present.
7. `.planning/phases/01-audit/01-AUDIT-COVERAGE.md` exists with 5 success-criterion sections.
8. `git status -- dydx-delivery/` shows zero changes (no skill files modified).
9. `git status -- README.md .claude-plugin/` shows zero changes (no manifest files modified).
</verification>

<success_criteria>
- [ ] AUDIT-02 absorption claim landed (canonical sentinel + running-prose form); structural-check assertion #4 passes
- [ ] Appendix A glossary populated
- [ ] Appendix B trace has every CONCERNS.md section row (>= 14) with numeric-index column-1; assertion #7 passes
- [ ] Executive Summary table populated (8 AUDIT-* rows)
- [ ] How-to-read preamble paragraph present
- [ ] Coverage doc exists at `.planning/phases/01-audit/01-AUDIT-COVERAGE.md` with 5 success-criterion sections
- [ ] `bash .planning/phases/01-audit/scripts/audit-structure-check.sh` exits `0` (all 8 assertions pass)
- [ ] No content under `dydx-delivery/`, `README.md`, or `.claude-plugin/` modified across the entire phase
- [ ] Phase 1 deliverable ready for human approval gate
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-09-SUMMARY.md` documenting:
- Final AUDIT.md state (sections present, total length, citation count)
- CONCERNS.md absorption confirmation (N sections traced; canonical sentinel + running-prose form both present)
- Coverage doc location
- Structural-check script result: exit `0` (all 8 assertions pass)
- Phase 1 readiness for approval: "AUDIT.md ready for human review per ROADMAP §Phase 1 approval gate"
- Pointer: "Approval gate: human reviews `.planning/AUDIT.md` + `.planning/phases/01-audit/01-AUDIT-COVERAGE.md`; explicit go-ahead unblocks Phase 2 (Design)"
</output>
