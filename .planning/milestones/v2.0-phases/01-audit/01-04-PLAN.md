---
phase: 01-audit
plan: 04
type: execute
wave: 4
depends_on: ["01-audit/03"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-04]
tags: [audit, missing-artefacts, design-only]

must_haves:
  truths:
    - "AUDIT-04 catalogues every referenced-but-missing artefact (platform-pipefy / platform-wrike skills; /refine-<skill> commands; workspace hub.md; client .env.example; missing scaffold dirs commands/agents/hooks) with exact citations from v0.3.0 docs."
    - "Every reference is cited in compact `file:line` form per D-14 with backtick wrapping."
    - "Findings tagged with severity per D-15: platform-pipefy/wrike refs are [BLOCKING] (downstream contract broken)."
    - "Negative-finding subsection preserved per CONCERNS.md:109-117 — verified working refs explicitly called out so absence-of-mention isn't read as untested."
    - "Each missing artefact has a 'Closes via:' DESIGN-* trailer pointing at v2 closure (no design move proposed)."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-04 section populated with 5 missing-artefact subsections + 1 negative-finding subsection."
      contains: "## AUDIT-04: Referenced-but-Missing Artefacts; ### 4.1; ### 4.2; ### 4.3; ### 4.4; ### 4.5; ### 4.6; **[BLOCKING]**; **Closes via:** DESIGN-"
  key_links:
    - from: "AUDIT-04 missing-artefact bullets"
      to: "v0.3.0 surfaces (READMEs + SKILL.md files)"
      via: "compact file:line citations"
      pattern: "`(README|dydx-delivery)\\.md:[0-9]+|`dydx-delivery/skills/.*/SKILL\\.md:[0-9]+"
    - from: "AUDIT-04 'Closes via:' trailers"
      to: ".planning/REQUIREMENTS.md DESIGN-04, DESIGN-05, DESIGN-14, DESIGN-15, DESIGN-16"
      via: "bold-keyed trailer line"
      pattern: "\\*\\*Closes via:\\*\\* DESIGN-(04|05|14|15|16)"
---

<objective>
Populate AUDIT-04 with the citation roll-up of every referenced-but-missing artefact in v0.3.0: missing platform skills (platform-pipefy, platform-wrike), missing slash commands (/refine-<skill>), missing workspace hub.md, missing client .env.example, missing scaffold directories (commands/, agents/, hooks/). Plus a negative-finding subsection naming the cross-references that DO resolve correctly so absence isn't conflated with gaps.

**Purpose:** AUDIT-04 satisfies ROADMAP success criterion 4 (referenced-but-missing artefacts catalogued with citations). RESEARCH.md §5 already enumerates every reference with `file:line` — this plan transcribes them under the established style with severity tags and DESIGN-* closures.

**Output:** AUDIT-04 section of `.planning/AUDIT.md` populated; 5 missing-artefact subsections + 1 negative-finding subsection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- AUDIT-04 input data is FULLY pre-built in RESEARCH.md §5 (lines 244-325). -->
<!-- The executor TRANSCRIBES; do not re-discover citations. -->
<!-- Negative-finding subsection sourced from CONCERNS.md:109-117 (cross-references that DO resolve). -->

The 5 missing-artefact subsections + 1 negative-finding subsection (per RESEARCH.md §5):

- `### 4.1 platform-pipefy / platform-wrike skills` — RESEARCH.md §5.1 — 4 citations — **[BLOCKING]** — Closes via DESIGN-14, DESIGN-15, DESIGN-16
- `### 4.2 /refine-<skill> slash commands` — RESEARCH.md §5.2 — 3 citations — **[STRUCTURAL]** — Closes via DESIGN-05 → Phase 4 OPEN-06
- `### 4.3 Workspace hub.md` — RESEARCH.md §5.3 — 3 citations — **[STRUCTURAL]** — Closes via Phase 4 OPEN-04 (no single DESIGN-*)
- `### 4.4 Client folder .env.example` — RESEARCH.md §5.4 — 2 citations — **[STRUCTURAL]** — Closes via Phase 4 OPEN-QUESTIONS (adjacent to DESIGN-09)
- `### 4.5 Missing scaffold directories (commands/, agents/, hooks/)` — RESEARCH.md §5.5 — 3 citations — **[STRUCTURAL]** — Closes via DESIGN-04
- `### 4.6 Verified working cross-references (negative finding)` — CONCERNS.md:109-117 — 3 working refs — (no severity / no closes-via — observation only)

Per PATTERNS.md Pattern 7, negative-finding callouts use plain English with parenthetical scope-clarifier.

Compact citation form per D-14: `` `dydx-delivery/README.md:89` `` (single line) or `` `dydx-delivery/skills/generate-technical-spec/SKILL.md:38-39` `` (range, ASCII hyphen).

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-04 section opener + subsections 4.1–4.5 (missing artefacts)</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §5 (lines 244-325) — every reference pre-cited
    - .planning/codebase/CONCERNS.md (lines 28-48, 96-105) — original CONCERNS.md framing for the same items
    - .planning/REQUIREMENTS.md AUDIT-04 (line 21); DESIGN-04, DESIGN-05, DESIGN-14, DESIGN-15, DESIGN-16
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 5 — Cross-reference style" + §"Pattern 6 — Tables"
    - .planning/phases/01-audit/01-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-04: Referenced-but-Missing Artefacts` in `.planning/AUDIT.md`.

    **Section opener** (1 paragraph, 2-3 sentences): "Catalogues every artefact that v0.3.0 docs reference but does not exist in the repo. Each subsection lists the exact references with `file:line` citations and points at the DESIGN-* requirement that closes the gap (audit names the gap; design proposes the fix per D-13)."

    Then write 5 H3 subsections per the index in `<interfaces>`. Each subsection has this exact shape:

    ```markdown
    ### 4.N <Topic name>

    <One-line context sentence describing what's referenced and what's missing.>

    | Reference | Citation | Context |
    |---|---|---|
    | <descriptor> | `<path>:<line>` | "<exact quoted snippet from the source>" |
    | <descriptor> | `<path>:<line>` | "<exact quoted snippet from the source>" |
    | ... | ... | ... |

    **Severity:** **[BLOCKING/STRUCTURAL]** — <one-line rationale>.

    **Closes via:** DESIGN-NN (one-line); DESIGN-MM (one-line). [or "Phase 4 OPEN-NN" if no DESIGN-* closes it]

    ---
    ```

    **Per-subsection content** — transcribe directly from RESEARCH.md §5 (lines 244-325):

    **§ 4.1 platform-pipefy / platform-wrike skills** — context: "Three downstream skills dispatch on `platform:` frontmatter and dynamically load `platform-pipefy` or `platform-wrike` — neither directory exists under `dydx-delivery/skills/`."
    Citation table (4 rows; from RESEARCH.md §5.1 lines 254-258):
    - Plugin README "Platform handling" — `` `dydx-delivery/README.md:89` `` — "Downstream skills...read this and dynamically load the matching platform skill (`platform-pipefy` or `platform-wrike`)"
    - generate-build-prompt SKILL — `` `dydx-delivery/skills/generate-build-prompt/SKILL.md:47` `` — "Based on `platform:` frontmatter, load `platform-pipefy`, `platform-wrike`, etc."
    - execute-tests SKILL — `` `dydx-delivery/skills/execute-tests/SKILL.md:55` `` — "load the matching skill (`platform-pipefy`, `platform-wrike`, etc.)"
    - generate-technical-spec SKILL — `` `dydx-delivery/skills/generate-technical-spec/SKILL.md:38-39` `` — "`platform: pipefy` → load the `platform-pipefy` skill"; "`platform: wrike` → load the `platform-wrike` skill"
    Severity: **[BLOCKING]** — three downstream skills dispatch on `platform:`; today the contract is broken in-repo.
    Closes via: DESIGN-14 (`platform-pipefy/`), DESIGN-15 (`platform-wrike/`), DESIGN-16 (`platform-ziflow/`).

    **§ 4.2 /refine-<skill> slash commands** — context: "Three orphan references to a `/refine-<skill>` command pattern; no `commands/` directory exists in the plugin and no `refine-*` skill exists."
    Citation table (3 rows):
    - Root README "How to publish" — `` `README.md:56` `` — "Example commit message: `feat(dydx-delivery): add refine-<skill> counterparts`"
    - Plugin README pipeline-loop step 5 — `` `dydx-delivery/README.md:51` `` — "Reviewer either edits in place...or runs `/refine-<skill>` to regenerate"
    - Plugin README versioning Option B — `` `dydx-delivery/README.md:85` `` — "Optional sibling `_review.md` for major iteration notes (used by `/refine-<skill>` if added later)"
    Severity: **[STRUCTURAL]** — orphan references; reader can't act on them.
    Closes via: DESIGN-05 (refine pattern resolution) → Phase 4 OPEN-06.

    **§ 4.3 Workspace hub.md** — context: "Workspace-level client index referenced in plugin README and discovery-intake; not present in this repo."
    Citation table (3 rows):
    - Plugin README file-locations — `` `dydx-delivery/README.md:57` `` — "Artefacts land in the standard client folder shape (see workspace `hub.md`)"
    - discovery-intake SKILL Step 1 — `` `dydx-delivery/skills/discovery-intake/SKILL.md:27` `` — "match it against the workspace `hub.md` client index"
    - discovery-intake SKILL Step 1 (second) — `` `dydx-delivery/skills/discovery-intake/SKILL.md:28` `` — "see `hub.md`"
    Severity: **[STRUCTURAL]** — orphan reference. v2 names a per-client `00_HUB.md` (different artefact, lives inside client folders) but the workspace-level `hub.md` reference does not match that.
    Closes via: No single DESIGN-* requirement closes this directly; OPEN-04 (hub-link backfill) addresses per-client `00_HUB.md`. Workspace-level `hub.md` resolution is deferred to Phase 4 OPEN-QUESTIONS.

    **§ 4.4 Client folder .env.example** — context: "Per-client `.env.example` file referenced as the canonical list of required env-var names; this is a *per-client* artefact (lives outside this repo by design), but the gap is that v2 may need to specify a canonical shape."
    Citation table (2 rows):
    - when-to-open-claude-code.md setup-step 3 — `` `dydx-delivery/skills/generate-build-prompt/references/when-to-open-claude-code.md:88` `` — "Set up env vars for sandbox API access...see your client folder's `.env.example`"
    - build-prompt-template.md env-var section — `` `dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md:120` `` (per CONCERNS.md:48) — Per-client `.env.example` referenced as the source for required env-var names
    Severity: **[STRUCTURAL]** — orphan reference (per-client artefact, missing in *this* repo is correct; the gap is canonical-shape spec for v2).
    Closes via: Adjacent to DESIGN-09 (directional-boundary contract); not a direct match. Likely Phase 4 OPEN-QUESTIONS.

    **§ 4.5 Missing scaffold directories (commands/, agents/, hooks/)** — context: "Plugin scaffold directories absent; v2 architecture introduces all three."
    Citation table (3 rows):
    - `dydx-delivery/commands/` — `` `.planning/codebase/CONCERNS.md:99` `` — "No `commands/` directory exists in the plugin"; v2 introduces 1 parameterised `refine.md` + 4 GSD-prefixed shortcuts (DESIGN-04)
    - `dydx-delivery/agents/` — `` `.planning/codebase/CONCERNS.md:100` `` — "No `agents/` directory exists in the plugin"; v2 introduces 1 (`test-bot-orchestrator`) (DESIGN-04)
    - `dydx-delivery/hooks/` — `` `.planning/codebase/CONCERNS.md:101` `` — "No `hooks/` directory exists in the plugin"; v2 introduces 2 (`validate-frontmatter`, `bump-artefact-version`); explicitly NOT auto-progression (DESIGN-04)
    Severity: **[STRUCTURAL]** — scaffold absence; v2 adds these surfaces.
    Closes via: DESIGN-04 (plugin surfaces).

    **Implementing decisions:** D-13 (observation-led), D-14 (compact citation form), D-15 (severity tags), D-08 (CONCERNS citations preserved).
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### 4\.[1-5] ' .planning/AUDIT.md)" = "5" ] &amp;&amp; [ "$(awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -c '\*\*Closes via:\*\*')" -ge "5" ] &amp;&amp; awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -q '\*\*\[BLOCKING\]\*\*' &amp;&amp; awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -q 'DESIGN-14' &amp;&amp; awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -q 'DESIGN-04' &amp;&amp; echo SUBSECTIONS_4_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 5 H3 subsections under AUDIT-04 (`### 4.1`, `### 4.2`, `### 4.3`, `### 4.4`, `### 4.5`): `grep -cE '^### 4\.[1-5] ' .planning/AUDIT.md` returns `5`
    - At least one **[BLOCKING]** tag (subsection 4.1): `awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -c '\*\*\[BLOCKING\]\*\*'` >= `1`
    - At least 4 **[STRUCTURAL]** tags (subsections 4.2-4.5): `awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -c '\*\*\[STRUCTURAL\]\*\*'` >= `4`
    - DESIGN-14, DESIGN-15, DESIGN-16, DESIGN-04, DESIGN-05 all referenced: `awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -cE 'DESIGN-(14|15|16|04|05)'` >= `5`
    - At least 5 `**Closes via:**` trailers: `awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -c '\*\*Closes via:\*\*'` >= `5`
    - At least 14 compact-form citations across the 5 subsections (4 + 3 + 3 + 2 + 3 = 15 expected): `awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -cE '\`(README|dydx-delivery|\\.planning/codebase/CONCERNS).*\.md:[0-9]+'` >= `14`
    - No design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-04:/,/^## AUDIT-05:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`
  </acceptance_criteria>
  <done>
    AUDIT-04 has 5 missing-artefact subsections; every reference cited; severity tags applied; DESIGN-* closures named; observation-led tone preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add subsection 4.6 — Negative findings (verified working cross-references)</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (lines 109-117) — Cross-references between docs (verified working)
    - .planning/phases/01-audit/01-RESEARCH.md §5.6 (lines 317-325) — Negative findings list
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 7 — Negative-finding callouts" — discipline of explicit "verified clean" sentences
  </read_first>
  <action>
    Append `### 4.6 Verified working cross-references (negative finding)` under AUDIT-04 (after § 4.5's `---`).

    Per PATTERNS.md Pattern 7, this section keeps the discipline of explicit "verified clean" sentences so reviewers don't read absence-of-mention as untested.

    Write **one short paragraph** (2 sentences): "Beyond the missing artefacts above, several cross-references in v0.3.0 docs DO resolve correctly. Listed here so reviewers do not conflate 'not flagged' with 'not checked'."

    Then 3 bullets (per RESEARCH.md §5.6):

    - `` `dydx-delivery/README.md:95` `` → points at `skills/generate-build-prompt/references/when-to-open-claude-code.md` — file resolves.
    - `` `dydx-delivery/skills/execute-tests/SKILL.md:23` `` → points at `references/safety-rules.md` — file resolves.
    - `` `dydx-delivery/skills/generate-test-plan/SKILL.md:49` `` → points at `references/test-plan-template.md` — file resolves.

    End with a closing parenthetical sentence: "(No broken intra-plugin references found beyond the 5 categories above. Verified by directory listings during the 2026-05-09 audit pass.)"

    Then `---` separator to close AUDIT-04.

    Note: this subsection has NO severity tag and NO `**Closes via:**` trailer — negative findings are observations of cleanliness, not gaps.

    **Implementing decisions:** D-08 (CONCERNS preservation), D-14 (citation form), Pattern 7 (negative finding discipline).
  </action>
  <verify>
    <automated>grep -qE '^### 4\.6 ' .planning/AUDIT.md &amp;&amp; [ "$(awk '/^### 4\.6 /,/^---$/' .planning/AUDIT.md | grep -c 'resolves')" -ge "3" ] &amp;&amp; echo NEGATIVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^### 4\.6 ' .planning/AUDIT.md` succeeds (subsection 4.6 anchor present)
    - Subsection 4.6 contains 3 working-reference bullets: `awk '/^### 4\.6 /,/^---$/' .planning/AUDIT.md | grep -c 'resolves'` >= `3`
    - All 3 working-reference citations present: `awk '/^### 4\.6 /,/^---$/' .planning/AUDIT.md | grep -cE '\`dydx-delivery/(README\.md:95|skills/execute-tests/SKILL\.md:23|skills/generate-test-plan/SKILL\.md:49)'` >= `3`
    - Closing parenthetical sentence present: `awk '/^### 4\.6 /,/^---$/' .planning/AUDIT.md | grep -q 'No broken intra-plugin references'` succeeds
    - Subsection 4.6 has NO severity tag (negative findings have no tag): `awk '/^### 4\.6 /,/^---$/' .planning/AUDIT.md | grep -cE '\*\*\[(BLOCKING|STRUCTURAL|COSMETIC)\]\*\*'` returns `0`
    - Subsection 4.6 has NO `**Closes via:**` trailer: `awk '/^### 4\.6 /,/^---$/' .planning/AUDIT.md | grep -c '\*\*Closes via:\*\*'` returns `0`
  </acceptance_criteria>
  <done>
    Negative-finding subsection 4.6 landed; AUDIT-04 has 6 H3 subsections (5 missing + 1 verified-clean); structural-check still functions correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH.md §5 → AUDIT-04 prose | Executor transcribes pre-built citation list. If executor invents references not in §5, AUDIT-04 inflates the missing-artefact count. |
| `file:line` citations → actual SKILL.md / README content | Citations must resolve. Line drift breaks reviewer navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | Executor invents missing-artefact entries not in RESEARCH.md §5 / CONCERNS.md | mitigate | `<read_first>` mandates RESEARCH.md §5; acceptance criteria check that 5 subsections + at least 14 citations land (matches §5 count). Manual review confirms each subsection maps to §5.X. |
| T-01-04-02 | Repudiation | Negative-finding subsection 4.6 omitted (silent CONCERNS.md drop) | mitigate | Acceptance criterion mandates `^### 4\.6 ` exists; structural assertion in audit-structure-check.sh (assertion #4: "all sections absorbed") catches this in Plan 09 cross-check. |
| T-01-04-03 | Information Disclosure | Citation drift (line numbers shift in v0.3.0 source files) | accept (manual review catches) | VALIDATION.md "Citations resolve" reviewer spot-check is the safety net. |
| T-01-04-04 | Tampering | Wrong DESIGN-* mapping (e.g. claiming DESIGN-04 closes hub.md when it doesn't) | mitigate | RESEARCH.md §10 (DESIGN-* cross-ref index) is canonical; executor reads it; acceptance criterion checks DESIGN-04, DESIGN-05, DESIGN-14, DESIGN-15, DESIGN-16 all appear (matches §5 closures). |
| T-01-04-05 | Repudiation | Section drifts into design proposal (violates D-13) | mitigate | Acceptance criterion runs canonical D-13 banned-phrase ERE `grep -ciE 'we should\|the design will\|recommend that v2\|propose\|v2 will' = 0` — same pattern enforced in every content plan for consistency. |
</threat_model>

<verification>
End of Plan 04 success-state:
1. AUDIT-04 has 6 H3 subsections (`### 4.1` through `### 4.6`).
2. At least 14 compact-form `file:line` citations across the 5 missing-artefact subsections.
3. Subsection 4.1 carries **[BLOCKING]** severity (the only one); 4.2–4.5 carry **[STRUCTURAL]**.
4. 5 `**Closes via:** DESIGN-` trailers (one per missing-artefact subsection).
5. Subsection 4.6 has 3 verified-clean bullets and no severity tag / no closes-via.
6. No design-move language (canonical D-13 ERE returns 0).
7. `bash .planning/phases/01-audit/scripts/audit-structure-check.sh` still exits 1 (other waves outstanding); 8-section count holds.
</verification>

<success_criteria>
- [ ] AUDIT-04 has 6 H3 subsections (5 missing + 1 verified-clean)
- [ ] At least 14 compact-form citations
- [ ] [BLOCKING] tag on subsection 4.1; [STRUCTURAL] on 4.2-4.5; no tag on 4.6
- [ ] Closes via DESIGN-04, DESIGN-05, DESIGN-14, DESIGN-15, DESIGN-16 referenced (one per missing subsection)
- [ ] No design-move language (canonical D-13 banned-phrase ERE)
- [ ] No content under `dydx-delivery/` modified
- [ ] Structural-check script still recognises 8 H2 sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-04-SUMMARY.md` documenting:
- Subsection count, citation count, severity-tag totals
- DESIGN-* IDs referenced
- Pointer: "Plan 05 (Wave 5) populates AUDIT-05 next"
</output>
