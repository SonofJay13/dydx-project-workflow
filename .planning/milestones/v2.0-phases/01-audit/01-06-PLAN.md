---
phase: 01-audit
plan: 06
type: execute
wave: 6
depends_on: ["01-audit/05"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-06]
tags: [audit, version-mismatches, design-only]

must_haves:
  truths:
    - "AUDIT-06 inventories every version-string location in v0.3.0 (8 locations per RESEARCH.md §7)."
    - "AUDIT-06 explicitly recommends `2.0.0` as the synced target version per D-17."
    - "Each location is cited in compact `file:line` form per D-14."
    - "Locations that drift from the plugin's declared 0.3.0 (e.g. README:9 says 0.1.0; results-template hardcodes v0.1.0) are tagged [STRUCTURAL]."
    - "AUDIT-06 does NOT execute the version bump — that work is scheduled for v2.1 Foundations build (FOUND-04) per AUDIT-07 + AUDIT-04 cross-refs."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-06 section populated with version-mismatch table + 2.0.0 synced-target recommendation."
      contains: "## AUDIT-06: Version-String Mismatches; 2.0.0; plugin.json; marketplace.json; v0.1.0"
  key_links:
    - from: "AUDIT-06 version-mismatch table"
      to: "v0.3.0 manifests + READMEs + results-template + safety-rules"
      via: "compact file:line citations"
      pattern: "`(dydx-delivery/\\.claude-plugin/plugin\\.json|\\.claude-plugin/marketplace\\.json|README\\.md|dydx-delivery/README\\.md|dydx-delivery/skills/execute-tests/references/(results-template|safety-rules)\\.md):[0-9]+"
    - from: "AUDIT-06 2.0.0 recommendation"
      to: ".planning/REQUIREMENTS.md AUDIT-06 / FOUND-04"
      via: "Synced target paragraph"
      pattern: "2\\.0\\.0"
---

<objective>
Populate AUDIT-06 with the exhaustive version-string mismatch inventory plus the 2.0.0 synced-target recommendation per D-17. Every version-bearing location in v0.3.0 is cited; the table marks current value, what it represents, and whether it drifts from `plugin.json`'s declared 0.3.0.

**Purpose:** AUDIT-06 satisfies ROADMAP success criterion 5 (version-string mismatches catalogued; recommend `2.0.0` synced target). RESEARCH.md §7 already enumerates every location with `file:line` and current value — this plan transcribes them under the established style.

**Output:** AUDIT-06 section of `.planning/AUDIT.md` populated; 8-row version-mismatch table + 2.0.0 recommendation paragraph; structural-check assertion #8 (`grep -qF '2.0.0'`) now passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- AUDIT-06 input data is FULLY pre-built in RESEARCH.md §7 (lines 397-414). -->
<!-- The 8-row version-mismatch table is already enumerated; the executor TRANSCRIBES it. -->

The 8 version-bearing locations (per RESEARCH.md §7):

| # | Location | Citation | Current value | What it represents | Drifts from 0.3.0? |
|---|----------|----------|---------------|---------------------|---------------------|
| 1 | Plugin manifest | `dydx-delivery/.claude-plugin/plugin.json:3` | `"version": "0.3.0"` | Plugin semver | Source of truth |
| 2 | Marketplace plugin entry | `.claude-plugin/marketplace.json:16` | `"version": "0.3.0"` | Plugin entry version (must match #1) | No |
| 3 | Marketplace metadata version | `.claude-plugin/marketplace.json:9` | `"version": "1.2.0"` | Marketplace metadata version — scope unclear | YES (different scheme/scope) |
| 4 | Root README plugin table | `README.md:9` | `0.1.0` | Documented plugin version | YES (drifted by 0.2.0) |
| 5 | Plugin README changelog | `dydx-delivery/README.md:126` | `**0.3.0** —` (truncated) | Plugin changelog | No (matches but content truncated → AUDIT-07) |
| 6 | Results-template runner | `dydx-delivery/skills/execute-tests/references/results-template.md:9` | `runner: dydx-delivery/execute-tests v0.1.0` | Hardcoded runner version | YES (drifted by 0.2.0) |
| 7 | Safety-rules `v1` reference | `dydx-delivery/skills/execute-tests/references/safety-rules.md:93` | "Parallel execution is not supported in v1" | Unclear-scope `v1` reference (no anchor) | Unclear (no companion anchor) |
| 8 | Root README versioning convention | `README.md:81-83` | (semver convention statement, no version) | Documents the semver convention | N/A (convention doc) |

Synced target per D-17: all version-bearing locations move to `2.0.0` in v2.1 Foundations build (FOUND-04). This audit DOES NOT execute the bump; it inventories the gap.

Compact citation form per D-14: `` `dydx-delivery/.claude-plugin/plugin.json:3` ``.

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive). Note: the literal phrase "Recommended sync target: 2.0.0" required by D-17 does NOT match the banned ERE — `propose` is not present, and `recommend that v2` requires the contiguous tokens which "Recommended sync target" does not contain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-06 section opener + 8-row version-mismatch table + 2.0.0 synced-target recommendation</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §7 (lines 397-414) — 8 version locations enumerated
    - .planning/codebase/CONCERNS.md (lines 9-24, 226-230) — original CONCERNS.md version-mismatch + versioning-convention sections
    - .planning/REQUIREMENTS.md AUDIT-06 (line 23); FOUND-04 (v2.1 manifest sync target)
    - .planning/phases/01-audit/01-CONTEXT.md D-17 (recommends 2.0.0); D-13, D-14, D-15
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 6 — Tables"
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-06: Version-String Mismatches` in `.planning/AUDIT.md`.

    **Section opener** (1 paragraph, 2-3 sentences): "Inventories every version-bearing location in v0.3.0 — manifests, READMEs, hardcoded version strings — and recommends `2.0.0` as the synced target version per D-17. This audit catalogues the gap; the actual version bump is scheduled for v2.1 Foundations build (FOUND-04), not this design milestone."

    Then write the **8-row version-mismatch table** with these EXACT columns (per RESEARCH.md §7):

    ```
    | # | Location | Citation | Current value | What it represents | Drifts from 0.3.0? |
    |---|---|---|---|---|---|
    ```

    Then 8 data rows transcribed verbatim from `<interfaces>`. Use compact-form `file:line` citation in the Citation column with backtick wrapping.

    After the table, add **two short paragraphs**:

    **Paragraph 1 — Summary observation (severity-tagged):**
    "Locations #4 (root README at `0.1.0`) and #6 (results-template hardcoded `v0.1.0`) are the most egregious — both drift the plugin's declared version backwards by `0.2.0`. **[STRUCTURAL]**. Locations #1, #2, #5 align at `0.3.0`. Location #3 (marketplace metadata at `1.2.0`) uses a different scheme and its scope is undocumented (`README.md:81-83` versioning convention does not address marketplace-vs-plugin version split). **[STRUCTURAL]**. Location #7 references `v1` without a companion anchor (`safety-rules.md:93`: 'Parallel execution is not supported in v1'). **[STRUCTURAL]**."

    **Paragraph 2 — Synced-target recommendation (D-17):**
    "**Recommended sync target: `2.0.0`** per AUDIT-06 requirement and D-17. All 6 version-bearing locations (#1, #2, #3, #4, #5, #6) move to `2.0.0` in v2.1 Foundations build (FOUND-04); location #7 needs an anchor decision (delete the `v1` phrase or replace with v2 anchor); location #8 is the convention statement (no value change required). **Cross-ref:** AUDIT-07 (cosmetic-but-client-visible aspect of locations #4 and #5)."

    End with `---` separator.

    **CRITICAL:** Per D-17 the literal string `2.0.0` MUST appear in this section so the structural-check script's assertion #8 (`grep -qF '2.0.0' "$AUDIT_FILE"`) passes after this plan lands.

    **Implementing decisions:** D-13 (observation-led — audit catalogues gap, schedules to v2.1), D-14 (compact citations), D-15 (severity tags), D-17 (2.0.0 synced target).
  </action>
  <verify>
    <automated>awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -F '| # | Location | Citation | Current value | What it represents | Drifts from 0.3.0? |' &amp;&amp; [ "$(awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -cE '^\| [0-9] \|')" = "8" ] &amp;&amp; awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -qF '2.0.0' &amp;&amp; awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -q 'plugin.json:3' &amp;&amp; awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -q 'marketplace.json:16' &amp;&amp; awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -q 'results-template.md:9' &amp;&amp; echo TABLE_8_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-06 contains the 6-column table header: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -F '| # | Location | Citation | Current value | What it represents | Drifts from 0.3.0? |'` succeeds
    - Exactly 8 numbered rows (`| 1 |` through `| 8 |`): `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -cE '^\| [0-9] \|'` returns `8`
    - The literal string `2.0.0` appears in AUDIT-06: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -cF '2.0.0'` >= `1` (this is the assertion #8 in structural-check)
    - All 6 distinct version-bearing source paths cited: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -cE '\`(dydx-delivery/\.claude-plugin/plugin\.json|\.claude-plugin/marketplace\.json|README\.md|dydx-delivery/README\.md|dydx-delivery/skills/execute-tests/references/(results-template|safety-rules)\.md):[0-9]+'` >= `6`
    - At least 3 [STRUCTURAL] tags in AUDIT-06: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -c '\*\*\[STRUCTURAL\]\*\*'` >= `3`
    - Synced-target paragraph mentions FOUND-04: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -q 'FOUND-04'` succeeds
    - Cross-ref to AUDIT-07: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -q 'AUDIT-07'` succeeds
    - Structural-check assertion #8 now passes: `grep -qF '2.0.0' .planning/AUDIT.md` succeeds
    - No design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-06:/,/^## AUDIT-07:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`. (Note: "Recommended sync target: `2.0.0`" is NOT matched by the banned ERE — `recommend that v2` requires the literal `that v2` after `recommend`, which the sync-target phrase does not contain. "scheduled for v2.1 Foundations build" is observation-led and likewise does not match the banned set.)
  </acceptance_criteria>
  <done>
    AUDIT-06 has 8-row version-mismatch table + summary + 2.0.0 synced-target recommendation; structural-check assertion #8 passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH.md §7 → AUDIT-06 table | Executor transcribes 8 pre-built rows. If line numbers drift in v0.3.0 source files (manifests, READMEs), citations break. |
| `2.0.0` synced-target recommendation | D-17 locks this value. Any other recommendation (`1.0.0`, `2.1.0`, etc.) violates D-17 and breaks structural-check assertion #8. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | Wrong synced target (e.g. `1.0.0`) | mitigate | D-17 explicitly locks `2.0.0`. Acceptance criterion + structural-check assertion #8 both check for `grep -qF '2.0.0'`. |
| T-01-06-02 | Repudiation | Audit prematurely "executes" the version bump (modifies plugin.json or manifest) | mitigate | Plan's `files_modified` is `.planning/AUDIT.md` only. Phase-level success criterion checks `git status -- dydx-delivery/` is empty. |
| T-01-06-03 | Information Disclosure | Citation drift (line 9 of marketplace.json moves) | accept (manual reviewer catches) | VALIDATION.md "Citations resolve" reviewer spot-check covers this. |
| T-01-06-04 | Repudiation | Section drifts into design proposal (violates D-13) | mitigate | Acceptance criterion runs canonical D-13 banned-phrase ERE `grep -ciE 'we should\|the design will\|recommend that v2\|propose\|v2 will' = 0` — same pattern enforced in every content plan. The D-17 phrase "Recommended sync target: 2.0.0" is intentionally NOT in the banned set (no `propose` token; `recommend that v2` requires literal contiguous match). |
| T-01-06-05 | Denial of Service | None applicable | accept | N/A (markdown documentation phase). |
</threat_model>

<verification>
End of Plan 06 success-state:
1. AUDIT-06 has section opener + 8-row version-mismatch table + summary paragraph + 2.0.0 recommendation paragraph.
2. The literal string `2.0.0` appears at least once in AUDIT-06.
3. Structural-check script's assertion #8 (`grep -qF '2.0.0' .planning/AUDIT.md`) NOW passes (was failing before this plan).
4. All 6 distinct version-bearing source paths cited.
5. No design-move language (canonical D-13 ERE returns 0); "scheduled for v2.1 Foundations build (FOUND-04)" wording is observation-led.
6. 8-H2-section count holds.
</verification>

<success_criteria>
- [ ] AUDIT-06 has 8-row version-mismatch table with the 6 specified columns
- [ ] `2.0.0` appears at least once in AUDIT-06 (D-17 + structural-check assertion #8)
- [ ] At least 3 [STRUCTURAL] tags
- [ ] FOUND-04 referenced as the v2.1 build target
- [ ] No design-move language (canonical D-13 banned-phrase ERE)
- [ ] No content under `dydx-delivery/` modified
- [ ] Structural-check script's assertion #8 now passes; assertion #4 (absorption claim) still fails (Plan 09 closes that)
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-06-SUMMARY.md` documenting:
- Row count (8); citation count
- Confirmation that structural-check assertion #8 (`2.0.0`) now passes
- Pointer: "Plan 07 (Wave 7) populates AUDIT-07 next"
</output>
