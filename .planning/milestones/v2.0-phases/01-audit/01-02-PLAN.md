---
phase: 01-audit
plan: 02
type: execute
wave: 2
depends_on: ["01-audit/01"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-01]
tags: [audit, per-skill-inventory, design-only]

must_haves:
  truths:
    - "AUDIT-01 section in AUDIT.md catalogues all 7 v0.3.0 skills (discovery-intake, generate-sow, generate-functional-spec, generate-technical-spec, generate-test-plan, generate-build-prompt, execute-tests) with purpose, inputs, outputs, hand-off contract, deps, brittleness, and what's missing for v2."
    - "AUDIT-01 opens with a 7-row scannable matrix (one row per skill) per D-11."
    - "Each skill has a focused prose subsection (D-12) with three bold-keyed mini-headers: Hand-off contract, Observed brittleness, What's missing for v2."
    - "Every brittleness bullet carries a `file:line` citation in compact form (per D-14) and a severity tag (per D-15)."
    - "Every 'What's missing for v2' callout points at a DESIGN-* requirement ID without proposing the design move (per D-13)."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-01 section populated with skill matrix + 7 prose subsections."
      contains: "## AUDIT-01: Per-Skill Inventory; ### 1.1 discovery-intake; ### 1.2 generate-sow; ### 1.3 generate-functional-spec; ### 1.4 generate-technical-spec; ### 1.5 generate-test-plan; ### 1.6 generate-build-prompt; ### 1.7 execute-tests; **Hand-off contract:**; **Observed brittleness:**; **What's missing for v2:**; **[BLOCKING]**; **[STRUCTURAL]**; **Closes via:** DESIGN-"
  key_links:
    - from: "AUDIT-01 brittleness bullets"
      to: ".planning/codebase/CONCERNS.md (preserved citations) + dydx-delivery/skills/*/SKILL.md (live frontmatter excerpts)"
      via: "compact file:line citations"
      pattern: "`dydx-delivery/skills/.*/SKILL\\.md:[0-9]+"
    - from: "AUDIT-01 'What's missing for v2' bullets"
      to: ".planning/REQUIREMENTS.md DESIGN-* IDs"
      via: "**Closes via:** trailer line per PATTERNS.md Pattern 5"
      pattern: "\\*\\*Closes via:\\*\\* DESIGN-"
---

<objective>
Populate AUDIT-01 with a 7-row matrix + 7 prose subsections (one per v0.3.0 skill) that catalogue purpose, inputs, outputs, hand-off contract, dependencies, observed brittleness, and what's missing for v2 — citation-dense, severity-tagged, and observation-led (no v2 design moves proposed).

**Purpose:** AUDIT-01 is the largest content section in AUDIT.md and the foundation Phase 2 design rests on (per ROADMAP success criterion 1: "readable as a self-contained reference without consulting the v0.3.0 source files"). RESEARCH.md §3 already enumerates every brittleness finding with `file:line` citations — this plan transcribes them into AUDIT.md under the established style (PATTERNS.md Patterns 2/3/4/5/6/8).

**Output:** AUDIT-01 section of `.planning/AUDIT.md` populated; structural-check script's existing assertions remain live (still exits 1 because Waves 3–9 haven't landed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- AUDIT-01 input data is FULLY pre-built in RESEARCH.md §3 (lines 99-198) and §10 (lines 480-509). -->
<!-- The executor should TRANSCRIBE these into AUDIT.md, not re-discover them from the SKILL.md files. -->
<!-- The 7 SKILL.md files are read-only references for citation verification, not first-look discovery. -->

The 7 skill audit units to produce (RESEARCH.md §3 maps each):

| Section anchor | Skill folder | RESEARCH.md source | Closes-via DESIGN |
|----------------|--------------|---------------------|-------------------|
| `### 1.1 discovery-intake` | `dydx-delivery/skills/discovery-intake/` | RESEARCH.md §3.1 (lines 105-114) | DESIGN-17, DESIGN-18, DESIGN-01 |
| `### 1.2 generate-sow` | `dydx-delivery/skills/generate-sow/` | RESEARCH.md §3.2 (lines 116-126) | DESIGN-19, DESIGN-01 |
| `### 1.3 generate-functional-spec` | `dydx-delivery/skills/generate-functional-spec/` | RESEARCH.md §3.3 (lines 128-139) | DESIGN-20 |
| `### 1.4 generate-technical-spec` | `dydx-delivery/skills/generate-technical-spec/` | RESEARCH.md §3.4 (lines 141-152) | DESIGN-21, DESIGN-14/15/16, DESIGN-01 |
| `### 1.5 generate-test-plan` | `dydx-delivery/skills/generate-test-plan/` | RESEARCH.md §3.5 (lines 154-165) | DESIGN-24, DESIGN-01, DESIGN-02, DESIGN-03 |
| `### 1.6 generate-build-prompt` | `dydx-delivery/skills/generate-build-prompt/` | RESEARCH.md §3.6 (lines 167-181) | DESIGN-23, DESIGN-01, DESIGN-12 |
| `### 1.7 execute-tests` | `dydx-delivery/skills/execute-tests/` | RESEARCH.md §3.7 (lines 183-197) | DESIGN-24, DESIGN-01, DESIGN-02, DESIGN-04 |

Matrix columns (per D-11) — exact column order for the AUDIT-01 opening table:

| Skill | Purpose (1 line) | Inputs | Outputs | Downstream consumer | Deps | Status flag(s) |

Citation style (per D-14, PATTERNS.md Pattern 3 compact form): `` `dydx-delivery/skills/discovery-intake/SKILL.md:11-13` ``. Range uses ASCII hyphen, NOT en-dash.

Severity tag style (per D-15, PATTERNS.md Pattern 4): bracketed all-caps at end of bullet, bold-wrapped: `... contract is broken in-repo. **[BLOCKING]**`. `[NEW]` precedes severity for net-new findings: `**[NEW]** **[STRUCTURAL]**`.

Cross-ref style (per D-13, PATTERNS.md Pattern 5): bold-keyed trailer line at end of "What's missing for v2" subsection:
```
**Closes via:** DESIGN-17 (Stage 1 Kickoff capture skill); DESIGN-18 (Stage 2 Discovery refactor); DESIGN-01 (frontmatter scheme).
**Pitfall ref:** CRIT-6 (frontmatter migration corruption).
```

NEVER write phrases like "v2 should...", "the design will...", "we recommend the v2 design..." — D-13 forbids design moves; only point at DESIGN-* IDs. The canonical D-13 banned-phrase ERE used by every plan's gate is `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-01 7-row skill matrix</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §3 (lines 99-198) — per-skill inputs/outputs/triggers excerpted from each SKILL.md frontmatter
    - .planning/codebase/CONVENTIONS.md — naming conventions for status lifecycle vocabulary
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 6 — Tables" (lines 134-156) — pipe-style markdown table convention
    - .planning/phases/01-audit/01-CONTEXT.md D-11 — matrix column spec
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-01: Per-Skill Inventory` in `.planning/AUDIT.md` with content. Open with one short paragraph (2–3 sentences) framing the section's role: "Catalogues all 7 v0.3.0 skills shipped under `dydx-delivery/skills/`. Each skill has a matrix row below plus a focused prose subsection covering hand-off contract, observed brittleness, and what's missing for v2 — observation-led; design moves are deferred to DESIGN-* requirements."

    Then write a markdown pipe-table per PATTERNS.md Pattern 6 (no alignment colons; backtick-wrapped citations in cells where applicable). Header row exactly:

    ```
    | Skill | Purpose (1 line) | Inputs | Outputs | Downstream consumer | Deps | Status flag(s) |
    |---|---|---|---|---|---|---|
    ```

    Then 7 data rows — one per skill in pipeline order (discovery-intake → generate-sow → generate-functional-spec → generate-technical-spec → generate-test-plan → generate-build-prompt → execute-tests). Each row's content comes from RESEARCH.md §3 and CONVENTIONS.md / STRUCTURE.md:

    - **Skill** column: skill name only (`discovery-intake`, `generate-sow`, etc.).
    - **Purpose** column: one-line description from each SKILL.md frontmatter `description` field — keep it under ~80 chars.
    - **Inputs** column: exact filename pattern (e.g. `00_discovery_v{N}.md` or "free-form context"). Use RESEARCH.md §3 "Inputs:" sub-bullets verbatim.
    - **Outputs** column: exact output path pattern (e.g. `<Client>/build-specs/<platform>/00_discovery_v{N}.md`).
    - **Downstream consumer** column: name of the next-stage skill (e.g. `generate-sow`) OR `(terminal — results summary)` for `execute-tests`.
    - **Deps** column: dependencies list — for the four skills with `platform:` dispatch, write "platform-pipefy / platform-wrike (referenced; missing — see AUDIT-04)". For others write `—`.
    - **Status flag(s)** column: status values used by the skill — e.g. `draft → approved` for fnspec/techspec/test-plan; `draft → client_review → approved` for generate-sow (the only one with client_review); `draft (no follow-up documented)` for build-prompt; `(no status field)` for execute-tests/results-template.

    End the matrix with a `---` separator. Do NOT write the per-skill prose subsections in this task — Task 2 handles them.

    **Implementing decisions:** D-11 (matrix shape), D-14 (citation form for cell values), AUDIT-01 success criterion 1.
  </action>
  <verify>
    <automated>grep -A 2 '^## AUDIT-01:' .planning/AUDIT.md | head -3 &amp;&amp; [ "$(awk '/^## AUDIT-01:/,/^---$/' .planning/AUDIT.md | grep -c '^|')" -ge "9" ] &amp;&amp; awk '/^## AUDIT-01:/,/^---$/' .planning/AUDIT.md | grep -q 'discovery-intake' &amp;&amp; awk '/^## AUDIT-01:/,/^---$/' .planning/AUDIT.md | grep -q 'execute-tests' &amp;&amp; echo MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## AUDIT-01:/,/^---$/' .planning/AUDIT.md | grep -c '^|'` returns >= `9` (header row + separator row + 7 data rows)
    - All 7 skill names appear in the matrix: `grep -E 'discovery-intake|generate-sow|generate-functional-spec|generate-technical-spec|generate-test-plan|generate-build-prompt|execute-tests' .planning/AUDIT.md | head -10` shows each at least once
    - The matrix header contains the 7 columns: `grep -F 'Skill | Purpose (1 line) | Inputs | Outputs | Downstream consumer | Deps | Status flag(s)' .planning/AUDIT.md` succeeds
    - The opening framing paragraph appears between the H2 anchor and the matrix (manual visual check; `awk '/^## AUDIT-01:/,/^\|/' .planning/AUDIT.md | wc -l` returns >= 4)
  </acceptance_criteria>
  <done>
    AUDIT-01 opens with framing paragraph + 7-row skill matrix; structural-check still runs without errors on the 8-section count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 7 per-skill prose subsections (### 1.1 .. ### 1.7) — observation-led, citation-dense</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §3.1 through §3.7 (lines 103-198) — every brittleness finding pre-cited with `file:line`
    - .planning/phases/01-audit/01-RESEARCH.md §10 (lines 480-509) — DESIGN-* cross-ref index per skill
    - .planning/phases/01-audit/01-RESEARCH.md §9 (lines 458-478) — PITFALLS cross-ref index
    - .planning/codebase/CONCERNS.md (lines 166-187) — frontmatter inconsistencies (status lifecycle, sandbox block, based_on_* naming)
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 5 — Cross-reference style" + §"Pattern 8 — Frontmatter-list / inventory style"
    - .planning/phases/01-audit/01-CONTEXT.md D-12, D-13, D-14, D-15 — subsection shape, observation-led tone, citation form, severity tags
    - dydx-delivery/skills/discovery-intake/SKILL.md (lines 1-130, focus on frontmatter + handoff)
    - dydx-delivery/skills/generate-sow/SKILL.md (lines 1-100, focus on lines 27-33 start-at-any-point + 93 client_review)
    - dydx-delivery/skills/generate-functional-spec/SKILL.md (lines 28-32 + frontmatter)
    - dydx-delivery/skills/generate-technical-spec/SKILL.md (lines 28-32, 38-39, frontmatter)
    - dydx-delivery/skills/generate-test-plan/SKILL.md (lines 30-34, frontmatter)
    - dydx-delivery/skills/generate-build-prompt/SKILL.md (lines 28-32, 47, 163-171)
    - dydx-delivery/skills/execute-tests/SKILL.md (lines 21-31, 40-44, 55)
  </read_first>
  <action>
    Append 7 H3 subsections under AUDIT-01 (after the matrix's `---`). Each subsection has the exact same shape per D-12:

    ```markdown
    ### 1.N <skill-name>

    **Hand-off contract:**
    - Carrier file path: <path from RESEARCH.md §3.N>
    - Frontmatter fields propagated: <list from RESEARCH.md §3.N "Frontmatter written">
    - Gating status flag: <status word from RESEARCH.md §3.N "Hand-off contract">
    - Hand-off message points at: `<next-skill-name>` (or "(terminal stage)")

    **Observed brittleness:**
    - <one bullet per finding from RESEARCH.md §3.N "Observed brittleness">. Each bullet:
      - opens with the citation in compact form: `` `dydx-delivery/skills/.../SKILL.md:NN-MM` ``
      - states the observation in present tense
      - ends with a bold severity tag: `**[BLOCKING]**` / `**[STRUCTURAL]**` / `**[COSMETIC]**` (use [NEW] prefix only if the finding is genuinely new beyond CONCERNS.md — most are not)

    **What's missing for v2:**
    - <one or two bullets naming the gap as observed today, in present tense>. Do NOT propose how v2 should solve it (D-13 forbids design moves).
    - **Closes via:** DESIGN-NN (one-line description); DESIGN-MM (one-line description). [from RESEARCH.md §10]
    - **Pitfall ref:** CRIT-X / MOD-Y (one-line description). [from RESEARCH.md §9, when applicable]

    ---
    ```

    **Source data per skill** (transcribe verbatim from RESEARCH.md §3 — do NOT re-discover from SKILL.md files):

    - **§ 1.1 discovery-intake**: Hand-off carrier `<Client>/build-specs/<platform>/00_discovery_vN.md`; gating `status: draft`; next-skill `generate-sow`. Brittleness: missing `hub.md` reference (`SKILL.md:27-28` [STRUCTURAL]); status lifecycle has no follow-up word documented (`SKILL.md:98-107` [STRUCTURAL]); template self-labels "Stage 0" while file-prefix is `00_` (`intake-template.md:13` [STRUCTURAL]). What's missing for v2: kickoff input branch, skip-stage on draft-SOW path, consume `01_kickoff_v*`, `frontmatter_version`. **Closes via:** DESIGN-17, DESIGN-18, DESIGN-01. **Pitfall ref:** CRIT-6.

    - **§ 1.2 generate-sow**: Hand-off carrier `01_sow_vN.md`; gating `status: draft → client_review → approved` (the ONLY skill that documents `client_review` — `SKILL.md:93`). Brittleness: 1st of 6 start-at-any-point copies (`SKILL.md:27-33` [STRUCTURAL]); `sow-template.md` is the sole template without a "Stage N" label ([STRUCTURAL]). What's missing for v2: single-spec-covers-platform-AND-integration capability; status locked to canonical scheme. **Closes via:** DESIGN-19, DESIGN-01. **Pitfall ref:** CRIT-6 (frontmatter — note: `client_review` MUST be retained).

    - **§ 1.3 generate-functional-spec**: Hand-off `02_functional-spec_vN.md`; gating `draft → approved` (NO client_review). Brittleness: 2nd of 6 start-at-any-point copies (`SKILL.md:28-32` [STRUCTURAL]); template self-labels "Stage 2" but coincidental with `02_` prefix (`functional-spec-template.md:13` [STRUCTURAL]); status jumps draft→approved skipping client_review used by sibling generate-sow (`SKILL.md:74-83` [STRUCTURAL]). What's missing for v2: split into 4a + 4b; per-requirement `delivery: native-ai|api` tagging. **Closes via:** DESIGN-20.

    - **§ 1.4 generate-technical-spec**: Hand-off `03_technical-spec_vN.md`; gating `draft → approved`. Brittleness: references missing `platform-pipefy` / `platform-wrike` skills (`SKILL.md:38-39` **[BLOCKING]**); 3rd of 6 start-at-any-point copies (`SKILL.md:28-32` [STRUCTURAL]); `based_on_functional_spec` (underscore key) vs filename `02_functional-spec` (hyphen) (`SKILL.md:99-108` [STRUCTURAL]); tier-claim text "Pipefy Business+, Wrike Pinnacle" without `tier_claims_last_verified:` mechanism (`SKILL.md:90` [STRUCTURAL]). What's missing for v2: scope-gate against fnspec-integration existence; platform-API addendum on 4a; explicit retries/idempotency for API portions. **Closes via:** DESIGN-21, DESIGN-14, DESIGN-15, DESIGN-16, DESIGN-01. **Pitfall ref:** MOD-7 (tier-claim currency); CRIT-6.

    - **§ 1.5 generate-test-plan**: Hand-off `<Client>/testing/<feature>/test-plan_vN.md`; gating `draft → approved`. Brittleness: 4th of 6 start-at-any-point copies (`SKILL.md:30-34` [STRUCTURAL]); template self-labels "Stage 5" but file has no numeric prefix (`test-plan-template.md:17` [STRUCTURAL]); sandbox block carries `pipe_id`+`space_id` unconditionally regardless of `platform:` (`SKILL.md:91-105` and `test-plan-template.md:9-13` [STRUCTURAL]); hard-rules block duplicated inline at `test-plan-template.md:36-44` (1 of 4 — see AUDIT-05 [STRUCTURAL]). What's missing for v2: path moves to `<Client> Brain/test-bot/test_cases/`; coexistence with new `provision-test-harness`. **Closes via:** DESIGN-24, DESIGN-01, DESIGN-02, DESIGN-03. **Pitfall ref:** CRIT-5 (sandbox-allowlist gap on Coda).

    - **§ 1.6 generate-build-prompt**: Hand-off `04_build-prompt_vN.md`; gating `draft (no follow-up status documented)`. Brittleness: references missing `platform-pipefy`/`platform-wrike` (`SKILL.md:47` **[BLOCKING]**); 5th of 6 start-at-any-point copies (`SKILL.md:28-32` [STRUCTURAL]); Cowork-vs-Claude-Code positioning at `SKILL.md:8` and `SKILL.md:163-171` (3rd of 3 copies — see AUDIT-05 [STRUCTURAL]); sibling-prompt convention `04a/04b` mentioned only in `SKILL.md:43`, not in template/README (`dydx-delivery/README.md:60-72` [STRUCTURAL]); `04_build-prompt_v<N>_deviations.md` referenced only in `build-prompt-template.md:129`, not elsewhere ([STRUCTURAL]); `references/when-to-open-claude-code.md:88` references missing `.env.example` ([STRUCTURAL]). What's missing for v2: becomes 7a + 7b dual prompt with per-platform shape; consume `delivery: native-ai|api` tag from 4a. **Closes via:** DESIGN-23, DESIGN-01, DESIGN-12.

    - **§ 1.7 execute-tests**: Hand-off carrier `<Client>/testing/<feature>/results-YYYY-MM-DD_vN.md`; gating "(terminal stage)". Brittleness: hardcoded `runner: dydx-delivery/execute-tests v0.1.0` while plugin is at 0.3.0 (`results-template.md:9` [STRUCTURAL]); `results-template.md` has NO `status:` field — divergent from every other artefact template (`results-template.md` frontmatter [STRUCTURAL]); references missing `platform-pipefy`/`platform-wrike` (`SKILL.md:55` **[BLOCKING]**); 6th of 6 start-at-any-point copies (`SKILL.md:40-44` [STRUCTURAL]); hard-rules block inlined at `SKILL.md:21-31` (1 of 4 — see AUDIT-05 [STRUCTURAL]); sandbox-allowlist schema covers `pipe_id`/`space_id`/`tenant` only — Coda doc IDs not covered (CRIT-5 gap [STRUCTURAL]); `safety-rules.md:93` "Parallel execution is not supported in v1" without versioning anchor [STRUCTURAL]; `results-template.md:18` self-labels "Stage 6" — same label as `build-prompt-template.md:15` (collision) [STRUCTURAL]. What's missing for v2: sandbox-allowlist extension to Coda; `harness_drift` failure class; test-case lifecycle states; `sandbox_lock.yaml` for concurrency. **Closes via:** DESIGN-24, DESIGN-01, DESIGN-02, DESIGN-04. **Pitfall ref:** CRIT-5; CRIT-6.

    Each subsection ends with `---` separator. Do NOT write `**Closes via:**` text that interprets the DESIGN-* requirement — name the ID and a short parenthetical only (per D-13 + PATTERNS.md Pattern 5).

    **Implementing decisions:** D-12 (subsection shape), D-13 (observation-led), D-14 (citation form), D-15 (severity tags), D-08 (CONCERNS citations preserved).
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### 1\.[1-7] ' .planning/AUDIT.md)" = "7" ] &amp;&amp; [ "$(awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*Hand-off contract:\*\*')" = "7" ] &amp;&amp; [ "$(awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*Observed brittleness:\*\*')" = "7" ] &amp;&amp; [ "$(awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*What.s missing for v2:\*\*')" = "7" ] &amp;&amp; awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -qE '\*\*\[BLOCKING\]\*\*' &amp;&amp; awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -qE '\*\*Closes via:\*\* DESIGN-' &amp;&amp; echo SUBSECTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 7 H3 subsections under AUDIT-01: `grep -cE '^### 1\.[1-7] ' .planning/AUDIT.md` returns `7`
    - Each subsection has the three required mini-headers (Hand-off contract / Observed brittleness / What's missing for v2): `awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*Hand-off contract:\*\*'` returns `7`, same for `Observed brittleness` and `What's missing for v2`
    - At least 3 `**[BLOCKING]**` tags in AUDIT-01 (the three skills that reference missing `platform-pipefy`/`platform-wrike`): `awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*\[BLOCKING\]\*\*'` returns >= `3`
    - At least 20 `**[STRUCTURAL]**` tags total (one per brittleness bullet across 7 skills): `awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*\[STRUCTURAL\]\*\*'` returns >= `20`
    - Every subsection contains a `**Closes via:** DESIGN-` trailer (7 minimum): `awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -c '\*\*Closes via:\*\* DESIGN-'` returns >= `7`
    - Compact-form citations present (D-14): `awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -cE '\`dydx-delivery/skills/[^[:space:]]*\.md:[0-9]+'` returns >= `15` (5+ citations per skill on average across 7 skills)
    - Section AUDIT-01 contains NO design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-01:/,/^## AUDIT-02:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0` (D-13 enforcement — observation-led only; pattern matches the canonical set used by every plan's gate)
    - Structural-check script's "8 sections" assertion still passes (no accidental H2 added): `[ "$(grep -cE '^## AUDIT-0[1-8]:' .planning/AUDIT.md)" = "8" ]`
  </acceptance_criteria>
  <done>
    AUDIT-01 has matrix + 7 subsections; every brittleness finding cited; every "What's missing for v2" closes via DESIGN-*; no design-move proposals; structural-check still functions correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH.md §3 → AUDIT-01 prose | The executor transcribes pre-built findings; if the executor "interprets" rather than transcribes, citations drift and observations slide into design proposals. |
| Skill SKILL.md files → cited line numbers | Citations must resolve. Line drift breaks reviewer navigation. |
| RESEARCH.md DESIGN-* index → "Closes via:" trailers | Wrong DESIGN-* mapping mis-aligns the audit with Phase 2 design scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Executor invents brittleness not in RESEARCH.md / CONCERNS.md | mitigate | Acceptance criteria check that severity-tag counts are within expected ranges (>= 3 BLOCKING, >= 20 STRUCTURAL); manual review confirms each bullet maps to a RESEARCH.md §3 line. |
| T-01-02-02 | Repudiation | Executor proposes v2 design moves under "What's missing for v2" (violates D-13) | mitigate | Acceptance criterion `grep -ciE 'we should\|the design will\|recommend that v2\|propose\|v2 will' = 0` (canonical D-13 banned-phrase ERE) blocks the most common phrasings across all content plans. |
| T-01-02-03 | Information Disclosure | Citation drift — `file:line` points at wrong line | accept (manual review catches) | VALIDATION.md Manual-Only Verification table row "Citations resolve" mandates reviewer spot-check 5 random citations. Structural check cannot enforce this. |
| T-01-02-04 | Tampering | Wrong DESIGN-* in "Closes via:" trailer | mitigate | RESEARCH.md §10 (lines 480-509) is the canonical mapping; executor MUST read it (in `<read_first>`). Acceptance criterion checks each subsection has a `**Closes via:** DESIGN-` trailer. |
</threat_model>

<verification>
End of Plan 02 success-state:
1. AUDIT-01 section is fully populated: matrix + 7 H3 subsections + DESIGN-* closures.
2. `grep -cE '^### 1\.[1-7] ' .planning/AUDIT.md` returns `7`.
3. Severity tags appear: `[BLOCKING]` >= 3 times, `[STRUCTURAL]` >= 20 times.
4. No design-move language in AUDIT-01 (canonical D-13 ERE returns 0).
5. `bash .planning/phases/01-audit/scripts/audit-structure-check.sh` still exits 1 (intended — Waves 3-9 haven't landed).
6. `grep -cE '^## AUDIT-0[1-8]:' .planning/AUDIT.md` still returns `8` (no accidental H2 anchors added).
</verification>

<success_criteria>
- [ ] AUDIT-01 matrix has 7 data rows (one per skill in pipeline order)
- [ ] AUDIT-01 has 7 H3 subsections (`### 1.1` through `### 1.7`)
- [ ] Every subsection has the three mini-headers (Hand-off contract, Observed brittleness, What's missing for v2)
- [ ] Every brittleness bullet has compact-form `file:line` citation + severity tag
- [ ] Every "What's missing for v2" has a `**Closes via:** DESIGN-NN` trailer
- [ ] No design-move language present (D-13 enforced via canonical banned-phrase ERE)
- [ ] No content under `dydx-delivery/` modified
- [ ] Structural-check script still asserts 8 sections correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-02-SUMMARY.md` documenting:
- AUDIT-01 row count, subsection count
- Severity-tag totals (BLOCKING / STRUCTURAL / COSMETIC)
- Number of DESIGN-* IDs cross-referenced
- Pointer: "Plan 03 (Wave 3) populates AUDIT-03 next"
</output>
