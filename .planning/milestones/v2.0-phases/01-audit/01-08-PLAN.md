---
phase: 01-audit
plan: 08
type: execute
wave: 8
depends_on: ["01-audit/07"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-08]
tags: [audit, mcp-probe, design-only]

must_haves:
  truths:
    - "AUDIT-08 records a live-MCP probe table (one row per MCP) with status / server identity / version pin / probe call / result for the 5 wired-and-connected MCPs (Coda, Miro, Google Drive, Gmail, Google Calendar) plus a [NEW] Slack row marked 'wired but unauthenticated'."
    - "Each probe call is the cheap-read endpoint per D-04 (Coda: listDocs; Miro: listBoards; Drive: searchFiles empty query; Gmail: listLabels; Calendar: listCalendars)."
    - "Each probe call ACTUALLY EXECUTES against the live MCP in the running session (not hallucinated) and the result is recorded with response status."
    - "Pipefy / Wrike / Ziflow APIs and Claude in Chrome are recorded as 'verification deferred' per D-06 — no probe attempts."
    - "Probe-time timestamp recorded at the top of the table (per CONTEXT.md `<specifics>`)."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-08 section populated with live MCP probe table + verification-deferred sub-table."
      contains: "## AUDIT-08: Live MCP Wiring Probe; Coda; Miro; Google Drive; Gmail; Google Calendar; Slack; verification deferred"
  key_links:
    - from: "AUDIT-08 probe table rows"
      to: "Live MCP tool calls executed during this plan's task"
      via: "Each row's 'Probe call' + 'Result' columns reflect actual MCP tool invocations"
      pattern: "✓ Connected|200 OK|listDocs|listBoards|searchFiles|listLabels|listCalendars"
    - from: "AUDIT-08 verification-deferred row"
      to: "Phase 4 OPEN-01 / OPEN-02 (open questions register)"
      via: "Cross-ref pointer per D-06"
      pattern: "OPEN-(01|02)|CHANGE-04"
---

<objective>
Populate AUDIT-08 with a live-MCP probe table that records actual probe-call outcomes for the 5 wired MCPs in this workspace (Coda, Miro, Google Drive, Gmail, Google Calendar), plus a [NEW] Slack row (wired-but-unauthenticated), plus a verification-deferred row for the not-wired connectors (Pipefy/Wrike/Ziflow APIs + Claude in Chrome). Each probe call is **actually executed live** during this task — results are real, not hallucinated.

**Purpose:** AUDIT-08 satisfies ROADMAP success criterion 3 (live-wiring probe table — present-and-working / present-but-broken / missing). RESEARCH.md §4 confirmed all 5 MCPs are Connected via `claude mcp list`; this plan calls one cheap-read endpoint per MCP and records the response code so the table has both connectivity confirmation AND endpoint-response confirmation per RESEARCH.md §12 Open Q1 recommendation.

**Output:** AUDIT-08 section of `.planning/AUDIT.md` populated; structural-check still exits 1 because Plan 09 (the absorption claim assertion #4 + Appendix B row count assertion #7) hasn't landed yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- AUDIT-08 input data is partly pre-built in RESEARCH.md §4 (lines 199-242). -->
<!-- However, the probe-call results column requires LIVE EXECUTION during this task. -->
<!-- The executor MUST call each MCP's cheap-read endpoint and record the actual response. -->

The 5 wired-and-connected MCPs to probe (per RESEARCH.md §4 + D-04):

| MCP | Endpoint | Cheap-read probe call | Expected status |
|-----|----------|------------------------|------------------|
| Coda | `https://coda.io/apis/mcp` | `listDocs` (read-only — returns user's accessible docs) | 200 OK |
| Miro | `https://mcp.miro.com` | `listBoards` (read-only — returns user's boards) | 200 OK |
| Google Drive | `https://drivemcp.googleapis.com/mcp/v1` | `searchFiles` with empty query | 200 OK |
| Gmail | `https://gmailmcp.googleapis.com/mcp/v1` | `listLabels` | 200 OK |
| Google Calendar | `https://calendarmcp.googleapis.com/mcp/v1` | `listCalendars` | 200 OK |

The Slack [NEW] row (per RESEARCH.md §4 [NEW] finding):

| MCP | Endpoint | Status | Why |
|-----|----------|--------|-----|
| Slack | `https://mcp.slack.com/mcp` | wired but unauthenticated | Wired but `! Needs authentication` per `claude mcp list` 2026-05-09 |

The verification-deferred row (per D-06):

| Connector | Why not probed |
|-----------|---------------|
| Pipefy API (GraphQL) | No MCP wired; sandbox credentials unavailable in design phase |
| Wrike API (REST) | Same |
| Ziflow API (REST) | Same |
| Claude in Chrome | Canonical product naming uncertain in 2026 — Phase 4 OPEN-01 |

Probe execution: The executor uses Claude Code's MCP tool invocations directly (e.g. `mcp__plugin_claude_ai_Coda__listDocs`, `mcp__plugin_claude_ai_Miro__listBoards`, etc.). Tool invocations may fail with auth errors or rate limits — record the actual outcome.

**Important caveat:** If a probe call fails (auth error, network error, MCP not actually exposed despite `Connected` status), record the failure honestly — do NOT fabricate `200 OK`. The whole point of AUDIT-08 is empirical truth (per VALIDATION.md Manual-Only verification: "MCP probe results are honest").

Probe-time timestamp at the top of the table (per CONTEXT.md `<specifics>` line 133): "Live-MCP probe results are timestamped (the probe is a snapshot — re-probing later may differ)."

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cheap-read probe against each of the 5 wired MCPs and record results</name>
  <files>(no files modified — probe execution + result capture only)</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §4 (lines 199-242) — MCP probe matrix with endpoint + recommended cheap-read call
    - .planning/phases/01-audit/01-CONTEXT.md D-04, D-05, D-06, D-07 — probe methodology + recording schema + out-of-scope rules
    - .planning/REQUIREMENTS.md AUDIT-08 (line 25) — exact requirement
  </read_first>
  <action>
    Execute the probe calls live in this Claude Code session. For each of the 5 MCPs, invoke the documented cheap-read tool and capture the outcome.

    **Step 1 — Get the current MCP wiring state for the timestamp + server identity column.**
    Run `claude mcp list` (or use the `Bash` tool to invoke it) and capture the output as the canonical list of wired servers. Record the timestamp at the moment of probe.

    **Step 2 — For each of the 5 wired MCPs, invoke its cheap-read tool and capture status + response shape:**

    a) **Coda** — call the Coda MCP's "list docs" or equivalent read endpoint (via the actual MCP tool name in the running session — e.g. `mcp__plugin_claude_ai_Coda__listDocs` if that's the exposed tool). Capture: tool returned 200 OK / docs count / error.

    b) **Miro** — call the Miro MCP's "list boards" or equivalent. Capture: 200 OK / boards count / error.

    c) **Google Drive** — call `searchFiles` with empty query (or equivalent). Capture: 200 OK / files count / error.

    d) **Gmail** — call `listLabels` (or equivalent). Capture: 200 OK / labels count / error.

    e) **Google Calendar** — call `listCalendars` (or equivalent). Capture: 200 OK / calendars count / error.

    **Step 3 — Capture the actual MCP tool name + endpoint for each (per D-05 "server identity" column).** Inspect the tool registry / `claude mcp list` output to record the canonical tool prefix.

    **Step 4 — Persist the probe outcomes in a temporary scratchpad (no files written yet — Task 2 writes to AUDIT.md).** Note for each row:
    - Server identity (e.g. `mcp__plugin_claude_ai_Coda__*`)
    - Endpoint URL
    - Probe call exact name
    - Response: status code OR error class
    - Response shape: count of items returned (e.g. "3 docs returned") OR error message

    **CRITICAL HONESTY RULE:** If a probe call fails — whether due to auth, network, MCP being actually unavailable despite `Connected` status, or any other reason — record the failure exactly. Do NOT fabricate success. AUDIT-08's value is empirical; a hallucinated probe result poisons the entire audit.

    **If `claude mcp list` is unavailable in this session** (Bash tool blocked, mcp commands not exposed): proceed with whatever tool-discovery mechanism IS available (e.g. examining `mcp__*` tool prefixes in the session); otherwise mark each row's status as "tool-discovery-failed" with an honest explanation.

    **Implementing decisions:** D-04 (cheap-read endpoint per MCP), D-05 (recording schema), D-07 (workspace state at probe time).
  </action>
  <verify>
    <automated>echo "PROBE_RESULTS_CAPTURED (verification done by Task 2 writing the table)"</automated>
  </verify>
  <acceptance_criteria>
    - Each of 5 MCPs has a captured outcome (success or honest failure) for its cheap-read probe.
    - Server identity / tool prefix recorded for each.
    - Probe timestamp recorded.
    - No fabricated results — if a probe failed, the failure is recorded as the result.
  </acceptance_criteria>
  <done>
    Probe outcomes captured (in scratchpad/working memory) ready for transcription into AUDIT-08 in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT-08 section opener + probe table + Slack [NEW] row + verification-deferred sub-table</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §4 (lines 199-242) — MCP probe matrix template
    - .planning/phases/01-audit/01-CONTEXT.md D-04, D-05, D-06, D-07 + `<specifics>` (line 133, probe timestamp at top of table)
    - .planning/research/PITFALLS.md — for the Coda CRIT-1/CRIT-2/CRIT-3/CRIT-9 cross-references per RESEARCH.md §9
    - The probe outcomes captured in Task 1
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-08: Live MCP Wiring Probe` in `.planning/AUDIT.md`.

    **Section opener** (1 paragraph, 3 sentences): "Records the live MCP probe state in this Claude Code workspace at probe time. Probe methodology per D-04 / D-05: one cheap-read endpoint per MCP; outcome captured (status / response code / item count). Probe results are timestamped — re-probing later may differ as MCP wiring evolves."

    **Probe timestamp line** (per CONTEXT.md `<specifics>`):
    ```
    **Probe time:** <ISO8601 timestamp from Task 1>; **Workspace:** dydx-project-workflow (branch `dydx-delivery-v2`); **Operator:** Claude Code CLI session.
    ```

    **Sub-section 1: 5 wired-and-connected MCPs** (header: `**Wired and connected — the 5 canonical MCPs**`).

    Write a table with these EXACT columns:

    ```
    | MCP | Server identity (per D-05) | Endpoint | Probe call (per D-04) | Probed status | Result | Version pin |
    |---|---|---|---|---|---|---|
    ```

    Then 5 data rows — one per MCP. Cell content sourced from Task 1's captured probe outcomes. The `Probed status` column uses the D-04 vocabulary (`working` / `present-but-broken` / `missing`); the `Result` column carries the actual response code + item count or error class. Reference RESEARCH.md §4 lines 215-221 for the canonical row shape.

    **Sub-section 2: [NEW] wired-but-out-of-scope row** (header: `**Wired but out-of-scope per D-06**`).

    1-row table:

    ```
    | MCP | Status | Why out-of-scope |
    |---|---|---|
    | **Slack** | wired, unauthenticated — `! Needs authentication` | Wired but unauthenticated. Slack is referenced in v0.3.0 only as a notification target inside generated artefacts (e.g. `#ops-alerts` in `dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md:119`); not a delivery-pipeline MCP. **[NEW]** **[STRUCTURAL]** — flagged to Phase 4 OPEN-QUESTIONS. |
    ```

    **Sub-section 3: NOT wired — verification deferred** (header: `**Not wired — verification deferred per D-06**`).

    Write a 4-row table:

    ```
    | Connector | Why not probed | Where it surfaces in v0.3.0 | Verification path |
    |---|---|---|---|
    | **Pipefy API** (GraphQL) | No MCP for Pipefy in this workspace; sandbox-tenant credentials unavailable in design phase | `dydx-delivery/skills/execute-tests/references/safety-rules.md:75`; `platform: pipefy` route in `dydx-delivery/skills/discovery-intake/SKILL.md:86`, `dydx-delivery/skills/generate-technical-spec/SKILL.md:38`, etc. | "verification deferred" per D-06; v2.1+ build phase per CHANGE-04 |
    | **Wrike API** (REST) | Same — no MCP, no creds | `dydx-delivery/skills/execute-tests/references/safety-rules.md:76`; `platform: wrike` routes in `dydx-delivery/skills/generate-technical-spec/SKILL.md:39` | "verification deferred" per D-06 |
    | **Ziflow API** (REST) | Same | `dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md:113` (`POST /api/v2/projects`) | "verification deferred" per D-06 |
    | **Claude in Chrome** | Canonical product naming uncertain in 2026 | Referenced in `.planning/PROJECT.md` "Connectors expected to be wired"; not in repo skill files | Phase 4 OPEN-01 per `.planning/REQUIREMENTS.md` |
    ```

    **Sub-section 4: PITFALLS cross-reference for Coda (since Coda is hard-dependency for v2 Stage 6/11)** — bold-keyed paragraph, 1 sentence:
    ```
    **Coda integration risk surface (PITFALLS cross-ref):** Coda MCP is wired and connected, but using it correctly for v2 Stage 6 / Stage 11 is gated by **CRIT-1** (formula column overwrite), **CRIT-2** (async-202), **CRIT-3** (rate-limit), and **CRIT-9** (token scope). These pitfalls are research-input to DESIGN-22 (Stage 6 cost estimate) and DESIGN-27 (Stage 11 sign-off + brain mirror).
    ```

    End with `---` separator.

    **Implementing decisions:** D-04 (probe call per MCP), D-05 (table schema), D-06 (verification deferred for non-MCP connectors), D-07 (workspace state at probe time), D-10 ([NEW] tag for Slack), D-15 (severity tags).
  </action>
  <verify>
    <automated>awk '/^## AUDIT-08:/,/^---$/' .planning/AUDIT.md | grep -F '| MCP | Server identity (per D-05) | Endpoint | Probe call (per D-04) | Probed status | Result | Version pin |' &amp;&amp; [ "$(awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -cE '^\| (Coda|Miro|Google Drive|Gmail|Google Calendar) \|')" = "5" ] &amp;&amp; awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -q 'Slack' &amp;&amp; awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -q 'verification deferred' &amp;&amp; awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -q 'Probe time' &amp;&amp; echo PROBE_TABLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-08 contains the 7-column probe table header: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -F '| MCP | Server identity (per D-05) | Endpoint | Probe call (per D-04) | Probed status | Result | Version pin |'` succeeds
    - All 5 MCPs have rows: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -cE '^\| \*?\*?(Coda|Miro|Google Drive|Gmail|Google Calendar)\*?\*? \|'` >= `5`
    - Slack [NEW] row present: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -E 'Slack.*wired.*unauthenticated|Slack.*\\[NEW\\]'` succeeds
    - Verification-deferred sub-table contains all 4 deferred connectors: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -cE 'Pipefy API|Wrike API|Ziflow API|Claude in Chrome'` >= `4`
    - Probe-time timestamp present: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -q 'Probe time'` succeeds
    - PITFALLS cross-ref for Coda present: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -cE 'CRIT-(1|2|3|9)'` >= `4`
    - "verification deferred" phrase present (per D-06): `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -c 'verification deferred'` >= `2`
    - 8-H2-section count holds: `grep -cE '^## AUDIT-0[1-8]:' .planning/AUDIT.md` returns `8`
    - No design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-08:/,/^## Appendix A:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`. (Note: "PITFALLS cross-ref" + "research-input to DESIGN-22/DESIGN-27" is observation-led pointing at requirements, not a design move; "Coda is hard-dependency for v2 Stage 6/11" — phrase "hard-dependency for v2" does not match the banned ERE since `v2 will` requires literal contiguous tokens.)
  </acceptance_criteria>
  <done>
    AUDIT-08 section populated with 5-MCP live probe table + Slack [NEW] row + verification-deferred sub-table + PITFALLS cross-ref; results are honest (not fabricated); 8-section count holds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live MCP tool calls → AUDIT-08 probe-result column | This is the most fabrication-prone area of the audit. Executor MUST call the actual tools and record actual outcomes. RESEARCH.md says "all 5 MCPs Connected via `claude mcp list`" but does NOT pre-populate per-endpoint response codes. |
| `claude mcp list` output → server identity column | If executor invents server prefixes (e.g. claims `mcp__plugin_claude_ai_Coda__listDocs` exists when the actual tool is something else), reviewer manual verification (VALIDATION.md "MCP probe results are honest") will catch the divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Tampering / Repudiation | Probe results fabricated (most-prone phase risk) | mitigate | Task 1 separates probe execution from result capture; Task 2 transcribes from captured outcomes only. VALIDATION.md Manual-Only Verification table mandates reviewer re-runs at least 3 probe calls and verifies status matches AUDIT-08. |
| T-01-08-02 | Information Disclosure | Slack [NEW] row not flagged (silent drop) | mitigate | Acceptance criterion mandates Slack row exists with `wired.*unauthenticated` text. RESEARCH.md §4 [NEW] finding (line 240-242) documents the policy. |
| T-01-08-03 | Tampering | Out-of-scope connectors (Pipefy/Wrike/Ziflow/Claude in Chrome) probed against expectations | mitigate | D-06 explicitly forbids probing these. Acceptance criterion verifies the 4-row deferred-connector sub-table is present and uses "verification deferred" phrase >= 2 times. |
| T-01-08-04 | Information Disclosure | Probe-time timestamp omitted (loses snapshot semantics) | mitigate | Acceptance criterion mandates `Probe time` literal appears in AUDIT-08. |
| T-01-08-05 | Denial of Service | MCP probe calls hit rate limits / consume tokens | accept | Each probe is one cheap-read call per MCP × 5 MCPs = 5 calls total; well within quotas. |
| T-01-08-06 | Repudiation | Section drifts into design proposal (violates D-13) | mitigate | Acceptance criterion runs canonical D-13 banned-phrase ERE `grep -ciE 'we should\|the design will\|recommend that v2\|propose\|v2 will' = 0` — same pattern enforced in every content plan. |
</threat_model>

<verification>
End of Plan 08 success-state:
1. AUDIT-08 has section opener + probe-time timestamp + 5-row probe table + Slack [NEW] sub-table + 4-row verification-deferred sub-table + PITFALLS cross-ref paragraph.
2. All 5 wired MCPs (Coda, Miro, Google Drive, Gmail, Google Calendar) have rows with actual probe outcomes.
3. Probe results are honest (not fabricated) — Task 1 actually executed the probes.
4. Slack flagged [NEW] [STRUCTURAL].
5. Pipefy/Wrike/Ziflow/Claude-in-Chrome marked "verification deferred" per D-06.
6. CRIT-1, CRIT-2, CRIT-3, CRIT-9 cross-referenced for Coda.
7. 8-H2-section count holds; structural-check still exits 1 because Plan 09's absorption claim and Appendix B row count haven't landed.
</verification>

<success_criteria>
- [ ] AUDIT-08 has live probe table (5 wired MCPs + Slack + 4 deferred)
- [ ] Probe-time timestamp recorded at top of section
- [ ] Probe results are honest (Task 1 ran live; Task 2 transcribed)
- [ ] Slack [NEW] row present
- [ ] verification deferred sub-table covers Pipefy/Wrike/Ziflow/Claude in Chrome
- [ ] Coda CRIT-* cross-refs present
- [ ] No design-move language (canonical D-13 banned-phrase ERE)
- [ ] No content under `dydx-delivery/` modified
- [ ] 8-H2-section count holds
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-08-SUMMARY.md` documenting:
- Probe-time timestamp
- 5 MCP probe outcomes (status / response code / item count per MCP)
- Slack [NEW] finding noted
- Pointer: "Plan 09 (Wave 9) does the synthesis: AUDIT-02 + Appendix B trace + Executive Summary + Preamble + Coverage doc"
</output>
