---
phase: 01-audit
plan: 03
type: execute
wave: 3
depends_on: ["01-audit/02"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-03]
tags: [audit, connector-dependencies, design-only]

must_haves:
  truths:
    - "AUDIT-03 section catalogues per-stage connector dependencies for all 7 v0.3.0 stages and all 7 referenced connectors (Miro, Coda, Drive, Gmail, Calendar, Claude in Chrome, Pipefy/Wrike/Ziflow APIs)."
    - "AUDIT-03 contains an inventory table marking each connector required vs graceful-degradation per stage (observation-only; design contract owned by DESIGN-07)."
    - "AUDIT-03 cites PITFALLS.md graceful-degradation matrix as research-derived input, with explicit pointer 'v2 design contract = DESIGN-07'."
    - "AUDIT-03 is observation-led — does not propose Phase 2 design moves (per D-13)."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-03 section populated with per-stage connector dependency table + research-cited graceful-degradation hint."
      contains: "## AUDIT-03: Per-Stage Connector Dependencies; required; graceful-degradation; DESIGN-07; PITFALLS"
  key_links:
    - from: "AUDIT-03 dependency table"
      to: ".planning/codebase/INTEGRATIONS.md (per-stage connector usage)"
      via: "stage row × connector column matrix"
      pattern: "discovery-intake.*generate-sow.*generate-functional-spec"
    - from: "AUDIT-03 fallback hint sub-table"
      to: ".planning/research/PITFALLS.md connector-availability fallback matrix + DESIGN-07"
      via: "**Research-derived; v2 design contract = DESIGN-07** trailer"
      pattern: "DESIGN-07"
---

<objective>
Populate AUDIT-03 with a per-stage connector dependency table observing which connectors each v0.3.0 stage uses and which are required vs graceful-degradation. Plus a research-cited fallback hint sub-table sourced from PITFALLS.md, with the explicit "v2 design contract = DESIGN-07" pointer (not a design proposal).

**Purpose:** AUDIT-03 anchors ROADMAP success criterion 3 (per-stage connector dependency table). Per CONTEXT.md deferred-ideas, the actual graceful-degradation contract is DESIGN-07 territory; AUDIT-03's role is observation only — list what each stage uses, mark required-vs-fallback as observed today, point at DESIGN-07 for the design lock.

**Output:** AUDIT-03 section of `.planning/AUDIT.md` populated with two tables (stage × connector matrix + fallback-hint pointer); structural-check still exits 1 (other waves outstanding).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Source data for AUDIT-03: -->
<!-- (a) INTEGRATIONS.md §"Stage Pipeline Wiring" (lines 175-197) names which connector each stage actually touches in v0.3.0. -->
<!-- (b) INTEGRATIONS.md per-platform sections (Pipefy/Wrike/Ziflow/Slack) name which constructs each connector exposes. -->
<!-- (c) PITFALLS.md "Connector-availability fallback matrix" provides the v2-design-derived graceful-degradation labels (research-derived; cited by reference, not transcribed verbatim — DESIGN-07 territory). -->
<!-- (d) Research §12 Open Q4 confirms planner assumption: option (c) — observation inventory PLUS PITFALLS-cited fallback hint, with explicit DESIGN-07 pointer. -->

The 7 v0.3.0 stages (per pipeline order, INTEGRATIONS.md §"Stage Pipeline Wiring"):

1. discovery-intake (input: free-form / Miro paste / notes)
2. generate-sow
3. generate-functional-spec
4. generate-technical-spec
5. generate-test-plan
6. generate-build-prompt
7. execute-tests

The connectors referenced in v0.3.0 (per CONTEXT.md AUDIT-03 spec):
- **Miro** (workflow map paste / Miro MCP)
- **Coda** (brain mirror, task tables — referenced by Stage 11 in v2 architecture; in v0.3.0 NOT used by any stage)
- **Google Drive** (documentation publishing — Stage 9 in v2; in v0.3.0 NOT used)
- **Gmail** (notification surface — referenced abstractly in templates; not used directly)
- **Google Calendar** (scheduling — referenced abstractly; not used directly)
- **Claude in Chrome** (referenced in PROJECT.md "Connectors expected to be wired"; not in skill files)
- **Pipefy API** (GraphQL — used by `execute-tests` against sandbox; referenced via `platform:` dispatch)
- **Wrike API** (REST — same)
- **Ziflow API** (REST — referenced in `technical-spec-template.md:113`)

INTEGRATIONS.md says NO MCP servers are defined in this repo (`.mcp.json` absent); "MCP" in v0.3.0 docs is forward-looking. v0.3.0 stages mostly DO NOT use connectors directly — pipeline is artefact-driven (filesystem markdown). The only stage that hits an external API is `execute-tests` (Pipefy/Wrike/Ziflow sandbox calls). Generated artefacts REFERENCE connectors but the skills themselves don't invoke them.

This makes AUDIT-03 mostly negative: most cells are "(none — artefact-driven)". The Required/Graceful-degradation column applies meaningfully ONLY to:
- `discovery-intake`: optional Miro paste-in (graceful — works without Miro by typed notes)
- `execute-tests`: Pipefy/Wrike/Ziflow API access against sandbox (REQUIRED for the runner; no fallback)

This is a key finding for AUDIT-03: the stark observation is that v0.3.0's connector surface is minimal; v2 introduces Coda/Drive/Gmail dependencies. State this explicitly in the section opener.

Cross-ref index per RESEARCH.md §12 Open Q4 — option (c): provide BOTH observation table AND PITFALLS-cited fallback hint with `**Research-derived; v2 design contract = DESIGN-07**` trailer.

Compact citation form per D-14: `` `dydx-delivery/skills/discovery-intake/SKILL.md:11-13` ``.

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-03 section opener + per-stage × per-connector dependency table</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/codebase/INTEGRATIONS.md (full file — lines 1-202) — stage-pipeline wiring + per-platform usage
    - .planning/REQUIREMENTS.md AUDIT-03 (line 20) — exact requirement spec
    - .planning/phases/01-audit/01-RESEARCH.md §4 (MCP probe matrix; lines 199-242) — confirms 5 MCPs Connected (Coda/Miro/Drive/Gmail/Calendar) + Slack unauthenticated
    - .planning/phases/01-audit/01-RESEARCH.md §12 Open Q4 (line 575) — confirmed approach: observation table + fallback hint + DESIGN-07 pointer
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 6 — Tables"
    - .planning/phases/01-audit/01-CONTEXT.md D-13 — observation-led, no design moves
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-03: Per-Stage Connector Dependencies` in `.planning/AUDIT.md` with content.

    **Step 1 — Section opener** (1 short paragraph, ~3 sentences):
    State the observation that v0.3.0 is **artefact-driven** (filesystem markdown), so most stages do NOT directly invoke connectors. Generated artefacts reference connectors (e.g. Pipefy GraphQL endpoints in `technical-spec-template.md:112-119`) but the skills themselves do not call them. The exceptions are `discovery-intake` (optional Miro paste) and `execute-tests` (the only skill that actually hits external APIs — against sandbox). State that v2 introduces Coda/Drive/Gmail dependencies (per ROADMAP Phase 2 scope), which is OUT OF SCOPE for AUDIT-03 — captured in DESIGN-07 instead.

    **Step 2 — Per-stage × per-connector dependency table.** Write a markdown table per PATTERNS.md Pattern 6 with these EXACT columns:

    ```
    | Stage | Miro | Coda | Drive | Gmail | Calendar | Claude in Chrome | Pipefy API | Wrike API | Ziflow API |
    |---|---|---|---|---|---|---|---|---|---|
    ```

    Then 7 data rows — one per v0.3.0 stage. Cell values use these exact phrases for clarity:
    - `(none)` — stage doesn't touch this connector
    - `(referenced in artefact only)` — stage's generated output references the connector but the skill doesn't invoke it (e.g. technical-spec mentions Slack `#ops-alerts`)
    - `optional (graceful)` — stage can use the connector but works without it (Miro paste in discovery-intake)
    - `REQUIRED` — stage cannot run without the connector (execute-tests + Pipefy/Wrike/Ziflow per `platform:`)

    Per-stage truth (transcribe from INTEGRATIONS.md):
    - **discovery-intake**: Miro = `optional (graceful)` (per `discovery-intake/SKILL.md:30-50` Miro paste optional). All others = `(none)`.
    - **generate-sow**: All `(none)` — pure artefact transformation reading `00_discovery_v*.md`.
    - **generate-functional-spec**: All `(none)` — pure artefact transformation.
    - **generate-technical-spec**: Pipefy / Wrike / Ziflow API columns = `(referenced in artefact only)` (skill writes API contracts into the spec; doesn't call the APIs). Others = `(none)`.
    - **generate-test-plan**: Pipefy / Wrike API = `(referenced in artefact only)` (test plan describes sandbox endpoints). Others = `(none)`.
    - **generate-build-prompt**: Pipefy / Wrike / Ziflow = `(referenced in artefact only)`. Others = `(none)`.
    - **execute-tests**: Pipefy / Wrike / Ziflow = `REQUIRED` (per `platform:` value — the runner makes actual sandbox API calls; one of the three is required per active platform). Slack = `(referenced in artefact only)` (test channels per `safety-rules.md:41`). Others = `(none)`.

    **Step 3 — One-line citation legend below the table** (so reviewers can navigate):
    ```
    *Citations: Per-stage usage derived from `dydx-delivery/skills/<skill>/SKILL.md` Inputs/Outputs sections; INTEGRATIONS.md §"Stage Pipeline Wiring" (lines 175-197) is the consolidated source.*
    ```

    Do NOT label any connector "graceful-degradation" beyond the discovery-intake row — that label is DESIGN-07 territory and applies ONLY where v0.3.0 actually exhibits the behaviour.

    Finish with `---` separator. Do NOT close the section yet — Task 2 adds the fallback hint sub-table.

    **Implementing decisions:** D-13 (observation-led), D-14 (citation form).
  </action>
  <verify>
    <automated>awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -F '| Stage | Miro | Coda | Drive | Gmail | Calendar | Claude in Chrome | Pipefy API | Wrike API | Ziflow API |' &amp;&amp; [ "$(awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -cE '^\| (discovery-intake|generate-sow|generate-functional-spec|generate-technical-spec|generate-test-plan|generate-build-prompt|execute-tests) \|')" = "7" ] &amp;&amp; awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -q 'REQUIRED' &amp;&amp; awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -q 'optional (graceful)' &amp;&amp; echo TABLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-03 section contains a 10-column table header with the exact column list (Stage + 9 connectors): `awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -F '| Stage | Miro | Coda | Drive | Gmail | Calendar | Claude in Chrome | Pipefy API | Wrike API | Ziflow API |'` succeeds
    - Exactly 7 stage rows (one per v0.3.0 skill): `awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -cE '^\| (discovery-intake|generate-sow|generate-functional-spec|generate-technical-spec|generate-test-plan|generate-build-prompt|execute-tests) \|'` returns `7`
    - At least one cell contains `REQUIRED` (the execute-tests Pipefy/Wrike/Ziflow cells): `awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -c 'REQUIRED'` >= `1`
    - At least one cell contains `optional (graceful)` (discovery-intake Miro): `awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -c 'optional (graceful)'` >= `1`
    - Citation legend present: `awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -q 'INTEGRATIONS.md'` succeeds
    - No design-move language in AUDIT-03 — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-03:/,/^---$/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`
  </acceptance_criteria>
  <done>
    AUDIT-03 has section opener + 7-row × 10-column dependency table + citation legend; observation-only; structural-check still asserts correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PITFALLS-cited fallback hint sub-table + DESIGN-07 pointer</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/research/PITFALLS.md — search for "Connector-availability fallback matrix" or "graceful-degradation"
    - .planning/REQUIREMENTS.md DESIGN-07 (line 39) — exact requirement text for the v2 contract
    - .planning/phases/01-audit/01-RESEARCH.md §12 Open Q4 (line 575) — confirmed approach
    - .planning/phases/01-audit/01-CONTEXT.md "Deferred Ideas" (lines 137-145) — "Per-stage connector graceful-degradation matrix as a contract → DESIGN-07 (Phase 2)"
  </read_first>
  <action>
    Append a sub-section under AUDIT-03 (still under the same H2, after the dependency table's `---`) covering the research-derived fallback hint per CONTEXT.md "Deferred Ideas" + RESEARCH.md §12 Open Q4 option (c).

    Write a `**Sub-section: Research-derived graceful-degradation hints**` paragraph header (bold-keyed paragraph per PATTERNS.md Pattern 2 — NOT an H3, since AUDIT-01 reserves H3 depth for skills). Then:

    1. **One paragraph (2-3 sentences)** stating: "PITFALLS.md (research synthesis, 2026-05-09) drafts a connector-availability fallback matrix for v2 stages that go beyond v0.3.0's surface — Coda → Stage 6 (cost) manual mode if MCP missing; Drive → Stage 9 (documentation) halt if MCP missing; etc. The full matrix is research-derived input to DESIGN-07; AUDIT.md cites it by reference and does NOT lock the contract here."

    2. **Citation block** (small markdown table, 4-5 rows max — the most representative fallbacks; DO NOT transcribe the full PITFALLS matrix):

    ```
    | v2 stage (per Phase 2 scope) | Connector | Research fallback hint | Closes via |
    |---|---|---|---|
    | Stage 6 Cost estimate | Coda MCP | manual mode (`.csv` export + paste-in) when missing | DESIGN-07; DESIGN-22 |
    | Stage 9 Documentation publishing | Google Drive MCP | halt — Stage 9 fails fast; rest of pipeline still runs | DESIGN-07; DESIGN-25 |
    | Stage 10 Native-AI knowledge push | Pipefy/Wrike/Ziflow APIs | copy-paste fallback when direct API missing | DESIGN-07; DESIGN-26 |
    | Stage 1 Kickoff capture | Miro MCP | paste-in fallback (already supported in v0.3.0 discovery-intake) | DESIGN-07; DESIGN-17 |
    ```

    3. **Closing pointer line** (mandatory per RESEARCH.md §12 Open Q4):
    ```
    **Research-derived; v2 design contract = DESIGN-07.** AUDIT.md does not propose the matrix; the matrix above is sourced from `.planning/research/PITFALLS.md` "Connector-availability fallback" content (per Open Q4 — option (c)) so reviewers see the input shape Phase 2 will work from.
    ```

    Then `---` separator to close AUDIT-03.

    **Implementing decisions:** D-13 (observation-led — explicit "DESIGN-07 territory" callout), D-14 (citation form).
  </action>
  <verify>
    <automated>awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -q 'DESIGN-07' &amp;&amp; awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -q 'PITFALLS' &amp;&amp; awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -q 'Research-derived' &amp;&amp; awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -q 'fallback' &amp;&amp; echo HINT_TABLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-03 section contains "DESIGN-07" reference: `awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -c 'DESIGN-07'` >= `2` (one in fallback table, one in pointer line)
    - AUDIT-03 contains "PITFALLS" or "PITFALLS.md" reference: `awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -q 'PITFALLS'` succeeds
    - The locked phrase "Research-derived; v2 design contract = DESIGN-07" or close variant appears: `awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -q 'Research-derived'` succeeds
    - Sub-section table has at least 4 fallback rows: `awk '/Sub-section: Research-derived/,/^---$/' .planning/AUDIT.md | grep -cE '^\| Stage'` >= `4`
    - Section AUDIT-03 closes with `---`: `awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -c '^---$'` >= `2` (one after main table, one closing the section)
    - No design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-03:/,/^## AUDIT-04:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`
  </acceptance_criteria>
  <done>
    AUDIT-03 sub-section + closing pointer landed; reviewer can see the research input shape without AUDIT.md proposing it; structural-check still functions correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INTEGRATIONS.md observations → dependency table cells | If executor mis-reads the artefact-driven nature of v0.3.0 and marks more cells "REQUIRED" than reality, AUDIT-03 inflates v0.3.0's connector surface and confuses Phase 2 design scope. |
| PITFALLS.md fallback matrix → AUDIT-03 hint table | The hint table is a **summary**, not a transcription. If executor copies the entire matrix verbatim, AUDIT-03 violates D-13 by becoming a design proposal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | Dependency-table cell over-claims (e.g. marking generate-sow's Coda cell "REQUIRED" when v0.3.0 generate-sow doesn't touch Coda) | mitigate | Acceptance criterion checks for at least one `REQUIRED` (execute-tests + Pipefy/Wrike/Ziflow) but the executor's `<read_first>` mandates reading INTEGRATIONS.md first. Manual review verifies cells against INTEGRATIONS.md. |
| T-01-03-02 | Repudiation | Section drifts into design proposal (violates D-13) | mitigate | Acceptance criteria run canonical D-13 banned-phrase ERE `grep -ciE 'we should\|the design will\|recommend that v2\|propose\|v2 will' = 0`. The locked phrase "Research-derived; v2 design contract = DESIGN-07" anchors the observation framing. |
| T-01-03-03 | Information Disclosure | Fallback hint table reproduces full PITFALLS matrix (violates "by reference, not transcription") | mitigate | Acceptance criterion caps fallback table at "at least 4 rows" (4-5 representative rows max per `<action>`); manual review confirms it's a sample, not a full mirror. |
</threat_model>

<verification>
End of Plan 03 success-state:
1. AUDIT-03 dependency table has 7 stage rows × 10 connector columns.
2. At least one `REQUIRED` cell (execute-tests Pipefy/Wrike/Ziflow).
3. At least one `optional (graceful)` cell (discovery-intake Miro).
4. Sub-section + DESIGN-07 pointer present.
5. No design-move language; observation-led tone preserved (canonical D-13 ERE returns 0).
6. Structural-check still functions; AUDIT-03 H2 anchor still detected.
</verification>

<success_criteria>
- [ ] AUDIT-03 dependency table populated (7 stages × 10 connectors with the cell vocabulary specified)
- [ ] PITFALLS-cited fallback hint sub-table present with DESIGN-07 pointer
- [ ] No design-move language (D-13 enforced via canonical banned-phrase ERE)
- [ ] No content under `dydx-delivery/` modified
- [ ] Structural-check script still recognises 8 H2 sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-03-SUMMARY.md` documenting:
- Stage-row count, connector-column count
- REQUIRED-cell count, graceful-cell count, none-cell count
- Pointer: "Plan 04 (Wave 4) populates AUDIT-04 next"
</output>
