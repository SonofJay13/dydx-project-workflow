---
plan_id: 02-07
phase: 02-design
plan: 07
type: execute
wave: 7
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
requirements: [DESIGN-22, DESIGN-23]
files_modified:
  - .planning/phases/02-design/02-07-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-07-SUMMARY.md
autonomous: true
tags: [design, stage-skills, cost-estimate, coda-mechanics, build-prompts]

must_haves:
  truths:
    - "DESIGN.md `## Stage 6: Cost estimate` (DESIGN-22) populated with the FULL Stage 6 contract per CONTEXT D-22 STRUCTURE-LOCK: closed L/M/H risk-multiplier taxonomy with mandatory `rationale` field per row; numeric defaults DEFERRED with placeholder syntax `L=<TBD-deferred>` / `M=<TBD-deferred>` / `H=<TBD-deferred>` and inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker; `estimated_hours` + `risk_adjusted_hours` columns; per-assignee task breakdown (dev / non-dev / QA / lead); schema-introspection of existing client task table cached in `00_HUB.md`; Coda writes via `rows/upsert` with `keyColumns` for idempotency, `mutationStatus` polling, rate-limit at 4 req/10s (80% of 5 req/10s ceiling); wait-for-commercial-inputs gate before client-facing summary."
    - "DESIGN.md `## Stage 7a: Build prompt — dev` (DESIGN-23 first half) carries forward existing `generate-build-prompt` skill (MODIFIED) — pulls `delivery: native-ai | api` from Stage 4a per DESIGN-20 routing key + Stage 5 tech spec for API portions."
    - "DESIGN.md `## Stage 7b: Build prompt — implementation per platform` (DESIGN-23 second half) — NEW skill `generate-implementation-prompt`; per-platform shape (Pipefy = Behaviors instructions + KB upload list; Wrike = Copilot workflow narrative; Ziflow = checklist/criteria spec); explicitly NOT a universal template (per `## Out of Scope`)."
    - "Echo lines: DESIGN-22 (1 under Stage 6), DESIGN-23 (2 — one each under 7a + 7b H2s, same 4a/4b precedent). Total NEW echoes = 3. Running total: 25/30."
    - "Plan 02-07 must place the inline `[OPEN: Phase 4 — risk-multiplier numeric defaults pending dYdX-historical validation per D-22]` marker in the Stage 6 contract body at point of use (per D-27). Plan 02-10 dedups against the seed marker placed by Plan 02-01 in the Deferred section."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-22 (Stage 6) + DESIGN-23 (Stage 7a + 7b) decision contracts populated"
      contains: "Stage 6 risk-multiplier taxonomy (structure locked, numerics deferred); Coda mechanics (keyColumns + mutationStatus + 4 req/10s); wait-for-commercial-inputs gate; Stage 7a (dev path); Stage 7b (per-platform shape); 3 echo lines"
  key_links:
    - from: "Stage 6 risk-multiplier taxonomy structure"
      to: "Phase 4 OPEN-QUESTIONS register (numeric defaults: 1.1 / 1.3 / 1.6 from research, validated against dYdX-historicals)"
      via: "Inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker at point of use"
      pattern: "L=<TBD-deferred>"
    - from: "Stage 6 Coda mechanics"
      to: "platform-pipefy/-wrike/-ziflow schema-introspection (DESIGN-14/15/16) + 00_HUB.md cache"
      via: "`coda_tasks_schema:` cache field in 00_HUB.md (per Phase 4 OPEN-05 default 00_HUB.md schema)"
      pattern: "coda_tasks_schema:"
---

<objective>
Populate `.planning/DESIGN.md` Stage 6 (DESIGN-22 — cost estimate with Coda mechanics) + Stage 7a (DESIGN-23 dev build prompt) + Stage 7b (DESIGN-23 per-platform implementation prompt) decision contracts.

**Purpose:** Stage 6 is the heaviest single contract in DESIGN.md per CONTEXT — Coda integration mechanics PLUS the risk-multiplier taxonomy STRUCTURE (numerics deferred per D-22). Stage 7 introduces the dual build prompt split (7a dev for API work; 7b per-platform implementation for native-AI work).

**Output:** `.planning/DESIGN.md` Stages 6, 7a, 7b sections populated. 3 echo lines added (DESIGN-22 once + DESIGN-23 twice). Running total: 25/30. Plus 1 NEW inline `[OPEN]` marker in Stage 6 body (the D-22 deferral at point of use).

**Forward-reference guardrails (per cross-AI review MEDIUM #6):** Cross-references to DESIGN-26 (Stage 10) from Stage 7b are FORWARD references at end of Wave 7. Cross-references cite the DESIGN-NN ID only as anchor placeholders; acceptance criteria do NOT assert the cross-referenced body content exists. Plan 02-10 verifies Appendix B traceability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/phases/02-design/02-05-PLAN.md
@.planning/phases/02-design/02-06-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Stage 6 is the heavyweight section — risk-multiplier taxonomy STRUCTURE locked, numerics DEFERRED per D-22.
     Coda mechanics are EXACT VALUES per REQUIREMENTS DESIGN-22 + CONTEXT D-22: keyColumns, mutationStatus polling,
     4 req/10s rate-limit (80% of 5/10s ceiling), schema-introspection cached in 00_HUB.md, wait-for-commercial-inputs gate.

     Section-range extraction in acceptance criteria uses awk ranges with DISTINCT start/end anchors
     (e.g., `awk '/^## Stage 7a:/,/^## Stage 7b:/'`) — these are SAFE because the start regex does NOT
     match the end regex on the same line. The broken `awk '/^## X/,/^## /'` pattern (cross-AI review
     HIGH #1) is forbidden; this plan does not use it.
     -->

Risk-multiplier taxonomy table shape (DESIGN-22 contract — STRUCTURE-only per D-22):

| Tier | Multiplier | Rationale required | Validation owner | Default value |
|------|------------|--------------------|--------------------|--------------- |
| L (Low risk) | `risk_adjusted_hours = estimated_hours × <L_multiplier>` | yes | Stage 6 author + reviewer | `L=<TBD-deferred>` `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` |
| M (Medium risk) | `risk_adjusted_hours = estimated_hours × <M_multiplier>` | yes | Stage 6 author + reviewer | `M=<TBD-deferred>` (research-recommended ~1.3) |
| H (High risk) | `risk_adjusted_hours = estimated_hours × <H_multiplier>` | yes | Stage 6 author + reviewer | `H=<TBD-deferred>` (research-recommended ~1.6) |

Coda mechanics subsection shape:

```markdown
**Coda integration mechanics (DESIGN-22 contract — exact values).**

- **Schema introspection:** First Stage 6 run per client introspects the client's existing Coda task table schema via Coda MCP `table_columns_read`. Schema cached in `<Client> Brain/00_HUB.md` `coda_tasks_schema:` block.
- **Upsert pattern:** Coda writes via `table_rows_manage` (Coda MCP) with `keyColumns` parameter set to the client task table's primary-key column(s). Idempotent.
- **Mutation status polling:** Each `table_rows_manage` call returns a `mutationStatus` ID. Stage 6 polls until terminal state. Failure halts.
- **Rate limit:** 4 req/10 second sliding window (= 80% of Coda public ceiling 5 req/10s).
- **Wait-for-commercial-inputs gate (DESIGN-22 contract):** Two halt points:
  1. Pre-write halt — `06_cost_inputs_v<N>.md` lists commercial inputs needed; resume on `commercial_inputs_status: provided`.
  2. Pre-publish halt — after compute, halt before Coda push; resume on `status: approved`.
```

DESIGN-23 dual build prompts:

```markdown
## Stage 7a: Build prompt — dev

> **DESIGN-23:** Stage 7a Build prompt — dev — `generate-build-prompt` skill (MODIFIED, carry forward from v0.3.0); pulls `delivery: native-ai | api` from Stage 4a per DESIGN-20 routing key; Stage 7a covers `delivery: api` requirements (dev-implementation territory) + Stage 5 tech-spec details for API endpoints.

[Skill / Stage / Complexity / Inputs / Outputs / Hand-off / Decisions / Cross-references]

## Stage 7b: Build prompt — implementation per platform

> **DESIGN-23:** Stage 7b Build prompt — implementation per platform — `generate-implementation-prompt` skill (NEW); per-platform shape (Pipefy = Behaviors instructions + KB upload list; Wrike = Copilot workflow narrative; Ziflow = checklist/criteria spec); explicitly NOT a universal template (per `## Out of Scope`); reads `delivery: native-ai` rows from Stage 4a per DESIGN-20 routing key.

[Skill / Stage / Complexity / Inputs / Outputs / Hand-off / Decisions / Cross-references]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Stage 6: Cost estimate` (DESIGN-22) decision contract — risk-multiplier taxonomy structure + Coda mechanics + wait-for-commercial-inputs gate</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-06 complete)
    - .planning/REQUIREMENTS.md §"DESIGN-22"
    - .planning/phases/02-design/02-CONTEXT.md §"Risk-multiplier defaults (DESIGN-22)" D-22 (structure-only — numerics DEFERRED with placeholder syntax + inline `[OPEN]` marker)
    - .planning/AUDIT.md §AUDIT-08 (Coda MCP wiring status)
    - .planning/research/PITFALLS.md (Coda CRIT/MOD entries)
    - .planning/DESIGN.md hand-off matrix Stage 5 → Stage 6 + Stage 6 → Stage 7a rows (verbatim per D-26)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — replace placeholder body of `## Stage 6: Cost estimate` H2.

    Required elements:
    1. Echo line: `> **DESIGN-22:** Stage 6 Cost estimate — per-assignee task breakdown (dev / non-dev / QA / lead); \`estimated_hours\` + \`risk_adjusted_hours\` columns with mandatory \`rationale\` field; closed risk-multiplier taxonomy (default L=<TBD-deferred> / M=<TBD-deferred> / H=<TBD-deferred> per D-22 — \`[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]\`); schema-introspection of existing client task table cached in \`00_HUB.md\`; Coda writes via \`table_rows_manage\` with \`keyColumns\` for idempotency, \`mutationStatus\` polling, rate-limit at 4 req/10s; wait-for-commercial-inputs gate before client-facing summary.`
    2. Skill / Stage / Complexity: `generate-cost-estimate/` (NEW) / Stage 6 / **High**.
    3. **Inputs:** Frontmatter (`based_on_fnspec_platform: 04a_*` AND/OR `based_on_fnspec_integration: 04b_*` AND optional `based_on_techspec: 05_techspec_v*`); upstream artefacts; external (Coda MCP for schema introspection + upserts; client `00_HUB.md` for `coda_tasks_schema:` cache).
    4. **Outputs:** carrier `06_cost_v<N>.md` (local) + Coda task-table rows (one-way mirror per DESIGN-09); intermediate `06_cost_inputs_v<N>.md` for the wait-for-commercial-inputs halt point. Frontmatter: `frontmatter_version: 2`; `risk_multiplier_version:`; `commercial_inputs_status: pending | provided`; `status: draft → client_review → approved`.
    5. **Risk-multiplier taxonomy (THE STRUCTURE-LOCK contract per D-22)** — explicit subsection with 5-column table (Tier / Multiplier / Rationale required / Validation owner / Default value). 3 rows: L / M / H. Default values column carries `L=<TBD-deferred>` etc. with the inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker. **Mandatory `rationale:` field per row.**
    6. **Per-assignee task breakdown** subsection: 4 assignee classes (dev / non-dev / QA / lead). Per-class summary table at top of artefact.
    7. **Coda integration mechanics** subsection — verbatim from `<interfaces>` (5 elements: schema introspection cache, upsert keyColumns, mutationStatus polling, 4 req/10s, halt-on-failure).
    8. **Wait-for-commercial-inputs gate** subsection — 2 halt points.
    9. **Downstream consumer:** generate-build-prompt (Stage 7a — same wave); generate-implementation-prompt (Stage 7b — same wave).
    10. **Status flag:** `status: approved`.
    11. **Hand-off message:** verbatim from matrix Stage 6 → Stage 7a row.
    12. **Key v2 decisions** (>= 6 numbered): closed L/M/H taxonomy with rationale field; numerics DEFERRED; per-assignee class breakdown; schema-introspection cache; Coda upsert + mutationStatus + 4 req/10s; wait-for-commercial-inputs 2-halt gate.
    13. **Dependencies:** DESIGN-04 (Coda MCP); DESIGN-09 (one-way mirror); DESIGN-14/15/16 (per-platform schema introspection).
    14. **Cross-references:** DESIGN-20 (`delivery:` routing); Stage 7a, 7b (same wave); AUDIT.md §AUDIT-08; Phase 4 OPEN-QUESTIONS.

    **Implementing decisions:** D-20, D-22 (structure locked + numerics DEFERRED with inline `[OPEN]` marker), D-26, D-27 + D-33, D-32, D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-22:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'risk_adjusted_hours' .planning/DESIGN.md &amp;&amp; grep -qF 'estimated_hours' .planning/DESIGN.md &amp;&amp; grep -qF '<TBD-deferred>' .planning/DESIGN.md &amp;&amp; grep -qF 'risk-multiplier defaults pending dYdX-historical validation per D-22' .planning/DESIGN.md &amp;&amp; grep -qF 'keyColumns' .planning/DESIGN.md &amp;&amp; grep -qF 'mutationStatus' .planning/DESIGN.md &amp;&amp; grep -qF '4 req/10' .planning/DESIGN.md &amp;&amp; grep -qF 'wait-for-commercial-inputs' .planning/DESIGN.md &amp;&amp; grep -qF 'coda_tasks_schema:' .planning/DESIGN.md &amp;&amp; grep -qF '06_cost_v' .planning/DESIGN.md &amp;&amp; grep -qF '06_cost_inputs_v' .planning/DESIGN.md &amp;&amp; grep -qF 'commercial_inputs_status:' .planning/DESIGN.md &amp;&amp; grep -qF 'rationale' .planning/DESIGN.md &amp;&amp; echo STAGE_6_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-22:\*\*' .planning/DESIGN.md` succeeds
    - **D-22 STRUCTURE-LOCK / numerics-DEFERRED contract:**
      - `grep -qF '<TBD-deferred>' .planning/DESIGN.md` succeeds
      - `grep -qF 'L=<TBD-deferred>' .planning/DESIGN.md` succeeds
      - `grep -qF 'risk-multiplier defaults pending dYdX-historical validation per D-22' .planning/DESIGN.md` succeeds
      - `grep -qF 'mandatory `rationale`' .planning/DESIGN.md` OR `grep -qF '`rationale:`' .planning/DESIGN.md` succeeds
      - `grep -qF 'risk_multiplier_version:' .planning/DESIGN.md` succeeds
    - **Coda mechanics — all 5 elements:**
      - `grep -qF 'keyColumns' .planning/DESIGN.md` succeeds
      - `grep -qF 'mutationStatus' .planning/DESIGN.md` succeeds
      - `grep -qF '4 req/10' .planning/DESIGN.md` succeeds
      - `grep -qF 'table_columns_read' .planning/DESIGN.md` OR `grep -qF 'schema introspection' .planning/DESIGN.md` succeeds
      - `grep -qF 'coda_tasks_schema:' .planning/DESIGN.md` succeeds
    - **Wait-for-commercial-inputs gate:**
      - `grep -qF 'wait-for-commercial-inputs' .planning/DESIGN.md` succeeds
      - `grep -qF '06_cost_inputs_v' .planning/DESIGN.md` succeeds
      - `grep -qF 'commercial_inputs_status:' .planning/DESIGN.md` succeeds
    - `grep -qF 'estimated_hours' .planning/DESIGN.md` AND `grep -qF 'risk_adjusted_hours' .planning/DESIGN.md` succeed
    - `grep -qF 'assignee_class:' .planning/DESIGN.md` succeeds
    - `grep -qF '06_cost_v' .planning/DESIGN.md` succeeds
    - Hand-off message verbatim from matrix Stage 6 → 7a row: `grep -qF '> Awaiting `status: approved` to `06`' .planning/DESIGN.md` succeeds
    - **Inline `[OPEN]` count grew by 1:** `grep -cE '\[OPEN: Phase 4' .planning/DESIGN.md` returns >= 9 (8 from Plan 02-04 + 1 D-22 marker)
  </acceptance_criteria>
  <done>
    Stage 6 cost estimate decision contract populated: DESIGN-22 echo + risk-multiplier taxonomy structure + per-assignee breakdown + Coda mechanics (5 elements) + wait-for-commercial-inputs gate (2 halt points) + hand-off message verbatim + cross-references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `## Stage 7a: Build prompt — dev` + `## Stage 7b: Build prompt — implementation per platform` (DESIGN-23) decision contracts + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — Stage 6 populated)
    - .planning/REQUIREMENTS.md §"DESIGN-23"
    - .planning/AUDIT.md §AUDIT-01.6
    - .planning/REQUIREMENTS.md §"## Out of Scope"
    - .planning/DESIGN.md `### platform-pipefy` / `### platform-wrike` / `### platform-ziflow` (Plan 02-04)
    - .planning/DESIGN.md hand-off matrix Stage 7a/7b → Stage 8a row
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate Stage 7a + Stage 7b H2 sections.

    **(A) `## Stage 7a: Build prompt — dev` (DESIGN-23 first half)** — replace placeholder body:
    1. Echo line: `> **DESIGN-23:** Stage 7a Build prompt — dev — \`generate-build-prompt\` skill (MODIFIED, carries forward from v0.3.0); pulls \`delivery: native-ai | api\` from Stage 4a per DESIGN-20 routing key; Stage 7a covers \`delivery: api\` requirements (dev-implementation territory) + Stage 5 tech-spec details for API endpoints.`
    2. Skill / Stage / Complexity: `generate-build-prompt/` (MODIFIED) / Stage 7a / Medium.
    3. **Inputs:** Frontmatter (`based_on_fnspec_integration: 04b_*` REQUIRED + `based_on_techspec: 05_techspec_v*` REQUIRED + `based_on_cost: 06_cost_v*` REQUIRED + optional `based_on_fnspec_platform: 04a_*`).
    4. **Outputs:** carrier `07a_build-prompt_v<N>.md`. Frontmatter: standard + `delivery_filter: api`.
    5. **What 7a does NOT cover:** `delivery: native-ai` requirements (those go to 7b).
    6. **Downstream consumer:** dev (human implementer).
    7. **Status flag:** `status: approved`.
    8. **Hand-off message:** verbatim from matrix Stage 7a/7b → Stage 8a row.
    9. **Key v2 decisions:** reads `delivery: api` rows; carries forward from v0.3.0 with `delivery:` filter; 7a + 7b complementary.
    10. **Dependencies:** DESIGN-20; DESIGN-21; DESIGN-22.
    11. **Cross-references:** Stage 4a, 4b, 5, 6, 7b; Stage 8a (FORWARD); AUDIT.md §AUDIT-01.6.

    **(B) `## Stage 7b: Build prompt — implementation per platform` (DESIGN-23 second half)** — replace placeholder body:
    1. Echo line: `> **DESIGN-23:** Stage 7b Build prompt — implementation per platform — \`generate-implementation-prompt\` skill (NEW); per-platform shape (Pipefy = Behaviors instructions + KB upload list; Wrike = Copilot workflow narrative; Ziflow = checklist/criteria spec); explicitly NOT a universal template (per \`## Out of Scope\`); reads \`delivery: native-ai\` rows from Stage 4a per DESIGN-20 routing key.`
    2. Skill / Stage / Complexity: `generate-implementation-prompt/` (NEW) / Stage 7b / Medium.
    3. **Inputs:** Frontmatter (`based_on_fnspec_platform: 04a_*` REQUIRED + `based_on_cost: 06_cost_v*` REQUIRED + `platform: pipefy | wrike | ziflow` REQUIRED); upstream (`04a`, `06`); external (per-platform `references/api-contract.md` + `native-ai-inventory.md`).
    4. **Outputs:** carrier `07b_implementation-prompt_v<N>.md`. Frontmatter: standard + `delivery_filter: native-ai` + `platform: <X>`.
    5. **Per-platform shape (DESIGN-23 contract — NOT a universal template):**
       - **Pipefy:** Behaviors instructions block + KB upload list. `## Behaviors instructions` H2 + `## KB documents to upload` H2.
       - **Wrike:** Copilot workflow narrative. `## Copilot workflow narrative` H2 + `## MCP tools required` H2.
       - **Ziflow:** Checklist/criteria spec. `## ReviewAI checklists` H2 + `## Manual paste fallback` H2.
       - **Universal anti-pattern (per `## Out of Scope`):** A single template that "fits all platforms" is FORBIDDEN. Skill dispatches on `platform:` frontmatter to one of three template paths.
    6. **Downstream consumer:** non-dev (human implementer per platform).
    7. **Status flag:** `status: approved`.
    8. **Hand-off message:** verbatim from matrix Stage 7a/7b → Stage 8a row.
    9. **Key v2 decisions:** NEW skill; per-platform shape (NOT universal); reads `delivery: native-ai` rows; per-platform reference docs drive affordances.
    10. **Dependencies:** DESIGN-14/15/16; DESIGN-20.
    11. **Cross-references:** Stage 4a, 6, 7a; Stage 8a (FORWARD); platform-pipefy / platform-wrike / platform-ziflow.

    **(C) Run structural-check**: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 1 (echo count = 25/30).

    **Implementing decisions:** D-20, D-26, D-32, D-34, D-35.
  </action>
  <verify>
    <automated>STAGE_7A_ECHO=$(awk '/^## Stage 7a:/,/^## Stage 7b:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-23:\*\*') &amp;&amp; [ "$STAGE_7A_ECHO" = "1" ] &amp;&amp; STAGE_7B_ECHO=$(awk '/^## Stage 7b:/,/^## Stage 8:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-23:\*\*') &amp;&amp; [ "$STAGE_7B_ECHO" = "1" ] &amp;&amp; grep -qF 'delivery_filter: api' .planning/DESIGN.md &amp;&amp; grep -qF 'delivery_filter: native-ai' .planning/DESIGN.md &amp;&amp; grep -qF '07a_build-prompt_v' .planning/DESIGN.md &amp;&amp; grep -qF '07b_implementation-prompt_v' .planning/DESIGN.md &amp;&amp; grep -qF 'Behaviors instructions' .planning/DESIGN.md &amp;&amp; grep -qF 'Copilot workflow narrative' .planning/DESIGN.md &amp;&amp; grep -qF 'ReviewAI checklists' .planning/DESIGN.md &amp;&amp; grep -qF 'NOT a universal template' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "25" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo STAGE_7_OK</automated>
  </verify>
  <acceptance_criteria>
    - Stage 7a subsection has exactly 1 `> **DESIGN-23:**` echo line — SAFE awk range: `awk '/^## Stage 7a:/,/^## Stage 7b:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-23:\*\*'` = 1
    - Stage 7b subsection has exactly 1 `> **DESIGN-23:**` echo line — SAFE awk range: `awk '/^## Stage 7b:/,/^## Stage 8:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-23:\*\*'` = 1
    - `grep -qF 'delivery_filter: api' .planning/DESIGN.md` succeeds
    - `grep -qF 'delivery_filter: native-ai' .planning/DESIGN.md` succeeds
    - `grep -qF '07a_build-prompt_v' .planning/DESIGN.md` AND `grep -qF '07b_implementation-prompt_v' .planning/DESIGN.md` succeed
    - **Per-platform shape — all 3 elements:**
      - `grep -qF 'Behaviors instructions' .planning/DESIGN.md` succeeds (Pipefy)
      - `grep -qF 'Copilot workflow narrative' .planning/DESIGN.md` succeeds (Wrike)
      - `grep -qF 'ReviewAI checklists' .planning/DESIGN.md` succeeds (Ziflow)
    - `grep -qF 'NOT a universal template' .planning/DESIGN.md` succeeds
    - Hand-off message subsections contain verbatim Stage 7a/7b → 8a string from matrix
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `25`
    - `bash design-structure-check.sh` exits with code `1` (mid-phase)
    - Total inline `[OPEN: Phase 4` count: `grep -cE '\[OPEN: Phase 4' .planning/DESIGN.md` returns >= 9 (no new markers in 7a / 7b)
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** Cross-references to Stage 8a (DESIGN-24 — populated in Plan 02-08) cited as anchor placeholder only.
  </acceptance_criteria>
  <done>
    Stage 7a + 7b decision contracts populated. Echo count = 25/30.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 6 risk-multiplier numerics → v2.x build phase | Numerics DEFERRED per D-22; placeholder + inline `[OPEN]` marker is the contract. |
| Stage 6 Coda mechanics → live Coda MCP behaviour | The 4 req/10s rate-limit assumes the documented Coda public ceiling of 5 req/10s. |
| Stage 7b per-platform shape → Out of Scope universal-template anti-pattern | Future v2.x might attempt to "consolidate" — explicitly forbidden. |
| Forward references to DESIGN-24 (Stage 8a) — populated in Plan 02-08 (cross-AI review MEDIUM #6) | Cross-references cite the ID only; body verification deferred to Plan 02-10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | Stage 6 author writes numeric defaults instead of `<TBD-deferred>` | mitigate | Acceptance criteria assert literal `<TBD-deferred>` AND `risk-multiplier defaults pending` marker. |
| T-02-07-02 | Tampering | Coda mechanics drift (e.g., "5 req/10s" instead of "4") | mitigate | Acceptance criteria assert literal `4 req/10`. |
| T-02-07-03 | Tampering | Wait-for-commercial-inputs gate omits one halt point | mitigate | Acceptance criteria assert both halt-point indicators. |
| T-02-07-04 | Tampering | Stage 7b shape consolidates platforms into single template | mitigate | Acceptance criteria assert ALL 3 per-platform shape strings + anti-pattern callout. |
| T-02-07-05 | Tampering | Stage 7a accidentally claims `delivery: native-ai` (overlaps 7b) | mitigate | Acceptance criteria assert disjoint `delivery_filter:` values. |
| T-02-07-06 | Tampering | DESIGN-23 echo count miscount | mitigate | Per-stage scoped checks via SAFE awk ranges (distinct start/end anchors). |
| T-02-07-07 | Tampering | Acceptance criteria assert content from forward-referenced DESIGN-24 (Stage 8a) that doesn't exist yet | mitigate | Cross-AI review MEDIUM #6 fix: forward references are anchor placeholders only. |
</threat_model>

<verification>
End of Plan 02-07 success-state:
1. Stage 6 H2 populated with FULL DESIGN-22 contract
2. Stage 7a + 7b H2s populated with DESIGN-23 contracts (each with own echo)
3. Echo count = 25/30
4. `bash design-structure-check.sh` exits 1 with assertion #4 failing
5. Inline `[OPEN]` count >= 9 (1 new D-22 marker added in Stage 6)
6. Numerics for risk-multiplier remain DEFERRED per D-22

Manual reviewer attestation:
- [ ] Stage 6 risk-multiplier table uses placeholder syntax; no numeric defaults shipped
- [ ] Coda mechanics match REQUIREMENTS canonical values
- [ ] Stage 7b per-platform shape is genuinely 3 distinct shapes
- [ ] No full SKILL.md prose drafted
</verification>

<success_criteria>
- [ ] DESIGN-22 echo + Stage 6 contract
- [ ] DESIGN-23 echoes under Stage 7a + Stage 7b H2s
- [ ] Stage 7b: 3 per-platform shapes + anti-pattern callout
- [ ] Echo count = 25/30
- [ ] Inline `[OPEN]` count >= 9
- [ ] Structural-check exits 1 (mid-phase)
- [ ] No content under `dydx-delivery/` modified
- [ ] Forward-reference guardrails applied (cross-AI review MEDIUM #6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-07-SUMMARY.md` documenting:
- Stage 6: risk-multiplier taxonomy structure + numerics deferred + Coda mechanics + wait-for-commercial-inputs gate
- Stage 7a (dev) vs Stage 7b (per-platform implementation)
- Stage 7b per-platform shapes
- Echo count: 25/30
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #6 (forward-reference guardrails — cross-references to DESIGN-24 are anchor placeholders only at end of Wave 7).
- Pointer: "Plan 02-08 (Wave 8) populates Stages 8/9/10/11 next."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-07-SUMMARY.md` to `files_modified`); MEDIUM #6 (forward-reference guardrails — explicit `<objective>` callout + threat T-02-07-07 added; cross-references to DESIGN-24 (Stage 8a) are anchor placeholders only). Existing safe `awk '/^## Stage 7a:/,/^## Stage 7b:/'` and `awk '/^## Stage 7b:/,/^## Stage 8:/'` patterns kept (distinct start/end anchors — not the broken `/^## X/,/^## /` pattern). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-22/23 still in `requirements`; D-20/22/26/27/32/33/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
