---
plan_id: 02-08
phase: 02-design
plan: 08
type: execute
wave: 8
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07]
requirements: [DESIGN-24, DESIGN-25, DESIGN-26, DESIGN-27]
files_modified:
  - .planning/phases/02-design/02-08-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-08-SUMMARY.md
autonomous: true
tags: [design, stage-skills, test-bot-overview, documentation, native-ai, sign-off]

must_haves:
  truths:
    - "DESIGN.md `## Stage 8: Test bot — overview` (DESIGN-24) populated with stage-level test-bot architecture: `provision-test-harness` (8a) bootstraps once + delta-updates each ship; persistent harness lives at `<Client> Brain/test-bot/` (outside this repo); tier-1 deterministic Python + tier-2 AI orchestrator with hard layer-separation contract; `harness_drift` failure class added to enum; `sandbox_lock.yaml` for concurrency; sandbox allowlist extended to Coda (CRIT-5 fix); test-case lifecycle states `active | obsolete | quarantined`. **Detailed tier-1/tier-2 boundary + client_state.yaml schema + drift-detection contract live under `## Test bot architecture` section (DESIGN-28..30) populated by Plan 02-09.**"
    - "DESIGN.md `## Stage 9: Documentation publishing` (DESIGN-25) populated: `update-documentation` skill writes `ChangeRequests/<CR>/doc-diff.md`; reviewer-approval gate before push; deterministic local→Drive folder/filename mapping; closed `doc_type` enum (`discovery | sow | platform_fnspec | integration_fnspec | tech_spec | test_plan | build_prompt | results | brain_spoke`); naming `<client_slug>__<project_slug>__<doc_type>__v<N>` (double-underscore separator); `doc_published_at` timestamp invariant; halt condition if `00_HUB.md` `Documentation:` link missing (graceful — does not halt other stages)."
    - "DESIGN.md `## Stage 10: Native-AI enablement` (DESIGN-26) populated: `push-native-ai-knowledge` skill reads Stage 4a + approved Stage 9 doc fragments + per-platform `native-ai-inventory.md`; branches on `native_ai_path: api | paste | none`; copy-paste fallback default; refuses ingest if `doc_published_at < last_diff_review_at` (CRIT-8 fix); per-client target ID from `00_HUB.md`; refuses ingest if target mismatches `client:` (MIN-4 fix); `doc_version: <semver>` + `ingested_at: <ISO>` per ingested doc."
    - "DESIGN.md `## Stage 11: Sign-off, brain update, archive` (DESIGN-27) populated: `sign-off-and-archive` skill updates local `<Client> Brain/<spokes>/`; one-way Coda mirror push with brain-mirror Coda doc template (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes); `tone_lint` pass before publish (MOD-9 prevention); CR move to `Archive/`; `00_Index.md` version bump; Field Notes table preserved (input-only); pre-archive sanity check (no orphan refs, no missing artefacts)."
    - "Echo lines: DESIGN-24, 25, 26, 27 — 4 NEW echoes. Running total: 29/30."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-24, 25, 26, 27 stage skill decision contracts populated"
      contains: "4 H2 stage subsections each with echo + decision contract per D-20; closed doc_type enum (DESIGN-25); `native_ai_path` branching (DESIGN-26); brain-mirror template sections (DESIGN-27); sandbox allowlist extended to Coda (DESIGN-24 CRIT-5 fix)"
  key_links:
    - from: "DESIGN-25 doc_type closed enum"
      to: "DESIGN-26 native-AI push (reads `doc_type:` to filter ingestable doc fragments)"
      via: "doc_type: enum value"
      pattern: "doc_type: (discovery|sow|platform_fnspec|integration_fnspec|tech_spec|test_plan|build_prompt|results|brain_spoke)"
    - from: "DESIGN-26 doc_published_at < last_diff_review_at refusal (CRIT-8 fix)"
      to: "DESIGN-25 doc_published_at + last_diff_review_at frontmatter contract"
      via: "Both fields set in Stage 9; Stage 10 reads + compares"
      pattern: "doc_published_at >= last_diff_review_at"
    - from: "DESIGN-27 brain-mirror Coda doc template"
      to: "Phase 4 OPEN-05 standard Coda templates"
      via: "Template content (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes) is the design contract"
      pattern: "brain-mirror Coda doc template"
---

<objective>
Populate `.planning/DESIGN.md` Stage 8 overview (DESIGN-24), Stage 9 (DESIGN-25), Stage 10 (DESIGN-26), Stage 11 (DESIGN-27) decision contracts.

**Purpose:** Stage 8 is the test bot overview at stage level — detailed architecture (DESIGN-28..30) lives in `## Test bot architecture` populated by Plan 02-09. Stages 9–11 are the publish/native-AI/sign-off triple — each carries critical CRIT-/MOD-/MIN- pitfall fixes.

**Output:** `.planning/DESIGN.md` Stages 8, 9, 10, 11 sections populated. 4 echo lines added. Running total: 29/30. Plan 02-09 adds remaining 3 echo lines (DESIGN-28/29/30) to reach 32/30.

**Forward-reference guardrails (per cross-AI review MEDIUM #6):** Cross-references to DESIGN-28/29/30 from Stage 8 overview are FORWARD references — those H3 sections are populated by Plan 02-09. Cross-references cite the DESIGN-NN ID only as anchor placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/PITFALLS.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/phases/02-design/02-05-PLAN.md
@.planning/phases/02-design/02-06-PLAN.md
@.planning/phases/02-design/02-07-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Stage 8 overview here is stage-level. Detailed architecture (DESIGN-28..30) lives under
     ## Test bot architecture (Plan 02-09).

     Section-range extraction in acceptance criteria uses awk ranges with DISTINCT start/end anchors
     (e.g., `awk '/^## Stage 8:/,/^## Stage 9:/'`) — these are SAFE because the start regex does NOT
     match the end regex on the same line. The broken `awk '/^## X/,/^## /'` pattern (cross-AI review
     HIGH #1) is forbidden; this plan does not use it. -->

DESIGN-25 closed `doc_type` enum (REQUIREMENTS canonical):
- discovery
- sow
- platform_fnspec
- integration_fnspec
- tech_spec
- test_plan
- build_prompt
- results
- brain_spoke

DESIGN-25 naming pattern: `<client_slug>__<project_slug>__<doc_type>__v<N>` — double-underscore separator (REQUIREMENTS canonical).

DESIGN-27 brain-mirror Coda doc template sections (REQUIREMENTS canonical):
- Overview
- Workflows
- Platforms
- Integrations
- Operating Model
- Change History
- Field Notes

DESIGN-24 stage 8 carrier files: `08a_test-harness_v<N>.md`, `08b_test-plan_v<N>.md`, `08c_uat-plan_v<N>.md`, `08d_test-results_v<N>.md` (note 8d in `<Client> Brain/test-bot/` per CONTEXT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Stage 8: Test bot — overview` (DESIGN-24) + `## Stage 9: Documentation publishing` (DESIGN-25)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-07 complete)
    - .planning/REQUIREMENTS.md §"DESIGN-24" + §"DESIGN-25"
    - .planning/AUDIT.md §AUDIT-01.5 + §AUDIT-01.7
    - .planning/research/PITFALLS.md — CRIT-5; CRIT-8; MOD-1; MIN-4
    - .planning/phases/02-design/02-CONTEXT.md §"Test-bot architecture detail" D-30
    - .planning/DESIGN.md hand-off matrix (relevant rows)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate Stage 8 + Stage 9 H2 sections.

    **(A) `## Stage 8: Test bot — overview` (DESIGN-24)** — replace placeholder body:
    1. Echo line: `> **DESIGN-24:** Stage 8 Test bot architecture (overview) — \`provision-test-harness\` skill (8a) bootstraps once + delta-updates each ship; persistent harness lives at \`<Client> Brain/test-bot/{client_state.yaml, test_runner.py, test_cases/}\` (outside this repo); tier-1 deterministic Python + tier-2 AI orchestrator with hard layer-separation contract; \`harness_drift\` failure class added to \`spec gap | implementation gap | environment issue | unknown\`; \`sandbox_lock.yaml\` for concurrency; sandbox allowlist extended to Coda (CRIT-5 fix); test-case lifecycle states \`active | obsolete | quarantined\`. Detailed tier-1/tier-2 boundary + \`client_state.yaml\` schema + drift-detection contract live under \`## Test bot architecture\` (DESIGN-28..30 — FORWARD reference, populated by Plan 02-09).`
    2. **Stage-level overview only** — note: "This section provides stage-level overview. Detailed architecture lives under `## Test bot architecture` (DESIGN-28..30 — populated by Plan 02-09)."
    3. Skills enumerated: 8a `provision-test-harness/` (NEW), 8b `generate-test-plan/` (MODIFIED), 8c `generate-uat-plan/` (NEW), 8d `execute-tests/` (MODIFIED — paths move to `<Client> Brain/test-bot/`).
    4. **Persistent harness location:** `<Client> Brain/test-bot/{client_state.yaml, test_runner.py, test_cases/}` — explicitly OUTSIDE this repo.
    5. **Sandbox allowlist (CRIT-5 fix):** v0.3.0 `safety-rules.md` allowlists Pipefy / Wrike / Ziflow sandbox tenants but OMITS Coda sandbox. v2 extends the allowlist to include Coda sandbox. Canonical safety-rules at `dydx-delivery/references/safety-rules.md` (per DESIGN-03) carries the extended allowlist.
    6. **Tier separation (high-level):** Python tier-1 (deterministic) + AI tier-2 (orchestrator) — hard separation: Python tests are HUMAN-AUTHORED; AI does NOT write tier-1. Per `## Out of Scope`.
    7. **Failure-class enum extension:** `harness_drift` added to `spec gap | implementation gap | environment issue | unknown` (5 classes total). Drift detection per DESIGN-30 (FORWARD).
    8. **Concurrency:** `sandbox_lock.yaml` per client at `<Client> Brain/test-bot/sandbox_lock.yaml`.
    9. **Test-case lifecycle states:** `active`, `obsolete`, `quarantined`.
    10. **Inputs / Outputs / Downstream / Status / Hand-off** subsections — span the 4 substages 8a/8b/8c/8d.
    11. **Hand-off message(s)** — verbatim from matrix (Stage 7a/7b → 8a + Stage 8a/b/c → 8d + Stage 8d → 9 rows).
    12. **Cross-references:** DESIGN-04 (test-bot-orchestrator agent + sandbox_lock); DESIGN-28/29/30 (FORWARD — detailed architecture); DESIGN-25 (downstream); AUDIT.md §AUDIT-01.5 + §AUDIT-01.7; PITFALLS CRIT-5.

    **(B) `## Stage 9: Documentation publishing` (DESIGN-25)** — replace placeholder body:
    1. Echo line: `> **DESIGN-25:** Stage 9 Documentation publishing — \`update-documentation\` skill writes \`ChangeRequests/<CR>/doc-diff.md\`; reviewer-approval gate before push; deterministic local→Drive folder/filename mapping; closed \`doc_type\` enum; naming \`<client_slug>__<project_slug>__<doc_type>__v<N>\` (double-underscore separator); \`doc_published_at\` timestamp invariant; halt condition if \`00_HUB.md\` \`Documentation:\` link missing (graceful — does not halt other stages per MOD-1).`
    2. Skill / Stage / Complexity: `update-documentation/` (NEW) / Stage 9 / Medium.
    3. **Inputs:** Frontmatter (`based_on_test_results: 08d_test-results_v<N>` REQUIRED + all upstream `04*/05/06/07*` artefacts referenced); upstream (8d approved + all upstream); external (Drive MCP for publish; client `00_HUB.md` for `Documentation:` Drive folder link).
    4. **Outputs:** `ChangeRequests/<CR>/doc-diff.md` + per-doc Drive uploads. Frontmatter on each Drive doc: `doc_type:`, `doc_version: <semver>`, `doc_published_at: <ISO>`, `last_diff_review_at: <ISO>`.
    5. **Closed `doc_type` enum (DESIGN-25 contract — exactly 9 values):** `discovery | sow | platform_fnspec | integration_fnspec | tech_spec | test_plan | build_prompt | results | brain_spoke`. Reject in skill quality gate.
    6. **Naming convention:** `<client_slug>__<project_slug>__<doc_type>__v<N>` — DOUBLE-UNDERSCORE separator. Example: `acme-inc__widget-redesign__platform_fnspec__v3`.
    7. **doc-diff workflow:** Skill computes diff. Writes `doc-diff.md`. Reviewer-approval gate: skill HALTS until reviewer writes `status: approved` + sets `last_diff_review_at: <ISO>`. Then pushes to Drive (sets `doc_published_at: <ISO>` — invariant).
    8. **Halt condition (MOD-1 graceful):** If `<Client> Brain/00_HUB.md` `Documentation:` link missing, skill halts with explicit error in `doc-diff.md`. GRACEFUL: only Stage 9 halts.
    9. **Downstream consumer:** push-native-ai-knowledge (Stage 10 — gated by `doc_published_at >= last_diff_review_at` per CRIT-8 fix).
    10. **Status flag:** `status: approved` on `doc-diff.md`.
    11. **Hand-off message:** verbatim from matrix Stage 9 → 10 row.
    12. **Key v2 decisions** (5 numbered): closed doc_type enum; double-underscore naming; doc-diff before publish; doc_published_at invariant; MOD-1 graceful halt.
    13. **Dependencies:** DESIGN-04 (Drive MCP); DESIGN-09 (Drive one-way mirror); DESIGN-26 (downstream).
    14. **Cross-references:** DESIGN-26 (CRIT-8); AUDIT.md §AUDIT-04; PITFALLS MOD-1, CRIT-8.

    **Implementing decisions:** D-20, D-26, D-32/D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-24:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-25:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'provision-test-harness' .planning/DESIGN.md &amp;&amp; grep -qF '<Client> Brain/test-bot/' .planning/DESIGN.md &amp;&amp; grep -qF 'harness_drift' .planning/DESIGN.md &amp;&amp; grep -qF 'sandbox_lock.yaml' .planning/DESIGN.md &amp;&amp; grep -qF 'sandbox allowlist extended to Coda' .planning/DESIGN.md &amp;&amp; grep -qF 'CRIT-5' .planning/DESIGN.md &amp;&amp; grep -qF 'doc_type' .planning/DESIGN.md &amp;&amp; grep -qF '__v' .planning/DESIGN.md &amp;&amp; grep -qF 'doc_published_at' .planning/DESIGN.md &amp;&amp; grep -qF 'last_diff_review_at' .planning/DESIGN.md &amp;&amp; grep -qF 'platform_fnspec' .planning/DESIGN.md &amp;&amp; grep -qF 'brain_spoke' .planning/DESIGN.md &amp;&amp; grep -qF 'MOD-1' .planning/DESIGN.md &amp;&amp; echo STAGES_8_9_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-24:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qE '^> \*\*DESIGN-25:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qF 'provision-test-harness' .planning/DESIGN.md` succeeds
    - `grep -qF '<Client> Brain/test-bot/' .planning/DESIGN.md` succeeds
    - `grep -qF 'harness_drift' .planning/DESIGN.md` succeeds
    - `grep -qF 'sandbox_lock.yaml' .planning/DESIGN.md` succeeds
    - `grep -qF 'sandbox allowlist extended to Coda' .planning/DESIGN.md` succeeds
    - `grep -qF 'CRIT-5' .planning/DESIGN.md` succeeds
    - `grep -qF 'active | obsolete | quarantined' .planning/DESIGN.md` OR all 3 lifecycle words present individually
    - **DESIGN-25 closed doc_type enum — all 9 values present:** `discovery`, `sow`, `platform_fnspec`, `integration_fnspec`, `tech_spec`, `test_plan`, `build_prompt`, `results`, `brain_spoke`
    - `grep -qF '__v' .planning/DESIGN.md` succeeds
    - `grep -qF '<client_slug>__<project_slug>__<doc_type>__v<N>' .planning/DESIGN.md` OR equivalent literal naming-convention example present
    - `grep -qF 'doc_published_at' .planning/DESIGN.md` AND `grep -qF 'last_diff_review_at' .planning/DESIGN.md` succeed
    - `grep -qF 'MOD-1' .planning/DESIGN.md` succeeds
    - `grep -qF 'Documentation:' .planning/DESIGN.md` succeeds
    - Hand-off messages verbatim from matrix: `grep -qF 'doc_published_at < last_diff_review_at' .planning/DESIGN.md` succeeds
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** Cross-references to DESIGN-28/29/30 from Stage 8 overview cited as anchor placeholders only — body verification deferred to Plan 02-09.
  </acceptance_criteria>
  <done>
    Stage 8 overview (DESIGN-24) + Stage 9 (DESIGN-25) populated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `## Stage 10: Native-AI enablement` (DESIGN-26) + `## Stage 11: Sign-off, brain update, archive` (DESIGN-27) + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1)
    - .planning/REQUIREMENTS.md §"DESIGN-26" + §"DESIGN-27"
    - .planning/research/PITFALLS.md — CRIT-8; MIN-4; MOD-9
    - .planning/AUDIT.md §AUDIT-04.4
    - .planning/DESIGN.md hand-off matrix Stage 10 → 11 row
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate Stage 10 + Stage 11 H2 sections.

    **(A) `## Stage 10: Native-AI enablement` (DESIGN-26)** — replace placeholder body:
    1. Echo line: `> **DESIGN-26:** Stage 10 Native-AI enablement — \`push-native-ai-knowledge\` skill reads Stage 4a + approved Stage 9 doc fragments + per-platform \`native-ai-inventory.md\`; branches on \`native_ai_path: api | paste | none\`; copy-paste fallback is the default; refuses to ingest if \`doc_published_at < last_diff_review_at\` (CRIT-8 fix); per-client target ID in \`00_HUB.md\` \`Pipefy AI:\` / \`Wrike AI:\` / \`Ziflow AI:\` blocks; refuses ingest if target mismatches \`client:\` frontmatter (MIN-4 fix); \`doc_version: <semver>\` + \`ingested_at: <ISO>\` per ingested doc.`
    2. Skill / Stage / Complexity: `push-native-ai-knowledge/` (NEW) / Stage 10 / High.
    3. **Inputs:** Frontmatter (`based_on_fnspec_platform: 04a_*` + `based_on_doc_diff:` (approved) + `client:` + `platform:`); upstream (`04a` + Stage 9 doc fragments approved per `last_diff_review_at:`); external (per-platform `native-ai-inventory.md`).
    4. **Outputs:** Per-platform native-AI knowledge ingestion records. Each ingested doc: `doc_version: <semver>` + `ingested_at: <ISO>` + `target_id: <platform-specific>`. Local artefact `10_native-ai-push_v<N>.md`.
    5. **Branching on `native_ai_path` (DESIGN-26 contract):**
       - `api`: Direct API ingestion. HIGH-confidence path.
       - `paste`: Copy-paste fallback — emits doc fragment formatted for paste, HALTS, prompts human, resumes when human confirms.
       - `none`: Skip — no native-AI surface for this doc fragment.
    6. **CRIT-8 refusal:** REFUSES to ingest any doc fragment whose source doc carries `doc_published_at < last_diff_review_at`. Hard halt.
    7. **MIN-4 refusal:** REFUSES if per-platform target ID does not match artefact's `client:` frontmatter. Hard halt.
    8. **Default: copy-paste fallback** — Per `## Out of Scope`. LOW-confidence native-AI capability → `paste` mode.
    9. **Downstream consumer:** sign-off-and-archive (Stage 11 — same wave).
    10. **Status flag:** `status: approved` on `10_native-ai-push_v<N>.md`.
    11. **Hand-off message:** verbatim from matrix Stage 10 → 11 row.
    12. **Key v2 decisions** (5 numbered): native_ai_path branching; CRIT-8 refusal; MIN-4 refusal; copy-paste default; per-doc traceability.
    13. **Dependencies:** DESIGN-14/15/16; DESIGN-25 (Stage 9 fields); platform-pipefy/-wrike/-ziflow.
    14. **Cross-references:** DESIGN-25 (CRIT-8 paired); DESIGN-27 (downstream same wave); PITFALLS CRIT-8 + MIN-4; AUDIT.md §AUDIT-04.

    **(B) `## Stage 11: Sign-off, brain update, archive` (DESIGN-27)** — replace placeholder body:
    1. Echo line: `> **DESIGN-27:** Stage 11 Sign-off, brain update, archive — \`sign-off-and-archive\` skill updates local \`<Client> Brain/<spokes>/\`; one-way push to Coda mirror with brain-mirror Coda doc template (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes); \`tone_lint\` pass before publish (MOD-9 prevention); CR move to \`Archive/\`; \`00_Index.md\` version bump; Field Notes table preserved (input-only, never overwritten); pre-archive sanity check (no orphan refs, no missing artefacts).`
    2. Skill / Stage / Complexity: `sign-off-and-archive/` (NEW) / Stage 11 / Medium.
    3. **Inputs:** Frontmatter (all upstream artefacts approved; `cr_id:`); upstream (entire CR's artefact set); external (Coda MCP; brain-mirror Coda doc template per Phase 4 OPEN-05).
    4. **Outputs:** Updated `<Client> Brain/<spokes>/`; Coda mirror updated; CR moved to `<Client> Brain/Archive/<CR>/`; `00_Index.md` version bump; `11_signoff_v<N>.md`. Frontmatter: standard + `archived_at: <ISO>` + `tone_lint_status: passed | failed`.
    5. **Brain-mirror Coda doc template (DESIGN-27 contract — 7 canonical sections):** Overview, Workflows, Platforms, Integrations, Operating Model, Change History, Field Notes. Push direction: ONE-WAY (local → Coda). Field Notes section is INPUT-ONLY in Coda.
    6. **`tone_lint` pass (MOD-9 prevention):** Runs against EACH spoke (per DESIGN-10 forbidden-phrasings list). Failure halts.
    7. **CR move to Archive/:** Source CR folder moved to `<Client> Brain/<Project>/Archive/<CR>/`.
    8. **`00_Index.md` version bump:** appends new entry per archived CR.
    9. **Pre-archive sanity check:** verifies no orphan references, no missing files, all upstream references resolve. Failure halts.
    10. **Field Notes preserved input-only:** Coda Field Notes table NEVER overwritten. Per DESIGN-09.
    11. **Downstream consumer:** None within v2 pipeline (terminal). Outputs feed back into next CR's Stage 1.
    12. **Status flag:** `status: approved` on `11_signoff_v<N>.md`.
    13. **Hand-off message:** Pipeline-end message — `> Pipeline complete for CR <cr_id>. Brain updated; Coda mirror published; CR archived. Next CR's Stage 1 reads from updated <Client> Brain/.`
    14. **Key v2 decisions** (6 numbered): brain-mirror 7-section template; one-way Coda mirror; tone_lint MOD-9; CR archive + 00_Index version bump; pre-archive sanity check; Field Notes preserved input-only.
    15. **Dependencies:** DESIGN-09; DESIGN-10; DESIGN-26 (upstream); Phase 4 OPEN-05.
    16. **Cross-references:** DESIGN-09, DESIGN-10, DESIGN-25; PITFALLS MOD-9; Phase 4 OPEN-05 + OPEN-04.

    **(C) Run structural-check** at end. Expected: exits 1 (echo count = 29/30; Plan 02-09 + 02-10 land remaining).

    **Implementing decisions:** D-20, D-26, D-32/D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-26:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-27:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'push-native-ai-knowledge' .planning/DESIGN.md &amp;&amp; grep -qF 'sign-off-and-archive' .planning/DESIGN.md &amp;&amp; grep -qF 'native_ai_path: api | paste | none' .planning/DESIGN.md &amp;&amp; grep -qF 'CRIT-8' .planning/DESIGN.md &amp;&amp; grep -qF 'MIN-4' .planning/DESIGN.md &amp;&amp; grep -qF 'MOD-9' .planning/DESIGN.md &amp;&amp; grep -qF 'tone_lint' .planning/DESIGN.md &amp;&amp; grep -qF 'brain-mirror' .planning/DESIGN.md &amp;&amp; grep -qF 'Operating Model' .planning/DESIGN.md &amp;&amp; grep -qF 'Change History' .planning/DESIGN.md &amp;&amp; grep -qF 'Field Notes' .planning/DESIGN.md &amp;&amp; grep -qF '00_Index.md' .planning/DESIGN.md &amp;&amp; grep -qF '10_native-ai-push_v' .planning/DESIGN.md &amp;&amp; grep -qF '11_signoff_v' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "29" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo STAGES_10_11_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-26:\*\*' .planning/DESIGN.md` AND `grep -qE '^> \*\*DESIGN-27:\*\*' .planning/DESIGN.md` succeed
    - `grep -qF 'push-native-ai-knowledge' .planning/DESIGN.md` AND `grep -qF 'sign-off-and-archive' .planning/DESIGN.md` succeed
    - `grep -qF 'native_ai_path: api | paste | none' .planning/DESIGN.md` succeeds
    - `grep -qF 'CRIT-8' .planning/DESIGN.md` succeeds
    - `grep -qF 'MIN-4' .planning/DESIGN.md` succeeds
    - `grep -qF 'MOD-9' .planning/DESIGN.md` succeeds
    - `grep -qF 'tone_lint' .planning/DESIGN.md` succeeds
    - **DESIGN-27 brain-mirror template — all 7 sections present:** `Overview`, `Workflows`, `Platforms`, `Integrations`, `Operating Model`, `Change History`, `Field Notes`
    - `grep -qF '00_Index.md' .planning/DESIGN.md` succeeds
    - `grep -qF '10_native-ai-push_v' .planning/DESIGN.md` succeeds
    - `grep -qF '11_signoff_v' .planning/DESIGN.md` succeeds
    - `grep -qF 'doc_version' .planning/DESIGN.md` AND `grep -qF 'ingested_at' .planning/DESIGN.md` succeed
    - `grep -qF 'pre-archive sanity check' .planning/DESIGN.md` succeeds
    - `grep -qF 'Field Notes preserved' .planning/DESIGN.md` OR `grep -qF 'Field Notes table is NEVER overwritten' .planning/DESIGN.md` succeeds
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `29`
    - `bash design-structure-check.sh` exits with code `1` (mid-phase)
    - No new `[OPEN]` markers in Stages 8–11
  </acceptance_criteria>
  <done>
    Stages 8 (overview), 9, 10, 11 decision contracts populated. Echo count = 29/30.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 9 doc_published_at + last_diff_review_at fields → Stage 10 CRIT-8 refusal | The two fields must be set in Stage 9 BEFORE Stage 10 reads them. |
| Stage 10 per-platform target ID → MIN-4 cross-client contamination prevention | Field naming or formatting drift breaks MIN-4 refusal. |
| Stage 11 Coda one-way mirror → Field Notes preservation | If Stage 11 writes to Field Notes table, the canonical input-only contract per DESIGN-09 is broken. |
| Forward references to DESIGN-28/29/30 from Stage 8 overview — populated in Plan 02-09 (cross-AI review MEDIUM #6) | Cross-references cite the ID only; body verification deferred. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | DESIGN-25 doc_type enum has fewer/extra values vs REQUIREMENTS canonical 9 | mitigate | Acceptance criteria assert all 9 enum values as literal substrings. |
| T-02-08-02 | Tampering | DESIGN-25 naming convention drifts to single underscore | mitigate | Acceptance criteria assert literal `__v` + full convention example. |
| T-02-08-03 | Tampering | DESIGN-26 native_ai_path uses reverse order | mitigate | Acceptance criteria assert literal `native_ai_path: api | paste | none` (canonical order). |
| T-02-08-04 | Tampering | DESIGN-26 omits `paste` default → optimistic-API anti-pattern | mitigate | Action explicitly states "copy-paste fallback IS the default" + cites `## Out of Scope`. |
| T-02-08-05 | Tampering | DESIGN-27 brain-mirror template missing one of the 7 sections | mitigate | Acceptance criteria assert all 7 section names. |
| T-02-08-06 | Tampering | DESIGN-27 Field Notes preservation contract reversed | mitigate | Acceptance criteria assert `Field Notes preserved` OR `never overwritten`. |
| T-02-08-07 | Tampering | Stage 8 overview drifts into Plan 02-09's DESIGN-28..30 territory | mitigate | Action explicitly states "Stage-level overview only". |
| T-02-08-08 | Tampering | Acceptance criteria assert content from forward-referenced DESIGN-28/29/30 that doesn't exist yet — false-fail at end of Wave 8 | mitigate | Cross-AI review MEDIUM #6 fix: forward references are anchor placeholders only. |
</threat_model>

<verification>
End of Plan 02-08 success-state:
1. Stages 8 (overview) + 9 + 10 + 11 H2 sections populated
2. DESIGN-24, 25, 26, 27 echo lines added (4 new)
3. Echo count = 29/30
4. `bash design-structure-check.sh` exits 1 with assertion #4 failing
5. Inline `[OPEN]` count unchanged from end of Plan 02-07
6. Stage 8 overview is overview-only — no DESIGN-28..30 detail leaked

Manual reviewer attestation:
- [ ] DESIGN-25 closed `doc_type` enum has exactly 9 values
- [ ] DESIGN-25 naming uses double-underscore separator
- [ ] DESIGN-26 native_ai_path enum order matches REQUIREMENTS
- [ ] DESIGN-26 default mode is `paste`
- [ ] DESIGN-27 brain-mirror template enumerates all 7 sections
- [ ] DESIGN-27 Field Notes preservation contract maintained
- [ ] Stage 8 overview does NOT contain DESIGN-28..30 detail
</verification>

<success_criteria>
- [ ] DESIGN-24/25/26/27 echo lines + decision contracts populated
- [ ] DESIGN-25: closed doc_type enum (9 values) + double-underscore naming + reviewer-approval gate + doc_published_at + MOD-1
- [ ] DESIGN-26: native_ai_path branching + CRIT-8 + MIN-4 refusals + copy-paste default
- [ ] DESIGN-27: brain-mirror 7-section template + tone_lint MOD-9 + CR archive + 00_Index bump + Field Notes preservation
- [ ] Echo count = 29/30
- [ ] No new `[OPEN]` markers in Stages 8–11
- [ ] Structural-check exits 1 (1 echo short of 30)
- [ ] No content under `dydx-delivery/` modified
- [ ] Forward-reference guardrails applied (cross-AI review MEDIUM #6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-08-SUMMARY.md` documenting:
- Stage 8 overview (skills + persistent harness + sandbox-allowlist Coda extension CRIT-5 + failure-class enum + sandbox_lock + lifecycle states)
- Stage 9: 9-value doc_type enum + double-underscore naming + reviewer-approval gate + doc_published_at + MOD-1
- Stage 10: native_ai_path branching + CRIT-8 + MIN-4 refusals + copy-paste default
- Stage 11: brain-mirror 7 sections + tone_lint MOD-9 + Field Notes preservation + 00_Index bump + pre-archive sanity
- Echo count: 29/30
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #6 (forward-reference guardrails — cross-references to DESIGN-28/29/30 from Stage 8 overview are anchor placeholders only).
- Pointer: "Plan 02-09 (Wave 9) populates `## Test bot architecture` (DESIGN-28, 29, 30)."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-08-SUMMARY.md` to `files_modified`); MEDIUM #6 (forward-reference guardrails — explicit `<objective>` callout + threat T-02-08-08 added; cross-references to DESIGN-28/29/30 from Stage 8 overview are anchor placeholders only at end of Wave 8). Existing safe `awk '/^## Stage 8:/,/^## Stage 9:/'` patterns kept (distinct start/end anchors — not the broken `/^## X/,/^## /` pattern). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-24/25/26/27 still in `requirements`; D-20/26/32/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
