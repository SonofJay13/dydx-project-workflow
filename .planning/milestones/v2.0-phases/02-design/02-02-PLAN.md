---
plan_id: 02-02
phase: 02-design
plan: 02
type: execute
wave: 2
depends_on: [02-01]
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06, DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10]
files_modified:
  - .planning/phases/02-design/02-02-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-02-SUMMARY.md
autonomous: true
tags: [design, cross-cutting, status-survey, persona]

must_haves:
  truths:
    - "DESIGN.md `## Cross-cutting decisions` section opens with a 10-row decision-summary table (one row per DESIGN-01..10) per D-19 matrix-then-prose, then per-decision prose under H3 anchors `### DESIGN-01` through `### DESIGN-10`."
    - "Each of the 10 prose subsections opens with a `> **DESIGN-NN:** <plain-English statement>` blockquote echo line per D-35 (10 echo lines added in this plan; remaining 20 added by plans 02-03..02-09)."
    - "DESIGN-08 cross-cuts to the `### Live status-lifecycle survey` H3 (already anchored by Plan 02-01) which now contains methodology + concrete sampled-folder list + observed `status:` value enumeration confirming canonical lifecycle (`draft → client_review → approved → archived`) does not orphan any live value (D-25)."
    - "DESIGN-10 persona contract subsection states ~5 voice principles + forbidden-phrasings list inline; full 3 worked before/after examples land in `## Appendix C: Persona contract worked examples` (D-29)."
    - "DESIGN-22 risk-multiplier deferral is REFERENCED from DESIGN-08 / DESIGN-22 cross-cutting context (the seed `[OPEN]` marker placed by Plan 02-01 is preserved; actual Stage 6 contract lands in Plan 02-07)."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-01..10 cross-cutting contracts + status-lifecycle survey + Appendix C persona examples populated"
      contains: "10-row decision-summary table; 10 H3 subsections (### DESIGN-01..DESIGN-10) each with > **DESIGN-NN:** echo; status-survey methodology + result; 5+ persona principles + forbidden-phrasings list; 3 before/after persona examples"
  key_links:
    - from: "DESIGN-01 frontmatter scheme prose"
      to: "Plan 02-03 hand-off matrix (Frontmatter fields propagated column references DESIGN-01 field names)"
      via: "underscore-snake-case key convention; status lifecycle vocabulary"
      pattern: "status: (draft|client_review|approved|archived)"
    - from: "DESIGN-08 migration co-existence prose"
      to: "### Live status-lifecycle survey H3 (D-25)"
      via: "DESIGN-08 contract cites survey result to confirm canonical lifecycle does not orphan live values"
      pattern: "see `### Live status-lifecycle survey`"
    - from: "DESIGN-10 persona contract prose"
      to: "## Appendix C: Persona contract worked examples (D-29)"
      via: "principles + forbidden list inline; 3 before/after examples in Appendix C"
      pattern: "see `## Appendix C`"
---

<objective>
Populate `.planning/DESIGN.md` cross-cutting decisions (DESIGN-01..10), the status-lifecycle survey under D-25, and Appendix C persona examples.

**Purpose:** Locks the 10 cross-cutting structural contracts that ground every later section — frontmatter scheme, stage numbering, hard-rules SoT, plugin surfaces, /refine resolution, approval-gate enforcement, connector probe + degradation matrix, frontmatter migration co-existence, directional boundary, persona contract. Per D-31 author flow, cross-cutting comes first because it grounds the rest.

**Output:** `.planning/DESIGN.md` with the `## Cross-cutting decisions` section + `### Live status-lifecycle survey` H3 + `## Appendix C: Persona contract worked examples` populated. 10 of the 30 DESIGN-NN echo lines added (DESIGN-01..10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-DISCUSSION-LOG.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- DESIGN.md is the single deliverable. This plan writes:
     1. The 10-row decision-summary table at the top of `## Cross-cutting decisions`.
     2. 10 H3 subsections (### DESIGN-01..DESIGN-10) each with success-criteria echo + prose contract.
     3. Body of `### Live status-lifecycle survey` (already anchored by Plan 02-01).
     4. Body of `## Appendix C: Persona contract worked examples` (already anchored by Plan 02-01).
     The structural-check script (Plan 02-01) gains 10 of its required 30 echo lines from this plan.

     Section-range extraction: this plan uses the same stateful section_between helper pattern as the
     structural-check script (Plan 02-01) when scoping persona-phrase greps to the DESIGN-10 H3 region.
     Per cross-AI review HIGH #1, naive `awk '/^## X/,/^## /'` ranges are forbidden — they capture only
     the header. Use the inline stateful awk pattern shown in `<acceptance_criteria>` below. -->

Decision-summary table shape (D-19 matrix-then-prose):

| ID | Decision area | Locked contract (one line) | Locking decision |
|----|---------------|-----------------------------|------------------|
| DESIGN-01 | Frontmatter scheme | `frontmatter_version: 2`; status `draft → client_review → approved → archived`; underscore-snake-case keys; platform-gated identifiers | D-25 (survey) |
| DESIGN-02 | Stage numbering | File-prefix = stage number `01..11`; substages `4a/4b/7a/7b/8a..8d`; canonical at `dydx-delivery/references/stage-numbering.md` | — |
| DESIGN-03 | Hard-rules SoT | Single SoT at `dydx-delivery/references/safety-rules.md` + per-client `safety-overrides.yaml` overlay (only `overridable: true` fields) | AUDIT.md §AUDIT-05 (4 hard-rules duplicates collapsed) |
| DESIGN-04 | Plugin surfaces | `commands/`: 1 parameterised `refine.md` + 4 GSD shortcuts; `agents/`: 1 test-bot-orchestrator; `hooks/`: 2 (`validate-frontmatter` + `bump-artefact-version`, NOT auto-progression); `mcpServers` field; manifest `2.0.0`; pytest self-tests at `dydx-delivery/tests/` per D-24 | D-24 |
| DESIGN-05 | `/refine-<skill>` resolution | Single parameterised `commands/refine.md` taking skill name as `$1`; namespace `/dydx-refine-*` (NOT bare `/refine-*`) | D-23 |
| DESIGN-06 | Approval-gate enforcement | Mandatory `approved_by` + `approved_at` on `status: approved` writes; hook refuses writes lacking these fields | — |
| DESIGN-07 | Connector probe + degradation | Session-start MCP probe + per-stage fallback per AUDIT.md §AUDIT-03 PITFALLS-cited fallback table | AUDIT.md §AUDIT-03 |
| DESIGN-08 | Frontmatter migration co-existence | CR-driven opt-in; `client_review` retained (live in `generate-sow`); v0.3.0 absent → lenient mode; in-flight artefacts NEVER auto-flip | D-25 (survey result locks) |
| DESIGN-09 | Directional boundary | Local `<Client> Brain/` canonical; Coda one-way mirror; Field Notes read-only triage queue; never auto-merged | — |
| DESIGN-10 | Persona contract | ~5 senior-implementer voice principles + forbidden-phrasings list inline; 3 worked before/after examples in Appendix C | D-29 |

Per-DESIGN H3 subsection shape (mirrors AUDIT.md per-skill subsection — Phase 1 D-12 precedent):

```markdown
### DESIGN-01 — Canonical frontmatter scheme

> **DESIGN-01:** Canonical frontmatter scheme — status lifecycle, field-name convention, platform-gated identifiers, frontmatter_version semantics.

**Contract.**

- `frontmatter_version: 2` on every new artefact; absent value → v0.3.0 lenient mode (per DESIGN-08).
- Status lifecycle (canonical): `draft → client_review → approved → archived`. `client_review` retained per AUDIT.md §AUDIT-01 (live in `generate-sow:54`); see `### Live status-lifecycle survey` for confirmation no live value is orphaned.
- Field-name convention: underscore-snake-case for keys (`based_on_kickoff`, `pipe_id`); hyphen-kebab-case for file paths inside `based_on_*` values (`02_kickoff_v1`).
- Platform-gated identifiers: `pipe_id` / `space_id` / `project_id` only present when the artefact's `platform:` value is active. Carrying `pipe_id` on a Wrike artefact is a validation failure per the `validate-frontmatter` hook (DESIGN-04).
- `frontmatter_version: 2` lenient-mode reader behaviour per DESIGN-08.

**Cross-references.** DESIGN-04 hook implementation; DESIGN-06 approval-gate field requirements; DESIGN-08 migration semantics.
```

(See per-DESIGN concrete contracts in `<action>` Task 2 — verbatim wording the executor writes.)

**Status-lifecycle survey methodology + result (D-25)** — runs as part of THIS plan, populates the `### Live status-lifecycle survey` H3 already anchored by Plan 02-01:

Methodology: Sample live `<Client> Brain/` folders accessible from this workspace. For each folder, enumerate every artefact's frontmatter `status:` value. If no live folders are reachable, fall back to v0.3.0-source enumeration (grep `status:` references in v0.3.0 SKILL.md files). Per CONTEXT D-25.

Survey output shape (in DESIGN.md):

```markdown
### Live status-lifecycle survey

**Methodology.** [Concrete description of what was sampled — folders if reachable, v0.3.0 sources if not. Cite each source `file:line` per D-32.]

**Sampled sources.**

| Source | Type | `status:` values observed |
|--------|------|---------------------------|
| dydx-delivery/skills/generate-sow/SKILL.md:54 | v0.3.0 source | `client_review` |
| ... | ... | ... |

**Distinct values found:** `{draft, client_review, approved}` (or whatever was found).

**Reconciliation against canonical `{draft, client_review, approved, archived}`:**
- (per-value reconciliation lines)

**Conclusion.** Canonical lifecycle `draft → client_review → approved → archived` does not orphan any observed `status:` value. DESIGN-08 contract is locked.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run status-lifecycle survey + populate `### Live status-lifecycle survey` section + write `## Cross-cutting decisions` opening (decision-summary table)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current skeleton state from Plan 02-01)
    - .planning/AUDIT.md §AUDIT-01 (skill brittleness findings — source for status enumeration if no live folders)
    - .planning/AUDIT.md §AUDIT-05 (hard-rules duplication finding — DESIGN-03 cites this)
    - .planning/phases/02-design/02-CONTEXT.md §"Frontmatter migration cutover" D-25; §"Document structure" D-19; §"Cross-cutting authoring decisions" D-32, D-34, D-35
    - .planning/REQUIREMENTS.md §"Cross-cutting structural decisions" DESIGN-01..10
    - dydx-delivery/skills/generate-sow/SKILL.md (only the lines needed for `status:` enumeration if no live folders)
    - dydx-delivery/skills/discovery-intake/SKILL.md
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` (already exists from Plan 02-01) — populate three regions in this order:

    **(A) Status-lifecycle survey first (D-25)** — replace the placeholder body of the existing `### Live status-lifecycle survey` H3 with the survey methodology + sampled-sources table + distinct-values-found + reconciliation + conclusion per the survey-output shape in `<interfaces>` above.

    Survey execution:
    1. Check for reachable `<Client> Brain/` folders. Use `find / -name 'Brain' -type d 2>/dev/null | head -5`. If unreachable, document transparently and fall back to v0.3.0-source enumeration.
    2. For each sampled source, record `file:line` and the `status:` value(s). Sources to grep at minimum:
       - `dydx-delivery/skills/generate-sow/SKILL.md`
       - `dydx-delivery/skills/discovery-intake/SKILL.md`
       - `dydx-delivery/skills/generate-functional-spec/SKILL.md`
       - `dydx-delivery/skills/generate-build-prompt/SKILL.md`
       - `dydx-delivery/skills/execute-tests/SKILL.md`
    3. Tally distinct `status:` values found.
    4. Reconcile against canonical `{draft, client_review, approved, archived}`. Flag non-canonical values for DESIGN-08 prose.
    5. Write the conclusion paragraph.

    **(B) Decision-summary table (D-19)** — under the existing `## Cross-cutting decisions` H2 (after the existing `frontmatter_version: 2` placeholder line), insert a 10-row decision-summary table per `<interfaces>` shape above.

    Keep the existing `frontmatter_version: 2` literal line in place.

    **(C) Cross-cutting section preamble** — between the H2 and the decision-summary table, add a one-paragraph prose preamble: "These ten cross-cutting decisions ground every later section. Each is locked here as a contract — substages that depend on a contract cite it back rather than re-arguing the decision. Per D-19, this section opens with a scannable decision-summary table; per-decision prose follows under H3 anchors below."

    **Implementing decisions:** D-19, D-25 (survey runs IN this phase), D-32 (`file:line` citations), D-34.
  </action>
  <verify>
    <automated>grep -qF '### Live status-lifecycle survey' .planning/DESIGN.md &amp;&amp; grep -qE '^\*\*Methodology\.\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^\*\*Sampled sources\.\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^\*\*Conclusion\.\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^\| ID \| Decision area \| Locked contract' .planning/DESIGN.md &amp;&amp; SUMMARY_BODY=$(awk -v hdr='## Cross-cutting decisions' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md) &amp;&amp; ROW_COUNT=$(printf '%s\n' "$SUMMARY_BODY" | grep -cE '^\| DESIGN-[0-9]{2} \|') &amp;&amp; [ "$ROW_COUNT" -ge "10" ] &amp;&amp; echo SURVEY_AND_TABLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF '### Live status-lifecycle survey' .planning/DESIGN.md` succeeds
    - `grep -qE '^\*\*Methodology\.\*\*' .planning/DESIGN.md` succeeds
    - `grep -qE '^\*\*Sampled sources\.\*\*' .planning/DESIGN.md` succeeds
    - `grep -qE '^\*\*Distinct values found:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qE '^\*\*Reconciliation against canonical' .planning/DESIGN.md` succeeds
    - `grep -qE '^\*\*Conclusion\.\*\*' .planning/DESIGN.md` succeeds
    - `grep -cE 'file:line|`[a-zA-Z0-9_/.-]+:[0-9]+`' .planning/DESIGN.md` returns >= 1 (D-32 citation)
    - **Decision-summary table row count uses stateful section extraction** (per cross-AI review HIGH #1):
      ```bash
      awk -v hdr='## Cross-cutting decisions' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md | grep -cE '^\| DESIGN-[0-9]{2} \|'
      ```
      Returns >= 10.
    - The DESIGN-01 literal sentinel `frontmatter_version: 2` is still present (`grep -qF 'frontmatter_version: 2' .planning/DESIGN.md`)
    - Cross-cutting preamble paragraph mentions "ten cross-cutting decisions" and "D-19" anchor reference
  </acceptance_criteria>
  <done>
    Status-lifecycle survey is executed, methodology + sampled sources + distinct values + reconciliation + conclusion are written into the H3 placeholder. Decision-summary table is inserted under `## Cross-cutting decisions` with 10 rows. Cross-cutting preamble paragraph in place. The `frontmatter_version: 2` sentinel is preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 10 H3 subsections (### DESIGN-01 through ### DESIGN-10) with success-criteria echo + concrete contract prose + Appendix C persona examples</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1)
    - .planning/REQUIREMENTS.md §"Cross-cutting structural decisions" DESIGN-01..10
    - .planning/phases/02-design/02-CONTEXT.md §"Plugin self-tests" D-24; §"/refine resolution" D-23; §"Persona contract concreteness" D-29; §"Cross-cutting authoring decisions" D-33, D-34, D-35
    - .planning/AUDIT.md §AUDIT-01, §AUDIT-04, §AUDIT-05, §AUDIT-06, §AUDIT-07, §AUDIT-08
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — under the cross-cutting section (after the decision-summary table from Task 1, before the `### Live status-lifecycle survey` H3), insert 10 H3 subsections in numeric order.

    For each H3, follow the per-DESIGN H3 subsection shape shown in `<interfaces>` above:
    1. **H3 heading line** (e.g., `### DESIGN-01 — Canonical frontmatter scheme`)
    2. **Blank line.**
    3. **Echo blockquote line** (D-35): `> **DESIGN-NN:** <plain-English statement>`. Exact echo per section:
       - `> **DESIGN-01:** Canonical frontmatter scheme — status lifecycle, field-name convention, platform-gated identifiers, frontmatter_version semantics.`
       - `> **DESIGN-02:** Canonical stage-numbering scheme — file-prefix as stage number, substages, canonical reference doc, old→new mapping.`
       - `> **DESIGN-03:** Single source of truth for hard rules at \`dydx-delivery/references/safety-rules.md\` plus per-client overrides overlay.`
       - `> **DESIGN-04:** Plugin surfaces — commands/agents/hooks/mcpServers/version + plugin self-tests (D-24).`
       - `> **DESIGN-05:** \`/refine-<skill>\` resolution — single parameterised \`commands/refine.md\`, namespace \`/dydx-refine-*\` (D-23).`
       - `> **DESIGN-06:** Approval-gate enforcement — mandatory \`approved_by\` + \`approved_at\` on \`status: approved\` writes, hook refuses otherwise.`
       - `> **DESIGN-07:** Connector-availability probe + per-stage graceful-degradation matrix.`
       - `> **DESIGN-08:** Frontmatter migration co-existence — CR-driven opt-in, \`client_review\` retained, lenient mode for v0.3.0 absent (D-25 survey locks).`
       - `> **DESIGN-09:** Directional-boundary contract — local canonical, Coda one-way mirror, Field Notes read-only triage queue.`
       - `> **DESIGN-10:** Persona contract — senior-implementer voice principles + forbidden-phrasings list inline + 3 worked examples in Appendix C (D-29).`
    4. **`**Contract.**` line** followed by concrete contract bullets (per `<interfaces>` per-DESIGN concrete contracts).
    5. **`**Cross-references.**` line** listing related DESIGN-NN sections.

    Concrete contracts (verbatim source per cross-DESIGN):
    - **DESIGN-01:** `frontmatter_version: 2`; absent → v0.3.0 lenient mode; status lifecycle (canonical); underscore-snake-case keys; hyphen-kebab-case file paths; platform-gated `pipe_id` / `space_id` / `project_id`.
    - **DESIGN-02:** File-prefix = stage number; canonical map per stage (`01_kickoff_*` ... `11_signoff_*`); canonical reference at `dydx-delivery/references/stage-numbering.md`; old→new mapping table.
    - **DESIGN-03:** Single SoT at `dydx-delivery/references/safety-rules.md`; per-client overlay at `<Client> Brain/safety-overrides.yaml` overlays ONLY `overridable: true` fields. Skills inline a one-line summary plus pointer.
    - **DESIGN-04:** `commands/`: 1 parameterised `refine.md` + 4 GSD-prefixed shortcuts; `agents/`: 1 (`test-bot-orchestrator.md`); `hooks/`: 2 (`validate-frontmatter.py`, `bump-artefact-version.py`); `mcpServers` field listing 5 wired MCPs; manifest `2.0.0`. **Plugin self-tests subsection (per D-24):** pytest at `dydx-delivery/tests/` covering validate-frontmatter (positive+negative), bump-artefact-version (idempotency), frontmatter parser (status-lifecycle/platform-gated/frontmatter_version handling). Run on every PR.
    - **DESIGN-05:** Single parameterised `commands/refine.md` taking skill name as `$1`. Namespace `/dydx-refine-*` (NOT bare `/refine-*` — D-23). Cite AUDIT.md §AUDIT-04.2 orphan-reference finding.
    - **DESIGN-06:** Mandatory `approved_by` + `approved_at` on `status: approved` writes; `validate-frontmatter` hook refuses writes lacking these.
    - **DESIGN-07:** Session-start MCP probe (per AUDIT.md §AUDIT-08 precedent). Per-stage graceful-degradation matrix (10 rows, lifted from PITFALLS).
    - **DESIGN-08:** v2 readers tolerate v0.3.0 frontmatter via `frontmatter_version` field (absent → lenient mode). CR-driven opt-in upgrade. `client_review` retained (live in `generate-sow:54` per AUDIT.md §AUDIT-01.2). Cross-reference `### Live status-lifecycle survey` for canonical-lifecycle reconciliation.
    - **DESIGN-09:** Local `<Client> Brain/` canonical. Coda is one-way mirror (Stage 11 push). Field Notes table read-only triage queue. Field Notes NEVER auto-merged.
    - **DESIGN-10:** 5 voice principles + forbidden-phrasings list. Forbidden phrasings (verbatim): "we recommend", "as an AI", "I would suggest", "perhaps consider", "might want to", "it's worth noting", "please be aware", "in order to", "make sure to", "feel free to". 3 worked before/after examples → Appendix C.

    **Special handling per DESIGN:**
    - **DESIGN-04** must include the `**Plugin self-tests subsection (per D-24)**` callout with concrete pytest scope.
    - **DESIGN-05** must explicitly state `/dydx-refine-*` namespace decision and cite D-23.
    - **DESIGN-08** must cross-reference `### Live status-lifecycle survey` (above). Add `[MIGRATION-RISK: ...]` marker only IF survey found a non-canonical value.
    - **DESIGN-10** prose ends with "see `## Appendix C: Persona contract worked examples`".

    **Inline `[OPEN]` markers in cross-cutting (D-27):** None expected in DESIGN-01..10.

    **Implementing decisions:** D-19, D-23, D-24, D-29, D-32, D-33, D-34, D-35.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### DESIGN-(0[1-9]|10) — ' .planning/DESIGN.md)" -eq "10" ] &amp;&amp; [ "$(grep -cE '^> \*\*DESIGN-(0[1-9]|10):\*\*' .planning/DESIGN.md)" -eq "10" ] &amp;&amp; grep -qF 'dydx-delivery/references/safety-rules.md' .planning/DESIGN.md &amp;&amp; grep -qF '/dydx-refine-*' .planning/DESIGN.md &amp;&amp; grep -qF 'dydx-delivery/tests/' .planning/DESIGN.md &amp;&amp; grep -qF 'approved_by' .planning/DESIGN.md &amp;&amp; grep -qF 'approved_at' .planning/DESIGN.md &amp;&amp; echo TEN_H3_SECTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^### DESIGN-(0[1-9]|10) — ' .planning/DESIGN.md` returns exactly `10`
    - `grep -cE '^> \*\*DESIGN-(0[1-9]|10):\*\*' .planning/DESIGN.md` returns exactly `10`
    - `grep -qE '^### DESIGN-01 — Canonical frontmatter scheme$' .planning/DESIGN.md` succeeds
    - `grep -qE '^### DESIGN-04 — Plugin surfaces$' .planning/DESIGN.md` succeeds
    - `grep -qE '^### DESIGN-10 — Persona contract$' .planning/DESIGN.md` succeeds
    - `grep -qF 'dydx-delivery/references/safety-rules.md' .planning/DESIGN.md` succeeds
    - `grep -qF 'safety-overrides.yaml' .planning/DESIGN.md` succeeds
    - `grep -qF '/dydx-refine-*' .planning/DESIGN.md` succeeds
    - `grep -qF 'dydx-delivery/tests/' .planning/DESIGN.md` succeeds
    - `grep -qF 'pytest' .planning/DESIGN.md` succeeds
    - `grep -qF 'approved_by' .planning/DESIGN.md` AND `grep -qF 'approved_at' .planning/DESIGN.md` succeed
    - `grep -qF 'validate-frontmatter' .planning/DESIGN.md` AND `grep -qF 'bump-artefact-version' .planning/DESIGN.md` succeed
    - `grep -qF 'mcpServers' .planning/DESIGN.md` succeeds
    - `grep -qF '2.0.0' .planning/DESIGN.md` succeeds
    - `grep -qF 'frontmatter_version' .planning/DESIGN.md` returns >= 2 occurrences
    - `grep -qF 'AUDIT.md §AUDIT-' .planning/DESIGN.md` returns >= 5
    - `grep -qF 'Field Notes' .planning/DESIGN.md` succeeds
    - `grep -qF 'one-way' .planning/DESIGN.md` succeeds
    - **DESIGN-10 forbidden-phrasings count — SCOPED to the DESIGN-10 H3 region only** (per cross-AI review MEDIUM #5 — avoids future false positives if broader linting is added):
      ```bash
      awk -v hdr='### DESIGN-10 — Persona contract' 'f && /^### /{exit} f{print} index($0,hdr)==1 && /^### /{f=1}' .planning/DESIGN.md \
        | grep -cE '"(we recommend|as an AI|I would suggest|perhaps consider|might want to|it'\''s worth noting|please be aware|in order to|make sure to|feel free to)"'
      ```
      Returns >= 5. (Forbidden phrasings appear only inside the DESIGN-10 region — not as broad document-wide grep.)
    - DESIGN-10 prose ends with reference to Appendix C (`grep -qF 'Appendix C' .planning/DESIGN.md` succeeds)
  </acceptance_criteria>
  <done>
    All 10 H3 subsections written with echo line + concrete contract bullets + cross-references. DESIGN-04 carries the plugin self-tests subsection; DESIGN-05 locks the `/dydx-refine-*` namespace; DESIGN-08 cross-references the survey result; DESIGN-10 prose carries principles + forbidden list (scoped to DESIGN-10 H3 region) and points at Appendix C.
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate `## Appendix C: Persona contract worked examples` with 3 before/after examples + run structural-check (assertion #4 echo count progress check)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 2 — DESIGN-10 prose body in place)
    - .planning/AUDIT.md §AUDIT-07 (truncated README + "test sheet" — exact `file:line` for examples 1 and 2)
    - .planning/AUDIT.md §AUDIT-01 (skill brittleness — pick one v0.3.0 SKILL.md hedging case for example 3)
    - .planning/phases/02-design/02-CONTEXT.md §"Persona contract concreteness" D-29
    - .planning/phases/02-design/scripts/design-structure-check.sh — run at end
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — under the existing `## Appendix C: Persona contract worked examples` H2, replace the placeholder body with 3 worked before/after examples per D-29.

    Each example shape:

    ```markdown
    ### Example N — <short label>

    **Source:** AUDIT.md §AUDIT-NN.M (or `file:line` per D-32).

    **Before (v0.3.0-style):**
    > <verbatim or representative bad-voice passage>

    **After (v2 senior-implementer voice):**
    > <rewritten passage>

    **Why the rewrite works:** <one or two sentences naming which voice principle from DESIGN-10 the rewrite satisfies and which forbidden phrasing it eliminates>.
    ```

    The 3 examples (per D-29):

    **Example 1 — Truncated changelog/README sentence.** Source: `AUDIT.md §AUDIT-07.1`. Before: hedged "we recommend that the README be updated". After: imperative — `Update README:14 — replace truncated sentence ending mid-clause with completed claim.` Why: eliminates "we recommend"; satisfies "no AI hedging" + "specific over abstract".

    **Example 2 — "Test sheet" residual.** Source: `AUDIT.md §AUDIT-07.2`. Before: hedged "Note that the document references a test sheet which may need attention." After: locked rewrite — `Replace \`test sheet\` (results-template.md:23) with \`test-results table\` — \`test sheet\` is v0.1.0 vocabulary; v2 uses \`test-results table\`.` Why: eliminates "Note that" + "may need attention"; satisfies "no apology-prefaces" + "specific over abstract" + "cite source for facts".

    **Example 3 — Generic AI-style hedging in skill prose.** Source: pick one v0.3.0 SKILL.md case from AUDIT.md §AUDIT-01. Cite the actual `file:line`. Before: verbatim hedged passage. After: imperative locked rewrite. Why: eliminates the specific forbidden phrasing(s); satisfies "end with handoff, not summary" + "specific over abstract".

    If no v0.3.0 SKILL.md case can be cleanly cited, use a representative pattern with cite "(representative pattern; AUDIT.md §AUDIT-01 brittleness category: hedged hand-off messages)" — Phase 1 D-32 / D-34 honesty precedent.

    **After populating Appendix C, run the structural-check script** to verify echo count progressed from 0 to 10:
    `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`
    Expected: exits 1 (Plans 02-03..02-09 must add remaining 20 echo lines AND Plan 02-03 must add hand-off matrix data rows).

    **Implementing decisions:** D-29, D-32, D-34, D-35.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### Example [1-3] — ' .planning/DESIGN.md)" -eq "3" ] &amp;&amp; grep -qF '**Before (v0.3.0-style):**' .planning/DESIGN.md &amp;&amp; grep -qF '**After (v2 senior-implementer voice):**' .planning/DESIGN.md &amp;&amp; grep -qF '**Why the rewrite works:**' .planning/DESIGN.md &amp;&amp; grep -qF 'AUDIT.md §AUDIT-07' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "10" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE (expect 1 — mid-phase state)"; [ "$CODE" = "1" ] &amp;&amp; echo APPENDIX_C_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^### Example [1-3] — ' .planning/DESIGN.md` returns exactly `3`
    - `grep -cF '**Before (v0.3.0-style):**' .planning/DESIGN.md` returns >= 3
    - `grep -cF '**After (v2 senior-implementer voice):**' .planning/DESIGN.md` returns >= 3
    - `grep -cF '**Why the rewrite works:**' .planning/DESIGN.md` returns >= 3
    - `grep -qF 'AUDIT.md §AUDIT-07' .planning/DESIGN.md` succeeds
    - `grep -qF 'AUDIT.md §AUDIT-01' .planning/DESIGN.md` succeeds
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `10`
    - `bash .planning/phases/02-design/scripts/design-structure-check.sh` exits with code `1` (mid-phase)
    - The failure message names "expected >= 30 'DESIGN-NN:' success-criteria echo" OR "hand-off contract matrix must have >= 10 transition rows"
  </acceptance_criteria>
  <done>
    Appendix C has 3 worked before/after examples drawn from real audit findings. Echo line count progressed from 0 to 10. Structural-check still fails by design.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Survey methodology → DESIGN-08 contract | Survey result locks the canonical lifecycle. If survey is run sloppily, DESIGN-08 ships a wrong contract. |
| AUDIT.md citations → DESIGN.md prose | Per D-34, DESIGN.md cites AUDIT.md as ground truth. A bad citation misleads readers. |
| DESIGN-10 forbidden-phrasings list scope (cross-AI review MEDIUM #5) | Forbidden-phrase greps are now scoped to the DESIGN-10 H3 region only — broader future linting (e.g., persona-lint at end-of-phase) won't false-positive on the persona contract's own quoted phrases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Survey result fabricated when no live folders are reachable | mitigate | Task 1 explicitly handles the no-live-folders case: methodology paragraph documents the fallback (v0.3.0 SKILL.md enumeration) transparently. |
| T-02-02-02 | Tampering | Forbidden-phrasing list in DESIGN-10 contradicts itself | mitigate | Task 2 prose body for DESIGN-10 must self-lint. Acceptance criteria require >= 5 forbidden phrasings present in the DESIGN-10 H3 region (scoped via stateful awk per cross-AI review MEDIUM #5). |
| T-02-02-03 | Tampering | Echo-line count assertion broken by H3 heading drift | mitigate | Task 2 acceptance regex `^### DESIGN-(0[1-9]|10) — ` requires zero-padded two-digit IDs. |
| T-02-02-04 | Information Disclosure | Status-survey leaks live client names | accept | Survey methodology is transparent; if live folders are sampled, anonymise client names. |
| T-02-02-05 | Tampering | Plan accidentally satisfies all structural-check assertions early | mitigate | Acceptance criteria for Task 3 explicitly require structural-check exit code `1`. |
| T-02-02-06 | Tampering | DESIGN-10 prose body repeats the 3 worked examples verbatim instead of pointing at Appendix C | mitigate | Task 2 acceptance criteria require DESIGN-10 prose to end with `Appendix C` reference. |
</threat_model>

<verification>
End of Plan 02-02 success-state:
1. `### Live status-lifecycle survey` body populated
2. `## Cross-cutting decisions` section opens with 10-row decision-summary table (verified via stateful section_between)
3. 10 H3 subsections (`### DESIGN-01` through `### DESIGN-10`) each with echo line + concrete contract prose
4. `## Appendix C: Persona contract worked examples` populated with 3 worked before/after examples
5. Echo-line count = 10
6. `bash design-structure-check.sh` exits 1 (mid-phase state)
7. Seed `[OPEN]` marker from Plan 02-01 still present; zero new `[OPEN]` markers added by this plan
8. Forbidden-phrasings list count is verified within the DESIGN-10 H3 region only (cross-AI review MEDIUM #5 fix)

Manual reviewer attestation:
- [ ] Decision-summary table rows match REQUIREMENTS.md DESIGN-01..10 wording (no scope-reduction)
- [ ] Survey methodology is honest (no fabricated `file:line` cites)
- [ ] DESIGN-10 prose body itself does not violate the persona contract (no "we recommend", etc. outside the forbidden-list verbatim quotes)
- [ ] `[OPEN]` and `[MIGRATION-RISK]` markers used sparingly
</verification>

<success_criteria>
- [ ] `### Live status-lifecycle survey` body complete
- [ ] 10-row decision-summary table under `## Cross-cutting decisions` (verified via stateful section_between)
- [ ] 10 H3 subsections (`### DESIGN-01..DESIGN-10`)
- [ ] `## Appendix C` populated with 3 worked before/after examples
- [ ] `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `10`
- [ ] `bash design-structure-check.sh` exits 1 (mid-phase state)
- [ ] No content under `dydx-delivery/` modified
- [ ] Forbidden-phrasings count check is scoped to DESIGN-10 H3 region (cross-AI review MEDIUM #5)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-02-SUMMARY.md` documenting:
- Survey methodology + key result
- 10 cross-cutting contracts locked + locking-decision IDs
- Appendix C 3 examples (one-line summary each)
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #5 (forbidden-phrasings grep now scoped to DESIGN-10 H3 region via stateful awk).
- Pointer: "Plan 02-03 (Wave 3) populates Skill layout + 13-skill inventory + Stage-by-stage hand-off contract next (DESIGN-11..13). Hand-off matrix needs >= 10 data rows to clear structural-check assertion #9."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-02-SUMMARY.md` to `files_modified`); MEDIUM #5 (DESIGN-10 forbidden-phrasings grep is now scoped to the `### DESIGN-10 — Persona contract` H3 region via stateful awk, preventing future false positives if broader persona-linting is added at end-of-phase); replaced the row-counting acceptance criterion for the decision-summary table with a stateful section_between extraction (consistent with cross-AI review HIGH #1 fix in Plan 02-01). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-01..10 still in `requirements`; D-19/23/24/25/29/32/33/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
