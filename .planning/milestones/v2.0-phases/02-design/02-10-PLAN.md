---
plan_id: 02-10
phase: 02-design
plan: 10
type: execute
wave: 10
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07, 02-08, 02-09]
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06, DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10, DESIGN-11, DESIGN-12, DESIGN-13, DESIGN-14, DESIGN-15, DESIGN-16, DESIGN-17, DESIGN-18, DESIGN-19, DESIGN-20, DESIGN-21, DESIGN-22, DESIGN-23, DESIGN-24, DESIGN-25, DESIGN-26, DESIGN-27, DESIGN-28, DESIGN-29, DESIGN-30]
files_modified:
  - .planning/phases/02-design/02-10-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-10-SUMMARY.md
autonomous: true
tags: [design, synthesis, executive-summary, glossary, traceability, open-questions-list]

must_haves:
  truths:
    - "DESIGN.md `## Executive Summary` populated as TOC + per-section decision summary table — one row per major H2."
    - "DESIGN.md `## Appendix A: Glossary` populated with v2-canonical vocabulary (>= 20 entries)."
    - "DESIGN.md `## Appendix B: DESIGN-* → DESIGN.md section traceability` populated with 30-row table mapping DESIGN-NN → section anchor → locking decision IDs."
    - "DESIGN.md `## Deferred to Phase 4 OPEN-QUESTIONS` populated with FULL enumerated list of every inline `[OPEN: Phase 4 — ...]` marker (Plan 02-01 seed marker REPLACED). Each list entry cites source section + owning phase per CHANGE-04 (per cross-AI review #10)."
    - "DESIGN.md preamble blockquote finalised (placeholder removed)."
    - "Structural-check exits 0 — DESIGN.md structurally complete and **reviewer-ready** (per cross-AI review MEDIUM #7 — Plan 02-10 is the terminal state where 'reviewer-ready' applies; Plan 02-09 was 'structurally passable')."
    - "Inline-marker-vs-list reconciliation algorithm executes correctly: scoped inline-body extraction (excludes Deferred section) AND scoped list-bullet count (anchored bullet pattern) AND normalised marker-text comparison — per cross-AI review HIGH #3."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "Final synthesis: executive summary + Appendix A glossary + Appendix B 30-row traceability + closed [OPEN] list + final preamble"
      contains: "TOC table; >= 20 glossary entries; 30-row Appendix B; enumerated [OPEN] list reconciled with inline markers via documented algorithm; finalised preamble"
  key_links:
    - from: "## Executive Summary table"
      to: "Every major H2 section in DESIGN.md (TOC function + skip-to-contract aid)"
      via: "Per-row Section column references H2 anchor; Decision summary one-liner; Locking IDs cite REQUIREMENTS DESIGN-NN + CONTEXT D-NN"
      pattern: "row per H2 section"
    - from: "## Deferred to Phase 4 OPEN-QUESTIONS list"
      to: "Phase 4 OPEN-QUESTIONS.md register (built mechanically by walking this list)"
      via: "One bullet per inline marker; format: `[OPEN: Phase 4 — <subject>] — source: §<H2 / H3> — owner: Phase <N> per CHANGE-04`"
      pattern: "list entries reconcile 1:1 with normalised inline marker text count outside the Deferred section"
    - from: "## Appendix B traceability"
      to: "REQUIREMENTS.md DESIGN-01..30"
      via: "30 rows; each maps DESIGN-NN → DESIGN.md section anchor → locking D-NN(s)"
      pattern: "DESIGN-NN → §<anchor> → D-NN, D-NN"
---

<objective>
Final synthesis pass on `.planning/DESIGN.md`: finalised preamble, executive summary TOC + decision-summary table, Appendix A glossary, Appendix B 30-row DESIGN-* traceability, and the finalised closed `[OPEN: Phase 4 — ...]` enumeration list.

**Purpose:** Plans 02-02..02-09 wrote the contracts. This plan makes DESIGN.md REVIEWABLE end-to-end. No new content contracts; pure synthesis.

**Output:** `.planning/DESIGN.md` complete and structurally final. Structural-check exits 0. **DESIGN.md is reviewer-ready** (cross-AI review MEDIUM #7 — terminal state phrase reserved for Plan 02-10).

**Critical fix per cross-AI review HIGH #3:** The closed `[OPEN]` list reconciliation algorithm is FRAGILE in its naive form because list entries quote the same marker text as the inline markers. Naive `grep -c '\[OPEN: Phase 4'` over the whole document double-counts (each marker appears once inline + once in the closed list). Fix:
- (a) Scope inline-marker scan to EXCLUDE the `## Deferred to Phase 4 OPEN-QUESTIONS` section (use stateful awk to extract the body BEFORE the Deferred H2).
- (b) Count Deferred section bullets separately by anchored bullet pattern `^- \*\*\[OPEN: Phase 4` (NOT just any `[OPEN: Phase 4` substring).
- (c) Compare normalised marker TEXT (not just counts) — extract the inline marker text content and assert each appears as a bullet in the Deferred list.
The reconciliation algorithm is documented in Task 2 below as concrete bash steps.

**Marker-ownership per cross-AI review #10:** Each Deferred list bullet MUST cite source section AND owning phase (e.g., `Phase 4`, `Phase 7`) per CHANGE-04. If owning phase is unknown, mark `owner: TBD` explicitly — never silently omit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/phases/02-design/02-05-PLAN.md
@.planning/phases/02-design/02-06-PLAN.md
@.planning/phases/02-design/02-07-PLAN.md
@.planning/phases/02-design/02-08-PLAN.md
@.planning/phases/02-design/02-09-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Synthesis writes to 5 regions:
     1. Preamble blockquote (replaces placeholder line)
     2. ## Executive Summary (TOC + per-section decision summary table)
     3. ## Appendix A: Glossary (>= 20 entries)
     4. ## Appendix B: DESIGN-* → DESIGN.md section traceability (30 rows)
     5. ## Deferred to Phase 4 OPEN-QUESTIONS (replaces seed marker with full enumerated list)
     No new echo lines; no new section bodies. Anchors already exist.

     Section-range extraction uses stateful awk patterns (consistent with the Plan 02-01 script's
     section_between helper) per cross-AI review HIGH #1. The reconciliation algorithm in Task 2
     is documented step-by-step per cross-AI review HIGH #3. -->

Executive Summary table shape (one row per major H2; >= 15 rows):

| Section | Decision summary | Locking IDs |
|---------|------------------|-------------|
| `## Cross-cutting decisions` | 10 cross-cutting contracts | DESIGN-01..10; D-23, D-24, D-25, D-29 |
| `## Skill layout` | v2 7-directory plugin layout | DESIGN-11; FOUND-01 |
| `## v2 skill inventory` | v2 end-state skills (15 = 6 NEW + 4 MODIFIED + 2 UNCHANGED + 3 NEW platform; 1 RETIRED `generate-functional-spec`) | DESIGN-12 |
| `## Stage-by-stage hand-off contract` | 12-row transition matrix | DESIGN-13; D-26 |
| `## Platform skills` | 3 internalised platform skills | DESIGN-14, 15, 16 |
| `## Stage 1: Kickoff capture` | NEW dual-branch artefact | DESIGN-17 |
| `## Stage 2: Discovery refactor` | MODIFIED to consume `01_kickoff_v*` | DESIGN-18 |
| `## Stage 3: SOW refactor` | UNCHANGED-structure; behaviour-modified | DESIGN-19 |
| `## Stage 4a/4b: Functional spec split` | NEW 2 skills replacing RETIRED single fnspec; per-requirement `delivery: native-ai \| api` | DESIGN-20 |
| `## Stage 5: Tech spec` | MODIFIED scope-gate | DESIGN-21 |
| `## Stage 6: Cost estimate` | NEW with risk-multiplier taxonomy STRUCTURE (numerics deferred per D-22) | DESIGN-22; D-22 |
| `## Stage 7a/7b: Build prompts (dual)` | 7a (MODIFIED dev) + 7b (NEW per-platform implementation) | DESIGN-23 |
| `## Stage 8: Test bot — overview` | Persistent harness; CRIT-5; harness_drift; sandbox_lock; lifecycle states | DESIGN-24 |
| `## Stage 9: Documentation publishing` | Closed doc_type 9-enum; double-underscore naming; doc-diff gate; doc_published_at; MOD-1 | DESIGN-25 |
| `## Stage 10: Native-AI enablement` | native_ai_path branching; copy-paste default; CRIT-8 + MIN-4 refusals | DESIGN-26 |
| `## Stage 11: Sign-off, brain update, archive` | Brain-mirror Coda template (7 sections); tone_lint MOD-9 | DESIGN-27 |
| `## Test bot architecture` | Tier-1 + tier-2; client_state.yaml skeleton; drift-detection | DESIGN-28, 29, 30; D-30 |
| `## Live status-lifecycle survey` | Survey methodology + result | DESIGN-08; D-25 |
| `## Appendix C: Persona contract worked examples` | 3 before/after voice-rewrite examples | DESIGN-10; D-29 |

Closed `[OPEN]` list shape (one bullet per inline marker, in document order). Format MANDATED per cross-AI review #10:

```markdown
- **`[OPEN: Phase 4 — risk-multiplier numeric defaults pending dYdX-historical validation per D-22]`** — source: §Stage 6: Cost estimate — owner: Phase 4 (decide before Stage 6 build per CHANGE-01 ordering)
- **`[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01]`** — source: §platform-pipefy → Native-AI capability matrix — owner: Phase 7 per CHANGE-04
... (one bullet per inline marker, with explicit owner — never silently omit)
```

Each bullet REQUIRED fields:
- Bold-quoted marker text (verbatim from inline)
- `source: §<H2 / H3 section name>`
- `owner: Phase <N> per CHANGE-04` (or `owner: TBD` if genuinely unknown — explicit, never silently omit per cross-AI review #10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise preamble + populate `## Executive Summary` TOC table + populate `## Appendix A: Glossary`</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-09 complete; structural-check exits 0; structurally passable)
    - .planning/phases/02-design/02-CONTEXT.md §"Specifics" — "Reviewer experience"
    - .planning/phases/02-design/02-CONTEXT.md §"Cross-cutting authoring decisions" D-32, D-33, D-34, D-35
    - .planning/phases/01-audit/01-09-SUMMARY.md — Phase 1 synthesis precedent
    - .planning/AUDIT.md §"Executive Summary" + §"Appendix A: Glossary"
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate three regions:

    **(A) Finalise preamble.** Replace the existing placeholder line `> (Preamble placeholder — finalised in synthesis plan 02-10)` with a full preamble paragraph (in blockquote form) that explains:
    - Document purpose: locks v2 architecture for v2.1+ rebuild without re-deciding.
    - Reading conventions: success-criteria echo per H2 (D-35); inline `[OPEN: Phase 4 — ...]` markers + closed list at end (D-27/D-33); citation format `file:line` (D-32); `AUDIT.md §X.Y` grounding (D-34).
    - Reviewer flow: read-in-order OR skip-to-contract via executive summary table.
    - Phase boundary: no skill files edited in this milestone; v2.1+ build phases per Phase 3 CHANGELIST.md execute against this design.
    - Approval gate: single approval at end (full DESIGN.md reviewed as a whole) per ROADMAP Phase 2 gate.

    Keep the preamble concise (1-2 short paragraphs in blockquote form). Other preamble lines (Design Date, Branch / commit, Phase 1 Audit ground truth) stay above; only the placeholder blockquote line is replaced.

    **(B) Populate `## Executive Summary` table.** Replace the placeholder body with a TOC-shaped table per `<interfaces>` shape above. Required columns: `Section | Decision summary | Locking IDs`. Use the rows shown in `<interfaces>` (>= 15 rows). Each Section cell uses the literal H2 anchor in backticks.

    Add framing paragraph above the table: "This table summarises every major design contract for skip-to-contract navigation. Each row's Section column links to the full contract; Decision summary captures the one-line lock; Locking IDs cite the REQUIREMENTS DESIGN-NN AND CONTEXT D-NN that produced the contract."

    **(C) Populate `## Appendix A: Glossary`.** Replace placeholder body with >= 20 glossary entries. Entry format: `**term** — definition citing the locking section`. Coverage:
    - Frontmatter fields: `frontmatter_version`, `based_on_*`, `status` enum, `client`, `project`, `platform`, `pipe_id`, `space_id`, `project_id`, `delivery`, `kickoff_branch`, `commercial_inputs_status`, `risk_multiplier_version`, `tech_spec_scope`, `delivery_filter`, `doc_type`, `doc_version`, `doc_published_at`, `last_diff_review_at`, `ingested_at`, `target_id`, `native_ai_path`, `tier_claims_last_verified`, `targets_artefact`, `last_passed_at`, `failure_class`, `cr_id`.
    - Status lifecycle: `draft`, `client_review`, `approved`, `archived`.
    - Stage-prefix conventions; substages (4a/4b/7a/7b/8a-8d).
    - Platform terms: Pipe / Phase / Card / Behaviors (Pipefy); Space / Folder / Task (Wrike); Project / Proof / ReviewAI / Checklist (Ziflow).
    - Test-bot terms: tier-1 / tier-2 / harness_drift / sandbox_lock.yaml / schema_drift_report.md / acknowledge / revert / quarantined / obsolete.
    - Plugin surfaces: `commands/`, `agents/`, `hooks/`, `tests/`, `references/`, `mcpServers`.
    - Doc + sign-off terms: doc-diff, brain-mirror, spoke, 00_Index.md, Field Notes, tone_lint.

    Each entry cites locking DESIGN-NN section.

    **Implementing decisions:** D-19 (executive summary = matrix shape), D-32, D-34, D-35.
  </action>
  <verify>
    <automated>EXEC_BODY=$(awk -v hdr='## Executive Summary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md) &amp;&amp; EXEC_ROWS=$(printf '%s\n' "$EXEC_BODY" | grep -cE '^\| \`?##') &amp;&amp; [ "$EXEC_ROWS" -ge "15" ] &amp;&amp; APPENDIX_A_BODY=$(awk -v hdr='## Appendix A: Glossary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md) &amp;&amp; APPENDIX_A=$(printf '%s\n' "$APPENDIX_A_BODY" | grep -cE '^\*\*[a-zA-Z]') &amp;&amp; [ "$APPENDIX_A" -ge "20" ] &amp;&amp; PLACEHOLDER=$(grep -cF '(Preamble placeholder' .planning/DESIGN.md) &amp;&amp; [ "$PLACEHOLDER" = "0" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh; CODE=$?; echo "STRUCTURAL_CHECK=$CODE"; [ "$CODE" = "0" ] &amp;&amp; echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Preamble placeholder removed: `grep -cF '(Preamble placeholder' .planning/DESIGN.md` returns `0`
    - Preamble final blockquote present: explains echo convention (D-35), inline + closed `[OPEN]` markers (D-27/D-33), `file:line` citations (D-32), `AUDIT.md §X.Y` grounding (D-34), reviewer skip-to-contract flow, phase boundary, single approval gate
    - **Executive Summary table has >= 15 rows** (extracted via stateful section_between — per cross-AI review HIGH #1):
      ```bash
      awk -v hdr='## Executive Summary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md | grep -cE '^\| \`?##'
      ```
      returns >= 15
    - Executive Summary table cell text references at least: Cross-cutting, Skill layout, v2 skill inventory, Stage-by-stage hand-off contract, Platform skills, Stages 1–11, Test bot architecture
    - **Appendix A has >= 20 glossary entries** (extracted via stateful section_between):
      ```bash
      awk -v hdr='## Appendix A: Glossary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md | grep -cE '^\*\*[a-zA-Z]'
      ```
      returns >= 20
    - Appendix A entries cover: at least 10 frontmatter field terms; status lifecycle terms; test-bot terms; plugin surfaces
    - Each Appendix A entry cites the locking DESIGN-NN section (`grep -cE '\*\*[a-zA-Z][a-zA-Z_-]+\*\* — .* DESIGN-' .planning/DESIGN.md` >= 15)
    - **Structural-check still exits 0**
  </acceptance_criteria>
  <done>
    Preamble finalised. Executive Summary populated with >= 15-row TOC table. Appendix A populated with >= 20 glossary entries each citing locking DESIGN-NN. Structural-check exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate `## Appendix B: DESIGN-* → DESIGN.md section traceability` (30 rows) + finalise `## Deferred to Phase 4 OPEN-QUESTIONS` enumerated list with documented reconciliation algorithm + final structural-check pass — DESIGN.md becomes REVIEWER-READY</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1)
    - .planning/REQUIREMENTS.md §"Design (DESIGN.md)" — DESIGN-01..30
    - .planning/phases/02-design/02-CONTEXT.md §"Open-questions handling" D-27
    - .planning/phases/02-design/02-REVIEWS.md HIGH #3 (fragile reconciliation logic — fix documented below); MEDIUM #10 (marker ownership in closed list)
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate two regions and run final structural-check:

    **(A) `## Appendix B: DESIGN-* → DESIGN.md section traceability`** — replace placeholder body with a 30-row traceability table. Columns: `| DESIGN-NN | Section anchor | Locking decision IDs |`. One row per DESIGN-01..30 in numeric order. Each row maps:
    - Column 1: DESIGN-NN ID
    - Column 2: DESIGN.md section anchor where the contract lives (use `§<section name>` reference matching the H2/H3 anchor; for DESIGN-20 cite both 4a + 4b anchors; for DESIGN-23 cite both 7a + 7b anchors; for DESIGN-08 also cite the survey H3)
    - Column 3: Locking decision IDs from CONTEXT D-NN (comma-separated; "—" if none)

    Example rows:
    ```
    | DESIGN-01 | §Cross-cutting decisions → §DESIGN-01 — Canonical frontmatter scheme | D-25 (survey result) |
    | DESIGN-08 | §Cross-cutting decisions → §DESIGN-08 — Frontmatter migration co-existence; §Live status-lifecycle survey | D-25 |
    | DESIGN-12 | §v2 skill inventory | (per cross-AI review MEDIUM #4 — anchor renamed from `## 13-skill inventory` to `## v2 skill inventory`) |
    | DESIGN-20 | §Stage 4a: Functional spec — platform; §Stage 4b: Functional spec — integration | D-20 |
    | DESIGN-22 | §Stage 6: Cost estimate | D-22 (structure-only; numerics deferred) |
    | DESIGN-23 | §Stage 7a: Build prompt — dev; §Stage 7b: Build prompt — implementation per platform | D-20 |
    | DESIGN-29 | §Test bot architecture → §DESIGN-29 client_state.yaml skeleton | D-30 |
    ```

    Add framing paragraph above table: "This appendix maps every REQUIREMENTS DESIGN-* ID to its DESIGN.md home section + locking CONTEXT decision IDs. Reviewer can verify 1:1 coverage against REQUIREMENTS.md §'Design (DESIGN.md)' DESIGN-01..30."

    **(B) `## Deferred to Phase 4 OPEN-QUESTIONS`** — REPLACE the seed marker placeholder body with a complete enumerated list of EVERY inline `[OPEN: Phase 4 — ...]` marker present in DESIGN.md, in document order.

    **Reconciliation algorithm (DOCUMENTED per cross-AI review HIGH #3 — replaces the previous fragile approach):**

    Step 1 — **Extract inline marker text from BODY (excluding the Deferred section).** Use stateful awk to scope to lines BEFORE the `## Deferred to Phase 4 OPEN-QUESTIONS` H2:
    ```bash
    INLINE_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/DESIGN.md)
    INLINE_MARKERS=$(printf '%s\n' "$INLINE_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u)
    INLINE_COUNT=$(printf '%s\n' "$INLINE_MARKERS" | grep -c '^' || true)
    ```
    The `awk '/.../{exit} {print}'` form reads from start-of-file UP TO the line BEFORE the Deferred H2 — it does NOT have the start-matches-end bug because there's no end pattern (just an exit on first match). `INLINE_COUNT` is the unique count of inline marker text strings appearing in the body before the Deferred section.

    Step 2 — **Generate one bullet per unique inline marker, in document order.** For each marker text in `INLINE_MARKERS`:
       - Find the line number in DESIGN.md where it appears (use `grep -nF "$marker_text"`).
       - Walk backward from that line to the nearest preceding `^## ` or `^### ` heading (use awk).
       - Emit one bulleted entry in the format mandated per cross-AI review #10:
         `- **\`[OPEN: Phase 4 — <marker text>]\`** — source: §<H2 / H3 section name> — owner: Phase <N> per CHANGE-04`
       - If owning phase is unknown, write `owner: TBD` explicitly. Never silently omit ownership per cross-AI review #10.

    Step 3 — **Reconciliation count check (anchored bullet pattern, NOT raw marker pattern):** After writing the list, count the Deferred-section bullets using ANCHORED bullet pattern (per cross-AI review HIGH #3 — distinguishes list bullets from any other text):
    ```bash
    DEFERRED_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/DESIGN.md)
    LIST_BULLETS=$(printf '%s\n' "$DEFERRED_BODY" | grep -cE '^- \*\*\`\[OPEN: Phase 4' || true)
    ```
    The `^- \*\*\`\[OPEN: Phase 4` pattern requires:
       - Line starts with `- **` (bulleted entry with bold opening)
       - Followed by backtick-quoted `[OPEN: Phase 4`
    This anchored pattern matches ONLY the Deferred-list bullet entries, NOT any prose that happens to contain `[OPEN: Phase 4` substring.

    Step 4 — **Normalised text comparison** (per cross-AI review HIGH #3 — counts can match while text differs):
    ```bash
    LIST_MARKER_TEXTS=$(printf '%s\n' "$DEFERRED_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u)
    diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS")
    ```
    Empty diff output = perfect 1:1 reconciliation. Non-empty diff = inline marker missing from list OR list bullet quoting nonexistent inline marker.

    Step 5 — **Assert all three reconciliation conditions:**
    - `INLINE_COUNT == LIST_BULLETS` (cardinality match)
    - `diff` produces empty output (textual match)
    - Each list entry contains either `owner: Phase` or `owner: TBD` (cross-AI review MEDIUM #10 — never silently omit ownership)

    Add framing paragraph above list: "This list enumerates every inline `[OPEN: Phase 4 — ...]` marker in DESIGN.md, in document order, as the canonical handoff to Phase 4 OPEN-QUESTIONS.md. Each item names the source section + recommended owning phase per CHANGE-04 (or `owner: TBD` if not yet decided — never silently omitted). Phase 4 register builds against this list mechanically."

    Add closing paragraph: "Verification (run by Phase 4 / final reviewer): the reconciliation algorithm in Plan 02-10 verifies (a) inline-marker text count outside this section matches list-bullet count; (b) `diff` of normalised marker texts is empty; (c) every bullet carries `owner:` (Phase or TBD). Mismatch on any condition halts Phase 4 register build."

    **(C) Final structural-check pass.** Run `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 0 (all 9 assertions pass).

    Additionally, run the documented reconciliation:
    ```bash
    INLINE_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/DESIGN.md)
    INLINE_MARKERS=$(printf '%s\n' "$INLINE_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u)
    INLINE_COUNT=$(printf '%s\n' "$INLINE_MARKERS" | grep -c '^' || true)
    DEFERRED_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/DESIGN.md)
    LIST_BULLETS=$(printf '%s\n' "$DEFERRED_BODY" | grep -cE '^- \*\*\`\[OPEN: Phase 4' || true)
    LIST_MARKER_TEXTS=$(printf '%s\n' "$DEFERRED_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u)
    echo "INLINE_COUNT=$INLINE_COUNT LIST_BULLETS=$LIST_BULLETS"
    [ "$INLINE_COUNT" = "$LIST_BULLETS" ] && echo "RECONCILE_COUNT_OK"
    diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS") && echo "RECONCILE_TEXT_OK"
    ```

    All conditions pass → DESIGN.md is **reviewer-ready** (cross-AI review MEDIUM #7 — Plan 02-10 terminal state phrase).

    **Implementing decisions:** D-19 (Appendix B = traceability matrix), D-27 (closed list enumerates every inline marker), D-32, D-35.
  </action>
  <verify>
    <automated>APPENDIX_B_BODY=$(awk -v hdr='## Appendix B:' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md) &amp;&amp; APPENDIX_B=$(printf '%s\n' "$APPENDIX_B_BODY" | grep -cE '^\| DESIGN-[0-9]{2} \|') &amp;&amp; [ "$APPENDIX_B" -ge "30" ] &amp;&amp; INLINE_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/DESIGN.md) &amp;&amp; INLINE_MARKERS=$(printf '%s\n' "$INLINE_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u) &amp;&amp; INLINE_COUNT=$(printf '%s\n' "$INLINE_MARKERS" | grep -c '^' || true) &amp;&amp; DEFERRED_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/DESIGN.md) &amp;&amp; LIST_BULLETS=$(printf '%s\n' "$DEFERRED_BODY" | grep -cE '^- \*\*\`\[OPEN: Phase 4' || true) &amp;&amp; echo "INLINE_COUNT=$INLINE_COUNT LIST_BULLETS=$LIST_BULLETS" &amp;&amp; [ "$INLINE_COUNT" = "$LIST_BULLETS" ] &amp;&amp; LIST_MARKER_TEXTS=$(printf '%s\n' "$DEFERRED_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u) &amp;&amp; diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS") &amp;&amp; OWNERS_PRESENT=$(printf '%s\n' "$DEFERRED_BODY" | grep -cE '^- \*\*\`\[OPEN.*owner: (Phase|TBD)' || true) &amp;&amp; [ "$OWNERS_PRESENT" = "$LIST_BULLETS" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh; CODE=$?; echo "FINAL_STRUCTURAL_CHECK=$CODE"; [ "$CODE" = "0" ] &amp;&amp; echo SYNTHESIS_COMPLETE_REVIEWER_READY</automated>
  </verify>
  <acceptance_criteria>
    - **Appendix B has >= 30 rows** (stateful section extraction per cross-AI review HIGH #1):
      ```bash
      awk -v hdr='## Appendix B:' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md | grep -cE '^\| DESIGN-[0-9]{2} \|'
      ```
      returns >= 30
    - Appendix B contains every DESIGN-NN ID (DESIGN-01..30) — verifiable: for N in 01..30, `grep -qF "| DESIGN-$N |" .planning/DESIGN.md` succeeds (30 individual checks)
    - DESIGN-20 row references both 4a + 4b section anchors
    - DESIGN-23 row references both 7a + 7b section anchors
    - DESIGN-08 row references both cross-cutting + status-lifecycle survey anchors
    - DESIGN-12 row references the renamed `## v2 skill inventory` anchor (cross-AI review MEDIUM #4 follow-through)
    - **Documented reconciliation algorithm passes all 3 conditions (cross-AI review HIGH #3):**
      - **Cardinality match:** `INLINE_COUNT == LIST_BULLETS` (computed via the documented bash steps in `<action>`)
      - **Textual match:** `diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS")` produces empty output
      - **Ownership-per-bullet (cross-AI review #10):** Every bullet contains `owner: Phase` OR `owner: TBD` — `grep -cE '^- \*\*\`\[OPEN.*owner: (Phase|TBD)'` count equals `LIST_BULLETS` count
    - **Inline-marker scope EXCLUDES the Deferred section** (cross-AI review HIGH #3 fix): the `INLINE_BODY` extraction uses `awk '/^## Deferred .../{exit} {print}'` form (start-to-line-before-match-or-EOF) — guarantees Deferred-section bullets do NOT contribute to the inline-marker count
    - **Deferred-bullet count uses anchored bullet pattern** (cross-AI review HIGH #3 fix): `LIST_BULLETS` count uses `^- \*\*\`\[OPEN: Phase 4` (NOT just any `[OPEN: Phase 4` substring) — distinguishes list entries from prose
    - Deferred list has >= 8 enumerated items (one per expected inline marker — Plan 02-04 platform markers + Plan 02-07 D-22 marker baseline)
    - Each list entry cites source `§<H2 / H3>` AND owner `Phase <N>` (or `TBD`)
    - **Final structural-check exits 0** — all 9 assertions pass; DESIGN.md is structurally complete AND **reviewer-ready** (cross-AI review MEDIUM #7 wording)
    - **Closing-paragraph reconciliation note present** in the Deferred section (instructs Phase 4 / final reviewer how to verify using the documented algorithm)
  </acceptance_criteria>
  <done>
    Appendix B has 30-row traceability table mapping DESIGN-01..30. Deferred section has full enumerated list of every inline `[OPEN: Phase 4 — ...]` marker (>= 8 items), each with explicit `source:` AND `owner:` (Phase OR TBD per cross-AI review #10). The documented reconciliation algorithm (per cross-AI review HIGH #3) passes all 3 conditions: cardinality match, textual match (via diff), ownership-per-bullet. Final structural-check exits 0. **DESIGN.md is structurally complete AND reviewer-ready** (cross-AI review MEDIUM #7 wording — terminal state phrase reserved for Plan 02-10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Closed `[OPEN]` list ↔ inline markers | Per D-27, the closed list MUST enumerate every inline marker. Drift breaks the Phase 4 register build. The naive count-matching approach (cross-AI review HIGH #3) is fragile because list entries quote the same marker text — both inline AND list contribute to the same `[OPEN: Phase 4` substring count. The fix uses scope (exclude Deferred section from inline-body scan) AND anchored bullet pattern (count list bullets distinctly) AND text comparison (via diff). |
| Appendix B traceability ↔ REQUIREMENTS DESIGN-01..30 | Each REQUIREMENTS DESIGN-* must appear in Appendix B exactly once. |
| Marker ownership (cross-AI review #10) | Every Deferred bullet cites owner phase. Silent omission breaks Phase 4 mechanical build. |
| End-of-Plan-02-10 wording (cross-AI review MEDIUM #7) | "Reviewer-ready" applies HERE (terminal state). Plan 02-09 was "structurally passable". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | Closed `[OPEN]` list omits an inline marker | mitigate | Documented reconciliation algorithm (cross-AI review HIGH #3): scoped inline-body extraction + anchored bullet count + text diff. All 3 conditions enforced. |
| T-02-10-02 | Tampering | Appendix B has fewer than 30 rows or skips a DESIGN-NN | mitigate | Acceptance criteria assert >= 30 rows AND specifically check each DESIGN-NN literal substring present. |
| T-02-10-03 | Tampering | Executive Summary table cell references wrong H2 anchor | mitigate | Reviewer-attestation manual check; acceptance criteria check for canonical section names. |
| T-02-10-04 | Tampering | Glossary entry definitions invent terms not used in DESIGN.md | mitigate | Each glossary entry cites the locking DESIGN-NN section. |
| T-02-10-05 | Tampering | Plan 02-10 accidentally REINTRODUCES placeholder content from earlier plans | mitigate | This plan ONLY edits 5 specific regions. Acceptance criteria use awk-scoped queries against the 5 target regions. |
| T-02-10-06 | Tampering | Final structural-check exits non-zero due to synthesis content breaking an existing assertion | mitigate | Each Task ends with structural-check run. Two checkpoints catch any structural break. |
| T-02-10-07 | Tampering | Naive `grep -c '\[OPEN: Phase 4'` over the whole document double-counts (each marker appears once inline + once in the closed list quoting it) — produces false-positive cardinality match (cross-AI review HIGH #3) | mitigate | Documented algorithm uses (a) scoped extraction via stateful awk to EXCLUDE the Deferred section from the inline-body scan; (b) anchored bullet pattern `^- \*\*\`\[OPEN: Phase 4` to count list bullets distinctly from any prose substring; (c) `diff` of normalised marker texts to catch cases where counts match but texts differ. |
| T-02-10-08 | Tampering | Deferred bullet quotes inline-marker text but accidentally includes a closed-list-section internal marker (e.g., the closing paragraph references `[OPEN: Phase 4 — example marker]` for illustration) — inflates `LIST_BULLETS` count | mitigate | Acceptance criteria require ONLY the bullet-format `^- \*\*\`\[OPEN: Phase 4` to be counted; prose mentions in the closing paragraph or framing prose do NOT match the anchored pattern. The closing paragraph in `<action>` step (B) closing avoids using the literal `[OPEN: Phase 4` substring inside an example. |
| T-02-10-09 | Tampering | A list entry omits ownership (cross-AI review #10) | mitigate | Acceptance criteria assert: every bullet contains `owner: Phase` OR `owner: TBD` — count of bullets with `owner:` equals `LIST_BULLETS` count. No silent omission allowed. |
| T-02-10-10 | Tampering | Plan 02-10 wording underclaims (still says "structurally passable" instead of "reviewer-ready") OR overclaims | mitigate | Plan 02-10's `<done>` block, `<verification>`, and `<output>` SUMMARY use "reviewer-ready" verbatim — this is the TERMINAL state per cross-AI review MEDIUM #7. Plan 02-09 was "structurally passable"; Plan 02-10 is "reviewer-ready". |
</threat_model>

<verification>
End of Plan 02-10 success-state:
1. Preamble blockquote finalised (placeholder removed)
2. `## Executive Summary` populated with >= 15-row TOC + per-section decision-summary table (extracted via stateful section_between)
3. `## Appendix A: Glossary` populated with >= 20 entries each citing locking DESIGN-NN
4. `## Appendix B: DESIGN-* → DESIGN.md section traceability` populated with 30-row table covering DESIGN-01..30 (incl. substage-spanning rows for DESIGN-20 + DESIGN-23 + the renamed `## v2 skill inventory` anchor for DESIGN-12)
5. `## Deferred to Phase 4 OPEN-QUESTIONS` populated with full enumerated list (>= 8 items); the documented reconciliation algorithm passes all 3 conditions (cardinality, textual, ownership-per-bullet)
6. Final structural-check exits 0 — DESIGN.md structurally complete
7. **DESIGN.md is reviewer-ready** (cross-AI review MEDIUM #7 — terminal state)

Manual reviewer attestation:
- [ ] Executive Summary cell contents match actual contracts in their referenced sections
- [ ] Glossary entries cover all unique frontmatter fields introduced across DESIGN-01..30
- [ ] Appendix B traceability is complete (DESIGN-01..30 each have a row)
- [ ] Deferred list reconciles 1:1 with inline `[OPEN]` markers via the documented algorithm — NO orphans either way
- [ ] Every Deferred bullet carries explicit ownership (`owner: Phase <N>` OR `owner: TBD` — cross-AI review #10)
- [ ] Final structural-check exits 0
- [ ] DESIGN.md is reviewer-ready (cross-AI review MEDIUM #7 — terminal state phrase used in this plan)
</verification>

<success_criteria>
- [ ] Preamble blockquote finalised
- [ ] Executive Summary >= 15-row TOC table (verified via stateful section_between)
- [ ] Appendix A >= 20 glossary entries (verified via stateful section_between)
- [ ] Appendix B 30-row DESIGN-* traceability table (covers DESIGN-01..30 incl. renamed `## v2 skill inventory` anchor)
- [ ] Deferred list enumerates every inline `[OPEN]` marker (>= 8 items); reconciliation passes all 3 conditions (cardinality + textual + ownership-per-bullet — cross-AI review HIGH #3 + #10)
- [ ] Final structural-check exits 0
- [ ] No content under `dydx-delivery/` modified
- [ ] No new echo lines added (Plan 02-09 already brought count to 32)
- [ ] **DESIGN.md is reviewer-ready** (cross-AI review MEDIUM #7 — terminal state phrase used)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-10-SUMMARY.md` documenting:
- Preamble finalised
- Executive Summary: N rows (one per major H2)
- Appendix A: N glossary entries
- Appendix B: 30-row DESIGN-* traceability — every REQUIREMENTS DESIGN-NN has a home section + locking D-NN
- Deferred list: N items (matches inline `[OPEN]` count via documented reconciliation algorithm); each cites source + owning phase per CHANGE-04 (or `TBD` explicitly per cross-AI review #10)
- **Final structural-check exits 0 — DESIGN.md is reviewer-ready and ready for Phase 2 approval gate review per ROADMAP.**
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); HIGH #3 (documented reconciliation algorithm — scoped inline-body extraction + anchored bullet count + text diff); MEDIUM #7 (terminal "reviewer-ready" wording reserved for Plan 02-10); #10 (every Deferred bullet has explicit ownership — Phase or TBD, never silently omitted).
- Pointer: "Phase 2 deliverable complete. Submit `.planning/DESIGN.md` for human approval per ROADMAP Phase 2 approval gate. After approval, Phase 3 (Change list) begins per ROADMAP sequencing — `/gsd-discuss-phase 3` next."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-10-SUMMARY.md` to `files_modified`); HIGH #3 (documented `[OPEN]` reconciliation algorithm in concrete bash steps — Step 1 scoped inline-body extraction via `awk '/.../{exit} {print}'` excluding Deferred section; Step 2 generate one bullet per unique inline marker; Step 3 anchored bullet count using `^- \*\*\`\[OPEN: Phase 4` pattern distinguishing list bullets from prose; Step 4 normalised text comparison via `diff <(...) <(...)`; Step 5 assert all 3 conditions — replaces fragile naive count-match approach; T-02-10-07 + T-02-10-08 added to threat register); MEDIUM #7 (terminal "reviewer-ready" phrase reserved for Plan 02-10 — explicit in `<done>`, `<verification>`, `<success_criteria>`, `<output>`; T-02-10-10 added); MEDIUM #10 (every Deferred bullet must have explicit ownership — `owner: Phase <N>` OR `owner: TBD`; never silently omitted; T-02-10-09 added; framing paragraph + closing paragraph both reinforce); skill-count framing follow-through (Appendix B DESIGN-12 row references the renamed `## v2 skill inventory` anchor per cross-AI review MEDIUM #4 fix in Plan 02-03). Acceptance criteria for Appendix B + Appendix A + Executive Summary use stateful section_between extraction (consistent with Plan 02-01 script fix HIGH #1). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: all 30 DESIGN-* IDs still in `requirements`; D-19/27/32/33/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
