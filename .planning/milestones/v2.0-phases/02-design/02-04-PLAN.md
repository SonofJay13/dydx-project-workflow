---
plan_id: 02-04
phase: 02-design
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
requirements: [DESIGN-14, DESIGN-15, DESIGN-16]
files_modified:
  - .planning/phases/02-design/02-04-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-04-SUMMARY.md
autonomous: true
tags: [design, platform-skills, native-ai, internalisation]

must_haves:
  truths:
    - "DESIGN.md `## Platform skills` section opens with a 3-row platform-comparison matrix per D-19 (rows: pipefy / wrike / ziflow; columns: native-AI surface / API protocol / sandbox access pattern / native_ai_path default / known gaps)."
    - "Each of `### platform-pipefy`, `### platform-wrike`, `### platform-ziflow` H3 subsections opens with `> **DESIGN-NN:**` echo (D-35) + 5-file `references/` shape declaration + 2026-grounded native-AI capability matrix + API surface for the gap + sandbox access pattern + `native_ai_path: api | paste | none` flag with confidence + `tier_claims_last_verified` frontmatter contract."
    - "Per D-21, ONLY decision contracts (purpose, 5-file shape, capability matrix, API surface, sandbox pattern, ingestion path, frontmatter) — NO full SKILL.md or references/*.md prose drafted."
    - "Inline `[OPEN: Phase 4 — ...]` markers added for the 5+ research-blocked items per CONTEXT Deferred Ideas."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-14, 15, 16 platform skill decision contracts populated"
      contains: "platform-comparison matrix; 3 H3 subsections each with echo + 5-file references shape + native-AI matrix + API surface + sandbox pattern + native_ai_path flag + tier_claims_last_verified frontmatter; >= 5 inline [OPEN: Phase 4] markers for research-blocked items"
  key_links:
    - from: "## Platform skills section"
      to: "Stage 6 (DESIGN-22), Stage 7b (DESIGN-23), Stage 8a (DESIGN-24), Stage 10 (DESIGN-26) — DOWNSTREAM forward references"
      via: "Each platform skill exposes a contract surface that downstream stage skills consume"
      pattern: "see `## platform-<X>` from Stage <N> sections"
    - from: "Inline [OPEN: Phase 4 — ...] markers in platform sections"
      to: "Phase 4 OPEN-QUESTIONS register (per CHANGE-04 research-blocked Phase 7 native-AI ingestion paths)"
      via: "Closed list at end of DESIGN.md (added by Plan 02-10) enumerates every inline marker"
      pattern: "[OPEN: Phase 4 — <one-line summary>]"
---

<objective>
Populate `.planning/DESIGN.md` Platform skills section (DESIGN-14, 15, 16): platform-pipefy / platform-wrike / platform-ziflow decision contracts.

**Purpose:** Internalises the 3 platform skills with identical 5-file `references/` shape per DESIGN-14/15/16. Each skill exposes the contract surface that Stage 6 (cost), Stage 7b (implementation prompt), Stage 8a (test harness), and Stage 10 (native-AI push) consume. Per D-21, ONLY decision contracts — NO full SKILL.md or references/*.md prose drafted.

**Output:** `.planning/DESIGN.md` `## Platform skills` body populated with comparison matrix + 3 H3 subsections. 3 of 30 echo lines added (DESIGN-14, 15, 16). Running total: 16/30. >= 5 inline `[OPEN: Phase 4]` markers added for research-blocked items per CONTEXT Deferred Ideas.

**Forward-reference guardrails (per cross-AI review MEDIUM #6):** Cross-references to DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 are FORWARD references — those sections do not exist yet at end of Wave 4. The cross-references in this plan's content cite DESIGN-NN as anchor placeholders only ("see DESIGN-22 Stage 6"); acceptance criteria in this plan do NOT assert that the cross-referenced body content actually exists in DESIGN.md (that's verified by the synthesis pass in Plan 02-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- This plan writes to ## Platform skills H2 region.
     Three H3 subsections (### platform-pipefy / -wrike / -ziflow) already anchored by Plan 02-01.
     Per D-21, decision-contract-only — NO full SKILL.md or 5-file references/*.md prose.

     Section-range extraction in acceptance criteria uses awk ranges with DISTINCT start/end anchors
     (e.g., `awk '/^### platform-pipefy/,/^### platform-wrike/'`) — these are SAFE because the start
     regex does NOT match the end regex on the same line. The broken `awk '/^## X/,/^## /'` pattern
     (per cross-AI review HIGH #1) is forbidden; this plan does not use it.

     Forward-reference guardrails (per cross-AI review MEDIUM #6): Cross-references to DESIGN-22 /
     DESIGN-23 / DESIGN-24 / DESIGN-26 are forward references — those sections do not exist yet
     at end of Wave 4. The cross-references in this plan's content cite the DESIGN-NN ID only as a
     placeholder anchor; acceptance criteria do NOT assert the cross-referenced body content exists.
     Plan 02-10 (synthesis) verifies Appendix B traceability covers all 30 IDs. -->

Platform-comparison matrix shape (D-19 — opens the section):

| Platform | Native-AI surface (2026) | API protocol | Sandbox access | `native_ai_path` default | Known research-blocked items |
|----------|--------------------------|--------------|-----------------|--------------------------|------------------------------|
| pipefy | AI Agents (KB + Skills + MCP + IDP + Web Search + BYO-LLM) | GraphQL | Sandbox tenant per client | `api` (HIGH on Behaviors/Skills; LOW on KB content-upload) | KB content-upload endpoint; GraphQL pagination cursor field names; 2026 rate-limit currency |
| wrike | Copilot + 16 MCP tools | REST | Sandbox space per client; `host` from OAuth token persisted (NOT hardcoded `www.wrike.com`) | `api` (MEDIUM; depends on Copilot knowledge-ingestion API) | AI Studio knowledge-ingestion API; 2026 rate-limit currency |
| ziflow | ReviewAI Checklists Public Preview (Change Verification + Brand Standards Coming Soon) | REST | Sandbox project per client | `paste` (Checklists Public Preview is the documented path; copy-paste fallback default) | ReviewAI knowledge-ingestion API; read-after-create consistency window |

Per-platform H3 subsection shape:

```markdown
### platform-pipefy

> **DESIGN-14:** `platform-pipefy/` skill structure — 5-file `references/` shape (`api-contract.md`, `native-ai-inventory.md`, `knowledge-ingestion.md`, `client-shape-gotchas.md`, `vocabulary.md`); 2026-grounded native-AI capability matrix; API surface for the gap; sandbox access pattern; `native_ai_path: api | paste | none` flag with confidence; `tier_claims_last_verified` frontmatter.

**Skill folder layout (5-file references/ shape per DESIGN-14):**

```text
dydx-delivery/skills/platform-pipefy/
├── SKILL.md
└── references/
    ├── api-contract.md           # GraphQL endpoints, paginate_all helper, rate-limit handling
    ├── native-ai-inventory.md    # AI Agents capability matrix
    ├── knowledge-ingestion.md    # KB content-upload path (LOW-confidence — see [OPEN] below)
    ├── client-shape-gotchas.md   # per-client pipe shape variations
    └── vocabulary.md             # Pipefy-specific terms
```

**Native-AI capability matrix (2026-grounded per RESEARCH.md / FEATURES.md):**

| Capability | Available? | Surface | Confidence |
|------------|------------|---------|------------|
| Knowledge base | yes | Pipefy AI Agents → KB | HIGH |
| Skills | yes | AI Agents → Skills | HIGH |
| MCP integration | yes | AI Agents → MCP | HIGH |
| IDP | yes | AI Agents → IDP | HIGH |
| Web Search | yes | AI Agents → Web Search | HIGH |
| BYO-LLM | yes | AI Agents config | HIGH |
| KB content-upload via API | unknown | `[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` | LOW |

**API surface for the gap:**

- Protocol: GraphQL via Pipefy public API.
- Helper: `paginate_all(query, cursor_field)` for cursor pagination.
- Cursor field names: `[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]`.
- Rate limit: `[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]`
- Auth: Bearer token from Pipefy app installation; sandbox token distinct from production.

**Sandbox access pattern:** Sandbox tenant per client; production OUT OF SCOPE. `client_state.yaml` (DESIGN-29 — forward reference, populated in Plan 02-09) carries `pipefy_sandbox_pipe_id:` per client.

**`native_ai_path` flag (DESIGN-26 routing — forward reference):** `api` for Behaviors instructions + Skills config (HIGH-confidence). `paste` fallback for KB content-upload UNTIL the `[OPEN]` resolves.

**Frontmatter contract:** `tier_claims_last_verified: <ISO date>`; `platform: pipefy`.

**Cross-references:** DESIGN-22 (Stage 6 cost — forward); DESIGN-23 (Stage 7b — forward); DESIGN-24 (Stage 8a — forward); DESIGN-26 (Stage 10 — forward); AUDIT.md §AUDIT-04.1.
```

Equivalent shape for **platform-wrike** (DESIGN-15) and **platform-ziflow** (DESIGN-16) per Task 2.

Inline `[OPEN: Phase 4 — ...]` markers added by this plan (count: >= 5):
1. **DESIGN-14:** Pipefy AI KB content-upload endpoint (Phase 7 owner)
2. **DESIGN-14:** Pipefy GraphQL pagination cursor field names (Phase 2 owner)
3. **DESIGN-14:** Pipefy 2026 rate-limit currency (Phase 1/Phase 2 owner)
4. **DESIGN-15:** Wrike AI Studio knowledge-ingestion API (Phase 7 owner)
5. **DESIGN-15:** Wrike 2026 rate-limit currency (Phase 1/Phase 2 owner)
6. **DESIGN-16:** Ziflow ReviewAI knowledge-ingestion API (Phase 7 owner)
7. **DESIGN-16:** Ziflow read-after-create consistency window (Phase 2 owner)

7 markers; satisfies >= 5 floor with margin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Platform skills` opening matrix + DESIGN-14 platform-pipefy subsection</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-03 complete)
    - .planning/REQUIREMENTS.md §"Platform skills (internalisation)" DESIGN-14
    - .planning/research/FEATURES.md — Pipefy AI Agents (KB, Skills, MCP, IDP, Web Search, BYO-LLM)
    - .planning/research/STACK.md — Pipefy GraphQL surface notes
    - .planning/research/PITFALLS.md — Pipefy CRIT/MOD/MIN entries
    - .planning/AUDIT.md §AUDIT-04.1
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-21; §"Open-questions handling" D-27, D-33
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Platform skills` body in two parts:

    **(A) Section opening + 3-row platform-comparison matrix.** Under the existing `## Platform skills` H2, before the H3 subsections, add:
    1. One-paragraph framing: "The three platform skills (`platform-pipefy/`, `platform-wrike/`, `platform-ziflow/`) internalise per-platform knowledge that v0.3.0 referenced as missing artefacts (per AUDIT.md §AUDIT-04.1). Each carries an identical 5-file `references/` shape: `api-contract.md` + `native-ai-inventory.md` + `knowledge-ingestion.md` + `client-shape-gotchas.md` + `vocabulary.md`. The native-AI capability matrices are 2026-grounded per RESEARCH.md / FEATURES.md. Per D-21, this section ships decision contracts only — full SKILL.md and references/*.md prose authoring runs in v2.2 per CHANGELIST.md PLAT-01..03."
    2. Platform-comparison matrix (3 rows × 6 columns) per `<interfaces>` shape above.

    **(B) `### platform-pipefy` H3 subsection (DESIGN-14)** per `<interfaces>` shape above.

    **Forward-reference handling (per cross-AI review MEDIUM #6):** Cross-references to DESIGN-22/23/24/26 are forward references. State them as anchor citations only ("see DESIGN-22 Stage 6 — forward reference, populated in Plan 02-07"). Do NOT include literal-content claims about those sections.

    **Implementing decisions:** D-19, D-21, D-27 + D-33, D-32, D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-14:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-pipefy/' .planning/DESIGN.md &amp;&amp; grep -qF 'api-contract.md' .planning/DESIGN.md &amp;&amp; grep -qF 'native-ai-inventory.md' .planning/DESIGN.md &amp;&amp; grep -qF 'knowledge-ingestion.md' .planning/DESIGN.md &amp;&amp; grep -qF 'client-shape-gotchas.md' .planning/DESIGN.md &amp;&amp; grep -qF 'vocabulary.md' .planning/DESIGN.md &amp;&amp; grep -qF 'paginate_all' .planning/DESIGN.md &amp;&amp; grep -qF 'tier_claims_last_verified' .planning/DESIGN.md &amp;&amp; PIPEFY_OPEN=$(awk '/^### platform-pipefy/,/^### platform-wrike/' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4') &amp;&amp; [ "$PIPEFY_OPEN" -ge "3" ] &amp;&amp; PLATFORM_TABLE=$(awk '/^## Platform skills/,/^### platform-pipefy/' .planning/DESIGN.md | grep -cE '^\| (pipefy|wrike|ziflow) \|') &amp;&amp; [ "$PLATFORM_TABLE" -ge "3" ] &amp;&amp; echo PIPEFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-14:\*\*' .planning/DESIGN.md` succeeds
    - **Platform-comparison matrix exists** between `## Platform skills` and `### platform-pipefy` (SAFE awk range — distinct start/end anchors): `awk '/^## Platform skills/,/^### platform-pipefy/' .planning/DESIGN.md | grep -cE '^\| (pipefy|wrike|ziflow) \|'` returns >= 3
    - Pipefy 5-file `references/` shape present: all of `api-contract.md`, `native-ai-inventory.md`, `knowledge-ingestion.md`, `client-shape-gotchas.md`, `vocabulary.md` referenced under `### platform-pipefy`
    - Native-AI capability matrix has >= 6 capability rows under platform-pipefy
    - `grep -qF 'paginate_all' .planning/DESIGN.md` succeeds
    - `grep -qF 'tier_claims_last_verified' .planning/DESIGN.md` succeeds
    - Pipefy section carries >= 3 `[OPEN: Phase 4` markers (SAFE awk range — distinct H3 anchors): `awk '/^### platform-pipefy/,/^### platform-wrike/' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4'` returns >= 3
    - Sandbox access pattern subsection mentions `pipefy_sandbox_pipe_id:` (forward link to DESIGN-29)
    - **Forward-reference handling (cross-AI review MEDIUM #6):** Cross-references line cites DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 as anchor placeholders only — acceptance criteria do NOT assert that those sections' body content exists in DESIGN.md at end of Wave 4 (they're populated by later waves). Verify via:
      - `grep -qF 'DESIGN-22' .planning/DESIGN.md` succeeds (anchor cited)
      - `grep -qF 'DESIGN-26' .planning/DESIGN.md` succeeds (anchor cited)
      - `grep -qF 'AUDIT.md §AUDIT-04' .planning/DESIGN.md` succeeds (backward reference — actually populated)
    - `grep -qF 'native_ai_path' .planning/DESIGN.md` succeeds
  </acceptance_criteria>
  <done>
    `## Platform skills` opening matrix + framing prose populated. `### platform-pipefy` H3 subsection populated with all DESIGN-14 sub-bullet contracts. Forward references to DESIGN-22/23/24/26 are cited as anchor placeholders only (per cross-AI review MEDIUM #6 — acceptance criteria do not assert their body content).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `### platform-wrike` (DESIGN-15) + `### platform-ziflow` (DESIGN-16) subsections + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1)
    - .planning/REQUIREMENTS.md §"Platform skills" DESIGN-15 + DESIGN-16
    - .planning/research/FEATURES.md — Wrike Copilot + 16 MCP tools; Ziflow ReviewAI sections
    - .planning/research/PITFALLS.md — Wrike `host` field hardcode bug; Ziflow read-after-create consistency
    - .planning/AUDIT.md §AUDIT-04.1
    - .planning/phases/02-design/02-CONTEXT.md §"Open-questions handling" D-27
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `### platform-wrike` and `### platform-ziflow` H3 subsections using the SAME shape as platform-pipefy but with platform-specific contracts:

    **(A) `### platform-wrike` (DESIGN-15)** — replace placeholder with:
    1. Echo line: `> **DESIGN-15:** \`platform-wrike/\` skill structure — 5-file \`references/\` shape; 2026-grounded native-AI capability matrix (Copilot + 16 MCP tools); API surface for the gap (REST; \`host\` from OAuth token persisted, NOT hardcoded \`www.wrike.com\`); sandbox access; knowledge-ingestion via attach-doc-via-MCP.`
    2. Skill folder layout — same 5-file shape, path `dydx-delivery/skills/platform-wrike/`.
    3. Native-AI capability matrix: rows for Copilot (HIGH), MCP tools (HIGH — 16 tools), Knowledge ingestion via attach-doc-via-MCP (MEDIUM), AI Studio knowledge-ingestion API (`[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]`).
    4. API surface for the gap:
       - Protocol: REST.
       - **CRITICAL:** `host` field MUST be persisted from OAuth token response. Hardcoding `www.wrike.com` is the v0.3.0 bug per RESEARCH.md / PITFALLS.md.
       - Rate limit: `[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]`
       - Auth: OAuth token; sandbox space distinct from production.
    5. Sandbox access: Sandbox space per client; `client_state.yaml` (DESIGN-29 — forward reference) carries `wrike_sandbox_space_id:` AND `wrike_host:`.
    6. `native_ai_path` flag: `api` for Copilot + 16 MCP tools (HIGH). `paste` fallback for AI Studio knowledge ingestion UNTIL `[OPEN]` resolves.
    7. Frontmatter contract: `tier_claims_last_verified: <ISO date>` + `platform: wrike`.
    8. Cross-references (forward): DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 / DESIGN-29 (`wrike_host` field) / AUDIT.md §AUDIT-04.1.

    **(B) `### platform-ziflow` (DESIGN-16)** — replace placeholder with:
    1. Echo line: `> **DESIGN-16:** \`platform-ziflow/\` skill structure — 5-file \`references/\` shape; 2026-grounded native-AI capability matrix (ReviewAI Checklists Public Preview; Change Verification + Brand Standards Coming Soon); API surface for the gap (REST; \`wait_for_proof\` helper for eventual consistency); sandbox access; knowledge-ingestion path = Checklist generation primarily + copy-paste fallback.`
    2. Skill folder layout — same 5-file shape, path `dydx-delivery/skills/platform-ziflow/`.
    3. Native-AI capability matrix: Checklists Public Preview (HIGH), Change Verification (Coming Soon — MEDIUM), Brand Standards (Coming Soon — MEDIUM), ReviewAI knowledge-ingestion API (`[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]`).
    4. API surface for the gap:
       - Protocol: REST.
       - Helper: `wait_for_proof(proof_id, max_wait_s)` for eventual consistency.
       - Read-after-create consistency window: `[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]`
       - Auth: API key in header; sandbox project distinct from production.
    5. Sandbox access: Sandbox project per client; `client_state.yaml` (DESIGN-29 — forward reference) carries `ziflow_sandbox_project_id:`.
    6. `native_ai_path` flag: `paste` (DEFAULT). `api` IF the knowledge-ingestion `[OPEN]` enables it.
    7. Frontmatter contract: `tier_claims_last_verified: <ISO date>` + `platform: ziflow`.
    8. Cross-references (forward): DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 / AUDIT.md §AUDIT-04.

    **(C) After both subsections, run structural-check**: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 1 (echo count = 16/30).

    **Implementing decisions:** D-19, D-21, D-27 + D-33, D-32, D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-15:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-16:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-wrike/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-ziflow/' .planning/DESIGN.md &amp;&amp; grep -qF 'wrike_host' .planning/DESIGN.md &amp;&amp; grep -qF 'wait_for_proof' .planning/DESIGN.md &amp;&amp; grep -qF 'OAuth' .planning/DESIGN.md &amp;&amp; grep -qF 'Checklists Public Preview' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "16" ] &amp;&amp; OPEN_COUNT=$(grep -cE '\[OPEN: Phase 4' .planning/DESIGN.md) &amp;&amp; [ "$OPEN_COUNT" -ge "5" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo PLATFORM_SKILLS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-15:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qE '^> \*\*DESIGN-16:\*\*' .planning/DESIGN.md` succeeds
    - Wrike 5-file references/ shape present under `### platform-wrike`
    - Ziflow 5-file references/ shape present under `### platform-ziflow`
    - `grep -qF 'wrike_host' .planning/DESIGN.md` succeeds (DESIGN-15 critical bug-prevention)
    - `grep -qF 'OAuth' .planning/DESIGN.md` succeeds
    - `grep -qF 'wait_for_proof' .planning/DESIGN.md` succeeds (DESIGN-16 helper)
    - `grep -qF 'Checklists Public Preview' .planning/DESIGN.md` succeeds
    - `grep -qF 'Change Verification' .planning/DESIGN.md` AND `grep -qF 'Brand Standards' .planning/DESIGN.md` succeed
    - **Wrike section has >= 2 inline `[OPEN: Phase 4` markers** (SAFE awk range — distinct H3 anchors):
      ```bash
      awk '/^### platform-wrike/,/^### platform-ziflow/' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4'
      ```
      returns >= 2
    - **Ziflow section has >= 2 inline `[OPEN: Phase 4` markers** (SAFE awk range — uses stateful section_between since the `### platform-ziflow` is the LAST H3 in the section, with no obvious next-H3 anchor):
      ```bash
      awk -v hdr='### platform-ziflow' 'f && /^### /{exit} f && /^## /{exit} f{print} index($0,hdr)==1 && /^### /{f=1}' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4'
      ```
      returns >= 2
    - Total inline `[OPEN: Phase 4` count across DESIGN.md: `grep -cE '\[OPEN: Phase 4' .planning/DESIGN.md` returns >= 8 (1 seed + 3 pipefy + 2 wrike + 2 ziflow)
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `16`
    - `bash design-structure-check.sh` exits with code `1` (mid-phase)
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** Cross-references to DESIGN-22/23/24/26 in this section's content are anchor placeholders. Acceptance criteria do NOT assert that those sections' body content exists yet. (No checks here for "Stage 6 risk-multiplier table content" or similar — those land in Plan 02-07/02-08.)
  </acceptance_criteria>
  <done>
    `### platform-wrike` and `### platform-ziflow` populated. Echo count = 16/30. >= 8 inline `[OPEN]` markers across DESIGN.md. Forward references handled per cross-AI review MEDIUM #6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native-AI capability matrices → 2026 platform reality | These claims are 2026-grounded but research-derived. `tier_claims_last_verified` frontmatter exists to surface this risk. |
| `[OPEN: Phase 4 — ...]` markers → Phase 4 OPEN-QUESTIONS register | Every inline marker must be enumerated in Plan 02-10's closed list. Marker text format must be parseable. |
| Forward references to DESIGN-22/23/24/26 — those sections do not exist yet at end of Wave 4 (cross-AI review MEDIUM #6) | Acceptance criteria treat cross-references as anchor placeholders only; full body-content verification happens in Plan 02-10 Appendix B traceability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Native-AI matrix claims a non-existent capability | mitigate | Each row carries explicit Confidence column. LOW = `[OPEN]` flagged. `tier_claims_last_verified` forces v2.2 re-verify. |
| T-02-04-02 | Tampering | Wrike `host` hardcode bug-prevention contract is buried | mitigate | Triple-mention: API surface CRITICAL callout, `client_state.yaml` reference, cross-references. |
| T-02-04-03 | Tampering | Plan author writes full SKILL.md prose (violates D-21) | mitigate | Acceptance criteria check for specific contract elements (5-file shape, capability matrix rows, helper names, frontmatter contract). |
| T-02-04-04 | Tampering | `[OPEN]` marker text format inconsistent | mitigate | Action specifies exact marker template. |
| T-02-04-05 | Repudiation | Confidence column values not justified | mitigate | Action specifies Confidence values per row with rationale. |
| T-02-04-06 | Tampering | DESIGN-15 omits the OAuth `host` persistence rule | mitigate | Acceptance criteria assert `grep -qF 'wrike_host'`. |
| T-02-04-07 | Tampering | Acceptance criteria assert content from forward-referenced sections (DESIGN-22/26 etc.) that don't exist yet — would create false-fail at end of Wave 4 | mitigate | Cross-AI review MEDIUM #6 fix: acceptance criteria treat cross-references as anchor placeholders only. The `<action>` block explicitly notes "forward reference, populated in later wave" for DESIGN-22/23/24/26 citations. Body-content verification deferred to Plan 02-10 (synthesis Appendix B). |
</threat_model>

<verification>
End of Plan 02-04 success-state:
1. `## Platform skills` opens with framing paragraph + 3-row platform-comparison matrix
2. `### platform-pipefy`, `### platform-wrike`, `### platform-ziflow` H3 subsections populated
3. >= 8 total inline `[OPEN: Phase 4]` markers in DESIGN.md
4. Echo count = 16/30
5. `bash design-structure-check.sh` exits 1 with assertion #4 (echo count) failing
6. Forward references to DESIGN-22/23/24/26 are anchor placeholders only (cross-AI review MEDIUM #6 — body verification deferred to Plan 02-10)

Manual reviewer attestation:
- [ ] Per-platform native-AI matrix capability rows match 2026-grounded RESEARCH.md / FEATURES.md
- [ ] All Confidence column values are HIGH / MEDIUM / LOW with consistent application
- [ ] No full SKILL.md or references/*.md prose drafted (D-21 honoured)
- [ ] All `[OPEN]` markers cite OPEN-01 OR Phase number per CHANGE-04 (uniform format for Plan 02-10)
- [ ] Wrike `host` persistence rule appears in DESIGN-15 + DESIGN-29 cross-reference
- [ ] Forward references handled as anchor placeholders only (no false-fail on not-yet-written content)
</verification>

<success_criteria>
- [ ] Platform-comparison matrix (3 rows × 6 columns) opens `## Platform skills`
- [ ] DESIGN-14 / 15 / 16 echo lines + per-platform contracts populated
- [ ] >= 8 total inline `[OPEN: Phase 4]` markers across DESIGN.md
- [ ] Echo count = 16/30
- [ ] Structural-check exits 1 (mid-phase)
- [ ] No content under `dydx-delivery/` modified
- [ ] Forward-reference guardrails applied (cross-AI review MEDIUM #6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-04-SUMMARY.md` documenting:
- Platform-comparison matrix: 3 platforms × 6 columns
- Per-platform contracts (each platform)
- Inline `[OPEN]` markers added: 7 (3 pipefy + 2 wrike + 2 ziflow); cumulative = 8 with seed
- Echo count: 16/30
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #6 (forward-reference guardrails — cross-references to DESIGN-22/23/24/26 are anchor placeholders only, body verification deferred to Plan 02-10).
- Pointer: "Plan 02-05 (Wave 5) populates Stage 1-3 skills next."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-04-SUMMARY.md` to `files_modified`); MEDIUM #6 (forward-reference guardrails — added explicit `<objective>` callout that DESIGN-22/23/24/26 cross-references are forward references; acceptance criteria do not assert their body content exists at end of Wave 4; T-02-04-07 added to threat register); Ziflow inline-`[OPEN]` count check uses stateful section_between awk pattern (since `### platform-ziflow` has no following H3 in the same section, only an H2 boundary). Existing safe `awk '/^### platform-pipefy/,/^### platform-wrike/'` patterns kept (distinct start/end anchors — not the broken `/^## X/,/^## /` pattern). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-14/15/16 still in `requirements`; D-19/21/27/32/33/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
