---
plan_id: 04-01
phase: 04-open-questions
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05, OPEN-06, OPEN-07]
files_modified:
  - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
  - .planning/OPEN-QUESTIONS.md
  - .planning/phases/04-open-questions/04-01-SUMMARY.md
autonomous: true
tags: [open-questions, scaffold, structural-check, design-only]

must_haves:
  truths:
    - "openquestions-structure-check.sh exists at .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh and is executable bash."
    - "OPEN-QUESTIONS.md exists at .planning/OPEN-QUESTIONS.md (planning root, NOT under phase folder) per D-13 precedent (Phase 1 audit-structure-check parity)."
    - "OPEN-QUESTIONS.md skeleton contains all 12 required H2 section anchors per D-53 (verbatim strings: Executive Summary / How to read this register / OPEN-01..07 / Appendix A / Appendix B / Appendix C) — empty bodies only."
    - "openquestions-structure-check.sh row regex supports decimal IDs `OPEN-Q[0-9]+(\\.[0-9]+)?` per cross-AI review C2 (A4 + A10 + A11 + A13 all use the decimal-aware regex)."
    - "openquestions-structure-check.sh A13 (no-placeholder final-only) targets ACTUAL stub patterns `Populated by 04-0[1-9]|Preamble placeholder|placeholder` per cross-AI review C1 — does NOT reject bare `04-0[1-9]` plan-ID references in narrative bodies (e.g., Appendix C precedent citations)."
    - "openquestions-structure-check.sh A4 register-row floor is `>= 21` per cross-AI review C4 (replaces brittle hard-floor 22); Appendix B 1:1 cardinality (A10) + Appendix C 5-condition block (A11) carry the rest of the count discipline."
    - "openquestions-structure-check.sh A14 citation-validity pass per cross-AI review C5 — for each backtick-wrapped `path:line` citation, asserts the file exists AND the line number is a valid line of that file (sampled subset for performance; FULL pass at synthesis time in Plan 04-05)."
    - "openquestions-structure-check.sh A8 owner enum tightened to `Phase [1-9]|TBD` per cross-AI review C6 — hybrid `Phase 1/Phase 2` removed; OPEN-Q06/Q07 in Plan 04-02 are split into single-owner rows."
    - "openquestions-structure-check.sh exits NON-ZERO at end of Wave 1 (sections empty / no register rows / no Appendix B traceability rows / no reconciliation block) — confirms assertions catch missing content (Wave 1 negative-invariant per Phase 2 02-01 + Phase 3 03-01 precedent)."
  artifacts:
    - path: ".planning/phases/04-open-questions/scripts/openquestions-structure-check.sh"
      provides: "Phase 4 structural verifier — grep/awk assertions for OPEN-QUESTIONS.md mirroring changelist-structure-check.sh shape per D-54, with cross-AI review fixes C1/C2/C4/C5/C6 wired into A4/A8/A10/A11/A13 + new A14."
      contains: "set -euo pipefail; OPENQ_FILE; fail() helper; section_between() stateful awk helper; assertions covering 12 H2 anchors (with H2 uniqueness loop per cross-AI C6 carried), per-OPEN-NN H3 row floor (decimal-ID-aware regex; >= 21 floor), severity closed enum, resolution-path closed enum, status closed enum, owning-phase enum (Phase [1-9]|TBD only — no hybrid), citation backtick-pattern, citation-VALIDITY sample (file exists + line valid), Appendix B traceability cardinality (decimal-ID-aware), Appendix C reconciliation block, no-placeholder narrowed-regex final-only assertion."
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "Self-contained register deliverable (skeleton at this stage; populated by plans 04-02..04-05)"
      contains: "H1 title + preamble placeholder blockquote + 12 H2 anchors: Executive Summary / How to read this register / OPEN-01..07 / Appendix A: Per-phase rollup / Appendix B: Source traceability / Appendix C: Reconciliation algorithm result"
  key_links:
    - from: ".planning/phases/04-open-questions/scripts/openquestions-structure-check.sh"
      to: ".planning/OPEN-QUESTIONS.md"
      via: "OPENQ_FILE variable + grep -qF / grep -cE / section_between assertions"
      pattern: 'OPENQ_FILE=".planning/OPEN-QUESTIONS.md"'
    - from: ".planning/OPEN-QUESTIONS.md skeleton"
      to: "Plans 04-02..04-05 (each writes its OPEN-NN slice / synthesis slice into the skeleton)"
      via: "H2 anchors land in this plan; later plans append content under matching anchor"
      pattern: '^## (Executive Summary|How to read this register|OPEN-0[1-7]:|Appendix [A-C]:)'
---

<objective>
Wave 1 scaffold: produce the Phase 4 structural-check script and the OPEN-QUESTIONS.md skeleton so every later plan (04-02 through 04-05) writes its slice into a known location and gets graded by the same assertions. Mirrors Phase 3 Plan 03-01 (changelist scaffold) which mirrored Phase 2 Plan 02-01 (design scaffold) which mirrored Phase 1 Plan 01-01 (audit scaffold).

**Purpose:** Establishes verification infrastructure (structural-check per D-54 mandate) AND the OPEN-QUESTIONS.md section spine before any content lands. Locks the document structure decisions D-46 (hybrid OPEN-NN-primary + per-phase-secondary), D-47 (closed 9-field row schema — anchor only at this stage), D-48 (severity 3-tier closed enum BLOCKER/GUARDRAIL/INFORMATIONAL), D-49 (resolution-path 5-value closed enum), D-53 (12 H2 anchor layout), D-54 (mandatory script + assertion floor).

**Cross-AI review fixes wired into the script (per `.planning/phases/04-open-questions/04-REVIEWS.md` C1..C8):**
- **C1 (A13 narrowed regex):** A13 no-placeholder final-only assertion targets `Populated by 04-0[1-9]|Preamble placeholder|placeholder` — NOT bare `04-0[1-9]`. This permits Plan 04-05 Appendix C narrative references to algorithm precedents (`Phase 3 03-07`, `Phase 2 02-10`, `synthesis Plan 04-05`) without false-fail.
- **C2 (decimal-ID regex):** A4 + A10 + A14 row regexes use `OPEN-Q[0-9]+(\.[0-9]+)?` so OPEN-Q21.1 (OPEN-06 namespace sub-decision row, Plan 04-04) is counted, not silently dropped.
- **C4 (floor >= 21):** A4 register-row floor is `>= 21` (replaces brittle hard-floor 22). Appendix B 1:1 cardinality (A10) + Appendix C 5-condition block (A11) carry the rest of the count discipline. Wave 4 baseline is 21 rows; A4 floor 21 still passes whether or not OPEN-Q21.1 sub-row exists.
- **C5 (citation-validity A14):** New A14 assertion runs a citation-validity SAMPLE pass — for the first N citations (configurable, default 20), confirms (i) the cited file exists AND (ii) the cited line number is a valid line in that file via `awk 'NR==N {found=1} END {exit !found}'`. The FULL citation-validity pass runs at synthesis time in Plan 04-05's reconciliation algorithm (Appendix C ALL_CITATIONS_VERIFIED = TRUE).
- **C6 (owner enum tightened):** A8 owner enum is `Phase [1-9]|TBD` only — hybrid `Phase 1/Phase 2` removed because downstream consumers `grep 'Owning phase: Phase <N>'` and would miss hybrid rows. Plan 04-02 splits OPEN-Q06 (Pipefy 2026 rate-limit) and OPEN-Q07 (Wrike 2026 rate-limit) each into TWO single-owner rows (one Phase 1, one Phase 2) so Phase 2 grep-consumer surfaces both items.

**Output:** `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` (executable bash) AND `.planning/OPEN-QUESTIONS.md` skeleton with all 12 H2 anchors but empty bodies. The full requirements list (OPEN-01..07) is carried on this plan because the scaffold creates the anchors all 7 land into; subsequent plans carry their concrete OPEN-NN subset.

**Wave 1 invariant (from Phase 2 02-01 / Phase 3 03-01 Task 3 precedent):** structural-check exits NON-ZERO at end of this plan because some assertions cannot pass against an empty skeleton (per-OPEN-NN row count, Appendix B row count, Appendix C reconciliation block contents). Task 3 verifies this expected fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/phases/04-open-questions/04-CONTEXT.md
@.planning/phases/04-open-questions/04-DISCUSSION-LOG.md
@.planning/phases/04-open-questions/04-REVIEWS.md
@.planning/phases/03-changelist/03-01-PLAN.md
@.planning/phases/03-changelist/03-01-SUMMARY.md
@.planning/phases/03-changelist/scripts/changelist-structure-check.sh
@.planning/phases/02-design/scripts/design-structure-check.sh

<interfaces>
<!-- The structural-check script exposes ONE contract: exit code 0 (all pass) or 1 (first failure with stderr message). -->
<!-- The OPEN-QUESTIONS.md skeleton exposes the H2 anchors that downstream plans append content under. -->
<!-- Script comments reflect actual behaviour: prose-matching greps use `grep -qF` (literal) and `grep -cE` (case-sensitive ERE). -->
<!-- Section-range extraction uses the stateful section_between() helper carried verbatim from Phase 3 (cross-AI HIGH #1 fix). -->
<!-- Cross-AI review C1/C2/C4/C5/C6 fixes wired into A4/A8/A10/A11/A13 + new A14 (see <objective>). -->

The 12 required H2 anchors (verbatim strings — D-53):

```
## Executive Summary
## How to read this register
## OPEN-01: Research-flagged unverified items
## OPEN-02: Connector-availability uncertainties
## OPEN-03: Design-decision-deferred items
## OPEN-04: Hub-link backfill rollout coordination
## OPEN-05: Standard Coda templates v2 must author
## OPEN-06: /refine-<skill> resolution
## OPEN-07: Plugin self-test scope
## Appendix A: Per-phase rollup index
## Appendix B: Source traceability
## Appendix C: Reconciliation algorithm result
```

Bash skeleton (CRITICAL — uses stateful section_between helper, mirroring Phase 3 03-01 patterns including cross-AI HIGH #1 / LOW #8 / C6 / C9 carried fixes; PLUS Phase 4 cross-AI C1/C2/C4/C5/C6 fixes):

```bash
#!/usr/bin/env bash
# openquestions-structure-check.sh — structural verifier for .planning/OPEN-QUESTIONS.md
#
# Comment-vs-code policy (carried from Phase 3 changelist-structure-check.sh per cross-AI LOW #8):
#   - Sentinel-token matches use `grep -qF` (literal-string, case-sensitive). Used for exact D-IDs,
#     OPEN-NN, exact H2 anchor strings, severity / resolution-path / status enum literals.
#   - Pattern matches use `grep -cE` (case-sensitive extended regex). Used for register row counts,
#     citation backtick-pattern, Appendix B / C traceability cardinality.
#   - This script does NOT use case-insensitive matching (no -i flag). Capitalisation MUST be exact.
#
# Section-range extraction (carried from Phase 2 cross-AI HIGH #1 / Phase 3 03-01):
#   The naive `awk '/^## X/,/^## /'` range pattern is broken because the start H2 also matches
#   the end H2 regex on the same line — the range captures only the header line. The stateful
#   helper section_between() exits only on the NEXT H2.
#
# Phase 4 cross-AI review fixes (per .planning/phases/04-open-questions/04-REVIEWS.md):
#   - C1: A13 narrowed regex. Targets ACTUAL stubs only (`Populated by 04-0[1-9]`, `Preamble placeholder`,
#     literal word `placeholder`). Does NOT reject bare `04-0[1-9]` — Plan 04-05 Appendix C cites
#     algorithm precedents (`Phase 3 03-07`, `Phase 2 02-10`, `synthesis Plan 04-05`) and those are
#     legitimate narrative references.
#   - C2: A4 / A10 / A14 row regexes use `OPEN-Q[0-9]+(\.[0-9]+)?` (decimal-ID-aware) so OPEN-Q21.1
#     (OPEN-06 namespace sub-decision row, Plan 04-04) is counted, not silently dropped.
#   - C4: A4 register-row floor is `>= 21` (replaces brittle hard-floor 22). Wave 4 baseline is 21
#     rows minimum (without OPEN-Q21.1 sub-row); the floor is robust to whether the sub-row exists.
#     Appendix B 1:1 cardinality (A10) + Appendix C 5-condition block (A11) carry the rest of the
#     count discipline; ROADMAP success-criteria coverage is checked at synthesis time in Plan 04-05.
#   - C5: A14 new citation-validity SAMPLE assertion. For the first 20 backtick-wrapped citations,
#     confirm (i) cited file exists AND (ii) cited line number is a valid line of that file. The
#     FULL pass runs at synthesis time in Plan 04-05's reconciliation algorithm.
#   - C6: A8 owner enum tightened to `Phase [1-9]|TBD` only. Hybrid `Phase 1/Phase 2` removed because
#     downstream consumers `grep 'Owning phase: Phase <N>'` and would miss hybrid rows. Plan 04-02
#     splits OPEN-Q06 + OPEN-Q07 each into TWO single-owner rows (one Phase 1, one Phase 2).
set -euo pipefail
OPENQ_FILE=".planning/OPEN-QUESTIONS.md"
fail() { echo "FAIL: $1" >&2; exit 1; }

section_between() {
  local heading="$1"
  awk -v hdr="$heading" '
    f && /^## /{exit}
    f{print}
    index($0, hdr) == 1 && /^## /{f=1}
  ' "$OPENQ_FILE"
}

[ -f "$OPENQ_FILE" ] || fail "$OPENQ_FILE not found"

# A1. Required H2 anchors per D-53 (12 sections — verbatim)
declare -a required_h2=(
  "## Executive Summary"
  "## How to read this register"
  "## OPEN-01: Research-flagged unverified items"
  "## OPEN-02: Connector-availability uncertainties"
  "## OPEN-03: Design-decision-deferred items"
  "## OPEN-04: Hub-link backfill rollout coordination"
  "## OPEN-05: Standard Coda templates v2 must author"
  "## OPEN-06: /refine-<skill> resolution"
  "## OPEN-07: Plugin self-test scope"
  "## Appendix A: Per-phase rollup index"
  "## Appendix B: Source traceability"
  "## Appendix C: Reconciliation algorithm result"
)
for h in "${required_h2[@]}"; do
  grep -qF "$h" "$OPENQ_FILE" || fail "missing required H2 anchor: $h"
done

# A2. H2 uniqueness loop (per cross-AI C6 carried from Phase 3 — codex MEDIUM):
#   every required H2 must appear EXACTLY once. Duplicates break section_between.
for h in "${required_h2[@]}"; do
  count=$(grep -cF "$h" "$OPENQ_FILE" || true)
  [ "$count" = "1" ] || fail "H2 anchor '$h' appears $count times (expected exactly 1)"
done

# A3. Exactly 7 OPEN-NN H2 anchors (per D-46 — REQUIREMENTS.md OPEN-01..07 1:1)
open_nn_count=$(grep -cE '^## OPEN-0[1-7]: ' "$OPENQ_FILE" || true)
[ "$open_nn_count" = "7" ] || fail "expected exactly 7 '## OPEN-0N: ' H2 anchors, found $open_nn_count"

# A4. Register-row count floor (per cross-AI C4 — was hard-22, now robust >= 21).
#   Each register row is identified by `**OPEN-Q<NN>**` per D-47 (per-row block form) OR
#   `| OPEN-Q<NN> |` (table-form). Cross-AI C2: regex is decimal-ID-aware to count OPEN-Q21.1.
register_rows=$(grep -cE '^\| OPEN-Q[0-9]+(\.[0-9]+)? \|' "$OPENQ_FILE" || true)
register_blocks=$(grep -cE '^\*\*OPEN-Q[0-9]+(\.[0-9]+)?\*\*' "$OPENQ_FILE" || true)
register_total=$(( register_rows + register_blocks ))
[ "$register_total" -ge 21 ] || fail "register row/block count $register_total < 21 (expected >= 21 per cross-AI C4 — Wave 4 baseline; Appendix B 1:1 cardinality + Appendix C reconciliation carry the rest)"

# A5. Severity closed enum (per D-48): every severity field must match BLOCKER|GUARDRAIL|INFORMATIONAL.
#   Disallow any severity literal outside the closed enum.
bad_severity=$(grep -nE 'Severity:[[:space:]]*[A-Z]+' "$OPENQ_FILE" \
  | grep -vE 'Severity:[[:space:]]*(BLOCKER|GUARDRAIL|INFORMATIONAL)' || true)
[ -z "$bad_severity" ] || fail "Severity field outside closed enum (D-48): $bad_severity"
sev_total=$(grep -cE 'Severity:[[:space:]]*(BLOCKER|GUARDRAIL|INFORMATIONAL)' "$OPENQ_FILE" || true)
[ "$sev_total" -ge 21 ] || fail "Severity field count $sev_total < 21 (expected one per register row; floor follows A4)"

# A6. Resolution-path closed enum (per D-49): every Resolution-path field must match one of 5 enum values.
bad_resolution=$(grep -nE 'Resolution path:[[:space:]]' "$OPENQ_FILE" \
  | grep -vE 'Resolution path:[[:space:]]*(/gsd-research-phase [0-9]+|decide-before-Phase-[0-9]+|Coda-template-authoring \(Phase 8\)|policy-pending-sign-off|live-workstream-pointer)' || true)
[ -z "$bad_resolution" ] || fail "Resolution path field outside closed enum (D-49): $bad_resolution"
res_total=$(grep -cE 'Resolution path:[[:space:]]*(/gsd-research-phase [0-9]+|decide-before-Phase-[0-9]+|Coda-template-authoring \(Phase 8\)|policy-pending-sign-off|live-workstream-pointer)' "$OPENQ_FILE" || true)
[ "$res_total" -ge 21 ] || fail "Resolution path field count $res_total < 21 (expected one per register row; floor follows A4)"

# A7. Status closed enum (per D-47): every status field must match open|proposed|decided|closed.
bad_status=$(grep -nE 'Status:[[:space:]]' "$OPENQ_FILE" \
  | grep -vE 'Status:[[:space:]]*(open|proposed|decided|closed)' || true)
[ -z "$bad_status" ] || fail "Status field outside closed enum (D-47): $bad_status"

# A8. Owning-phase enum tightened per cross-AI C6: `Phase [1-9]|TBD` only (no hybrid).
#   Hybrid `Phase 1/Phase 2` removed because downstream consumers grep `Owning phase: Phase <N>` and
#   would miss hybrid rows. Plan 04-02 splits OPEN-Q06 (Pipefy rate-limit) + OPEN-Q07 (Wrike rate-limit)
#   each into TWO single-owner rows (one Phase 1, one Phase 2).
bad_owner=$(grep -nE 'Owning phase:[[:space:]]' "$OPENQ_FILE" \
  | grep -vE 'Owning phase:[[:space:]]*(Phase [1-9]|TBD)[[:space:]]*$' || true)
[ -z "$bad_owner" ] || fail "Owning phase field outside enum (D-47 + cross-AI C6 — single-owner only): $bad_owner"

# A9. Citation backtick-pattern (per D-14 carried): every register row carries >= 1 backtick-wrapped
#   `path:line` citation in its Source citations field. Floor: total citation occurrences >= register_total.
citations=$(grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`' "$OPENQ_FILE" || true)
[ "$citations" -ge "$register_total" ] || fail "citation count $citations < register_total $register_total (D-14: every row needs >= 1 backtick-wrapped path:line citation)"

# A10. Appendix B traceability table cardinality (per D-53): Appendix B row count == register_total.
#   Cross-AI C2: decimal-ID-aware regex.
appendix_b_rows=$(section_between "## Appendix B: Source traceability" \
  | grep -cE '^\| OPEN-Q[0-9]+(\.[0-9]+)? \|' || true)
[ "$appendix_b_rows" = "$register_total" ] || fail "Appendix B row count $appendix_b_rows != register total $register_total (D-53 1:1 cardinality)"

# A11. Appendix C reconciliation algorithm result block (per D-53): all 5 conditions present + TRUE.
appendix_c_body=$(section_between "## Appendix C: Reconciliation algorithm result")
echo "$appendix_c_body" | grep -qF 'INPUT_COUNT_AFTER_DEDUP' \
  || fail "Appendix C missing 'INPUT_COUNT_AFTER_DEDUP' assertion line"
echo "$appendix_c_body" | grep -qF 'REGISTER_ROW_COUNT' \
  || fail "Appendix C missing 'REGISTER_ROW_COUNT' assertion line"
echo "$appendix_c_body" | grep -qF 'CARDINALITY_MATCH = TRUE' \
  || fail "Appendix C missing 'CARDINALITY_MATCH = TRUE' result line"
echo "$appendix_c_body" | grep -qF 'ALL_CITATIONS_VERIFIED = TRUE' \
  || fail "Appendix C missing 'ALL_CITATIONS_VERIFIED = TRUE' result line"
echo "$appendix_c_body" | grep -qF 'ALL_OWNERS_ASSIGNED = TRUE' \
  || fail "Appendix C missing 'ALL_OWNERS_ASSIGNED = TRUE' result line"

# A12. D-37 OPEN-01 contingent fallback wording (verbatim where Phase 7 BLOCKER rows are described).
#   Phase 4 carries the verbatim D-37 sentence in the OPEN-01 section preamble or the Phase 7 register rows.
grep -qF 'slide P8/P9 → v2.7' "$OPENQ_FILE" \
  || fail "missing D-37 OPEN-01 contingent fallback verbatim sentence (substring 'slide P8/P9 → v2.7')"

# A13. No-placeholder synthesis-end assertion (per cross-AI review C1 — NARROWED regex).
#   Targets ACTUAL skeleton stubs only:
#     - `Populated by 04-0[1-9]` (skeleton populated-by notes)
#     - `Preamble placeholder` (skeleton preamble line)
#     - literal word `placeholder` (any leftover stub text)
#   Does NOT reject bare `04-0[1-9]` plan-IDs — Plan 04-05 Appendix C narratively cites algorithm
#   precedents (`Phase 3 03-07`, `Phase 2 02-10`, `synthesis Plan 04-05`) and those are legitimate.
#   EXPECTED to fail in Waves 1..4; Plan 04-05 synthesis MUST leave the document clean of stubs.
#   The legitimate `owner: TBD` literal is permitted and not matched here.
! grep -qE 'Populated by 04-0[1-9]|Preamble placeholder|\bplaceholder\b' "$OPENQ_FILE" \
  || fail "leftover skeleton stub strings detected — A13 narrowed regex (Populated by 04-0[1-9] | Preamble placeholder | placeholder); EXPECTED to fail in Waves 1..4, MUST pass at end of Plan 04-05 synthesis"

# A14. Citation-validity SAMPLE pass (per cross-AI review C5).
#   For the first 20 backtick-wrapped `path:line` citations encountered, confirm:
#     (i) the cited file exists, AND
#     (ii) the cited line number is a valid line in that file (i.e., NR >= line_number).
#   Pattern-only validation (A9) is too weak for D-14/D-50; this gives mechanical proof that
#   citations actually point to real lines. The FULL pass runs at synthesis time in Plan 04-05's
#   reconciliation algorithm (Appendix C ALL_CITATIONS_VERIFIED = TRUE).
#   In Waves 1..3 the document may have 0 citations — the sample is empty and A14 passes vacuously.
sample_count=0
sample_max=20
while IFS= read -r match; do
  [ "$sample_count" -ge "$sample_max" ] && break
  cited_path=$(printf '%s' "$match" | sed -E 's/^.*`(\.planning\/[A-Za-z0-9_./-]+\.md):([0-9]+)`.*$/\1/')
  cited_line=$(printf '%s' "$match" | sed -E 's/^.*`(\.planning\/[A-Za-z0-9_./-]+\.md):([0-9]+)`.*$/\2/')
  [ -f "$cited_path" ] || fail "A14 citation-validity: cited file missing: $cited_path (line $cited_line)"
  total_lines=$(wc -l < "$cited_path" | tr -d '[:space:]')
  if [ "$cited_line" -gt "$total_lines" ]; then
    fail "A14 citation-validity: cited line $cited_line exceeds total lines $total_lines in $cited_path"
  fi
  sample_count=$((sample_count + 1))
done < <(grep -oE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`' "$OPENQ_FILE" || true)

echo "OK: all structural checks passed"
exit 0
```

OPEN-QUESTIONS.md skeleton shape (exact H2 anchors per D-53; empty bodies — Wave 1 only fills the structure):

```markdown
# Open questions register: dydx-delivery v2.0 → v2.x build sequence

**Register date:** 2026-05-10
**Branch / commit:** dydx-delivery-v2 / (record current commit at synthesis plan)
**Phase 1 Audit (v0.3.0 ground truth):** `.planning/AUDIT.md` (approved 2026-05-09)
**Phase 2 Design (v2 architecture ground truth):** `.planning/DESIGN.md` (approved 2026-05-10)
**Phase 3 Change list (v2.x build sequence):** `.planning/CHANGELIST.md` (approved 2026-05-10)

> (Preamble placeholder — finalised in synthesis plan 04-05.)

## Executive Summary

(Executive summary 3-table placeholder — populated by 04-05-PLAN.md / Wave 5 synthesis. Severity rollup + Owning-phase rollup + Resolution-path rollup tables.)

## How to read this register

(Populated by 04-05-PLAN.md / Wave 5 synthesis. 5 bold-headed paragraphs.)

---

## OPEN-01: Research-flagged unverified items

(Populated by 04-02-PLAN.md / Wave 2. Catalogues every research-flagged "couldn't verify" item with Owning phase + Verification owner per D-47 row schema. Heaviest section ~9-11 rows after cross-AI C6 split: 3x BLOCKER native-AI ingestion (Pipefy AI KB / Wrike AI Studio / Ziflow ReviewAI) + 1x Pipefy GraphQL pagination GUARDRAIL + 1x Ziflow read-after-create GUARDRAIL + Pipefy 2026 rate-limit SPLIT into Q06a Phase 1 + Q06b Phase 2 + Wrike 2026 rate-limit SPLIT into Q07a Phase 1 + Q07b Phase 2 + Miro export-whole-board INFORMATIONAL + Claude in Chrome canonical product naming INFORMATIONAL.)

## OPEN-02: Connector-availability uncertainties

(Populated by 04-02-PLAN.md / Wave 2. ~3-4 rows for connector probes — Coda MCP / Google Workspace MCP / Miro MCP / Wrike auth header. All Phase 1 owner; resolution = `/gsd-research-phase 1`.)

## OPEN-03: Design-decision-deferred items

(Populated by 04-03-PLAN.md / Wave 3. ~3-4 rows: risk-multiplier defaults / frontmatter migration cutover date / status-lifecycle survey / plugin self-test scope cross-link to OPEN-07.)

## OPEN-04: Hub-link backfill rollout coordination

(Populated by 04-03-PLAN.md / Wave 3. Single canonical row per D-51 — pointer-only + invariant.)

## OPEN-05: Standard Coda templates v2 must author

(Populated by 04-03-PLAN.md / Wave 3. ~3 rows: brain-mirror template / task-table template / 00_HUB.md Coda block schema. All Phase 8 owner; resolution = `Coda-template-authoring (Phase 8)`.)

## OPEN-06: /refine-<skill> resolution

(Populated by 04-04-PLAN.md / Wave 4. 1-2 rows: namespace decision + parameterised-vs-orphan-delete. Phase 1 deadline; resolution = `policy-pending-sign-off`.)

## OPEN-07: Plugin self-test scope

(Populated by 04-04-PLAN.md / Wave 4. 1 row: smoke tests scope. Phase 9 owner; resolution = `policy-pending-sign-off`.)

---

## Appendix A: Per-phase rollup index

(Populated by 04-05-PLAN.md / Wave 5 synthesis. `| Phase | Question IDs | BLOCKER | GUARDRAIL | INFORMATIONAL |` — one row per v2.x phase that owns >= 1 question.)

## Appendix B: Source traceability

(Populated by 04-05-PLAN.md / Wave 5 synthesis. `| OPEN-QN | Source citations |` — one row per register row; 1:1 cardinality with register-total per D-53. Sorted numerically by OPEN-QNN per cross-AI G2.)

## Appendix C: Reconciliation algorithm result

(Populated by 04-05-PLAN.md / Wave 5 synthesis. Terminal-state proof block: INPUT_COUNT_AFTER_DEDUP / REGISTER_ROW_COUNT / CARDINALITY_MATCH=TRUE / ALL_CITATIONS_VERIFIED=TRUE / ALL_OWNERS_ASSIGNED=TRUE. Reproduces Phase 2 02-10 / Phase 3 03-07 reconciliation precedent — REAL multiset comparison per cross-AI C3, NOT assumed equality.)

---

*Open questions register produced 2026-05-10; catalogues every "couldn't verify" + "needs human decision" item across `.planning/AUDIT.md` (v0.3.0 ground truth) → `.planning/DESIGN.md` (v2 architecture) → `.planning/CHANGELIST.md` (v2.x sequence). Owning phases assigned per CHANGELIST.md CHANGE-01 sequence; v2.x build phases inherit assigned rows as their resolution backlog.*
```

Why this skeleton structure:
- **D-46** — primary by OPEN-NN category (7 H2s mapping 1:1 to REQUIREMENTS.md OPEN-01..07); secondary index by phase in Appendix A.
- **D-53** — exact 12 H2 anchors in document order. Preamble is blockquote (NOT H2) per CONTEXT.md.
- **D-54** — mandatory script artefact written here; assertion floor lands here so downstream plans inherit graded structure.
- **D-44 carry-equivalent** — D-N numbering frozen at D-55 (CONTEXT.md decisions D-46..D-55); Phase 4 plans introduce no new D-N decisions (cross-AI C6 is mitigated by SPLITTING hybrid rows in Plan 04-02 — no new D-N required).

Wave 1 negative-invariant: structural-check FAILS because register-row count is 0 < 21, Appendix B has 0 rows, Appendix C has no reconciliation block, citation count is 0. Task 3 verifies this expected fail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/ directory + write openquestions-structure-check.sh (with stateful section_between helper + 14 assertions including cross-AI review C1/C2/C4/C5/C6 fixes)</name>
  <files>.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh</files>
  <read_first>
    - .planning/phases/04-open-questions/04-CONTEXT.md §"D-53 Synthesis structure" (12 H2 anchor list); §"D-54 Wave / plan structure" (mandatory script + assertion floor); §"D-47 Per-row schema" (closed 9-field shape); §"D-48 Severity closed enum"; §"D-49 Resolution-path closed enum"; §"Cross-cutting authoring decisions" (D-14, D-27, D-28, D-37, D-42)
    - .planning/phases/04-open-questions/04-REVIEWS.md §Codex Review (concerns C1..C8 driving this revision)
    - .planning/phases/03-changelist/scripts/changelist-structure-check.sh — direct precedent for bash idiom (section_between helper, grep -qF / -cE conventions, set -euo pipefail discipline, fail() helper, cross-AI C6 H2-uniqueness loop, cross-AI C9 no-placeholder final-only assertion)
    - .planning/phases/03-changelist/03-01-PLAN.md — direct precedent for this scaffold pattern's task structure
    - .planning/phases/02-design/scripts/design-structure-check.sh — secondary precedent (similar count of assertions to Phase 4)
  </read_first>
  <rationale>
    Mitigates cross-AI review concerns:
    - **C1 (HIGH)** — A13 regex narrowed to actual stub patterns (`Populated by 04-0[1-9]|Preamble placeholder|\bplaceholder\b`). Bare `04-0[1-9]` no longer rejected, so Plan 04-05 Appendix C precedent citations (`Phase 3 03-07`, `Phase 2 02-10`, `synthesis Plan 04-05`) pass cleanly.
    - **C2 (HIGH)** — A4 / A10 / A14 row regexes use `OPEN-Q[0-9]+(\.[0-9]+)?` so OPEN-Q21.1 (Plan 04-04 namespace sub-decision row) is counted, not silently dropped.
    - **C4 (MEDIUM)** — A4 floor lowered from hard-22 to robust `>= 21`. Wave 4 baseline is 21 rows (without the optional OPEN-Q21.1 sub-row). The strictness comes from A10 (Appendix B 1:1) + A11 (5-condition Appendix C block) + ROADMAP coverage walk in Plan 04-05.
    - **C5 (MEDIUM)** — New A14 citation-validity SAMPLE pass: for the first 20 backtick-wrapped citations, confirm file exists AND line number is valid. Full pass runs at synthesis in Plan 04-05.
    - **C6 (MEDIUM)** — A8 owner enum tightened to `Phase [1-9]|TBD` only (no hybrid). Plan 04-02 splits OPEN-Q06 + OPEN-Q07 hybrid-owner rows into single-owner rows.
    - **C7 (LOW)** — Action wording uses platform-neutral verbs ("create file at path", "edit file at path") instead of Claude-specific tool names.
  </rationale>
  <action>
    Create directory `.planning/phases/04-open-questions/scripts/` if not present (run `mkdir -p .planning/phases/04-open-questions/scripts` via bash).

    Create file at path `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` using the EXACT bash skeleton in the `<interfaces>` block above (verbatim — copy character-for-character). The script must contain:

    **A. Header comment block** — accurately describes actual behaviour (carried from Phase 3 03-01 LOW #8 fix) AND documents the Phase 4 cross-AI review fixes C1/C2/C4/C5/C6.

    **B. `section_between()` helper function** — defined near the top, after `set -euo pipefail` and `fail()`. Body verbatim from precedent.

    **C. All 14 assertions in order (named A1..A14 in `<interfaces>` skeleton; each uses `fail()` on miss):**
    1. **A1 — Required H2 anchor presence loop** — each of 12 literal H2 strings (per D-53) must be present via `grep -qF`. (List unchanged.)
    2. **A2 — H2 anchor uniqueness loop** (per cross-AI C6 carried from Phase 3) — for each of 12 anchors, `grep -cF "$h"` MUST return exactly `1`.
    3. **A3 — Exactly 7 OPEN-NN H2 anchors** (per D-46) — `grep -cE '^## OPEN-0[1-7]: '` returns `7`.
    4. **A4 — Register-row count floor `>= 21`** (per cross-AI C4) — sum of `grep -cE '^\| OPEN-Q[0-9]+(\.[0-9]+)? \|'` AND `grep -cE '^\*\*OPEN-Q[0-9]+(\.[0-9]+)?\*\*'`. Decimal-ID-aware (cross-AI C2). Floor: `>= 21`.
    5. **A5 — Severity closed enum** (per D-48) — disallow any `Severity:` literal outside `BLOCKER|GUARDRAIL|INFORMATIONAL` AND require severity-total `>= 21`.
    6. **A6 — Resolution-path closed enum** (per D-49) — disallow any `Resolution path:` literal outside the 5 enum values AND require res-total `>= 21`.
    7. **A7 — Status closed enum** (per D-47) — disallow any `Status:` literal outside `open|proposed|decided|closed`.
    8. **A8 — Owning-phase enum tightened** (per cross-AI C6) — disallow any `Owning phase:` literal outside `Phase [1-9]|TBD` (with optional trailing whitespace anchor `[[:space:]]*$`). Hybrid `Phase 1/Phase 2` REJECTED. Plan 04-02 splits the historic hybrid rows into single-owner rows.
    9. **A9 — Citation backtick-pattern** (per D-14 carried) — `grep -cE '` `` `\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+` `` `'` returns `>= register_total`.
    10. **A10 — Appendix B 1:1 cardinality** (per D-53) — `section_between "## Appendix B: Source traceability" | grep -cE '^\| OPEN-Q[0-9]+(\.[0-9]+)? \|'` MUST equal register_total exactly. Decimal-ID-aware (cross-AI C2).
    11. **A11 — Appendix C reconciliation algorithm result block** (per D-53) — body MUST contain 5 substrings via `grep -qF`: `INPUT_COUNT_AFTER_DEDUP`, `REGISTER_ROW_COUNT`, `CARDINALITY_MATCH = TRUE`, `ALL_CITATIONS_VERIFIED = TRUE`, `ALL_OWNERS_ASSIGNED = TRUE`.
    12. **A12 — D-37 OPEN-01 contingent fallback verbatim** (carried from Phase 3 — wherever Phase 7 BLOCKER rows are described). Substring: `slide P8/P9 → v2.7`.
    13. **A13 — No-placeholder synthesis-end assertion (NARROWED per cross-AI C1)** — `! grep -qE 'Populated by 04-0[1-9]|Preamble placeholder|\bplaceholder\b'`. Targets only ACTUAL stub patterns. Bare `04-0[1-9]` no longer rejected — Plan 04-05 Appendix C narrative references to algorithm precedents pass cleanly. EXPECTED to fail in Waves 1..4; Plan 04-05 synthesis MUST leave document clean.
    14. **A14 — Citation-validity SAMPLE pass (NEW per cross-AI C5)** — iterate first 20 backtick-wrapped `` `path:line` `` citations; for each: (i) `[ -f "$cited_path" ]` (file exists), AND (ii) `cited_line <= total_lines` (line number valid). Vacuously passes when document has 0 citations (Waves 1..3). Use `grep -oE` to extract citations + `sed -E` to parse path + line. Full pass runs at synthesis in Plan 04-05.

    Use `set -euo pipefail` and the single `fail()` helper. Final passing message: `echo "OK: all structural checks passed"; exit 0`.

    After creating the file, mark executable: run `chmod +x .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` (works on Windows Git Bash; Linux honours the bit; `bash <path>` invocation works regardless).

    **Implementing decisions:** D-46 (12 H2 anchors), D-47 (closed 9-field schema enum-grep), D-48 (severity closed enum), D-49 (resolution-path closed enum), D-53 (12 H2 layout + reconciliation algorithm result block + Appendix B 1:1 cardinality), D-54 (mandatory script + assertion floor), D-14 carried (citation backtick-pattern), D-37 carried (OPEN-01 fallback verbatim assertion), cross-AI C6 carried (H2 uniqueness), cross-AI Phase 4 C1 (A13 narrowed), C2 (decimal-ID regex), C4 (floor >= 21), C5 (A14 citation-validity), C6 (A8 owner-enum tightened).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh && bash -n .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh && echo SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` exits 0
    - File is syntactically valid bash: `bash -n .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` exits 0
    - File contains stateful section_between helper: `grep -qF 'section_between()' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds
    - File contains all 12 required H2 anchor literals (12 individual `grep -qF` checks)
    - File contains the H2 uniqueness loop: `grep -qF 'expected exactly 1' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds
    - **C1 fix** — A13 regex narrowed: `grep -qF 'Populated by 04-0[1-9]|Preamble placeholder' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds (literal regex string present in script)
    - **C2 fix** — decimal-ID-aware regex present in A4 + A10: `grep -cF 'OPEN-Q[0-9]+(\.[0-9]+)?' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` returns >= 3 (A4 row form + A4 block form + A10)
    - **C4 fix** — A4 floor is `>= 21`, NOT `>= 22`: `grep -qF 'register_total" -ge 21' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds AND `grep -qF 'register_total" -ge 22' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` returns no match
    - **C5 fix** — A14 citation-validity SAMPLE pass present: `grep -qF 'A14' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds AND `grep -qF 'sample_max' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds AND `grep -qF 'cited_path' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds
    - **C6 fix** — A8 owner-enum tightened (no `Phase 1/Phase 2`): `grep -qF 'Phase 1/Phase 2' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` returns NO MATCH within the disallow regex (the literal string MUST NOT appear in the A8 enum-allow regex; comments referencing the historic hybrid are permitted but the allow-list itself MUST NOT include `Phase 1/Phase 2`). Verify by checking the A8 allow-regex literal: `grep -qF 'Owning phase:[[:space:]]*(Phase [1-9]|TBD)' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds
    - File contains the closed-enum disallow patterns for Severity/Resolution path/Status: `grep -cE 'BLOCKER|GUARDRAIL|INFORMATIONAL' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` returns >= 2; `grep -qF '/gsd-research-phase' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds; `grep -qF 'policy-pending-sign-off' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds; `grep -qF 'live-workstream-pointer' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds; `grep -qF 'Coda-template-authoring (Phase 8)' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds
    - File contains the 5 reconciliation-result substrings: 5 individual `grep -qF` checks for `INPUT_COUNT_AFTER_DEDUP`, `REGISTER_ROW_COUNT`, `CARDINALITY_MATCH = TRUE`, `ALL_CITATIONS_VERIFIED = TRUE`, `ALL_OWNERS_ASSIGNED = TRUE`
    - File contains the D-37 fallback substring: `grep -qF 'slide P8/P9 → v2.7' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` succeeds
    - File contains >= 14 `fail()` invocations (one per assertion): `grep -cE '\|\| fail "' .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` returns >= 14
  </acceptance_criteria>
  <done>
    Phase 4 structural-check script exists at `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh`, syntactically valid, contains stateful section_between helper and all 14 assertions A1..A14 (including cross-AI review C1/C2/C4/C5/C6 fixes). The script will be invoked by Plans 04-02..04-05 to grade their slice of OPEN-QUESTIONS.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OPEN-QUESTIONS.md skeleton with H1 + preamble blockquote + 12 H2 anchors (empty bodies + populated-by placeholder notes)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh (just created in Task 1)
    - .planning/phases/04-open-questions/04-CONTEXT.md §"D-53 Synthesis structure" (skeleton document order)
    - .planning/CHANGELIST.md (skeleton precedent for top matter and closing line)
    - .planning/phases/03-changelist/03-01-SUMMARY.md (Phase 3 scaffold summary for shape reference)
  </read_first>
  <rationale>
    Skeleton populated-by notes use `04-NN-PLAN.md` form which IS matched by A13 narrowed regex (the `Populated by 04-0[1-9]` pattern). Mid-phase A13 fails are EXPECTED per Wave 1..4 invariant; Plan 04-05 synthesis replaces the last 6 notes (preamble + ES + How-to-read + Appendix A + B + C). Cross-AI C7: action wording is platform-neutral.
  </rationale>
  <action>
    Create file at path `.planning/OPEN-QUESTIONS.md` (planning root, NOT under the phase folder) using the EXACT skeleton from the `<interfaces>` block above. Use a file-creation operation (Write or equivalent) — never inline heredoc.

    Skeleton must contain in order:

    **Top matter** (5 lines):
    ```
    # Open questions register: dydx-delivery v2.0 → v2.x build sequence

    **Register date:** 2026-05-10
    **Branch / commit:** dydx-delivery-v2 / (record current commit at synthesis plan)
    **Phase 1 Audit (v0.3.0 ground truth):** `.planning/AUDIT.md` (approved 2026-05-09)
    **Phase 2 Design (v2 architecture ground truth):** `.planning/DESIGN.md` (approved 2026-05-10)
    **Phase 3 Change list (v2.x build sequence):** `.planning/CHANGELIST.md` (approved 2026-05-10)
    ```

    **Preamble blockquote placeholder line:**
    ```
    > (Preamble placeholder — finalised in synthesis plan 04-05.)
    ```

    **All 12 H2 anchors verbatim, each followed by a 1-3 line populated-by note** so subsequent plans know which H2 they own. Use the verbatim populated-by texts from the `<interfaces>` skeleton block above (do not reword). H2 list in document order:

    1. `## Executive Summary` — populated-by note: `(Executive summary 3-table placeholder — populated by 04-05-PLAN.md / Wave 5 synthesis. Severity rollup + Owning-phase rollup + Resolution-path rollup tables.)`
    2. `## How to read this register` — populated-by note: `(Populated by 04-05-PLAN.md / Wave 5 synthesis. 5 bold-headed paragraphs.)`
    3. `## OPEN-01: Research-flagged unverified items` — populated-by note covering Wave 2 ownership + ~9-11 row count after cross-AI C6 split.
    4. `## OPEN-02: Connector-availability uncertainties` — populated-by note covering Wave 2 ownership + ~3-4 rows.
    5. `## OPEN-03: Design-decision-deferred items` — populated-by note covering Wave 3 ownership + ~3-4 rows.
    6. `## OPEN-04: Hub-link backfill rollout coordination` — populated-by note covering Wave 3 ownership + D-51 single-canonical-row pattern.
    7. `## OPEN-05: Standard Coda templates v2 must author` — populated-by note covering Wave 3 ownership + ~3 rows + Phase 8 owner.
    8. `## OPEN-06: /refine-<skill> resolution` — populated-by note covering Wave 4 ownership + 1-2 rows.
    9. `## OPEN-07: Plugin self-test scope` — populated-by note covering Wave 4 ownership + 1 row + Phase 9 owner.
    10. `## Appendix A: Per-phase rollup index` — populated-by note covering Wave 5 ownership + 5-column table shape.
    11. `## Appendix B: Source traceability` — populated-by note covering Wave 5 ownership + 1:1 cardinality with register-total per D-53 + sorted numerically per cross-AI G2.
    12. `## Appendix C: Reconciliation algorithm result` — populated-by note covering Wave 5 ownership + REAL multiset comparison terminal-state proof block per cross-AI C3.

    Insert horizontal rule `---` between Executive Summary block and OPEN-01..07 block, AND between OPEN-07 and Appendix A, AND before the closing italics paragraph (3 horizontal rules total — mirrors CHANGELIST.md skeleton).

    **Closing italics paragraph:**
    ```
    *Open questions register produced 2026-05-10; catalogues every "couldn't verify" + "needs human decision" item across `.planning/AUDIT.md` (v0.3.0 ground truth) → `.planning/DESIGN.md` (v2 architecture) → `.planning/CHANGELIST.md` (v2.x sequence). Owning phases assigned per CHANGELIST.md CHANGE-01 sequence; v2.x build phases inherit assigned rows as their resolution backlog.*
    ```

    **Critical anchor literals — every H2 must be character-exact** including the colons, the inline `<skill>` literal in OPEN-06, and the en-dashes / hyphens. Verify after writing by running each H2 through `grep -qF "## …"` (sample command in verify block).

    **Implementing decisions:** D-46 (primary by OPEN-NN), D-53 (12 H2 anchors document-order), D-44 carry-equivalent (D-N pool frozen at D-55; no new D-N introduced).
  </action>
  <verify>
    <automated>test -f .planning/OPEN-QUESTIONS.md && grep -qF '# Open questions register: dydx-delivery v2.0 → v2.x build sequence' .planning/OPEN-QUESTIONS.md && grep -qF '## Executive Summary' .planning/OPEN-QUESTIONS.md && grep -qF '## How to read this register' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-01: Research-flagged unverified items' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-02: Connector-availability uncertainties' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-03: Design-decision-deferred items' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-04: Hub-link backfill rollout coordination' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-05: Standard Coda templates v2 must author' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-06: /refine-<skill> resolution' .planning/OPEN-QUESTIONS.md && grep -qF '## OPEN-07: Plugin self-test scope' .planning/OPEN-QUESTIONS.md && grep -qF '## Appendix A: Per-phase rollup index' .planning/OPEN-QUESTIONS.md && grep -qF '## Appendix B: Source traceability' .planning/OPEN-QUESTIONS.md && grep -qF '## Appendix C: Reconciliation algorithm result' .planning/OPEN-QUESTIONS.md && OPEN_NN_COUNT=$(grep -cE '^## OPEN-0[1-7]: ' .planning/OPEN-QUESTIONS.md) && [ "$OPEN_NN_COUNT" = "7" ] && echo SKELETON_OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/OPEN-QUESTIONS.md`: `test -f .planning/OPEN-QUESTIONS.md` exits 0
    - File has H1 title: `grep -qF '# Open questions register: dydx-delivery v2.0 → v2.x build sequence' .planning/OPEN-QUESTIONS.md` succeeds
    - All 12 H2 anchors present (12 individual `grep -qF` checks); all succeed
    - Exactly 7 OPEN-NN H2 anchors: `grep -cE '^## OPEN-0[1-7]: ' .planning/OPEN-QUESTIONS.md` returns `7`
    - Each H2 appears EXACTLY once: 12 individual `grep -cF` checks each return `1`
    - Preamble blockquote placeholder present: `grep -qF '> (Preamble placeholder' .planning/OPEN-QUESTIONS.md` succeeds
    - Top-matter cites all three prior deliverables (AUDIT.md / DESIGN.md / CHANGELIST.md): 3 individual `grep -qF` checks
    - Closing italics paragraph present: `grep -qF 'Open questions register produced 2026-05-10' .planning/OPEN-QUESTIONS.md` succeeds
  </acceptance_criteria>
  <done>
    OPEN-QUESTIONS.md skeleton exists at `.planning/OPEN-QUESTIONS.md` with 12 H2 anchors verbatim + preamble blockquote placeholder + populated-by notes for each section. Bodies are intentionally empty (placeholder notes only); subsequent plans append content under matching anchors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Wave 1 negative-invariant — structural-check exits NON-ZERO against empty skeleton</name>
  <files>.planning/phases/04-open-questions/04-01-SUMMARY.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (just created in Task 2)
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh (just created in Task 1)
    - .planning/phases/03-changelist/03-01-SUMMARY.md — Phase 3 Wave 1 negative-invariant verification precedent
    - $HOME/.claude/get-shit-done/templates/summary.md — SUMMARY.md template shape
  </read_first>
  <rationale>
    Wave 1 negative-invariant proves the script catches missing content. After cross-AI C1+C2+C4+C5+C6 fixes, Wave 1 first-fail will likely be A4 (register_total = 0 < 21) OR A10 (Appendix B has 0 rows) OR A11 (Appendix C reconciliation block missing) OR A13 (the narrowed regex still catches the skeleton's `Populated by 04-NN-PLAN.md` notes). All acceptable.
  </rationale>
  <action>
    Run `bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo "EXIT_CODE=$?"`.

    Expected outcome: structural-check exits NON-ZERO (FAIL message printed to stderr; exit code 1). Per the Wave 1 invariant the first failure should be on A4 (register_total = 0 < 21) OR A10 (Appendix B body has 0 rows; cardinality 0 != 0 evaluates depending on register_total) OR A11 (Appendix C reconciliation block missing) OR A13 (narrowed regex still catches `Populated by 04-NN-PLAN.md` skeleton stubs). Any of these is acceptable as the first-fail; the assertion logic short-circuits at the first miss.

    Note: A14 (citation-validity sample) passes vacuously when register has 0 citations.

    Document the expected-fail in the SUMMARY.md (per Phase 3 03-01 precedent + Phase 2 02-01 precedent). The Wave 1 invariant is: **scaffold WITHOUT content MUST fail the script — proves assertions are wired correctly and will catch missing content as Plans 04-02..04-04 progressively populate sections.**

    Then create file at path `.planning/phases/04-open-questions/04-01-SUMMARY.md` using the SUMMARY.md template shape (see Phase 3 03-01-SUMMARY.md for direct precedent). Required sections:

    1. **Plan ID + execution date + outcome** (`Plan 04-01 — Wave 1 scaffold (post-cross-AI-review revision) — executed 2026-05-10 — outcome: COMPLETE (Wave 1 negative-invariant verified)`)
    2. **Artefacts created** — list with absolute-path-from-repo-root form:
       - `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` — N lines, 14 assertions A1..A14 (cross-AI C1/C2/C4/C5/C6 fixes wired)
       - `.planning/OPEN-QUESTIONS.md` — N lines, H1 + preamble blockquote + 12 H2 anchors with populated-by notes
    3. **Cross-AI review fixes wired into script** — explicit list:
       - C1: A13 regex narrowed to `Populated by 04-0[1-9]|Preamble placeholder|\bplaceholder\b`
       - C2: A4 + A10 + A14 use `OPEN-Q[0-9]+(\.[0-9]+)?` (decimal-ID-aware)
       - C4: A4 floor lowered from 22 to `>= 21`
       - C5: A14 new — citation-validity sample pass (first 20 citations checked for file-exists + line-valid)
       - C6: A8 owner-enum tightened to `Phase [1-9]|TBD` only (Plan 04-02 will split hybrid rows)
    4. **Wave 1 invariant verified** — record verbatim the structural-check output (exit code NON-ZERO + first-fail message). Confirm: "Negative invariant met — script catches missing content; downstream plans 04-02..04-04 will progressively replace populated-by notes; Plan 04-05 synthesis runs final pass + reviewer-ready terminal-state."
    5. **Decisions implemented** — list D-46, D-47, D-48, D-49, D-53, D-54 + cross-cutting D-14 / D-27 / D-28 / D-37 carried.
    6. **Hand-off to next plan** — Wave 2 (Plan 04-02) populates OPEN-01 + OPEN-02 sections AND splits OPEN-Q06 + OPEN-Q07 each into 2 single-owner rows per cross-AI C6. Structural-check first-fail will move to a different assertion as content lands; final pass at 04-05 synthesis.

    **Implementing decisions:** D-44 carry-equivalent (no new D-N introduced); D-54 (Wave 1 mandatory artefact present); Wave 1 negative-invariant per Phase 2 02-01 + Phase 3 03-01 precedent.
  </action>
  <verify>
    <automated>EXIT_CODE=0; bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh > /tmp/wave1_check.out 2>&1 || EXIT_CODE=$?; if [ "$EXIT_CODE" = "0" ]; then echo "UNEXPECTED PASS — Wave 1 must fail per invariant"; exit 1; fi; echo "Wave 1 negative-invariant: structural-check EXITED $EXIT_CODE (NON-ZERO as expected)"; cat /tmp/wave1_check.out; test -f .planning/phases/04-open-questions/04-01-SUMMARY.md && grep -qF 'Wave 1 negative-invariant' .planning/phases/04-open-questions/04-01-SUMMARY.md && grep -qF 'C1' .planning/phases/04-open-questions/04-01-SUMMARY.md && grep -qF 'C5' .planning/phases/04-open-questions/04-01-SUMMARY.md && echo TASK_3_OK</automated>
  </verify>
  <acceptance_criteria>
    - structural-check script invocation exits NON-ZERO: `bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo $?` prints a number `> 0`
    - First failure message present in stderr (begins with `FAIL: `)
    - SUMMARY.md exists at `.planning/phases/04-open-questions/04-01-SUMMARY.md`: `test -f .planning/phases/04-open-questions/04-01-SUMMARY.md` exits 0
    - SUMMARY.md documents the Wave 1 negative-invariant verbatim: `grep -qF 'Wave 1 negative-invariant' .planning/phases/04-open-questions/04-01-SUMMARY.md` succeeds
    - SUMMARY.md lists cross-AI review fixes (C1/C2/C4/C5/C6): 5 individual `grep -qF` checks for `C1`, `C2`, `C4`, `C5`, `C6` succeed
    - SUMMARY.md lists both artefacts created (script + skeleton): 2 individual `grep -qF` checks for the absolute paths succeed
    - SUMMARY.md names downstream consumer (Plan 04-02 / Wave 2): `grep -qF '04-02' .planning/phases/04-open-questions/04-01-SUMMARY.md` succeeds
    - SUMMARY.md instructs Plan 04-02 to split hybrid OPEN-Q06 + OPEN-Q07 rows per C6: `grep -qF 'split' .planning/phases/04-open-questions/04-01-SUMMARY.md` succeeds (or equivalent SPLIT/Split casing) AND `grep -qF 'C6' .planning/phases/04-open-questions/04-01-SUMMARY.md` succeeds
  </acceptance_criteria>
  <done>
    Wave 1 negative-invariant verified: structural-check exits NON-ZERO against the empty skeleton (proves assertions catch missing content). SUMMARY.md committed with verbatim script output + cross-AI C1/C2/C4/C5/C6 fixes documented + decisions implemented + hand-off note instructing Plan 04-02 to split hybrid OPEN-Q06/Q07 rows. Phase 4 scaffold ready for Plans 04-02..04-05 to progressively populate sections under known H2 anchors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structural-check ↔ register content | The script grades downstream plans' content via grep/awk assertions. Closed enums (severity D-48 / resolution-path D-49 / status D-47 / owning-phase tightened to single-owner-only per cross-AI C6) prevent drift. Tampering: an executor could write content that satisfies the floor but violates the closed enum (e.g., severity = `WARNING`). Mitigation: A5/A6/A7/A8 disallow patterns reject any literal outside the enum. Cross-AI C6: A8 explicitly rejects hybrid `Phase 1/Phase 2`. |
| Skeleton populated-by notes ↔ no-placeholder synthesis-end (A13 narrowed) | The populated-by notes contain `Populated by 04-NN-PLAN.md` strings. A13 narrowed regex (per cross-AI C1) catches these — EXPECTED to fail in Waves 1..4 (mid-phase invariant); MUST pass at end of Plan 04-05 synthesis when notes are replaced. Bare `04-0[1-9]` plan-IDs in narrative bodies (e.g., Appendix C precedent citations) are NOT rejected. Mitigation: A13 narrowed; Wave 1 negative-invariant verification (Task 3) confirms it fires on the skeleton. |
| H2 anchor literal-string drift | Downstream plans must write content under EXACT H2 strings. Drift on capitalisation, spaces, en-dash vs hyphen breaks `section_between` extraction. Mitigation: A1 + A2 grep -qF (literal) + uniqueness loop catches drift before it propagates. |
| Citation-validity assumption ↔ A14 sample pass | Cross-AI C5: pattern-only validation (A9) is too weak for D-14/D-50. A14 samples first 20 citations for file-exists + line-valid. Risk: a citation that points to a non-existent line beyond the sampled subset slips through. Mitigation: full pass at synthesis (Plan 04-05 reconciliation algorithm) checks ALL citations before declaring `ALL_CITATIONS_VERIFIED = TRUE`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Structural-check script | mitigate | Script committed via Task 1; downstream plans cannot bypass — every wave invokes it |
| T-04-02 | Information disclosure | Skeleton populated-by notes | accept | Notes contain only plan-ID references and section purposes; no PII or secrets |
| T-04-03 | Tampering | OPEN-QUESTIONS.md skeleton | mitigate | A1/A2/A3 ensure 12 H2 anchors present + unique; downstream plans cannot remove anchors silently |
| T-04-04 | Tampering | Decimal-ID rows silently dropped (cross-AI C2) | mitigate | A4 + A10 + A14 use `OPEN-Q[0-9]+(\.[0-9]+)?` regex; OPEN-Q21.1 counted, not silently dropped |
| T-04-05 | Tampering | Hybrid-owner rows missed by phase consumers (cross-AI C6) | mitigate | A8 enum tightened to single-owner; Plan 04-02 splits OPEN-Q06/Q07 |
| T-04-06 | Tampering | Citation pointing to non-existent line (cross-AI C5) | mitigate | A14 sample pass at every wave + full pass at synthesis |
</threat_model>

<verification>
- All 3 tasks complete in order (script written → skeleton written → negative-invariant verified)
- Wave 1 SUMMARY.md committed
- Cross-AI review fixes C1/C2/C4/C5/C6 wired into script and documented in SUMMARY
</verification>

<success_criteria>
- `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` exists, executable, syntactically valid bash, contains 14 assertions A1..A14
- Cross-AI review fixes verified in script: C1 (A13 narrowed), C2 (decimal-ID regex), C4 (floor >= 21), C5 (A14 new), C6 (A8 single-owner-only)
- `.planning/OPEN-QUESTIONS.md` exists, contains H1 + preamble blockquote + 12 H2 anchors verbatim + closing italics paragraph
- Each H2 appears EXACTLY once
- Exactly 7 OPEN-NN H2 anchors
- Structural-check invocation exits NON-ZERO against empty skeleton (negative-invariant)
- SUMMARY.md committed at `.planning/phases/04-open-questions/04-01-SUMMARY.md` documenting the negative-invariant + cross-AI fixes + artefacts created + decisions implemented
</success_criteria>

<output>
After completion, create `.planning/phases/04-open-questions/04-01-SUMMARY.md` per template.
</output>
