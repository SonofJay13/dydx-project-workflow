---
plan_id: 03-05
phase: 03-changelist
plan: 05
type: execute
wave: 5
depends_on: [03-01, 03-02, 03-03, 03-04]
requirements: [CHANGE-03, CHANGE-05]
files_modified:
  - .planning/CHANGELIST.md
  - .planning/phases/03-changelist/03-05-SUMMARY.md
autonomous: true
tags: [changelist, change-03, change-05, appendix-b, appendix-d, design-only]

must_haves:
  truths:
    - "`## Appendix B: Cosmetic-fix list (CHANGE-03)` H2 section populated with content-verbatim lift of all 6 fix subsections from `.planning/AUDIT.md` §AUDIT-07 (subsections 7.1..7.6) per D-41 — per cross-AI C5 (codex MEDIUM): content-verbatim with two allowed transformations (H3→H4 heading demote, `7.N`→`B.N` prefix renumber); everything else (citations, severity tags, fix wording, FOUND-NN tags, sentinel sentences) is byte-identical."
    - "Each Appendix B fix bullet retains the per-bullet sentinel `**Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` per D-16/D-41 — verbatim from AUDIT-07."
    - "Appendix B opening sentence (D-41 verbatim) already pre-laid in 03-01 skeleton — confirm preserved unchanged."
    - "`## Appendix D: Migration cutover rules (CHANGE-05)` H2 section populated with numbered checklist of 5-7 rules per D-43, each one line, drawn from DESIGN.md §DESIGN-08 + CONTEXT D-43 list."
    - "Appendix D opening sentence (D-43 verbatim) already pre-laid in 03-01 skeleton — confirm preserved unchanged."
    - "Structural-check assertion #8 (Appendix B per-bullet sentinel count >= 6) PASSES after this plan."
  artifacts:
    - path: ".planning/CHANGELIST.md"
      provides: "Appendix B (verbatim AUDIT-07 lift — 6 fix subsections, each with per-bullet sentinel) + Appendix D (numbered 7-rule migration cutover checklist citing DESIGN-08 as authoritative contract)"
      contains: "Appendix B: 6 subsections (7.1 README truncation, 7.2 test-sheet wording, 7.3 pipeline-step count, 7.4 missing LICENSE, 7.5 owner-email, 7.6 homepage asymmetry); Appendix D: 7 numbered rules (frontmatter_version lenient mode / CR-driven opt-in / no auto-flip client_review / status lifecycle retention / canonical lifecycle / file renumbering CR-driven / status-lifecycle survey result)"
  key_links:
    - from: "Appendix B verbatim lift"
      to: ".planning/AUDIT.md §AUDIT-07 (7.1..7.6)"
      via: "D-41 verbatim-lift discipline — single source of truth"
      pattern: "Each Appendix B subsection mirrors an AUDIT-07 subsection 1:1"
    - from: "Appendix D numbered checklist"
      to: ".planning/DESIGN.md §DESIGN-08 (frontmatter migration co-existence) + §Live status-lifecycle survey"
      via: "D-43 citation-header opening + 7-rule checklist drawn from CONTEXT D-43 + DESIGN-08 contract"
      pattern: "Appendix D restates DESIGN-08; if rules conflict, DESIGN-08 wins (per opening sentence)"
---

<objective>
Wave 5: populate `## Appendix B: Cosmetic-fix list (CHANGE-03)` and `## Appendix D: Migration cutover rules (CHANGE-05)` in `.planning/CHANGELIST.md`. Implements two CHANGE-* requirements that share a common pattern — both lift / restate from a source-of-truth file with the opening-sentence sentinel already pre-laid by 03-01.

**Purpose:**
- **Appendix B (CHANGE-03)** — surface every cosmetic fix scheduled for v2.1 Foundations build (NOT this design milestone) so v2.1 implementer has a single hand-off list. **Content-verbatim lift** from AUDIT-07 (per cross-AI C5 — codex MEDIUM — reworded from earlier "verbatim" framing weakened by allowed renumbering) — content is byte-identical except for two allowed transformations: H3→H4 heading demote + `7.N`→`B.N` prefix renumber. Inherits the per-bullet sentinel discipline (D-16) without re-authoring; reviewer cannot lift one bullet out of milestone context.
- **Appendix D (CHANGE-05)** — restate the migration cutover rules in implementer-readable form so v2.1+ executors don't re-derive from DESIGN-08. The opening sentence makes clear DESIGN-08 wins on conflict; this appendix is a reading aid, not a divergent contract.

**Output:** Two appendix bodies populated. After this plan, structural-check assertion #8 (Appendix B per-bullet sentinel count >= 6) PASSES — driven by the 6 AUDIT-07 fix subsections (7.1..7.6), each carrying the verbatim per-bullet sentinel. Assertion #10 (Appendix E bullets) still fails — Plan 03-07 unblocks.

**Plan-bundling rationale (CHANGE-03 + CHANGE-05 in one plan):** Both appendices are short, both have pre-laid opening sentinels, both are verbatim/near-verbatim from a source-of-truth file. Bundling them keeps Wave 5 scope tight; CHANGE-04 (Appendix C — research-blocked phases) is more substantial (per-blocker analysis + recommended `/gsd-research-phase` invocation) and gets its own Wave 6 plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/phases/03-changelist/03-CONTEXT.md
@.planning/phases/03-changelist/03-01-PLAN.md

<interfaces>
<!-- Two appendix sections; each has its opening sentinel pre-laid by 03-01. This plan populates the bodies. -->

**Appendix B body shape (verbatim lift from AUDIT-07 per D-41):**

AUDIT-07 contains 6 subsections (7.1..7.6); each carries:
- A `### 7.N <title>` H3 header
- A `- **Citation:** \`file:line\` ... ` bullet
- A `- **Context:** ...` bullet
- A `- **Severity:** **[COSMETIC]** ... ` bullet (carrying [COSMETIC] severity tag per D-15 narrowed — preserved on AUDIT-07 lift)
- A `- **Fix:** ... **Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` bullet (the per-bullet sentinel per D-16)

The lift MUST preserve all 6 subsections in document order (7.1 → 7.6) with the H3 demoted to H4 (or kept H3 — the structural-check operates on prose substrings, not heading levels; H3 is acceptable to mirror AUDIT-07 exactly). Use H4 (`####`) so the section's H2 anchor remains visually dominant.

The 6 AUDIT-07 fix subsections (verbatim source — read AUDIT.md lines 489..540):
- **7.1 README truncation (plugin-level)** — Citation `dydx-delivery/README.md:126`. Severity [COSMETIC]. Fix: Complete the truncated sentence in the changelog entry. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**
- **7.2 Residual "test sheet" wording** — Citation `README.md:9`. Severity [COSMETIC]. Fix: Change "test sheet" to "test plan" at `README.md:9`. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**
- **7.3 Pipeline-step count mismatch (root README)** — Citation `README.md:9`, `.claude-plugin/marketplace.json:15`, `dydx-delivery/.claude-plugin/plugin.json:4`, `dydx-delivery/README.md:33-41`. Severity [COSMETIC]. Fix: Update `README.md:9`. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**
- **7.4 Missing LICENSE file** — Citation `dydx-delivery/.claude-plugin/plugin.json:10`. Severity [COSMETIC]. Fix: Add `LICENSE` file + `license` field to `marketplace.json`. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.**
- **7.5 Owner-email mismatch with stated org** — Citation `.claude-plugin/marketplace.json:5`, `dydx-delivery/.claude-plugin/plugin.json:7`. Severity [COSMETIC]. Fix: Change to org-domain address. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.** Cross-ref: MIN-6 PITFALL.
- **7.6 [NEW] Homepage asymmetry** — Citation `dydx-delivery/.claude-plugin/plugin.json:9`, `.claude-plugin/marketplace.json` (per `.planning/codebase/CONCERNS.md:197-199`). Severity [NEW] [COSMETIC]. Fix: Add matching `homepage` field to `marketplace.json`. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.**

**Appendix D body shape (numbered checklist per D-43):**

7 numbered rules drawn from CONTEXT D-43 (verbatim list provided in CONTEXT.md §"Migration cutover rules"):

1. v2 readers tolerate v0.3.0 frontmatter via the `frontmatter_version` field (absent → v0.3.0 lenient mode).
2. Migration is opt-in per change request — artefacts upgrade only when their owning CR fires.
3. In-flight `client_review` artefacts NEVER auto-flip to `approved`.
4. Status lifecycle MUST retain `client_review` (live in `dydx-delivery/skills/generate-sow/SKILL.md:93`).
5. The canonical lifecycle is `draft → client_review → approved → archived` (per DESIGN-01).
6. Artefact file renumbering (00→02 / 01→03 / 02→04 / 03→05 / 04→07a per FOUND-03) applies only to NEW artefacts written by v2 skills; existing files stay at v0.3.0 prefixes until their owning CR opts in.
7. The status-lifecycle survey result (DESIGN.md §"Live status-lifecycle survey") is the empirical confirmation that no live `status:` value gets orphaned by this contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Appendix B — verbatim lift of AUDIT-07's 6 fix subsections (preserving per-bullet sentinels)</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Appendix B H2 + opening sentinel from 03-01 + populated-by note)
    - .planning/AUDIT.md lines 485..540 — AUDIT-07 6 fix subsections (7.1..7.6) — VERBATIM SOURCE
    - .planning/phases/03-changelist/03-CONTEXT.md §"Cosmetic-fix list (CHANGE-03 → Appendix B)" D-41 (verbatim lift discipline); §"Cross-cutting authoring decisions" D-15 carried (severity tags preserved on AUDIT-07 lift), D-16 carried (per-bullet sentinel)
    - .planning/REQUIREMENTS.md §"v2.1 — Foundations (deferred)" — confirms FOUND-04 + FOUND-07 are the 2 owning REQ-IDs for the cosmetic fixes (FOUND-04 = manifest sync + LICENSE + email + homepage; FOUND-07 = README cleanup + test-sheet residual)
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate `## Appendix B: Cosmetic-fix list (CHANGE-03)` H2 section IN PLACE.

    **CRITICAL — opening sentinel preservation:** The H2 anchor and opening sentence (`Lifted verbatim from \`.planning/AUDIT.md\` § AUDIT-07 (per D-16 sentinel discipline). Editing here without also editing AUDIT-07 introduces drift; if a fix needs revision, edit AUDIT.md first and re-lift.`) are PRE-LAID by 03-01. DO NOT modify those lines. APPEND the populated content AFTER the opening sentence (REPLACING the populated-by note).

    **Body content (verbatim lift from AUDIT.md §AUDIT-07 — 6 fix subsections in document order):**

    Use H4 (`####`) headers (NOT H3) so the H2 anchor remains visually dominant. For each fix subsection, emit in this exact shape:

    ```
    #### B.N <title — verbatim from AUDIT-07>

    - **Citation:** `<file:line — verbatim from AUDIT-07>`
    - **Context:** <verbatim from AUDIT-07>
    - **Severity:** **[COSMETIC]** <or **[NEW]** **[COSMETIC]** for B.6 — verbatim from AUDIT-07>
    - **Fix:** <verbatim fix description>. **Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.** <plus optional cross-ref line if AUDIT-07 carries one>
    ```

    Renumber the AUDIT-07 7.N to B.N (B.1 through B.6) for the Appendix B context — but preserve all other content content-verbatim. **Acceptance criterion (per cross-AI C5 — codex MEDIUM, reword "verbatim" weakened by allowed renumbering):** Appendix B is **content-verbatim with two allowed transformations** — (a) heading-level demote (H3 sub-section headings → H4 to nest under Appendix B's H2 anchor), (b) prefix renumber (`7.N` → `B.N`). Everything else (citations, severity tags, fix wording, FOUND-NN tags, the per-bullet sentinel sentence, the [COSMETIC] / [NEW] tags) MUST be byte-identical to AUDIT.md lines 489..540. **Per-bullet sentinel byte-identity:** every Appendix B `**Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` sentinel MUST match the corresponding AUDIT-07 sentinel byte-for-byte (only the surrounding heading prefix differs — the sentinel sentence itself is identical bytes).

    Concrete content (lifted from AUDIT.md):

    **B.1 README truncation (plugin-level)**
    - **Citation:** `dydx-delivery/README.md:126` — Changelog entry for `0.3.0` reads: *"The bot-run terminal stage (`execute-tests`) now c"* and the file terminates mid-sentence. No closing punctuation, no further entries.
    - **Context:** Plugin README is the install-time README; truncation is visible to anyone reading the plugin's marketplace listing.
    - **Severity:** **[COSMETIC]** — client-visible (plugin README).
    - **Fix:** Complete the truncated sentence in the changelog entry. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**

    **B.2 Residual "test sheet" wording**
    - **Citation:** `README.md:9` — Root README still says "discovery → SOW → functional spec → technical spec → **test sheet** → execution".
    - **Context:** Plugin README changelog (`dydx-delivery/README.md:126`) records the 0.3.0 rename `generate-test-sheet` → `generate-test-plan`; root README never followed the rename.
    - **Severity:** **[COSMETIC]** — client-visible (root marketplace README).
    - **Fix:** Change "test sheet" to "test plan" at `README.md:9`. **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**

    **B.3 Pipeline-step count mismatch (root README)**
    - **Citation:** `README.md:9` says 5 pipeline steps; `.claude-plugin/marketplace.json:15` and `dydx-delivery/.claude-plugin/plugin.json:4` describe **seven skills**; `dydx-delivery/README.md:33-41` lists seven.
    - **Context:** Two-vs-three sources of truth disagree on pipeline length; new readers see contradictory information.
    - **Severity:** **[COSMETIC]** — client-visible (root README).
    - **Fix:** Update `README.md:9` to reflect the 7-skill v0.3.0 pipeline (or v2's 13-skill pipeline post-rebuild). **Scheduled for v2.1 Foundations build (FOUND-07), NOT this milestone.**

    **B.4 Missing LICENSE file**
    - **Citation:** `dydx-delivery/.claude-plugin/plugin.json:10` declares `"license": "Proprietary"`; no `LICENSE` or `LICENSE.md` file exists at repo root or plugin folder; `.claude-plugin/marketplace.json` has no `license` field.
    - **Context:** Provenance gap — the `Proprietary` declaration is unbacked by an actual licence text; clients viewing the marketplace see a license field that doesn't resolve to a document.
    - **Severity:** **[COSMETIC]** — client-visible (provenance).
    - **Fix:** Add `LICENSE` file at repo root (or `dydx-delivery/LICENSE`) with proprietary licence terms; add matching `license` field to `marketplace.json`. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.**

    **B.5 Owner-email mismatch with stated org**
    - **Citation:** `.claude-plugin/marketplace.json:5` (`owner.email`) and `dydx-delivery/.claude-plugin/plugin.json:7` (`author.email`) both list `jasonmichaelb@gmail.com`; README and marketplace metadata describe the team as "dYdX Digital" (e.g. `.claude-plugin/marketplace.json:4`, `dydx-delivery/.claude-plugin/plugin.json:4`); no `@dydx.digital` or org domain appears in the manifests.
    - **Context:** Provenance — clients viewing the marketplace see a personal Gmail; mismatches the stated organisational identity.
    - **Severity:** **[COSMETIC]** — client-visible (provenance).
    - **Fix:** Change to an org-domain address. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.** **Cross-ref:** MIN-6 PITFALL.

    **B.6 [NEW] Homepage asymmetry**
    - **Citation:** `dydx-delivery/.claude-plugin/plugin.json:9` has `"homepage": "https://github.com/SonofJay13/dydx-project-workflow"`; `.claude-plugin/marketplace.json` has no `homepage` field. (`.planning/codebase/CONCERNS.md:197-199`).
    - **Context:** Asymmetric metadata — the marketplace listing omits a link the plugin manifest provides.
    - **Severity:** **[NEW]** **[COSMETIC]** — client-visible (marketplace listing). [NEW] tag because CONCERNS.md flagged the asymmetry but didn't formally categorize it; AUDIT.md elevates it to cosmetic-client-visible.
    - **Fix:** Add matching `homepage` field to `marketplace.json`. **Scheduled for v2.1 Foundations build (FOUND-04), NOT this milestone.**

    **Implementing decisions:** D-41 (verbatim-lift discipline + opening sentinel preservation), D-15 carried (severity tags `[COSMETIC]` / `[NEW]` `[COSMETIC]` preserved from AUDIT-07 lift — these tags apply only to migrated AUDIT-07 items, not to CHANGE-N rows themselves), D-16 carried (per-bullet sentinel preserved on every fix), D-14 carried (`file:line` citation format preserved with backtick wrapping).

    **CRITICAL — DO NOT modify Phase 1..9 sections OR Appendix A OR Appendix C OR Appendix D OR Appendix E from this task.**
  </action>
  <verify>
    <automated>APPENDIX_B=$(awk -v hdr='## Appendix B: Cosmetic-fix list (CHANGE-03)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && SENTINELS=$(printf '%s\n' "$APPENDIX_B" | grep -cE 'Scheduled for v2\.1 Foundations build \(FOUND-[0-9]+\), NOT this milestone') && [ "$SENTINELS" -ge "6" ] && SUBSECTIONS=$(printf '%s\n' "$APPENDIX_B" | grep -cE '^#### B\.[1-6]') && [ "$SUBSECTIONS" = "6" ] && printf '%s\n' "$APPENDIX_B" | grep -qF 'Lifted verbatim from' && echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Appendix B opening sentinel preserved unchanged from 03-01: `grep -qF 'Lifted verbatim from \`.planning/AUDIT.md\` § AUDIT-07 (per D-16 sentinel discipline). Editing here without also editing AUDIT-07 introduces drift; if a fix needs revision, edit AUDIT.md first and re-lift.' .planning/CHANGELIST.md` succeeds
    - Appendix B body has exactly 6 H4 subsections labelled B.1..B.6:
      ```
      APPENDIX_B=$(awk -v hdr='## Appendix B: Cosmetic-fix list (CHANGE-03)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md)
      printf '%s\n' "$APPENDIX_B" | grep -cE '^#### B\.[1-6]'
      ```
      returns `6`
    - Per-bullet sentinel count >= 6 (one per fix subsection): `printf '%s\n' "$APPENDIX_B" | grep -cE 'Scheduled for v2\.1 Foundations build \(FOUND-[0-9]+\), NOT this milestone'` returns `6`
    - **Cross-AI C5 byte-identical sentinel assertion (codex MEDIUM):** every Appendix B per-bullet sentinel MUST match its AUDIT-07 counterpart byte-for-byte. Run:
      ```bash
      diff \
        <(awk -v hdr='## Appendix B: Cosmetic-fix list (CHANGE-03)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md | grep -oE 'Scheduled for v2\.1 Foundations build \(FOUND-[0-9]+\), NOT this milestone\.' | sort) \
        <(grep -oE 'Scheduled for v2\.1 Foundations build \(FOUND-[0-9]+\), NOT this milestone\.' .planning/AUDIT.md | sort)
      ```
      MUST produce empty output (DIFF_EXIT=0). Sentinel sentences are content-verbatim with NO transformation — only the H3→H4 demote + 7.N→B.N renumber are permitted by C5; the sentinel itself is byte-identical.
    - All 6 fix titles present (verbatim from AUDIT-07): `grep -qF 'README truncation (plugin-level)'` ✓; `grep -qF 'Residual "test sheet" wording'` ✓; `grep -qF 'Pipeline-step count mismatch (root README)'` ✓; `grep -qF 'Missing LICENSE file'` ✓; `grep -qF 'Owner-email mismatch with stated org'` ✓; `grep -qF '[NEW] Homepage asymmetry'` ✓
    - All severity tags preserved per D-15 carried: `grep -cE '\[COSMETIC\]'` returns >= 6; `grep -qF '[NEW] [COSMETIC]'` succeeds (B.6)
    - FOUND-NN tag distribution per AUDIT-07: `grep -c 'FOUND-04'` returns 3 (B.4, B.5, B.6); `grep -c 'FOUND-07'` returns 3 (B.1, B.2, B.3)
    - All 6 AUDIT-07 citations preserved verbatim (sample-check 3 citations): `grep -qF 'dydx-delivery/README.md:126'` ✓; `grep -qF '.claude-plugin/marketplace.json:5'` ✓; `grep -qF 'dydx-delivery/.claude-plugin/plugin.json:9'` ✓
    - **Structural-check assertion #8 NOW PASSES:** running `bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh` does NOT fail on Appendix B sentinel count (still fails on Appendix E bullets)
  </acceptance_criteria>
  <done>
    Appendix B populated with content-verbatim lift of all 6 AUDIT-07 fix subsections (B.1..B.6). Per cross-AI C5 (codex MEDIUM): the lift is **content-verbatim with two allowed transformations** — (a) H3→H4 heading demote, (b) `7.N`→`B.N` prefix renumber. Each carries the per-bullet sentinel `**Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` per D-16, byte-identical to the AUDIT-07 sentinel. Severity tags `[COSMETIC]` / `[NEW]` `[COSMETIC]` preserved per D-15 (narrowed). Opening sentence per D-41 preserved unchanged. Structural-check assertion #8 (Appendix B per-bullet sentinel count >= 6) PASSES.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate Appendix D — numbered 7-rule migration cutover checklist citing DESIGN-08</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Appendix D H2 + opening sentinel from 03-01 + populated-by note; Appendix B populated by Task 1)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Migration cutover rules (CHANGE-05 → Appendix D)" D-43 — lists the 7 rules verbatim
    - .planning/DESIGN.md §"DESIGN-08 — Frontmatter migration co-existence" (lines 181..193) — authoritative contract
    - .planning/DESIGN.md §"Live status-lifecycle survey" (lines 245..274) — empirical lock on rule 7
    - .planning/REQUIREMENTS.md §"v2.1 — Foundations (deferred)" FOUND-03 — file renumbering reference for rule 6
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate `## Appendix D: Migration cutover rules (CHANGE-05)` H2 section IN PLACE.

    **CRITICAL — opening sentinel preservation:** The H2 anchor and opening sentence (`Authoritative contract: \`.planning/DESIGN.md\` § DESIGN-08 (locked by D-25). This appendix restates the rules in implementer-readable form; if rules conflict, DESIGN-08 wins.`) are PRE-LAID by 03-01. DO NOT modify those lines. APPEND the populated content AFTER the opening sentence (REPLACING the populated-by note).

    **Body content (numbered 7-rule checklist drawn from CONTEXT D-43 verbatim — see `<interfaces>` block above for the exact 7 rules):**

    Use a numbered list (NOT a table — D-43 specifies "reader-friendly numbered checklist"). Each rule = one numbered item, one sentence each. End each rule with the locking citation in parens (`(per DESIGN-NN)` for design rules; `(per FOUND-NN)` for build rules).

    ```
    1. v2 readers tolerate v0.3.0 frontmatter via the `frontmatter_version` field (absent → v0.3.0 lenient mode) — per DESIGN-08.
    2. Migration is opt-in per change request — artefacts upgrade only when their owning CR fires (per DESIGN-08).
    3. In-flight `client_review` artefacts NEVER auto-flip to `approved` (per DESIGN-08).
    4. Status lifecycle MUST retain `client_review` (live in `dydx-delivery/skills/generate-sow/SKILL.md:93` per AUDIT.md §AUDIT-01.2 + DESIGN.md §"Live status-lifecycle survey").
    5. The canonical lifecycle is `draft → client_review → approved → archived` (per DESIGN-01).
    6. Artefact file renumbering (00→02 / 01→03 / 02→04 / 03→05 / 04→07a per FOUND-03) applies only to NEW artefacts written by v2 skills; existing files stay at v0.3.0 prefixes until their owning CR opts in (per FOUND-03 + DESIGN-08).
    7. The status-lifecycle survey result (`.planning/DESIGN.md` §"Live status-lifecycle survey") is the empirical confirmation that no live `status:` value gets orphaned by this contract — sampled v0.3.0 sources show only `{draft, client_review, approved}`; `archived` is additive (per D-25 / DESIGN-08).
    ```

    Add closing paragraph: `**Source-of-truth pointer.** This appendix is a reading aid. The authoritative migration contract lives at .planning/DESIGN.md §DESIGN-08 (locked by D-25 — status-lifecycle survey result). If a v2.1+ implementer encounters a conflict between this appendix and DESIGN-08, DESIGN-08 wins. If a CR's migration step is unclear, re-read DESIGN-08 + the survey section before writing migration code.`

    **Implementing decisions:** D-43 (numbered-checklist shape + 7-rule content + closing source-of-truth pointer + opening sentinel preservation), D-25 carried (status-lifecycle survey result locks contract), D-14 carried (`file:line` citation in rule 4).

    **CRITICAL — DO NOT modify Phase 1..9 sections OR Appendix A OR Appendix B OR Appendix C OR Appendix E from this task.**
  </action>
  <verify>
    <automated>APPENDIX_D=$(awk -v hdr='## Appendix D: Migration cutover rules (CHANGE-05)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && RULES=$(printf '%s\n' "$APPENDIX_D" | grep -cE '^[1-7]\. ') && [ "$RULES" -ge "7" ] && printf '%s\n' "$APPENDIX_D" | grep -qF 'Authoritative contract:' && printf '%s\n' "$APPENDIX_D" | grep -qF 'frontmatter_version' && printf '%s\n' "$APPENDIX_D" | grep -qF 'client_review' && printf '%s\n' "$APPENDIX_D" | grep -qF 'draft → client_review → approved → archived' && printf '%s\n' "$APPENDIX_D" | grep -qF 'DESIGN-08 wins' && bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh 2>&1 | tail -3; echo "STRUCTURAL_RAN"; echo TASK_2_OK</automated>
  </verify>
  <acceptance_criteria>
    - Appendix D opening sentinel preserved unchanged from 03-01: `grep -qF 'Authoritative contract: \`.planning/DESIGN.md\` § DESIGN-08 (locked by D-25). This appendix restates the rules in implementer-readable form; if rules conflict, DESIGN-08 wins.' .planning/CHANGELIST.md` succeeds
    - Appendix D body has exactly 7 numbered rules:
      ```
      APPENDIX_D=$(awk -v hdr='## Appendix D: Migration cutover rules (CHANGE-05)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md)
      printf '%s\n' "$APPENDIX_D" | grep -cE '^[1-7]\. '
      ```
      returns `7`
    - All 7 rules cite DESIGN / FOUND IDs: `printf '%s\n' "$APPENDIX_D" | grep -cE '\(per (DESIGN|FOUND)-' returns >= 7
    - Rule 1 mentions `frontmatter_version` AND `lenient mode`: 2 substring checks succeed
    - Rule 3 mentions `client_review` AND `NEVER auto-flip`: 2 substring checks succeed
    - Rule 4 cites `dydx-delivery/skills/generate-sow/SKILL.md:93` (per AUDIT-01.2)
    - Rule 5 mentions canonical lifecycle `draft → client_review → approved → archived`
    - Rule 6 mentions file renumbering `00→02 / 01→03 / 02→04 / 03→05 / 04→07a` AND `FOUND-03`
    - Rule 7 mentions `Live status-lifecycle survey` AND empirical confirmation
    - Closing source-of-truth pointer present: `printf '%s\n' "$APPENDIX_D" | grep -qF 'DESIGN-08 wins'` succeeds
    - **No accidental modifications** to Phase 1..9 / Appendix A / Appendix B (Task 1's lift) / Appendix C / Appendix E sections
  </acceptance_criteria>
  <done>
    Appendix D populated with numbered 7-rule migration cutover checklist per D-43. Each rule cites locking DESIGN-NN / FOUND-NN ID. Opening sentence per D-43 preserved unchanged. Closing source-of-truth pointer reinforces "DESIGN-08 wins on conflict." CHANGE-05 status: COMMITTED — migration cutover rules captured in implementer-readable form (ROADMAP Phase 3 success criterion 5 satisfied).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Appendix B verbatim lift ↔ AUDIT.md §AUDIT-07 | Drift class. The lift discipline (D-41) requires byte-identical content for all but the renumbering 7.N → B.N. Edits to Appendix B without parallel edits to AUDIT-07 introduce silent drift. Mitigation: opening sentence makes the rule explicit; reviewer attestation catches; structural-check sentinel-count assertion (#8) catches per-bullet sentinel count drift. |
| Appendix D rule list ↔ DESIGN.md §DESIGN-08 | Drift class — but bounded. D-43 specifies this appendix is a reading aid, NOT the contract. Opening sentence makes "DESIGN-08 wins on conflict" explicit. Mitigation: opening sentence + closing source-of-truth pointer; reviewer attestation. |
| Severity tags `[COSMETIC]` / `[NEW]` ↔ D-15 narrowed | Tampering class. D-15 (carried, narrowed) restricts severity tags to AUDIT-07 lifts in Appendix B — they MUST NOT appear on CHANGE-N rows themselves (Phase 1..9 mini-tables / Appendix A rows / Appendix D rules). Mitigation: this plan's edits are scoped to Appendix B + Appendix D; severity tags are only emitted in Appendix B (Task 1 verbatim lift); Task 2's Appendix D rules carry NO severity tags. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Appendix B opening sentinel modified — drift discipline lost | mitigate | Acceptance criteria explicitly assert the verbatim opening sentinel from 03-01 is still present; structural-check assertion #5 verifies the substring. |
| T-03-05-02 | Tampering | Appendix B drops the per-bullet sentinel on a fix (e.g. paraphrases "Scheduled for v2.1 Foundations build" without the comma + "NOT this milestone" closing) | mitigate | Acceptance criteria assert sentinel-count = 6 via `grep -cE 'Scheduled for v2\.1 Foundations build \(FOUND-[0-9]+\), NOT this milestone'`. The regex matches the exact verbatim form including the FOUND-NN tag and the comma + "NOT this milestone" closing. |
| T-03-05-03 | Tampering | Appendix B paraphrases an AUDIT-07 citation (`dydx-delivery/README.md:126` becomes `plugin README line 126`) | mitigate | Acceptance criteria spot-check 3 specific verbatim citation strings via `grep -qF`. Reviewer attestation catches systemic paraphrasing. |
| T-03-05-04 | Tampering | Severity tag `[COSMETIC]` added to a CHANGE-N row outside Appendix B (D-15 narrowed violation) | accept | This plan's edit scope is limited to Appendix B + Appendix D bodies; the action block forbids modifications outside those H2 sections. Cross-plan severity-tag pollution would require a different plan (or a typo in this one) — reviewer attestation catches. |
| T-03-05-05 | Tampering | Appendix D rule 5 mis-states the canonical lifecycle (e.g. drops `client_review` or `archived`) | mitigate | Acceptance criteria assert verbatim substring `draft → client_review → approved → archived` is present. |
| T-03-05-06 | Tampering | Appendix D rule 4 fails to cite `generate-sow/SKILL.md:93` — breaks the empirical-lock chain to AUDIT-01.2 | mitigate | Acceptance criteria assert the verbatim citation `dydx-delivery/skills/generate-sow/SKILL.md:93` is present. |
| T-03-05-07 | Tampering | Appendix D rule 6 omits the file-renumbering map (00→02 etc.) — implementer can't reconstruct from the rule alone | mitigate | Acceptance criteria assert the verbatim renumbering map substring `00→02 / 01→03 / 02→04 / 03→05 / 04→07a` is present. |
| T-03-05-08 | Tampering | Appendix D body adds a divergent rule not in DESIGN-08 (e.g. a date-based cutover rule contradicting the CR-driven opt-in contract) | accept (review-time catch) | Opening sentence makes "DESIGN-08 wins on conflict" explicit; reviewer attestation enforces. Acceptance criteria do not validate semantic alignment beyond the 7 specified rules. |
</threat_model>

<verification>
End of Plan 03-05 success-state:
1. Appendix B populated with 6 H4 subsections (B.1..B.6) verbatim from AUDIT-07 7.1..7.6.
2. Appendix B opening sentinel preserved from 03-01 (D-41).
3. Each Appendix B fix bullet carries the verbatim per-bullet sentinel `**Scheduled for v2.1 Foundations build (FOUND-NN), NOT this milestone.**` (D-16 carried).
4. Severity tags `[COSMETIC]` (5 instances) + `[NEW] [COSMETIC]` (1 instance for B.6) preserved per D-15 narrowed.
5. Appendix D populated with 7 numbered rules per D-43.
6. Appendix D opening sentinel preserved from 03-01 (D-43).
7. Appendix D closing source-of-truth pointer reinforces "DESIGN-08 wins on conflict."
8. Structural-check assertion #8 (Appendix B per-bullet sentinel count >= 6) NOW PASSES.
9. Structural-check assertion #10 (Appendix E bullets) STILL FAILS — Plan 03-07 unblocks.
10. Phase 1..9 mini-tables + Appendix A unchanged from prior plans.
11. Appendix C + Appendix E unchanged from 03-01 skeleton state.

Manual reviewer attestation:
- [ ] Appendix B is byte-identical to AUDIT-07 except for the H3→H4 demotion + 7.N→B.N renumbering.
- [ ] Each Appendix B citation uses `file:line` format with backtick wrapping per D-14 carried.
- [ ] Appendix D rules align with DESIGN-08 — no new rules invented; no rules dropped.
- [ ] Appendix D rule 7 cites the live status-lifecycle survey.
- [ ] No CHANGE-N row carries severity tags outside Appendix B (D-15 narrowed enforcement).
</verification>

<success_criteria>
- [ ] `## Appendix B: Cosmetic-fix list (CHANGE-03)` populated with 6 verbatim AUDIT-07 fix subsections
- [ ] Each Appendix B fix carries verbatim per-bullet sentinel per D-16
- [ ] Appendix B opening sentinel preserved from 03-01 (D-41)
- [ ] `## Appendix D: Migration cutover rules (CHANGE-05)` populated with 7 numbered rules per D-43
- [ ] Appendix D opening sentinel preserved from 03-01 (D-43)
- [ ] Appendix D closing source-of-truth pointer reinforces "DESIGN-08 wins"
- [ ] Structural-check assertion #8 (Appendix B sentinel count >= 6) PASSES
- [ ] No content under `dydx-delivery/` modified
- [ ] No modifications outside `.planning/CHANGELIST.md` Appendix B + Appendix D H2 sections + `.planning/phases/03-changelist/03-05-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/03-changelist/03-05-SUMMARY.md` documenting:
- Appendix B populated: 6 H4 subsections (B.1..B.6) verbatim from AUDIT-07 7.1..7.6
- Per-bullet sentinel preserved on every fix (FOUND-04: B.4/B.5/B.6; FOUND-07: B.1/B.2/B.3) — 6 total sentinels per D-16
- Severity tags preserved per D-15 narrowed: 5 `[COSMETIC]` + 1 `[NEW] [COSMETIC]`
- Appendix D populated: 7 numbered rules drawn from CONTEXT D-43 verbatim list, each citing locking DESIGN-NN / FOUND-NN
- Source-of-truth pointer reinforced in closing paragraph
- Structural-check status: assertion #8 (Appendix B sentinel count) PASSES; assertion #10 (Appendix E bullets) STILL FAILS — Plan 03-07 unblocks
- CHANGE-03 status: COMMITTED — cosmetic-fix list scheduled for v2.1 (ROADMAP Phase 3 success criterion 3 satisfied)
- CHANGE-05 status: COMMITTED — migration cutover rules captured (ROADMAP Phase 3 success criterion 5 satisfied)
- Pointer: "Plan 03-06 (Wave 6) populates Appendix C (CHANGE-04 — research-blocked phases) + adds Phase-1 + Phase-2 inline `[OPEN: Phase 4]` markers (Pipefy/Wrike/Ziflow connector + 2026 rate-limit currency + Ziflow read-after-create — verbatim from DESIGN.md Appendix E bullets 2, 3, 4, 5, 6, 7); Plan 03-07 (Wave 7 — synthesis) does Executive Summary + How-to-read + Appendix E mechanical walk + final structural-check pass."
</output>
</content>
</invoke>