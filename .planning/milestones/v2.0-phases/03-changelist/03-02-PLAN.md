---
plan_id: 03-02
phase: 03-changelist
plan: 02
type: execute
wave: 2
depends_on: [03-01]
requirements: [CHANGE-01]
files_modified:
  - .planning/CHANGELIST.md
  - .planning/phases/03-changelist/03-02-SUMMARY.md
autonomous: true
tags: [changelist, change-01a, phase-mini-tables, design-only]

must_haves:
  truths:
    - "Phases 1, 2, 3, 4, 5 H2 sections in `.planning/CHANGELIST.md` populated with 6-row Attribute/Detail mini-table per D-38 (Deliverables / Depends on / Addresses / Avoids pitfalls / Skills introduced/modified / Research-blocked)."
    - "Each Phase H2 section carries a brief ordering-rationale narrative paragraph (hard cap 4 sentences per CONTEXT Claude's-Discretion) sourced verbatim where possible from `.planning/research/SUMMARY.md` § Phase Ordering Rationale."
    - "Phase 1 mini-table Research-blocked cell carries an inline `⚠` marker linking to Appendix C (D-42 visibility) for the connector-probe blocker."
    - "Each Skills introduced/modified cell ties Appendix A rows to phases (D-38 — anchors the per-skill matrix to the milestone sequence)."
    - "Structural-check assertions for `| Deliverables |` / `| Addresses |` / `| Avoids pitfalls |` row counts each gain >= 5 (one per phase populated by this plan); cumulative count toward the >= 9 floor enforced by 03-01 script."
  artifacts:
    - path: ".planning/CHANGELIST.md"
      provides: "5 phase H2 sections (1, 2, 3, 4, 5) with ordering-rationale narrative + 6-row mini-table per D-38"
      contains: "Phase 1: Foundations + Connector Verification (v2.1) — connector probe + canonical references; Phase 2: Internalise Platform Skills (v2.1) — platform-pipefy/wrike/ziflow; Phase 3: Stage 1 + Stage 4 split (v2.2) — kickoff-capture + fnspec split; Phase 4: Tech spec + Cost + Implementation prompt (v2.3) — Stage 5/6/7a/7b; Phase 5: Test bot rebuild (v2.4) — Stages 8a-d + agent"
  key_links:
    - from: "Phase 1..5 mini-tables 'Skills introduced/modified' cells"
      to: ".planning/CHANGELIST.md § Appendix A: Per-skill delta matrix (CHANGE-02) — populated by Plan 03-04"
      via: "Each cell names skill rows that appear in Appendix A; Appendix A 'Introduced (phase)' column resolves the back-pointer per D-39"
      pattern: "skill-name cross-cite"
    - from: "Phase 1..5 mini-tables 'Addresses' cells"
      to: ".planning/REQUIREMENTS.md § Future Requirements (deferred — v2.1+) FOUND-/PLAT-/STG*-/SURF- families"
      via: "Each cell names REQ-IDs; CHANGELIST.md is the authoritative sequence per CHANGE-01"
      pattern: "REQ-ID cross-cite"
    - from: "Phase 1..5 mini-tables 'Avoids pitfalls' cells"
      to: ".planning/research/PITFALLS.md CRIT-/MOD-/MIN- IDs"
      via: "Each cell names pitfall IDs; PITFALLS.md is the source of truth per D-38"
      pattern: "PITFALL-ID cross-cite"
---

<objective>
Wave 2 sequential plan A (CHANGE-01 first half): populate Phases 1-5 H2 sections in `.planning/CHANGELIST.md` with the 6-row Attribute/Detail mini-table per D-38 plus brief ordering-rationale narrative paragraph per phase. Implements CHANGE-01 first half (5 of 9 phases — v2.1 + v2.2 + v2.3 + v2.4 milestones); Plan 03-03 covers Phases 6-9 second half (v2.5 + v2.6).

**Purpose:** Materialise the 9-phase build plan from `.planning/research/SUMMARY.md` as the authoritative v2.x milestone sequence. Each phase carries deliverables / dependencies / REQ-IDs addressed / pitfalls avoided / skills introduced / research-blocked status — all in mini-table form for reviewer scanability per D-38 matrix-then-prose pattern.

**Output:** 5 populated phase H2 sections in CHANGELIST.md. Each section: `### Phase N: <name> (v2.X)` H2 anchor (already laid by 03-01) → brief narrative paragraph (ordering rationale, hard cap 4 sentences) → 6-row mini-table (Attribute / Detail).

**Split rationale (CHANGE-01 spans 03-02 + 03-03):** 9 phase mini-tables × ~50 lines each = ~450 lines of structured content. Single plan exceeds 50% context budget. Split into v2.1+v2.2+v2.3+v2.4 (5 phases — this plan) and v2.5+v2.6 (4 phases — plan 03-03) preserves natural milestone-bundling boundary. Wave 2 (this plan) runs sequentially before Wave 3 (Plan 03-03) because both plans share `.planning/CHANGELIST.md` as their `files_modified` target — file-ownership on a single markdown file forces serialisation across waves regardless of disjoint H2 ownership (D-45 sanctions planner discretion on final shape; the 7-wave sequential structure is the carried Phase 2 02-01..02-10 precedent). The "parallel" framing in earlier drafts was a documentation error caught by cross-AI C8 (gemini LOW); the actual `wave` numbers and `depends_on` chain serialise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/03-changelist/03-CONTEXT.md
@.planning/phases/03-changelist/03-01-PLAN.md

<interfaces>
<!-- This plan writes 5 phase H2 sections. Each section has the SAME structure per D-38: -->
<!--   1. (H2 anchor already exists from 03-01 skeleton — DO NOT re-emit; APPEND under it) -->
<!--   2. Brief narrative paragraph: ordering rationale only (why this phase here, not earlier or later). -->
<!--      Hard cap 4 sentences (CONTEXT Claude's-Discretion). Source: SUMMARY.md § "Phase Ordering Rationale" -->
<!--      verbatim where it carries the rule for the phase, paraphrased where it doesn't. Cite inline: -->
<!--      `per .planning/research/SUMMARY.md § "Phase Ordering Rationale"`. -->
<!--   3. 6-row Attribute / Detail mini-table. EXACT column headers and row order: -->
<!--        | Attribute | Detail | -->
<!--        |---|---| -->
<!--        | Deliverables | <bulleted list of what this phase ships> | -->
<!--        | Depends on | <prior-phase IDs or `— (root phase)`> | -->
<!--        | Addresses | <REQ-IDs from FOUND-/PLAT-/STG*-/SURF- families, comma-separated> | -->
<!--        | Avoids pitfalls | <CRIT-/MOD-/MIN- IDs from PITFALLS.md, comma-separated> | -->
<!--        | Skills introduced/modified | <subset of the per-skill delta — ties Appendix A to the phase> | -->
<!--        | Research-blocked | `⚠ <one-line summary>` linking to Appendix C, OR `—` if not blocked | -->

Per-phase content sources (this plan):

**Phase 1: Foundations + Connector Verification (v2.1)**
- **Ordering rationale source:** SUMMARY.md § Phase Ordering Rationale: "Phase 1 before Phase 2 because platform skills also point at canonical references; landing Phase 2 first leaves dangling pointers." (verbatim).
- **Deliverables source:** SUMMARY.md § Phase 1 — `dydx-delivery/references/{safety-rules.md, stage-numbering.md, frontmatter-scheme.md, glossary.md}`; existing 7 skills updated to point at canonical references; file renumbering 00→02 / 01→03 / 02→04 / 03→05 / 04→07a; empty `commands/`, `agents/`, `hooks/` scaffolded; plugin manifest 0.3.0 → 2.0.0; marketplace metadata locked to 2.0.0; connector probe verifying Coda MCP, Google Workspace MCP, Miro path, Pipefy/Wrike/Ziflow auth + native-AI ingestion paths; owner-email mismatch corrected.
- **Depends on:** `— (root phase)`.
- **Addresses (REQ-IDs):** FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, FOUND-07 (all v2.1 Foundations REQ-IDs per REQUIREMENTS.md § "v2.1 — Foundations (deferred)").
- **Avoids pitfalls (PITFALLS IDs):** CRIT-6 (frontmatter migration), MIN-5 (stage-numbering orphans), MOD-16 (hard-rules duplicate-and-edit), MIN-6 (email mismatch), per SUMMARY.md § Phase 1 verbatim.
- **Skills introduced/modified:** existing 7 skills MODIFIED to point at canonical references (per AUDIT.md §AUDIT-05.1 hard-rules collapse + §AUDIT-05.2 start-at-any-point block + §AUDIT-05.4 pipeline diagram); no NEW skills in this phase.
- **Research-blocked:** `⚠ Connector availability per tenant unverified (Coda MCP version pin, Google Workspace MCP choice, Miro MCP existence, Wrike rate-limit currency, Ziflow auth header) — see Appendix C` per CONTEXT D-42 + CHANGE-04.

**Phase 2: Internalise Platform Skills (v2.1)**
- **Ordering rationale source:** SUMMARY.md § Phase Ordering Rationale: "Phase 2 before Phases 3, 4, 5, 7 because all four phases load platform skills." (verbatim).
- **Deliverables source:** SUMMARY.md § Phase 2 — `skills/platform-pipefy/`, `skills/platform-wrike/`, `skills/platform-ziflow/`, each with `SKILL.md` + `references/{api-contract.md, native-ai-inventory.md, knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md}`. `tier_claims_last_verified:` frontmatter on each per MOD-7. Per-platform `native_ai_path: api | paste | none` decision frontmatter.
- **Depends on:** Phase 1 (canonical references must exist before platform skills can point at them).
- **Addresses (REQ-IDs):** PLAT-01, PLAT-02, PLAT-03 (per REQUIREMENTS.md § "v2.2+ — Skill builds (deferred per CHANGE-01 phase plan)" — milestone phase number is v2.1 per D-37, REQ-ID family stays PLAT- because the IDs were stamped before the milestone-bundling decision).
- **Avoids pitfalls (PITFALLS IDs):** MOD-4 (Pipefy GraphQL pagination silently truncates), MOD-5 (Wrike OAuth token-host trap), MOD-6 (Ziflow eventual consistency on proof creation), MOD-7 (Platform-tier capability claims age fast) per SUMMARY.md § Phase 2.
- **Skills introduced/modified:** 3 NEW platform skills — `platform-pipefy/`, `platform-wrike/`, `platform-ziflow/` (per Appendix A rows 17-19 per DESIGN-12 matrix).
- **Research-blocked:** `⚠ Pipefy GraphQL pagination cursor field names + Pipefy/Wrike 2026 rate-limit currency + Ziflow read-after-create consistency window unverified — see Appendix C`.

**Phase 3: Stage 1 + Stage 4 split (v2.2)**
- **Ordering rationale source:** SUMMARY.md § Phase Ordering Rationale: "Phase 3 before Phase 4 because tech spec reads fnspec-integration; cost estimate reads both fnspecs." (verbatim, partial). Add: "These are the two largest shape-changes to the existing pipeline; deferring means later phases inherit the broken single-fnspec contract." (paraphrased from SUMMARY.md § Phase 3 rationale opening).
- **Deliverables source:** SUMMARY.md § Phase 3 — `kickoff-capture` (Stage 1, NEW), `discovery-intake` MODIFIED (Stage 2), `generate-sow` MODIFIED (Stage 3), `generate-fnspec-platform` (Stage 4a, NEW), `generate-fnspec-integration` (Stage 4b, NEW), `generate-functional-spec` retired. Cross-spec consistency check between 4a and 4b. `delivery: native-ai|api` tag carries through all downstream `based_on_*` chains.
- **Depends on:** Phase 1 (canonical references), Phase 2 (platform skills loaded by Stage 4a).
- **Addresses (REQ-IDs):** STG1-01, STG1-02, STG3-01, STG4-01, STG4-02 (per REQUIREMENTS.md).
- **Avoids pitfalls (PITFALLS IDs):** MOD-8 (Field Notes pile-up — kickoff filters by `processed_at IS NULL`); plus the Phase 3 anti-features "single fnspec for everything" + "AP-6 splitting fnspec along feature lines instead of buildable surfaces" per SUMMARY.md § Phase 3.
- **Skills introduced/modified:** 1 NEW (`kickoff-capture`, Stage 1) + 1 MODIFIED (`discovery-intake`, Stage 2) + 1 UNCHANGED-structure / behaviour-modified (`generate-sow`, Stage 3) + 2 NEW (`generate-fnspec-platform` Stage 4a, `generate-fnspec-integration` Stage 4b) + 1 RETIRED (`generate-functional-spec`).
- **Research-blocked:** `—` (no Phase-3-specific research blocker; Phase 2 platform-skill blockers cover the platform side; SUMMARY.md flags Miro structured ingest as DEEPER research warranted before Stage 1 build).

**Phase 4: Tech spec + Cost + Implementation prompt (v2.3)**
- **Ordering rationale source:** SUMMARY.md § Phase 4 — "Tech spec scope-gates against fnspec-integration existence; cost estimate adds Coda integration (depends on Phase 1 Coda verification); implementation prompt is a sibling of the existing build prompt. Cannot run before Phase 3 because cost estimate reads fnspecs." (verbatim).
- **Deliverables source:** SUMMARY.md § Phase 4 — `generate-technical-spec` MODIFIED (Stage 5), `generate-cost-estimate` (Stage 6, NEW; reads Coda task-table schema + caches in 00_HUB.md, writes per-assignee rows via `rows/upsert` with `keyColumns`, polls `mutationStatus`); `generate-build-prompt` MODIFIED (Stage 7a); `generate-implementation-prompt` (Stage 7b, NEW; per-platform shape). Risk-multiplier taxonomy structure locked (numerics deferred per D-22).
- **Depends on:** Phase 1 (Coda MCP verified), Phase 2 (platform skills loaded for per-platform Stage 7b shapes), Phase 3 (fnspec-platform + fnspec-integration to read).
- **Addresses (REQ-IDs):** STG5-01, STG6-01, STG6-02, STG7-01, STG7-02 (per REQUIREMENTS.md).
- **Avoids pitfalls (PITFALLS IDs):** CRIT-1 (formula column overwrite — schema-introspect first), CRIT-2 (async-202 — `mutate_and_wait`), CRIT-3 (rate-limit — batch upsert + 4/10s buffer + idempotent retry), CRIT-9 (token scope — per-client tokens + 00_HUB.md doc IDs), MOD-10 (multiplier indefensible — closed taxonomy + rationale), per SUMMARY.md § Phase 4.
- **Skills introduced/modified:** 1 MODIFIED (`generate-technical-spec`, Stage 5) + 1 NEW (`generate-cost-estimate`, Stage 6) + 1 MODIFIED (`generate-build-prompt`, Stage 7a) + 1 NEW (`generate-implementation-prompt`, Stage 7b).
- **Research-blocked:** `—` (Coda API HIGH-confidence per RESEARCH.md; per-platform implementation-prompt shapes documented in FEATURES.md). Note inline: risk-multiplier numeric defaults DEFERRED to Phase 4 OPEN-QUESTIONS per D-22 — carries inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker (lifts from DESIGN.md baseline list per Appendix E).

**Phase 5: Test bot rebuild (v2.4)**
- **Ordering rationale source:** SUMMARY.md § Phase 5 — "Test bot needs the new fnspec-platform + fnspec-integration to derive cases properly; building before Phase 3 means rebuilding to handle the split. Self-contained otherwise; depends only on `<Client> Brain/test-bot/` shape and existing `execute-tests` safety contract." (verbatim).
- **Deliverables source:** SUMMARY.md § Phase 5 — `provision-test-harness` (Stage 8a, NEW), `generate-test-plan` UNCHANGED body / MODIFIED path (Stage 8b), `generate-uat-plan` (Stage 8c, NEW), `execute-tests` UNCHANGED user-facing / MODIFIED-internally (Stage 8d) invokes test-bot agent; `agents/test-bot-orchestrator/`. Per-client `client_state.yaml` schema. Sandbox-allowlist extended to Coda. `harness_drift` failure class added. Test-case lifecycle states (`active | obsolete | quarantined`).
- **Depends on:** Phase 3 (fnspec-platform + fnspec-integration to derive cases).
- **Addresses (REQ-IDs):** STG8-01, STG8-02, STG8-03, STG8-04 (per REQUIREMENTS.md).
- **Avoids pitfalls (PITFALLS IDs):** CRIT-5 (sandbox enforcement gap on Coda — extend allowlist), CRIT-7 (harness drift — `client_state.yaml` + drift detection), MOD-11 (stale tests linger — lifecycle states), MOD-12 (Python/AI boundary creep — hard contract on layer separation), MOD-13 (concurrency conflict — `sandbox_lock.yaml`), MOD-14 (sandbox cleanup-via-no-deletes — fixture run-ID prefix), per SUMMARY.md § Phase 5. Also avoids Stage 8 anti-feature "recreating test-bot on each ship" (AP-3).
- **Skills introduced/modified:** 1 NEW (`provision-test-harness`, Stage 8a) + 1 MODIFIED (`generate-test-plan`, Stage 8b — body unchanged, path moves to `<Client> Brain/test-bot/test_cases/`) + 1 NEW (`generate-uat-plan`, Stage 8c) + 1 MODIFIED (`execute-tests`, Stage 8d — internally invokes agent). Plus 1 NEW agent `test-bot-orchestrator/`.
- **Research-blocked:** `—` (self-contained per SUMMARY.md § Phase 5; depends on `<Client> Brain/test-bot/` shape + existing `execute-tests` safety contract — both validated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Phase 1 + Phase 2 + Phase 3 H2 sections (v2.1 + v2.2 milestone phases)</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — 03-01 skeleton with H2 anchors, populated-by notes only)
    - .planning/phases/03-changelist/03-CONTEXT.md §"9-phase plan presentation depth" D-38 (mini-table column headers + 6 mandatory rows + ordering-rationale source); §"Research-blocked flag visibility" D-42 (inline `⚠` marker for Phase 1)
    - .planning/research/SUMMARY.md §"Phase 1 — Foundations & Connector Verification" + §"Phase 2 — Internalise Platform Skills" + §"Phase 3 — Stage 1 Kickoff + Stage 4 fnspec Split" + §"Phase Ordering Rationale" (verbatim source for ordering paragraphs)
    - .planning/research/PITFALLS.md — read CRIT-1..10 + MOD-1..16 + MIN-1..6 IDs to confirm citations
    - .planning/REQUIREMENTS.md §"Future Requirements (deferred — v2.1+)" — FOUND-01..07, PLAT-01..03, STG1-01..02, STG3-01, STG4-01..02 IDs
    - .planning/DESIGN.md §"v2 skill inventory" matrix (lines 335..370) — confirms NEW/MODIFIED/UNCHANGED/RETIRED tagging used in Skills cells
    - .planning/AUDIT.md §AUDIT-05 (duplicates inventory grounding Phase 1 collapse rationale) + §AUDIT-04.1 (missing platform skills justifying Phase 2 internalisation)
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate three phase H2 sections IN PLACE under their existing H2 anchors (DO NOT re-emit the H2 — append after the populated-by note line, REPLACING that note line with the actual content).

    **For each of Phase 1, Phase 2, Phase 3 — emit in this exact shape:**

    1. **Ordering-rationale paragraph** (single blockquote `>`-prefixed line OR plain paragraph — pick blockquote for visual distinction from prose); hard cap 4 sentences. Cite inline: `per .planning/research/SUMMARY.md § "Phase Ordering Rationale"` for verbatim citations; `per .planning/research/SUMMARY.md § "Phase N — <name>"` for paraphrased citations.

    2. **6-row mini-table per D-38** — exact column headers and EXACT row labels (literal strings — the structural-check assertions match these verbatim):

       ```
       | Attribute | Detail |
       |---|---|
       | Deliverables | <list> |
       | Depends on | <list or `— (root phase)`> |
       | Addresses | <REQ-IDs comma-separated> |
       | Avoids pitfalls | <PITFALL-IDs comma-separated> |
       | Skills introduced/modified | <skill names with status tags in parens> |
       | Research-blocked | <`⚠ <summary>` linking to Appendix C, OR `—`> |
       ```

       Cell content sourcing (per-phase, exact wording where verbatim is mandated):

       **Phase 1: Foundations + Connector Verification (v2.1)**
       - Ordering paragraph (verbatim from SUMMARY.md): `> **Why this phase here.** Every later phase depends on canonical references existing and being authoritative; renumbering files before adding new skills is cheaper than after. Connector availability gates which Stage 5/9/11 designs ship vs degrade. *(per .planning/research/SUMMARY.md § "Phase 1 — Foundations & Connector Verification" + § "Phase Ordering Rationale": "Phase 1 before Phase 2 because platform skills also point at canonical references; landing Phase 2 first leaves dangling pointers.")*`
       - Deliverables: bulleted list of (a) `dydx-delivery/references/{safety-rules.md, stage-numbering.md, frontmatter-scheme.md, glossary.md}`; (b) existing 7 skills updated to point at canonical references (collapses 4 hard-rules duplicates per AUDIT-05.1; fixes sandbox-block bug; normalises `based_on_*` field names); (c) file renumbering 00→02 / 01→03 / 02→04 / 03→05 / 04→07a per FOUND-03; (d) plugin manifest `2.0.0` synced across `plugin.json` + `marketplace.json`; (e) owner-email mismatch corrected; (f) `LICENSE` file added; (g) empty `commands/`, `agents/`, `hooks/` scaffolded; (h) connector-probe + graceful-degradation matrix codified; (i) all cosmetic CONCERNS items cleaned per Appendix B.
       - Depends on: `— (root phase)`
       - Addresses: `FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, FOUND-07`
       - Avoids pitfalls: `CRIT-6 (frontmatter migration corrupts in-flight builds), MIN-5 (stage-numbering orphans), MOD-16 (hard-rules duplicate-and-edit), MIN-6 (email mismatch with stated org)`
       - Skills introduced/modified: `existing 7 v0.3.0 skills MODIFIED to point at canonical references (no NEW skills this phase — see Appendix A rows tagged MODIFIED with Introduced (phase) = Phase 1)`
       - Research-blocked: `⚠ Connector availability per tenant unverified (Coda MCP version pin, Google Workspace MCP choice, Miro MCP existence, Wrike rate-limit currency, Ziflow auth header) — see Appendix C`

       **Phase 2: Internalise Platform Skills (v2.1)**
       - Ordering paragraph (verbatim from SUMMARY.md): `> **Why this phase here.** v0.3.0 references \`platform-pipefy/wrike/ziflow\` but they don't exist in the repo today (per AUDIT.md §AUDIT-04.1). Every later phase that loads a platform skill (Stage 4 fnspec split, Stage 5 tech spec, Stage 7b implementation prompt, Stage 8 test bot, Stage 10 native-AI push) inherits the broken contract until these land. *(per .planning/research/SUMMARY.md § "Phase 2 — Internalise Platform Skills" + § "Phase Ordering Rationale": "Phase 2 before Phases 3, 4, 5, 7 because all four phases load platform skills.")*`
       - Deliverables: bulleted list of (a) `skills/platform-pipefy/` + `references/{api-contract.md, native-ai-inventory.md, knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md}`; (b) `skills/platform-wrike/` (same shape; persists `host` from OAuth token); (c) `skills/platform-ziflow/` (same shape; `wait_for_proof` helper); (d) `tier_claims_last_verified:` frontmatter on each (MOD-7); (e) per-platform `native_ai_path: api | paste | none` decision frontmatter.
       - Depends on: `Phase 1 (canonical references must exist before platform skills point at them — per ordering rule)`
       - Addresses: `PLAT-01, PLAT-02, PLAT-03`
       - Avoids pitfalls: `MOD-4 (Pipefy GraphQL pagination silently truncates), MOD-5 (Wrike OAuth token-host trap), MOD-6 (Ziflow eventual consistency on proof creation), MOD-7 (Platform-tier capability claims age fast)`
       - Skills introduced/modified: `3 NEW platform skills — \`platform-pipefy/\`, \`platform-wrike/\`, \`platform-ziflow/\` (Appendix A rows 17-19 per DESIGN-12 matrix). No stage-skill changes this phase.`
       - Research-blocked: `⚠ Pipefy GraphQL pagination cursor field names + Pipefy/Wrike 2026 rate-limit currency + Ziflow read-after-create consistency window unverified — see Appendix C`

       **Phase 3: Stage 1 + Stage 4 split (v2.2)**
       - Ordering paragraph: `> **Why this phase here.** Two largest shape-changes to the existing pipeline (Stage 1 kickoff + Stage 4 fnspec split). Deferring means later phases inherit the broken single-fnspec contract. Stage 4 split is the highest-leverage v2 feature — the \`delivery: native-ai|api\` tag is the routing key for Stages 5/6/7b/10. *(per .planning/research/SUMMARY.md § "Phase 3 — Stage 1 Kickoff + Stage 4 fnspec Split" + § "Phase Ordering Rationale": "Phase 3 before Phase 4 because tech spec reads fnspec-integration; cost estimate reads both fnspecs.")*`
       - Deliverables: bulleted list of (a) `kickoff-capture/` skill (Stage 1, NEW; reads Miro/notes/feedback, branches into discovery or draft SOW); (b) `discovery-intake/` MODIFIED to consume `01_kickoff_v*` (Stage 2); (c) `generate-sow/` MODIFIED (Stage 3, status lifecycle locked); (d) `generate-fnspec-platform/` (Stage 4a, NEW); (e) `generate-fnspec-integration/` (Stage 4b, NEW); (f) `generate-functional-spec/` RETIRED; (g) cross-spec consistency check between 4a and 4b; (h) `delivery: native-ai|api` tag carries through all downstream `based_on_*` chains.
       - Depends on: `Phase 1 (canonical references), Phase 2 (platform skills loaded by Stage 4a per-platform capability matrix)`
       - Addresses: `STG1-01, STG1-02, STG3-01, STG4-01, STG4-02`
       - Avoids pitfalls: `MOD-8 (Field Notes pile-up — kickoff filters by \`processed_at IS NULL\`)` plus anti-features avoided: single-fnspec-for-everything (legacy v0.3.0 shape — explicitly retired per REQUIREMENTS.md Out of Scope row 12); AI auto-classifying delivery tags without human review.
       - Skills introduced/modified: `1 NEW (\`kickoff-capture\`, Stage 1) + 1 MODIFIED (\`discovery-intake\`, Stage 2) + 1 UNCHANGED-structure/behaviour-modified (\`generate-sow\`, Stage 3) + 2 NEW (\`generate-fnspec-platform\` Stage 4a, \`generate-fnspec-integration\` Stage 4b) + 1 RETIRED (\`generate-functional-spec\`). Appendix A rows 1, 2, 3, 4, 5 + retired-row.`
       - Research-blocked: `—`

    **Implementing decisions:** D-36 (writing under exact H2 anchors), D-37 (milestone tag inline already on H2 anchor from 03-01 — do not duplicate), D-38 (mini-table column headers + 6 mandatory rows verbatim), D-42 (Phase 1 inline `⚠` Research-blocked cell), D-44 (D-N numbering — only re-cite, no new D-IDs introduced this plan), D-14 carried (`file:line` citation format on any v0.3.0 source citations), D-15 carried (no severity tags on CHANGE-N rows themselves).

    **CRITICAL — DO NOT modify Phase 4, Phase 5 sections in this task** (those live in Task 2 below). DO NOT touch Phase 6..9, Appendices A..E (those belong to other plans).
  </action>
  <verify>
    <automated>P1=$(awk -v hdr='## Phase 1: Foundations + Connector Verification (v2.1)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && [ "$(printf '%s\n' "$P1" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|')" = "6" ] && P2=$(awk -v hdr='## Phase 2: Internalise Platform Skills (v2.1)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && [ "$(printf '%s\n' "$P2" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|')" = "6" ] && P3=$(awk -v hdr='## Phase 3: Stage 1 + Stage 4 split (v2.2)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && [ "$(printf '%s\n' "$P3" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|')" = "6" ] && echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Phase 1 H2 section body has all 6 mini-table rows (extracted via stateful section_between):
      ```
      P1=$(awk -v hdr='## Phase 1: Foundations + Connector Verification (v2.1)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md)
      printf '%s\n' "$P1" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|'
      ```
      returns `6`
    - Phase 1 Research-blocked cell carries `⚠` and references `Appendix C` (D-42): `printf '%s\n' "$P1" | grep -qF '⚠'` succeeds AND `printf '%s\n' "$P1" | grep -qF 'see Appendix C'` succeeds
    - Phase 1 Addresses cell contains FOUND-01..07 (sample-check 3 IDs): `printf '%s\n' "$P1" | grep -qF 'FOUND-01'` AND `grep -qF 'FOUND-04'` AND `grep -qF 'FOUND-07'` all succeed
    - Phase 1 Avoids pitfalls cell contains CRIT-6 + MIN-5 + MOD-16 + MIN-6: 4 individual `grep -qF` checks succeed
    - Phase 1 ordering-rationale paragraph cites SUMMARY.md: `printf '%s\n' "$P1" | grep -qF 'per .planning/research/SUMMARY.md'` succeeds
    - Phase 1 ordering paragraph length <= 4 sentences (CONTEXT Claude's-Discretion cap) — verifiable by counting `.` characters in non-citation prose: `printf '%s\n' "$P1" | head -10 | grep -E '^>' | grep -oE '\. ' | wc -l` returns <= 5 (allows 4 sentences + the `*per ...*` citation)
    - Phase 2 H2 section body has all 6 mini-table rows (analogous extraction); Addresses cell contains PLAT-01, PLAT-02, PLAT-03; Avoids pitfalls cell contains MOD-4, MOD-5, MOD-6, MOD-7
    - Phase 2 Skills cell mentions all 3 platform skill names (`platform-pipefy`, `platform-wrike`, `platform-ziflow`)
    - Phase 2 ordering paragraph cites SUMMARY.md AND mentions "Phase 2 before Phases 3, 4, 5, 7" rationale (verbatim from § Phase Ordering Rationale)
    - Phase 3 H2 section body has all 6 mini-table rows; Addresses cell contains STG1-01, STG1-02, STG3-01, STG4-01, STG4-02; Skills cell mentions `kickoff-capture` + `generate-fnspec-platform` + `generate-fnspec-integration` + retirement of `generate-functional-spec`
    - Phase 3 Depends on cell mentions both Phase 1 AND Phase 2 (the rationale's two predecessors)
    - Total `| Deliverables |` count in CHANGELIST.md is at least 3 (Phase 1, 2, 3 added by this task; full Wave 1 / Wave 2 hits >= 9 floor)
    - Total `| Addresses |` count >= 3 ; total `| Avoids pitfalls |` count >= 3
    - **Structural-check still allows Phase 4, 5 H2 sections to remain placeholder** — no overwrite of unfinished sections
  </acceptance_criteria>
  <done>
    Phases 1, 2, 3 mini-tables populated under their existing H2 anchors. Each carries the 6-row Attribute/Detail mini-table per D-38 plus an ordering-rationale paragraph (hard cap 4 sentences) citing SUMMARY.md. Phase 1 Research-blocked cell carries inline `⚠` marker linking to Appendix C per D-42. CHANGELIST.md gains 3 of the 9 phase mini-tables required by structural-check assertion #11.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate Phase 4 + Phase 5 H2 sections (v2.3 + v2.4 milestone phases)</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Task 1 populated Phase 1, 2, 3; Phase 4, 5 still placeholder)
    - .planning/phases/03-changelist/03-CONTEXT.md §"9-phase plan presentation depth" D-38; §"Open-questions handling" D-27 (carried — risk-multiplier inline marker for Phase 4); §"Cross-cutting authoring" D-22 carried (numerics deferred from DESIGN-22)
    - .planning/research/SUMMARY.md §"Phase 4 — Tech Spec Scope Gate, Stage 6 Cost Estimate, Stage 7b Implementation Prompt" + §"Phase 5 — Test Bot Rebuild" + §"Phase Ordering Rationale" (verbatim source)
    - .planning/research/PITFALLS.md §CRIT-1, 2, 3, 5, 7, 9; §MOD-10, 11, 12, 13, 14
    - .planning/REQUIREMENTS.md §"Future Requirements (deferred — v2.1+)" — STG5-01, STG6-01..02, STG7-01..02, STG8-01..04 IDs
    - .planning/DESIGN.md §"Stage 6: Cost estimate" (DESIGN-22 — risk-multiplier structure; numerics deferred per D-22 inline marker baseline)
    - .planning/DESIGN.md §"Stage 8: Test bot — overview" (DESIGN-24)
    - .planning/DESIGN.md §"Test bot architecture" (DESIGN-28, 29, 30)
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate Phase 4 + Phase 5 H2 sections IN PLACE under their existing H2 anchors (REPLACE the populated-by note line with the mini-table content per the same shape used in Task 1).

    **Phase 4: Tech spec + Cost + Implementation prompt (v2.3)**
    - Ordering paragraph (verbatim from SUMMARY.md § Phase 4 + § Phase Ordering Rationale): `> **Why this phase here.** Tech spec scope-gates against fnspec-integration existence; cost estimate adds Coda integration (depends on Phase 1 Coda verification); implementation prompt is a sibling of the existing build prompt. Cannot run before Phase 3 because cost estimate reads fnspecs. *(per .planning/research/SUMMARY.md § "Phase 4 — Tech Spec Scope Gate, Stage 6 Cost Estimate, Stage 7b Implementation Prompt")*`
    - Deliverables: bulleted list of (a) `generate-technical-spec/` MODIFIED (Stage 5, scope-gated; emits platform-API addendum on 4a if no 4b); (b) `generate-cost-estimate/` (Stage 6, NEW; reads Coda task-table schema + caches in 00_HUB.md, writes per-assignee rows via `rows/upsert` with `keyColumns`, polls `mutationStatus`, rate-limits at 4 req/10s); (c) `generate-build-prompt/` MODIFIED (Stage 7a); (d) `generate-implementation-prompt/` (Stage 7b, NEW; per-platform shape — Pipefy = Behaviors instructions + KB upload list; Wrike = Copilot workflow narrative; Ziflow = checklist/criteria spec); (e) risk-multiplier taxonomy STRUCTURE locked per DESIGN-22 (with mandatory `rationale` field per row) — numeric defaults DEFERRED per D-22.
    - Depends on: `Phase 1 (Coda MCP verified), Phase 2 (platform skills loaded for per-platform Stage 7b shapes), Phase 3 (fnspec-platform + fnspec-integration to read)`
    - Addresses: `STG5-01, STG6-01, STG6-02, STG7-01, STG7-02`
    - Avoids pitfalls: `CRIT-1 (Coda formula column overwrite — schema-introspect first), CRIT-2 (Coda async-202 — \`mutate_and_wait\`), CRIT-3 (Coda write rate-limit — batch \`rows/upsert\` + 4/10s buffer + idempotent retry via \`keyColumns\`), CRIT-9 (Coda token over-scope — per-client tokens + 00_HUB.md doc IDs), MOD-10 (risk-multiplier indefensible — closed taxonomy + \`rationale\` field per row)`
    - Skills introduced/modified: `1 MODIFIED (\`generate-technical-spec\`, Stage 5) + 1 NEW (\`generate-cost-estimate\`, Stage 6) + 1 MODIFIED (\`generate-build-prompt\`, Stage 7a) + 1 NEW (\`generate-implementation-prompt\`, Stage 7b). Appendix A rows 6, 7, 8, 9.`
    - Research-blocked: `— (Coda API HIGH-confidence per RESEARCH.md; per-platform Stage 7b implementation-prompt shapes documented in FEATURES.md). Risk-multiplier numeric defaults [OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22] — Phase 4 OPEN-QUESTIONS register owner.`

    **CRITICAL Phase-4 inline marker requirement (D-27 carried):** The Research-blocked cell MUST contain the exact substring `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` (verbatim from DESIGN.md Appendix E baseline list, bullet 8). This is the ONLY new inline `[OPEN]` marker introduced by Phase 3 authoring AT THIS POINT — the other 7 baseline markers already live in DESIGN.md Appendix E and are pulled forward by Plan 03-07 synthesis. Plan 03-06 (Appendix C) introduces additional inline markers on Phase 1 + Phase 7 mini-tables tied to CHANGE-04 research-blocked phases.

    **Phase 5: Test bot rebuild (v2.4)**
    - Ordering paragraph (verbatim from SUMMARY.md § Phase 5): `> **Why this phase here.** Test bot needs the new fnspec-platform + fnspec-integration to derive cases properly; building before Phase 3 means rebuilding to handle the split. Self-contained otherwise; depends only on \`<Client> Brain/test-bot/\` shape and existing \`execute-tests\` safety contract. *(per .planning/research/SUMMARY.md § "Phase 5 — Test Bot Rebuild" + § "Phase Ordering Rationale": "Phase 3 before Phase 5 because test bot derives cases from fnspecs.")*`
    - Deliverables: bulleted list of (a) `provision-test-harness/` (Stage 8a, NEW; bootstrap once, delta-update thereafter against `client_state.yaml`); (b) `generate-test-plan/` UNCHANGED body / MODIFIED path (Stage 8b — moves to `<Client> Brain/test-bot/test_cases/`); (c) `generate-uat-plan/` (Stage 8c, NEW); (d) `execute-tests/` UNCHANGED user-facing / MODIFIED-internally (Stage 8d — invokes test-bot agent); (e) `agents/test-bot-orchestrator/` agent (NEW); (f) per-client `client_state.yaml` schema (sandbox tenant IDs gated by platform, fixtures, integration toggles, `wrike_host`, `last_known_schema` per platform, `last_passed_at` per test case, `targets_artefact` per test case for obsolescence detection); (g) sandbox-allowlist extended to Coda (CRIT-5 fix); (h) `harness_drift` failure class added to `spec gap | implementation gap | environment issue | unknown`; (i) `sandbox_lock.yaml` for concurrency; (j) test-case lifecycle states `active | obsolete | quarantined`; (k) drift-detection algorithm per DESIGN-30 (pre-flight schema diff; mismatch halts + emits `schema_drift_report.md`).
    - Depends on: `Phase 3 (fnspec-platform + fnspec-integration to derive cases)`
    - Addresses: `STG8-01, STG8-02, STG8-03, STG8-04`
    - Avoids pitfalls: `CRIT-5 (sandbox enforcement gap on Coda — extend allowlist), CRIT-7 (harness drift — \`client_state.yaml\` + drift detection), MOD-11 (stale tests linger — lifecycle states), MOD-12 (Python/AI orchestrator boundary creep — hard contract on layer separation), MOD-13 (concurrency conflict in sandbox — \`sandbox_lock.yaml\`), MOD-14 (sandbox cleanup-via-no-deletes — fixture run-ID prefix)`
    - Skills introduced/modified: `1 NEW (\`provision-test-harness\`, Stage 8a) + 1 MODIFIED (\`generate-test-plan\`, Stage 8b — body unchanged; path moves to \`<Client> Brain/test-bot/test_cases/\`) + 1 NEW (\`generate-uat-plan\`, Stage 8c) + 1 MODIFIED (\`execute-tests\`, Stage 8d — internally invokes \`test-bot-orchestrator\` agent). Plus 1 NEW agent: \`agents/test-bot-orchestrator/\`. Appendix A rows 10, 11, 12, 13.`
    - Research-blocked: `— (self-contained — depends on \`<Client> Brain/test-bot/\` shape + existing \`execute-tests\` safety contract; both validated)`

    **Implementing decisions:** D-36, D-37 (milestone tags already in H2 anchors from 03-01), D-38 (mini-table shape verbatim), D-22 carried (Phase 4 risk-multiplier inline `[OPEN]` marker), D-27 carried (only 1 new marker introduced this plan; Plan 03-07 reconciles full closed list).
  </action>
  <verify>
    <automated>P4=$(awk -v hdr='## Phase 4: Tech spec + Cost + Implementation prompt (v2.3)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && [ "$(printf '%s\n' "$P4" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|')" = "6" ] && printf '%s\n' "$P4" | grep -qF '[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]' && P5=$(awk -v hdr='## Phase 5: Test bot rebuild (v2.4)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && [ "$(printf '%s\n' "$P5" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|')" = "6" ] && [ "$(grep -cE '^\| Deliverables \|' .planning/CHANGELIST.md)" -ge "5" ] && echo TASK_2_OK</automated>
  </verify>
  <acceptance_criteria>
    - Phase 4 H2 section body has all 6 mini-table rows (stateful section_between extraction returns 6)
    - Phase 4 Research-blocked cell contains the exact substring `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` (verbatim from DESIGN.md Appendix E bullet 8) — `printf '%s\n' "$P4" | grep -qF '[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]'` succeeds
    - Phase 4 Avoids pitfalls cell contains CRIT-1, CRIT-2, CRIT-3, CRIT-9, MOD-10 (5 individual `grep -qF` checks)
    - Phase 4 Addresses cell contains STG5-01, STG6-01, STG6-02, STG7-01, STG7-02
    - Phase 4 Skills cell mentions `generate-cost-estimate` (NEW) AND `generate-implementation-prompt` (NEW) AND `generate-technical-spec` (MODIFIED) AND `generate-build-prompt` (MODIFIED)
    - Phase 5 H2 section body has all 6 mini-table rows (stateful section_between extraction returns 6)
    - Phase 5 Avoids pitfalls cell contains CRIT-5, CRIT-7, MOD-11, MOD-12, MOD-13, MOD-14
    - Phase 5 Addresses cell contains STG8-01, STG8-02, STG8-03, STG8-04
    - Phase 5 Skills cell mentions `provision-test-harness` AND `generate-uat-plan` (NEW) AND `agents/test-bot-orchestrator/`
    - Phase 5 Depends on cell mentions only Phase 3 (per SUMMARY.md ordering rule "Phase 3 before Phase 5")
    - Total `| Deliverables |` count after Tasks 1+2 is at least 5 (covers Phase 1..5)
    - Total `| Addresses |` count >= 5 ; total `| Avoids pitfalls |` count >= 5
    - **No accidental modifications** to Phase 6..9 / Appendix sections (verifiable by `awk` extracting Phase 6 body — should still contain the populated-by placeholder note from 03-01 skeleton)
    - **No accidental new D-IDs introduced** (D-N numbering frozen at D-45 per CONTEXT D-44 — this plan reuses existing D-IDs only)
  </acceptance_criteria>
  <done>
    Phases 4 + 5 mini-tables populated under their existing H2 anchors. Each carries the 6-row Attribute/Detail mini-table per D-38 plus an ordering-rationale paragraph citing SUMMARY.md. Phase 4 Research-blocked cell carries the inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker (verbatim from DESIGN.md baseline). CHANGELIST.md now has 5 of the 9 phase mini-tables required by structural-check assertion #11; remaining 4 phases populated by sequential Plan 03-03 (Wave 3 — file-ownership on `.planning/CHANGELIST.md` forces serialisation per cross-AI C8).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 03-02 mini-table content ↔ SUMMARY.md / PITFALLS.md / REQUIREMENTS.md / DESIGN.md | Drift class. Each mini-table cell cites a source-of-truth file; mis-citations break reviewer cross-reference flow. Mitigation: every cell's content has a known source-of-truth pointer in this plan's `<interfaces>` block; acceptance criteria spot-check 3+ cited IDs per cell. |
| Plan 03-02 ↔ Plan 03-03 (sequential per cross-AI C8) | File-conflict class. Both plans write to `.planning/CHANGELIST.md` but to DIFFERENT H2 sections (this plan: Phase 1..5; Plan 03-03: Phase 6..9). Disjoint H2 ownership prevents content conflict, BUT shared file ownership forces sequential execution (`wave: 2` → `wave: 3`); structural-check assertion #11 (per-phase row count) catches if either plan accidentally writes outside its assigned H2 range. |
| Inline `[OPEN: Phase 4 — ...]` markers ↔ DESIGN.md Appendix E baseline | Drift class. Plan 03-02 introduces 1 new inline marker (Phase 4 risk-multiplier); the marker text MUST match DESIGN.md bullet 8 verbatim so Plan 03-07 synthesis reconciliation passes. Mitigation: action block specifies the exact verbatim text; acceptance criteria asserts substring presence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Wrong column header in mini-table (e.g. `| Avoids |` instead of `| Avoids pitfalls |`) breaks structural-check assertion #11 | mitigate | Acceptance criteria match the EXACT row labels via `grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|'` returning exactly 6 per phase. |
| T-03-02-02 | Tampering | Phase H2 anchor accidentally re-emitted under itself (duplicate `## Phase N:` line) | mitigate | Action block instructs to APPEND under existing H2 (not re-emit); acceptance criteria do not match an exact duplicate count but the structural-check H2-anchor assertions in 03-01 use `grep -qF` (substring presence, not count) so a duplicate would not cause a fail — review must ensure no duplicate via diff against pre-Task-1 state. |
| T-03-02-03 | Tampering | Mis-cited PITFALL ID (e.g. citing CRIT-4 instead of CRIT-3 in Phase 4 — semantic error not caught by structural-check) | mitigate | Acceptance criteria spot-check the EXACT PITFALL IDs per phase via individual `grep -qF` calls (5 per phase for Avoids cells). Reviewer attestation required for completeness. |
| T-03-02-04 | Tampering | Phase 4 inline `[OPEN]` marker text drifts from DESIGN.md Appendix E bullet 8 — breaks Plan 03-07 reconciliation | mitigate | Acceptance criteria asserts EXACT verbatim substring `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` (including em-dash, the word "defaults" without `numeric`, and the trailing `per D-22`). |
| T-03-02-05 | Tampering | Ordering paragraph exceeds 4-sentence cap (CONTEXT Claude's-Discretion) — degrades reviewer experience | mitigate | Acceptance criteria use a heuristic count of `. ` (full-stop + space) in the blockquote-prefixed paragraph; >5 indicates probable cap violation and needs review. |
| T-03-02-06 | Information Disclosure | Cited `file:line` references reveal internal paths | accept | All cited paths are inside `.planning/` or `dydx-delivery/` — both already public on the planning-artefact path. |
| T-03-02-07 | Tampering | Plan 03-02 + Plan 03-03 race-condition on CHANGELIST.md | mitigate | Per cross-AI C8 (gemini LOW): the plans are SEQUENTIAL (`wave: 2` → `wave: 3`), not parallel — file-ownership on `.planning/CHANGELIST.md` forces serialisation regardless of disjoint H2 ownership. No race-condition possible because the orchestrator runs Wave N+1 only after Wave N commits. Worst-case: a second plan's Edit fails because the file changed between Read and Edit — executor re-reads and re-applies (per execute-plan workflow). |
</threat_model>

<verification>
End of Plan 03-02 success-state:
1. Phase 1, 2, 3, 4, 5 H2 sections each have a 6-row Attribute/Detail mini-table with EXACT row labels.
2. Phase 1 Research-blocked cell carries inline `⚠` marker per D-42 referencing Appendix C.
3. Phase 4 Research-blocked cell carries inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker (verbatim from DESIGN.md Appendix E bullet 8).
4. Each phase has an ordering-rationale paragraph (hard cap 4 sentences) citing SUMMARY.md inline.
5. Total `| Deliverables |` count >= 5 ; `| Addresses |` count >= 5 ; `| Avoids pitfalls |` count >= 5.
6. No modifications to Phase 6..9 / Appendix sections (Plan 03-03 + 03-04..07 territory).
7. structural-check exit status: still 1 (because Phase 6..9 not yet populated; Appendix A/B/E still empty) — NOT 0 yet; that's normal Wave 1 state.

Manual reviewer attestation:
- [ ] Each of Phase 1, 2, 3, 4, 5 mini-tables has exactly 6 rows in canonical order (Deliverables / Depends on / Addresses / Avoids pitfalls / Skills introduced/modified / Research-blocked).
- [ ] Each ordering paragraph quotes SUMMARY.md verbatim where possible (verifiable by spot-check against the source file).
- [ ] No CHANGE-N row carries severity tags (D-15 narrowed — severity tags only on AUDIT-07 lifts in Appendix B).
- [ ] No new D-IDs introduced (D-N pool frozen per CONTEXT D-44).
</verification>

<success_criteria>
- [ ] Phase 1, 2, 3, 4, 5 H2 sections populated with ordering paragraph + 6-row mini-table per D-38
- [ ] Phase 1 mini-table Research-blocked cell carries `⚠` linking to Appendix C (D-42)
- [ ] Phase 4 mini-table Research-blocked cell carries verbatim inline `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` marker (D-27 carried)
- [ ] Each Avoids pitfalls cell cites the specific PITFALL IDs called out in SUMMARY.md per-phase rationale
- [ ] Each Addresses cell cites the FOUND-/PLAT-/STG*- REQ-IDs per REQUIREMENTS.md
- [ ] Each Skills cell ties phase to Appendix A row enumeration (per D-39 Appendix A row ordering rule)
- [ ] No content under `dydx-delivery/` modified
- [ ] No modifications outside `.planning/CHANGELIST.md` Phase 1..5 H2 sections + `.planning/phases/03-changelist/03-02-SUMMARY.md` (created by `<output>` step)
</success_criteria>

<output>
After completion, create `.planning/phases/03-changelist/03-02-SUMMARY.md` documenting:
- Phase 1, 2, 3, 4, 5 mini-tables populated (5 of 9 — Plan 03-03 covers the rest)
- Per-phase ordering-rationale source (verbatim from SUMMARY.md § Phase Ordering Rationale + § Phase N — <name>)
- Cited PITFALL IDs per phase (Phase 1: 4 IDs; Phase 2: 4 IDs; Phase 3: 1 ID + anti-features; Phase 4: 5 IDs; Phase 5: 6 IDs)
- Cited REQ-IDs per phase (FOUND-01..07 / PLAT-01..03 / STG1-01..02 / STG3-01 / STG4-01..02 / STG5-01 / STG6-01..02 / STG7-01..02 / STG8-01..04 — 18 distinct IDs)
- Cited Skills cells (Appendix A rows tied: rows 1-12 for the 12 stage-skill changes Phases 1-5 introduce + 3 platform skills + 1 RETIRED + the test-bot agent)
- Inline `[OPEN]` markers introduced this plan: 1 (Phase 4 risk-multiplier — verbatim from DESIGN.md Appendix E bullet 8)
- Pointer: "Plan 03-03 (sequential Wave 3) populates Phase 6..9 mini-tables (v2.5 + v2.6 milestones); Plan 03-04 (Wave 4) populates Appendix A; Plan 03-05 (Wave 5) populates Appendices B + D; Plan 03-06 (Wave 6) populates Appendix C + adds inline markers on Phase 1 + Phase 2 mini-tables; Plan 03-07 (Wave 7) does synthesis + final structural-check pass. Per cross-AI C8 — file-ownership on `.planning/CHANGELIST.md` forces sequential execution despite disjoint H2 ownership."
</output>
</content>
</invoke>