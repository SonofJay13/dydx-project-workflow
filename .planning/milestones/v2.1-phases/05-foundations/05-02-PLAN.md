---
phase: 05-foundations
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - dydx-delivery/skills/execute-tests/SKILL.md
  - dydx-delivery/skills/execute-tests/references/safety-rules.md
  - dydx-delivery/skills/execute-tests/references/results-template.md
  - dydx-delivery/skills/generate-test-plan/SKILL.md
  - dydx-delivery/skills/generate-test-plan/references/test-plan-template.md
  - dydx-delivery/skills/generate-sow/SKILL.md
  - dydx-delivery/skills/generate-sow/references/sow-template.md
  - dydx-delivery/skills/generate-functional-spec/SKILL.md
  - dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md
  - dydx-delivery/skills/generate-technical-spec/SKILL.md
  - dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md
  - dydx-delivery/skills/generate-build-prompt/SKILL.md
  - dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md
  - dydx-delivery/skills/discovery-intake/SKILL.md
  - dydx-delivery/skills/discovery-intake/references/intake-template.md
  - dydx-delivery/README.md
autonomous: true
requirements: [FOUND-05, FOUND-06]
tags: [foundations, skill-repoint, hard-rules-dedup, file-renumbering, based-on-normalisation]

must_haves:
  truths:
    - "Per D-59, the same one-line hard-rules pointer wording appears VERBATIM in exactly 4 files (AUDIT-05.1 ground truth, NOT D-59's nominee list)"
    - "Per D-62, dydx-delivery/skills/execute-tests/references/safety-rules.md collapses to a one-line pointer (NOT deleted) to preserve cite-anchor stability"
    - "Per D-58 + DESIGN-02, all 13 based_on_* values + skill prose filename refs + template Stage-N self-labels carry the new v2 prefix"
    - "Per RESEARCH §Codebase Reality §3, based_on_* keys are already snake_case — 'normalisation' is filename-value renumbering, not key-name casing"
    - "Cross-AI flag 1: AUDIT-05.1 supersedes D-59's nominee list (discovery-intake/SKILL.md + generate-sow/SKILL.md do NOT carry hard-rules duplicates per grep)"
  artifacts:
    - path: "dydx-delivery/skills/execute-tests/SKILL.md"
      provides: "execute-tests skill with hard-rules block replaced by D-59 pointer + filename renumbers"
      contains: "See `dydx-delivery/references/safety-rules.md`"
    - path: "dydx-delivery/skills/execute-tests/references/safety-rules.md"
      provides: "Collapsed-to-pointer cite-anchor stub per D-62"
      contains: "Pointer-only"
    - path: "dydx-delivery/skills/generate-test-plan/references/test-plan-template.md"
      provides: "test-plan template with hard-rules block replaced + Stage 8b label + 05_techspec based_on"
      contains: "Stage 8b"
    - path: "dydx-delivery/README.md"
      provides: "plugin README with hard-rules section collapsed to D-59 pointer (B.1/B.3 fixes deferred to Wave 5)"
      contains: "See `dydx-delivery/references/safety-rules.md`"
  key_links:
    - from: "All 4 hard-rules-duplicate files (AUDIT-05.1)"
      to: "dydx-delivery/references/safety-rules.md"
      via: "D-59 verbatim one-line pointer"
      pattern: "See `dydx-delivery/references/safety-rules.md` for the canonical ruleset"
    - from: "Every based_on_* value in skills + templates"
      to: "dydx-delivery/references/stage-numbering.md (Wave 1)"
      via: "DESIGN-02 old to new prefix mapping"
      pattern: "based_on_(discovery|sow|functional_spec|technical_spec|test_plan): (02|03|04a|05|07a|08b)_"
---

<objective>
Wave 2 of Phase 5: repoint the 7 v0.3.0 skills at the Wave 1 canonical references. Three sub-deliverables:
1. **Hard-rules dedup (FOUND-05).** Replace inlined hard-rules blocks in 4 files (AUDIT-05.1 ground truth) with the D-59 verbatim one-line pointer to `dydx-delivery/references/safety-rules.md`.
2. **File renumbering (FOUND-06).** Apply DESIGN-02 old to new prefix mapping (00→02 / 01→03 / 02→04a / 03→05 / 04→07a / test-plan_v*→08b / results-…v→08d) to: (a) all 13 `based_on_*` value strings in SKILL.md + templates, (b) skill prose filename references in `## Inputs` / `## Output` sections, (c) template "Stage N" self-labels.
3. **based_on_* normalisation (FOUND-05 sub-clause).** Already snake_case per grep — this is a NO-OP for casing; satisfied by the filename renumbering in (2).

Purpose: After Wave 2, the 4 AUDIT-05.1 hard-rules-duplicate surfaces all point at the canonical SoT (zero inlined duplicates), and all 13 `based_on_*` values + skill prose + template labels reflect the v2 stage-numbering. Wave 3 onward can land manifest/scaffold/connector-matrix work without skill-content drift.

Output: 14 modified skill / README files (each touched in exactly one place per file per task) with grep-verifiable invariants.

**Cross-AI flag 1 adjudication (surfaced in planning_context):** D-59 nominates `discovery-intake/SKILL.md` and `generate-sow/SKILL.md` as 2 of 4 pointer-replacement targets. AUDIT-05.1 ground truth (verified by grep) shows those files do NOT carry hard-rules duplicates. This plan uses **AUDIT-05.1's 4 files** (execute-tests SKILL.md + test-plan-template + dydx-delivery/README.md + execute-tests safety-rules.md collapse), NOT D-59's nominees. D-59's *wording* of the pointer remains authoritative.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_prerequisite>
**Execution prerequisite (C-3 — Codex MEDIUM):** This plan's acceptance-criteria commands assume Bash 4+, `jq`, and `grep -E` semantics (Git Bash, WSL, or equivalent). On Windows PowerShell, run via `bash` or in a Git-Bash terminal. The prerequisite check itself is gated by Task 0 in Plan 05-01 (Wave 1); if Plan 05-01 Task 0 did not pass, Plan 05-02 MUST NOT proceed.
</execution_prerequisite>

<context>
@.planning/phases/05-foundations/05-CONTEXT.md
@.planning/phases/05-foundations/05-RESEARCH.md
@.planning/phases/05-foundations/05-PATTERNS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@dydx-delivery/references/safety-rules.md
@dydx-delivery/references/stage-numbering.md

<interfaces>
<!-- The D-59 verbatim pointer one-liner. Lands in 4 files (AUDIT-05.1 ground truth). -->

D-59 pointer text (literal, including the markdown blockquote `>`):
```
> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

DESIGN-02 old to new prefix mapping (lifted from Wave 1 stage-numbering.md):
| v0.3.0 prefix | v2 prefix | Stage |
|---|---|---|
| 00_discovery_* | 02_discovery_* | Stage 2 |
| 01_sow_* | 03_sow_* | Stage 3 |
| 02_functional-spec_* | 04a_fnspec-platform_* | Stage 4a |
| 03_technical-spec_* | 05_techspec_* | Stage 5 |
| 04_build-prompt_* | 07a_build-prompt-dev_* | Stage 7a |
| test-plan_v* | 08b_test-plan_* | Stage 8b |
| results-YYYY-MM-DD_v* | 08d_test-results_* | Stage 8d |

Template Stage-N self-label renumbers (per RESEARCH §Codebase Reality):
- intake-template.md:13 "Stage 0" -> "Stage 2"
- functional-spec-template.md:13 "Stage 2" -> "Stage 4a"
- technical-spec-template.md:13 "Stage 3" -> "Stage 5"
- test-plan-template.md:17 "Stage 5" -> "Stage 8b"
- build-prompt-template.md:15 "Stage 6" -> "Stage 7a"
- results-template.md:18 "Stage 6" -> "Stage 8d"
- sow-template.md — no Stage label currently; OPTIONAL add "Stage 3"

13 based_on_* renumbers (per RESEARCH §Skill Repoint Mechanics table):
1. generate-sow/SKILL.md:79                                based_on_discovery: 00_discovery_v{N}.md      -> 02_discovery_v{N}.md
2. generate-sow/references/sow-template.md:7               based_on_discovery: 00_discovery_v<N>.md       -> 02_discovery_v<N>.md
3. generate-functional-spec/SKILL.md:81                    based_on_sow: 01_sow_v{N}.md                   -> 03_sow_v{N}.md
4. generate-functional-spec/references/functional-spec-template.md:7  based_on_sow: 01_sow_v<N>.md        -> 03_sow_v<N>.md
5. generate-technical-spec/SKILL.md:105                    based_on_functional_spec: 02_functional-spec_v{N}.md -> 04a_fnspec-platform_v{N}.md
6. generate-technical-spec/references/technical-spec-template.md:7  based_on_functional_spec: 02_functional-spec_v<N>.md -> 04a_fnspec-platform_v<N>.md
7. generate-test-plan/SKILL.md:99                          based_on_technical_spec: 03_technical-spec_v{N}.md -> 05_techspec_v{N}.md
8. generate-test-plan/references/test-plan-template.md:7   based_on_technical_spec: 03_technical-spec_v<N>.md -> 05_techspec_v<N>.md
9. generate-build-prompt/SKILL.md:118                      based_on_technical_spec: 03_technical-spec_v{N}.md -> 05_techspec_v{N}.md
10. generate-build-prompt/SKILL.md:119                     based_on_test_plan: test-plan_v{N}.md          -> 08b_test-plan_v{N}.md
11. generate-build-prompt/references/build-prompt-template.md:7  based_on_technical_spec: 03_technical-spec_v<N>.md -> 05_techspec_v<N>.md
12. generate-build-prompt/references/build-prompt-template.md:8  based_on_test_plan: test-plan_v<N>.md    -> 08b_test-plan_v<N>.md
13. execute-tests/references/results-template.md:6         based_on_test_plan: test-plan_v<N>.md          -> 08b_test-plan_v<N>.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hard-rules dedup — replace 4 inlined blocks with D-59 verbatim pointer (AUDIT-05.1 ground truth)</name>
  <files>dydx-delivery/skills/execute-tests/SKILL.md, dydx-delivery/skills/generate-test-plan/references/test-plan-template.md, dydx-delivery/README.md, dydx-delivery/skills/execute-tests/references/safety-rules.md</files>
  <read_first>
    - `.planning/AUDIT.md:362-381` (AUDIT-05.1 — authoritative 4-file ground truth, NOT D-59's nominee list)
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-59 (pointer wording — verbatim) and D-62 (safety-rules.md COLLAPSE NOT delete)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Skill Repoint Mechanics / Hard-rules pointer wording" — lock-table of 4 files with line ranges
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "Shared Patterns / Hard-rules pointer one-liner" — 4-file target list (AUDIT-05.1 ground truth)
    - Current state of each of the 4 target files BEFORE edit (read each file first to confirm line ranges still match AUDIT-05.1 citations)
    - `dydx-delivery/references/safety-rules.md` (Wave 1 output — the canonical SoT being pointed at)
  </read_first>
  <action>
    Apply the SAME verbatim D-59 pointer wording to exactly **4 files** (AUDIT-05.1 ground truth, NOT D-59's nominees — D-59 named discovery-intake and generate-sow which grep verifies do NOT carry hard-rules duplicates):

    **The 4 target files + per-file edit:**

    **File A — `dydx-delivery/skills/execute-tests/SKILL.md` (lines ~20-30):**

    Locate the H2 section currently titled `## Hard rules — enforced regardless of test plan content` (around line 20) and its 7-rule numbered list (lines ~24-30). Replace the entire section (heading + list) with:
    ```markdown
    ## Hard rules

    > **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
    ```

    **File B — `dydx-delivery/skills/generate-test-plan/references/test-plan-template.md` (lines ~36-44):**

    Locate the H2 section currently titled `## Hard rules (enforced by execute-tests)` (around line 36) and its 5 condensed bullets (lines ~38-44). Replace heading + bullets with the SAME pointer block as File A:
    ```markdown
    ## Hard rules

    > **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
    ```

    **File C — `dydx-delivery/README.md` (lines ~99-105):**

    Locate the H2 section currently titled `## Test execution — safety rules (hard)` (around line 99) and its 5 condensed bullets (lines ~101-105). Replace heading + bullets with:
    ```markdown
    ## Test execution — safety rules

    > **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
    ```

    Do NOT touch any other line in `dydx-delivery/README.md` — B.1 truncation fix (line ~126) and B.3 step-count fix (root README) are Wave 5 work.

    **File D — `dydx-delivery/skills/execute-tests/references/safety-rules.md` (full file collapse per D-62):**

    Per D-62, this file's 101-line content was lifted in Wave 1 to `dydx-delivery/references/safety-rules.md`. Now collapse this file to a one-line pointer stub (do NOT delete — preserves cite-anchor stability for any external reference like `execute-tests/SKILL.md:22` citations). Replace the entire file content with EXACTLY:
    ```markdown
    # execute-tests — Safety rules

    > **Pointer-only.** The canonical hard-rules ruleset has moved to `dydx-delivery/references/safety-rules.md` (per D-62 / FOUND-01). This pointer is preserved for cite-anchor stability — do not delete.
    ```

    (3 lines total: H1 + blank + blockquote.)

    **Critical:** the pointer wording in Files A, B, C is identical character-for-character. The pointer in File D is intentionally different (it's a collapse-marker, not the D-59 pointer). Acceptance criteria validate both shapes separately.
  </action>
  <verify>
    <automated>grep -l "See \`dydx-delivery/references/safety-rules.md\` for the canonical ruleset" dydx-delivery/skills/execute-tests/SKILL.md dydx-delivery/skills/generate-test-plan/references/test-plan-template.md dydx-delivery/README.md | wc -l | tr -d ' \n' | grep -qx 3 &amp;&amp; grep -qF "Pointer-only" dydx-delivery/skills/execute-tests/references/safety-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lF "See \`dydx-delivery/references/safety-rules.md\` for the canonical ruleset" dydx-delivery/skills/execute-tests/SKILL.md dydx-delivery/skills/generate-test-plan/references/test-plan-template.md dydx-delivery/README.md | wc -l` returns 3 (the D-59 pointer appears in exactly these 3 files)
    - `grep -cF "Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions." dydx-delivery/skills/execute-tests/SKILL.md` returns 1 (uniform wording)
    - `grep -cF "Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions." dydx-delivery/skills/generate-test-plan/references/test-plan-template.md` returns 1
    - `grep -cF "Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions." dydx-delivery/README.md` returns 1
    - `! grep -qE '^## Hard rules — enforced regardless of test plan content' dydx-delivery/skills/execute-tests/SKILL.md` exits 0 (old heading gone)
    - `! grep -qE '^## Hard rules \(enforced by execute-tests\)' dydx-delivery/skills/generate-test-plan/references/test-plan-template.md` exits 0
    - `! grep -qE '^## Test execution — safety rules \(hard\)$' dydx-delivery/README.md` exits 0
    - `test -f dydx-delivery/skills/execute-tests/references/safety-rules.md` exits 0 (NOT deleted — D-62 collapse, not delete)
    - `grep -qF "Pointer-only" dydx-delivery/skills/execute-tests/references/safety-rules.md` exits 0
    - `grep -qF "The canonical hard-rules ruleset has moved to" dydx-delivery/skills/execute-tests/references/safety-rules.md` exits 0
    - `wc -l dydx-delivery/skills/execute-tests/references/safety-rules.md | awk '{print $1}'` returns at most 5 (file collapsed; originally 101 lines)
    - `grep -cE '^## [0-9]+\. ' dydx-delivery/skills/execute-tests/references/safety-rules.md` returns 0 (no rule headings survive in the collapsed stub)
    - `! grep -qE '^- Sending real emails' dydx-delivery/skills/execute-tests/SKILL.md` exits 0 (inlined rule bullets removed)
    - `! grep -qE '^- Sending real emails' dydx-delivery/README.md` exits 0
    - `! grep -qE '^- Sending real emails' dydx-delivery/skills/generate-test-plan/references/test-plan-template.md` exits 0
    - Across the entire `dydx-delivery/` tree EXCLUDING `dydx-delivery/references/safety-rules.md` (the canonical), the verbatim phrase "Sandbox enforcement" appears 0 times outside the collapsed pointer file: `grep -rln "Sandbox enforcement" dydx-delivery/ --include='*.md' | grep -v 'dydx-delivery/references/safety-rules.md' | xargs -I{} grep -L Pointer-only {} | wc -l` returns 0
  </acceptance_criteria>
  <done>All 4 AUDIT-05.1 hard-rules-duplicate surfaces carry the D-59 verbatim pointer (3 files) OR the D-62 collapse marker (1 file). No inlined hard-rules bullets survive across the 3 pointer-target files. The canonical safety-rules.md at the plugin-level is the sole authoritative source.</done>
</task>

<task type="auto">
  <name>Task 2: based_on_* filename renumber across 7 SKILL.md files + 7 template files (13 occurrences)</name>
  <files>dydx-delivery/skills/generate-sow/SKILL.md, dydx-delivery/skills/generate-sow/references/sow-template.md, dydx-delivery/skills/generate-functional-spec/SKILL.md, dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md, dydx-delivery/skills/generate-technical-spec/SKILL.md, dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md, dydx-delivery/skills/generate-test-plan/SKILL.md, dydx-delivery/skills/generate-test-plan/references/test-plan-template.md, dydx-delivery/skills/generate-build-prompt/SKILL.md, dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md, dydx-delivery/skills/execute-tests/references/results-template.md</files>
  <read_first>
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "based_on_* filename renumber (per D-58)" — the 13-row table with file:line + old/new value (lines ~349-365)
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "Filename renumber (apply to all 13 based_on_* value strings + all prose filename refs)"
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-58 (renumber surface = stage-numbering.md + skill bodies + skill internal templates)
    - `dydx-delivery/references/stage-numbering.md` (Wave 1 output — the canonical mapping authority)
    - Current state of each target file (verify line numbers in `<interfaces>` block above are still accurate)
  </read_first>
  <action>
    Apply DESIGN-02 old to new prefix mapping to all 13 `based_on_*` value strings. Each is a SINGLE-LINE in-place substitution (key unchanged, value's filename prefix updated). Per RESEARCH §3, the keys are already snake_case so no key edits — only value-string filename prefix updates.

    Execute these 13 edits VERBATIM (file:line → old value → new value):

    1. `dydx-delivery/skills/generate-sow/SKILL.md:79` —
       FROM: `based_on_discovery: 00_discovery_v{N}.md`
       TO:   `based_on_discovery: 02_discovery_v{N}.md`

    2. `dydx-delivery/skills/generate-sow/references/sow-template.md:7` —
       FROM: `based_on_discovery: 00_discovery_v<N>.md`
       TO:   `based_on_discovery: 02_discovery_v<N>.md`

    3. `dydx-delivery/skills/generate-functional-spec/SKILL.md:81` —
       FROM: `based_on_sow: 01_sow_v{N}.md`
       TO:   `based_on_sow: 03_sow_v{N}.md`

    4. `dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md:7` —
       FROM: `based_on_sow: 01_sow_v<N>.md`
       TO:   `based_on_sow: 03_sow_v<N>.md`

    5. `dydx-delivery/skills/generate-technical-spec/SKILL.md:105` —
       FROM: `based_on_functional_spec: 02_functional-spec_v{N}.md`
       TO:   `based_on_functional_spec: 04a_fnspec-platform_v{N}.md`

    6. `dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md:7` —
       FROM: `based_on_functional_spec: 02_functional-spec_v<N>.md`
       TO:   `based_on_functional_spec: 04a_fnspec-platform_v<N>.md`

    7. `dydx-delivery/skills/generate-test-plan/SKILL.md:99` —
       FROM: `based_on_technical_spec: 03_technical-spec_v{N}.md`
       TO:   `based_on_technical_spec: 05_techspec_v{N}.md`

    8. `dydx-delivery/skills/generate-test-plan/references/test-plan-template.md:7` —
       FROM: `based_on_technical_spec: 03_technical-spec_v<N>.md`
       TO:   `based_on_technical_spec: 05_techspec_v<N>.md`

    9. `dydx-delivery/skills/generate-build-prompt/SKILL.md:118` —
       FROM: `based_on_technical_spec: 03_technical-spec_v{N}.md`
       TO:   `based_on_technical_spec: 05_techspec_v{N}.md`

    10. `dydx-delivery/skills/generate-build-prompt/SKILL.md:119` —
        FROM: `based_on_test_plan: test-plan_v{N}.md`
        TO:   `based_on_test_plan: 08b_test-plan_v{N}.md`

    11. `dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md:7` —
        FROM: `based_on_technical_spec: 03_technical-spec_v<N>.md`
        TO:   `based_on_technical_spec: 05_techspec_v<N>.md`

    12. `dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md:8` —
        FROM: `based_on_test_plan: test-plan_v<N>.md`
        TO:   `based_on_test_plan: 08b_test-plan_v<N>.md`

    13. `dydx-delivery/skills/execute-tests/references/results-template.md:6` —
        FROM: `based_on_test_plan: test-plan_v<N>.md`
        TO:   `based_on_test_plan: 08b_test-plan_v<N>.md`

    For each edit: locate the exact line by reading the file first; replace only the value-string filename prefix; preserve indentation, surrounding lines, frontmatter YAML structure, and the angle-bracket vs curly-bracket convention (some files use `<N>`, others `{N}`). Do NOT change the key name.
  </action>
  <verify>
    <automated>! grep -rn "based_on_discovery: 00_discovery_" dydx-delivery/skills/ &amp;&amp; ! grep -rn "based_on_sow: 01_sow_" dydx-delivery/skills/ &amp;&amp; ! grep -rn "based_on_functional_spec: 02_functional-spec_" dydx-delivery/skills/ &amp;&amp; ! grep -rn "based_on_technical_spec: 03_technical-spec_" dydx-delivery/skills/ &amp;&amp; ! grep -rn "based_on_test_plan: test-plan_v" dydx-delivery/skills/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rcE 'based_on_discovery: 02_discovery_v[{<]N[}>]\.md' dydx-delivery/skills/generate-sow/` returns at least 2 (SKILL.md + sow-template.md)
    - `grep -rcE 'based_on_sow: 03_sow_v[{<]N[}>]\.md' dydx-delivery/skills/generate-functional-spec/` returns at least 2
    - `grep -rcE 'based_on_functional_spec: 04a_fnspec-platform_v[{<]N[}>]\.md' dydx-delivery/skills/generate-technical-spec/` returns at least 2
    - `grep -rcE 'based_on_technical_spec: 05_techspec_v[{<]N[}>]\.md' dydx-delivery/skills/` returns at least 4 (generate-test-plan SKILL+template + generate-build-prompt SKILL+template)
    - `grep -rcE 'based_on_test_plan: 08b_test-plan_v[{<]N[}>]\.md' dydx-delivery/skills/` returns at least 3 (generate-build-prompt SKILL+template + execute-tests results-template)
    - `! grep -rn "based_on_discovery: 00_discovery_" dydx-delivery/skills/` exits 0 (zero old-prefix survivors)
    - `! grep -rn "based_on_sow: 01_sow_" dydx-delivery/skills/` exits 0
    - `! grep -rn "based_on_functional_spec: 02_functional-spec_" dydx-delivery/skills/` exits 0
    - `! grep -rn "based_on_technical_spec: 03_technical-spec_" dydx-delivery/skills/` exits 0
    - `! grep -rn "based_on_test_plan: test-plan_v" dydx-delivery/skills/` exits 0 (the bare `test-plan_v` form is gone)
    - All 13 file paths in `<files>` still exist (no accidental deletion): `for f in dydx-delivery/skills/generate-sow/SKILL.md dydx-delivery/skills/generate-sow/references/sow-template.md dydx-delivery/skills/generate-functional-spec/SKILL.md dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md dydx-delivery/skills/generate-technical-spec/SKILL.md dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md dydx-delivery/skills/generate-test-plan/SKILL.md dydx-delivery/skills/generate-test-plan/references/test-plan-template.md dydx-delivery/skills/generate-build-prompt/SKILL.md dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md dydx-delivery/skills/execute-tests/references/results-template.md; do test -f "$f" || exit 1; done`
  </acceptance_criteria>
  <done>All 13 `based_on_*` value strings carry the new v2 stage-numbering prefix. Zero old-prefix survivors. Keys unchanged (already snake_case). File structure preserved (no accidental frontmatter damage).</done>
</task>

<task type="auto">
  <name>Task 3: Renumber skill prose filename refs + template Stage-N self-labels + discovery-intake (the 7th skill)</name>
  <files>dydx-delivery/skills/discovery-intake/SKILL.md, dydx-delivery/skills/discovery-intake/references/intake-template.md, dydx-delivery/skills/generate-sow/SKILL.md, dydx-delivery/skills/generate-functional-spec/SKILL.md, dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md, dydx-delivery/skills/generate-technical-spec/SKILL.md, dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md, dydx-delivery/skills/generate-test-plan/SKILL.md, dydx-delivery/skills/generate-test-plan/references/test-plan-template.md, dydx-delivery/skills/generate-build-prompt/SKILL.md, dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md, dydx-delivery/skills/execute-tests/references/results-template.md</files>
  <read_first>
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Other renumber surfaces in skill bodies (non-`based_on_*` references to filenames)" and § "Templates' Stage N self-labels"
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "Stage-N self-label renumber (apply to 6 of 7 templates)"
    - `dydx-delivery/references/stage-numbering.md` (Wave 1 canonical)
    - Each target file's current state (read before editing to confirm line content)
  </read_first>
  <action>
    Two sub-deliverables:

    **Sub-deliverable A — Skill prose filename references.**

    Each of the 7 SKILL.md files carries prose references to artefact filenames in `## Inputs`, `## Output`, `## How to run`, and the start-at-any-point triage form. Apply DESIGN-02 mapping wherever an old-prefix filename appears in prose (NOT just `based_on_*`).

    Grep candidates per skill (locate first, then in-place replace each match):
    - `discovery-intake/SKILL.md` — `00_discovery_v*.md` → `02_discovery_v*.md`
    - `generate-sow/SKILL.md` — `00_discovery_v*.md` → `02_discovery_v*.md`; `01_sow_v*.md` → `03_sow_v*.md`
    - `generate-functional-spec/SKILL.md` — `01_sow_v*.md` → `03_sow_v*.md`; `02_functional-spec_v*.md` → `04a_fnspec-platform_v*.md`
    - `generate-technical-spec/SKILL.md` — `02_functional-spec_v*.md` → `04a_fnspec-platform_v*.md`; `03_technical-spec_v*.md` → `05_techspec_v*.md`
    - `generate-test-plan/SKILL.md` — `03_technical-spec_v*.md` → `05_techspec_v*.md`; `test-plan_v*.md` → `08b_test-plan_v*.md`
    - `generate-build-prompt/SKILL.md` — `03_technical-spec_v*.md` → `05_techspec_v*.md`; `04_build-prompt_v*.md` → `07a_build-prompt-dev_v*.md`; `test-plan_v*.md` → `08b_test-plan_v*.md`
    - `execute-tests/SKILL.md` — `test-plan_v*.md` → `08b_test-plan_v*.md`; any `results-YYYY-MM-DD_v*.md` → `08d_test-results_v*.md`

    For each file: grep for the old patterns (e.g., `grep -nE "00_discovery_v|01_sow_v|02_functional-spec_v|03_technical-spec_v|04_build-prompt_v|test-plan_v[^a]|results-YYYY-MM-DD_v"`), then apply targeted edits at each match preserving surrounding prose.

    **Sub-deliverable B — Template Stage-N self-label renumbers.**

    Each of 6 templates carries a "Stage N" self-label on a specific line (per RESEARCH §Codebase Reality). Update each:

    1. `dydx-delivery/skills/discovery-intake/references/intake-template.md:13`
       FROM literal substring: `Stage 0 of the dydx-delivery pipeline`
       TO   literal substring: `Stage 2 of the dydx-delivery pipeline`

    2. `dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md:13`
       FROM: `Stage 2 of the dydx-delivery pipeline`
       TO:   `Stage 4a of the dydx-delivery pipeline`

    3. `dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md:13`
       FROM: `Stage 3 of the dydx-delivery pipeline`
       TO:   `Stage 5 of the dydx-delivery pipeline`

    4. `dydx-delivery/skills/generate-test-plan/references/test-plan-template.md:17`
       FROM: `Stage 5 of the dydx-delivery pipeline`
       TO:   `Stage 8b of the dydx-delivery pipeline`

    5. `dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md:15`
       FROM: `Stage 6 of the dydx-delivery pipeline`
       TO:   `Stage 7a of the dydx-delivery pipeline`

    6. `dydx-delivery/skills/execute-tests/references/results-template.md:18`
       FROM: `Stage 6 of the dydx-delivery pipeline`
       TO:   `Stage 8d of the dydx-delivery pipeline`

    `sow-template.md` has no Stage label currently — leave unchanged (do NOT add one; out of D-58 minimum-change scope).

    Do not touch any `Stage N` references in narrative prose elsewhere in the same files unless they refer to THIS template's stage identity (e.g., a "Stage 5 produces ..." sentence inside test-plan-template referring to its own output stays — that's a separate semantic reference). Use the per-line citations above as the binding edit set.
  </action>
  <verify>
    <automated>grep -qF "Stage 2 of the dydx-delivery pipeline" dydx-delivery/skills/discovery-intake/references/intake-template.md &amp;&amp; grep -qF "Stage 4a of the dydx-delivery pipeline" dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md &amp;&amp; grep -qF "Stage 5 of the dydx-delivery pipeline" dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md &amp;&amp; grep -qF "Stage 8b of the dydx-delivery pipeline" dydx-delivery/skills/generate-test-plan/references/test-plan-template.md &amp;&amp; grep -qF "Stage 7a of the dydx-delivery pipeline" dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md &amp;&amp; grep -qF "Stage 8d of the dydx-delivery pipeline" dydx-delivery/skills/execute-tests/references/results-template.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "Stage 2 of the dydx-delivery pipeline" dydx-delivery/skills/discovery-intake/references/intake-template.md` exits 0
    - `! grep -qF "Stage 0 of the dydx-delivery pipeline" dydx-delivery/skills/discovery-intake/references/intake-template.md` exits 0
    - `grep -qF "Stage 4a of the dydx-delivery pipeline" dydx-delivery/skills/generate-functional-spec/references/functional-spec-template.md` exits 0
    - `grep -qF "Stage 5 of the dydx-delivery pipeline" dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md` exits 0
    - `grep -qF "Stage 8b of the dydx-delivery pipeline" dydx-delivery/skills/generate-test-plan/references/test-plan-template.md` exits 0
    - `grep -qF "Stage 7a of the dydx-delivery pipeline" dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md` exits 0
    - `grep -qF "Stage 8d of the dydx-delivery pipeline" dydx-delivery/skills/execute-tests/references/results-template.md` exits 0
    - `! grep -qF "Stage 6 of the dydx-delivery pipeline" dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md` exits 0 (old label gone)
    - `! grep -qF "Stage 6 of the dydx-delivery pipeline" dydx-delivery/skills/execute-tests/references/results-template.md` exits 0
    - Zero-survivor grep across skill prose: `! grep -rn "01_sow_v\|02_functional-spec_v\|03_technical-spec_v\|04_build-prompt_v" dydx-delivery/skills/ --include='*.md'` exits 0 (no old prefixes survive in skill bodies)
    - `! grep -rnE "[^a-z]00_discovery_v" dydx-delivery/skills/ --include='*.md'` exits 0 (no `00_discovery_v` survivors except possibly as old-prefix examples in stage-numbering.md, which is a reference file not skill)
    - For positive coverage: `grep -rcE "02_discovery_v" dydx-delivery/skills/` returns at least 2 (discovery-intake + generate-sow reference the new prefix in prose+based_on)
    - For positive coverage: `grep -rcE "08b_test-plan_v" dydx-delivery/skills/` returns at least 3
  </acceptance_criteria>
  <done>All 6 template Stage-N self-labels updated to new v2 substage labels. All skill prose filename references migrated to new prefixes. Zero old-prefix survivors anywhere in `dydx-delivery/skills/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 2 skill edits → 14 modified files | High-volume mechanical edits — risk of accidental rule deletion or filename mistakes |
| Hard-rules pointer text identity | The pointer wording MUST be byte-identical across 3 files — drift breaks the dedup contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05W2-01 | Tampering | Hard-rules pointer wording drift across the 3 files | mitigate | `grep -lF` for the exact phrase in 3 files must return exactly 3 (acceptance criterion); any character drift fails the gate |
| T-05W2-02 | Information disclosure | Accidental skill-deletion in safety-rules.md collapse | mitigate | D-62 mandates COLLAPSE not delete; acceptance criterion `test -f` confirms file still exists with the Pointer-only stub |
| T-05W2-03 | Repudiation | based_on_* renumber gaps | mitigate | 13 positive grep assertions (one per file/key pair) + 5 zero-survivor negative grep assertions; cannot pass with partial migration |
| T-05W2-04 | Spoofing | Skill prose drift outside intended renumber scope | mitigate | Per-skill grep candidate list in Task 3 action is bounded — executor only edits the named prefixes; surrounding prose untouched |
| T-05W2-05 | Elevation of privilege | Wave 5 cosmetic fixes (B.1/B.3 in dydx-delivery/README.md) accidentally landed in Wave 2 | mitigate | Task 1 explicitly forbids touching `dydx-delivery/README.md` outside the lines ~99-105 hard-rules section; lines ~126 (B.1) and root README B.3 deferred to Wave 5 |
</threat_model>

<verification>
After all 3 Wave 2 tasks:
- D-59 pointer text appears in exactly 3 files (execute-tests/SKILL.md + test-plan-template + dydx-delivery/README.md)
- execute-tests/references/safety-rules.md exists and is collapsed to Pointer-only stub (D-62)
- Zero old-prefix `based_on_*` value survivors
- All 6 template Stage-N labels updated
- All 11 modified files committed
</verification>

<success_criteria>
- 14 files modified across 3 tasks (4 hard-rules + 11 renumber, with overlap on test-plan-template counted once)
- All grep zero-survivor assertions pass
- Skill behaviour unchanged structurally — only content references updated
- Wave 3 (manifest + LICENSE + scaffold) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundations/05-02-SUMMARY.md` recording:
- Per-file line ranges actually edited (vs RESEARCH-cited line numbers — drift OK if content matched)
- Confirmation of D-59 pointer text identity across the 3 files (cite grep output)
- Cross-AI flag 1 resolution: used AUDIT-05.1 ground truth (4 files), NOT D-59 nominees
- Count of based_on_* renumbers applied (target: 13)
</output>
