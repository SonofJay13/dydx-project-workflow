---
phase: 05-foundations
plan: 04
type: execute
wave: 4
depends_on: [05-01]
files_modified:
  - dydx-delivery/references/connector-matrix.md
  - .planning/OPEN-QUESTIONS.md
  - dydx-delivery/references/glossary.md
autonomous: true
requirements: [FOUND-10, FOUND-13]
tags: [foundations, connector-matrix, open-questions-resolution, mcp-probe, rate-limits]

must_haves:
  truths:
    - "Per D-56, connector-matrix.md is DOC-ONLY — no slash command, no agent, no probe hook ships in Phase 5"
    - "Per D-57, all 8 OPEN-Qs (Q06.1 / Q07.1 / Q09 / Q10 / Q11 / Q12 / Q13 / Q25) resolve INLINE in this wave (no separate research phase)"
    - "Per UAT-3.5, Pipefy/Wrike/Ziflow connectors are API-only through v2.6 — connector-matrix.md must NOT carry an 'MCP path' branch for these 3 platforms"
    - "Per UAT-6.1, native-AI ingestion APIs are OUT-OF-SCOPE entirely — connector-matrix.md MUST NOT show 'api' branch for native-AI ingestion paths"
    - "Per RESEARCH §FOUND-10, matrix covers 6 connectors × 11 stages = 66-cell grid PLUS per-stage fallback narrative"
    - "Per D-47 9-field schema (Phase 4 lock) AND C-1 fix (Codex HIGH), the 8 OPEN-Q row flips PRESERVE the row schema (no new columns, no new rows). Status field is updated to `decided`. The EXISTING Resolution / Resolution-path field is populated with the cited resolution text. NO other field is touched. Pre/post row column counts (awk -F'|' '{print NF}') are identical for every touched row."
    - "Per C-4 fix (Codex MEDIUM), Plan 05-01 Task 5 marked the Q09 + Q13 glossary entries `[provisional — ratified by W4 OPEN-Q closure]`. After this wave's OPEN-Q closure, Task 2 below strips the `[provisional — ratified by W4 OPEN-Q closure]` marker from those two glossary entries — the entries become ratified."
    - "Per C-8 fix (Codex LOW), the DESIGN-04 `mcpServers` field deferral (currently undocumented anywhere persistent) is recorded as an OPEN-QUESTIONS row with `Status: deferred-to-future-phase` and a one-line rationale tying it to v2.6 / SURF-* delivery. The deferral row is added by Task 2 alongside the 8 status flips."
  artifacts:
    - path: "dydx-delivery/references/connector-matrix.md"
      provides: "Plugin-level canonical connector + graceful-degradation matrix (FOUND-10)"
      contains: "REQUIRED"
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "8 row Status field flips (open|proposed -> decided) per FOUND-13 + 1 new mcpServers deferral row per C-8 fix"
      contains: "Status: decided"
    - path: "dydx-delivery/references/glossary.md"
      provides: "C-4 post-W4 ratification — strip `[provisional — ratified by W4 OPEN-Q closure]` marker from Q09 + Q13 entries created by Plan 05-01 Task 5"
      contains: "Claude for Chrome"
  key_links:
    - from: "dydx-delivery/references/connector-matrix.md"
      to: ".planning/DESIGN.md:162-178 (DESIGN-07)"
      via: "Cross-ref preamble for connector-availability probe contract"
      pattern: "DESIGN-07"
    - from: "dydx-delivery/references/connector-matrix.md"
      to: "dydx-delivery/references/safety-rules.md (W1 output)"
      via: "Rule 1 sandbox-enforcement narrative — connector probe respects sandbox allowlist"
      pattern: "sandbox"
    - from: ".planning/OPEN-QUESTIONS.md (8 rows)"
      to: "dydx-delivery/references/connector-matrix.md"
      via: "Backtick path:line citation per D-14 in each resolution line"
      pattern: "Status: decided"
---

<objective>
Wave 4 of Phase 5: codify the connector probe + graceful-degradation matrix (FOUND-10) and resolve 8 connector-related OPEN-QUESTIONS inline (FOUND-13). Two deliverables:

1. **`dydx-delivery/references/connector-matrix.md` (FOUND-10).** Doc-only canonical (per D-56 — NO slash command, NO agent, NO probe hook). Documents session-start probe behaviour for each of 6 MCPs/APIs (Coda MCP / Google Workspace MCP / Miro MCP / Pipefy API / Wrike API / Ziflow API) AND per-stage fallback table covering Stages 1–11. Carries inline resolved values from the 8 OPEN-Qs.

2. **`.planning/OPEN-QUESTIONS.md` row Status flips (FOUND-13).** For each of Q06.1 / Q07.1 / Q09 / Q10 / Q11 / Q12 / Q13 / Q25: flip Status from `open` (or `proposed`) → `decided`, append a one-line resolution citing `connector-matrix.md` at the relevant line.

Purpose: Wave 4 closes the v0.3.0 connector-availability ambiguity that blocked Phase 6 platform-skill authoring (PLAT-01..06 need to know whether to plan against an MCP path or an API path per platform). Pre-locks throttle/host/concurrency defaults that PLAT-01/02/03 inherit.

Output: 1 NEW canonical reference file + 8 row-flip edits to OPEN-QUESTIONS.md = 2 files modified.

**Resolution method (D-57 inline):**
- **Web research items** (Q06.1 Pipefy rate-limit / Q07.1 Wrike rate-limit / Q09 Claude in Chrome / Q13 Wrike host / Q25 Wrike+Ziflow auth-concurrency) — resolved values come from RESEARCH.md § "Open Questions Resolution" (already gathered 2026-05-10). Plans land the answers in `connector-matrix.md` cells; OPEN-QUESTIONS.md row-flips cite the matrix.
- **Workspace probe items** (Q10 Coda MCP / Q11 Google Workspace MCP / Q12 Miro MCP) — re-confirm via `claude mcp list` against AUDIT-08 baseline at execution time. If drift detected from AUDIT-08, document the drift in the matrix.

**Cross-AI flag adjudication relevant here:** None directly — Q10/Q11/Q12 probe re-runs use live MCP availability detected during execution (per Q10/Q11/Q12 answers in RESEARCH.md §725-758: Coda + 3 Anthropic-maintained Google MCPs + Miro all WIRED at 2026-05-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_prerequisite>
**Execution prerequisite (C-3 — Codex MEDIUM):** This plan's acceptance-criteria commands assume Bash 4+, `jq`, and `grep -E` semantics (Git Bash, WSL, or equivalent). On Windows PowerShell, run via `bash` or in a Git-Bash terminal. The prerequisite check itself is gated by Task 0 in Plan 05-01 (Wave 1); if Plan 05-01 Task 0 did not pass, Plan 05-04 MUST NOT proceed.
</execution_prerequisite>

<context>
@.planning/phases/05-foundations/05-CONTEXT.md
@.planning/phases/05-foundations/05-RESEARCH.md
@.planning/phases/05-foundations/05-PATTERNS.md
@.planning/DESIGN.md
@.planning/AUDIT.md
@.planning/OPEN-QUESTIONS.md
@dydx-delivery/references/safety-rules.md

<interfaces>
<!-- The 6 connectors required in the matrix (per FOUND-10 + UAT-3.5 lock — NO Pipefy/Wrike/Ziflow MCP branches): -->

| # | Connector | Probe method | Endpoint | Owner phase |
|---|---|---|---|---|
| 1 | Coda MCP | `claude mcp list` + `whoami` | `apis/v1` | Phase 6+ |
| 2 | Google Workspace MCP (Drive / Gmail / Calendar — 3 separate Anthropic-maintained MCP servers) | `claude mcp list` + per-MCP cheap-read | `drivemcp.googleapis.com/mcp/v1` + `gmailmcp.googleapis.com/mcp/v1` + `calendarmcp.googleapis.com/mcp/v1` | Phase 8+ |
| 3 | Miro MCP | `claude mcp list` + `board_search_boards` | `https://mcp.miro.com` | Phase 7+ |
| 4 | Pipefy API (GraphQL) | HTTP GET probe | `https://api.pipefy.com/graphql` (canonical-only per UAT-4.1 / Q24) | Phase 6 PLAT-01 |
| 5 | Wrike API (REST) | OAuth token-exchange dry-run | per-tenant `host` from OAuth response (Q13) | Phase 6 PLAT-02 |
| 6 | Ziflow API (REST) | OAuth token-exchange dry-run | per-tenant | Phase 6 PLAT-03 |

<!-- Required per-stage fallback rows (per DESIGN-07 + RESEARCH §FOUND-10): -->

| Stage | Connector | Required? | Fallback if missing |
|---|---|---|---|
| Stage 1 Kickoff | Miro MCP | GRACEFUL | Paste-from-screenshot |
| Stage 1 Kickoff | Coda MCP | GRACEFUL | Field Notes triage via paste-from-Coda |
| Stage 6 Cost estimate | Coda MCP | GRACEFUL | Manual mode — cost estimate emitted as local .md only; Coda upsert deferred |
| Stage 9 Documentation | Google Drive MCP | REQUIRED | HALT with explicit error |
| Stage 10 Native-AI | Pipefy/Wrike/Ziflow API | N/A (UAT-6.1 — paste-only) | Humans paste manually per UAT-6.1 |
| Stage 11 Sign-off | Coda MCP | GRACEFUL | Local brain only; Coda one-way mirror push skipped; logged for retry |
| Stage 8 Test bot | Pipefy/Wrike/Ziflow API | REQUIRED | HALT — runner cannot execute sandbox tests without platform API |

<!-- The 8 OPEN-Q resolution values (from RESEARCH §Open Questions Resolution): -->

- Q06.1: Pipefy GraphQL rate-limit = 500 req / 30s ≈ 16.67 req/sec per token. Throttle at 13 req/sec (80% buffer). Source: Pipefy Community + Help Center 2026-05-10.
- Q07.1: Wrike REST API rate-limit = 400 req/min per user + 5000 req/min per IP. Throttle at 320 req/min per user (80%). Source: Wrike Help Center 2026-05-10.
- Q09: Canonical product name primary = "Claude for Chrome" (marketing page); also "Claude in Chrome" (Help Center). Both in active use per Anthropic 2026-05-10.
- Q10: Coda MCP WIRED + authenticated; endpoint `apis/v1`; no drift from AUDIT-08 baseline.
- Q11: Google Workspace MCP = 3 separate Anthropic-maintained MCP servers (Drive / Gmail / Calendar) at `*mcp.googleapis.com/mcp/v1`; NOT `taylorwilsdon` or `piotr-agier`.
- Q12: Miro MCP WIRED + authenticated; endpoint `https://mcp.miro.com`; no drift from AUDIT-08 baseline.
- Q13: Wrike host = OAuth token response `host` field (per-tenant); SoT destination = `<Client> Brain/00_HUB.md` Coda block (final destination at Phase 6 PLAT-02 discretion).
- Q25: Wrike + Ziflow auth-concurrency = default `exclusive` (Pipefy precedent — UAT-4.2); LOW confidence — live tenant test deferred to Phase 6.

<!-- D-47 9-field row schema (preserve verbatim — only Status field changes): -->
OPEN-QN / Question / Source citations / Owning phase / Verification owner / Severity / Resolution path / Proposed default / Status

<!-- D-14 citation format (per Phase 4 lock): -->
`path:line` backtick-wrapped (e.g., `dydx-delivery/references/connector-matrix.md:42`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FOUND-10 — author dydx-delivery/references/connector-matrix.md (doc-only, 6 connectors × 11 stages + per-stage fallback + 8 inline OPEN-Q values)</name>
  <files>dydx-delivery/references/connector-matrix.md</files>
  <read_first>
    - `.planning/DESIGN.md:162-178` (DESIGN-07 — connector-availability probe + graceful-degradation matrix contract)
    - `.planning/AUDIT.md` § AUDIT-08 (`.planning/AUDIT.md:543`) (live MCP probe baseline from 2026-05-09)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Connector Matrix Schema (FOUND-10)" (lines ~466-518) — full required structure
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Open Questions Resolution" (lines ~675-790) — all 8 resolved values for inline matrix cells
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-56 (doc-only contract) + spec_lock UAT-3.5 + UAT-6.1 (Pipefy/Wrike/Ziflow API-only; native-AI ingestion APIs OUT-OF-SCOPE)
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: dydx-delivery/references/connector-matrix.md" — required structure (no exact analog; AUDIT-08 narrative cousin)
    - `dydx-delivery/references/safety-rules.md` (Wave 1 output — for cross-ref to Rule 1 sandbox enforcement)
    - `dydx-delivery/references/stage-numbering.md` (Wave 1 output — for the 11-stage list)
  </read_first>
  <action>
    Author `dydx-delivery/references/connector-matrix.md` as a doc-only canonical reference (no executable surface per D-56).

    **Before writing, re-probe live MCP availability:**

    Run `claude mcp list` (or equivalent — in this conversation the MCP instructions block at the top documents claude.ai Coda is wired). For Q10 / Q11 / Q12, confirm:
    - Coda MCP listed and authenticated
    - Google Drive + Gmail + Google Calendar MCPs listed and authenticated (3 separate servers, all Anthropic-maintained per `mcp.googleapis.com` host domain)
    - Miro MCP listed and authenticated

    If any drift from AUDIT-08 (which baselined 2026-05-09T17:05Z showing all 5 wired), document the drift in the matrix Probe-status column for that row. If no drift, record `Re-confirmed wired 2026-05-10 (no drift vs AUDIT-08 baseline)`.

    **Required content (write to `dydx-delivery/references/connector-matrix.md`):**

    1. **H1 + DESIGN-07 cross-ref preamble** (mirror Wave 1 canonical-reference pattern):
       ```markdown
       # dYdX Delivery — Connector availability matrix (canonical)

       > Canonical SoT per DESIGN-07 (`.planning/DESIGN.md:162-178`). Documents session-start probe behaviour for each MCP/API the plugin depends on, plus per-stage graceful-degradation fallback when a connector is missing. **Doc-only per D-56** — no slash command, no agent, no probe hook ships in Phase 5; skills read this matrix at session start. Manual re-run via `claude mcp list` against the matrix.
       >
       > **Scope locks (UAT 2026-05-10):** Pipefy/Wrike/Ziflow are API-only through v2.6 (UAT-3.5) — no MCP path branches for those 3 platforms. Native-AI ingestion APIs are OUT-OF-SCOPE entirely (UAT-6.1) — Stage 10 = paste-only. Pipefy API endpoint canonical-only `api.pipefy.com/graphql` (UAT-4.1 / Q24 verified 2026-05-10).
       ```

    2. **H2 `## Cell value enum`** — define `REQUIRED` / `GRACEFUL` / `N/A` semantics in a 3-row table (Value / Meaning / Halt behaviour).

    3. **H2 `## Connectors`** — 6-row table with columns: `Connector | Probe method | Endpoint / cheap-read | Probe status (2026-05-10) | Owner phase`. Use the 6 connectors from `<interfaces>` block above. Probe status column carries the live re-probe result (no drift OR drift detected — be specific per connector).

    4. **H2 `## Stage × connector grid`** — 11 row × 6 col table. Rows = `Stage 1 Kickoff`, `Stage 2 Discovery`, `Stage 3 SOW`, `Stage 4a fnspec-platform`, `Stage 4b fnspec-integration`, `Stage 5 Tech spec`, `Stage 6 Cost estimate`, `Stage 7a Build prompt (dev)`, `Stage 7b Implementation prompt`, `Stage 8 Test bot`, `Stage 9 Documentation`, `Stage 10 Native-AI`, `Stage 11 Sign-off`. Cells = `REQUIRED | GRACEFUL | N/A`. (Note: this is technically 13 rows for 13 substages — the matrix can collapse Stage 8 / Stage 10 / Stage 11 substages as the planner sees fit, but cover all 11 numeric stages explicitly).

    5. **H2 `## Per-stage fallback behaviour`** — for each non-trivial graceful row, one bullet block describing the fallback. At minimum cover the 7 rows from `<interfaces>` block above (Stage 1 Miro / Stage 1 Coda / Stage 6 Coda / Stage 9 Drive REQUIRED-HALT / Stage 10 native-AI N/A per UAT-6.1 paste-only / Stage 11 Coda / Stage 8 Pipefy+Wrike+Ziflow REQUIRED-HALT).

    6. **H2 `## Resolved OPEN-Q values (inline)`** — 8 entries with the resolved value + source citation:
       - **Q06.1 Pipefy rate-limit:** 500 req / 30s ≈ 16.67 req/sec per token. Throttle at 13 req/sec (80% buffer). Source: Pipefy Community + Help Center 2026-05-10. Re-verify at Phase 6 PLAT-01 kickoff per Q06.2.
       - **Q07.1 Wrike rate-limit:** 400 req/min per user + 5000 req/min per IP. Throttle at 320 req/min per user (80%). Source: Wrike Help Center 2026-05-10. Re-verify at Phase 6 PLAT-02 per Q07.2.
       - **Q09 Claude in Chrome canonical naming:** Primary = "Claude for Chrome" (marketing page); also "Claude in Chrome" (Help Center). Both in active use. Glossary entry in `glossary.md`.
       - **Q10 Coda MCP:** WIRED + authenticated; endpoint `apis/v1`; <re-probe result>.
       - **Q11 Google Workspace MCP:** 3 separate Anthropic-maintained MCP servers (Drive / Gmail / Calendar); endpoints `*mcp.googleapis.com/mcp/v1`; NOT `taylorwilsdon` / `piotr-agier`.
       - **Q12 Miro MCP:** WIRED + authenticated; endpoint `https://mcp.miro.com`; <re-probe result>.
       - **Q13 Wrike host SoT:** OAuth token response `host` field (per-tenant); SoT destination = `<Client> Brain/00_HUB.md` Coda block; final destination at Phase 6 PLAT-02 discretion.
       - **Q25 Wrike + Ziflow auth-concurrency:** Default `exclusive` (Pipefy precedent — UAT-4.2). LOW confidence — live tenant test deferred to Phase 6 PLAT-02/PLAT-03 kickoff.

    7. **H2 `## Session-start probe schema`** — short narrative describing `claude mcp list` transport-status semantics (`✓ Connected` / `! Needs authentication` / `not listed`) + per-MCP cheap-read endpoint table. Note that `connector_probe.yaml` (the session-scoped cache file) schema is **deferred to v2.6 / SURF-01..03** per CONTEXT D-56.

    8. **H2 `## Probe-cache deferral`** — single paragraph noting D-56 doc-only contract and explicit deferral of on-demand probe hooks/slash commands.

    **Threat-model constraint per T-3 (security_threat_model):** When documenting auth headers / OAuth response shapes in §Connectors and §Per-stage fallback, document header NAMES + value SHAPES only (e.g., "Authorization: Bearer <token>"); NEVER include real tokens, API keys, or secrets in the matrix.

    **Threat-model constraint:** The matrix MUST NOT carry any `native_ai_path: api` branch for Pipefy/Wrike/Ziflow (UAT-6.1 lock — Stage 10 paste-only). Acceptance criterion enforces this via zero-survivor grep.
  </action>
  <verify>
    <automated>test -f dydx-delivery/references/connector-matrix.md &amp;&amp; grep -qF "DESIGN-07" dydx-delivery/references/connector-matrix.md &amp;&amp; grep -qF "Coda MCP" dydx-delivery/references/connector-matrix.md &amp;&amp; grep -qF "Pipefy API" dydx-delivery/references/connector-matrix.md &amp;&amp; grep -qF "16.67 req/sec" dydx-delivery/references/connector-matrix.md &amp;&amp; grep -qF "400 req/min per user" dydx-delivery/references/connector-matrix.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dydx-delivery/references/connector-matrix.md` exits 0
    - `grep -qE "^# dYdX Delivery — Connector availability matrix \(canonical\)" dydx-delivery/references/connector-matrix.md` exits 0
    - `grep -qF "DESIGN-07" dydx-delivery/references/connector-matrix.md` exits 0
    - `grep -qF "doc-only" dydx-delivery/references/connector-matrix.md` exits 0 (D-56 contract)
    - `grep -qF "UAT-3.5" dydx-delivery/references/connector-matrix.md` exits 0 (API-first lock)
    - `grep -qF "UAT-6.1" dydx-delivery/references/connector-matrix.md` exits 0 (paste-only lock)
    - `grep -qF "api.pipefy.com/graphql" dydx-delivery/references/connector-matrix.md` exits 0 (Q24 canonical endpoint)
    - All 6 connectors present: `for c in "Coda MCP" "Google Workspace MCP" "Miro MCP" "Pipefy API" "Wrike API" "Ziflow API"; do grep -qF "$c" dydx-delivery/references/connector-matrix.md || exit 1; done` exits 0
    - All 11 stages addressed: `grep -cE "Stage (1 Kickoff|2 Discovery|3 SOW|4a|4b|5 Tech|6 Cost|7a|7b|8 Test|9 Documentation|10 Native-AI|11 Sign-off)" dydx-delivery/references/connector-matrix.md` returns at least 11
    - Cell-value enum present: `grep -qF "REQUIRED" dydx-delivery/references/connector-matrix.md` AND `grep -qF "GRACEFUL" dydx-delivery/references/connector-matrix.md` AND `grep -qF "N/A" dydx-delivery/references/connector-matrix.md`
    - Per-stage fallback narrative present: `grep -cE "HALT|paste|manual mode|degrade" dydx-delivery/references/connector-matrix.md` returns at least 5
    - Q06.1 resolved value: `grep -qF "16.67 req/sec" dydx-delivery/references/connector-matrix.md` AND `grep -qF "13 req/sec" dydx-delivery/references/connector-matrix.md`
    - Q07.1 resolved value: `grep -qF "400 req/min per user" dydx-delivery/references/connector-matrix.md` AND `grep -qF "320 req/min" dydx-delivery/references/connector-matrix.md`
    - Q09 resolved value: `grep -qF "Claude for Chrome" dydx-delivery/references/connector-matrix.md`
    - Q10 resolved value: `grep -qF "Coda MCP" dydx-delivery/references/connector-matrix.md` AND `grep -qF "apis/v1" dydx-delivery/references/connector-matrix.md`
    - Q11 resolved value: `grep -qF "Anthropic-maintained" dydx-delivery/references/connector-matrix.md` AND `grep -qF "mcp.googleapis.com" dydx-delivery/references/connector-matrix.md`
    - Q12 resolved value: `grep -qF "Miro MCP" dydx-delivery/references/connector-matrix.md` AND `grep -qF "mcp.miro.com" dydx-delivery/references/connector-matrix.md`
    - Q13 resolved value: `grep -qF "OAuth token response" dydx-delivery/references/connector-matrix.md` AND `grep -qF "host" dydx-delivery/references/connector-matrix.md`
    - Q25 resolved value: `grep -qF "exclusive" dydx-delivery/references/connector-matrix.md` AND `grep -qF "live tenant test" dydx-delivery/references/connector-matrix.md`
    - Probe-cache deferral documented: `grep -qF "v2.6" dydx-delivery/references/connector-matrix.md` OR `grep -qF "SURF-01" dydx-delivery/references/connector-matrix.md`
    - **Threat-model constraint (T-3):** no real tokens / API keys in the matrix: `! grep -rE "Bearer [A-Za-z0-9._-]{20,}" dydx-delivery/references/connector-matrix.md` exits 0; `! grep -qE "sk-[A-Za-z0-9]{20,}" dydx-delivery/references/connector-matrix.md` exits 0
    - **UAT-6.1 lock (T-3 / scope creep):** `! grep -qF "native_ai_path: api" dydx-delivery/references/connector-matrix.md` exits 0 (no api branch for native-AI per UAT-6.1)
    - **UAT-3.5 lock:** `! grep -qF "Pipefy MCP" dydx-delivery/references/connector-matrix.md` exits 0 (no MCP path for Pipefy through v2.6)
  </acceptance_criteria>
  <done>connector-matrix.md exists with DESIGN-07 cross-ref preamble, 6-connector table with re-probe results, 11-stage × 6-connector grid, per-stage fallback narrative, all 8 OPEN-Q resolved values inline with citations, session-start probe schema, and probe-cache deferral note. No secrets leaked. No UAT-locked branches survive (api native-AI / Pipefy MCP).</done>
</task>

<task type="auto">
  <name>Task 2: FOUND-13 — flip 8 OPEN-Q row Status fields (open|proposed → decided) with citations into connector-matrix.md</name>
  <files>.planning/OPEN-QUESTIONS.md, dydx-delivery/references/glossary.md</files>
  <read_first>
    - `.planning/OPEN-QUESTIONS.md` (current state — locate the 8 rows for Q06.1 / Q07.1 / Q09 / Q10 / Q11 / Q12 / Q13 / Q25 and confirm current Status field values)
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "MODIFIED: .planning/OPEN-QUESTIONS.md (8 row Status flips)" — per-row edit pattern + citation format
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-57 (inline resolution method); spec_lock OPEN-Q schema (D-47 9-field, locked)
    - `.planning/phases/04-open-questions/04-CONTEXT.md` D-47 (the 9-field schema not to break); D-14 (backtick `path:line` citation format)
    - `dydx-delivery/references/connector-matrix.md` (Task 1 output — the citation target with actual line numbers known after Task 1 completes)
  </read_first>
  <action>
    Edit `.planning/OPEN-QUESTIONS.md` to flip the Status field + populate the Resolution field for 8 specific rows. After the 8 flips, perform 2 follow-up edits in the same task: C-4 (strip provisional markers from glossary.md Q09/Q13 entries) and C-8 (append a new OPEN-QUESTIONS row recording the DESIGN-04 `mcpServers` deferral).

    **C-1 FIX — OPEN-Q row-edit semantics (Codex HIGH):** The earlier "change ONLY the Status field" wording was contradictory because the plan also appends resolution text. The correct rule is:

    > **Preserve the row schema. Update the `Status` field to `decided`. Populate the existing `Resolution` (or `Resolution path`) field with the cited resolution text. Do NOT add new columns or rows; do NOT touch any field other than `Status` + `Resolution` / `Resolution path`.**

    Concretely, the D-47 9-field schema is: `OPEN-QN | Question | Source citations | Owning phase | Verification owner | Severity | Resolution path | Proposed default | Status`. Two fields change per row (`Status` set to `decided`; `Resolution path` populated with the resolution text). Seven fields are preserved verbatim (`OPEN-QN`, `Question`, `Source citations`, `Owning phase`, `Verification owner`, `Severity`, `Proposed default`). Column count is unchanged.

    **Row-schema sanity gate (C-1 acceptance criterion driver):** Before any edit, capture the column count for each touched row using `awk -F"|" "{print NF}"` on the row line. After the edit, the column count for each touched row MUST be IDENTICAL to the pre-edit value. Also compare table-header lines (the first 3 lines containing `|` in OPEN-QUESTIONS.md) pre/post — they MUST be byte-identical (`head -3` diff is empty)

    **Citation format (per D-14):** backtick-wrapped `path:line` with the actual line numbers from `dydx-delivery/references/connector-matrix.md` (Task 1 output). Locate each resolved value's line in the matrix and use that line number.

    **Line-resolution method (W-05 fix — REQUIRED for every Q-id):**

    Line numbers in the matrix may drift between Task 1's commit and Task 2's read (e.g., trailing newline added by editor, CRLF normalisation). Do NOT reuse line numbers cached during Task 1 authoring. For EACH Q-id below, immediately before substituting the line number into the resolution text, run a grep against the freshly-committed matrix to obtain the live line number:

    ```bash
    grep -n '<unique-value-anchor>' dydx-delivery/references/connector-matrix.md | head -1 | cut -d: -f1
    ```

    Use a value-unique anchor (not a generic word) so the grep returns exactly one line. If the grep returns >1 line, narrow the anchor.

    **Worked example — Q06.1 (Pipefy rate-limit):**
    ```bash
    grep -n '500 req/30s' dydx-delivery/references/connector-matrix.md | head -1 | cut -d: -f1
    # → e.g., outputs `74`
    ```
    Then cite `\`dydx-delivery/references/connector-matrix.md:74\`` in the Q06.1 resolution.

    **Anchors per Q-id (use these literal strings; refine if needed for uniqueness in the actual committed file):**
    - Q06.1: anchor = `500 req/30s` (or `16.67 req/sec` if the former is non-unique)
    - Q07.1: anchor = `400 req/min per user`
    - Q09:  anchor = `Claude for Chrome` (only the §Resolved OPEN-Q values entry, not the §Connectors row — narrow with `Q09 Claude in Chrome canonical` if needed)
    - Q10:  anchor = `Q10 Coda MCP`
    - Q11:  anchor = `Q11 Google Workspace MCP`
    - Q12:  anchor = `Q12 Miro MCP`
    - Q13:  anchor = `Q13 Wrike host SoT`
    - Q25:  anchor = `Q25 Wrike + Ziflow auth-concurrency`

    Run the grep against the freshly-committed `dydx-delivery/references/connector-matrix.md` (not a cached copy). Only after obtaining each live line number do you edit the OPEN-QUESTIONS.md row.

    **Per-row edits:**

    1. **Q06.1 Pipefy 2026 rate-limit:**
       - Find row whose key begins `OPEN-Q06.1` (or `**OPEN-Q06.1**`)
       - Change Status field value FROM current (`open` or `proposed`) TO `decided`
       - Resolution / Resolution path field populated with: `Resolution: 500 req / 30s ≈ 16.67 req/sec per token; throttle 13 req/sec (80% buffer). Re-verify at Phase 6 PLAT-01 per Q06.2. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    2. **Q07.1 Wrike 2026 rate-limit:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: 400 req/min per user + 5000 req/min per IP; throttle 320 req/min per user (80%). Re-verify at Phase 6 PLAT-02 per Q07.2. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    3. **Q09 Claude in Chrome canonical naming:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: Primary "Claude for Chrome" (marketing page); also "Claude in Chrome" (Help Center) — interchangeable. Glossary entry in \`dydx-delivery/references/glossary.md\`. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    4. **Q10 Coda MCP wired + version pin:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: Re-confirmed wired + authenticated 2026-05-10 (no drift vs AUDIT-08 baseline 2026-05-09T17:05Z); endpoint \`apis/v1\`. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    5. **Q11 Google Workspace MCP server choice:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: 3 separate Anthropic-maintained MCP servers (Drive / Gmail / Calendar) at \`*mcp.googleapis.com/mcp/v1\`; NOT \`taylorwilsdon\` / \`piotr-agier\`. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    6. **Q12 Miro MCP wired vs paste-only:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: Re-confirmed WIRED + authenticated 2026-05-10 (no drift vs AUDIT-08 baseline); endpoint \`https://mcp.miro.com\`. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    7. **Q13 Wrike host field source-of-truth:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: OAuth token response \`host\` field is per-tenant SoT; persisted at \`<Client> Brain/00_HUB.md\` Coda block; final destination at Phase 6 PLAT-02 discretion. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    8. **Q25 Wrike + Ziflow auth-concurrency:**
       - Status field → `decided`
       - Resolution / Resolution path field populated with: `Resolution: Default \`exclusive\` per Pipefy precedent (UAT-4.2); LOW confidence — live tenant test deferred to Phase 6 PLAT-02/PLAT-03 kickoff. Cited at \`dydx-delivery/references/connector-matrix.md:<line>\`.`

    **Preserved verbatim per row (C-1 fix):** OPEN-QN / Question / Source citations / Owning phase / Verification owner / Severity / Proposed default — these 7 fields are NOT touched. Only the `Status` field changes (`open|proposed -> decided`) AND the existing `Resolution path` field is populated with the resolution text + citation. Column count is preserved (no new columns added; pre/post `awk -F"|" "{print NF}"` per row is identical). Table-header lines (rows 1-3 containing `|`) are pre/post byte-identical.

    Do NOT touch the OTHER rows in `.planning/OPEN-QUESTIONS.md` (rows for Q01 / Q02 / Q03 / Q04 / Q05 / Q06.2 / Q07.2 / Q08 / Q14 / Q15 / Q16 / Q17 / Q18 / Q19 / Q20 / Q21 / Q21.1 / Q22 / Q23 / Q24 etc.). Wave 4 writes exactly 8 named rows in this step, then adds 1 new row in the C-8 follow-up (below); all other existing rows retain their existing Status values.

    ---

    **C-4 FOLLOW-UP — strip `[provisional — ratified by W4 OPEN-Q closure]` markers from glossary.md (Codex MEDIUM).**

    After the 8 status flips land, edit `dydx-delivery/references/glossary.md` to remove the `[provisional — ratified by W4 OPEN-Q closure]` marker from the Q09 (`Claude for Chrome`) and Q13 (`Wrike host field`) entries. Plan 05-01 Task 5 introduced these markers as a soft wave-boundary annotation; Wave 4 OPEN-Q closure now ratifies them, so the markers come off. Concretely:

    - Locate the Q09 entry line: it currently contains `Claude for Chrome` followed by `[provisional — ratified by W4 OPEN-Q closure]`. Remove the bracketed marker (along with any preceding space) so the entry reads cleanly without it.
    - Locate the Q13 entry line: same treatment for `Wrike host field [provisional — ratified by W4 OPEN-Q closure]`.

    Do NOT touch any other line in glossary.md. Do NOT remove the term definitions themselves — only the bracketed `[provisional — ratified by W4 OPEN-Q closure]` marker.

    ---

    **C-8 FOLLOW-UP — record DESIGN-04 `mcpServers` deferral in OPEN-QUESTIONS.md (Codex LOW).**

    Plan 05-03 Task 1 asserts `jq -e \'has("mcpServers")\' plugin.json` returns `false` (deferred per cross-AI flag 5). The deferral itself is not recorded anywhere persistent. Per C-8, append a NEW row to `.planning/OPEN-QUESTIONS.md` (preserve D-47 9-field schema; column count matches existing rows). Use the next sequential OPEN-Q identifier (look up the highest existing `OPEN-Q<N>` row and use N+1; if a non-numeric scheme exists, follow that convention).

    Row content (fill column values, preserving table column ordering):
    - **OPEN-QN:** next sequential ID (e.g., `OPEN-Q26` if Q25 is currently the last)
    - **Question:** `DESIGN-04 plugin.json mcpServers wiring — when does the field land?`
    - **Source citations:** `.planning/DESIGN.md` DESIGN-04; `.planning/phases/05-foundations/05-03-PLAN.md` cross-AI flag 5
    - **Owning phase:** `v2.6 (SURF-*) or earlier — first MCP-pinning skill`
    - **Verification owner:** `executor of the v2.6 SURF-* plan that first wires an MCP`
    - **Severity:** `LOW`
    - **Resolution path:** `Add mcpServers field to dydx-delivery/.claude-plugin/plugin.json when the first skill needs an MCP pinned. Phase 5 FOUND-07 scope is narrow (2.0.0 sync only) so mcpServers does NOT ship in this milestone.`
    - **Proposed default:** `defer — no field today`
    - **Status:** `deferred-to-future-phase`

    Column count must match existing rows exactly (use `awk -F"|" "{print NF}"` on the new row vs an arbitrary existing row — equal counts).
  </action>
  <verify>
    <automated>for q in Q06.1 Q07.1 Q09 Q10 Q11 Q12 Q13 Q25; do grep -A20 "OPEN-${q}" .planning/OPEN-QUESTIONS.md | grep -q "Status: decided" || { echo "FAIL: $q not decided"; exit 1; }; done &amp;&amp; ! grep -qF "[provisional — ratified by W4 OPEN-Q closure]" dydx-delivery/references/glossary.md &amp;&amp; grep -qF "Status: deferred-to-future-phase" .planning/OPEN-QUESTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - For each Q in {Q06.1, Q07.1, Q09, Q10, Q11, Q12, Q13, Q25}: the row's Status field carries `decided`. Concretely: `for q in Q06.1 Q07.1 Q09 Q10 Q11 Q12 Q13 Q25; do grep -A20 "OPEN-${q}" .planning/OPEN-QUESTIONS.md | grep -qF "Status: decided" || exit 1; done` exits 0
    - Each resolved row cites connector-matrix.md: `grep -c "dydx-delivery/references/connector-matrix.md" .planning/OPEN-QUESTIONS.md` returns at least 8
    - Q06.1 resolution carries throttle target: `grep -A5 "OPEN-Q06.1" .planning/OPEN-QUESTIONS.md | grep -qF "13 req/sec"`
    - Q07.1 resolution carries throttle target: `grep -A5 "OPEN-Q07.1" .planning/OPEN-QUESTIONS.md | grep -qF "320 req/min"`
    - Q09 resolution carries primary name: `grep -A5 "OPEN-Q09" .planning/OPEN-QUESTIONS.md | grep -qF "Claude for Chrome"`
    - Q10 resolution carries endpoint: `grep -A5 "OPEN-Q10" .planning/OPEN-QUESTIONS.md | grep -qF "apis/v1"`
    - Q11 resolution carries `Anthropic-maintained`: `grep -A5 "OPEN-Q11" .planning/OPEN-QUESTIONS.md | grep -qF "Anthropic-maintained"`
    - Q12 resolution carries endpoint: `grep -A5 "OPEN-Q12" .planning/OPEN-QUESTIONS.md | grep -qF "mcp.miro.com"`
    - Q13 resolution carries OAuth host: `grep -A5 "OPEN-Q13" .planning/OPEN-QUESTIONS.md | grep -qF "OAuth token response"`
    - Q25 resolution carries `exclusive`: `grep -A5 "OPEN-Q25" .planning/OPEN-QUESTIONS.md | grep -qF "exclusive"`
    - **W-05 line-resolution sanity check:** every cited `connector-matrix.md:<line>` resolves to a real line in the freshly-committed matrix. Concretely: `for n in $(grep -oE 'connector-matrix\.md:[0-9]+' .planning/OPEN-QUESTIONS.md | cut -d: -f2 | sort -u); do total=$(wc -l < dydx-delivery/references/connector-matrix.md); [ "$n" -le "$total" ] || exit 1; done` exits 0 (no orphan citations beyond EOF)
    - Other rows untouched (sample assertion): `grep -A20 "OPEN-Q01" .planning/OPEN-QUESTIONS.md | head -25 | grep -qF "Status:"` exits 0 AND the value MAY remain unchanged (Q01 was BLOCKER / withdrawn per UAT-6.1; not touched in Wave 4)
    - D-47 9-field schema preserved per touched row: each of the 8 rows still contains its `Question`, `Owning phase`, `Verification owner`, `Severity`, `Resolution path`, `Proposed default` field labels (sample assertion): `for q in Q06.1 Q07.1 Q09 Q10 Q11 Q12 Q13 Q25; do grep -A20 "OPEN-${q}" .planning/OPEN-QUESTIONS.md | grep -qF "Owning phase" || exit 1; done`
    - **C-1 row-schema preservation (Codex HIGH):** Pre/post column count is identical for every touched row. Concretely, capture a pre-edit snapshot of column counts for the 8 touched rows BEFORE editing: `for q in Q06.1 Q07.1 Q09 Q10 Q11 Q12 Q13 Q25; do grep -E "OPEN-${q}\b" .planning/OPEN-QUESTIONS.md | head -1 | awk -F"|" "{print NF}" >> /tmp/pre-cols.txt; done`. Post-edit, the same command writes to /tmp/post-cols.txt. Assertion: `diff /tmp/pre-cols.txt /tmp/post-cols.txt` exits 0.
    - **C-1 table-header invariance (Codex HIGH):** The first three lines of `OPEN-QUESTIONS.md` containing the pipe character `|` (i.e., the table header rows) are pre/post byte-identical. Concretely, capture pre BEFORE edit: `grep -n "|" .planning/OPEN-QUESTIONS.md | head -3 > /tmp/pre-header.txt`. Post: same command writes to /tmp/post-header.txt. Assertion: `diff /tmp/pre-header.txt /tmp/post-header.txt` exits 0.
    - **C-1 only-2-fields-touched (Codex HIGH):** No new columns added across the file. Concretely, capture pre-edit distinct column-count signature: `awk -F"|" "{print NF}" .planning/OPEN-QUESTIONS.md | sort -u > /tmp/pre-cols-signature.txt`. Post-edit, same command writes to /tmp/post-cols-signature.txt. Assertion: `diff /tmp/pre-cols-signature.txt /tmp/post-cols-signature.txt` exits 0 (the set of distinct column counts is unchanged — e.g., still {1, 11} or whatever pre-edit baseline was; the new C-8 row uses the same column count as existing rows).
    - **C-4 provisional markers stripped from glossary.md (Codex MEDIUM):** `! grep -qF "[provisional — ratified by W4 OPEN-Q closure]" dydx-delivery/references/glossary.md` exits 0 (zero markers remain after this task). The two terms still exist: `grep -qF "Claude for Chrome" dydx-delivery/references/glossary.md` exits 0 AND `grep -qF "Wrike host field" dydx-delivery/references/glossary.md` exits 0 (definitions preserved; only the bracket marker removed).
    - **C-8 mcpServers deferral row recorded (Codex LOW):** A new OPEN-Q row exists in `.planning/OPEN-QUESTIONS.md` referencing DESIGN-04 `mcpServers` with `Status: deferred-to-future-phase`. Concretely: `grep -qF "mcpServers" .planning/OPEN-QUESTIONS.md` exits 0 AND `grep -qF "Status: deferred-to-future-phase" .planning/OPEN-QUESTIONS.md` exits 0 AND `grep -qF "DESIGN-04" .planning/OPEN-QUESTIONS.md` exits 0 AND the row mentions the v2.6 / SURF rationale (`grep -qE "v2\.6|SURF-" .planning/OPEN-QUESTIONS.md`).
  </acceptance_criteria>
  <done>8 rows flipped to `Status: decided` with backtick `dydx-delivery/references/connector-matrix.md:<line>` citations populated in the existing `Resolution path` field (C-1 fix — row schema preserved, no new columns, no schema corruption). Line numbers obtained via grep against freshly-committed matrix (W-05 line-resolution method) — no orphan citations beyond EOF. D-47 9-field row schema preserved (column count + table-header bytes identical pre/post). C-4 follow-up: glossary.md Q09 + Q13 entries have `[provisional — ratified by W4 OPEN-Q closure]` markers stripped (entries now ratified). C-8 follow-up: new OPEN-Q row records DESIGN-04 `mcpServers` deferral with `Status: deferred-to-future-phase` and v2.6 / SURF-* rationale. No other existing OPEN-Q rows touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 4 author → connector-matrix.md content | Captures API endpoints, auth header SHAPES, rate-limit values from external research; any token leakage here propagates downstream |
| Wave 4 author → OPEN-QUESTIONS.md row mutations | 8 status flips must preserve D-47 9-field schema; over-edits could corrupt the Phase 4-locked register |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05W4-01 | Information disclosure | Real API tokens / credentials leaked into `connector-matrix.md` | mitigate | Task 1 acceptance criteria include explicit negative grep against `Bearer <20+chars>` and `sk-<20+chars>` token shapes; matrix documents header NAMES + shapes only (T-3 from security_threat_model) |
| T-05W4-02 | Tampering | UAT-6.1 lock breach — native_ai_path: api branch slips into matrix | mitigate | Task 1 acceptance criterion `! grep -qF "native_ai_path: api"` enforces UAT-6.1 |
| T-05W4-03 | Tampering | UAT-3.5 lock breach — Pipefy MCP branch slips into matrix | mitigate | Task 1 acceptance criterion `! grep -qF "Pipefy MCP"` enforces UAT-3.5 |
| T-05W4-04 | Repudiation | OPEN-QUESTIONS.md row Status flip without citation | mitigate | Task 2 acceptance criteria require backtick `dydx-delivery/references/connector-matrix.md:<line>` cite per row + resolution text — failing the gate catches drive-by status changes |
| T-05W4-05 | Spoofing | D-47 9-field schema corruption via over-edit | mitigate | Task 2 acceptance criteria assert `Owning phase` / `Severity` / `Resolution path` field labels survive on each touched row |
| T-05W4-06 | Elevation of privilege | Other OPEN-Q rows accidentally flipped (e.g., Q01/Q02/Q03 native-AI BLOCKERs reactivated) | mitigate | Task 2 action scopes mutation to exactly 8 named Qs; sample assertion verifies a Q01 row still carries its original Status value |
| T-05W4-07 | Repudiation | Stale line-number citations in OPEN-QUESTIONS.md (drift between Task 1 commit and Task 2 read) | mitigate | Task 2 action mandates per-Q grep against freshly-committed matrix to resolve live line numbers (W-05 fix); acceptance criterion verifies every cited line ≤ matrix EOF (no orphan cites) |
</threat_model>

<verification>
- `dydx-delivery/references/connector-matrix.md` exists with 6 connectors + 11 stages + 7 fallback rows + 8 inline OPEN-Q resolutions
- No real tokens / API keys in matrix
- No UAT-6.1 `native_ai_path: api` branch
- No UAT-3.5 `Pipefy MCP` branch
- 8 OPEN-Q rows flipped to `Status: decided` with `path:line` citations
- All cited line numbers ≤ matrix EOF (W-05 line-resolution sanity check)
- D-47 9-field schema preserved on all 8 touched rows
- Other ~17 OPEN-Q rows untouched
</verification>

<success_criteria>
- 2 files modified (1 new + 1 edited)
- Wave 4 unblocks Phase 6 platform skills (PLAT-01..06 read connector-matrix.md for endpoint / throttle / host defaults)
- Wave 4 unblocks Phase 6 PLAT-06 OPEN-Q resolution (Q05 Ziflow read-after-create / Q06.2 Pipefy throttle calibration / Q07.2 Wrike throttle calibration depend on Q06.1 + Q07.1 resolved here)
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundations/05-04-SUMMARY.md` recording:
- Live MCP probe re-confirmation result for Coda / GWorkspace (×3) / Miro (no drift OR drift detected per connector)
- Number of `connector-matrix.md` cells filled
- 8 OPEN-Q rows flipped (one-line per Q) with the live line numbers resolved
- Threat-model gate results (no secrets, no UAT-violation branches)
- D-N counter unchanged at D-62 (no new decisions made in Wave 4 execution)
</output>
