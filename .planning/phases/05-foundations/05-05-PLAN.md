---
phase: 05-foundations
plan: 05
type: execute
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - dydx-delivery/README.md
  - README.md
  - .planning/phases/05-foundations/scripts/phase5-structure-check.sh
  - .planning/phases/05-foundations/05-VALIDATION.md
autonomous: false
requirements: [FOUND-11]
tags: [foundations, cosmetic-fixes, audit-07, validation-script, b1-truncation, b3-step-count]

must_haves:
  truths:
    - "Per FOUND-11 + AUDIT-07 post-UAT-3.1 reclassification, exactly 4 active cosmetic fixes ship in W5: B.1 (README truncation) / B.2 ('test sheet' wording) / B.3 (pipeline-step count) / B.6 (homepage — already landed in W3, documented here)"
    - "B.4 LICENSE is cross-referenced (covered by Wave 3 / FOUND-08); B.5 owner-email is INTENTIONAL per UAT-3.1 — NO FIX, NO TOUCH"
    - "Per ROADMAP SC-4 + REQUIREMENTS.md FOUND-11 verbatim 'B.3 pipeline-step count corrected to 13', the W-04-fix default for B.3 is Option (ii) v2 end-state (13 stages); Option (i) requires explicit user override that updates ROADMAP/REQUIREMENTS in the same commit"
    - "Per VALIDATION.md Wave 0 Requirements (footnote per W-02 fix), `scripts/phase5-structure-check.sh` is authored in this wave (W5) — Wave 0 deferred from W1-W4 which used inline grep/jq/test commands; per-task inline assertions cover Nyquist sampling continuity"
    - "Per W-01 fix, the canonical script path is `.planning/phases/05-foundations/scripts/phase5-structure-check.sh` (mirrors Phase 4 analog convention); VALIDATION.md is updated to point at that path + `wave_0_complete: true` set in same task"
    - "Per task acceptance criteria pattern from Phase 4, the structure-check script mirrors `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` structure"
  artifacts:
    - path: "dydx-delivery/README.md"
      provides: "Plugin README with B.1 truncation closure + B.3 step-count fix"
      contains: "results-YYYY-MM-DD"
    - path: "README.md"
      provides: "Root README with B.2 'test plan' wording + B.3 pipeline-step count"
      contains: "test plan"
    - path: ".planning/phases/05-foundations/scripts/phase5-structure-check.sh"
      provides: "Wave 0 validation script (FOUND-11 + ratifies all Wave 1-4 work)"
      contains: "#!/usr/bin/env bash"
    - path: ".planning/phases/05-foundations/05-VALIDATION.md"
      provides: "Validation strategy doc with W-01/W-02 fixes — script path corrected + Wave 0 deferral footnote + wave_0_complete flipped to true"
      contains: "wave_0_complete: true"
  key_links:
    - from: "scripts/phase5-structure-check.sh"
      to: "All Phase 5 deliverables (4 canonical refs + repointed skills + manifests + LICENSE + scaffold dirs + connector matrix + OPEN-Q row flips + cosmetic fixes)"
      via: "~40 grep / jq / file-exists / diff assertions A1..A40"
      pattern: "fail|grep|jq|test"
    - from: "dydx-delivery/README.md (B.1 closure)"
      to: "dydx-delivery/skills/execute-tests/references/safety-rules.md (cite-anchor stub from W2)"
      via: "Optional cross-ref text in changelog completion sentence (per RESEARCH.md suggested completion)"
      pattern: "safety-rules"
    - from: ".planning/phases/05-foundations/05-VALIDATION.md"
      to: ".planning/phases/05-foundations/scripts/phase5-structure-check.sh"
      via: "Path reference + Wave 0 deferral footnote (W-01 + W-02 fixes); flipped to wave_0_complete: true once script lands"
      pattern: "scripts/phase5-structure-check.sh"
---

<objective>
Wave 5 of Phase 5: close out the 4 active AUDIT-07 cosmetic CONCERNS (B.1 / B.2 / B.3 — B.4 already covered by Wave 3 FOUND-08 LICENSE creation; B.5 owner-email INTENTIONAL per UAT-3.1; B.6 already landed in Wave 3 inline with marketplace.json version sync per cross-AI flag 2). Plus author the Wave 0 validation script `scripts/phase5-structure-check.sh` that ratifies ALL Wave 1-4 work via ~40 grep / jq / file-exists / diff assertions.

Two deliverables:
1. **3 active cosmetic fixes (FOUND-11):**
   - **B.1** `dydx-delivery/README.md:~126` changelog mid-word truncation — complete the sentence using the RESEARCH.md suggested completion text (or a user-adjudicated alternative).
   - **B.2** `README.md:9` (root) — replace `test sheet` with `test plan` (single literal substitution).
   - **B.3** `README.md:9` (root) — fix pipeline-step count per W-04 fix: default Option (ii) v2 end-state (13 stages) per ROADMAP SC-4 + REQUIREMENTS.md FOUND-11 verbatim text. Override path to Option (i) requires explicit user instruction + same-commit update of ROADMAP/REQUIREMENTS.
2. **`scripts/phase5-structure-check.sh` (Wave 0 retrospective)** — single bash script with ~40 assertions covering all 13 FOUND-* requirements. Script exits 0 only when every Wave 1-5 deliverable passes. Mirrors `openquestions-structure-check.sh` structure. **Same task also updates VALIDATION.md (W-01 + W-02 fixes)** — corrects the script-path reference, adds the Wave 0 deferral footnote, and flips `wave_0_complete: false → true`.

Output: 2 README files modified + 1 NEW script file + 1 updated VALIDATION.md = 4 files modified.

**Note on autonomy:** This plan has `autonomous: false` because **B.1 completion text** and **B.3 step-count phrasing** both require user adjudication per RESEARCH.md flags. The plan includes a `checkpoint:decision` task before applying the literal text edits. The validation script + VALIDATION.md update task is fully autonomous.

**Cross-AI flag 3 adjudication (B.3 step count) — REVISED per W-04 fix:** AUDIT-07 says "correct to 13" (v2 end-state). ROADMAP Phase 5 SC-4 + REQUIREMENTS.md FOUND-11 carry the verbatim language "B.3 pipeline-step count corrected to 13". This plan now defaults to Option (ii) v2 end-state (13 stages) so the default aligns with ROADMAP/REQUIREMENTS verbatim text. The checkpoint still allows the override path (Option (i) v2.1 reality, 7 skills / 6-step pipeline), but the override path requires the user to update ROADMAP/REQUIREMENTS in the same commit so the source-of-truth artefacts remain consistent with the README content.

**B.4 + B.5 + B.6 dispositions:**
- **B.4 (LICENSE created):** covered by Wave 3 / FOUND-08 — this plan documents closure only (no edit).
- **B.5 (owner-email):** INTENTIONAL per UAT-3.1 — NO FIX. The structure-check script in Task 4 asserts `jq -r .author.email plugin.json == "jasonmichaelb@gmail.com"` (i.e., NO change) to catch any unauthorised mutation.
- **B.6 (homepage):** completed by Wave 3 marketplace.json edit per cross-AI flag 2 — this plan documents closure only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-foundations/05-CONTEXT.md
@.planning/phases/05-foundations/05-RESEARCH.md
@.planning/phases/05-foundations/05-PATTERNS.md
@.planning/phases/05-foundations/05-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/AUDIT.md
@.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
@dydx-delivery/README.md
@README.md

<interfaces>
<!-- B.1 — dydx-delivery/README.md changelog truncation. Current line 126 ends mid-word "now c". -->
<!-- RESEARCH.md suggested completion (subject to checkpoint adjudication): -->
Suggested B.1 completion text (replaces "...execute-tests`) now c" terminator):
    "...The bot-run terminal stage (`execute-tests`) now carries explicit sandbox-enforcement rules in `references/safety-rules.md`; results are written to versioned `results-YYYY-MM-DD_vN.md` files."

<!-- B.2 — root README.md line 9 literal substitution. -->
Current line 9 (root README):
    Stage-gated client delivery pipeline (discovery → SOW → functional spec → technical spec → **test sheet** → execution)
Target:
    Stage-gated client delivery pipeline (discovery → SOW → functional spec → technical spec → **test plan** → execution)

<!-- B.3 — root README.md line 9 pipeline-step count. -->
<!-- W-04 fix: default to Option (ii) v2 end-state (13 stages) per ROADMAP SC-4 + REQUIREMENTS.md FOUND-11 verbatim. -->
ROADMAP Phase 5 SC-4 verbatim text:        "B.3 pipeline-step count corrected to 13 in README.md:9"
REQUIREMENTS.md FOUND-11 verbatim text:    "B.3 pipeline-step count corrected to 13"
Default action (Option ii):
    Restate root README:9 to read (or contain) "13 stages" / "13-stage pipeline" — aligned with DESIGN-12 v2 end-state inventory.
Override (Option i, requires user instruction + same-commit ROADMAP/REQUIREMENTS update):
    Keep the 6-destination narrative (or "6-step pipeline") matching v2.1 reality.

<!-- Structure-check script pattern source -->
.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh (192 lines, 14 assertions A1..A14; Phase 5 script targets ~40 assertions A1..A40)

<!-- VALIDATION.md current path mismatch (W-01) -->
VALIDATION.md currently says: scripts/validation/phase5-structure-check.sh
This plan creates the script at:  .planning/phases/05-foundations/scripts/phase5-structure-check.sh  (mirrors Phase 4 analog)
W-01 fix: update VALIDATION.md to point at the new path inside Task 4.

<!-- VALIDATION.md Wave 0 Requirements deferral footnote (W-02) -->
VALIDATION.md currently says: "Wave 0 delivers `scripts/validation/phase5-structure-check.sh` before any other plan wave runs assertions against it"
W-02 fix: add a one-line footnote noting Wave 0 is deferred to Wave 5 retrospective + per-task inline grep/jq/test assertions in Plans 01-04 cover Nyquist continuity; flip `wave_0_complete: false → true` once script lands.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1 (checkpoint): adjudicate B.1 completion text + B.3 step-count phrasing</name>
  <decision>Two cosmetic-fix wordings need user sign-off before literal text lands in README files.</decision>
  <context>
    **B.1 — README truncation completion:** RESEARCH.md proposes the completion text below. Approval needed.

    **B.3 — pipeline-step count (W-04 FIX — default REVISED to Option ii):**

    > **Default Option (ii) is mandated by ROADMAP SC-4 + REQUIREMENTS.md FOUND-11 verbatim text "B.3 pipeline-step count corrected to 13". Choosing Option (i) requires an explicit user override that updates ROADMAP/REQUIREMENTS in the same commit** so the source-of-truth artefacts (ROADMAP, REQUIREMENTS) and the README content remain consistent. The checkpoint still allows the override path, but the gravity defaults to Option (ii).

    These choices land in user-visible README content. Surface them as a single checkpoint so the W5 implementer applies the user's literal text in Tasks 2 & 3 without re-deciding mid-implementation.
  </context>
  <options>
    <option id="b1-research-suggested-plus-b3-v2-endstate">
      <name>B.1 = RESEARCH-suggested text + B.3 = v2 end-state (Option ii, "13 stages") — DEFAULT (per ROADMAP SC-4 + REQUIREMENTS.md FOUND-11 verbatim)</name>
      <pros>Default per ROADMAP SC-4 + REQUIREMENTS FOUND-11 verbatim text; single README update for the milestone; aligns with DESIGN-12 13-skill end-state inventory; no follow-up README pass required when v2.6+ adds remaining skills</pros>
      <cons>v2.1 plugin currently ships 7 skills; "13 stages" forward-states the v2 end-state. Mitigation: this is acceptable per ROADMAP/REQUIREMENTS-locked text; the README narrative is product-roadmap-shaped, not artefact-inventory-shaped</cons>
    </option>
    <option id="b1-research-suggested-plus-b3-v21-reality">
      <name>B.1 = RESEARCH-suggested text + B.3 = v2.1 reality (Option i, 7 skills / 6-step pipeline) — OVERRIDE (requires same-commit ROADMAP/REQUIREMENTS update)</name>
      <pros>Matches what the plugin currently ships; no forward-stating that mismatches v2.1 manifests</pros>
      <cons>Contradicts ROADMAP Phase 5 SC-4 + REQUIREMENTS.md FOUND-11 verbatim text "corrected to 13". Selecting this option REQUIRES the user to also update ROADMAP/REQUIREMENTS verbatim in the SAME commit so the source-of-truth artefacts and README stay consistent. Failing to do so leaves a drift between SC-4 wording and README content</cons>
    </option>
    <option id="user-alternative">
      <name>User provides alternative B.1 completion text and/or B.3 phrasing</name>
      <pros>Captures user's preferred wording verbatim</pros>
      <cons>If alternative B.3 phrasing differs from ROADMAP SC-4 + REQUIREMENTS FOUND-11, user must update those artefacts in the same commit</cons>
    </option>
  </options>
  <resume-signal>Reply with selected option ID (b1-research-suggested-plus-b3-v2-endstate / b1-research-suggested-plus-b3-v21-reality / user-alternative + provided text). If `b1-research-suggested-plus-b3-v21-reality` or `user-alternative` is chosen with B.3 phrasing that differs from "13 stages", confirm that ROADMAP SC-4 + REQUIREMENTS.md FOUND-11 will be updated in the same commit. Tasks 2 & 3 apply the chosen text without further adjudication.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: FOUND-11 B.1 — close dydx-delivery/README.md changelog truncation (literal text per Task 1 checkpoint decision)</name>
  <files>dydx-delivery/README.md</files>
  <read_first>
    - `dydx-delivery/README.md` (full file, especially lines ~120-126 — confirm line 126 still terminates mid-word at "now c" before edit)
    - `.planning/AUDIT.md` § AUDIT-07 §B.1 (the original truncation finding)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "B.1 — README truncation (plugin-level)" (lines ~554-564) — proposed completion text
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "MODIFIED: dydx-delivery/README.md (3 edits)" — B.1 + B.3 + (already-W2) hard-rules pointer
    - Task 1 checkpoint decision (which completion text was approved)
  </read_first>
  <action>
    Locate the truncation point near line 126 in `dydx-delivery/README.md`. The line currently reads (approximately):
    `**0.3.0** — Renamed `generate-test-sheet` → `generate-test-plan` (and `test-sheet_v*.md` → `test-plan_v*.md`) for clearer team-facing language. The bot-run terminal stage (`execute-tests`) now c`

    Replace the truncated terminator `now c` with the completion text approved in Task 1. Default text (Option `b1-research-suggested-plus-b3-v2-endstate`):
    `now carries explicit sandbox-enforcement rules in `references/safety-rules.md`; results are written to versioned `results-YYYY-MM-DD_vN.md` files.`

    Resulting full line 126 (default):
    `**0.3.0** — Renamed `generate-test-sheet` → `generate-test-plan` (and `test-sheet_v*.md` → `test-plan_v*.md`) for clearer team-facing language. The bot-run terminal stage (`execute-tests`) now carries explicit sandbox-enforcement rules in `references/safety-rules.md`; results are written to versioned `results-YYYY-MM-DD_vN.md` files.`

    If Task 1 selected `user-alternative` with provided text, use the user's verbatim completion instead.

    Do NOT touch any other line in `dydx-delivery/README.md`. Lines 99-105 are already converted to the D-59 hard-rules pointer in Wave 2 (plan 05-02). Wave 5 only completes the changelog at the end.

    **B.3 in plugin README:** RESEARCH.md §B.3 also flags `dydx-delivery/README.md` skill rows at lines 33-41 (currently 7 skills listed). Plugin README is internally consistent at 7 skills already — no plugin-README B.3 edit needed. B.3 lands ONLY in root README (Task 3).
  </action>
  <verify>
    <automated>! grep -qE "execute-tests\`\) now c$" dydx-delivery/README.md &amp;&amp; grep -qF "results-YYYY-MM-DD" dydx-delivery/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dydx-delivery/README.md` exits 0
    - `! grep -qE 'execute-tests\`\) now c$' dydx-delivery/README.md` exits 0 (no line ends with mid-word truncation)
    - `! grep -qE ' now c$' dydx-delivery/README.md` exits 0 (no line ends with " now c")
    - If default Option `b1-research-suggested-plus-b3-v2-endstate` was selected: `grep -qF "results-YYYY-MM-DD_vN.md" dydx-delivery/README.md` exits 0 AND `grep -qF "sandbox-enforcement rules" dydx-delivery/README.md` exits 0
    - The line previously ending in `now c` now ends with `.md files.` (or the user-alternative terminator) — last line of the changelog entry punctuated and complete
    - File line count grew by 0 (in-place completion, no new lines added — the truncation was mid-line)
    - W2 hard-rules pointer still present (cross-wave invariant): `grep -qF "See \`dydx-delivery/references/safety-rules.md\` for the canonical ruleset" dydx-delivery/README.md` exits 0
    - W2 hard-rules section H2 still present: `grep -qE "^## Test execution — safety rules$" dydx-delivery/README.md` exits 0
  </acceptance_criteria>
  <done>B.1 truncation closed. Changelog entry for v0.3.0 is grammatically complete. No other lines touched.</done>
</task>

<task type="auto">
  <name>Task 3: FOUND-11 B.2 + B.3 — root README literal substitutions (test sheet → test plan; pipeline-step count fix per Task 1 decision)</name>
  <files>README.md</files>
  <read_first>
    - `README.md` (root, all 87 lines — confirm line 9 carries `**test sheet**` AND a pipeline narrative)
    - `.planning/AUDIT.md` § AUDIT-07 §B.2 + §B.3 (the original cosmetic findings)
    - `.planning/REQUIREMENTS.md` FOUND-11 verbatim text "B.3 pipeline-step count corrected to 13"
    - `.planning/ROADMAP.md` Phase 5 SC-4 verbatim text "B.3 pipeline-step count corrected to 13 in README.md:9"
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "B.2 — Residual 'test sheet' wording" + § "B.3 — Pipeline-step count mismatch (root README)" (lines ~566-580)
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "MODIFIED: README.md (root, 2 edits)"
    - Task 1 checkpoint decision (B.3 option selected: default v2 end-state, override v2.1 reality, or user-alternative)
  </read_first>
  <action>
    Apply two edits to root `README.md:9` (or whichever line carries the pipeline narrative — read first to locate).

    **Edit B.2 — single literal substitution (bold-wrapped phrase):**
    FROM literal: `**test sheet**`
    TO   literal: `**test plan**`

    Resulting line 9 fragment (B.2 only):
    `Stage-gated client delivery pipeline (discovery → SOW → functional spec → technical spec → **test plan** → execution)`

    **Edit B.3 — pipeline-step count per Task 1 checkpoint decision (W-04 fix — default REVISED to Option ii).**

    **Default Option `b1-research-suggested-plus-b3-v2-endstate` = use v2 end-state (Option ii — "13 stages").** This default is mandated by ROADMAP Phase 5 SC-4 + REQUIREMENTS.md FOUND-11 verbatim text "B.3 pipeline-step count corrected to 13". Update line 9 (or the closest pipeline narrative line) to carry the literal phrase `13 stages` (or `13-stage pipeline`). Concretely:
    - If line 9 currently states `5 pipeline steps` / `Five-step pipeline:` / `5-stage`, replace with `13 stages` / `13-stage pipeline`.
    - If line 9 has no explicit count, prepend `13-stage pipeline:` to the narrative or insert `(13 stages)` parenthetical so the count is explicit.
    - If line 9 already states a different number (e.g., `6`), replace with `13`.

    **Override path Option `b1-research-suggested-plus-b3-v21-reality`** = use v2.1 reality (Option i, 6 stages / 7 skills). Selecting this option REQUIRES the user (in the resume signal) to confirm same-commit update of ROADMAP SC-4 + REQUIREMENTS FOUND-11 verbatim text. If confirmed, update line 9 to "6 pipeline steps" / "Six-stage pipeline:" + same-commit edits to:
    - `.planning/ROADMAP.md` Phase 5 SC-4 line: `B.3 pipeline-step count corrected to 13` → `B.3 pipeline-step count corrected to 6 (v2.1 reality; 13 deferred to v2 end-state)`
    - `.planning/REQUIREMENTS.md` FOUND-11 line: `B.3 pipeline-step count corrected to 13` → `B.3 pipeline-step count corrected to 6 (v2.1 reality)`

    If Task 1 selected `user-alternative` with B.3 phrasing, use user's verbatim text + apply any required ROADMAP/REQUIREMENTS updates in the same commit if the user-alternative number ≠ 13.

    Do NOT touch any other line in root README. The root README has no other Phase-5-owned edits.
  </action>
  <verify>
    <automated>! grep -qiF "test sheet" README.md &amp;&amp; grep -qF "**test plan**" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `! grep -qiF "test sheet" README.md` exits 0 (B.2 — old wording fully removed, case-insensitive)
    - `grep -qF "**test plan**" README.md` exits 0 (B.2 — new bold-wrapped phrase present)
    - `grep -qF "pipeline" README.md` exits 0 (pipeline narrative still present)
    - If default Option `b1-research-suggested-plus-b3-v2-endstate` was selected: `grep -qE "13 (stages|step pipeline|-stage pipeline)" README.md` exits 0 (B.3 — "13" present per ROADMAP SC-4 + REQUIREMENTS FOUND-11 verbatim)
    - If default Option was selected: `grep -qF "B.3 pipeline-step count corrected to 13" .planning/REQUIREMENTS.md` exits 0 (REQUIREMENTS still carries the verbatim "13" text — same-commit consistency check)
    - If default Option was selected: `grep -qF "B.3 pipeline-step count corrected to 13" .planning/ROADMAP.md` exits 0 (ROADMAP SC-4 still carries verbatim "13" text)
    - If Option `b1-research-suggested-plus-b3-v21-reality` was selected: `grep -qE "6 (pipeline )?steps|Six-stage pipeline|six destinations" README.md` exits 0 AND `grep -qF "v2.1 reality" .planning/ROADMAP.md` exits 0 AND `grep -qF "v2.1 reality" .planning/REQUIREMENTS.md` exits 0 (override path requires same-commit update of source-of-truth artefacts)
    - File line count unchanged or +/- a small delta (in-place substitutions only on README.md; ROADMAP/REQUIREMENTS only modified if override path chosen)
    - B.5 UAT-3.1 cross-wave invariant (cross-check): both manifests still carry `jasonmichaelb@gmail.com`. Cross-plan: `jq -r '.author.email' dydx-delivery/.claude-plugin/plugin.json` returns `jasonmichaelb@gmail.com` AND `jq -r '.owner.email' .claude-plugin/marketplace.json` returns `jasonmichaelb@gmail.com` (no W5 mutation of the email)
  </acceptance_criteria>
  <done>B.2 ("test sheet" → "test plan") and B.3 (step-count per Task 1 decision; default = "13 stages" per ROADMAP SC-4 / REQUIREMENTS FOUND-11 verbatim) closed in root README. If override path was chosen, ROADMAP SC-4 + REQUIREMENTS FOUND-11 are updated in the same commit. B.5 owner-email cross-wave check confirms no mutation.</done>
</task>

<task type="auto">
  <name>Task 4: Wave 0 retrospective — author scripts/phase5-structure-check.sh + update VALIDATION.md (W-01 + W-02 fixes; flips wave_0_complete to true)</name>
  <files>.planning/phases/05-foundations/scripts/phase5-structure-check.sh, .planning/phases/05-foundations/05-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh` (full 192 lines — the analog structure to mirror)
    - `.planning/phases/05-foundations/05-VALIDATION.md` § "Validation Architecture" + § "Wave 0 Requirements" + frontmatter `wave_0_complete: false`
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Validation Architecture (Nyquist Dimension 8)" / § "Phase Requirements → Assertion Map" (lines ~614-657) — the ~40 specific assertions
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: .planning/phases/05-foundations/scripts/phase5-structure-check.sh" — header pattern + section-extraction helper + assertion patterns to copy verbatim
    - All files produced by Waves 1-4 + Tasks 2-3 of this wave (the assertions reference these as their fail-points)
  </read_first>
  <action>
    Two coordinated edits in this task — the structure-check script and the VALIDATION.md updates land together so VALIDATION.md cross-refs are not left dangling (W-01 + W-02 fixes).

    **PART A — Create executable Bash script `.planning/phases/05-foundations/scripts/phase5-structure-check.sh`.** Structure mirrors `openquestions-structure-check.sh` (header + `fail()` helper + per-assertion sections + closing exit 0).

    **Header pattern (copy verbatim from Phase 4 analog, retitle):**
    ```bash
    #!/usr/bin/env bash
    # phase5-structure-check.sh — structural verifier for Phase 5 deliverables
    #
    # Comment-vs-code policy (carried from Phase 4 openquestions-structure-check.sh):
    #   - Sentinel-token matches use `grep -qF` (literal-string, case-sensitive)
    #   - Pattern matches use `grep -cE` (case-sensitive extended regex)
    #   - Script does NOT use case-insensitive matching (no -i flag) except B.2 (B.2 uses -i deliberately)
    #
    set -euo pipefail
    fail() { echo "FAIL: $1" >&2; exit 1; }
    pass() { echo "PASS: $1"; }
    ```

    Support `--quick` (only the smoke-test subset) and `--section <name>` (run a specific FOUND-NN section).

    **Required assertion groups (target ~40 total, organized by FOUND-NN):**

    Per `05-RESEARCH.md` §Phase Requirements → Assertion Map, the script must include at least one assertion per row. Group them as:

    **A1..A4 — FOUND-01 safety-rules.md:**
    - A1: `[ -f dydx-delivery/references/safety-rules.md ] || fail "FOUND-01: safety-rules.md missing"`
    - A2: `grep -qF "Coda writes ARE permitted" dydx-delivery/references/safety-rules.md || fail "FOUND-01: CRIT-5 Coda clause missing"`
    - A3: `grep -qF "DESIGN-03" dydx-delivery/references/safety-rules.md || fail "FOUND-01: DESIGN-03 cross-ref missing"`
    - A4: `grep -qE "^## [0-9]+\\. " dydx-delivery/references/safety-rules.md | wc -l` returns at least 10

    **A5..A8 — FOUND-02 stage-numbering.md:**
    - A5: file exists
    - A6: `grep -qF "00_discovery_" dydx-delivery/references/stage-numbering.md && grep -qF "02_discovery_" ...` (both old + new present)
    - A7: `grep -qF "04a_fnspec-platform_" ...` (substages documented)
    - A8: `grep -qF "OPEN-Q15" ...` (lenient-mode authority)

    **A9..A12 — FOUND-03 frontmatter-scheme.md:**
    - A9: file exists
    - A10: `grep -qiF "lenient"`
    - A11: `grep -qF "client_review"`
    - A12: `grep -qF "frontmatter_version: 2"`

    **A13..A14 — FOUND-04 glossary.md:**
    - A13: file exists
    - A14: `grep -qF "Claude for Chrome" && grep -qF "Wrike host field"` (FOUND-13 entries present)

    **A15..A17 — FOUND-05 hard-rules dedup (Wave 2):**
    - A15: pointer wording present in 3 files: `[ "$(grep -lF "See \`dydx-delivery/references/safety-rules.md\` for the canonical ruleset" dydx-delivery/skills/execute-tests/SKILL.md dydx-delivery/skills/generate-test-plan/references/test-plan-template.md dydx-delivery/README.md | wc -l | tr -d ' ')" = "3" ]`
    - A16: collapse stub at `dydx-delivery/skills/execute-tests/references/safety-rules.md`: `grep -qF "Pointer-only"`
    - A17: zero based_on_* old-prefix survivors: `! grep -rn "based_on_discovery: 00_discovery_\\|based_on_sow: 01_sow_\\|based_on_functional_spec: 02_functional-spec_\\|based_on_technical_spec: 03_technical-spec_\\|based_on_test_plan: test-plan_v" dydx-delivery/skills/`

    **A18..A20 — FOUND-06 file renumbering (Wave 2):**
    - A18: positive cover `02_discovery_v` ≥ 2 hits
    - A19: positive cover `08b_test-plan_v` ≥ 3 hits
    - A20: Stage-N self-labels updated (sample): `grep -qF "Stage 8b of the dydx-delivery pipeline" dydx-delivery/skills/generate-test-plan/references/test-plan-template.md`

    **A21..A26 — FOUND-07 manifest sync (Wave 3):**
    - A21: `[ "$(jq -r '.version' dydx-delivery/.claude-plugin/plugin.json)" = "2.0.0" ]`
    - A22: `[ "$(jq -r '.metadata.version' .claude-plugin/marketplace.json)" = "2.0.0" ]`
    - A23: `[ "$(jq -r '.plugins[0].version' .claude-plugin/marketplace.json)" = "2.0.0" ]`
    - A24: `[ "$(jq -r '.author.email' dydx-delivery/.claude-plugin/plugin.json)" = "jasonmichaelb@gmail.com" ]` (UAT-3.1 unchanged)
    - A25: `[ "$(jq -r '.owner.email' .claude-plugin/marketplace.json)" = "jasonmichaelb@gmail.com" ]` (UAT-3.1 unchanged)
    - A26: cross-manifest equality: `[ "$(jq -r '.version' dydx-delivery/.claude-plugin/plugin.json)" = "$(jq -r '.plugins[0].version' .claude-plugin/marketplace.json)" ]`

    **A27..A28 — FOUND-08 LICENSE:**
    - A27: `[ -f LICENSE ]`
    - A28: `diff <(printf 'All rights reserved.\nNot licensed for redistribution.\n') LICENSE >/dev/null` (also asserts via `! grep -lU $'\r' LICENSE` — W-03 cross-platform CR check)

    **A29..A31 — FOUND-09 scaffold dirs:**
    - A29: `[ -d dydx-delivery/commands ] && [ -d dydx-delivery/agents ] && [ -d dydx-delivery/hooks ]`
    - A30: each contains exactly `.gitkeep` only: `for d in dydx-delivery/commands dydx-delivery/agents dydx-delivery/hooks; do [ "$(ls -1A $d | grep -v '\.gitkeep' | wc -l | tr -d ' ')" = "0" ] || fail "FOUND-09: $d has non-.gitkeep content"; done`
    - A31: each .gitkeep is zero-byte: `[ "$(wc -c < dydx-delivery/commands/.gitkeep)" -eq 0 ]` etc.

    **A32..A34 — FOUND-10 connector matrix:**
    - A32: `[ -f dydx-delivery/references/connector-matrix.md ]`
    - A33: all 6 connectors named: `for c in "Coda MCP" "Google Workspace MCP" "Miro MCP" "Pipefy API" "Wrike API" "Ziflow API"; do grep -qF "$c" dydx-delivery/references/connector-matrix.md || fail "FOUND-10: connector $c missing"; done`
    - A34: UAT-6.1 + UAT-3.5 locks honoured: `! grep -qF "native_ai_path: api" dydx-delivery/references/connector-matrix.md && ! grep -qF "Pipefy MCP" dydx-delivery/references/connector-matrix.md`

    **A35..A36 — FOUND-11 cosmetic fixes:**
    - A35: B.2 removed: `! grep -qiF "test sheet" README.md`
    - A36: B.1 closed: `! grep -qE " now c$" dydx-delivery/README.md`

    **A37 — FOUND-12 status survey:**
    - A37: `[ -f .planning/phases/05-foundations/05-STATUS-SURVEY.md ] && grep -qE "no drift|drift detected" .planning/phases/05-foundations/05-STATUS-SURVEY.md`

    **A38..A40 — FOUND-13 OPEN-Q row flips:**
    - A38: `for q in Q06.1 Q07.1 Q09 Q10 Q11 Q12 Q13 Q25; do grep -A20 "OPEN-${q}" .planning/OPEN-QUESTIONS.md | grep -qF "Status: decided" || fail "FOUND-13: ${q} not decided"; done`
    - A39: each resolved row cites connector-matrix.md: `[ "$(grep -c 'dydx-delivery/references/connector-matrix.md' .planning/OPEN-QUESTIONS.md)" -ge 8 ]`
    - A40: B.5 cross-check (UAT-3.1 unchanged): `[ "$(jq -r '.author.email' dydx-delivery/.claude-plugin/plugin.json)" = "jasonmichaelb@gmail.com" ]` (intentional duplicate of A24 — explicit B.5 sentinel)

    **Closing pattern (copy from Phase 4 analog):**
    ```bash
    echo "OK: all structural checks passed"
    exit 0
    ```

    Make the file executable: per Phase 4 convention, the file carries the shebang and the executor of the plan will `chmod +x` it (or invoke with `bash <path>`). The acceptance criterion below uses `bash <path>` so executable bit is not strictly required.

    **Grep gate hygiene per planner instructions:** every `grep -c` count comparison MUST exclude `^#` comment lines via `grep -v '^#' | grep -c ...` if the file under inspection contains comments and the assertion checks for token absence. Most assertions here are on markdown/JSON which don't have shell-style `#` comments — direct grep is OK. The script itself is bash and its own `#`-prefixed comment lines should never be subject to its own greps (they aren't — script greps target other files).

    Final script length target: ~150-200 lines (Phase 4 analog is 192; Phase 5 has ~40 assertions vs Phase 4's 14, so denser — possibly slightly longer).

    **PART B — Update `.planning/phases/05-foundations/05-VALIDATION.md` (W-01 + W-02 fixes).**

    Read the current `05-VALIDATION.md`. Apply three surgical edits (preserve all other content verbatim):

    **Edit 1 (W-01 — script path correction):** Replace EVERY occurrence of `scripts/validation/phase5-structure-check.sh` with `.planning/phases/05-foundations/scripts/phase5-structure-check.sh`. Concretely the substitutions land in:
    - Test Infrastructure table rows for "Quick run command" and "Full suite command"
    - Sampling Rate bullets ("After every task commit", "After every plan wave")
    - Manual-Only Verifications references (none currently — confirm via grep)
    - Wave 0 Requirements bullet ("`scripts/validation/phase5-structure-check.sh` — single bash script with all assertions above")
    - Validation Sign-Off bullet ("Wave 0 delivers `scripts/validation/phase5-structure-check.sh`")
    - Phase 5 — Validation Strategy preamble Note section ("Validation = grep/jq/file-existence assertions baked into a single shell script (`scripts/validation/phase5-structure-check.sh`) ...")

    **Edit 2 (W-02 — Wave 0 deferral footnote):** Inside the `## Wave 0 Requirements` section, add a single-line footnote IMMEDIATELY AFTER the existing 3-bullet list, BEFORE the italicised "_If none: ..._" line. Verbatim text:

    > **Note (W-02 fix):** Wave 0 deferred to Wave 5 retrospective; per-task inline grep/jq/test assertions in Plans 01-04 cover Nyquist sampling continuity. Script lands in W5 as ratification artefact.

    **Edit 3 (W-01 + W-02 — flip frontmatter):** In the YAML frontmatter at the top of `05-VALIDATION.md`, change the line `wave_0_complete: false` → `wave_0_complete: true`. This flip happens AS PART OF THIS SAME TASK, after the script (PART A) has been written and the `bash <path>` smoke test passes — flipping is the final action of Task 4.

    Do NOT touch:
    - `nyquist_compliant: false` (that flip is owned by `/gsd-verify-work` after the per-task verification map is populated)
    - The Per-Task Verification Map table (still TBD)
    - The Manual-Only Verifications table
    - The Approval line ("**Approval:** pending")
  </action>
  <verify>
    <automated>test -f .planning/phases/05-foundations/scripts/phase5-structure-check.sh &amp;&amp; bash -n .planning/phases/05-foundations/scripts/phase5-structure-check.sh &amp;&amp; bash .planning/phases/05-foundations/scripts/phase5-structure-check.sh &amp;&amp; grep -qF '.planning/phases/05-foundations/scripts/phase5-structure-check.sh' .planning/phases/05-foundations/05-VALIDATION.md &amp;&amp; grep -qF 'wave_0_complete: true' .planning/phases/05-foundations/05-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-foundations/scripts/phase5-structure-check.sh` exits 0
    - `bash -n .planning/phases/05-foundations/scripts/phase5-structure-check.sh` exits 0 (syntactically valid bash)
    - `head -1 .planning/phases/05-foundations/scripts/phase5-structure-check.sh` returns `#!/usr/bin/env bash` (correct shebang)
    - `grep -qF "set -euo pipefail" .planning/phases/05-foundations/scripts/phase5-structure-check.sh` exits 0
    - `grep -qF "fail()" .planning/phases/05-foundations/scripts/phase5-structure-check.sh` exits 0
    - Script supports section invocation: `grep -qF "--section" .planning/phases/05-foundations/scripts/phase5-structure-check.sh` AND `grep -qF "--quick" .planning/phases/05-foundations/scripts/phase5-structure-check.sh`
    - Assertion coverage: `grep -cE "(grep -[qcl]|jq -[er]|test -[fd]|diff )" .planning/phases/05-foundations/scripts/phase5-structure-check.sh` returns at least 40 (proxy for ~40 assertions)
    - All 13 FOUND-NN identifiers cited: `for n in 01 02 03 04 05 06 07 08 09 10 11 12 13; do grep -qF "FOUND-${n}" .planning/phases/05-foundations/scripts/phase5-structure-check.sh || exit 1; done`
    - Script exits 0 when run end-to-end (all assertions pass — proves Waves 1-5 actually delivered): `bash .planning/phases/05-foundations/scripts/phase5-structure-check.sh` exits 0
    - Closing pattern: `tail -3 .planning/phases/05-foundations/scripts/phase5-structure-check.sh | grep -qF "OK: all structural checks passed"`
    - **W-01 fix:** `grep -qF '.planning/phases/05-foundations/scripts/phase5-structure-check.sh' .planning/phases/05-foundations/05-VALIDATION.md` exits 0 (new path referenced)
    - **W-01 fix:** `! grep -qF 'scripts/validation/phase5-structure-check.sh' .planning/phases/05-foundations/05-VALIDATION.md` exits 0 (old path no longer referenced)
    - **W-02 fix:** `grep -qF "Wave 0 deferred to Wave 5 retrospective" .planning/phases/05-foundations/05-VALIDATION.md` exits 0 (deferral footnote present in Wave 0 Requirements section)
    - **W-01 + W-02 fix:** `grep -qF 'wave_0_complete: true' .planning/phases/05-foundations/05-VALIDATION.md` exits 0 (frontmatter flipped)
    - **W-01 + W-02 fix:** `! grep -qF 'wave_0_complete: false' .planning/phases/05-foundations/05-VALIDATION.md` exits 0 (no stale false value)
    - VALIDATION.md `nyquist_compliant: false` PRESERVED (not flipped here): `grep -qF 'nyquist_compliant: false' .planning/phases/05-foundations/05-VALIDATION.md` exits 0
  </acceptance_criteria>
  <done>Wave 0 validation script exists at `.planning/phases/05-foundations/scripts/phase5-structure-check.sh`, syntactically valid bash, covers all 13 FOUND-NN requirements via ~40 assertions, exits 0 against actual Wave 1-5 deliverables. VALIDATION.md updated with corrected script path (W-01), Wave 0 deferral footnote (W-02), and `wave_0_complete: true` flipped (W-01 + W-02). Provides the per-wave / per-task sampling tool referenced in 05-VALIDATION.md without dangling cross-refs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cosmetic fix author -> user-visible README content | B.1/B.2/B.3 text lands in repo-public README files; mis-wording impacts external perception |
| Wave 0 script -> all Phase 5 deliverables | If the structure-check script over-asserts (too strict), CI false-fails; if under-asserts, regressions slip through |
| B.5 owner-email cross-wave invariant | UAT-3.1 lock; structure-check explicitly re-asserts even though no wave is supposed to touch it |
| README.md ↔ ROADMAP/REQUIREMENTS source-of-truth consistency | B.3 phrasing in README must match SC-4 / FOUND-11 verbatim text; W-04 fix elevates the default to Option (ii) so the trio stays consistent |
| VALIDATION.md cross-refs ↔ on-disk script path | Path mismatch between docs and actual script breaks downstream "where do I run the gate?" lookup; W-01 + W-02 fixes close this in the same task that creates the script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05W5-01 | Tampering | README content wording drift from user-approved completion | mitigate | Task 1 checkpoint:decision captures explicit user sign-off on B.1 completion + B.3 phrasing; Tasks 2 & 3 apply the literal approved text |
| T-05W5-02 | Tampering | B.5 owner-email mutation slips through any wave | mitigate | A40 in structure-check duplicates A24 as explicit B.5 sentinel; cross-task acceptance criterion in Task 3 also re-asserts (T-2 / UAT-3.1 from security_threat_model) |
| T-05W5-03 | Repudiation | Wave 0 script claims pass when assertions are stubs | mitigate | Task 4 acceptance criterion requires `bash <script>` to exit 0 against actual deliverables — pseudo-stub assertions that always return true would fail because the deliverables they SHOULD assert against (e.g., D-59 pointer in 3 files) wouldn't be sentinel-grepped |
| T-05W5-04 | Spoofing | B.3 default forward-states v2.1 reality (W-04 fix mitigation) | accept | Default is Option (ii) "13 stages" because that is the verbatim text in ROADMAP SC-4 + REQUIREMENTS FOUND-11. Override to Option (i) is allowed but requires same-commit ROADMAP/REQUIREMENTS update so the source-of-truth artefacts and README stay consistent. Task 3 acceptance criteria enforce this same-commit consistency |
| T-05W5-05 | Information disclosure | RESEARCH.md suggested B.1 text leaks unintended technical details | accept | B.1 text is changelog-narrative scope (mentions safety-rules.md + results filename pattern only); no secrets or implementation details leaked |
| T-05W5-06 | Elevation of privilege | Wave 5 cosmetic-fix author touches W1-W4 deliverables outside cosmetic-fix scope | mitigate | Task 2 + Task 3 actions scope edits to specific lines (line 126 in plugin README; line 9 in root README); cross-wave invariant assertions in acceptance criteria (e.g., W2 hard-rules pointer still present in dydx-delivery/README.md) catch over-reach |
| T-05W5-07 | Repudiation | VALIDATION.md path mismatch leaves dangling cross-refs to a non-existent script (W-01 / W-02) | mitigate | Task 4 PART B updates VALIDATION.md path verbatim, adds the Wave 0 deferral footnote, and flips `wave_0_complete: true` in the same task that creates the script — acceptance criteria assert the new path is present and the old path is absent |
</threat_model>

<verification>
- B.1 changelog complete (no truncation, no `now c` line terminator)
- B.2 root README contains `**test plan**` not `**test sheet**`
- B.3 pipeline-step count aligned with Task 1 decision (default = "13 stages" per ROADMAP SC-4 + REQUIREMENTS FOUND-11 verbatim; override path requires same-commit ROADMAP/REQUIREMENTS update)
- B.4 LICENSE evidence cross-references Wave 3
- B.5 owner-email unchanged in both manifests (cross-wave invariant)
- B.6 marketplace.json homepage symmetric with plugin.json homepage (W3 closure)
- `scripts/phase5-structure-check.sh` exists at the new canonical path, runs, and exits 0 against actual deliverables
- VALIDATION.md path-references updated (W-01) and Wave 0 deferral footnote present (W-02); `wave_0_complete: true` flipped
- All 13 FOUND-* requirements cited by at least one assertion in the script
</verification>

<success_criteria>
- 4 files modified (2 READMEs + 1 NEW validation script + 1 updated VALIDATION.md)
- Task 1 checkpoint adjudicated by user
- Tasks 2-4 apply the approved text + ratify Waves 1-4 + close VALIDATION.md cross-refs
- `bash .planning/phases/05-foundations/scripts/phase5-structure-check.sh` exits 0
- VALIDATION.md `wave_0_complete: true` set; no `scripts/validation/phase5-structure-check.sh` references survive
- Phase 5 ready for `/gsd-verify-work` and Phase 6 kickoff
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundations/05-05-SUMMARY.md` recording:
- Task 1 checkpoint decision: which option ID was selected + B.3 phrasing applied (note whether default Option (ii) was kept or override Option (i) chosen with same-commit ROADMAP/REQUIREMENTS update)
- B.1 literal completion text applied (verbatim line 126 after-state)
- B.2/B.3 literal substitutions applied (verbatim line 9 after-state)
- Cross-AI flag 2 closure: B.6 ratified (landed in W3)
- Cross-AI flag 3 closure (W-04 fix): B.3 default Option (ii) chosen + verbatim ROADMAP/REQUIREMENTS alignment OR override Option (i) chosen with same-commit update
- B.5 UAT-3.1 cross-wave invariant re-asserted (both manifests still carry jasonmichaelb@gmail.com)
- `phase5-structure-check.sh` assertion count + run result (PASS / which assertions if any FAIL)
- VALIDATION.md W-01 + W-02 fix evidence: new path referenced, old path absent, Wave 0 deferral footnote present, `wave_0_complete: true`
- Phase 5 entry point for `/gsd-verify-work {5}` next
</output>
