---
phase: 05-foundations
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - dydx-delivery/.claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - LICENSE
  - .gitattributes
  - dydx-delivery/commands/.gitkeep
  - dydx-delivery/agents/.gitkeep
  - dydx-delivery/hooks/.gitkeep
autonomous: true
requirements: [FOUND-07, FOUND-08, FOUND-09]
tags: [foundations, manifest-sync, license, scaffold-dirs, b6-homepage]

must_haves:
  truths:
    - "Per FOUND-07 + AUDIT-06 D-17, plugin.json + marketplace.json metadata + marketplace.json plugins[0] all carry version 2.0.0"
    - "Per UAT-3.1, plugin.json author.email and marketplace.json owner.email remain `jasonmichaelb@gmail.com` (B.5 NO FIX — dYdX-approved private email)"
    - "Per OPEN-Q23, LICENSE contains EXACTLY two lines: 'All rights reserved.\\nNot licensed for redistribution.' (byte-exact, LF-only)"
    - "Per FOUND-09 + DESIGN-11, three empty scaffold dirs (commands/, agents/, hooks/) exist at dydx-delivery/ root (siblings of skills/), each with .gitkeep"
    - "Per RESEARCH §B.6, B.6 homepage asymmetry lands here (W3) inline with marketplace.json version sync (cross-AI flag 2 adjudication — single-pass on marketplace.json, NOT W5)"
    - "Per RESEARCH §3 / cross-AI flag 5, the DESIGN-04 mcpServers field is DEFERRED out of Phase 5 scope (FOUND-07 is narrow: 2.0.0 sync only)"
    - "Per W-03 fix, .gitattributes pins LICENSE to LF line endings so Windows CRLF auto-conversion cannot break the byte-exact assertion"
    - "Per C-5 fix (Codex MEDIUM), .gitattributes edit is create-or-append (never overwrite) with idempotent assertion (`grep -c '^LICENSE text eol=lf$' .gitattributes == 1`) and no-clobber assertion (`wc -l` non-decrease vs pre-task baseline). Conflicting pre-existing LICENSE rule aborts with explicit error"
    - "Per C-6 fix (Codex MEDIUM), 'empty scaffold dir' is defined canonically as `ls -A <dir>` equals exactly `.gitkeep` — consumed by both Wave 3 inline checks and Plan 05-05 Task 4 structure-check script"
  artifacts:
    - path: "dydx-delivery/.claude-plugin/plugin.json"
      provides: "Plugin manifest with version 2.0.0 (author.email UNCHANGED)"
      contains: "2.0.0"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace manifest with metadata + plugins[0] version 2.0.0 + plugins[0].homepage (B.6 fix)"
      contains: "2.0.0"
    - path: "LICENSE"
      provides: "Repo-root LICENSE per OPEN-Q23 boilerplate (LF line endings)"
      contains: "All rights reserved."
    - path: ".gitattributes"
      provides: "Pins LICENSE to LF line endings cross-platform (Windows CRLF auto-conversion neutralised)"
      contains: "LICENSE text eol=lf"
    - path: "dydx-delivery/commands/.gitkeep"
      provides: "Empty scaffold dir tracking marker"
    - path: "dydx-delivery/agents/.gitkeep"
      provides: "Empty scaffold dir tracking marker"
    - path: "dydx-delivery/hooks/.gitkeep"
      provides: "Empty scaffold dir tracking marker"
  key_links:
    - from: "dydx-delivery/.claude-plugin/plugin.json"
      to: ".claude-plugin/marketplace.json"
      via: "Synced version field (both 2.0.0); both reference jasonmichaelb@gmail.com author/owner"
      pattern: "2.0.0"
    - from: ".claude-plugin/marketplace.json plugins[0].homepage"
      to: "dydx-delivery/.claude-plugin/plugin.json homepage"
      via: "Matching URL strings (B.6 symmetry)"
      pattern: "https://github.com/SonofJay13/dydx-project-workflow"
    - from: ".gitattributes"
      to: "LICENSE"
      via: "`LICENSE text eol=lf` directive guarantees LF on every checkout regardless of host platform"
      pattern: "LICENSE text eol=lf"
---

<objective>
Wave 3 of Phase 5: land the three structural foundations that Phase 6 platform skills + every later v2.x phase will read at:
1. **Plugin manifest 2.0.0 sync (FOUND-07).** Bump `plugin.json` version to 2.0.0. Bump `marketplace.json` metadata.version + plugins[0].version to 2.0.0. Add `plugins[0].homepage` to marketplace.json (B.6 fix per cross-AI flag 2). Owner/author email UNCHANGED per UAT-3.1.
2. **LICENSE file (FOUND-08).** Create `LICENSE` at REPO ROOT (not `dydx-delivery/LICENSE`) with the OPEN-Q23 two-line boilerplate. Pin to LF line endings via `.gitattributes` so Windows CRLF auto-conversion cannot break the byte-exact assertion.
3. **Scaffold dirs (FOUND-09).** Create three empty directories `dydx-delivery/commands/` + `dydx-delivery/agents/` + `dydx-delivery/hooks/`, each with a `.gitkeep` for git tracking. These ship EMPTY in Phase 5 per D-60 + DESIGN-04 (substantive content lands v2.6 / SURF-01..03).

Purpose: Wave 3 unblocks (a) every downstream phase's "what version is the plugin?" assumption, (b) the LICENSE-citing `plugin.json:10` `license: "Proprietary"` field's correctness, and (c) Phase 6 platform-skill authoring (which lands inside `dydx-delivery/skills/` but assumes the plugin-root scaffold is in place).

Output: 2 JSON manifest files edited + 1 new LICENSE file + 1 new/updated `.gitattributes` + 3 new scaffold dirs (each with `.gitkeep`) = 7 files modified or created.

**Cross-AI flag 2 adjudication:** D-60's wave table puts B.6 (marketplace.json homepage) in W5. RESEARCH recommends W3 inline with version sync (single-pass on marketplace.json, no two-pass edit). This plan lands B.6 in W3; Wave 5 (plan 05-05) documents the closure with footnote "completed by W3".

**Cross-AI flag 5 adjudication:** DESIGN-04 calls for an `mcpServers` field in plugin.json. FOUND-07's scope is narrow ("2.0.0 synced") — `mcpServers` is OUT OF SCOPE for Phase 5 per RESEARCH §Manifest Sync / Out-of-scope. Wave 3 does NOT add `mcpServers`. Defer to a later v2.x phase (likely v2.6 / SURF-01..03 alongside the substantive `hooks/` and `commands/` content). Acceptance criterion explicitly confirms the field is absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_prerequisite>
**Execution prerequisite (C-3 — Codex MEDIUM):** This plan's acceptance-criteria commands assume Bash 4+, `jq`, and `grep -E` semantics (Git Bash, WSL, or equivalent). On Windows PowerShell, run via `bash` or in a Git-Bash terminal. The prerequisite check itself is gated by Task 0 in Plan 05-01 (Wave 1); if Plan 05-01 Task 0 did not pass, Plan 05-03 MUST NOT proceed.
</execution_prerequisite>

<context>
@.planning/phases/05-foundations/05-CONTEXT.md
@.planning/phases/05-foundations/05-RESEARCH.md
@.planning/phases/05-foundations/05-PATTERNS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/OPEN-QUESTIONS.md
@dydx-delivery/.claude-plugin/plugin.json
@.claude-plugin/marketplace.json

<interfaces>
<!-- Current plugin.json (25 lines) — confirmed via cat verified 2026-05-10. -->
<!-- Only line 3 changes (version field). All other lines preserved verbatim. -->
Current line 3:    "version": "0.3.0",
Target  line 3:    "version": "2.0.0",

<!-- Current marketplace.json (21 lines) — confirmed verified 2026-05-10. -->
<!-- 3 edits: line 9 (metadata.version), line 16 (plugins[0].version), and new homepage key inserted into plugins[0] object. -->
Current line 9:     "version": "1.2.0"        (metadata.version)
Target  line 9:     "version": "2.0.0"

Current line 16:    "version": "0.3.0",       (plugins[0].version)
Target  line 16:    "version": "2.0.0",

New key in plugins[0] object (insert as last key before its closing }):
    "homepage": "https://github.com/SonofJay13/dydx-project-workflow"

<!-- LICENSE byte-exact content (OPEN-Q23) — LF line endings only: -->
Line 1: All rights reserved.
Line 2: Not licensed for redistribution.
(Trailing newline conventional; the `diff <(printf 'All rights reserved.\nNot licensed for redistribution.\n') LICENSE` acceptance test expects a final newline. LF-only — `.gitattributes` enforces this on every checkout regardless of host OS.)

<!-- .gitattributes directive (W-03 fix): -->
LICENSE text eol=lf

<!-- .gitkeep files are zero-byte: -->
dydx-delivery/commands/.gitkeep   (size 0)
dydx-delivery/agents/.gitkeep     (size 0)
dydx-delivery/hooks/.gitkeep      (size 0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FOUND-07 — sync plugin.json version to 2.0.0</name>
  <files>dydx-delivery/.claude-plugin/plugin.json</files>
  <read_first>
    - `dydx-delivery/.claude-plugin/plugin.json` (full 25-line file — confirm line 3 still carries `"version": "0.3.0"` before edit)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Manifest Sync to 2.0.0 (FOUND-07)" — exact line + key
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "MODIFIED: dydx-delivery/.claude-plugin/plugin.json (single key edit)"
    - `.planning/AUDIT.md` § AUDIT-06 D-17 (2.0.0 sync target rationale)
    - `.planning/phases/05-foundations/05-CONTEXT.md` spec_lock UAT-3.1 (author.email INTENTIONAL — DO NOT TOUCH)
  </read_first>
  <action>
    Edit `dydx-delivery/.claude-plugin/plugin.json` line 3 ONLY.

    Locate the line currently reading: `  "version": "0.3.0",`
    Replace with:                       `  "version": "2.0.0",`

    Do NOT touch any other line. Specifically preserved verbatim:
    - Line 1: `{`
    - Line 2: `  "name": "dydx-delivery",`
    - Line 4: `  "description": ...` (the existing description string)
    - Lines 5-8: `"author": { "name": "Jason Blignaut", "email": "jasonmichaelb@gmail.com" }` block (UAT-3.1 LOCK — author.email is the dYdX-approved private email; B.5 NO FIX)
    - Line 9: `  "homepage": "https://github.com/SonofJay13/dydx-project-workflow",`
    - Line 10: `  "license": "Proprietary",` (backed by FOUND-08 LICENSE file in Task 3 of this plan)
    - Lines 11+: `keywords` array and closing brace

    Do NOT add an `mcpServers` field — that's deferred (cross-AI flag 5).
    Do NOT edit `description`, `keywords`, `homepage`, or `license` text — out of FOUND-07 scope.

    Preserve 2-space indentation. Preserve trailing newline at EOF.
  </action>
  <verify>
    <automated>jq -e '.version == "2.0.0"' dydx-delivery/.claude-plugin/plugin.json &amp;&amp; jq -e '.author.email == "jasonmichaelb@gmail.com"' dydx-delivery/.claude-plugin/plugin.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.version' dydx-delivery/.claude-plugin/plugin.json` returns the literal string `2.0.0`
    - `jq -r '.author.email' dydx-delivery/.claude-plugin/plugin.json` returns `jasonmichaelb@gmail.com` (UAT-3.1 unchanged)
    - `jq -r '.author.name' dydx-delivery/.claude-plugin/plugin.json` returns `Jason Blignaut`
    - `jq -r '.name' dydx-delivery/.claude-plugin/plugin.json` returns `dydx-delivery`
    - `jq -r '.homepage' dydx-delivery/.claude-plugin/plugin.json` returns `https://github.com/SonofJay13/dydx-project-workflow`
    - `jq -r '.license' dydx-delivery/.claude-plugin/plugin.json` returns `Proprietary`
    - `jq -e 'has("mcpServers")' dydx-delivery/.claude-plugin/plugin.json` returns `false` (deferred per cross-AI flag 5)
    - `jq '.' dydx-delivery/.claude-plugin/plugin.json > /dev/null` exits 0 (valid JSON; no syntax breakage)
    - `wc -l dydx-delivery/.claude-plugin/plugin.json | awk '{print $1}'` returns at most 27 (originally 25; no growth from this task — version edit is in-place)
  </acceptance_criteria>
  <done>plugin.json carries version 2.0.0 + author.email unchanged + all other keys preserved verbatim + valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: FOUND-07 + B.6 — sync marketplace.json (3 edits: metadata.version, plugins[0].version, plugins[0].homepage)</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - `.claude-plugin/marketplace.json` (full 21-line file — confirm current line 9 `"version": "1.2.0"`, line 16 `"version": "0.3.0"`, no `homepage` key on plugins[0])
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Manifest Sync to 2.0.0 (FOUND-07)" § "marketplace.json" + § "B.6 — Homepage asymmetry"
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "MODIFIED: .claude-plugin/marketplace.json (3 edits — version sync + B.6 homepage add)"
    - `.planning/phases/05-foundations/05-CONTEXT.md` spec_lock UAT-3.1 (owner.email INTENTIONAL — DO NOT TOUCH); cross-AI flag 2 (B.6 lands here in W3, not W5)
    - `.planning/AUDIT.md` § AUDIT-07 §7.6 (B.6 homepage asymmetry — described); § AUDIT-07 §7.5 (B.5 owner-email — INTENTIONAL)
  </read_first>
  <action>
    Edit `.claude-plugin/marketplace.json` with EXACTLY three key updates (no other edits).

    **Edit 1 — line 9 (metadata.version):**
    FROM: `    "version": "1.2.0"`
    TO:   `    "version": "2.0.0"`

    **Edit 2 — line 16 (plugins[0].version):**
    FROM: `      "version": "0.3.0",`
    TO:   `      "version": "2.0.0",`

    **Edit 3 — add `homepage` key to the plugins[0] object (B.6 fix per cross-AI flag 2).**

    The current plugins[0] object closes around line 19-20 with the `tags` array followed by `}`. Insert a new key AFTER the `tags` array's closing `]` and BEFORE the object's closing `}`. Add a trailing comma to whatever key currently precedes the closing brace so the JSON remains valid. Final shape of the plugins[0] object should be:
    ```json
    {
      "name": "dydx-delivery",
      "source": "./dydx-delivery",
      "description": "<existing description>",
      "version": "2.0.0",
      "category": "delivery",
      "tags": [<existing tags>],
      "homepage": "https://github.com/SonofJay13/dydx-project-workflow"
    }
    ```

    The URL must match exactly the `homepage` value in `dydx-delivery/.claude-plugin/plugin.json:9`.

    **Preserved verbatim (DO NOT TOUCH):**
    - Line 1: `{`
    - Line 2: `  "name": "dydx-digital",`
    - Lines 3-6: `"owner": { "name": "dYdX Digital", "email": "jasonmichaelb@gmail.com" }` block (UAT-3.1 LOCK — B.5 NO FIX — dYdX-approved private email)
    - Line 8: `  "metadata": {`
    - Line 10: `  },` (metadata closing)
    - Line 12: `  "plugins": [`
    - Lines 13-15: plugins[0] `name`, `source`, `description` keys
    - Line 17: `      "category": "delivery",`
    - Line 18: `      "tags": [...]` (existing array)
    - All other braces / brackets / formatting

    Preserve 2-space indentation. Preserve trailing newline at EOF.
  </action>
  <verify>
    <automated>jq -e '.metadata.version == "2.0.0" and .plugins[0].version == "2.0.0" and .plugins[0].homepage == "https://github.com/SonofJay13/dydx-project-workflow" and .owner.email == "jasonmichaelb@gmail.com"' .claude-plugin/marketplace.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.metadata.version' .claude-plugin/marketplace.json` returns `2.0.0`
    - `jq -r '.plugins[0].version' .claude-plugin/marketplace.json` returns `2.0.0`
    - `jq -r '.plugins[0].homepage' .claude-plugin/marketplace.json` returns `https://github.com/SonofJay13/dydx-project-workflow` (B.6 fix per cross-AI flag 2)
    - `jq -r '.owner.email' .claude-plugin/marketplace.json` returns `jasonmichaelb@gmail.com` (UAT-3.1 — B.5 NO FIX; SAME value as plugin.json author.email)
    - `jq -r '.owner.name' .claude-plugin/marketplace.json` returns `dYdX Digital`
    - `jq -r '.name' .claude-plugin/marketplace.json` returns `dydx-digital`
    - `jq -r '.plugins[0].name' .claude-plugin/marketplace.json` returns `dydx-delivery`
    - `jq -r '.plugins[0].category' .claude-plugin/marketplace.json` returns `delivery`
    - `jq -e '.plugins[0].tags | type == "array"' .claude-plugin/marketplace.json` exits 0 (tags array preserved)
    - `jq '.' .claude-plugin/marketplace.json > /dev/null` exits 0 (valid JSON)
    - Cross-manifest consistency: `[ "$(jq -r '.version' dydx-delivery/.claude-plugin/plugin.json)" = "$(jq -r '.plugins[0].version' .claude-plugin/marketplace.json)" ]` exits 0 (both 2.0.0)
    - Cross-manifest homepage symmetry: `[ "$(jq -r '.homepage' dydx-delivery/.claude-plugin/plugin.json)" = "$(jq -r '.plugins[0].homepage' .claude-plugin/marketplace.json)" ]` exits 0
  </acceptance_criteria>
  <done>marketplace.json carries metadata.version 2.0.0 + plugins[0].version 2.0.0 + plugins[0].homepage matching plugin.json + owner.email unchanged + valid JSON. B.6 asymmetry closed inline with version sync (cross-AI flag 2 resolution).</done>
</task>

<task type="auto">
  <name>Task 3: FOUND-08 — create LICENSE at repo root (byte-exact per OPEN-Q23, LF-only via .gitattributes)</name>
  <files>LICENSE, .gitattributes</files>
  <read_first>
    - `.planning/OPEN-QUESTIONS.md` row OPEN-Q23 (lines ~407-409 — locked content)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "LICENSE Content (FOUND-08)" — byte-exact content + repo-root path
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: LICENSE (repo root, two-line boilerplate)"
    - `.planning/CHANGELIST.md:223` (Appendix B item B.4 — OPEN-Q23 reference)
    - Confirm `LICENSE` does NOT already exist at repo root: `test -f LICENSE` should currently exit 1
    - Inspect existing `.gitattributes` if present: `test -f .gitattributes && cat .gitattributes` — if present, append; if absent, create
  </read_first>
  <action>
    Two coordinated edits in this task — both required so the byte-exact LICENSE assertion holds on every host platform (W-03 fix).

    **Edit 1 — create-or-append `.gitattributes` at repo root with the LF directive (C-5 overwrite-safety fix per Codex MEDIUM).**

    Per C-5 (Codex MEDIUM finding), `.gitattributes` could already exist with unrelated rules. The edit operation MUST be a create-or-append (NEVER an overwrite). Apply this decision tree:

    1. **If `.gitattributes` does NOT exist:** create it at repo root with the single line:
       ```
       LICENSE text eol=lf
       ```

    2. **If `.gitattributes` exists AND already contains a line matching `^LICENSE text eol=lf$` exactly:** no edit needed (idempotent — leave the file untouched). The existing directive matches the required form; the task is satisfied.

    3. **If `.gitattributes` exists AND contains a `LICENSE` rule but it does NOT match `^LICENSE text eol=lf$` exactly:** FAIL with explicit mismatch error to the user (do NOT silently rewrite a different rule). The error message should read: `ABORT: .gitattributes carries a conflicting LICENSE rule. Expected: 'LICENSE text eol=lf'. Found: '<current line>'. Resolve manually and re-run.` This is a true human-action gate — never overwrite a pre-existing different LICENSE rule.

    4. **If `.gitattributes` exists WITHOUT any `LICENSE` rule:** append `LICENSE text eol=lf` as a NEW line at the end of the file. Use the Write tool: read the file, append the line preserving all existing directives, write back. The append MUST NOT remove or modify any pre-existing directive (no clobber).

    The `LICENSE text eol=lf` directive guarantees git-managed checkout on Windows / Mac / Linux all materialise LICENSE with LF line endings regardless of `core.autocrlf` config. This neutralises Windows CRLF auto-conversion that would otherwise inflate the byte count from 54 to 56 and break the byte-exact LICENSE assertion.

    **Pre-task baseline capture (required for C-5 no-clobber assertion):** Before any edit, record `wc -l .gitattributes` (or `0` if absent) and the full contents of `.gitattributes` in a temp variable / log line. After edit, the file must contain every pre-existing line PLUS the new `LICENSE text eol=lf` line (if it was appended). The acceptance criterion uses `wc -l` non-decrease as the no-clobber sentinel.

    **Edit 2 — create `LICENSE` at repo root (NOT `dydx-delivery/LICENSE` — per OPEN-Q23 and CHANGELIST.md:223).**

    Exact byte content (UTF-8, no BOM, LF line endings, trailing newline):
    ```
    All rights reserved.
    Not licensed for redistribution.
    ```

    Two lines + trailing newline. No copyright year. No frontmatter. No markdown headers. No additional whitespace. Use the Write tool with these exact two lines followed by a single newline at EOF.

    The acceptance criterion uses `diff <(printf 'All rights reserved.\nNot licensed for redistribution.\n') LICENSE` which expects the trailing newline. Byte count is exactly 54: `"All rights reserved."` (20 bytes) + LF (1) + `"Not licensed for redistribution."` (32 bytes) + LF (1) = 54.

    Per the W-03 fix, the `.gitattributes` directive is the source of truth for line endings — Windows checkouts that would otherwise convert LF → CRLF (and inflate byte count to 56) are pinned to LF.
  </action>
  <verify>
    <automated>test -f LICENSE &amp;&amp; test -f .gitattributes &amp;&amp; grep -qF 'LICENSE text eol=lf' .gitattributes &amp;&amp; ! grep -lU $'\r' LICENSE &amp;&amp; diff <(printf 'All rights reserved.\nNot licensed for redistribution.\n') LICENSE</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` exits 0 (file exists at repo root)
    - `test -f .gitattributes` exits 0 (file exists at repo root)
    - `grep -qF 'LICENSE text eol=lf' .gitattributes` exits 0 (LF directive present)
    - **C-5 idempotent-append:** `grep -c '^LICENSE text eol=lf$' .gitattributes` returns exactly `1` (the directive appears exactly once — neither missing nor duplicated)
    - **C-5 no-clobber:** `wc -l .gitattributes` does NOT decrease vs the pre-task baseline captured before the edit (the append preserved every pre-existing directive). Concretely: if the pre-task baseline was N lines, the post-task line count is at least N (equal if directive already existed; N+1 if appended).
    - **C-5 no-conflicting-LICENSE-rule:** `grep -E '^LICENSE ' .gitattributes | grep -vE '^LICENSE text eol=lf$' | wc -l` returns `0` (no other LICENSE rule exists; if a different one was present, the task aborts per Edit 1 decision tree)
    - `! test -f dydx-delivery/LICENSE` exits 0 (NOT placed inside dydx-delivery/ — wrong location)
    - `! grep -lU $'\r' LICENSE` exits 0 (fast-fail CR detection — no CR characters anywhere in LICENSE; this is the W-03 cross-platform sanity gate)
    - `diff <(printf 'All rights reserved.\nNot licensed for redistribution.\n') LICENSE` exits 0 (byte-exact match)
    - `wc -l LICENSE | awk '{print $1}'` returns `2` (exactly 2 lines per the trailing-newline convention)
    - `wc -c LICENSE | awk '{print $1}'` returns `54` (LF-only; "All rights reserved." = 20 + LF + "Not licensed for redistribution." = 32 + LF = 54 bytes)
    - `grep -qF "All rights reserved." LICENSE` exits 0
    - `grep -qF "Not licensed for redistribution." LICENSE` exits 0
    - `! grep -qi "MIT\|Apache\|GPL\|BSD\|copyright (c)" LICENSE` exits 0 (no permissive-licence boilerplate leaked in)
  </acceptance_criteria>
  <done>LICENSE exists at repo root with byte-exact OPEN-Q23 two-line boilerplate (54 bytes, LF-only). `.gitattributes` carries `LICENSE text eol=lf` directive guaranteeing LF on every checkout. Diff against expected printf output returns empty; CR-character grep returns no matches.</done>
</task>

<task type="auto">
  <name>Task 4: FOUND-09 — create 3 empty scaffold dirs (commands/, agents/, hooks/) at dydx-delivery/ root with .gitkeep</name>
  <files>dydx-delivery/commands/.gitkeep, dydx-delivery/agents/.gitkeep, dydx-delivery/hooks/.gitkeep</files>
  <read_first>
    - `.planning/DESIGN.md:284-329` (DESIGN-11 v2 folder layout — confirms scaffold dirs at `dydx-delivery/<dir>/`, siblings of `skills/`, NOT inside `.claude-plugin/`)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Scaffold Dirs (FOUND-09)" — confirms dydx-delivery/ root location + .gitkeep convention
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: dydx-delivery/{commands,agents,hooks}/.gitkeep"
    - `.planning/phases/05-foundations/05-CONTEXT.md` spec_lock DESIGN-04 (hooks/ scope = validate-frontmatter + bump-artefact-version ONLY — but those land v2.6; Phase 5 ships EMPTY); OPEN-Q21+Q21.1 (commands/ ships empty; refine.md lands v2.6); D-56 (agents/ ships empty; probe-related agents are deferred)
  </read_first>
  <action>
    Create three empty directories at the plugin root (NOT inside `.claude-plugin/`):
    - `dydx-delivery/commands/`
    - `dydx-delivery/agents/`
    - `dydx-delivery/hooks/`

    In each, write a zero-byte `.gitkeep` file so the empty dir is tracked by git.

    Use the Write tool with empty content `""` for each .gitkeep file (Write creates parent directories as needed).

    Each scaffold dir MUST contain ONLY `.gitkeep`. Do NOT add README.md / CLAUDE.md / placeholder skill files / hook stubs / agent stubs / refine.md / probe-related substance — every one of those is deferred to a later v2.x phase per DESIGN-04 + OPEN-Q21 + D-56.

    Do not create dirs inside `.claude-plugin/` — that's the manifest location, not the surface location. Per RESEARCH §3, scaffold dirs are siblings of `skills/`.
  </action>
  <verify>
    <automated>test -d dydx-delivery/commands &amp;&amp; test -d dydx-delivery/agents &amp;&amp; test -d dydx-delivery/hooks &amp;&amp; test -f dydx-delivery/commands/.gitkeep &amp;&amp; test -f dydx-delivery/agents/.gitkeep &amp;&amp; test -f dydx-delivery/hooks/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d dydx-delivery/commands` exits 0
    - `test -d dydx-delivery/agents` exits 0
    - `test -d dydx-delivery/hooks` exits 0
    - `test -f dydx-delivery/commands/.gitkeep` exits 0
    - `test -f dydx-delivery/agents/.gitkeep` exits 0
    - `test -f dydx-delivery/hooks/.gitkeep` exits 0
    - `wc -c dydx-delivery/commands/.gitkeep | awk '{print $1}'` returns `0` (zero-byte)
    - `wc -c dydx-delivery/agents/.gitkeep | awk '{print $1}'` returns `0`
    - `wc -c dydx-delivery/hooks/.gitkeep | awk '{print $1}'` returns `0`
    - `[ "$(ls -1 dydx-delivery/commands/ | wc -l)" = "1" ]` (exactly one entry: .gitkeep)
    - `[ "$(ls -1 dydx-delivery/agents/ | wc -l)" = "1" ]`
    - `[ "$(ls -1 dydx-delivery/hooks/ | wc -l)" = "1" ]`
    - **C-6 canonical empty-dir definition (Codex MEDIUM):** "empty" is defined as "contains exactly one file named `.gitkeep` and nothing else". Asserted as: `[ "$(ls -A dydx-delivery/commands/)" = ".gitkeep" ]` AND `[ "$(ls -A dydx-delivery/agents/)" = ".gitkeep" ]` AND `[ "$(ls -A dydx-delivery/hooks/)" = ".gitkeep" ]` — each exits 0. This is the canonical definition consumed by Plan 05-05 Task 4 (the structure-check script) so empty-dir semantics are consistent across both Wave 3 inline checks and the Wave 5 retrospective gate.
    - `[ "$(ls -1A dydx-delivery/commands/ | grep -v '\.gitkeep' | wc -l)" = "0" ]` (NO substantive files — only .gitkeep)
    - `[ "$(ls -1A dydx-delivery/agents/ | grep -v '\.gitkeep' | wc -l)" = "0" ]`
    - `[ "$(ls -1A dydx-delivery/hooks/ | grep -v '\.gitkeep' | wc -l)" = "0" ]`
    - `! test -d dydx-delivery/.claude-plugin/commands` exits 0 (NOT placed inside .claude-plugin/ — wrong location per DESIGN-11)
    - `! test -d dydx-delivery/.claude-plugin/agents` exits 0
    - `! test -d dydx-delivery/.claude-plugin/hooks` exits 0
  </acceptance_criteria>
  <done>Three empty scaffold dirs exist at dydx-delivery/ root, each with a zero-byte .gitkeep tracking marker and zero other content. Substantive content for these surfaces is deferred to v2.6 / SURF-01..03 per OPEN-Q21 + DESIGN-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON manifest edit -> downstream version-reading consumers | Every plugin loader, marketplace.json reader, and DESIGN-* doc that cross-references "2.0.0 target" consumes these fields |
| LICENSE byte-exact content -> legal/redistribution posture | LICENSE drift could imply different licensing terms than intended |
| Owner/author email field -> persisted across all manifests | UAT-3.1 lock; B.5 NO FIX; deviation = unauthorised mutation |
| `.gitattributes` LF pin -> cross-platform LICENSE byte-count invariant | Windows CRLF auto-conversion would otherwise break the 54-byte assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05W3-01 | Tampering | plugin.json + marketplace.json version drift | mitigate | Cross-manifest equality check in Task 2 acceptance criteria (`[ plugin.version = marketplace.plugins[0].version ]`) — failing the gate catches accidental skew (T-2 from security_threat_model) |
| T-05W3-02 | Tampering | Owner/author email accidental change to org domain | mitigate | UAT-3.1 LOCK; Task 1 + Task 2 acceptance criteria explicitly assert `jq -r '.author.email' == "jasonmichaelb@gmail.com"` and `jq -r '.owner.email' == "jasonmichaelb@gmail.com"`; any change fails the gate |
| T-05W3-03 | Information disclosure | LICENSE content drift to permissive boilerplate | mitigate | `diff <(printf ...)` byte-exact assertion in Task 3 acceptance criteria + negative grep against MIT/Apache/GPL/BSD/copyright (c) (T-4 from security_threat_model) |
| T-05W3-04 | Elevation of privilege | Scaffold dir substantive content leaks in | mitigate | Task 4 acceptance criteria forbid any file other than .gitkeep in the 3 dirs (ls-count = 1) |
| T-05W3-05 | Spoofing | mcpServers field accidentally added (cross-AI flag 5) | mitigate | Task 1 acceptance criterion `jq -e 'has("mcpServers")' returns false` — deferred to later phase explicitly |
| T-05W3-06 | Repudiation | Scaffold dirs placed inside .claude-plugin/ (wrong location) | mitigate | Task 4 acceptance criteria include negative `! test -d` against each `.claude-plugin/<dir>` location |
| T-05W3-07 | Tampering | Windows CRLF auto-conversion breaks LICENSE byte-exact assertion | mitigate | Task 3 creates/updates `.gitattributes` with `LICENSE text eol=lf` directive; acceptance criterion includes `! grep -lU $'\r' LICENSE` fast-fail CR detection (W-03 fix) |
| T-05W3-08 (T-6) | Tampering | `.gitattributes` overwrite clobbers an unrelated pre-existing rule, breaking Git behaviour elsewhere in the repo | mitigate | Task 3 Edit 1 decision tree mandates create-or-append (NEVER overwrite); acceptance criteria assert (a) exactly one `^LICENSE text eol=lf$` line, (b) `wc -l` non-decrease vs pre-task baseline, (c) no conflicting `LICENSE` rule survives. If a different LICENSE rule pre-exists, task aborts with explicit mismatch error rather than rewriting (C-5 fix per Codex MEDIUM) |
</threat_model>

<verification>
- plugin.json version equals marketplace.json plugins[0].version (cross-manifest consistency)
- plugin.json author.email equals marketplace.json owner.email (cross-manifest UAT-3.1 lock)
- plugin.json homepage equals marketplace.json plugins[0].homepage (B.6 symmetry)
- LICENSE byte-diff against expected printf returns empty (54 bytes, LF-only)
- `.gitattributes` carries `LICENSE text eol=lf` directive
- LICENSE contains zero CR characters (cross-platform LF invariant)
- 3 scaffold dirs each contain exactly one file (.gitkeep, zero bytes)
- No mcpServers field on plugin.json (cross-AI flag 5 deferred)
</verification>

<success_criteria>
- All 7 files modified or created (2 manifests + LICENSE + .gitattributes + 3 .gitkeep)
- All 4 tasks' acceptance criteria pass (jq + diff + test -d/-f + CR-grep)
- Wave 3 unblocks Wave 4 (connector matrix needs nothing from W3 but loadbearing for future phases)
- Phase 6 platform skills can land cleanly because plugin manifests + scaffold dirs are now v2-shaped
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundations/05-03-SUMMARY.md` recording:
- plugin.json before/after version (single-line diff)
- marketplace.json before/after 3-edit summary
- LICENSE byte count + diff against printf expectation (54 bytes, LF-only)
- `.gitattributes` directive landed (W-03 fix)
- Scaffold dir creation evidence (ls -1A output)
- Cross-AI flag 2 closure: B.6 landed in W3 inline with version sync
- Cross-AI flag 5 disposition: mcpServers deferred to v2.6 / SURF-01..03
</output>
