---
phase: 05-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-foundations/05-STATUS-SURVEY.md
  - dydx-delivery/references/safety-rules.md
  - dydx-delivery/references/stage-numbering.md
  - dydx-delivery/references/frontmatter-scheme.md
  - dydx-delivery/references/glossary.md
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-12]
tags: [foundations, canonical-references, safety-rules, stage-numbering, frontmatter, glossary, status-survey]

must_haves:
  truths:
    - "Per D-61, status-lifecycle survey runs BEFORE any canonical reference is authored"
    - "Per D-62, safety-rules.md lifts content verbatim from execute-tests/references/safety-rules.md and adds ONE Coda-sandbox clause"
    - "Per DESIGN-02, stage-numbering.md carries the verbatim old→new mapping table with 7 rows"
    - "Per DESIGN-01 + OPEN-Q15, frontmatter-scheme.md documents the PERMANENT lenient-mode contract for absent frontmatter_version"
    - "Per FOUND-04, glossary.md lifts DESIGN.md Appendix A and narrows to plugin-runtime terms"
  artifacts:
    - path: ".planning/phases/05-foundations/05-STATUS-SURVEY.md"
      provides: "FOUND-12 status-lifecycle re-survey deliverable"
      contains: "no drift"
    - path: "dydx-delivery/references/safety-rules.md"
      provides: "Plugin-level canonical hard rules (FOUND-01)"
      contains: "Coda writes ARE permitted"
    - path: "dydx-delivery/references/stage-numbering.md"
      provides: "Canonical stage-numbering scheme + old→new mapping (FOUND-02)"
      contains: "02_discovery_"
    - path: "dydx-delivery/references/frontmatter-scheme.md"
      provides: "Canonical frontmatter scheme + lenient-mode contract (FOUND-03)"
      contains: "frontmatter_version"
    - path: "dydx-delivery/references/glossary.md"
      provides: "Canonical glossary (FOUND-04)"
      contains: "## "
  key_links:
    - from: "dydx-delivery/references/safety-rules.md"
      to: ".planning/DESIGN.md:93-104"
      via: "DESIGN-03 cross-ref preamble"
      pattern: "DESIGN-03"
    - from: "dydx-delivery/references/frontmatter-scheme.md"
      to: ".planning/DESIGN.md:181-193 (DESIGN-08)"
      via: "Lenient-mode + migration co-existence narrative"
      pattern: "frontmatter_version"
    - from: ".planning/phases/05-foundations/05-STATUS-SURVEY.md"
      to: "dydx-delivery/references/frontmatter-scheme.md"
      via: "Status values surveyed feed the canonical status lifecycle"
      pattern: "status"
---

<objective>
Wave 1 of Phase 5: re-run the OPEN-Q16 status-lifecycle survey (FOUND-12) FIRST so any drift surfaces before scaffold authoring, then author the four plugin-level canonical reference files (FOUND-01..04) at `dydx-delivery/references/`. These four files are the bedrock that Wave 2 (skill repoint) and Wave 4 (connector matrix) and Phase 6 (platform skills) all point at.

Purpose: Establish single sources of truth for safety rules, stage numbering, frontmatter scheme, and glossary so the 7 existing v0.3.0 skills can be repointed at them in Wave 2 (collapsing the 4 AUDIT-05.1 hard-rules duplicates and fixing the CRIT-5 sandbox-block bug along the way).

Output: 1 survey artefact + 4 canonical reference files committed under `dydx-delivery/references/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-foundations/05-CONTEXT.md
@.planning/phases/05-foundations/05-RESEARCH.md
@.planning/phases/05-foundations/05-PATTERNS.md
@.planning/phases/05-foundations/05-VALIDATION.md
@.planning/DESIGN.md
@.planning/AUDIT.md
@.planning/OPEN-QUESTIONS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@dydx-delivery/skills/execute-tests/references/safety-rules.md

<interfaces>
<!-- Existing content to lift verbatim into the new plugin-level canonical safety-rules.md (per D-62). -->
<!-- Source: dydx-delivery/skills/execute-tests/references/safety-rules.md (101 lines). -->

From dydx-delivery/skills/execute-tests/references/safety-rules.md:1-3 (current preamble pattern to mirror):
```markdown
# execute-tests — Safety rules

> These rules are hardcoded into the runner. They override anything in the test plan. They are not configurable per run.
```

From dydx-delivery/skills/execute-tests/references/safety-rules.md:30-40 (the existing §3 block that needs the Coda-sandbox clause added):
```markdown
## 3. No destructive integrations outside scope

These actions are refused unless the test plan explicitly lists them in scope AND the integration is pointed at a sandbox / mock endpoint:

- Sending real emails to non-test recipients
- Posting to real Slack channels (only `#test-*` channels permitted)
- Triggering real Ziflow proofs (sandbox project only)
- Triggering real billing or invoice generation
- Calling third-party publishing APIs (social, CMS push)
```

The CRIT-5 fix is content-only — append a positive-authorisation bullet to this list inside the new plugin-level file (NOT here in the skill-internal file).

From .planning/DESIGN.md:78-89 (DESIGN-02 verbatim — old→new mapping for stage-numbering.md):
| v0.3.0 prefix | v2 prefix | Stage |
|---|---|---|
| 00_discovery_* | 02_discovery_* | Stage 2 |
| 01_sow_* | 03_sow_* | Stage 3 |
| 02_functional-spec_* | 04a_fnspec-platform_* | Stage 4a |
| 03_technical-spec_* | 05_techspec_* | Stage 5 |
| 04_build-prompt_* | 07a_build-prompt-dev_* | Stage 7a |
| test-plan_v* | 08b_test-plan_* | Stage 8b |
| results-YYYY-MM-DD_v* | 08d_test-results_* | Stage 8d |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FOUND-12 status-lifecycle survey re-run (Coda MCP probe, Option A; SKILL.md fallback Option B)</name>
  <files>.planning/phases/05-foundations/05-STATUS-SURVEY.md</files>
  <read_first>
    - `.planning/DESIGN.md:245-274` (DESIGN-08 Live status-lifecycle survey methodology — the analog whose structure this artefact mirrors)
    - `.planning/OPEN-QUESTIONS.md` row OPEN-Q16 (re-survey owner = Phase 5; baseline date = 2026-05-10)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Status Survey Re-run (FOUND-12, OPEN-Q16)" (lines ~521-548) — specifies Option A (Coda MCP `page_read` against Up & Up Group + VodafoneZiggo brain doc URLs) vs Option B (SKILL.md-only fallback per DESIGN-08)
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-61 (this survey runs FIRST, before any canonical reference authoring)
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: .planning/phases/05-foundations/05-STATUS-SURVEY.md (FOUND-12 deliverable)" — required content schema
    - User memory `reference_client_brain_coda_docs.md` (Up & Up Group + VodafoneZiggo are the 2 established Coda brain doc clients — Stage 11 mirror targets)
  </read_first>
  <action>
    Per D-61, this task runs FIRST in Wave 1 — before any other Wave 1 task touches the `dydx-delivery/references/` tree.

    Create `.planning/phases/05-foundations/05-STATUS-SURVEY.md` documenting an OPEN-Q16 re-survey of live `status:` values. Execute Option A (rigorous) first; fall back to Option B (DESIGN-08 SKILL.md-only methodology) only if Coda MCP read access is denied.

    **Option A execution steps (preferred):**
    1. Use Coda MCP `page_read` (tool name `mcp__plugin_context-mode_context-mode__ctx_*` or the Coda MCP `page_read` directly) against the 2 known brain doc URLs from `reference_client_brain_coda_docs.md`:
       - The Up & Up Group: `https://coda.io/d/M-C-Saatchi-Process-Automation_dITb4lVmQ67/The-Up-Up-Brain_sux7GT-N#_luUaEH88`
       - VodafoneZiggo: `https://coda.io/d/VodafoneZiggo_dUW9wD-EKrb/VFZ-Brain-Do-Not-Delete-speak-to-Jason_suHgD2Jd`
    2. For each brain doc, decode the URL with `url_convert action: decode`, then `page_read` each subpage with status-bearing content (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes per DESIGN-27 spoke layout).
    3. Extract every distinct `status:` literal value found (search for the YAML key `status:` and adjacent table-cell rows in Field Notes / Change History tables).
    4. Aggregate into a sorted distinct-values list.

    **Option B fallback (only if Coda permission denied on a brain doc):**
    Re-run DESIGN-08 methodology: `grep -rhE '^status:|status: \w+' dydx-delivery/skills/ --include='*.md'` to enumerate `status:` values in skill SKILL.md / template sources only. Log the no-access deviation in the survey conclusion.

    **Survey artefact content (write to `.planning/phases/05-foundations/05-STATUS-SURVEY.md`):**

    ```markdown
    # Phase 5 — Status-Lifecycle Survey (FOUND-12 / OPEN-Q16 re-run)

    **Survey date:** 2026-05-10
    **Baseline:** DESIGN-08 § "Live status-lifecycle survey" (`.planning/DESIGN.md:245-274`)
    **Methodology applied:** Option A (Coda MCP page_read) | Option B (SKILL.md fallback) [pick one]

    ## Methodology

    [If Option A used:]
    Coda MCP `page_read` against the 2 known brain doc URLs (Up & Up Group + VodafoneZiggo per `reference_client_brain_coda_docs.md`). Sampled subpages: <list>. Tokens extracted via grep for `status:` literal.

    [If Option B used:]
    DESIGN-08 SKILL.md-only fallback methodology. Reason for fallback: <documented Coda read-access denial OR other constraint>. Sampled sources: `dydx-delivery/skills/**/*.md`.

    ## Sample sources

    - <URL or path 1>
    - <URL or path 2>
    ...

    ## Distinct status: values found

    - `draft`
    - `client_review`
    - `approved`
    - <any additional values found>

    ## Reconciliation against canonical lifecycle

    Canonical v2 lifecycle (per DESIGN-01 / DESIGN-08): `{draft, client_review, approved, archived}`.

    | Found value | Canonical match? | Action |
    |---|---|---|
    | draft | yes | n/a |
    | client_review | yes | n/a |
    | approved | yes | n/a |
    | <new value> | no | adjudication required |

    ## Conclusion

    **no drift** — every value found maps cleanly to the canonical lifecycle.

    [OR if drift found:]

    **drift detected — adjudication required** — values <list> have no canonical match. Recommended adjudication: <add to lifecycle | retire | alias to existing>. User decision required before `frontmatter-scheme.md` finalises in Task 4.
    ```

    Commit the artefact. If `no drift` is concluded, Tasks 2-5 proceed. If `drift detected` is concluded, surface the deviation to the user before authoring Task 4 (`frontmatter-scheme.md`).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-foundations/05-STATUS-SURVEY.md &amp;&amp; grep -qE 'no drift|drift detected' .planning/phases/05-foundations/05-STATUS-SURVEY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-foundations/05-STATUS-SURVEY.md` exits 0
    - `grep -cE 'no drift|drift detected' .planning/phases/05-foundations/05-STATUS-SURVEY.md` returns at least 1
    - `grep -qF '## Methodology' .planning/phases/05-foundations/05-STATUS-SURVEY.md` exits 0
    - `grep -qF '## Distinct status: values found' .planning/phases/05-foundations/05-STATUS-SURVEY.md` exits 0
    - `grep -qF '## Reconciliation against canonical lifecycle' .planning/phases/05-foundations/05-STATUS-SURVEY.md` exits 0
    - `grep -qE 'Up &amp; Up Group|VodafoneZiggo|SKILL.md fallback' .planning/phases/05-foundations/05-STATUS-SURVEY.md` exits 0 (proves Option A sources OR Option B fallback explicitly documented)
    - `grep -qF 'DESIGN-08' .planning/phases/05-foundations/05-STATUS-SURVEY.md` exits 0 (baseline cross-ref)
  </acceptance_criteria>
  <done>Status-lifecycle survey artefact exists, identifies methodology, lists distinct status values, reconciles against canonical lifecycle, concludes drift state explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: FOUND-01 — author plugin-level canonical safety-rules.md (lift-and-fix per D-62 + CRIT-5)</name>
  <files>dydx-delivery/references/safety-rules.md</files>
  <read_first>
    - `dydx-delivery/skills/execute-tests/references/safety-rules.md` (all 101 lines — content lifted verbatim per D-62)
    - `.planning/DESIGN.md:93-104` (DESIGN-03 hard-rules SoT contract — the authorising decision)
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-62 (lift-and-fix mandate; do NOT author from scratch)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Sandbox-block bug" (lines ~198-222) — exact CRIT-5 fix text and where it lands
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: dydx-delivery/references/safety-rules.md (canonical-reference, lift-and-fix)" — heading + preamble + Coda-allowlist insertion patterns
    - `.planning/AUDIT.md:362-381` (AUDIT-05.1 — confirms execute-tests/references/safety-rules.md is the canonical SoT to lift from)
  </read_first>
  <action>
    Per D-62, this is a lift-and-fix operation: copy the entire content of `dydx-delivery/skills/execute-tests/references/safety-rules.md` (101 lines, 10 numbered H2 rules) to the new path `dydx-delivery/references/safety-rules.md`, then apply exactly THREE textual transformations.

    **Transformation 1 — rename the H1 heading and rewrite the preamble blockquote.**

    Replace lines 1-3 of the existing content:
    ```markdown
    # execute-tests — Safety rules

    > These rules are hardcoded into the runner. They override anything in the test plan. They are not configurable per run.
    ```

    With (verbatim — per D-62, D-59, and DESIGN-03):
    ```markdown
    # dYdX Delivery — Safety rules (canonical)

    > Canonical SoT for hard rules per DESIGN-03 (`.planning/DESIGN.md:93-104`). These rules are hardcoded into the runner. They override anything in the test plan. They are not configurable per run.
    >
    > **Override resolution:** skill loads canonical SoT → loads per-client `<Client> Brain/safety-overrides.yaml` overlay if present → applies overrides only to fields tagged `overridable: true`. Rule 1 (sandbox enforcement) and Rule 3 (no destructive integrations outside scope) are non-overridable.
    ```

    **Transformation 2 — add the CRIT-5 Coda-sandbox positive-authorisation bullet to Rule 3.**

    Inside `## 3. No destructive integrations outside scope`, append a new bullet to the existing list (which currently ends with "Calling third-party publishing APIs (social, CMS push)"). Add VERBATIM:
    ```markdown
    - Coda writes ARE permitted against the per-client sandbox doc/table identified in the test plan frontmatter `sandbox.coda_doc:` block. Coda writes targeting any other doc are refused (treated as "out_of_sandbox" per Rule 1).
    ```

    **Transformation 3 — preserve every other line VERBATIM.**

    Rules 1, 2, 4, 5, 6, 7, 8, 9, 10 and all their narrative content / Implementation blocks / Detection blocks / tables / code-block log examples copy across unchanged. Section/list ordering preserved. Markdown formatting preserved (table pipes, code fences, bold labels).

    Do NOT delete the source file `dydx-delivery/skills/execute-tests/references/safety-rules.md` — Wave 2 (plan 05-02) will collapse it to a one-line pointer per D-59. Wave 1 only WRITES the new canonical file; the source-file collapse is Wave 2 work.
  </action>
  <verify>
    <automated>test -f dydx-delivery/references/safety-rules.md &amp;&amp; grep -qF 'Coda writes ARE permitted' dydx-delivery/references/safety-rules.md &amp;&amp; grep -qF 'DESIGN-03' dydx-delivery/references/safety-rules.md &amp;&amp; grep -qE '^# dYdX Delivery — Safety rules \(canonical\)' dydx-delivery/references/safety-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dydx-delivery/references/safety-rules.md` exits 0
    - `grep -cE '^## [0-9]+\. ' dydx-delivery/references/safety-rules.md` returns at least 10 (all 10 numbered rules lifted)
    - `grep -qF 'Coda writes ARE permitted against the per-client sandbox doc/table identified in the test plan frontmatter `sandbox.coda_doc:` block.' dydx-delivery/references/safety-rules.md` exits 0 (CRIT-5 fix verbatim)
    - `grep -qF 'DESIGN-03 (`.planning/DESIGN.md:93-104`)' dydx-delivery/references/safety-rules.md` exits 0 (cross-ref preamble)
    - `grep -qE '^# dYdX Delivery — Safety rules \(canonical\)' dydx-delivery/references/safety-rules.md` exits 0 (new H1)
    - `grep -qF 'safety-overrides.yaml' dydx-delivery/references/safety-rules.md` exits 0 (DESIGN-03 overlay mechanism documented)
    - `grep -qF 'Override resolution' dydx-delivery/references/safety-rules.md` exits 0 (override resolution paragraph present)
    - `! grep -qE '^# execute-tests — Safety rules$' dydx-delivery/references/safety-rules.md` exits 0 (the old H1 did NOT carry over)
    - `wc -l dydx-delivery/references/safety-rules.md` reports at least 100 lines (source was 101; CRIT-5 adds 1, preamble rewrite delta-neutral)
    - `test -f dydx-delivery/skills/execute-tests/references/safety-rules.md` exits 0 (Wave 1 does NOT delete the source file — Wave 2 collapses it)
  </acceptance_criteria>
  <done>Plugin-level canonical safety-rules.md exists with all 10 lifted rules, the CRIT-5 Coda-sandbox clause appended verbatim to Rule 3, DESIGN-03 cross-ref preamble, and the new "dYdX Delivery — Safety rules (canonical)" H1. Source file remains untouched at its original location.</done>
</task>

<task type="auto">
  <name>Task 3: FOUND-02 — author canonical stage-numbering.md</name>
  <files>dydx-delivery/references/stage-numbering.md</files>
  <read_first>
    - `.planning/DESIGN.md:78-89` (DESIGN-02 — canonical mapping table; lift verbatim)
    - `.planning/DESIGN.md:341-364` (DESIGN-12 — 13-skill inventory; substages 4a/4b/7a/7b/8a-8d sourced here)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "stage-numbering.md (FOUND-02)" (lines ~279-298) — required content + lenient-mode cross-ref
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: dydx-delivery/references/stage-numbering.md" — preamble + table pattern
    - `.planning/phases/05-foundations/05-CONTEXT.md` D-58 (renumbering surface = stage-numbering.md + skill bodies + templates — this file is the canonical mapping authority)
    - `.planning/OPEN-QUESTIONS.md` row OPEN-Q15 (lenient-mode = PERMANENT)
  </read_first>
  <action>
    Author `dydx-delivery/references/stage-numbering.md` mirroring the canonical-reference preamble pattern established in Task 2.

    **Required content sections:**

    1. **H1 heading + DESIGN-02 cross-ref preamble.** Write:
       ```markdown
       # dYdX Delivery — Stage numbering (canonical)

       > Canonical SoT for the v2 stage-numbering scheme per DESIGN-02 (`.planning/DESIGN.md:78-89`). File-prefix is the stage number. Substages use letter suffixes (`4a/4b/7a/7b/8a-8d`). v0.3.0 in-flight artefacts NEVER renamed — readers tolerate old prefixes permanently per OPEN-Q15 decision (lenient-mode contract).
       ```

    2. **H2 `## Canonical v2 stages` table.** 11 rows, columns `Stage | File-prefix | Skill | Output filename pattern | Downstream consumer`. Use DESIGN-12 inventory verbatim. Example row format:
       ```markdown
       | Stage 2 | `02_` | `discovery-intake` | `02_discovery_v<N>.md` | `generate-sow` |
       ```

    3. **H2 `## Substages` section.** One H3 per substage cluster:
       - `### Stage 4 (split: 4a / 4b)` — fnspec-platform vs fnspec-integration per DESIGN-20
       - `### Stage 7 (split: 7a / 7b)` — dev build prompt vs implementation prompt per DESIGN-23
       - `### Stage 8 (split: 8a / 8b / 8c / 8d)` — provision-test-harness / test plan / UAT plan / execute-tests results per DESIGN-24

    4. **H2 `## Old → new mapping table (v0.3.0 → v2)`.** Verbatim from DESIGN-02 / RESEARCH.md §Stage Numbering:
       ```markdown
       | v0.3.0 prefix | v2 prefix | Stage | Notes |
       |---|---|---|---|
       | `00_discovery_*` | `02_discovery_*` | Stage 2 | discovery-intake MODIFIED |
       | `01_sow_*` | `03_sow_*` | Stage 3 | generate-sow UNCHANGED-structure |
       | `02_functional-spec_*` | `04a_fnspec-platform_*` | Stage 4a | SPLIT into 4a + 4b per DESIGN-20; legacy `generate-functional-spec` RETIRED |
       | `03_technical-spec_*` | `05_techspec_*` | Stage 5 | generate-technical-spec MODIFIED |
       | `04_build-prompt_*` | `07a_build-prompt-dev_*` | Stage 7a | generate-build-prompt MODIFIED (dev path) |
       | `test-plan_v*` | `08b_test-plan_*` | Stage 8b | path also moves to `<Client> Brain/test-bot/test_cases/` |
       | `results-YYYY-MM-DD_v*` | `08d_test-results_*` | Stage 8d | resolves AUDIT-05.5 [NEW] label collision |
       ```

    5. **H2 `## Lenient-mode policy (PERMANENT)`** — closing section cross-referencing OPEN-Q15 and DESIGN-08:
       ```markdown
       v0.3.0 artefact files already on disk in client folders are NEVER renamed. v2 readers tolerate old prefixes permanently — absent `frontmatter_version` field on an artefact means the reader assumes v0.3.0 conventions and accepts the old prefix. Migration is opt-in per Change Request; never bulk-rewritten. See DESIGN-08 (`.planning/DESIGN.md:181-193`) for the full migration co-existence contract.
       ```
  </action>
  <verify>
    <automated>test -f dydx-delivery/references/stage-numbering.md &amp;&amp; grep -qF "02_discovery_" dydx-delivery/references/stage-numbering.md &amp;&amp; grep -qF "04a_fnspec-platform_" dydx-delivery/references/stage-numbering.md &amp;&amp; grep -qF "DESIGN-02" dydx-delivery/references/stage-numbering.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qE "^# dYdX Delivery — Stage numbering \(canonical\)" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "DESIGN-02" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -cE "^## " dydx-delivery/references/stage-numbering.md` returns at least 4
    - `grep -qF "00_discovery_*" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "02_discovery_*" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "04a_fnspec-platform_*" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "07a_build-prompt-dev_*" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "08b_test-plan_*" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "08d_test-results_*" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "OPEN-Q15" dydx-delivery/references/stage-numbering.md` exits 0
    - `grep -qF "DESIGN-08" dydx-delivery/references/stage-numbering.md` exits 0
  </acceptance_criteria>
  <done>Canonical stage-numbering.md exists with DESIGN-02 cross-ref preamble, canonical v2 stage table, substage section, old to new mapping (7 rows verbatim from DESIGN-02), and lenient-mode policy section.</done>
</task>

<task type="auto">
  <name>Task 4: FOUND-03 — author canonical frontmatter-scheme.md</name>
  <files>dydx-delivery/references/frontmatter-scheme.md</files>
  <read_first>
    - `.planning/DESIGN.md:62-74` (DESIGN-01 — frontmatter scheme contract)
    - `.planning/DESIGN.md:147-158` (DESIGN-06 — approval-gate fields)
    - `.planning/DESIGN.md:181-193` (DESIGN-08 — migration co-existence + lenient-mode)
    - `.planning/OPEN-QUESTIONS.md` row OPEN-Q15 (lenient-mode is PERMANENT, not cutover-date-tied)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "frontmatter-scheme.md (FOUND-03)"
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: dydx-delivery/references/frontmatter-scheme.md"
    - `.planning/phases/05-foundations/05-STATUS-SURVEY.md` (output of Task 1)
    - `.planning/phases/05-foundations/05-CONTEXT.md` (spec_lock UAT-3.1, OPEN-Q15 lock)
  </read_first>
  <action>
    Author `dydx-delivery/references/frontmatter-scheme.md` covering DESIGN-01 + DESIGN-06 + DESIGN-08 (PERMANENT lenient-mode).

    **Required content sections:**

    1. **H1 + DESIGN-01 cross-ref preamble** — mirror Task 2/3 pattern:
       Heading line: `# dYdX Delivery — Frontmatter scheme (canonical)`
       Preamble blockquote naming DESIGN-01 (`.planning/DESIGN.md:62-74`), DESIGN-06 (`.planning/DESIGN.md:147-158`), DESIGN-08 (`.planning/DESIGN.md:181-193`), and OPEN-Q15 (lenient-mode PERMANENT).

    2. **H2 `## Status lifecycle`** — canonical `draft -> client_review -> approved -> archived` as ordered list AND state-machine arrows. Note `client_review` retained per the live status-lifecycle survey (cite `05-STATUS-SURVEY.md`).

    3. **H2 `## Field-name conventions`** — underscore-snake-case for keys (`based_on_kickoff`, `pipe_id`, `approved_by`, `approved_at`, `tier_claims_last_verified`); hyphen-kebab-case for file paths inside `based_on_*` values.

    4. **H2 `## Platform-gated identifiers`** — `pipe_id` only when `platform: pipefy`; `space_id` only when `platform: wrike`; `project_id` only when `platform: ziflow`.

    5. **H2 `## Canonical v2 artefact frontmatter`** — example YAML block (per RESEARCH.md / PATTERNS.md):
       ```yaml
       ---
       client: <CLIENT_NAME>
       feature: <feature-slug>
       platform: <pipefy | wrike | ziflow | other>
       version: 1
       status: draft | client_review | approved | archived
       based_on_technical_spec: 05_techspec_v<N>.md
       frontmatter_version: 2
       generated_at: <YYYY-MM-DD>
       approved_by: <human-name>            # required when status: approved
       approved_at: <ISO-8601 timestamp>    # required when status: approved
       sandbox:
         pipe_id: <id>                      # only when platform: pipefy
         space_id: <id>                     # only when platform: wrike
         project_id: <id>                   # only when platform: ziflow
         tenant: <name>
       ---
       ```

    6. **H2 `## Approval-gate fields (DESIGN-06)`** — `status: approved` writes MUST carry `approved_by: <human-name>` (NOT `Claude` / `AI` / `system`) + `approved_at: <ISO-8601 timestamp>`. Approval-gate hook (deferred to v2.6) refuses approved writes lacking these fields.

    7. **H2 `## Lenient-mode contract (PERMANENT)`** — copy this block verbatim:
       > v2 readers tolerate v0.3.0 frontmatter that is missing the `frontmatter_version` field. When `frontmatter_version` is absent, the reader assumes v0.3.0 conventions: do not raise on missing `archived` status / `approved_by` / `approved_at`. v0.3.0 in-flight artefacts are NEVER auto-flipped (per DESIGN-08); migration is opt-in per CR. This is not tied to a cutover date — it is the permanent reader contract per OPEN-Q15.

    8. **H2 `## Migration co-existence (DESIGN-08)`** — `client_review` retained in canonical lifecycle (live use in `generate-sow` today per DESIGN-08). No bulk rewriting. Cross-ref `stage-numbering.md` for old-to-new file-prefix tolerance.

    9. **H2 `## Status-survey result`** — single sentence cross-referencing `.planning/phases/05-foundations/05-STATUS-SURVEY.md` and stating the conclusion verbatim ("no drift" or "drift detected — see survey artefact").
  </action>
  <verify>
    <automated>test -f dydx-delivery/references/frontmatter-scheme.md &amp;&amp; grep -qF "frontmatter_version" dydx-delivery/references/frontmatter-scheme.md &amp;&amp; grep -qF "client_review" dydx-delivery/references/frontmatter-scheme.md &amp;&amp; grep -qiF "lenient" dydx-delivery/references/frontmatter-scheme.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qE "^# dYdX Delivery — Frontmatter scheme \(canonical\)" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "DESIGN-01" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "DESIGN-06" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "DESIGN-08" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "OPEN-Q15" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "draft" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "client_review" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "approved" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "archived" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "frontmatter_version: 2" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "approved_by" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "approved_at" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qiF "lenient" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qiF "permanent" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "pipe_id" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "space_id" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "project_id" dydx-delivery/references/frontmatter-scheme.md` exits 0
    - `grep -qF "05-STATUS-SURVEY.md" dydx-delivery/references/frontmatter-scheme.md` exits 0
  </acceptance_criteria>
  <done>Canonical frontmatter-scheme.md exists with status lifecycle, field-name conventions, platform-gated identifiers, canonical YAML example, approval-gate fields, PERMANENT lenient-mode contract, DESIGN-08 migration co-existence, and status-survey result cross-ref.</done>
</task>

<task type="auto">
  <name>Task 5: FOUND-04 — author canonical glossary.md (lift DESIGN.md Appendix A, narrow to plugin-runtime)</name>
  <files>dydx-delivery/references/glossary.md</files>
  <read_first>
    - `.planning/DESIGN.md` Appendix A (the 69-entry glossary from Phase 2 02-10 plan — source to lift)
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "glossary.md (FOUND-04)"
    - `.planning/phases/05-foundations/05-PATTERNS.md` § "NEW: dydx-delivery/references/glossary.md" — entry pattern + H2 grouping
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Q09 — Claude in Chrome canonical product naming" — adds `Claude for Chrome` glossary entry
    - `.planning/phases/05-foundations/05-RESEARCH.md` § "Q13 — Wrike host field source-of-truth" — adds `Wrike host field` glossary entry
  </read_first>
  <action>
    Author `dydx-delivery/references/glossary.md` by lifting `.planning/DESIGN.md` Appendix A and narrowing scope to **plugin-runtime terms only** (drop design-process-only entries like `structural-check.sh`, `gsd-verifier`, `gsd-planner`, SUMMARY/plan workflow terms).

    **Required content:**

    1. **H1 + cross-ref preamble** (mirror canonical-reference pattern). Use H1 `# dYdX Delivery — Glossary (canonical)`. Preamble notes narrowing from DESIGN.md Appendix A and adds the two Q09/Q13 entries.

    2. **H2 groupings (alphabetised within each):**
       - `## Pipeline / Stage terms` — Stage 1..11 + substages (4a/4b/7a/7b/8a-8d); `kickoff`, `discovery`, `SOW`, `fnspec-platform`, `fnspec-integration`, `techspec`, `cost estimate`, `build-prompt-dev`, `implementation-prompt`, `test bot`, `test plan`, `UAT plan`, `documentation`, `native-AI`, `sign-off`, `archive`.
       - `## Frontmatter terms` — `status` (with the 4 lifecycle values), `frontmatter_version`, `based_on_*`, `approved_by`, `approved_at`, `tier_claims_last_verified`, `native_ai_path`, `doc_published_at`, `last_diff_review_at`, `client`, `feature`, `platform`, `sandbox`.
       - `## Platform terms` — `Pipefy`, `Wrike`, `Ziflow`, `Coda`, `Miro`, `Google Workspace`, `Pipe`, `Space`, `Project (Ziflow)`, `paginate_all`, `wait_for_proof`, `Wrike host field` (NEW — Q13 — "Per-tenant Wrike API base URL parsed from the OAuth token response `host` field; persisted as canonical SoT at `<Client> Brain/00_HUB.md`; hardcoding `www.wrike.com` breaks multi-tenant per MOD-5."), `Claude for Chrome` (NEW — Q09 — "Anthropic browser extension; canonical product name per `claude.com/claude-for-chrome` marketing page; also `Claude in Chrome` per Help Center — terms interchangeable."), `Pipefy AI Agents`, `Wrike Copilot`, `Ziflow ReviewAI`.
       - `## Test-bot terms` — `client_state.yaml`, `sandbox_lock.yaml`, `harness_drift`, `tier-1 deterministic`, `tier-2 AI orchestrator`, `last_known_schema`, `targets_artefact`, `active | obsolete | quarantined`, `auth_switch_required`.
       - `## Plugin-surface terms` — `commands/`, `agents/`, `hooks/`, `.claude-plugin/`, `mcpServers`, `validate-frontmatter` hook, `bump-artefact-version` hook, `/dydx-refine-*` namespace.
       - `## Doc + sign-off terms` — `doc_type` enum (8 values), `<client_slug>__<project_slug>__<doc_type>__v<N>` filename pattern, `tone_lint`, Field Notes, brain-mirror Coda doc, `Documentation:` link in `00_HUB.md`.

    3. **Entry pattern** (per PATTERNS.md — Markdown definition style):
       `**Term** — Definition. Cross-ref \`<file>:<line>\` if applicable.`

    Skip terms that exist in DESIGN.md Appendix A but are NOT plugin-runtime (e.g., `gsd-planner`, `structural-check.sh`, `gsd-verifier`, `SUMMARY.md`). The narrowing is the differentiator — bar = "would a skill author at runtime need this term?".
  </action>
  <verify>
    <automated>test -f dydx-delivery/references/glossary.md &amp;&amp; grep -qF "Claude for Chrome" dydx-delivery/references/glossary.md &amp;&amp; grep -qF "Wrike host field" dydx-delivery/references/glossary.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dydx-delivery/references/glossary.md` exits 0
    - `grep -qE "^# dYdX Delivery — Glossary \(canonical\)" dydx-delivery/references/glossary.md` exits 0
    - `grep -cE "^## " dydx-delivery/references/glossary.md` returns at least 6
    - `grep -qF "Claude for Chrome" dydx-delivery/references/glossary.md` exits 0 (Q09 entry per FOUND-13)
    - `grep -qF "Wrike host field" dydx-delivery/references/glossary.md` exits 0 (Q13 entry per FOUND-13)
    - `grep -qF "paginate_all" dydx-delivery/references/glossary.md` exits 0
    - `grep -qF "wait_for_proof" dydx-delivery/references/glossary.md` exits 0
    - `grep -qF "frontmatter_version" dydx-delivery/references/glossary.md` exits 0
    - `grep -qF "native_ai_path" dydx-delivery/references/glossary.md` exits 0
    - `grep -qF "tier_claims_last_verified" dydx-delivery/references/glossary.md` exits 0
    - `! grep -q "structural-check.sh" dydx-delivery/references/glossary.md` exits 0 (design-process term EXCLUDED per narrowing)
    - `! grep -q "gsd-planner" dydx-delivery/references/glossary.md` exits 0 (design-process term EXCLUDED)
    - `! grep -q "gsd-verifier" dydx-delivery/references/glossary.md` exits 0 (design-process term EXCLUDED)
  </acceptance_criteria>
  <done>Canonical glossary.md exists with 6 H2 groupings, plugin-runtime terms only, and the two FOUND-13-research entries (`Claude for Chrome` + `Wrike host field`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 W1 author -> canonical reference content | Authoritative content lands at `dydx-delivery/references/*.md`; every later phase reads from these |
| Coda MCP read -> status-survey artefact | Live brain doc data sampled into local artefact; redaction applied (status field counts ONLY) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05W1-01 | Tampering | `dydx-delivery/references/safety-rules.md` | mitigate | D-62 lift-and-fix preserves 10 existing rules verbatim; Task 2 acceptance criteria require at least 10 numbered H2 rules and the CRIT-5 Coda clause verbatim; deviation from lifted content fails grep gate |
| T-05W1-02 | Information disclosure | `.planning/phases/05-foundations/05-STATUS-SURVEY.md` | mitigate | Survey records ONLY distinct `status:` field values and source URLs — never tabular content from Up & Up Group / VodafoneZiggo Field Notes / Change History rows; redaction explicit in Task 1 action (T-5 from security_threat_model) |
| T-05W1-03 | Repudiation | Sandbox-block bug fix scope | mitigate | The Coda-sandbox clause text is byte-exact in Task 2 action and pinned by grep `-qF` in acceptance criteria; reviewer can diff the line to confirm exactly what was authorised (T-1 from security_threat_model) |
| T-05W1-04 | Spoofing | Canonical-reference H1 headings | accept | Low risk — H1 headings are reader-facing labels, not auth tokens; grep gates fail if H1 drifts |
| T-05W1-05 | Elevation of privilege | Sandbox allowlist over-permissive (CRIT-5 fix scope creep) | mitigate | Task 2 specifies the Coda clause as a SINGLE positive-authorisation bullet scoped to `sandbox.coda_doc:` block in test plan frontmatter — no broader Coda access granted; non-overridable per DESIGN-03 override resolution (T-1 from security_threat_model) |
</threat_model>

<verification>
All 5 Wave 1 tasks complete with grep/jq/file-exists checks passing. Specifically:
1. `test -f` succeeds for all 5 NEW files (1 survey + 4 references)
2. No old H1 `# execute-tests — Safety rules` leaks into the new plugin-level canonical
3. Survey artefact carries a `no drift` OR `drift detected` conclusion
4. All four canonical references carry their `DESIGN-NN` cross-ref preamble
5. Glossary excludes design-process terms (negative grep on `gsd-planner` / `structural-check.sh`)

Wave 1 does NOT yet run the central `phase5-structure-check.sh` — that script is authored in Wave 5 per Wave 0 Requirements (`05-VALIDATION.md`). Wave 1 uses inline grep/jq/test commands per acceptance criteria.
</verification>

<success_criteria>
- All 5 files committed at correct paths (1 in `.planning/phases/05-foundations/`, 4 in `dydx-delivery/references/`)
- Per-task `<acceptance_criteria>` checks pass (grep/jq/file-exists)
- Survey methodology (Option A vs Option B fallback) explicitly documented
- Canonical references trace to DESIGN-01/02/03 cross-refs verbatim
- Wave 1 unblocks Wave 2 (skill repoint) and Wave 4 (connector matrix references `safety-rules.md` Rule 1 sandbox-enforcement narrative)
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundations/05-01-SUMMARY.md` recording:
- Survey methodology used (Option A success / Option B fallback) + conclusion
- Any drift adjudication required (and resulting frontmatter-scheme.md adjustment)
- Lines-of-content in each of the 4 canonical references
- Cross-refs established for Wave 2 (skill repoint targets `safety-rules.md` and `stage-numbering.md`)
</output>
