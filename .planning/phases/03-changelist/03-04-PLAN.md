---
plan_id: 03-04
phase: 03-changelist
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
requirements: [CHANGE-02]
files_modified:
  - .planning/CHANGELIST.md
  - .planning/phases/03-changelist/03-04-SUMMARY.md
autonomous: true
tags: [changelist, change-02, appendix-a, per-skill-delta, design-only]

must_haves:
  truths:
    - "`## Appendix A: Per-skill delta matrix (CHANGE-02)` H2 section populated with single matrix, one row per v2 skill (>= 15 rows: 13 v2 end-state skills + minimum 2 RETIRED rows = 15 minimum per CONTEXT specifics)."
    - "Matrix columns are EXACT per D-39: `v0.3.0 origin | v2 name | Status | Change | Introduced (phase) | DESIGN`"
    - "Status column uses ONLY values from the closed enum: `NEW / NEW (split) / MODIFIED / RETIRED / RETIRED → SPLIT / UNCHANGED`"
    - "Rows ordered by introducing milestone (v2.1 first → v2.6 last), within milestone by introducing phase, within phase by status (NEW → MODIFIED → RETIRED → UNCHANGED)"
    - "Bottom totals paragraph at end of Appendix A states ONE authoritative count model per cross-AI C4: 13-skill v2 universe per DESIGN-12 (10 stage-skills + 3 platform-skills) + 6 MODIFIED carry-forwards + 1 RETIRED → SPLIT row = 20 total rows. NEW = 13; MODIFIED = 6; RETIRED → SPLIT = 1."
    - "Each `Change` cell carries 1-3 short bullets per D-40 (cited-bullets); each bullet ends with `(per DESIGN-NN)` cite where applicable"
    - "Structural-check assertion #9 (Appendix A row count >= 15) PASSES after this plan"
  artifacts:
    - path: ".planning/CHANGELIST.md"
      provides: "Appendix A — single matrix mapping every v2 skill to v0.3.0 origin / Status / Change / Introduced phase / DESIGN-NN section"
      contains: "15+ row matrix; column headers per D-39; closed-enum Status values; cited-bullet Change cells per D-40; ordered by milestone → phase → status; bottom totals row reconciling against DESIGN-12 categorical breakdown"
  key_links:
    - from: "Appendix A 'Introduced (phase)' column"
      to: "CHANGELIST.md § Phase 1..9 mini-tables 'Skills introduced/modified' cells (populated by 03-02 + 03-03)"
      via: "Each skill's Introduced phase value matches the phase mini-table that names it"
      pattern: "skill-name back-pointer"
    - from: "Appendix A 'DESIGN' column"
      to: ".planning/DESIGN.md per-skill DESIGN-NN sections"
      via: "DESIGN-12 (inventory matrix) + DESIGN-14/15/16 (platform skills) + DESIGN-17..27 (stage skills)"
      pattern: "DESIGN-NN cross-cite per row"
    - from: "Appendix A v0.3.0 origin column"
      to: ".planning/AUDIT.md §AUDIT-01 (per-skill brittleness inventory) + §AUDIT-04 (missing artefacts)"
      via: "v0.3.0 file paths cited via `file:line` per D-14 carried"
      pattern: "v0.3.0 source-of-truth grounding"
---

<objective>
Wave 4: populate `## Appendix A: Per-skill delta matrix (CHANGE-02)` in `.planning/CHANGELIST.md` with the single matrix per D-39 — one row per v2 skill — that inventories the per-skill v0.3.0 → v2 delta. Implements CHANGE-02 (per-skill NEW/MODIFIED/RETIRED/UNCHANGED tagging with introducing phase from the 9-phase plan committed in CHANGE-01).

**Purpose:** Ground the v2 skill universe in a single reviewer-scannable matrix. Reader asking "is skill X changing in v2?" lands here. The matrix ties Phase 1..9 mini-tables (which cite "Skills introduced/modified") back to the per-skill change description, and ties forward to DESIGN.md per-skill sections for the contract detail.

**Output:** `## Appendix A: Per-skill delta matrix (CHANGE-02)` H2 section populated with a 15+ row matrix per D-39 + bottom totals row + framing paragraph. After this plan, structural-check assertion #9 (Appendix A row count >= 15) PASSES; assertions #8 (Appendix B sentinels) and #10 (Appendix E bullets) remain failing for Plans 03-05 and 03-07.

**Skill inventory source:** DESIGN.md §"v2 skill inventory" matrix grounds the v2 13-skill universe per DESIGN-12 (10 stage-skills + 3 platform-skills). v0.3.0 contributed 7 skills (per AUDIT-01); 6 carry-forward as MODIFIED rows; 1 retires (`generate-functional-spec`) as RETIRED → SPLIT row. Total Appendix A rows = 13 (NEW v2 universe) + 6 (MODIFIED carry-forwards) + 1 (RETIRED → SPLIT) = 20 rows. Per CONTEXT specifics floor of 15, the actual count of 20 gives margin. Per cross-AI C4 (codex MEDIUM): single authoritative count model — no earlier 13/15/16/19/20 framing that overlapped framing across CONTEXT, DESIGN, and Appendix totals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/03-changelist/03-CONTEXT.md
@.planning/phases/03-changelist/03-01-PLAN.md

<interfaces>
<!-- Single matrix in Appendix A per D-39. Column headers + Status enum LOCKED. -->
<!-- Row ordering rule per D-39: by milestone (v2.1 first → v2.6 last), within milestone by introducing phase, within phase by status (NEW → MODIFIED → RETIRED → UNCHANGED). -->
<!-- Cited-bullets format per D-40: each `Change` cell = 1-3 short bullets, each ending with `(per DESIGN-NN)` cite where applicable. -->

The matrix shape:

```
| v0.3.0 origin | v2 name | Status | Change | Introduced (phase) | DESIGN |
|---|---|---|---|---|---|
| ... | ... | NEW/MODIFIED/RETIRED/etc | (cited bullets) | Phase N (v2.X) | DESIGN-NN |
```

**Authoritative skill enumeration (from DESIGN.md §"v2 skill inventory" matrix lines 343..363 + AUDIT.md §AUDIT-01 v0.3.0 inventory):**

v0.3.0 inventory (per AUDIT-01.1..01.7) — 7 skills:
1. `discovery-intake/`
2. `generate-sow/`
3. `generate-functional-spec/`
4. `generate-technical-spec/`
5. `generate-test-plan/`
6. `generate-build-prompt/`
7. `execute-tests/`

v2 end-state inventory (per DESIGN-12 reconciliation = 15 v2 skills shipped, with `generate-functional-spec` retired):

| # | Skill | Status | Stage | v0.3.0 origin | Introduced (phase) | DESIGN |
|---|---|---|---|---|---|---|
| 1 | `kickoff-capture/` | NEW | 1 | none | Phase 3 (v2.2) | DESIGN-17 |
| 2 | `discovery-intake/` | MODIFIED | 2 | `dydx-delivery/skills/discovery-intake/SKILL.md` (per AUDIT-01.1) | Phase 3 (v2.2) | DESIGN-18 |
| 3 | `generate-sow/` | MODIFIED | 3 | `dydx-delivery/skills/generate-sow/SKILL.md` (per AUDIT-01.2) | Phase 3 (v2.2) | DESIGN-19 |
| 4 | `generate-fnspec-platform/` | NEW (split) | 4a | derived from RETIRED `generate-functional-spec` (per AUDIT-01.3 + DESIGN-20) | Phase 3 (v2.2) | DESIGN-20 |
| 5 | `generate-fnspec-integration/` | NEW (split) | 4b | derived from RETIRED `generate-functional-spec` (per AUDIT-01.3 + DESIGN-20) | Phase 3 (v2.2) | DESIGN-20 |
| 6 | `generate-technical-spec/` | MODIFIED | 5 | `dydx-delivery/skills/generate-technical-spec/SKILL.md` (per AUDIT-01.4) | Phase 4 (v2.3) | DESIGN-21 |
| 7 | `generate-cost-estimate/` | NEW | 6 | none | Phase 4 (v2.3) | DESIGN-22 |
| 8 | `generate-build-prompt/` | MODIFIED | 7a | `dydx-delivery/skills/generate-build-prompt/SKILL.md` (per AUDIT-01.6) | Phase 4 (v2.3) | DESIGN-23 |
| 9 | `generate-implementation-prompt/` | NEW | 7b | none | Phase 4 (v2.3) | DESIGN-23 |
| 10 | `provision-test-harness/` | NEW | 8a | none | Phase 5 (v2.4) | DESIGN-24 |
| 11 | `generate-test-plan/` | MODIFIED | 8b | `dydx-delivery/skills/generate-test-plan/SKILL.md` (per AUDIT-01.5) | Phase 5 (v2.4) | DESIGN-24 |
| 12 | `generate-uat-plan/` | NEW | 8c | none | Phase 5 (v2.4) | DESIGN-24 |
| 13 | `execute-tests/` | MODIFIED | 8d | `dydx-delivery/skills/execute-tests/SKILL.md` (per AUDIT-01.7) | Phase 5 (v2.4) | DESIGN-24 |
| 14 | `update-documentation/` | NEW | 9 | none | Phase 6 (v2.5) | DESIGN-25 |
| 15 | `push-native-ai-knowledge/` | NEW | 10 | none | Phase 7 (v2.5) | DESIGN-26 |
| 16 | `sign-off-and-archive/` | NEW | 11 | none | Phase 8 (v2.6) | DESIGN-27 |
| 17 | `platform-pipefy/` | NEW | n/a | referenced-but-missing (per AUDIT-04.1) | Phase 2 (v2.1) | DESIGN-14 |
| 18 | `platform-wrike/` | NEW | n/a | referenced-but-missing (per AUDIT-04.1) | Phase 2 (v2.1) | DESIGN-15 |
| 19 | `platform-ziflow/` | NEW | n/a | none in v0.3.0 | Phase 2 (v2.1) | DESIGN-16 |

Plus 1 RETIRED row:
| 20 | `generate-functional-spec/` (per AUDIT-01.3) | (none — replaced by Stage 4a + 4b split) | RETIRED → SPLIT | per DESIGN-20 | (Phase 3 v2.2 — replacement landing) | DESIGN-20 |

**Final Appendix A row count: 20 (>= 15 floor) — 16 v2 stage skills (1-16) + 3 platform skills (17-19) + 1 RETIRED (20).**

**Required ordering inside the matrix (per D-39):** group by introducing milestone (v2.1 → v2.6), within milestone by introducing phase, within phase by status (NEW → NEW (split) → MODIFIED → UNCHANGED → RETIRED → SPLIT). Resulting order (per cross-AI C2 — codex HIGH — Status enum shrinks back to D-39's 6-value closed list; platform-skill framing moves into the Change column per D-40):

1. **v2.1 block:**
   - Phase 1: (no skills introduced — Phase 1 modifies existing 7 skills only by repointing at canonical refs; not new rows in Appendix A — tracked as Phase 1 effect in the per-Stage-skill MODIFIED rows below).
   - Phase 2 (3 NEW platform skills — Status `NEW` per D-39 closed enum; platform framing in Change column per cross-AI C2):
     - Row 17: `platform-pipefy/` — NEW
     - Row 18: `platform-wrike/` — NEW
     - Row 19: `platform-ziflow/` — NEW
2. **v2.2 block:**
   - Phase 3:
     - Row 1: `kickoff-capture/` — NEW
     - Row 4: `generate-fnspec-platform/` — NEW (split)
     - Row 5: `generate-fnspec-integration/` — NEW (split)
     - Row 2: `discovery-intake/` — MODIFIED
     - Row 3: `generate-sow/` — MODIFIED (behaviour-modified per cross-AI C2; structural-vs-behaviour distinction lives in Change cell per D-40)
     - Row 20: `generate-functional-spec/` — RETIRED → SPLIT
3. **v2.3 block:**
   - Phase 4:
     - Row 7: `generate-cost-estimate/` — NEW
     - Row 9: `generate-implementation-prompt/` — NEW
     - Row 6: `generate-technical-spec/` — MODIFIED
     - Row 8: `generate-build-prompt/` — MODIFIED
4. **v2.4 block:**
   - Phase 5:
     - Row 10: `provision-test-harness/` — NEW
     - Row 12: `generate-uat-plan/` — NEW
     - Row 11: `generate-test-plan/` — MODIFIED
     - Row 13: `execute-tests/` — MODIFIED
5. **v2.5 block:**
   - Phase 6:
     - Row 14: `update-documentation/` — NEW
   - Phase 7:
     - Row 15: `push-native-ai-knowledge/` — NEW
6. **v2.6 block:**
   - Phase 8:
     - Row 16: `sign-off-and-archive/` — NEW
   - Phase 9: (no skills — Phase 9 ships plugin surfaces only).

**Bottom totals paragraph (per cross-AI C4 — codex MEDIUM — single authoritative count model):** NEW = 13 (10 stage-skill NEW + 3 platform-skill NEW per DESIGN-12 categorical breakdown). MODIFIED = 6 (`discovery-intake` + `generate-sow` + `generate-technical-spec` + `generate-build-prompt` + `generate-test-plan` + `execute-tests`). RETIRED → SPLIT = 1 (`generate-functional-spec`). Sum = 20 total Appendix A rows = 13 v2 universe per DESIGN-12 (10 stage-skills + 3 platform-skills) + 6 MODIFIED v0.3.0 carry-forwards + 1 RETIRED → SPLIT row tracking the v0.3.0 skill no longer present in v2. RETIRED → SPLIT row cites NEW (split) replacements above (rows 4, 5). The v2 end-state ship count is 13 skills (per DESIGN-12); the 6 MODIFIED rows are upgrades to skills inside the 13 universe; the RETIRED row ships nothing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Appendix A — 20-row per-skill delta matrix + framing paragraph + bottom totals reconciliation</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Appendix A H2 anchor present from 03-01; Phase 1..9 mini-tables populated by 03-02 + 03-03; Skills cells reference Appendix A rows that this plan creates)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Per-skill delta presentation (CHANGE-02 → Appendix A)" D-39 (column headers + Status enum + ordering rule); §"Skill-delta tagging granularity" D-40 (cited-bullets format)
    - .planning/DESIGN.md §"v2 skill inventory" — 19-row inventory matrix (lines 343..363) — source for Appendix A row content
    - .planning/DESIGN.md §"v2 skill inventory" reconciliation note (lines 339, 365..369) — explains 13/15/16 categorical-vs-actual reconciliation
    - .planning/AUDIT.md §AUDIT-01.1..01.7 — v0.3.0 origin paths for the 7 existing skills (cited via `file:line` per D-14 carried)
    - .planning/AUDIT.md §AUDIT-04.1 — referenced-but-missing platform skills (input to platform-skill v0.3.0 origin = "referenced-but-missing")
    - .planning/research/SUMMARY.md §"Architecture Approach" — confirms 13-skill v2 framing for the totals row
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate `## Appendix A: Per-skill delta matrix (CHANGE-02)` H2 section IN PLACE (REPLACE the populated-by note with the matrix content).

    **Structure (in this exact order under the H2):**

    1. **Framing paragraph** (1-2 sentences):
       `This appendix inventories every skill in the v2 universe — every existing v0.3.0 skill tagged \`NEW / MODIFIED / RETIRED / RETIRED → SPLIT / UNCHANGED\` with the change description, and every NEW v2 skill tagged with its introducing phase. Single source of truth for "is skill X changing in v2"; the matrix ties forward to \`.planning/DESIGN.md\` per-skill sections via the DESIGN column and back to the 9-phase build plan via the Introduced (phase) column.`

    2. **Matrix** with EXACT D-39 column headers and ordered per the D-39 rule (milestone → phase → status). Use this exact column header row + alignment row:

       ```
       | v0.3.0 origin | v2 name | Status | Change | Introduced (phase) | DESIGN |
       |---|---|---|---|---|---|
       ```

       Then the 20 data rows in the order specified in `<interfaces>` block. Each `Change` cell carries 1-3 short bullets per D-40, each ending with `(per DESIGN-NN)` cite where applicable. Use `<br>` for in-cell newlines between bullets (markdown-table soft-break idiom — keeps the row a single line in markdown source for grep regularity).

       Required Status enum values (closed list per D-39 — per cross-AI C2 codex HIGH, no widening): use ONLY `NEW`, `NEW (split)`, `MODIFIED`, `UNCHANGED`, `RETIRED`, `RETIRED → SPLIT`. Platform-skill framing moves into the Change column per D-40 (cited bullets carry the `Platform-knowledge skill ...` prefix).

       **Row content (verbatim where possible — content sourced from DESIGN.md inventory matrix):**

       v2.1 block (Phase 2, 3 platform skills — Status `NEW` per D-39 closed enum; platform framing in Change column per cross-AI C2):
       - Row 17 (`platform-pipefy/`): v0.3.0 origin = `referenced-but-missing (per .planning/AUDIT.md §AUDIT-04.1)`; v2 name = `\`platform-pipefy/\``; Status = `NEW`; Change = 3 bullets — (a) `Platform-knowledge skill (sibling-of-stage shape per cross-AI C2) — internalises platform knowledge loaded by \`platform: pipefy\` frontmatter (per DESIGN-14)`; (b) `Carries 5-file \`references/\` shape: api-contract / native-ai-inventory / knowledge-ingestion / client-shape-gotchas / vocabulary (per DESIGN-14)`; (c) `Provides \`paginate_all\` helper for GraphQL cursor pagination (per DESIGN-14 / MOD-4 prevention)`; Introduced (phase) = `Phase 2 (v2.1)`; DESIGN = `DESIGN-14`.
       - Row 18 (`platform-wrike/`): v0.3.0 origin = `referenced-but-missing (per .planning/AUDIT.md §AUDIT-04.1)`; v2 name = `\`platform-wrike/\``; Status = `NEW`; Change = 3 bullets — (a) `Platform-knowledge skill (sibling-of-stage shape per cross-AI C2) — internalises Wrike platform knowledge with same 5-file references shape (per DESIGN-15)`; (b) `Persists \`host\` from OAuth token response (NOT hardcoded \`www.wrike.com\` — per MOD-5 prevention)`; (c) `Native-AI matrix grounded in Wrike Copilot + 16 MCP tools (per DESIGN-15 + RESEARCH.md FEATURES)`; Introduced (phase) = `Phase 2 (v2.1)`; DESIGN = `DESIGN-15`.
       - Row 19 (`platform-ziflow/`): v0.3.0 origin = `none in v0.3.0`; v2 name = `\`platform-ziflow/\``; Status = `NEW`; Change = 3 bullets — (a) `Platform-knowledge skill (sibling-of-stage shape per cross-AI C2) — new platform knowledge surface; same 5-file references shape (per DESIGN-16)`; (b) `Provides \`wait_for_proof\` helper for read-after-create eventual consistency (per DESIGN-16 / MOD-6 prevention)`; (c) `Native-AI matrix grounded in Ziflow ReviewAI Checklists Public Preview (per DESIGN-16)`; Introduced (phase) = `Phase 2 (v2.1)`; DESIGN = `DESIGN-16`.

       v2.2 block (Phase 3 — 6 rows: NEW → NEW (split) ×2 → MODIFIED → UNCHANGED → RETIRED → SPLIT):
       - Row 1 (`kickoff-capture/`): v0.3.0 origin = `none`; v2 name = `\`kickoff-capture/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 1 dual-branch artefact capture from meeting notes / Miro / Field Notes (per DESIGN-17)`; (b) `Field Notes triage default \`processed_at IS NULL\` — never auto-merges (per DESIGN-17 / MOD-8 prevention)`; (c) `Auto-classifies inputs into kickoff template sections with explicit "unknown" markers (per DESIGN-17)`; Introduced (phase) = `Phase 3 (v2.2)`; DESIGN = `DESIGN-17`.
       - Row 4 (`generate-fnspec-platform/`): v0.3.0 origin = `derived from RETIRED \`generate-functional-spec\` (per .planning/AUDIT.md §AUDIT-01.3 + DESIGN-20)`; v2 name = `\`generate-fnspec-platform/\``; Status = `NEW (split)`; Change = 3 bullets — (a) `Stage 4a — platform-only fnspec; per-requirement \`delivery: native-ai \| api\` tagging is the routing key for downstream Stages 5/6/7b/10 (per DESIGN-20)`; (b) `Per-platform capability matrix as classifier input (per DESIGN-20 + DESIGN-14/15/16)`; (c) `Either spec optional for single-track projects (per DESIGN-20)`; Introduced (phase) = `Phase 3 (v2.2)`; DESIGN = `DESIGN-20`.
       - Row 5 (`generate-fnspec-integration/`): v0.3.0 origin = `derived from RETIRED \`generate-functional-spec\` (per .planning/AUDIT.md §AUDIT-01.3 + DESIGN-20)`; v2 name = `\`generate-fnspec-integration/\``; Status = `NEW (split)`; Change = 3 bullets — (a) `Stage 4b — integration-only fnspec; cross-spec consistency check vs 4a (per DESIGN-20)`; (b) `Skip if no integration work (per DESIGN-20 — single-track shape)`; (c) `Tags every requirement with \`delivery: native-ai \| api\` mirroring Stage 4a (per DESIGN-20)`; Introduced (phase) = `Phase 3 (v2.2)`; DESIGN = `DESIGN-20`.
       - Row 2 (`discovery-intake/`): v0.3.0 origin = `\`dydx-delivery/skills/discovery-intake/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.1)`; v2 name = `\`discovery-intake/\``; Status = `MODIFIED`; Change = 3 bullets — (a) `Consumes \`01_kickoff_v*\` artefact (skip raw-notes mode per DESIGN-18)`; (b) `Skips entire stage when kickoff produced draft SOW (per DESIGN-18)`; (c) `Same template structure as v0.3.0 otherwise (per DESIGN-18)`; Introduced (phase) = `Phase 3 (v2.2)`; DESIGN = `DESIGN-18`.
       - Row 3 (`generate-sow/`): v0.3.0 origin = `\`dydx-delivery/skills/generate-sow/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.2)`; v2 name = `\`generate-sow/\``; Status = `MODIFIED`; Change = 2 bullets — (a) `Structurally unchanged from v0.3.0 / behaviour-modified per cross-AI C2 — single SOW covers platform AND integration (per DESIGN-19)`; (b) `Status lifecycle locked to canonical scheme: \`draft → client_review → approved → archived\` (\`client_review\` retained per DESIGN-08 + AUDIT.md §AUDIT-01.2 — sole skill carrying \`client_review\` in v0.3.0)`; Introduced (phase) = `Phase 3 (v2.2)`; DESIGN = `DESIGN-19`.
       - Row 20 (`generate-functional-spec/`): v0.3.0 origin = `\`dydx-delivery/skills/generate-functional-spec/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.3)`; v2 name = `(none — replaced by Stage 4a + 4b split)`; Status = `RETIRED → SPLIT`; Change = 2 bullets — (a) `Single-fnspec-for-everything legacy v0.3.0 shape retired in favour of Stage 4a / Stage 4b split (per DESIGN-20)`; (b) `v0.3.0 artefacts using old skill name (\`02_functional-spec_v*.md\`) remain readable per DESIGN-08 lenient mode; CR-driven opt-in upgrades to new names (\`04a_fnspec-platform_v*.md\` / \`04b_fnspec-integration_v*.md\` per DESIGN-02)`; Introduced (phase) = `Phase 3 (v2.2 — replacement landing)`; DESIGN = `DESIGN-20`.

       v2.3 block (Phase 4 — 4 rows: NEW ×2 → MODIFIED ×2):
       - Row 7 (`generate-cost-estimate/`): v0.3.0 origin = `none`; v2 name = `\`generate-cost-estimate/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 6 cost estimate via Coda \`rows/upsert\` with \`keyColumns\` for idempotency, \`mutationStatus\` polling, rate-limit at 4 req/10s (per DESIGN-22 + CRIT-2/3/9 prevention)`; (b) `Per-assignee task breakdown with \`estimated_hours\` + \`risk_adjusted_hours\` columns + mandatory \`rationale\` per row (per DESIGN-22 + MOD-10 prevention)`; (c) `Wait-for-commercial-inputs gate before client-facing summary (per DESIGN-22 \`commercial_inputs_status: provided\` gate)`; Introduced (phase) = `Phase 4 (v2.3)`; DESIGN = `DESIGN-22`.
       - Row 9 (`generate-implementation-prompt/`): v0.3.0 origin = `none`; v2 name = `\`generate-implementation-prompt/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 7b per-platform implementation prompt (Pipefy = Behaviors instructions + KB upload list; Wrike = Copilot workflow narrative; Ziflow = checklist/criteria spec — per DESIGN-23)`; (b) `NOT a universal template (per DESIGN-23 + REQUIREMENTS.md Out of Scope row 13 — anti-feature explicitly excluded)`; (c) `Pulls \`delivery: native-ai\` rows from Stage 4a fnspec (per DESIGN-23)`; Introduced (phase) = `Phase 4 (v2.3)`; DESIGN = `DESIGN-23`.
       - Row 6 (`generate-technical-spec/`): v0.3.0 origin = `\`dydx-delivery/skills/generate-technical-spec/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.4)`; v2 name = `\`generate-technical-spec/\``; Status = `MODIFIED`; Change = 2 bullets — (a) `Scope-gated to Stage 4b existence — emits platform-API addendum on 4a if no 4b exists (per DESIGN-21)`; (b) `Covers error handling + observability + retries + idempotency for API portions; never hand-waves error paths (per DESIGN-21)`; Introduced (phase) = `Phase 4 (v2.3)`; DESIGN = `DESIGN-21`.
       - Row 8 (`generate-build-prompt/`): v0.3.0 origin = `\`dydx-delivery/skills/generate-build-prompt/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.6)`; v2 name = `\`generate-build-prompt/\``; Status = `MODIFIED`; Change = 2 bullets — (a) `Stage 7a — dev prompt; carries forward existing v0.3.0 generate-build-prompt body (per DESIGN-23)`; (b) `Pulls \`delivery: api\` rows from Stage 4a/4b fnspecs (per DESIGN-23)`; Introduced (phase) = `Phase 4 (v2.3)`; DESIGN = `DESIGN-23`.

       v2.4 block (Phase 5 — 4 rows: NEW ×2 → MODIFIED ×2):
       - Row 10 (`provision-test-harness/`): v0.3.0 origin = `none`; v2 name = `\`provision-test-harness/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 8a — bootstraps the per-client harness once at \`<Client> Brain/test-bot/\` (outside this repo); delta-updates each ship via diff against \`client_state.yaml\` (per DESIGN-24)`; (b) `Sandbox allowlist extended to Coda (per DESIGN-24 / CRIT-5 fix)`; (c) `Drift-detection algorithm (per DESIGN-30) — pre-flight schema diff; mismatch halts + emits \`schema_drift_report.md\``; Introduced (phase) = `Phase 5 (v2.4)`; DESIGN = `DESIGN-24`.
       - Row 12 (`generate-uat-plan/`): v0.3.0 origin = `none`; v2 name = `\`generate-uat-plan/\``; Status = `NEW`; Change = 2 bullets — (a) `Stage 8c — auto-generated UAT plan (per DESIGN-24)`; (b) `Reads Stage 4a + Stage 4b + Stage 8b test-plan as inputs (per DESIGN-24)`; Introduced (phase) = `Phase 5 (v2.4)`; DESIGN = `DESIGN-24`.
       - Row 11 (`generate-test-plan/`): v0.3.0 origin = `\`dydx-delivery/skills/generate-test-plan/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.5)`; v2 name = `\`generate-test-plan/\``; Status = `MODIFIED`; Change = 3 bullets — (a) `Stage 8b — body unchanged from v0.3.0 (per DESIGN-24)`; (b) `Path moves to \`<Client> Brain/test-bot/test_cases/\` (per DESIGN-24)`; (c) `Each TC marked with tier-1 (Python deterministic) or tier-2 (AI orchestrator) layer per DESIGN-28`; Introduced (phase) = `Phase 5 (v2.4)`; DESIGN = `DESIGN-24`.
       - Row 13 (`execute-tests/`): v0.3.0 origin = `\`dydx-delivery/skills/execute-tests/SKILL.md\` (per .planning/AUDIT.md §AUDIT-01.7)`; v2 name = `\`execute-tests/\``; Status = `MODIFIED`; Change = 3 bullets — (a) `Stage 8d — user-facing entry unchanged (per DESIGN-24)`; (b) `Internally invokes \`agents/test-bot-orchestrator/\` agent for tier-2 free-form interpretation (per DESIGN-04 + DESIGN-28)`; (c) `Sandbox-only enforcement carry-forward (per DESIGN-24 + REQUIREMENTS.md Out of Scope row 18)`; Introduced (phase) = `Phase 5 (v2.4)`; DESIGN = `DESIGN-24`.

       v2.5 block (Phases 6, 7 — 2 rows):
       - Row 14 (`update-documentation/`): v0.3.0 origin = `none`; v2 name = `\`update-documentation/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 9 — writes \`ChangeRequests/<CR>/doc-diff.md\`; reviewer-approval gate before push (per DESIGN-25 + MIN-1 prevention)`; (b) `Closed \`doc_type\` 9-value enum + naming \`<client_slug>__<project_slug>__<doc_type>__v<N>\` with double-underscore separator (per DESIGN-25 / MOD-15 prevention)`; (c) `Halt condition if \`<Client> Brain/00_HUB.md\` \`Documentation:\` link missing — graceful at Stage 9 only (per DESIGN-25 / MOD-1 prevention)`; Introduced (phase) = `Phase 6 (v2.5)`; DESIGN = `DESIGN-25`.
       - Row 15 (`push-native-ai-knowledge/`): v0.3.0 origin = `none`; v2 name = `\`push-native-ai-knowledge/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 10 — branches on \`native_ai_path: api \| paste \| none\`; copy-paste fallback default (per DESIGN-26)`; (b) `Refuses ingest if \`doc_published_at < last_diff_review_at\` (per DESIGN-26 / CRIT-8 fix)`; (c) `Refuses ingest if target mismatches \`client:\` frontmatter (per DESIGN-26 / MIN-4 fix)`; Introduced (phase) = `Phase 7 (v2.5) — BLOCKED BY OPEN-01 per CHANGE-04`; DESIGN = `DESIGN-26`.

       v2.6 block (Phases 8, 9 — 1 row + Phase 9 = surfaces only):
       - Row 16 (`sign-off-and-archive/`): v0.3.0 origin = `none`; v2 name = `\`sign-off-and-archive/\``; Status = `NEW`; Change = 3 bullets — (a) `Stage 11 — local \`<Client> Brain/<spokes>/\` update + one-way Coda mirror push (per DESIGN-27)`; (b) `\`tone_lint\` pass before publish (per DESIGN-27 / MOD-9 prevention)`; (c) `Field Notes preserved input-only; pre-archive sanity check + 00_Index.md version bump (per DESIGN-27)`; Introduced (phase) = `Phase 8 (v2.6)`; DESIGN = `DESIGN-27`.

    3. **Bottom totals paragraph** (per cross-AI C4 — codex MEDIUM — ONE authoritative count model; collapses earlier 13/15/19/20 framing). Use a single paragraph (NOT a matrix data row):

       `**Tag totals.** 13-skill v2 universe per DESIGN-12 (10 stage-skills + 3 platform-skills) + 6 MODIFIED rows tracking v0.3.0 skills carried forward into the v2 universe + 1 RETIRED → SPLIT row tracking the v0.3.0 skill no longer present in v2 (\`generate-functional-spec\`, replaced by \`generate-fnspec-platform\` Stage 4a + \`generate-fnspec-integration\` Stage 4b — see rows 4, 5 above) = 20 total Appendix A rows. NEW = 13 (10 stage-skill NEW + 3 platform-skill NEW). MODIFIED = 6 (\`discovery-intake\` / \`generate-sow\` / \`generate-technical-spec\` / \`generate-build-prompt\` / \`generate-test-plan\` / \`execute-tests\` — all carry-forwards from v0.3.0). RETIRED → SPLIT = 1. v2 end-state ship count = 13 skills (per DESIGN-12); the 6 MODIFIED rows are upgrades to skills inside the 13 universe; the RETIRED row ships nothing. AUDIT.md §AUDIT-01 grounds the v0.3.0 7-skill starting state.`

    **Implementing decisions:** D-39 (single-matrix shape + column headers + Status closed enum + ordering rule), D-40 (cited-bullets `Change` column with `(per DESIGN-NN)` cite per bullet), D-14 carried (`file:line` citations to AUDIT.md / SKILL.md sources), D-44 (D-N numbering preserved — no new D-IDs introduced).

    **CRITICAL — DO NOT modify Phase 1..9 sections from this task** (those live in 03-02 / 03-03). DO NOT touch Appendices B / C / D / E (those belong to other plans).
  </action>
  <verify>
    <automated>APPENDIX_A=$(awk -v hdr='## Appendix A: Per-skill delta matrix (CHANGE-02)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && ROWS=$(printf '%s\n' "$APPENDIX_A" | grep -cE '^\| .+ \| .+ \| (NEW|NEW \(split\)|MODIFIED|UNCHANGED|RETIRED|RETIRED → SPLIT) \|') && [ "$ROWS" -ge "15" ] && printf '%s\n' "$APPENDIX_A" | grep -qF 'kickoff-capture' && printf '%s\n' "$APPENDIX_A" | grep -qF 'platform-pipefy' && printf '%s\n' "$APPENDIX_A" | grep -qF 'RETIRED → SPLIT' && ! printf '%s\n' "$APPENDIX_A" | grep -qF 'NEW (platform)' && ! printf '%s\n' "$APPENDIX_A" | grep -qF 'UNCHANGED-structure' && printf '%s\n' "$APPENDIX_A" | grep -qF 'NEW = 13' && printf '%s\n' "$APPENDIX_A" | grep -qF 'MODIFIED = 6' && bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh 2>&1 | grep -E 'Appendix A|FAIL:' || echo TASK_1_OK_BUT_RUN_STRUCTURAL_BELOW; bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh 2>&1; echo "STRUCTURAL_EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Appendix A H2 section body (extracted via section_between) has >= 15 data rows matching the Status-column closed-enum pattern per cross-AI C1 (D-39 enum: `NEW`, `NEW (split)`, `MODIFIED`, `UNCHANGED`, `RETIRED`, `RETIRED → SPLIT`):
      ```
      APPENDIX_A=$(awk -v hdr='## Appendix A: Per-skill delta matrix (CHANGE-02)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md)
      ROWS=$(printf '%s\n' "$APPENDIX_A" | grep -cE '^\| .+ \| .+ \| (NEW|NEW \(split\)|MODIFIED|UNCHANGED|RETIRED|RETIRED → SPLIT) \|')
      [ "$ROWS" -ge "15" ]
      ```
    - All 13 v2 stage-skill names + 3 platform-skill names + 1 RETIRED skill name appear (17 individual `grep -qF` checks succeed):
      `kickoff-capture` AND `discovery-intake` AND `generate-sow` AND `generate-fnspec-platform` AND `generate-fnspec-integration` AND `generate-technical-spec` AND `generate-cost-estimate` AND `generate-build-prompt` AND `generate-implementation-prompt` AND `provision-test-harness` AND `generate-test-plan` AND `generate-uat-plan` AND `execute-tests` AND `update-documentation` AND `push-native-ai-knowledge` AND `sign-off-and-archive` AND `platform-pipefy` AND `platform-wrike` AND `platform-ziflow` AND `generate-functional-spec` (all 20 names present in Appendix A)
    - Status closed-enum coverage (per cross-AI C2 — D-39 6-value enum, no widening): all 4 used values appear at least once: `grep -qF 'NEW (split)' Appendix A` ✓; `grep -qF 'RETIRED → SPLIT' Appendix A` ✓; `grep -qF 'MODIFIED' Appendix A` ✓; bare `NEW` (most common; covers platform-pipefy / platform-wrike / platform-ziflow plus stage-skill NEW rows) ✓. NOT used in this matrix: `UNCHANGED` (no v0.3.0 skill carries forward unchanged), `RETIRED` (the only retired skill is RETIRED → SPLIT). Forbidden values absent: `NEW (platform)` and `UNCHANGED-structure / behaviour-modified` MUST NOT appear: `! grep -qF 'NEW (platform)' Appendix A` succeeds AND `! grep -qF 'UNCHANGED-structure' Appendix A` succeeds.
    - Bottom totals paragraph present per cross-AI C4: `printf '%s\n' "$APPENDIX_A" | grep -qF 'Tag totals'` succeeds AND mentions the 3 used tag categories with one-authoritative-count framing: `grep -qF 'NEW = 13'` ✓; `grep -qF 'MODIFIED = 6'` ✓; `grep -qF 'RETIRED → SPLIT = 1'` ✓; `grep -qF '13-skill v2 universe per DESIGN-12'` ✓ (single authoritative count source)
    - Each Change cell carries citation `(per DESIGN-NN)` — count of `(per DESIGN-` substrings in Appendix A: `printf '%s\n' "$APPENDIX_A" | grep -oE '\(per DESIGN-[0-9]+' | wc -l` returns >= 35 (minimum 2 bullets × 18 rows × ~half-cited = ~36; actual ~50 with most rows carrying 3 bullets)
    - **Structural-check assertion #9 NOW PASSES:** `bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh` does NOT fail on the Appendix A row count assertion (still fails on Appendix B / Appendix E because those are not yet populated)
    - Row ordering follows D-39 rule: first non-header row is from v2.1 block (a `platform-*` skill — Status `NEW`, with platform framing in Change cell per cross-AI C2); last row is from v2.6 block (`sign-off-and-archive`) OR the totals paragraph
    - **No accidental modifications** to Phase 1..9 mini-tables OR Appendices B / C / D / E (each section's H2 anchor + populated-by note from 03-01 still present where unchanged)
  </acceptance_criteria>
  <done>
    Appendix A populated with 20-row per-skill delta matrix per D-39: column headers exact, Status closed-enum used, rows ordered by milestone → phase → status, cited-bullet `Change` cells per D-40 with `(per DESIGN-NN)` cites, bottom totals paragraph reconciling against DESIGN-12. Structural-check assertion #9 (Appendix A row count >= 15) PASSES. CHANGE-02 status: COMMITTED — every v2 skill inventoried with v0.3.0 origin + Status + Change + Introducing phase + DESIGN section.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Appendix A row content ↔ DESIGN.md §"v2 skill inventory" matrix | Drift class. Appendix A is a CHANGELIST.md re-presentation of DESIGN.md's inventory through the v0.3.0 → v2 lens. If DESIGN.md inventory adds/removes a skill, Appendix A must follow. Mitigation: every row's `DESIGN` column cites the locking DESIGN-NN section; any reviewer noting a row missing in DESIGN.md must update both. |
| Appendix A row count ↔ structural-check assertion #9 (>= 15 floor) | Underflow class. If DESIGN-12 reduces v2 skill count below 15, the structural-check breaks. Acceptable failure: review-time signal that DESIGN.md changed and CHANGELIST.md needs re-derivation. |
| Status closed enum ↔ D-39 | Tampering class. Adding new Status values silently expands the enum and breaks downstream tooling that reads Appendix A. Mitigation: action block specifies the closed list; acceptance criteria check all 6 values appear. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Wrong column header order (e.g. `v2 name | v0.3.0 origin` swapped) breaks reviewer cross-reference flow | mitigate | Acceptance criteria assert the exact header row substring `\| v0.3.0 origin \| v2 name \| Status \| Change \| Introduced (phase) \| DESIGN \|` is present in the Appendix A body. |
| T-03-04-02 | Tampering | Skill name dropped from matrix (e.g. `provision-test-harness` accidentally omitted) | mitigate | Acceptance criteria assert all 20 distinct skill names appear via individual `grep -qF` checks. |
| T-03-04-03 | Tampering | Status value outside the closed enum used (e.g. `NEW-MODIFIED` invented) | accept (review-time catch) | The closed enum is documented in D-39 + this plan's `<interfaces>`; reviewer attestation catches deviations. Acceptance criteria spot-check that all 6 enum values appear (catches missing values, not invented values). |
| T-03-04-04 | Tampering | Bottom totals row miscounts — e.g. NEW = 12 instead of 13 | mitigate | Acceptance criteria explicitly assert `NEW = 13` substring in totals paragraph; review must re-run row-by-row count if mismatch. |
| T-03-04-05 | Tampering | Citation `(per DESIGN-NN)` missing on too many bullets — silently weakens traceability | mitigate | Acceptance criteria count `(per DESIGN-` substrings in Appendix A returning >= 35 — catches systemic citation drop. |
| T-03-04-06 | Repudiation | Plan 03-04 claims structural-check now passes assertion #9 but doesn't run the script | mitigate | Task 1 verify block runs the structural-check; the explicit `bash <script>` invocation surfaces any remaining failures. The `structural_check` exit code is logged in the verify output. |
| T-03-04-07 | Tampering | Appendix A row ordering violates D-39 rule | accept (manual reviewer attestation) | The ordering rule is too granular for grep-based assertion; reviewer manual attestation catches violations. Acceptance criteria spot-check that the FIRST non-header row is v2.1 / `platform-*` and the LAST data row is v2.6 / `sign-off-and-archive`. |
</threat_model>

<verification>
End of Plan 03-04 success-state:
1. Appendix A H2 section populated with 20-row matrix per D-39.
2. Column headers exact: `v0.3.0 origin | v2 name | Status | Change | Introduced (phase) | DESIGN`.
3. Status closed-enum (D-39 6 values; per cross-AI C2 no widening) all permitted; values actually used in this matrix: `NEW`, `NEW (split)`, `MODIFIED`, `RETIRED → SPLIT` (4 used; `UNCHANGED` and `RETIRED` are permitted but not used in this matrix). Forbidden enum values `NEW (platform)` and `UNCHANGED-structure / behaviour-modified` MUST NOT appear (cross-AI C2 codex HIGH).
4. Rows ordered by milestone (v2.1 → v2.6) → phase → status per D-39.
5. Each `Change` cell carries 1-3 cited bullets per D-40 with `(per DESIGN-NN)` citations.
6. Bottom totals paragraph reconciles against DESIGN-12 (NEW = 13; MODIFIED = 6; RETIRED → SPLIT = 1; UNCHANGED = 0 in this matrix).
7. Structural-check assertion #9 (Appendix A row count >= 15) NOW PASSES.
8. Phase 1..9 mini-tables unchanged from 03-02 + 03-03.
9. Appendices B / C / D / E unchanged from 03-01 skeleton state.

Manual reviewer attestation:
- [ ] All 7 v0.3.0 skills appear with their v0.3.0 origin path cited via `file:line`.
- [ ] All 13 v2 end-state stage skills appear (kickoff-capture / discovery-intake / generate-sow / generate-fnspec-platform / generate-fnspec-integration / generate-technical-spec / generate-cost-estimate / generate-build-prompt / generate-implementation-prompt / provision-test-harness / generate-test-plan / generate-uat-plan / execute-tests / update-documentation / push-native-ai-knowledge / sign-off-and-archive — actually 16 stage skills given Stage 4a/4b + 7a/7b + 8a/b/c/d substages).
- [ ] All 3 platform skills appear with Status `NEW` per D-39 closed enum (per cross-AI C2 — platform framing moved into Change column).
- [ ] `generate-functional-spec` appears with `RETIRED → SPLIT` Status.
- [ ] Bottom totals paragraph arithmetic checks: NEW + MODIFIED + RETIRED → SPLIT = 13 + 6 + 1 = 20.
- [ ] No CHANGE-N row carries severity tags (D-15 narrowed).
- [ ] No new D-IDs introduced (D-N pool frozen per CONTEXT D-44).
</verification>

<success_criteria>
- [ ] `## Appendix A: Per-skill delta matrix (CHANGE-02)` populated with 20-row matrix per D-39
- [ ] Column headers exact + Status closed enum used + ordered per D-39 rule
- [ ] Each `Change` cell carries 1-3 cited bullets per D-40
- [ ] Bottom totals paragraph reconciles against DESIGN-12 categorical breakdown
- [ ] Structural-check assertion #9 (Appendix A row count >= 15) PASSES
- [ ] No content under `dydx-delivery/` modified
- [ ] No modifications outside `.planning/CHANGELIST.md` Appendix A H2 section + `.planning/phases/03-changelist/03-04-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/03-changelist/03-04-SUMMARY.md` documenting:
- Appendix A populated: 20 rows total (16 stage skills + 3 platform skills + 1 RETIRED → SPLIT)
- v2 end-state ship count: 19 skills (per DESIGN-12 reconciliation note)
- Status closed-enum (D-39 6 values; per cross-AI C2 no widening). Values actually used in this matrix: NEW / NEW (split) / MODIFIED / RETIRED → SPLIT (4 used; UNCHANGED / RETIRED permitted but not present). Forbidden values `NEW (platform)` and `UNCHANGED-structure / behaviour-modified` absent (cross-AI C2 codex HIGH)
- Cited-bullets: ~50 `(per DESIGN-NN)` citations across the matrix
- Tag totals reconciliation: NEW = 13; MODIFIED = 6; RETIRED → SPLIT = 1; UNCHANGED = 0 in this matrix; sum = 20
- Structural-check status: assertion #9 PASSES (Appendix A row count >= 15); assertion #8 (Appendix B sentinels >= 6) and #10 (Appendix E bullets >= 8) STILL FAIL — Plans 03-05 + 03-07 unblock those
- CHANGE-02 status: COMMITTED — per-skill delta presented in single-matrix form per D-39 (ROADMAP Phase 3 success criterion 2 satisfied)
- Pointer: "Plan 03-05 (Wave 5) populates Appendices B + D (CHANGE-03 + CHANGE-05); Plan 03-06 (Wave 6) populates Appendix C + Phase-1/-2 inline markers (CHANGE-04); Plan 03-07 (Wave 7 — synthesis) populates Executive Summary + How-to-read + Appendix E mechanical walk + final structural-check pass."
</output>
</content>
</invoke>