---
plan_id: 03-06
phase: 03-changelist
plan: 06
type: execute
wave: 6
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05]
requirements: [CHANGE-04]
files_modified:
  - .planning/CHANGELIST.md
  - .planning/phases/03-changelist/03-06-SUMMARY.md
autonomous: true
tags: [changelist, change-04, appendix-c, research-blocked, design-only]

must_haves:
  truths:
    - "`## Appendix C: Research-blocked phases (CHANGE-04)` H2 section populated with a 2-row matrix (Phase 1 + Phase 7) per CONTEXT D-42 default + per-blocker unknown-list + recommended `/gsd-research-phase` invocation."
    - "Phase 1 mini-table Research-blocked cell (populated by 03-02) is enriched IN PLACE with verbatim inline `[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified...]` + `[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified...]` markers (DESIGN.md Appendix E bullets 3, 5)."
    - "Phase 2 mini-table Research-blocked cell (populated by 03-02) is enriched IN PLACE with verbatim inline `[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names...]` + `[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API...]` + `[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API...]` + `[OPEN: Phase 4 — Ziflow read-after-create consistency window...]` markers (DESIGN.md Appendix E bullets 2, 4, 6, 7)."
    - "Phase 7 already carries Pipefy AI KB marker per Plan 03-03; Phase 4 already carries risk-multiplier marker per Plan 03-02 — DO NOT re-introduce duplicates; verify those remain unchanged."
    - "After this plan, the cumulative inline `[OPEN: Phase 4 — ...]` marker count in CHANGELIST.md reaches the DESIGN.md baseline 8 (Plan 03-02 added marker 8 risk-multiplier; Plan 03-03 added marker 1 Pipefy AI KB; this plan adds markers 2, 3, 4, 5, 6, 7) — total 8 unique inline markers ready for Plan 03-07 reconciliation."
  artifacts:
    - path: ".planning/CHANGELIST.md"
      provides: "Appendix C (CHANGE-04 research-blocked phases — 2-row matrix Phase 1 + Phase 7 with full unknown-lists + recommended `/gsd-research-phase` invocations) + 6 enriched inline `[OPEN: Phase 4 — ...]` markers in Phase 1 + Phase 2 mini-tables"
      contains: "Appendix C 2-row matrix; recommended `/gsd-research-phase 1` + `/gsd-research-phase 7` invocations; Phase 1 unknown-list (5 connector items per CHANGE-04); Phase 7 unknown-list (3 native-AI ingestion APIs per CHANGE-04 / OPEN-01); 6 verbatim inline markers added to existing Phase 1 + Phase 2 mini-tables"
  key_links:
    - from: "Appendix C Phase 1 row"
      to: "CHANGELIST.md § Phase 1 mini-table Research-blocked cell + DESIGN.md Appendix E bullets 3, 5"
      via: "D-42 — marker at point of use + closed list at end + Appendix C cross-reference"
      pattern: "Reader scanning Phase 1 mini-table sees `⚠ ... see Appendix C` + verbatim inline markers; reader scanning Appendix C sees the full unknown-list"
    - from: "Appendix C Phase 7 row"
      to: "CHANGELIST.md § Phase 7 mini-table Research-blocked cell (already populated per 03-03) + DESIGN.md Appendix E bullets 1, 4, 6 + REQUIREMENTS.md OPEN-01"
      via: "D-42 + D-37 OPEN-01 hard blocker — Phase 7 fully blocked"
      pattern: "Phase 7 cell already carries OPEN-01 contingent fallback + Pipefy AI KB marker; Appendix C provides the full 3-API unknown-list and the explicit `/gsd-research-phase 7` invocation"
---

<objective>
Wave 6: populate `## Appendix C: Research-blocked phases (CHANGE-04)` in `.planning/CHANGELIST.md` AND enrich Phase 1 + Phase 2 mini-table Research-blocked cells with verbatim inline `[OPEN: Phase 4 — ...]` markers from DESIGN.md Appendix E. Implements CHANGE-04 (research-blocked phase flagging) AND completes the inline-marker baseline so Plan 03-07 synthesis reconciliation has the full 8-marker set in place.

**Purpose:**
- **Appendix C (CHANGE-04)** — surface every research-blocked phase in a single matrix so a reader scanning "what's still unknown" lands here. Recommends explicit `/gsd-research-phase 1` and `/gsd-research-phase 7` invocations before v2.1 + v2.5 milestone kickoffs. Mirrors D-42 "marker at point of use + closed list at end" pattern carried from D-27.
- **Phase 1 + Phase 2 inline-marker enrichment** — pulls forward 6 verbatim inline markers from DESIGN.md Appendix E (bullets 2, 3, 4, 5, 6, 7) to anchor Plan 03-07's reconciliation algorithm. After this plan, CHANGELIST.md carries all 8 DESIGN.md baseline markers (Plan 03-02 added bullet 8 risk-multiplier; Plan 03-03 added bullet 1 Pipefy AI KB; this plan adds bullets 2, 3, 4, 5, 6, 7).

**Output:** Appendix C populated with 2-row matrix + per-row unknown-list + per-row recommended invocation. Phase 1 + Phase 2 mini-table Research-blocked cells enriched with verbatim inline markers.

**No new D-IDs introduced.** No new content beyond what DESIGN.md and CONTEXT D-42 already lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/phases/03-changelist/03-CONTEXT.md
@.planning/phases/03-changelist/03-01-PLAN.md
@.planning/phases/03-changelist/03-02-PLAN.md
@.planning/phases/03-changelist/03-03-PLAN.md

<interfaces>
<!-- Two work products in this plan:
     1. Appendix C body: 2-row matrix (Phase 1 / Phase 7) per CONTEXT D-42 default
     2. Phase 1 + Phase 2 mini-table enrichment: append verbatim inline [OPEN] markers to existing Research-blocked cells
-->

**Appendix C matrix shape (per CONTEXT D-42 default — single 2-row matrix; Claude's-Discretion permits per-phase sub-tables but default is single matrix):**

```
| Phase | Research-blocked items | Recommended `/gsd-research-phase` invocation |
|---|---|---|
| Phase 1 (v2.1) — Foundations + Connector Verification | <5 connector probe items> | `/gsd-research-phase 1` before v2.1 plan-phase locks the connector-probe plan |
| Phase 7 (v2.5) — Native-AI knowledge push **[BLOCKED — see Phase 7 mini-table]** | <3 native-AI ingestion API items + OPEN-01 reference> | `/gsd-research-phase 7` MANDATORY before v2.5 plan-phase locks Phase 7. **HARD BLOCKER per OPEN-01.** |
```

Per-phase unknown-list content (per CHANGE-04 verbatim from REQUIREMENTS.md + CONTEXT D-42):

**Phase 1 row — 5 connector probe items:**
- Coda MCP version pin (per Plan 03-02 Phase 1 deliverable + CHANGE-04 + STACK.md "Stack open questions" #1)
- Google Workspace MCP choice (`taylorwilsdon/google_workspace_mcp` recommended per RESEARCH.md, but version pin + tenant-availability not verified — STACK.md #2)
- Miro MCP existence (no canonical Miro MCP found in Context7 per RESEARCH.md — STACK.md context)
- Wrike rate-limit currency (200 req/min from v0.3.0 safety-rules.md not re-verified — DESIGN.md Appendix E bullet 5; STACK.md #4)
- Ziflow auth header (`technical-spec-template.md:113` only verified source per AUDIT.md context; STACK.md #5)

**Phase 7 row — 3 native-AI ingestion APIs + OPEN-01 reference:**
- Pipefy AI KB content-upload endpoint not externally verified per OPEN-01 (DESIGN.md Appendix E bullet 1 — already on Phase 7 mini-table per Plan 03-03)
- Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 (DESIGN.md Appendix E bullet 4 — added to Phase 2 mini-table this plan; cross-referenced here)
- Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 (DESIGN.md Appendix E bullet 6 — added to Phase 2 mini-table this plan; cross-referenced here)

**Phase 1 mini-table enrichment (2 markers — DESIGN.md Appendix E bullets 3, 5):**

The existing Phase 1 Research-blocked cell (per Plan 03-02) reads: `⚠ Connector availability per tenant unverified (Coda MCP version pin, Google Workspace MCP choice, Miro MCP existence, Wrike rate-limit currency, Ziflow auth header) — see Appendix C`

This plan APPENDS to that cell (using `<br><br>` soft-break for visual separation while keeping the row a single line in markdown source):

`<br><br>**Inline markers (per D-27 carried — pulled forward from DESIGN.md Appendix E bullets 3, 5):**<br>` + the two verbatim markers (each on its own visual line via `<br>`):
- `\`[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]\``
- `\`[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]\``

**Phase 2 mini-table enrichment (4 markers — DESIGN.md Appendix E bullets 2, 4, 6, 7):**

The existing Phase 2 Research-blocked cell (per Plan 03-02) reads: `⚠ Pipefy GraphQL pagination cursor field names + Pipefy/Wrike 2026 rate-limit currency + Ziflow read-after-create consistency window unverified — see Appendix C`

This plan APPENDS to that cell:

`<br><br>**Inline markers (per D-27 carried — pulled forward from DESIGN.md Appendix E bullets 2, 4, 6, 7):**<br>` + the four verbatim markers:
- `\`[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]\``
- `\`[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]\``
- `\`[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]\``
- `\`[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]\``

**Reconciliation note (after this plan):** CHANGELIST.md will carry 8 unique inline `[OPEN: Phase 4 — ...]` markers in document order:
1. (Phase 1 mini-table) Pipefy 2026 rate-limit currency — DESIGN.md bullet 3
2. (Phase 1 mini-table) Wrike 2026 rate-limit currency — DESIGN.md bullet 5
3. (Phase 2 mini-table) Pipefy GraphQL pagination cursor field names — DESIGN.md bullet 2
4. (Phase 2 mini-table) Wrike AI Studio knowledge-ingestion API — DESIGN.md bullet 4
5. (Phase 2 mini-table) Ziflow ReviewAI knowledge-ingestion API — DESIGN.md bullet 6
6. (Phase 2 mini-table) Ziflow read-after-create consistency window — DESIGN.md bullet 7
7. (Phase 4 mini-table — already present per Plan 03-02) risk-multiplier defaults — DESIGN.md bullet 8
8. (Phase 7 mini-table — already present per Plan 03-03) Pipefy AI KB content-upload endpoint — DESIGN.md bullet 1

This 8-marker set matches the DESIGN.md Appendix E baseline exactly. Plan 03-07 reconciliation algorithm walks this list to populate CHANGELIST.md Appendix E.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate Appendix C — 2-row research-blocked matrix (Phase 1 + Phase 7) with per-phase unknown-list + recommended /gsd-research-phase invocation</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Phase 1..9 mini-tables populated; Appendix A populated by 03-04; Appendix B + D populated by 03-05; Appendix C still placeholder)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Research-blocked flag visibility (CHANGE-04 → Appendix C)" D-42 (matrix shape default; per-phase sub-tables permitted by Claude's-Discretion); §"Milestone-sizing call" D-37 (OPEN-01 contingent fallback — partial cross-reference here, full sentence already in Phase 7 mini-table per Plan 03-03)
    - .planning/REQUIREMENTS.md §"Change list (CHANGELIST.md)" CHANGE-04 verbatim (the 5 Phase 1 connector items + 3 Phase 7 native-AI ingestion APIs)
    - .planning/REQUIREMENTS.md OPEN-01 — full text (catalogues every "couldn't verify" item)
    - .planning/research/STACK.md "Stack open questions" #1, #2, #4, #5, #6 (verbatim source for Phase 1 connector items + Phase 7 native-AI APIs)
    - .planning/DESIGN.md §"Deferred to Phase 4 OPEN-QUESTIONS" — bullets 1-7 verbatim text for cross-referencing
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate `## Appendix C: Research-blocked phases (CHANGE-04)` H2 section IN PLACE (REPLACE the populated-by note with matrix content).

    **Structure (in this exact order under the H2):**

    1. **Framing paragraph:**
       `This appendix flags every v2.x phase whose plan-phase commit is gated on additional research per CHANGE-04. Two phases are research-blocked: **Phase 1 (v2.1)** — connector availability per tenant; and **Phase 7 (v2.5)** — native-AI ingestion paths per platform. Phase 7 is the SINGLE BIGGEST research-blocked unknown — Phase 7 inherits OPEN-01 as a hard blocker. Both rows recommend an explicit \`/gsd-research-phase\` invocation BEFORE v2.1 + v2.5 plan-phase locks the corresponding plans. Mirrors the D-27 / D-42 "marker at point of use + closed list at end" pattern carried from Phase 2.`

    2. **2-row research-blocked matrix:**

       ```
       | Phase | Research-blocked items | Recommended `/gsd-research-phase` invocation |
       |---|---|---|
       | **Phase 1 (v2.1)** — Foundations + Connector Verification | <5 connector items, see below> | `/gsd-research-phase 1` BEFORE v2.1 plan-phase locks the connector-probe plan |
       | **Phase 7 (v2.5)** — Native-AI knowledge push **[BLOCKED]** | <3 native-AI APIs, see below> | `/gsd-research-phase 7` **MANDATORY** before v2.5 plan-phase locks Phase 7. HARD BLOCKER per OPEN-01. |
       ```

       Use `<br>` for in-cell newlines.

       **Phase 1 Research-blocked items cell content** (5 bullets — verbatim from CHANGE-04 + STACK.md):
       ```
       - **Coda MCP version pin** — install latest, capture exact version. Per `.planning/research/STACK.md` "Stack open questions" #1.<br>
       - **Google Workspace MCP choice** — `taylorwilsdon/google_workspace_mcp` recommended per RESEARCH.md (MEDIUM confidence, community); avoid splitting Drive/Gmail/Calendar into separate servers. Pin specific version; never `@latest`. Per STACK.md #2.<br>
       - **Miro MCP existence** — no canonical Miro MCP found in Context7. Stage 1 kickoff branches: API ingest if available, image paste otherwise. Do NOT assume "render whole board" endpoint exists. Per STACK.md context.<br>
       - **Wrike rate-limit currency (2026)** — 200 req/min from v0.3.0 `safety-rules.md` not re-verified. Per STACK.md #4 + DESIGN.md Appendix E bullet 5.<br>
       - **Ziflow auth header + base URL** — verified only at `dydx-delivery/skills/generate-technical-spec/references/technical-spec-template.md:113`. Per STACK.md #5.
       ```

       **Phase 7 Research-blocked items cell content** (3 bullets + OPEN-01 reference + contingent-fallback cross-reference):
       ```
       - **Pipefy AI KB content-upload endpoint** — not visible in Pipefy Help Center / Pipefy Developers GraphQL reference (Agent CRUD verified; KB upload NOT visible). Per RESEARCH.md SUMMARY.md "Sources" §Tertiary + STACK.md #6.<br>
       - **Wrike AI Studio knowledge-ingestion API** — could not be externally verified. Wrike MCP server documents 16 tools (task/folder/project CRUD, search, custom fields, workflows, contacts, approvals); knowledge ingestion not in that list. Per STACK.md #6.<br>
       - **Ziflow ReviewAI knowledge-ingestion API** — Ziflow ReviewAI page documents Checklists Public Preview + Change Verification + Brand Standards "Coming Soon"; knowledge-ingestion API not visible. Per STACK.md #6.<br><br>**OPEN-01 cross-reference:** OPEN-01 (per `.planning/REQUIREMENTS.md`) catalogues every "couldn't verify" item from research with owning phase + verification owner. The 3 native-AI APIs above are the SINGLE BIGGEST unknown.<br><br>**Contingent fallback cross-reference:** Phase 7 mini-table carries the full D-37 contingent-fallback verbatim sentence (`If OPEN-01 (native-AI ingestion paths per platform) remains unresolved at v2.5 kickoff...`). See Phase 7 H2 Research-blocked cell for the full text.
       ```

       **Phase 1 recommended invocation cell:** `\`/gsd-research-phase 1\` BEFORE v2.1 plan-phase locks the connector-probe plan. Probe verifies: Coda MCP version pin; Google Workspace MCP choice; Miro MCP existence/path; Wrike rate-limit currency; Ziflow auth header + base URL. Outcome: per-tenant graceful-degradation matrix per the PITFALLS connector-fallback table.`

       **Phase 7 recommended invocation cell:** `\`/gsd-research-phase 7\` **MANDATORY** before v2.5 plan-phase locks Phase 7. **HARD BLOCKER per OPEN-01.** Probe verifies: Pipefy AI KB content-upload endpoint; Wrike AI Studio knowledge-ingestion API; Ziflow ReviewAI knowledge-ingestion API. Outcome: \`native_ai_path: api \| paste \| none\` decision per platform. If any API remains unverified, fall back to copy-paste path (default per DESIGN-26).`

    3. **Closing paragraph:**
       `**Phase 4 OPEN-QUESTIONS register handoff.** Every unknown listed above is also enumerated in Appendix E with the owning phase already assigned per CHANGE-04. The Phase 4 OPEN-QUESTIONS register builds against Appendix E mechanically — every Appendix E row becomes a register entry; the recommended \`/gsd-research-phase\` invocation per blocked phase is the resolution path that closes the register entries.`

    **Implementing decisions:** D-42 (research-blocked flag visibility — both inline markers AND dedicated Appendix C; default 2-row matrix), D-37 carried (Phase 7 row cross-references the contingent fallback in the Phase 7 mini-table; full verbatim sentence already in Phase 7 mini-table per Plan 03-03 — not duplicated here, just cross-referenced), D-14 carried (`file:line` citation format for STACK.md / RESEARCH.md citations).

    **CRITICAL — DO NOT modify Phase 1..9 sections OR Appendix A OR Appendix B OR Appendix D OR Appendix E from this task.**
  </action>
  <verify>
    <automated>APPENDIX_C=$(awk -v hdr='## Appendix C: Research-blocked phases (CHANGE-04)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && ROWS=$(printf '%s\n' "$APPENDIX_C" | grep -cE '^\| \*\*Phase [17]') && [ "$ROWS" = "2" ] && printf '%s\n' "$APPENDIX_C" | grep -qF '/gsd-research-phase 1' && printf '%s\n' "$APPENDIX_C" | grep -qF '/gsd-research-phase 7' && printf '%s\n' "$APPENDIX_C" | grep -qF 'OPEN-01' && printf '%s\n' "$APPENDIX_C" | grep -qF 'HARD BLOCKER' && printf '%s\n' "$APPENDIX_C" | grep -qF 'Pipefy AI KB content-upload endpoint' && printf '%s\n' "$APPENDIX_C" | grep -qF 'Coda MCP version pin' && echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Appendix C has exactly 2 data rows (Phase 1 + Phase 7):
      ```
      APPENDIX_C=$(awk -v hdr='## Appendix C: Research-blocked phases (CHANGE-04)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md)
      printf '%s\n' "$APPENDIX_C" | grep -cE '^\| \*\*Phase [17]'
      ```
      returns `2`
    - Both rows reference the recommended `/gsd-research-phase` invocation: `printf '%s\n' "$APPENDIX_C" | grep -qF '/gsd-research-phase 1'` succeeds AND `printf '%s\n' "$APPENDIX_C" | grep -qF '/gsd-research-phase 7'` succeeds
    - Phase 7 row carries `HARD BLOCKER` and `OPEN-01` substrings
    - Phase 1 row contains all 5 connector items: 5 individual `grep -qF` checks — `Coda MCP version pin` ✓; `Google Workspace MCP` ✓; `Miro MCP existence` ✓; `Wrike rate-limit` ✓; `Ziflow auth header` ✓
    - Phase 7 row contains all 3 native-AI ingestion APIs: 3 individual `grep -qF` checks — `Pipefy AI KB content-upload endpoint` ✓; `Wrike AI Studio knowledge-ingestion API` ✓; `Ziflow ReviewAI knowledge-ingestion API` ✓
    - Closing paragraph references Appendix E and CHANGE-04: `printf '%s\n' "$APPENDIX_C" | grep -qF 'Appendix E'` succeeds AND `grep -qF 'CHANGE-04'` succeeds
    - Appendix C does NOT duplicate the D-37 contingent-fallback verbatim sentence (cross-references Phase 7 mini-table instead): `printf '%s\n' "$APPENDIX_C" | grep -cF 'slide P8/P9 → v2.7'` returns 0 (the sentence lives in Phase 7 mini-table only)
    - **No accidental modifications** to Phase 1..9 / Appendix A / Appendix B / Appendix D sections from this task (Phase 1 + Phase 2 mini-table enrichment lives in Task 2)
  </acceptance_criteria>
  <done>
    Appendix C populated with 2-row research-blocked matrix (Phase 1 + Phase 7) per D-42. Phase 1 row enumerates 5 connector probe unknowns; Phase 7 row enumerates 3 native-AI ingestion APIs + OPEN-01 reference + cross-reference to Phase 7 mini-table for the D-37 contingent-fallback. Both rows recommend explicit `/gsd-research-phase` invocations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich Phase 1 + Phase 2 mini-table Research-blocked cells with verbatim inline `[OPEN: Phase 4 — ...]` markers (DESIGN.md Appendix E bullets 2, 3, 4, 5, 6, 7)</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Phase 1 + Phase 2 mini-tables populated by 03-02 with Research-blocked cells; Appendix C populated by Task 1)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Research-blocked flag visibility" D-42 (inline markers AT POINT OF USE + Appendix C closed list); §"Cross-cutting authoring decisions" D-27 carried (verbatim inline markers + closed-list reconciliation)
    - .planning/DESIGN.md lines 1416..1429 — Deferred to Phase 4 OPEN-QUESTIONS section, bullets 2, 3, 4, 5, 6, 7 (verbatim source for the 6 markers added by this task)
    - .planning/phases/02-design/02-10-SUMMARY.md — confirms 8-marker baseline + reconciliation algorithm; this plan adds 6 of those 8 to CHANGELIST.md (Plan 03-02 added bullet 8; Plan 03-03 added bullet 1)
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — APPEND verbatim inline `[OPEN: Phase 4 — ...]` markers to the EXISTING Research-blocked cells in Phase 1 + Phase 2 mini-tables. DO NOT replace the existing cell content; APPEND using `<br><br>` soft-break for visual separation while keeping each cell a single line in markdown source.

    **Phase 1 Research-blocked cell — APPEND 2 markers (DESIGN.md Appendix E bullets 3, 5):**

    Locate the existing Phase 1 Research-blocked cell (populated by 03-02). The cell currently reads:
    `| Research-blocked | ⚠ Connector availability per tenant unverified (Coda MCP version pin, Google Workspace MCP choice, Miro MCP existence, Wrike rate-limit currency, Ziflow auth header) — see Appendix C |`

    REPLACE with:
    `| Research-blocked | ⚠ Connector availability per tenant unverified (Coda MCP version pin, Google Workspace MCP choice, Miro MCP existence, Wrike rate-limit currency, Ziflow auth header) — see Appendix C.<br><br>**Inline markers (per D-27 carried — pulled forward from .planning/DESIGN.md Appendix E bullets 3, 5):**<br>• \`[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]\`<br>• \`[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]\` |`

    **CRITICAL — verbatim text:** The two marker strings MUST match DESIGN.md Appendix E bullets 3 and 5 EXACTLY (including em-dashes, semicolons, the word "ceiling" / "user", the exact `~5 req/sec per token` / `~100 req/min per user` phrasing). Plan 03-07 reconciliation diff will fail on any deviation.

    **Phase 2 Research-blocked cell — APPEND 4 markers (DESIGN.md Appendix E bullets 2, 4, 6, 7):**

    Locate the existing Phase 2 Research-blocked cell (populated by 03-02). The cell currently reads:
    `| Research-blocked | ⚠ Pipefy GraphQL pagination cursor field names + Pipefy/Wrike 2026 rate-limit currency + Ziflow read-after-create consistency window unverified — see Appendix C |`

    REPLACE with:
    `| Research-blocked | ⚠ Pipefy GraphQL pagination cursor field names + Pipefy/Wrike 2026 rate-limit currency + Ziflow read-after-create consistency window unverified — see Appendix C.<br><br>**Inline markers (per D-27 carried — pulled forward from .planning/DESIGN.md Appendix E bullets 2, 4, 6, 7):**<br>• \`[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]\`<br>• \`[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]\`<br>• \`[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]\`<br>• \`[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]\` |`

    **CRITICAL — verbatim text:** All four marker strings MUST match DESIGN.md Appendix E bullets 2, 4, 6, 7 EXACTLY. Plan 03-07 reconciliation diff will fail on any deviation.

    **Implementing decisions:** D-27 carried (inline `[OPEN]` marker at point of use + closed-list mechanical-walk reconciliation in Plan 03-07), D-42 (markers visible inline at the phase that hits the blocker — reader scanning top-to-bottom sees blocker at point of use), D-44 (no new D-IDs).

    **CRITICAL — DO NOT modify ANYTHING ELSE.** Specifically: do not modify Phase 3..9 sections, do not modify Phase 1's other 5 mini-table rows (Deliverables / Depends on / Addresses / Avoids pitfalls / Skills introduced/modified), do not modify Phase 2's other 5 rows, do not modify Appendix A / B / C (just-populated by Task 1) / D / E.

    **Cumulative inline-marker count after this task:** 8 unique markers in CHANGELIST.md document order:
    - 2 in Phase 1 mini-table Research-blocked cell (this task)
    - 4 in Phase 2 mini-table Research-blocked cell (this task)
    - 1 in Phase 4 mini-table Research-blocked cell (Plan 03-02 — risk-multiplier)
    - 1 in Phase 7 mini-table Research-blocked cell (Plan 03-03 — Pipefy AI KB)
    - Total: 8 unique inline markers, exactly matching DESIGN.md Appendix E baseline.

    Plan 03-07 synthesis reconciliation will walk these 8 markers + populate Appendix E with one bullet per unique marker.
  </action>
  <verify>
    <automated>P1=$(awk -v hdr='## Phase 1: Foundations + Connector Verification (v2.1)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && printf '%s\n' "$P1" | grep -qF '[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]' && printf '%s\n' "$P1" | grep -qF '[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]' && P2=$(awk -v hdr='## Phase 2: Internalise Platform Skills (v2.1)' 'f && /^## /{exit} f{print} index($0, hdr) == 1 && /^## /{f=1}' .planning/CHANGELIST.md) && printf '%s\n' "$P2" | grep -qF '[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]' && printf '%s\n' "$P2" | grep -qF '[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]' && printf '%s\n' "$P2" | grep -qF '[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]' && printf '%s\n' "$P2" | grep -qF '[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]' && CUMULATIVE=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/CHANGELIST.md | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u | wc -l) && echo "CUMULATIVE_UNIQUE_MARKERS_BEFORE_APPENDIX_E=$CUMULATIVE" && [ "$CUMULATIVE" = "8" ] && echo TASK_2_OK</automated>
  </verify>
  <acceptance_criteria>
    - Phase 1 Research-blocked cell contains 2 verbatim inline markers (DESIGN.md bullets 3 + 5):
      - `[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]` ✓
      - `[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]` ✓
    - Phase 2 Research-blocked cell contains 4 verbatim inline markers (DESIGN.md bullets 2 + 4 + 6 + 7):
      - `[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]` ✓
      - `[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` ✓
      - `[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` ✓
      - `[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]` ✓
    - **Cumulative unique inline-marker count outside Appendix E reaches 8** (matching DESIGN.md Appendix E baseline):
      ```
      CUMULATIVE=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/CHANGELIST.md | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u | wc -l)
      [ "$CUMULATIVE" = "8" ]
      ```
    - Phase 1 mini-table still has all 6 row labels (no row dropped during cell-replace): `printf '%s\n' "$P1" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|'` returns `6`
    - Phase 2 mini-table still has all 6 row labels: `printf '%s\n' "$P2" | grep -cE '^\| (Deliverables|Depends on|Addresses|Avoids pitfalls|Skills introduced/modified|Research-blocked) \|'` returns `6`
    - Phase 1 + Phase 2 Research-blocked cells still carry the original `⚠` summary (the prepended text from 03-02 was preserved): both `printf '%s\n' "$P1" | grep -qF '⚠ Connector availability per tenant unverified'` AND `printf '%s\n' "$P2" | grep -qF '⚠ Pipefy GraphQL pagination cursor field names'` succeed
    - Phase 4 mini-table still carries its risk-multiplier marker (Plan 03-02 unchanged): `grep -qF '[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]' .planning/CHANGELIST.md` succeeds
    - Phase 7 mini-table still carries its Pipefy AI KB marker (Plan 03-03 unchanged): `grep -qF '[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]' .planning/CHANGELIST.md` succeeds
    - **No accidental modifications** to Phase 3 / 5 / 6 / 8 / 9 mini-tables OR Appendix A / B / C (Task 1) / D / E sections
  </acceptance_criteria>
  <done>
    Phase 1 + Phase 2 mini-table Research-blocked cells enriched with 6 verbatim inline `[OPEN: Phase 4 — ...]` markers (DESIGN.md Appendix E bullets 2, 3, 4, 5, 6, 7). Cumulative unique inline-marker count in CHANGELIST.md document body (excluding Appendix E) = 8, exactly matching DESIGN.md Appendix E baseline. Plan 03-07 reconciliation algorithm has its full marker set ready. CHANGE-04 status: COMMITTED — research-blocked phases flagged inline AND in dedicated Appendix C (ROADMAP Phase 3 success criterion 4 satisfied).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 + Phase 2 inline markers ↔ DESIGN.md Appendix E bullets 2, 3, 4, 5, 6, 7 | Drift class. The marker text MUST match DESIGN.md verbatim so Plan 03-07 reconciliation diff returns empty. Mitigation: action block specifies exact verbatim text; acceptance criteria assert each substring via `grep -qF`. |
| Cumulative inline-marker count ↔ DESIGN.md baseline 8 | Coverage class. Plan 03-02 added 1 marker (bullet 8); Plan 03-03 added 1 marker (bullet 1); this plan adds 6 markers (bullets 2, 3, 4, 5, 6, 7). After this plan: 8 total = DESIGN.md baseline. Mitigation: Task 2 acceptance criteria explicitly assert cumulative count = 8. |
| Existing Phase 1 + Phase 2 mini-table content ↔ this plan's edits | File-write class. Task 2 must REPLACE only the Research-blocked cell content — NOT touch the other 5 rows of each mini-table. Mitigation: acceptance criteria assert all 6 row labels still present per phase after edit; reviewer attestation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | Marker text drifts from DESIGN.md verbatim (e.g. drops the trailing period or em-dash) | mitigate | Acceptance criteria use literal `grep -qF` checks against the exact verbatim strings (including punctuation, em-dashes, exact phrasing like `~5 req/sec per token`). |
| T-03-06-02 | Tampering | Cumulative inline-marker count != 8 (e.g. 7 markers added but Phase 4 risk-multiplier marker accidentally removed during Phase 1 + Phase 2 edits) | mitigate | Task 2 acceptance criteria assert `[ "$CUMULATIVE" = "8" ]` via the documented awk + grep -oE + sort -u + wc pipeline. |
| T-03-06-03 | Tampering | Phase 1 or Phase 2 mini-table loses a row during the Research-blocked cell replace | mitigate | Acceptance criteria assert the row-label count = 6 for each mini-table after edit. |
| T-03-06-04 | Tampering | Markers added to Appendix E body BEFORE Plan 03-07 synthesis runs (would inflate Plan 03-07 reconciliation count) | mitigate | Task 2 action block forbids modifications to Appendix E; acceptance criteria check ONLY the inline-body cumulative count (excluding Appendix E by `awk '/^## Appendix E/{exit} {print}'` scope). |
| T-03-06-05 | Tampering | Plan 03-06 accidentally adds a 9th inline marker (e.g. duplicating one of the 8 with slightly different wording) | mitigate | Task 2 acceptance criteria assert cumulative count = 8 (exact equality, not >= 8). A 9th marker would break the equality. |
| T-03-06-06 | Information Disclosure | Marker text contains tenant-identifying information | accept | All marker text is generic / external-API-vendor-name level; no client-tenant identifiers. |
| T-03-06-07 | Tampering | Appendix C row count != 2 (e.g. accidentally adding a Phase 3 row) | mitigate | Task 1 acceptance criteria assert `[ "$ROWS" = "2" ]` exactly via the `^\| \*\*Phase [17]` pattern. Only Phase 1 and Phase 7 are research-blocked per CONTEXT D-42. |
| T-03-06-08 | Tampering | Appendix C duplicates the D-37 contingent-fallback verbatim sentence (already in Phase 7 mini-table) | mitigate | Task 1 acceptance criteria explicitly assert the contingent-fallback sentence (substring `slide P8/P9 → v2.7`) is NOT in Appendix C — it lives in Phase 7 mini-table only. Appendix C cross-references via prose. |
</threat_model>

<verification>
End of Plan 03-06 success-state:
1. Appendix C populated with 2-row research-blocked matrix (Phase 1 + Phase 7) per D-42.
2. Phase 1 Research-blocked cell carries 2 verbatim inline markers (DESIGN.md bullets 3, 5) appended to the existing `⚠` summary.
3. Phase 2 Research-blocked cell carries 4 verbatim inline markers (DESIGN.md bullets 2, 4, 6, 7) appended to the existing `⚠` summary.
4. Cumulative unique inline-marker count outside Appendix E = 8 (matches DESIGN.md baseline exactly).
5. Phase 4 + Phase 7 markers from Plans 03-02 + 03-03 unchanged.
6. Phase 1..9 mini-tables still carry their 6 row labels each.
7. Appendix A + Appendix B + Appendix D unchanged from prior plans.
8. Appendix E unchanged from 03-01 skeleton state.
9. Structural-check status: assertion #10 (Appendix E bullets >= 8) STILL FAILS because Appendix E body still has 0 bullets — Plan 03-07 unblocks. All other assertions PASS.

Manual reviewer attestation:
- [ ] Each verbatim inline marker matches DESIGN.md Appendix E bullet text byte-for-byte.
- [ ] Appendix C 2-row matrix correctly identifies Phase 1 + Phase 7 as the only research-blocked phases per CONTEXT D-42.
- [ ] Recommended `/gsd-research-phase` invocations are clear and per-phase actionable.
- [ ] No new D-IDs introduced (D-N pool frozen per CONTEXT D-44).
</verification>

<success_criteria>
- [ ] `## Appendix C: Research-blocked phases (CHANGE-04)` populated with 2-row matrix per D-42
- [ ] Each Appendix C row recommends explicit `/gsd-research-phase N` invocation
- [ ] Phase 7 row carries `HARD BLOCKER` per OPEN-01 + cross-reference to Phase 7 mini-table for contingent fallback
- [ ] Phase 1 mini-table Research-blocked cell enriched with 2 verbatim inline markers (DESIGN.md bullets 3, 5)
- [ ] Phase 2 mini-table Research-blocked cell enriched with 4 verbatim inline markers (DESIGN.md bullets 2, 4, 6, 7)
- [ ] Cumulative unique inline-marker count in CHANGELIST.md body (excluding Appendix E) = 8 (DESIGN.md baseline match)
- [ ] No content under `dydx-delivery/` modified
- [ ] No modifications outside `.planning/CHANGELIST.md` Appendix C body + Phase 1 / Phase 2 mini-table Research-blocked cells + `.planning/phases/03-changelist/03-06-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/03-changelist/03-06-SUMMARY.md` documenting:
- Appendix C populated: 2-row matrix (Phase 1 — 5 connector items / Phase 7 — 3 native-AI ingestion APIs + OPEN-01 reference)
- Recommended `/gsd-research-phase 1` (BEFORE v2.1) + `/gsd-research-phase 7` (MANDATORY before v2.5)
- Phase 1 + Phase 2 mini-table Research-blocked cells enriched: +6 verbatim inline markers (DESIGN.md Appendix E bullets 2, 3, 4, 5, 6, 7)
- Cumulative inline-marker baseline in CHANGELIST.md body: 8 unique markers exactly matching DESIGN.md Appendix E (Plan 03-02 contributed bullet 8 risk-multiplier; Plan 03-03 contributed bullet 1 Pipefy AI KB; this plan contributed bullets 2-7)
- Reconciliation readiness: Plan 03-07 reconciliation algorithm has its full 8-marker set ready
- Structural-check status: assertion #10 (Appendix E bullets >= 8) STILL FAILS because Appendix E body still empty; all other assertions PASS
- CHANGE-04 status: COMMITTED — research-blocked phases flagged BOTH inline (D-42 marker at point of use) AND in dedicated Appendix C closed list (ROADMAP Phase 3 success criterion 4 satisfied)
- Pointer: "Plan 03-07 (Wave 7 — synthesis) is the FINAL plan: Executive Summary table + How-to-read this change list paragraph + Appendix E mechanical walk (8 bullets enumerating the 8 inline markers via the Phase 2 02-10 reconciliation algorithm) + final preamble + final structural-check pass (all 12 assertions exit 0). After 03-07 completes, `.planning/CHANGELIST.md` is reviewer-ready and ready for Phase 3 approval gate review per ROADMAP."
</output>
</content>
</invoke>
