---
plan_id: 03-07
phase: 03-changelist
plan: 07
type: execute
wave: 7
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]
requirements: [CHANGE-01, CHANGE-02, CHANGE-03, CHANGE-04, CHANGE-05]
files_modified:
  - .planning/CHANGELIST.md
  - .planning/phases/03-changelist/03-07-SUMMARY.md
autonomous: true
tags: [changelist, synthesis, executive-summary, how-to-read, appendix-e, reviewer-ready, design-only]

must_haves:
  truths:
    - "CHANGELIST.md `## Executive Summary` populated with 5-column condensed milestone-bundling table (per CONTEXT Claude's-Discretion default) — one row per phase: `Phase | Milestone | Theme | Skills delta count | Blocker`. Plus the verbatim D-37 re-bundling caveat sentence appended after the table per CONTEXT Specifics."
    - "CHANGELIST.md `## How to read this change list` populated with reader-flow guide — explicit pointers (read-in-order vs Appendix A for skill lookup vs Appendix C for unknown lookup vs Appendix E for deferral lookup)."
    - "CHANGELIST.md `## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS` populated with FULL enumerated list of every inline `[OPEN: Phase 4 — ...]` marker in CHANGELIST.md (8 unique markers per Plan 03-06 baseline) — each bullet cites source section + owning phase per CHANGE-04."
    - "Reconciliation algorithm (carried from Phase 2 02-10 HIGH #3) executes correctly: scoped inline-body extraction (excludes Appendix E) + anchored bullet count + diff of normalised marker texts."
    - "Preamble blockquote finalised — placeholder removed; reviewer-flow paragraph added."
    - "Structural-check exits 0 — all 12 assertions PASS. CHANGELIST.md is **reviewer-ready** (terminal state)."
  artifacts:
    - path: ".planning/CHANGELIST.md"
      provides: "Final synthesis: finalised preamble + populated Executive Summary 9-row condensed table + populated How-to-read reader-flow guide + populated Appendix E 8-bullet enumeration + final structural-check pass"
      contains: "Executive Summary 5-column 9-row table; D-37 re-bundling caveat verbatim sentence; reader-flow guide with skip-to-contract pointers; Appendix E 8 bullets in document order with source + owner per cross-AI #10 carried; reconciliation algorithm executed and verified"
  key_links:
    - from: "## Executive Summary table"
      to: "Every Phase H2 + every Appendix H2 in CHANGELIST.md (skip-to-contract aid)"
      via: "Per-row Phase column + Skills-delta-count column + Blocker column reference downstream sections"
      pattern: "row per phase / appendix"
    - from: "## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS list"
      to: "Phase 4 OPEN-QUESTIONS.md register (built mechanically by walking this list per ROADMAP Phase 4 dependency)"
      via: "One bullet per inline marker; format: `- **\`[OPEN: Phase 4 — <subject>]\`** — source: §<H2 / mini-table cell> — owner: Phase <N> per CHANGE-04`"
      pattern: "list entries reconcile 1:1 with normalised inline marker text count outside the Appendix E section (per Plan 02-10 HIGH #3 algorithm carried)"
    - from: "Final preamble blockquote"
      to: "Every reading-convention referenced in CHANGELIST.md"
      via: "Reader knows: D-38 mini-table convention; D-27 inline `[OPEN]` markers + Appendix E closed list; D-14 `file:line` citations; D-41 Appendix B verbatim-lift discipline; D-43 Appendix D citation header; D-37 re-bundling caveat + OPEN-01 contingent fallback"
      pattern: "preamble = reader's contract for navigating the document"
---

<objective>
Wave 7 / synthesis (terminal plan): finalise `.planning/CHANGELIST.md` end-to-end. Plans 03-02..03-06 wrote the contracts (9-phase build plan + per-skill matrix + cosmetic-fix list + research-blocked appendix + migration cutover rules). This plan makes CHANGELIST.md REVIEWABLE end-to-end:
1. Finalise the preamble blockquote (placeholder removed).
2. Populate `## Executive Summary` with the 5-column condensed milestone-bundling table per CONTEXT Claude's-Discretion default.
3. Populate `## How to read this change list` with the reader-flow guide.
4. Populate `## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS` with the 8-bullet enumeration of every inline `[OPEN: Phase 4 — ...]` marker (mechanical walk via the Phase 2 02-10 reconciliation algorithm carried).
5. Run final structural-check — exits 0 — CHANGELIST.md is **reviewer-ready** (terminal state).

**Critical fix carried from Phase 2 02-10 HIGH #3 (reconciliation algorithm fragility):** Naive `grep -c '\[OPEN: Phase 4'` over the whole document double-counts each marker (once inline + once in Appendix E quoting it). Fix: (a) Scope inline-marker scan to EXCLUDE the Appendix E section. (b) Count Appendix E bullets separately by anchored bullet pattern. (c) Compare normalised marker TEXT (not just counts) via `diff`. Documented step-by-step in Task 3 below.

**Marker-ownership per cross-AI #10 carried from Phase 2 02-10:** Each Appendix E bullet MUST cite source section AND owning phase per CHANGE-04. If owning phase is unknown, mark `owner: TBD` explicitly — never silently omit.

**Output:** `.planning/CHANGELIST.md` complete and structurally final. Structural-check exits 0 (all 12 assertions pass). **CHANGELIST.md is reviewer-ready.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/phases/03-changelist/03-CONTEXT.md
@.planning/phases/03-changelist/03-01-PLAN.md
@.planning/phases/03-changelist/03-02-PLAN.md
@.planning/phases/03-changelist/03-03-PLAN.md
@.planning/phases/03-changelist/03-04-PLAN.md
@.planning/phases/03-changelist/03-05-PLAN.md
@.planning/phases/03-changelist/03-06-PLAN.md
@.planning/phases/02-design/02-10-PLAN.md
@.planning/phases/02-design/02-10-SUMMARY.md

<interfaces>
<!-- Synthesis writes to 4 regions:
     1. Preamble blockquote (replaces placeholder line)
     2. ## Executive Summary (5-column condensed milestone-bundling table per CONTEXT Claude's-Discretion default + D-37 re-bundling caveat verbatim)
     3. ## How to read this change list (reader-flow guide)
     4. ## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS (replaces opening sentinel + populated-by note with full 8-bullet enumeration)
     No new content contracts; pure synthesis.

     Section-range extraction uses stateful awk patterns (consistent with 03-01 script's section_between helper).
     The reconciliation algorithm in Task 3 is documented step-by-step per cross-AI HIGH #3 carried from Phase 2 02-10. -->

**Executive Summary table shape (per CONTEXT Claude's-Discretion default — 5 columns; 9 rows):**

```
| Phase | Milestone | Theme | Skills delta count | Blocker |
|---|---|---|---|---|
| Phase 1 | v2.1 | Foundations + Connector Verification | 7 MODIFIED (existing skills repointed at canonical refs) | ⚠ Connector availability per tenant — see Appendix C |
| Phase 2 | v2.1 | Internalise Platform Skills | 3 NEW (platform) | ⚠ Pipefy/Wrike/Ziflow API specifics — see Appendix C |
| Phase 3 | v2.2 | Stage 1 Kickoff + Stage 4 fnspec split | 3 NEW + 1 MODIFIED + 1 UNCHANGED + 1 RETIRED | — |
| Phase 4 | v2.3 | Tech spec + Cost + Implementation prompt | 2 NEW + 2 MODIFIED | `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` |
| Phase 5 | v2.4 | Test bot rebuild | 2 NEW + 2 MODIFIED + 1 NEW agent | — |
| Phase 6 | v2.5 | Documentation publishing | 1 NEW | — |
| Phase 7 | v2.5 | Native-AI knowledge push **[BLOCKED]** | 1 NEW | ⚠ **HARD BLOCKER** — OPEN-01 native-AI ingestion APIs unverified. See Appendix C + Phase 7 mini-table for D-37 contingent fallback. |
| Phase 8 | v2.6 | Sign-off + Coda mirror | 1 NEW | — |
| Phase 9 | v2.6 | Surfaces (commands/agents/hooks) | 0 skill changes; ships plugin surfaces | — |
```

After the table — verbatim per CONTEXT Specifics — emit two mandatory sentences:

1. **D-37 re-bundling caveat (verbatim — CONTEXT Specifics MANDATORY):** `**Re-bundling caveat.** User may re-bundle phases at the start of any v2.x milestone kickoff via \`/gsd-new-milestone\` — this bundling is the recommended sequence, not a contract.`

2. **D-37 OPEN-01 contingent fallback (verbatim — CONTEXT Specifics MANDATORY for v2.5 row):** `**OPEN-01 contingent fallback (v2.5 row).** If OPEN-01 (native-AI ingestion paths per platform) remains unresolved at v2.5 kickoff, split P6 → v2.5 (Documentation only) and P7 → v2.6 (Native-AI when unblocked); slide P8/P9 → v2.7. Phase 7 is the only blocked-by-OPEN phase; isolating it preserves cadence on the rest. (Verbatim per D-37; also present in Phase 7 mini-table Research-blocked cell per Plan 03-03.)`

**How to read this change list (Section §"How to read this change list" body shape):**

3-4 short paragraphs explaining:
- Document purpose: sequenced, justified delta from v0.3.0 to v2 — committing the 9-phase build plan as the v2.x milestone sequence.
- Reading conventions: per-phase mini-tables per D-38 (Attribute / Detail rows: Deliverables / Depends on / Addresses / Avoids pitfalls / Skills introduced/modified / Research-blocked); inline `[OPEN: Phase 4 — ...]` markers at point of use + closed list in Appendix E (D-27 carried); citation format `file:line` (D-14 carried); per-bullet sentinel discipline on Appendix B fixes (D-16 carried).
- Reviewer flow: read-in-order (Executive Summary → 9-phase plan → appendices) OR skip-to-specific-item (Executive Summary as TOC; Appendix A is the "is skill X changing" lookup; Appendix C is the "what's still unknown" lookup; Appendix E is the "what's deferred" lookup).
- Source-of-truth pointers: `.planning/AUDIT.md` for v0.3.0 facts; `.planning/DESIGN.md` for v2 architecture; `.planning/research/SUMMARY.md` § Phase Ordering Rationale for the ordering decisions; `.planning/research/PITFALLS.md` for CRIT/MOD/MIN IDs.
- Phase boundary: no skill files edited in this milestone (kickoff design-only mandate); v2.1+ build phases per CHANGE-01 sequence execute against this design.
- Approval gate: single approval at end (full CHANGELIST.md reviewed as a whole) per ROADMAP Phase 3 approval gate.

**Appendix E body shape (closed list per D-27 carried — 8 unique inline marker bullets in document order):**

The opening sentence (`Closed list of every \`[OPEN: Phase 4 — ...]\` marker in this document. Phase 4 OPEN-QUESTIONS register can be built mechanically by walking this list — every row becomes a register entry with the owning phase already assigned. New deferrals discovered during Phase 3 authoring add a row here AND inline at point of use.`) is PRE-LAID by 03-01. DO NOT modify.

After the opening sentence, emit one bullet per unique inline marker, in CHANGELIST.md document order. Format MANDATED per cross-AI #10 carried from Phase 2 02-10:

```markdown
- **`[OPEN: Phase 4 — <verbatim marker text>]`** — source: §<H2 anchor / mini-table cell> — owner: Phase <N> per CHANGE-04
```

Required field per bullet:
- Bold-quoted marker text (verbatim from inline)
- `source: §<source-section>`
- `owner: Phase <N> per CHANGE-04` (or `owner: TBD` if genuinely unknown — explicit, never silently omit)

The 8 expected bullets in CHANGELIST.md document order (matching the Plan 03-06 cumulative-marker enumeration):

1. (Phase 1 mini-table Research-blocked cell) `[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]` — source: §Phase 1: Foundations + Connector Verification (v2.1) → mini-table Research-blocked cell — owner: Phase 1/Phase 2 per CHANGE-04
2. (Phase 1) `[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]` — source: §Phase 1: Foundations + Connector Verification (v2.1) → mini-table Research-blocked cell — owner: Phase 1/Phase 2 per CHANGE-04
3. (Phase 2) `[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]` — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04 (`platform-pipefy` build)
4. (Phase 2) `[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04
5. (Phase 2) `[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04
6. (Phase 2) `[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]` — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 2 per CHANGE-04
7. (Phase 4) `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` — source: §Phase 4: Tech spec + Cost + Implementation prompt (v2.3) → mini-table Research-blocked cell — owner: Phase 4 per CHANGE-04 (decide before Stage 6 build per CHANGE-01 ordering)
8. (Phase 7) `[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` — source: §Phase 7: Native-AI knowledge push (v2.5) [BLOCKED — see Appendix C] → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise preamble + populate `## Executive Summary` 5-column 9-row table + verbatim D-37 sentences</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state — Plans 03-01..03-06 complete; Phase mini-tables + Appendix A/B/C/D populated; Executive Summary + How-to-read + Appendix E still placeholder; preamble still placeholder)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Specifics" — "Reviewer experience" (read-in-order vs skip-to-specific) + "Re-bundling caveat" + "OPEN-01 contingent fallback" + "Executive Summary default = condensed 5-col"; §"Milestone-sizing call" D-37 (verbatim sentences); §"Cross-cutting authoring decisions" D-14, D-15, D-16, D-25, D-27, D-28
    - .planning/phases/02-design/02-10-PLAN.md §"interfaces" — Phase 2 synthesis precedent (Executive Summary 21-row TOC vs Phase 3 5-col 9-row condensed — different shape per CONTEXT Claude's-Discretion default)
    - .planning/phases/02-design/02-10-SUMMARY.md — Phase 2 synthesis SUMMARY for cross-AI HIGH #3 reconciliation algorithm precedent
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate three regions:

    **(A) Finalise preamble blockquote.** Replace the existing placeholder line `> (Preamble placeholder — finalised in synthesis plan 03-07.)` with a finalised blockquote paragraph (3-4 short paragraphs in blockquote form `>`) that explains:
    - Document purpose: locks the v0.3.0 → v2 delta as the v2.x milestone sequence; commits the SUMMARY.md 9-phase build plan; flags research-blocked phases (Phase 1 + Phase 7) for `/gsd-research-phase` invocation; locks migration cutover rules so v0.3.0 in-flight artefacts don't get auto-corrupted.
    - Reading conventions: per-phase mini-tables per D-38; inline `[OPEN: Phase 4 — ...]` markers + closed list in Appendix E (D-27 carried); citation format `file:line` (D-14 carried); per-bullet sentinel discipline on Appendix B fixes (D-16 carried); severity tags `[COSMETIC]` / `[NEW]` apply only to AUDIT-07 lifts in Appendix B (D-15 narrowed); `[BLOCKED — see Appendix C]` H2 tag on Phase 7 (D-42).
    - Reviewer flow: read-in-order (Executive Summary → 9-phase plan → appendices) OR skip-to-contract via Executive Summary table (acts as TOC).
    - Phase boundary: no skill files edited in this design-only milestone; v2.1+ build phases sequenced by CHANGELIST.md execute against this design + DESIGN.md.
    - Approval gate: single approval at end (full CHANGELIST.md reviewed as a whole) per ROADMAP Phase 3 approval gate.

    Other preamble lines (Change list date / Branch / Phase 1 Audit ground truth / Phase 2 Design ground truth) stay above; only the placeholder blockquote line is replaced.

    **CRITICAL — meta-pattern reword (Phase 2 02-10 Rule-2 precedent):** When emitting the preamble, do NOT use the literal pattern `[OPEN: Phase 4 — ...]` (with verbatim ellipsis) as inline pattern documentation — the regex `\[OPEN: Phase 4 — [^]]+\]` would match it and inflate INLINE_COUNT during Task 3 reconciliation. Instead use the prose form `square-bracketed \`OPEN: Phase 4 — <subject>\` strings` OR `[OPEN: Phase 4 — <subject>]`-style references (with placeholder syntax inside backticks) OR plain prose `OPEN: Phase 4 markers`. This carries the Phase 2 02-10 Rule-2 auto-add fix into Phase 3.

    **(B) Populate `## Executive Summary` table.** Replace the placeholder body with the 5-column 9-row condensed milestone-bundling table per CONTEXT Claude's-Discretion default. Use the EXACT table shape shown in `<interfaces>` block above:

    ```
    | Phase | Milestone | Theme | Skills delta count | Blocker |
    |---|---|---|---|---|
    | Phase 1 | v2.1 | Foundations + Connector Verification | 7 MODIFIED (existing skills repointed at canonical refs) | ⚠ Connector availability per tenant — see Appendix C |
    | Phase 2 | v2.1 | Internalise Platform Skills | 3 NEW (platform) | ⚠ Pipefy/Wrike/Ziflow API specifics — see Appendix C |
    | Phase 3 | v2.2 | Stage 1 Kickoff + Stage 4 fnspec split | 3 NEW + 1 MODIFIED + 1 UNCHANGED + 1 RETIRED | — |
    | Phase 4 | v2.3 | Tech spec + Cost + Implementation prompt | 2 NEW + 2 MODIFIED | `OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22` |
    | Phase 5 | v2.4 | Test bot rebuild | 2 NEW + 2 MODIFIED + 1 NEW agent | — |
    | Phase 6 | v2.5 | Documentation publishing | 1 NEW | — |
    | Phase 7 | v2.5 | Native-AI knowledge push **[BLOCKED]** | 1 NEW | ⚠ **HARD BLOCKER** — OPEN-01 native-AI ingestion APIs unverified. See Appendix C + Phase 7 mini-table for D-37 contingent fallback. |
    | Phase 8 | v2.6 | Sign-off + Coda mirror | 1 NEW | — |
    | Phase 9 | v2.6 | Surfaces (commands/agents/hooks) | 0 skill changes; ships plugin surfaces | — |
    ```

    **CRITICAL — meta-pattern reword in Blocker column for Phase 4:** The Phase 4 Blocker cell shows the marker WITHOUT the surrounding `[...]` brackets to avoid the reconciliation regex matching it and inflating INLINE_COUNT. Use backtick-quoted prose form `\`OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22\`` — the substring still communicates the deferral but does NOT match `\[OPEN: Phase 4 — [^]]+\]` regex. (The full bracket form lives in Phase 4 mini-table Research-blocked cell only.)

    Add framing paragraph above the table: `This table summarises the v2.x milestone sequence in condensed form for skip-to-contract navigation. Each row's Phase column links to the full mini-table; Skills-delta-count column ties forward to Appendix A; Blocker column ties forward to Appendix C (research-blocked) or Appendix E (deferral-blocked).`

    Add the two D-37 verbatim sentences AFTER the table (per CONTEXT Specifics MANDATORY belt-and-braces):

    ```
    **Re-bundling caveat.** User may re-bundle phases at the start of any v2.x milestone kickoff via `/gsd-new-milestone` — this bundling is the recommended sequence, not a contract.

    **OPEN-01 contingent fallback (v2.5 row).** If OPEN-01 (native-AI ingestion paths per platform) remains unresolved at v2.5 kickoff, split P6 → v2.5 (Documentation only) and P7 → v2.6 (Native-AI when unblocked); slide P8/P9 → v2.7. Phase 7 is the only blocked-by-OPEN phase; isolating it preserves cadence on the rest. (Verbatim per D-37; also present in Phase 7 mini-table Research-blocked cell per Plan 03-03.)
    ```

    **Implementing decisions:** D-19 carried (Executive Summary = matrix shape — condensed 5-col per CONTEXT Claude's-Discretion default; not 21-row TOC like Phase 2 because Phase 3 has fewer top-level sections), D-37 (verbatim re-bundling caveat + OPEN-01 contingent fallback), D-32 carried, D-34 carried.
  </action>
  <verify>
    <automated>EXEC_BODY=$(awk -v hdr='## Executive Summary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/CHANGELIST.md) && EXEC_ROWS=$(printf '%s\n' "$EXEC_BODY" | grep -cE '^\| Phase [1-9]') && [ "$EXEC_ROWS" = "9" ] && printf '%s\n' "$EXEC_BODY" | grep -qF 'Re-bundling caveat' && printf '%s\n' "$EXEC_BODY" | grep -qF 'this bundling is the recommended sequence, not a contract' && printf '%s\n' "$EXEC_BODY" | grep -qF 'slide P8/P9 → v2.7' && PLACEHOLDER=$(grep -cF '(Preamble placeholder' .planning/CHANGELIST.md) && [ "$PLACEHOLDER" = "0" ] && bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh 2>&1; CODE=$?; echo "STRUCTURAL_CHECK_AFTER_TASK_1=$CODE"; echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Preamble placeholder removed: `grep -cF '(Preamble placeholder' .planning/CHANGELIST.md` returns `0`
    - Preamble final blockquote present and explains: per-phase mini-tables per D-38; inline OPEN markers + Appendix E (D-27); `file:line` citations (D-14); per-bullet sentinels on Appendix B (D-16); severity tags only in Appendix B (D-15); Phase 7 [BLOCKED] tag (D-42); reviewer skip-to-contract flow; phase boundary; single approval gate
    - **Executive Summary table has exactly 9 phase rows** (extracted via stateful section_between):
      ```
      EXEC_BODY=$(awk -v hdr='## Executive Summary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/CHANGELIST.md)
      EXEC_ROWS=$(printf '%s\n' "$EXEC_BODY" | grep -cE '^\| Phase [1-9]')
      [ "$EXEC_ROWS" = "9" ]
      ```
    - Executive Summary table has the 5-column header `| Phase | Milestone | Theme | Skills delta count | Blocker |`: `printf '%s\n' "$EXEC_BODY" | grep -qF '| Phase | Milestone | Theme | Skills delta count | Blocker |'` succeeds
    - Each phase row mentions correct milestone tag (v2.1 for P1+P2; v2.2 for P3; v2.3 for P4; v2.4 for P5; v2.5 for P6+P7; v2.6 for P8+P9): 6 individual `grep -qF` checks per row mapping
    - Phase 7 row carries `[BLOCKED]` tag in Theme column AND `HARD BLOCKER` in Blocker column
    - **D-37 re-bundling caveat verbatim sentence present** (CONTEXT Specifics MANDATORY): `printf '%s\n' "$EXEC_BODY" | grep -qF 'Re-bundling caveat'` AND `grep -qF 'this bundling is the recommended sequence, not a contract'` both succeed
    - **D-37 OPEN-01 contingent fallback verbatim sentence present** (CONTEXT Specifics MANDATORY for v2.5 row): `printf '%s\n' "$EXEC_BODY" | grep -qF 'slide P8/P9 → v2.7'` succeeds (this unique phrase confirms the verbatim sentence is present)
    - **Meta-pattern reword applied** — Executive Summary table cells must NOT match `\[OPEN: Phase 4 — [^]]+\]` regex (would inflate INLINE_COUNT); the Phase 4 Blocker cell uses backtick-quoted prose form WITHOUT brackets: count of `\[OPEN: Phase 4` substrings inside Executive Summary section is 0:
      ```
      printf '%s\n' "$EXEC_BODY" | grep -cE '\[OPEN: Phase 4'
      ```
      returns `0`
    - **Structural-check still exits 0 OR fails only on Appendix E bullets** (Tasks 2 + 3 will fix that)
  </acceptance_criteria>
  <done>
    Preamble finalised. Executive Summary populated with 5-column 9-row condensed milestone-bundling table per CONTEXT Claude's-Discretion default. D-37 re-bundling caveat + OPEN-01 contingent fallback verbatim sentences emitted after the table. Meta-pattern reword applied to Phase 4 Blocker cell (no `[OPEN]` bracket form in Executive Summary).
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate `## How to read this change list` reader-flow guide</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state after Task 1)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Specifics" — "Reviewer experience (carried from Phase 1)" + "Milestone tag rendering" + "Per-phase ordering-rationale source"
    - .planning/AUDIT.md §"How to read this audit" (Phase 1 precedent for the section shape)
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate `## How to read this change list` H2 section IN PLACE (REPLACE the populated-by note with reader-flow guide content).

    **Body content (3-4 short paragraphs):**

    ```
    **Document purpose.** This change list sequences the v0.3.0 → v2 delta as the authoritative v2.x milestone sequence per CHANGE-01. The 9-phase build plan from `.planning/research/SUMMARY.md` is committed verbatim as the milestone spine; per-skill change tagging lives in Appendix A; the cosmetic-fix list (verbatim from `.planning/AUDIT.md` §AUDIT-07) lives in Appendix B; research-blocked phases are flagged in Appendix C; migration cutover rules (restating `.planning/DESIGN.md` §DESIGN-08 in implementer-readable form) live in Appendix D; every Phase-4 deferral is enumerated in Appendix E for the OPEN-QUESTIONS register handoff.

    **Reading conventions.** Each phase H2 carries an inline milestone tag `(v2.X)` so milestone bundling is visible without re-reading the executive summary. Phase 7's H2 explicitly carries `[BLOCKED — see Appendix C]` because it inherits OPEN-01 as a hard blocker. Per D-38, each phase section opens with a brief ordering-rationale paragraph (why this phase appears at this position) and a 6-row Attribute / Detail mini-table (Deliverables / Depends on / Addresses / Avoids pitfalls / Skills introduced/modified / Research-blocked). Cited REQ-IDs come from `.planning/REQUIREMENTS.md` § "Future Requirements (deferred — v2.1+)" (FOUND-/PLAT-/STG*-/SURF- families); cited PITFALL IDs come from `.planning/research/PITFALLS.md` (CRIT-/MOD-/MIN- families). Inline `OPEN: Phase 4` deferral markers appear at point of use AND in Appendix E closed list per D-27 carried. Citations use `file:line` format with backtick wrapping per D-14 carried. Severity tags `[COSMETIC]` / `[NEW]` apply only to AUDIT-07 lifts in Appendix B (D-15 narrowed); CHANGE-N rows themselves carry phase / milestone tags, not severity tags.

    **Reviewer flow.** Two paths through this document. **Read-in-order** (executive summary → 9-phase plan → appendices) gives the full sequencing argument. **Skip-to-contract** uses the executive summary table as a TOC — Phase column links to the full mini-table; Skills-delta-count column ties forward to Appendix A; Blocker column ties forward to Appendix C or Appendix E. For specific lookups: Appendix A is the "is skill X changing in v2" lookup; Appendix B is the "what cosmetic fixes ship in v2.1" lookup; Appendix C is the "what's still unknown" lookup; Appendix D is the "how does v2 read v0.3.0 frontmatter" lookup; Appendix E is the "what's deferred to OPEN-QUESTIONS" lookup.

    **Source-of-truth pointers.** v0.3.0 facts → `.planning/AUDIT.md` (Phase 1 deliverable, approved 2026-05-09). v2 architecture → `.planning/DESIGN.md` (Phase 2 deliverable, approved 2026-05-10). Phase ordering rationale → `.planning/research/SUMMARY.md` § "Phase Ordering Rationale". CRIT/MOD/MIN pitfall IDs → `.planning/research/PITFALLS.md`. v2.1+ deferred REQ-IDs → `.planning/REQUIREMENTS.md` § "Future Requirements (deferred — v2.1+)".

    **Phase boundary.** No skill files edited in this milestone (kickoff design-only mandate; per `.planning/PROJECT.md`). v2.1+ build phases sequenced by this change list execute against `.planning/DESIGN.md` (architecture) AND this change list (sequencing) AND any `/gsd-research-phase` outputs filling Appendix C blockers. **Approval gate:** single approval at end (full CHANGELIST.md reviewed as a whole) per `.planning/ROADMAP.md` Phase 3 approval gate. Phase 4 (Open questions register) follows after this approval.
    ```

    **Meta-pattern reword (Phase 2 02-10 Rule-2 carried):** Use prose form `OPEN: Phase 4` (without surrounding `[...]` brackets) — do NOT emit any literal `[OPEN: Phase 4 — ...]` pattern in this section. The reconciliation regex `\[OPEN: Phase 4 — [^]]+\]` would match it.

    **Implementing decisions:** D-19 carried (matrix-then-prose framing), D-32 carried (citation format), D-34 carried (AUDIT.md grounding), D-38 carried (mini-table convention reference), D-42 carried (Phase 7 [BLOCKED] convention reference), D-27 carried (OPEN markers convention reference).
  </action>
  <verify>
    <automated>HOWREAD_BODY=$(awk -v hdr='## How to read this change list' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/CHANGELIST.md) && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Document purpose' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Reading conventions' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Reviewer flow' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Phase boundary' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Source-of-truth pointers' && OPEN_BRACKETS=$(printf '%s\n' "$HOWREAD_BODY" | grep -cE '\[OPEN: Phase 4 — [^]]+\]') && [ "$OPEN_BRACKETS" = "0" ] && echo TASK_2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## How to read this change list` body is populated (placeholder note removed)
    - Body has 5 bold-headed paragraphs: `Document purpose`, `Reading conventions`, `Reviewer flow`, `Source-of-truth pointers`, `Phase boundary`. 5 individual `grep -qF` checks succeed.
    - Body explicitly mentions: per-phase mini-tables per D-38; inline `OPEN: Phase 4` markers (prose form, NOT bracket form); `file:line` citation format; per-bullet sentinel discipline on Appendix B; severity tags `[COSMETIC]` apply only to Appendix B; Phase 7 `[BLOCKED]` H2 tag.
    - Body explicitly names all 4 source-of-truth files: AUDIT.md, DESIGN.md, SUMMARY.md, PITFALLS.md, REQUIREMENTS.md (5 files).
    - Body explicitly names all 5 Appendices' lookup roles: A (skill lookup) / B (cosmetic-fix) / C (research-blocked) / D (migration cutover) / E (deferred OPEN-QUESTIONS).
    - **Meta-pattern reword applied:** `printf '%s\n' "$HOWREAD_BODY" | grep -cE '\[OPEN: Phase 4 — [^]]+\]'` returns `0` (no bracket-form `[OPEN]` patterns leak into the reader-flow guide).
    - **No accidental modifications** to Executive Summary (Task 1) OR Phase 1..9 mini-tables OR Appendix A / B / C / D / E from this task.
  </acceptance_criteria>
  <done>
    `## How to read this change list` populated with 5-paragraph reader-flow guide explaining document purpose, reading conventions, reviewer flow, source-of-truth pointers, and phase boundary. No `[OPEN: Phase 4 — ...]` bracket-form patterns leak into the body (meta-pattern reword applied per Phase 2 02-10 Rule-2 precedent carried).
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate `## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS` with 8-bullet enumeration via documented reconciliation algorithm + final structural-check pass — CHANGELIST.md becomes REVIEWER-READY</name>
  <files>.planning/CHANGELIST.md</files>
  <read_first>
    - .planning/CHANGELIST.md (current state after Tasks 1 + 2)
    - .planning/phases/03-changelist/03-CONTEXT.md §"Open-questions handling" D-27 carried
    - .planning/phases/02-design/02-10-PLAN.md Task 2 — Phase 2 reconciliation algorithm precedent (HIGH #3 fix); §"interfaces" reconciliation algorithm steps verbatim
    - .planning/phases/02-design/02-10-SUMMARY.md — Phase 2 reconciliation algorithm execution result (8 bullets, 3 conditions all pass)
    - .planning/phases/03-changelist/scripts/changelist-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/CHANGELIST.md` — populate `## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS` H2 section IN PLACE (REPLACE the populated-by note with bullet enumeration).

    **CRITICAL — opening sentinel preservation:** The H2 anchor and opening sentence (`Closed list of every \`[OPEN: Phase 4 — ...]\` marker in this document. Phase 4 OPEN-QUESTIONS register can be built mechanically by walking this list — every row becomes a register entry with the owning phase already assigned. New deferrals discovered during Phase 3 authoring add a row here AND inline at point of use.`) is PRE-LAID by 03-01. DO NOT modify those lines. APPEND the populated content AFTER the opening sentence.

    **Reconciliation algorithm (DOCUMENTED per cross-AI HIGH #3 carried from Phase 2 02-10):**

    Step 1 — **Extract inline marker text from BODY (excluding Appendix E section).** Use stateful awk:
    ```bash
    INLINE_BODY=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/CHANGELIST.md)
    INLINE_MARKERS=$(printf '%s\n' "$INLINE_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u)
    INLINE_COUNT=$(printf '%s\n' "$INLINE_MARKERS" | grep -c '^' || true)
    ```
    Expected: INLINE_COUNT = 8 (matches Plan 03-06 cumulative-marker baseline).

    Step 2 — **Generate one bullet per unique inline marker, in document order.** For each marker, find its line number via `grep -nF "<marker text>" .planning/CHANGELIST.md`, walk backward to the nearest preceding `^## ` H2 to identify the source section, and emit one bulleted entry in this format:

    `- **\`[OPEN: Phase 4 — <marker text>]\`** — source: §<H2 section name> → mini-table Research-blocked cell — owner: Phase <N> per CHANGE-04`

    Concretely, the 8 bullets in CHANGELIST.md document order (per Plan 03-06 enumeration):

    ```markdown
    - **`[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]`** — source: §Phase 1: Foundations + Connector Verification (v2.1) → mini-table Research-blocked cell — owner: Phase 1/Phase 2 per CHANGE-04
    - **`[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]`** — source: §Phase 1: Foundations + Connector Verification (v2.1) → mini-table Research-blocked cell — owner: Phase 1/Phase 2 per CHANGE-04
    - **`[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]`** — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04 (`platform-pipefy` build)
    - **`[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]`** — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04
    - **`[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]`** — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04
    - **`[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]`** — source: §Phase 2: Internalise Platform Skills (v2.1) → mini-table Research-blocked cell — owner: Phase 2 per CHANGE-04
    - **`[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]`** — source: §Phase 4: Tech spec + Cost + Implementation prompt (v2.3) → mini-table Research-blocked cell — owner: Phase 4 per CHANGE-04 (decide before Stage 6 build per CHANGE-01 ordering)
    - **`[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]`** — source: §Phase 7: Native-AI knowledge push (v2.5) [BLOCKED — see Appendix C] → mini-table Research-blocked cell — owner: Phase 7 per CHANGE-04
    ```

    Step 3 — **Anchored bullet count check (per cross-AI HIGH #3):**
    ```bash
    APPENDIX_E_BODY=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/CHANGELIST.md)
    LIST_BULLETS=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -cE '^- \*\*\`\[OPEN: Phase 4' || true)
    ```
    Expected: LIST_BULLETS = 8.

    Step 4 — **Normalised text comparison** (per cross-AI HIGH #3):
    ```bash
    LIST_MARKER_TEXTS=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u)
    diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS")
    ```
    Expected: empty diff (DIFF_EXIT=0).

    Step 5 — **Ownership-per-bullet (cross-AI #10):**
    ```bash
    OWNERS_PRESENT=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -cE '^- \*\*\`\[OPEN.*owner: (Phase|TBD)' || true)
    ```
    Expected: OWNERS_PRESENT = 8 = LIST_BULLETS.

    Step 6 — **Run final structural-check.** `bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 0 (all 12 assertions pass).

    Add framing paragraph above bullet list: `This list enumerates every inline OPEN-Phase-4 marker in CHANGELIST.md, in document order, as the canonical handoff to Phase 4 OPEN-QUESTIONS.md. Each item names the source section + recommended owning phase per CHANGE-04 (or \`owner: TBD\` if not yet decided — never silently omitted). Phase 4 register builds against this list mechanically.`

    Add closing paragraph: `**Verification (run by Phase 4 / final reviewer).** The reconciliation algorithm in Plan 03-07 (Phase 3 synthesis) verifies (a) inline-marker text count outside this section matches list-bullet count; (b) \`diff\` of normalised marker texts is empty; (c) every bullet carries \`owner:\` (Phase or TBD). Mismatch on any condition halts Phase 4 register build. Algorithm precedent: Phase 2 02-10 PLAN.md Task 2 (cross-AI HIGH #3 fix carried). New deferrals discovered after Phase 3 approval add a bullet here AND inline at point of use; reconciliation re-runs.`

    All conditions pass → CHANGELIST.md is **reviewer-ready** (terminal state).

    **Implementing decisions:** D-19 carried (Appendix E = closed-list mechanical-walk shape), D-27 carried (closed list enumerates every inline marker; Phase 4 register builds mechanically), D-32 carried, D-35 carried (success-criteria echo precedent — Phase 3 has no per-CHANGE echoes per D-38; Appendix E is the closed-list equivalent for OPEN markers).
  </action>
  <verify>
    <automated>APPENDIX_E_BODY=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/CHANGELIST.md) && LIST_BULLETS=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -cE '^- \*\*`\[OPEN: Phase 4' || true) && [ "$LIST_BULLETS" = "8" ] && INLINE_BODY=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/CHANGELIST.md) && INLINE_MARKERS=$(printf '%s\n' "$INLINE_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u) && INLINE_COUNT=$(printf '%s\n' "$INLINE_MARKERS" | grep -c '^' || true) && [ "$INLINE_COUNT" = "$LIST_BULLETS" ] && LIST_MARKER_TEXTS=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -oE '\[OPEN: Phase 4 — [^]]+\]' | sort -u) && diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS") && OWNERS_PRESENT=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -cE '^- \*\*`\[OPEN.*owner: (Phase|TBD)' || true) && [ "$OWNERS_PRESENT" = "$LIST_BULLETS" ] && bash .planning/phases/03-changelist/scripts/changelist-structure-check.sh; CODE=$?; echo "FINAL_STRUCTURAL_CHECK=$CODE"; [ "$CODE" = "0" ] && echo SYNTHESIS_COMPLETE_REVIEWER_READY</automated>
  </verify>
  <acceptance_criteria>
    - Appendix E opening sentinel preserved unchanged from 03-01: `grep -qF 'Closed list of every' .planning/CHANGELIST.md` succeeds AND opening sentence still verbatim
    - Appendix E body has exactly 8 anchored bullets:
      ```
      APPENDIX_E_BODY=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/CHANGELIST.md)
      LIST_BULLETS=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -cE '^- \*\*`\[OPEN: Phase 4' || true)
      [ "$LIST_BULLETS" = "8" ]
      ```
    - **Reconciliation cardinality match:** `INLINE_COUNT == LIST_BULLETS == 8` (the algorithm in `<action>` step 1 + step 3 produces equal counts)
    - **Reconciliation textual match:** `diff <(printf '%s\n' "$INLINE_MARKERS") <(printf '%s\n' "$LIST_MARKER_TEXTS")` produces empty output (DIFF_EXIT=0)
    - **Reconciliation ownership-per-bullet (cross-AI #10):** Every bullet contains `owner: Phase` OR `owner: TBD`:
      ```
      OWNERS_PRESENT=$(printf '%s\n' "$APPENDIX_E_BODY" | grep -cE '^- \*\*`\[OPEN.*owner: (Phase|TBD)' || true)
      [ "$OWNERS_PRESENT" = "8" ]
      ```
    - **Inline-marker scope EXCLUDES Appendix E section** (cross-AI HIGH #3 fix): `INLINE_BODY` extraction uses `awk '/^## Appendix E .../{exit} {print}'` form
    - **Anchored bullet pattern** (cross-AI HIGH #3 fix): LIST_BULLETS count uses `^- \*\*\`\[OPEN: Phase 4` (NOT just any `[OPEN: Phase 4` substring) — distinguishes list bullets from prose
    - Framing paragraph present: `printf '%s\n' "$APPENDIX_E_BODY" | grep -qF 'as the canonical handoff to Phase 4 OPEN-QUESTIONS.md'` succeeds
    - Closing paragraph present + cites Phase 2 02-10 algorithm precedent: `printf '%s\n' "$APPENDIX_E_BODY" | grep -qF 'Phase 2 02-10'` succeeds
    - **Final structural-check exits 0** — all 12 assertions pass; CHANGELIST.md is structurally complete AND **reviewer-ready** (terminal state)
    - **Bullet 7 (Phase 4 risk-multiplier) format check:** the bullet for the Phase 4 marker contains `decide before Stage 6 build per CHANGE-01 ordering` substring (per the action-block format)
    - **All 8 unique markers represented in bullets:** 8 individual `grep -qF` checks against the verbatim marker text strings (Pipefy 2026 rate-limit / Wrike 2026 rate-limit / Pipefy GraphQL pagination / Wrike AI Studio / Ziflow ReviewAI / Ziflow read-after-create / risk-multiplier defaults / Pipefy AI KB) — all succeed
  </acceptance_criteria>
  <done>
    Appendix E populated with 8-bullet enumeration of every unique inline `[OPEN: Phase 4 — ...]` marker in CHANGELIST.md document order. Each bullet carries verbatim marker text + source section + explicit `owner: Phase <N> per CHANGE-04`. Reconciliation algorithm (carried from Phase 2 02-10 HIGH #3) executes correctly: cardinality match (INLINE_COUNT=LIST_BULLETS=8); textual match (diff empty); ownership-per-bullet (OWNERS_PRESENT=8). Final structural-check exits 0 — all 12 assertions PASS. **CHANGELIST.md is structurally complete AND reviewer-ready** (terminal state).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Closed `[OPEN]` list ↔ inline markers | Carried from Phase 2 02-10. Per D-27, the closed list MUST enumerate every inline marker. The naive count-matching approach is fragile because list entries quote the same marker text — both inline AND list contribute to the same `[OPEN: Phase 4` substring count. The fix uses scope (exclude Appendix E section from inline-body scan) AND anchored bullet pattern (count list bullets distinctly) AND text comparison (via diff). |
| Marker-ownership per cross-AI #10 carried | Tampering class. Every Appendix E bullet cites owner phase. Silent omission breaks Phase 4 mechanical build. Mitigation: acceptance criteria assert `OWNERS_PRESENT == LIST_BULLETS`. |
| Executive Summary table cells ↔ inline-marker reconciliation | Tampering class. If Phase 4 Blocker cell uses bracket-form `[OPEN: Phase 4 — ...]`, INLINE_COUNT inflates to 9 and reconciliation breaks. Mitigation: action block forbids bracket form in Executive Summary; acceptance criteria check `grep -cE '\[OPEN: Phase 4` against Executive Summary returns 0. |
| Reviewer-ready terminal state | Wording class. "Reviewer-ready" applies HERE (Plan 03-07 = Phase 3 terminal state, mirroring Phase 2 02-10). Mitigation: SUMMARY.md uses "reviewer-ready" verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Closed `[OPEN]` list omits an inline marker | mitigate | Documented reconciliation algorithm (HIGH #3 carried): scoped inline-body extraction + anchored bullet count + text diff. All 3 conditions enforced via acceptance criteria. |
| T-03-07-02 | Tampering | Naive `grep -c '\[OPEN: Phase 4'` over whole document double-counts (HIGH #3 carried) | mitigate | Algorithm uses scoped inline-body extraction (`awk '/^## Appendix E .../{exit} {print}'` excludes the closed-list section) + anchored bullet pattern (`^- \*\*\`\[OPEN: Phase 4` matches list bullets only) + diff of normalised texts. T-03-07-01 and T-03-07-02 share the same fix. |
| T-03-07-03 | Tampering | Executive Summary Phase 4 Blocker cell uses bracket-form `[OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22]` — inflates INLINE_COUNT to 9, reconciliation breaks (Phase 2 02-10 Rule-2 meta-pattern reword precedent) | mitigate | Action block specifies prose form `\`OPEN: Phase 4 — risk-multiplier defaults pending dYdX-historical validation per D-22\`` (without brackets) for the Executive Summary Phase 4 Blocker cell. Acceptance criteria explicitly check `grep -cE '\[OPEN: Phase 4'` against Executive Summary section returns 0. |
| T-03-07-04 | Tampering | Preamble or How-to-read body emits literal `[OPEN: Phase 4 — ...]` ellipsis pattern as documentation — inflates INLINE_COUNT (Phase 2 02-10 Rule-2 carried) | mitigate | Action block specifies prose form `OPEN: Phase 4 markers` for both sections. Acceptance criteria assert grep returns 0 in both regions. |
| T-03-07-05 | Tampering | Plan 03-07 reintroduces placeholder content from earlier plans | mitigate | This plan only edits 4 specific regions (preamble / Executive Summary / How-to-read / Appendix E). Acceptance criteria use awk-scoped queries against the 4 target regions. |
| T-03-07-06 | Tampering | Final structural-check exits non-zero due to synthesis content breaking an existing assertion | mitigate | Each Task ends with structural-check check (Task 1 + Task 3 both run the script). Multiple checkpoints catch any structural break. Task 3 specifically asserts exit code = 0. |
| T-03-07-07 | Tampering | Bullet text drifts from inline marker text — `diff` non-empty even though count matches | mitigate | Action block explicitly specifies the verbatim bullet text (matching DESIGN.md Appendix E baseline + Plan 03-02/03/06 inline-marker text). Acceptance criteria run `diff` and assert empty output. |
| T-03-07-08 | Tampering | Bullet missing `owner: Phase <N>` field (cross-AI #10 carried) | mitigate | Acceptance criteria explicitly assert `OWNERS_PRESENT == LIST_BULLETS == 8`. The pattern `^- \*\*\`\[OPEN.*owner: (Phase\|TBD)` requires every bullet to carry `owner:`. |
| T-03-07-09 | Tampering | Wording underclaims (still says "structurally passable" instead of "reviewer-ready") OR overclaims | mitigate | Plan 03-07's `<done>`, `<verification>`, and `<output>` SUMMARY use "reviewer-ready" verbatim. Phase 2 02-10 reserved this phrase for the terminal plan; Phase 3 03-07 mirrors. |
| T-03-07-10 | Tampering | Plan 03-07 race-condition with parallel write — no other plan writes simultaneously, but if 03-06 hadn't completed by the time 03-07 runs, the inline-marker baseline would be incomplete | mitigate | `depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]` enforces sequential execution. Plan 03-07 is the LAST plan and depends on ALL six prior plans; the orchestrator will not run 03-07 until 03-06 completes. |
</threat_model>

<verification>
End of Plan 03-07 success-state:
1. Preamble blockquote finalised (placeholder removed; reviewer-flow + reading-conventions + phase-boundary + single-gate-approval prose added).
2. `## Executive Summary` populated with 5-column 9-row condensed milestone-bundling table per CONTEXT Claude's-Discretion default.
3. Two D-37 verbatim sentences emitted after the Executive Summary table (re-bundling caveat + OPEN-01 contingent fallback).
4. `## How to read this change list` populated with 5-paragraph reader-flow guide.
5. `## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS` populated with 8-bullet enumeration in document order; each bullet carries source + owner per cross-AI #10 carried.
6. Reconciliation algorithm (cross-AI HIGH #3 carried) passes all 3 conditions: cardinality match + textual match + ownership-per-bullet.
7. Final structural-check exits 0 — all 12 assertions pass.
8. **CHANGELIST.md is reviewer-ready** (terminal state).

Manual reviewer attestation:
- [ ] Preamble accurately describes the document.
- [ ] Executive Summary table cells match actual contracts in their referenced phase / appendix sections.
- [ ] How-to-read reader-flow paragraphs are clear and accurate (no missing source-of-truth pointer).
- [ ] Appendix E reconciles 1:1 with inline `[OPEN]` markers via the documented algorithm — NO orphans either way.
- [ ] Every Appendix E bullet carries explicit ownership (`owner: Phase <N>` — none use TBD in this synthesis since all 8 markers have explicit owners).
- [ ] Final structural-check exits 0.
- [ ] CHANGELIST.md is reviewer-ready (terminal state phrase used in this plan only — Plans 03-01..03-06 were "in progress").
</verification>

<success_criteria>
- [ ] Preamble blockquote finalised (placeholder removed)
- [ ] Executive Summary populated with 5-column 9-row condensed milestone-bundling table per CONTEXT Claude's-Discretion default
- [ ] D-37 re-bundling caveat verbatim sentence + OPEN-01 contingent fallback verbatim sentence emitted after the Executive Summary table
- [ ] How to read this change list populated with 5-paragraph reader-flow guide
- [ ] Appendix E populated with 8-bullet enumeration in document order
- [ ] Reconciliation algorithm passes all 3 conditions (cardinality + textual + ownership-per-bullet — cross-AI HIGH #3 + #10 carried)
- [ ] Final structural-check exits 0 — all 12 assertions pass
- [ ] No content under `dydx-delivery/` modified
- [ ] **CHANGELIST.md is reviewer-ready** (terminal state phrase used in `<done>`, `<verification>`, `<success_criteria>`, `<output>` SUMMARY — mirrors Phase 2 02-10 terminal-state precedent)
- [ ] No modifications outside `.planning/CHANGELIST.md` (preamble + Executive Summary + How-to-read + Appendix E sections) + `.planning/phases/03-changelist/03-07-SUMMARY.md`
</success_criteria>

<output>
After completion, create `.planning/phases/03-changelist/03-07-SUMMARY.md` documenting:
- Preamble finalised; placeholder removed
- Executive Summary: 9 rows (one per phase) in 5-column condensed milestone-bundling shape per CONTEXT Claude's-Discretion default; D-37 re-bundling caveat + OPEN-01 contingent fallback verbatim sentences emitted after the table
- How to read this change list: 5 bold-headed paragraphs (Document purpose / Reading conventions / Reviewer flow / Source-of-truth pointers / Phase boundary)
- Appendix E: 8-bullet enumeration in document order — each bullet cites source section + explicit `owner: Phase <N> per CHANGE-04` (cross-AI #10 carried; never silently omitted)
- Reconciliation algorithm executed (cross-AI HIGH #3 carried): INLINE_COUNT = LIST_BULLETS = 8 (cardinality match); diff empty (textual match); OWNERS_PRESENT = 8 = LIST_BULLETS (ownership-per-bullet)
- Final structural-check: exits 0 — all 12 assertions PASS
- **CHANGELIST.md is reviewer-ready and ready for Phase 3 approval gate review per ROADMAP.**
- **All 5 CHANGE-* requirements COMMITTED:**
  - CHANGE-01 (9-phase build plan with rationale): COMMITTED via Plans 03-02 + 03-03 (Phases 1-9 mini-tables) + Executive Summary milestone-bundling table
  - CHANGE-02 (per-skill delta): COMMITTED via Plan 03-04 (Appendix A 20-row matrix)
  - CHANGE-03 (cosmetic-fix list scheduled for v2.1): COMMITTED via Plan 03-05 (Appendix B verbatim AUDIT-07 lift)
  - CHANGE-04 (research-blocked phases flagged): COMMITTED via Plan 03-06 (Appendix C 2-row matrix + Phase 1/Phase 2 inline marker enrichment)
  - CHANGE-05 (migration cutover rules): COMMITTED via Plan 03-05 (Appendix D 7-rule numbered checklist)
- **Patterns carried from Phase 2 02-10:** stateful section_between helper (HIGH #1 → 03-01 script); reconciliation algorithm with scoped extraction + anchored bullet count + diff (HIGH #3 → this plan); ownership-per-bullet enforcement (#10 → this plan); meta-pattern reword on preamble/How-to-read/Executive Summary to avoid `[OPEN: Phase 4 — ...]` literal substring leakage (Rule-2 → Tasks 1 + 2)
- Pointer: "Phase 3 deliverable complete. Submit `.planning/CHANGELIST.md` for human approval per ROADMAP Phase 3 approval gate. After approval, Phase 4 (Open questions register) begins per ROADMAP sequencing — `/gsd-discuss-phase 4` next. Phase 4 OPEN-QUESTIONS.md register builds mechanically by walking Appendix E (8 baseline rows + any Phase 4 additions for OPEN-04..07 per REQUIREMENTS.md OPEN-* family)."
</output>
</content>
</invoke>
