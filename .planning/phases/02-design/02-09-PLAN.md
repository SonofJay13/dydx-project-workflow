---
plan_id: 02-09
phase: 02-design
plan: 09
type: execute
wave: 9
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07, 02-08]
requirements: [DESIGN-28, DESIGN-29, DESIGN-30]
files_modified:
  - .planning/phases/02-design/02-09-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-09-SUMMARY.md
autonomous: true
tags: [design, test-bot-architecture, tier-boundary, client-state-schema, drift-detection]

must_haves:
  truths:
    - "DESIGN.md `## Test bot architecture` `### DESIGN-28: tier-1 / tier-2 boundary` populated with interface-level contract (per D-30): Python tier-1 asserts state, schema, presence/absence, equality, regex, retry-count, status-code class; AI tier-2 interprets free-form output, classifies failure causes, suggests remediation; mixed-layer cases flagged for human design; 1 worked test-case classification example."
    - "DESIGN.md `## Test bot architecture` `### DESIGN-29: client_state.yaml skeleton` populated with skeleton YAML (per D-30): top-level keys (sandbox tenant IDs gated by platform, fixtures, integration toggles, `wrike_host`, `last_known_schema` per platform, `last_passed_at` per test case, `targets_artefact` per test case for obsolescence detection); 1 worked example per platform; not full per-test fixtures."
    - "DESIGN.md `## Test bot architecture` `### DESIGN-30: drift-detection contract` populated (per D-30): inputs (current sandbox schema, cached `last_known_schema`); outputs (match → proceed / mismatch → halt + emit `schema_drift_report.md`); halt condition; report shape; human-action requirement (acknowledge or revert); without numbered pseudocode."
    - "Echo lines: DESIGN-28, 29, 30 — 3 NEW echoes (one per H3 subsection). Running total: 32/30 (assertion #4 echo-count >= 30 now PASSES with margin)."
    - "End-of-Plan-02-09 state: DESIGN.md is **structurally passable** (per cross-AI review MEDIUM #7 — wording revised). The structural-check script exits 0 — all 9 assertions pass. However, **reviewer-readiness** (full enumerated `[OPEN]` list reconciled with inline markers; executive summary populated; Appendix A glossary; Appendix B traceability; preamble finalised) is the END-OF-PHASE state, achieved only by Plan 02-10 synthesis. Plan 02-09 hands off a structurally-passable DESIGN.md; Plan 02-10 makes it reviewer-ready."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-28, DESIGN-29, DESIGN-30 populated under `## Test bot architecture` H2"
      contains: "Tier-1/tier-2 boundary table + 1 worked TC classification; client_state.yaml YAML skeleton with 3 worked per-platform examples; drift-detection contract; 3 echo lines"
  key_links:
    - from: "DESIGN-28 tier boundary"
      to: "DESIGN-04 test-bot-orchestrator agent"
      via: "agent file `dydx-delivery/agents/test-bot-orchestrator.md`"
      pattern: "test-bot-orchestrator"
    - from: "DESIGN-29 client_state.yaml schema"
      to: "Stage 8a (DESIGN-24) provision-test-harness skill"
      via: "Persistent harness location `<Client> Brain/test-bot/client_state.yaml`"
      pattern: "<Client> Brain/test-bot/client_state.yaml"
    - from: "DESIGN-30 drift-detection contract"
      to: "Stage 8d (DESIGN-24) execute-tests skill"
      via: "Pre-flight algorithm: fetch current schema → diff against cached → halt + emit drift report on mismatch"
      pattern: "schema_drift_report.md"
---

<objective>
Populate `.planning/DESIGN.md` `## Test bot architecture` H2 region with DESIGN-28 (tier-1 / tier-2 boundary), DESIGN-29 (client_state.yaml skeleton), DESIGN-30 (drift-detection contract).

**Purpose:** Per CONTEXT D-30 — interface contract + skeleton schema. Locks the architectural shape that Stage 8 (DESIGN-24 overview from Plan 02-08) builds against. Algorithmic detail deferred to Stage 8 build phase per D-30. This is the LAST content plan; Plan 02-10 handles synthesis.

**Output:** `.planning/DESIGN.md` `## Test bot architecture` body populated. 3 echo lines added. Running total: 32/30 — structural-check assertion #4 NOW PASSES.

**End-of-Plan-02-09 state (per cross-AI review MEDIUM #7 — wording revised):** DESIGN.md is **structurally passable** — the structural-check script exits 0 and all 9 assertions pass. **Reviewer-readiness** is the END-OF-PHASE state, achieved only by Plan 02-10 synthesis (executive summary + Appendix A + Appendix B + finalised preamble + closed `[OPEN]` list with full inline-marker reconciliation). The seed `[OPEN]` marker placed by Plan 02-01 in the Deferred section is still just a seed at end of Wave 9; Plan 02-10 expands it into the full enumerated list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-08-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- DESIGN-28..30 live under `## Test bot architecture` H2.
     Per CONTEXT D-30: interface-level contract + skeleton schema. NO numbered pseudocode.
     The 3 H3 anchors already exist from Plan 02-01 skeleton.

     Section-range extraction in acceptance criteria uses awk ranges with DISTINCT start/end anchors
     (e.g., `awk '/^### DESIGN-28:/,/^### DESIGN-29:/'`) — these are SAFE because the start regex
     does NOT match the end regex on the same line. -->

DESIGN-28 tier boundary table (concrete values per REQUIREMENTS):

| Tier | Owner | Asserts | Authoring | Examples |
|------|-------|---------|-----------|----------|
| Tier-1 (deterministic) | Python (`test_runner.py`) | state, schema, presence/absence, equality, regex, retry-count, status-code class | HUMAN-AUTHORED (per Out of Scope: "Generating Python tier-1 tests from natural language alone") | `assert response.status_code == 200`; `assert "client_id" in response.json()`; `assert re.match(r"^pipe-[0-9]+$", value)` |
| Tier-2 (AI orchestrator) | `test-bot-orchestrator` agent (DESIGN-04) | free-form output interpretation, failure cause classification, remediation suggestion | AI-GENERATED via agent invocation in 8d | "Classify this failure: <stderr>" → returns `failure_class:` per DESIGN-24 enum |

DESIGN-29 client_state.yaml skeleton (top-level keys per CONTEXT D-30):

```yaml
# <Client> Brain/test-bot/client_state.yaml
# Skeleton — DESIGN-29 contract (Phase 2). Stage 8a bootstraps + delta-updates per ship.
client: <client_slug>
client_state_version: <semver>
last_provisioned_at: <ISO>

sandbox:
  pipefy:
    pipe_id: <sandbox-pipe-id>
    api_token_ref: env:PIPEFY_SANDBOX_TOKEN
  wrike:
    space_id: <sandbox-space-id>
    host: <wrike-host-from-OAuth-token-response>  # PERSISTED per DESIGN-15
    api_token_ref: env:WRIKE_SANDBOX_TOKEN
  ziflow:
    project_id: <sandbox-project-id>
    api_token_ref: env:ZIFLOW_SANDBOX_TOKEN
  coda:
    doc_id: <sandbox-coda-doc>   # CRIT-5 fix per DESIGN-24
    api_token_ref: env:CODA_SANDBOX_TOKEN

fixtures:
  shared:
    test_user_email: test@<client_slug>.example
  pipefy:
    test_card_template: <template-id>
  wrike:
    test_task_template: <template-id>
  ziflow:
    test_proof_template: <template-id>

integration_toggles:
  pipefy_to_wrike: true | false
  pipefy_to_ziflow: true | false
  wrike_to_ziflow: true | false
  any_to_coda: true

last_known_schema:
  pipefy:
    fetched_at: <ISO>
    schema_hash: <sha256>
    schema_snapshot_path: ./schema_cache/pipefy_<ISO>.json
  wrike:
    fetched_at: <ISO>
    schema_hash: <sha256>
    schema_snapshot_path: ./schema_cache/wrike_<ISO>.json
  ziflow:
    fetched_at: <ISO>
    schema_hash: <sha256>
    schema_snapshot_path: ./schema_cache/ziflow_<ISO>.json

test_cases:
  TC-001:
    targets_artefact: <Client> Brain/<Project>/04a_fnspec-platform_v2.md
    last_passed_at: <ISO>
    layer: tier-1 | tier-2 | mixed
    state: active | obsolete | quarantined
  TC-002:
    targets_artefact: <Client> Brain/<Project>/04b_fnspec-integration_v2.md
    last_passed_at: <ISO>
    layer: tier-2
    state: active
```

3 worked per-platform examples:
- **Pipefy example**: client `acme-inc`, sandbox `pipe-12345`, schema fetched 2026-04-15.
- **Wrike example**: client `acme-inc`, sandbox `space-67890`, host `https://app-us2.wrike.com/api/v4` (PERSISTED).
- **Ziflow example**: client `acme-inc`, sandbox `project-abcde`.

DESIGN-30 drift-detection contract shape:

```markdown
**Inputs.**
- Current sandbox schema (fetched fresh).
- Cached `last_known_schema.<platform>.schema_hash` from `client_state.yaml`.

**Outputs.**
- **Match**: proceed.
- **Mismatch**: HALT + emit `schema_drift_report.md`.

**Halt condition.** Mismatch → no tests run, no sandbox writes, no `last_passed_at:` updates.

**`schema_drift_report.md` shape.**
- Frontmatter: `client:`, `platform:`, `previous_schema_hash:`, `current_schema_hash:`, `detected_at:`.
- Body: per-column diff; recommended human action.

**Human-action requirement.**
1. **Acknowledge**: human runs 8a, updates `last_known_schema`, marks affected TCs as `quarantined`.
2. **Revert**: human fixes sandbox to match `last_known_schema`; reruns 8d.

**Algorithmic detail (Stage 8 build phase territory).**
Per CONTEXT D-30, this contract specifies inputs / outputs / halt / report shape / human-action requirement WITHOUT numbered pseudocode.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Test bot architecture` opening + `### DESIGN-28: tier-1 / tier-2 boundary` subsection</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-08 complete)
    - .planning/REQUIREMENTS.md §"DESIGN-28"
    - .planning/phases/02-design/02-CONTEXT.md §"Test-bot architecture detail" D-30
    - .planning/REQUIREMENTS.md §"## Out of Scope"
    - .planning/DESIGN.md `## Stage 8: Test bot — overview` (Plan 02-08)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Test bot architecture` opening prose + `### DESIGN-28: tier-1 / tier-2 boundary` H3:

    **(A) Section opening.** Under the existing `## Test bot architecture` H2, add framing paragraph: "The test bot architecture comprises three architectural commitments locked here as decision contracts: DESIGN-28 (tier-1 / tier-2 boundary); DESIGN-29 (client_state.yaml skeleton); DESIGN-30 (drift-detection contract). Per D-30, these contracts ship interface-level + skeleton-schema fidelity — algorithmic detail is implemented in v2.1+ Phase 5 Stage 8 build per CHANGE-01 9-phase plan. Stage 8 overview (above) declared the stage-level architecture; this section provides the per-DESIGN architectural detail."

    **(B) `### DESIGN-28: tier-1 / tier-2 boundary` H3.** Replace placeholder body with:
    1. Echo line: `> **DESIGN-28:** Python ↔ AI orchestrator boundary — Python tier-1 asserts state, schema, presence/absence, equality, regex, retry-count, status-code class; AI tier-2 interprets free-form output, classifies failure causes, suggests remediation. Test plans mark each TC with the layer it belongs to. Mixed-layer cases flagged for human design.`
    2. **Tier boundary table** (per `<interfaces>` shape): 2 rows × 5 columns. Tier-1 row's "Authoring" cell explicitly cites `## Out of Scope` "Generating Python tier-1 tests from natural language alone" anti-feature.
    3. **Mixed-layer cases subsection:** When a single TC needs BOTH tier-1 AND tier-2, the test plan marks it `layer: mixed` AND flags it for human design. The mixed case is split into two TC entries OR explicitly kept as `mixed` with both layers cited per-step.
    4. **1 worked TC classification example:**
       ```
       **Example TC: "Pipefy card creation returns valid pipe_id and AI Agent picks up new card."**

       Layer split:
       - **Tier-1 (Python `test_runner.py`):**
         - `assert response.status_code == 201`
         - `assert "id" in response.json()["card"]`
         - `assert re.match(r"^[0-9]+$", str(response.json()["card"]["id"]))`
       - **Tier-2 (AI orchestrator via `test-bot-orchestrator` agent):**
         - Wait 30s for AI Agent picker.
         - Invoke agent: "Read Pipefy AI Agent log entries since <timestamp>. Did the agent acknowledge card <id>? If not, classify failure: ingest_lag | agent_skip | log_silence | unknown."
         - Tier-2 returns `failure_class:` per DESIGN-24 enum OR `success`.
       ```
    5. **Cross-references:** DESIGN-04 (test-bot-orchestrator agent location); DESIGN-24 (Stage 8 overview + failure-class enum incl. `harness_drift`); REQUIREMENTS `## Out of Scope`.

    **Implementing decisions:** D-20, D-32, D-35.
  </action>
  <verify>
    <automated>grep -qE '^### DESIGN-28: tier-1 / tier-2 boundary' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-28:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'test_runner.py' .planning/DESIGN.md &amp;&amp; grep -qF 'test-bot-orchestrator' .planning/DESIGN.md &amp;&amp; grep -qF 'HUMAN-AUTHORED' .planning/DESIGN.md &amp;&amp; grep -qF 'AI-GENERATED' .planning/DESIGN.md &amp;&amp; grep -qF 'mixed' .planning/DESIGN.md &amp;&amp; grep -qF 'failure_class:' .planning/DESIGN.md &amp;&amp; grep -qF 'Generating Python tier-1 tests from natural language alone' .planning/DESIGN.md &amp;&amp; echo DESIGN_28_OK</automated>
  </verify>
  <acceptance_criteria>
    - Section-opening framing paragraph present under `## Test bot architecture` H2 (before H3s)
    - `grep -qE '^> \*\*DESIGN-28:\*\*' .planning/DESIGN.md` succeeds
    - **Tier boundary table present (2 rows)** — SAFE awk range with distinct H3 anchors: `awk '/^### DESIGN-28:/,/^### DESIGN-29:/' .planning/DESIGN.md | grep -cE '^\| (Tier-1|Tier-2) \|'` returns >= 2
    - `grep -qF 'test_runner.py' .planning/DESIGN.md` succeeds
    - `grep -qF 'test-bot-orchestrator' .planning/DESIGN.md` succeeds
    - `grep -qF 'HUMAN-AUTHORED' .planning/DESIGN.md` succeeds
    - `grep -qF 'AI-GENERATED' .planning/DESIGN.md` succeeds
    - `grep -qF 'Generating Python tier-1 tests from natural language alone' .planning/DESIGN.md` succeeds
    - `grep -qF 'failure_class:' .planning/DESIGN.md` succeeds
    - **Worked TC classification example present** (SAFE awk range): `awk '/^### DESIGN-28:/,/^### DESIGN-29:/' .planning/DESIGN.md | grep -qF 'Tier-1 (Python'` succeeds AND `awk '/^### DESIGN-28:/,/^### DESIGN-29:/' .planning/DESIGN.md | grep -qF 'Tier-2 (AI'` succeeds
    - Mixed-layer cases handling present: `grep -qF 'layer: mixed' .planning/DESIGN.md` OR `grep -qF '`mixed`' .planning/DESIGN.md` succeeds
  </acceptance_criteria>
  <done>
    `## Test bot architecture` opening + `### DESIGN-28` populated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `### DESIGN-29: client_state.yaml skeleton` + `### DESIGN-30: drift-detection contract` subsections + run final structural-check (DESIGN.md becomes structurally passable, NOT yet reviewer-ready)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1)
    - .planning/REQUIREMENTS.md §"DESIGN-29" + §"DESIGN-30"
    - .planning/phases/02-design/02-CONTEXT.md §"Test-bot architecture detail" D-30
    - .planning/DESIGN.md `### platform-pipefy` / `### platform-wrike` / `### platform-ziflow` (Plan 02-04)
    - .planning/DESIGN.md `## Stage 8: Test bot — overview` (Plan 02-08)
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `### DESIGN-29: client_state.yaml skeleton` + `### DESIGN-30: drift-detection contract` H3s.

    **(A) `### DESIGN-29: client_state.yaml skeleton`** — replace placeholder body with:
    1. Echo line: `> **DESIGN-29:** \`client_state.yaml\` schema — sandbox tenant IDs gated by platform; fixtures; integration toggles; \`wrike_host\`; \`last_known_schema\` per platform; \`last_passed_at\` per test case; \`targets_artefact\` per test case for obsolescence detection.`
    2. **Skeleton YAML** in fenced code block (` ```yaml ` fence) per `<interfaces>` shape. Must include all top-level keys: `client:`, `client_state_version:`, `last_provisioned_at:`, `sandbox:` (with pipefy / wrike / ziflow / coda subblocks; coda per CRIT-5; wrike has `host:` per DESIGN-15), `fixtures:`, `integration_toggles:`, `last_known_schema:`, `test_cases:`.
    3. **3 worked per-platform examples** subsection: Pipefy / Wrike (host PERSISTED) / Ziflow with concrete sandbox IDs (`pipe-12345`, `space-67890`, `project-abcde`) and 1-3 TCs with `targets_artefact:` referencing canonical artefact paths.
    4. **Field-by-field rationale** subsection: brief one-line explanation per top-level key, citing the locking decision.
    5. **Cross-references:** DESIGN-15 (wrike_host); DESIGN-24 (sandbox_lock + lifecycle states + CRIT-5 Coda); DESIGN-30 (drift-detection consumes `last_known_schema`).

    **(B) `### DESIGN-30: drift-detection contract`** — replace placeholder body with:
    1. Echo line: `> **DESIGN-30:** Drift detection — pre-flight fetches current sandbox schema and diffs against \`client_state.yaml.last_known_schema\`; mismatch halts + emits \`schema_drift_report.md\` instead of executing; drift requires explicit human action (acknowledge or revert).`
    2. **Inputs subsection** per `<interfaces>` shape.
    3. **Outputs subsection:** Match → proceed; Mismatch → HALT + emit `<Client> Brain/test-bot/schema_drift_report.md`.
    4. **Halt condition subsection.**
    5. **`schema_drift_report.md` shape subsection:** Frontmatter (client / platform / previous_schema_hash / current_schema_hash / detected_at) + body (per-column diff + recommended human action).
    6. **Human-action requirement subsection:** Two paths — Acknowledge / Revert.
    7. **Algorithmic detail boundary subsection:** "Per CONTEXT D-30, this contract specifies inputs / outputs / halt condition / report shape / human-action requirement WITHOUT numbered pseudocode. The actual algorithm is implemented in v2.1+ Phase 5 Stage 8 build per CHANGE-01 9-phase plan."
    8. **Cross-references:** DESIGN-24 (`harness_drift` failure-class); DESIGN-29 (`last_known_schema` data shape input); platform-pipefy/-wrike/-ziflow.

    **(C) Run structural-check** at end: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`.

    **Expected: exits 0** (all 9 assertions pass — DESIGN.md is now **structurally passable** per cross-AI review MEDIUM #7 wording revision). Echo count = 32 (>= 30 floor met). All other assertions pass via the stateful section_between helper from Plan 02-01.

    **IMPORTANT — wording precision (per cross-AI review MEDIUM #7):** The phrase "structurally passable" describes the END-OF-PLAN-02-09 state. The phrase "reviewer-ready" / "complete and ready for Phase 2 approval gate" is reserved for the END-OF-PLAN-02-10 state (after synthesis). Plan 02-09 hands off a structurally-passable DESIGN.md; Plan 02-10 makes it reviewer-ready by adding executive summary + Appendix A glossary + Appendix B traceability + finalised preamble + the full enumerated `[OPEN]` list reconciliation.

    **Implementing decisions:** D-20, D-30, D-32, D-35.
  </action>
  <verify>
    <automated>grep -qE '^### DESIGN-29: client_state.yaml skeleton' .planning/DESIGN.md &amp;&amp; grep -qE '^### DESIGN-30: drift-detection contract' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-29:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-30:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^```yaml' .planning/DESIGN.md &amp;&amp; grep -qF 'sandbox:' .planning/DESIGN.md &amp;&amp; grep -qF 'last_known_schema:' .planning/DESIGN.md &amp;&amp; grep -qF 'targets_artefact:' .planning/DESIGN.md &amp;&amp; grep -qF 'integration_toggles:' .planning/DESIGN.md &amp;&amp; grep -qF 'wrike' .planning/DESIGN.md &amp;&amp; grep -qF 'host:' .planning/DESIGN.md &amp;&amp; grep -qF 'schema_drift_report.md' .planning/DESIGN.md &amp;&amp; grep -qF 'acknowledge or revert' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" -ge "30" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "0" ] &amp;&amp; echo TEST_BOT_ARCH_OK_STRUCTURALLY_PASSABLE</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-29:\*\*' .planning/DESIGN.md` AND `grep -qE '^> \*\*DESIGN-30:\*\*' .planning/DESIGN.md` succeed
    - YAML skeleton present (` ```yaml ` fence) — SAFE awk range with distinct H3 anchors: `awk '/^### DESIGN-29:/,/^### DESIGN-30:/' .planning/DESIGN.md | grep -qE '^```yaml'` succeeds
    - **DESIGN-29 top-level keys — all present:** `client:`, `client_state_version:`, `sandbox:`, `fixtures:`, `integration_toggles:`, `last_known_schema:`, `test_cases:`
    - 4 sandbox sub-blocks present: `pipefy:`, `wrike:`, `ziflow:`, `coda:` (coda per CRIT-5)
    - `grep -qF 'host:' .planning/DESIGN.md` succeeds (Wrike host PERSISTED)
    - `grep -qF 'targets_artefact:' .planning/DESIGN.md` succeeds
    - `grep -qF 'last_passed_at:' .planning/DESIGN.md` succeeds
    - `grep -qF 'last_known_schema:' .planning/DESIGN.md` succeeds
    - **3 worked per-platform examples present** (SAFE awk range): `awk '/^### DESIGN-29:/,/^### DESIGN-30:/' .planning/DESIGN.md` body contains concrete sandbox IDs `pipe-12345`, `space-67890`, `project-abcde` (or comparable concrete IDs)
    - **DESIGN-30 contract elements — all present:**
      - `grep -qF 'schema_drift_report.md' .planning/DESIGN.md` succeeds
      - `grep -qF 'previous_schema_hash:' .planning/DESIGN.md` AND `grep -qF 'current_schema_hash:' .planning/DESIGN.md` succeed
      - `grep -qF 'acknowledge or revert' .planning/DESIGN.md` OR (`grep -qF 'Acknowledge'` AND `grep -qF 'Revert'`) succeed
      - `grep -qF 'WITHOUT numbered pseudocode' .planning/DESIGN.md` OR `grep -qF 'without numbered pseudocode' .planning/DESIGN.md` succeeds
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns >= `30` (assertion #4 floor met; actual count = 32)
    - **Structural-check now PASSES (exit 0): DESIGN.md is structurally passable** (cross-AI review MEDIUM #7 wording — NOT "reviewer-ready"; that's Plan 02-10 state)
    - **Wording precision (cross-AI review MEDIUM #7):** Plan 02-09's `<done>` block, `<verification>` block, and `<output>` SUMMARY MUST use "structurally passable" — NOT "reviewer-ready", NOT "complete and ready for Phase 2 approval gate". Reviewer-readiness is reserved for Plan 02-10's terminal state after synthesis.
  </acceptance_criteria>
  <done>
    `### DESIGN-29` populated with skeleton client_state.yaml + 3 worked per-platform examples + field-by-field rationale. `### DESIGN-30` populated with drift-detection contract WITHOUT numbered pseudocode. Echo count = 32/30. **Structural-check exits 0 — DESIGN.md is structurally passable.** (Reviewer-ready state is the END-OF-PHASE state, achieved only by Plan 02-10 synthesis — see cross-AI review MEDIUM #7 wording revision.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DESIGN-29 client_state.yaml field naming → Stage 8a + 8d skill consumption | Field-name drift breaks the test loop. |
| DESIGN-30 algorithm boundary → Stage 8 build phase implementation | Per D-30, the algorithm is INTENTIONALLY deferred. |
| End-of-Plan-02-09 state precision (cross-AI review MEDIUM #7) | "Structurally passable" ≠ "reviewer-ready". The closed `[OPEN]` list is still a seed; full reconciliation happens in Plan 02-10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Tampering | DESIGN-29 YAML omits a top-level key | mitigate | Acceptance criteria assert all 7 top-level keys + key sub-block fields. |
| T-02-09-02 | Tampering | DESIGN-29 forgets the Coda sandbox block (CRIT-5 fix) | mitigate | Acceptance criteria explicitly assert `grep -qF 'coda:'` AND CRIT-5 cited. |
| T-02-09-03 | Tampering | DESIGN-30 ships pseudocode despite D-30 saying "WITHOUT numbered pseudocode" | mitigate | Acceptance criteria assert literal substring `WITHOUT numbered pseudocode`. |
| T-02-09-04 | Tampering | DESIGN-29 worked examples use placeholder IDs only | mitigate | Acceptance criteria require concrete sandbox IDs. |
| T-02-09-05 | Tampering | Plan 02-09 leaves the structural-check failing | mitigate | Acceptance criteria require `bash design-structure-check.sh` exits 0. |
| T-02-09-06 | Tampering | DESIGN-29 wrike sub-block hardcodes `host: www.wrike.com` | mitigate | Worked example explicitly shows `host: https://app-us2.wrike.com/api/v4/`. |
| T-02-09-07 | Tampering | Plan 02-09 wording overclaims "reviewer-ready" when only structurally-passable (cross-AI review MEDIUM #7) | mitigate | All `<done>`, `<verification>`, `<output>` text uses "structurally passable" verbatim; "reviewer-ready" is forbidden in Plan 02-09 (reserved for Plan 02-10). Acceptance criteria explicitly check that wording. |
</threat_model>

<verification>
End of Plan 02-09 success-state:
1. `## Test bot architecture` opening framing prose written
2. DESIGN-28 (tier boundary) + DESIGN-29 (client_state.yaml skeleton + 3 per-platform examples) + DESIGN-30 (drift-detection contract) populated
3. Echo count = 32/30 — assertion #4 PASSES with margin
4. **`bash design-structure-check.sh` exits 0** — all 9 assertions pass; **DESIGN.md is structurally passable** (NOT yet reviewer-ready — that's Plan 02-10's terminal state per cross-AI review MEDIUM #7)
5. Inline `[OPEN]` count unchanged from Plan 02-08
6. The closed `## Deferred to Phase 4 OPEN-QUESTIONS` list is still just the Plan 02-01 seed marker — Plan 02-10 expands it into the full enumerated reconciliation list

Manual reviewer attestation:
- [ ] DESIGN-28 tier boundary table is concrete
- [ ] DESIGN-29 YAML skeleton has all REQUIREMENTS top-level keys + 4 sandbox sub-blocks (incl. Coda CRIT-5) + Wrike host PERSISTED
- [ ] DESIGN-29 worked examples are realistic
- [ ] DESIGN-30 contract specifies interfaces only — no numbered pseudocode
- [ ] Structural-check exits 0
- [ ] Plan 02-09 wording uses "structurally passable" — NOT "reviewer-ready" (cross-AI review MEDIUM #7)
</verification>

<success_criteria>
- [ ] DESIGN-28/29/30 echo lines + decision contracts populated
- [ ] DESIGN-29: YAML skeleton + 7 top-level keys + 4 sandbox sub-blocks + 3 worked examples
- [ ] DESIGN-30: inputs / outputs / halt / report shape / human-action paths WITHOUT pseudocode
- [ ] Echo count = 32/30
- [ ] **Structural-check exits 0 — DESIGN.md is structurally passable** (cross-AI review MEDIUM #7 wording)
- [ ] No content under `dydx-delivery/` modified
- [ ] Plan wording uses "structurally passable" throughout (not "reviewer-ready" — that's Plan 02-10)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-09-SUMMARY.md` documenting:
- DESIGN-28: tier boundary + worked TC example
- DESIGN-29: client_state.yaml skeleton + 3 per-platform worked examples
- DESIGN-30: drift-detection contract (interface only)
- Echo count: 32/30
- **Structural-check exits 0 — DESIGN.md is structurally passable** (cross-AI review MEDIUM #7 wording — NOT "reviewer-ready")
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #7 (wording revised — `<done>`/`<verification>`/`<output>` use "structurally passable" instead of implying full reviewer-readiness; reviewer-readiness reserved for Plan 02-10's terminal state).
- Pointer: "Plan 02-10 (Wave 10 synthesis) handles executive summary + Appendix A glossary + Appendix B DESIGN-* traceability table + finalising the closed `[OPEN: Phase 4 — ...]` enumeration list. After Plan 02-10, DESIGN.md is reviewer-ready for Phase 2 approval gate."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-09-SUMMARY.md` to `files_modified`); MEDIUM #7 (wording precision — `<done>` block, `<verification>` block, `<success_criteria>`, `<output>`/SUMMARY pointer all use "structurally passable" instead of implying full reviewer-readiness; "reviewer-ready" / "complete and ready for Phase 2 approval gate" reserved for Plan 02-10's terminal state; T-02-09-07 added to threat register; acceptance criterion explicitly checks the wording). Existing safe `awk '/^### DESIGN-28:/,/^### DESIGN-29:/'` patterns kept (distinct start/end anchors). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-28/29/30 still in `requirements`; D-20/30/32/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
