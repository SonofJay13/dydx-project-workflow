---
plan_id: 02-10
phase: 02-design
plan: 10
type: execute
wave: 10
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07, 02-08, 02-09]
requirements: [DESIGN-01, DESIGN-02, DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06, DESIGN-07, DESIGN-08, DESIGN-09, DESIGN-10, DESIGN-11, DESIGN-12, DESIGN-13, DESIGN-14, DESIGN-15, DESIGN-16, DESIGN-17, DESIGN-18, DESIGN-19, DESIGN-20, DESIGN-21, DESIGN-22, DESIGN-23, DESIGN-24, DESIGN-25, DESIGN-26, DESIGN-27, DESIGN-28, DESIGN-29, DESIGN-30]
files_modified:
  - .planning/phases/02-design/02-10-PLAN.md
  - .planning/DESIGN.md
autonomous: true
tags: [design, synthesis, executive-summary, glossary, traceability, open-questions-list]

must_haves:
  truths:
    - "DESIGN.md `## Executive Summary` populated as TOC + per-section decision summary table — one row per major H2."
    - "DESIGN.md `## Appendix A: Glossary` populated with v2-canonical vocabulary (>= 20 entries)."
    - "DESIGN.md `## Appendix B: DESIGN-* → DESIGN.md section traceability` populated with 30-row table mapping DESIGN-NN → section anchor → locking decision IDs."
    - "DESIGN.md `## Deferred to Phase 4 OPEN-QUESTIONS` populated with FULL enumerated list of every inline `[OPEN: Phase 4 — ...]` marker (Plan 02-01 seed marker REPLACED)."
    - "DESIGN.md preamble blockquote finalised (placeholder removed)."
    - "Structural-check exits 0 — DESIGN.md structurally complete and reviewer-ready."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "Final synthesis: executive summary + Appendix A glossary + Appendix B 30-row traceability + closed [OPEN] list + final preamble"
      contains: "TOC table; >= 20 glossary entries; 30-row Appendix B; enumerated [OPEN] list reconciled with inline markers; finalised preamble"
  key_links:
    - from: "## Executive Summary table"
      to: "Every major H2 section in DESIGN.md (TOC function + skip-to-contract aid)"
      via: "Per-row Section column references H2 anchor; Decision summary one-liner; Locking IDs cite REQUIREMENTS DESIGN-NN + CONTEXT D-NN"
      pattern: "row per H2 section"
    - from: "## Deferred to Phase 4 OPEN-QUESTIONS list"
      to: "Phase 4 OPEN-QUESTIONS.md register (built mechanically by walking this list)"
      via: "One bullet per inline marker; format: `[OPEN: Phase 4 — <subject>] — source: §<H2 / H3> — owner: Phase <N> per CHANGE-04`"
      pattern: "list entries reconcile 1:1 with inline marker count outside the Deferred section"
    - from: "## Appendix B traceability"
      to: "REQUIREMENTS.md DESIGN-01..30"
      via: "30 rows; each maps DESIGN-NN → DESIGN.md section anchor → locking D-NN(s)"
      pattern: "DESIGN-NN → §<anchor> → D-NN, D-NN"
---

<objective>
Final synthesis pass on `.planning/DESIGN.md`: finalised preamble, executive summary TOC + decision-summary table, Appendix A glossary, Appendix B 30-row DESIGN-* traceability, and the finalised closed `[OPEN: Phase 4 — ...]` enumeration list.

**Purpose:** Plans 02-02..02-09 wrote the contracts. This plan makes DESIGN.md REVIEWABLE end-to-end. No new content contracts; pure synthesis.

**Output:** `.planning/DESIGN.md` complete and structurally final. Structural-check exits 0. Ready for human review per ROADMAP Phase 2 approval gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/phases/02-design/02-05-PLAN.md
@.planning/phases/02-design/02-06-PLAN.md
@.planning/phases/02-design/02-07-PLAN.md
@.planning/phases/02-design/02-08-PLAN.md
@.planning/phases/02-design/02-09-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Synthesis writes to 5 regions:
     1. Preamble blockquote (replaces placeholder line)
     2. ## Executive Summary (TOC + per-section decision summary table)
     3. ## Appendix A: Glossary (>= 20 entries)
     4. ## Appendix B: DESIGN-* → DESIGN.md section traceability (30 rows)
     5. ## Deferred to Phase 4 OPEN-QUESTIONS (replaces seed marker with full enumerated list)
     No new echo lines; no new section bodies. Anchors already exist. -->

Executive Summary table shape (one row per major H2; >= 15 rows):

| Section | Decision summary | Locking IDs |
|---------|------------------|-------------|
| `## Cross-cutting decisions` | 10 cross-cutting contracts (frontmatter, stage numbering, hard-rules SoT, plugin surfaces incl. self-tests, /refine resolution, approval gates, connector probe + degradation, migration co-existence, directional boundary, persona) | DESIGN-01..10; D-23, D-24, D-25, D-29 |
| `## Skill layout` | v2 7-directory plugin layout with NEW commands/agents/hooks/tests/references | DESIGN-11; FOUND-01 |
| `## 13-skill inventory` | v2 end-state skills (6 NEW stage + 4 MODIFIED + 2 UNCHANGED + 3 NEW platform; 1 RETIRED `generate-functional-spec`) | DESIGN-12 |
| `## Stage-by-stage hand-off contract` | 12-row transition matrix (carrier file paths, frontmatter propagation, gating status flags, hand-off message templates) | DESIGN-13; D-26 |
| `## Platform skills` | 3 internalised platform skills with identical 5-file references/ shape; 2026-grounded native-AI matrices; native_ai_path branching | DESIGN-14, 15, 16 |
| `## Stage 1: Kickoff capture` | NEW dual-branch artefact (discovery-ready vs draft SOW); Field Notes triage `processed_at IS NULL`; Miro paste fallback; auto-classification with `[unknown]` markers | DESIGN-17 |
| `## Stage 2: Discovery refactor` | MODIFIED to consume `01_kickoff_v*`; raw-notes mode RETIRED; SKIP when kickoff produced draft SOW | DESIGN-18 |
| `## Stage 3: SOW refactor` | UNCHANGED-structure; behaviour-modified to canonical lifecycle; single SOW covers platform AND integration | DESIGN-19 |
| `## Stage 4a/4b: Functional spec split` | NEW 2 skills replacing RETIRED single fnspec; per-requirement `delivery: native-ai \| api` routing key; cross-spec consistency check at start of 4b | DESIGN-20 |
| `## Stage 5: Tech spec` | MODIFIED scope-gate (full / platform-API addendum / skip); error-paths discipline (no hand-waving) | DESIGN-21 |
| `## Stage 6: Cost estimate` | NEW with risk-multiplier taxonomy STRUCTURE (numerics deferred per D-22); Coda mechanics (keyColumns + mutationStatus + 4 req/10s + schema cache); wait-for-commercial-inputs 2-halt gate | DESIGN-22; D-22 |
| `## Stage 7a/7b: Build prompts (dual)` | 7a (MODIFIED dev) + 7b (NEW per-platform implementation — Pipefy Behaviors / Wrike Copilot / Ziflow ReviewAI; NOT universal) | DESIGN-23 |
| `## Stage 8: Test bot — overview` | Skills 8a/8b/8c/8d; persistent harness at `<Client> Brain/test-bot/`; sandbox allowlist extended to Coda (CRIT-5); harness_drift failure class; sandbox_lock; lifecycle states | DESIGN-24 |
| `## Stage 9: Documentation publishing` | Closed doc_type 9-enum; double-underscore naming; doc-diff reviewer-approval gate; doc_published_at invariant; MOD-1 graceful halt | DESIGN-25 |
| `## Stage 10: Native-AI enablement` | native_ai_path branching (api / paste / none); copy-paste default; CRIT-8 + MIN-4 refusal gates; per-doc traceability | DESIGN-26 |
| `## Stage 11: Sign-off, brain update, archive` | Brain-mirror Coda template (7 sections); tone_lint MOD-9; CR archive + 00_Index version bump; pre-archive sanity check; Field Notes preserved input-only | DESIGN-27 |
| `## Test bot architecture` | Tier-1 (Python deterministic — HUMAN-AUTHORED) + tier-2 (AI orchestrator); client_state.yaml skeleton; drift-detection contract (interface only — algorithm deferred to Stage 8 build) | DESIGN-28, 29, 30; D-30 |
| `## Live status-lifecycle survey` | Survey methodology + result; canonical lifecycle reconciliation per D-25 | DESIGN-08; D-25 |
| `## Appendix C: Persona contract worked examples` | 3 before/after voice-rewrite examples drawn from real audit findings | DESIGN-10; D-29 |

Closed `[OPEN]` list shape (one bullet per inline marker, in document order). Expected items (Plan 02-01 seed + Plan 02-04 platform-skill markers + Plan 02-07 D-22 marker = 9 items minimum):

```markdown
- **`[OPEN: Phase 4 — risk-multiplier numeric defaults pending dYdX-historical validation per D-22]`** — source: §Stage 6: Cost estimate — owner: Phase 4 (decide before Stage 6 build per CHANGE-01 ordering)
- **`[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01]`** — source: §platform-pipefy → Native-AI capability matrix — owner: Phase 7 per CHANGE-04
- **`[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]`** — source: §platform-pipefy → API surface — owner: Phase 2 per CHANGE-04
- **`[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified per OPEN-01]`** — source: §platform-pipefy → API surface — owner: Phase 1 / Phase 2 per CHANGE-04
- **`[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01]`** — source: §platform-wrike → Native-AI capability matrix — owner: Phase 7 per CHANGE-04
- **`[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01]`** — source: §platform-wrike → API surface — owner: Phase 1 / Phase 2 per CHANGE-04
- **`[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01]`** — source: §platform-ziflow → Native-AI capability matrix — owner: Phase 7 per CHANGE-04
- **`[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01]`** — source: §platform-ziflow → API surface — owner: Phase 2 per CHANGE-04
```

(Executor will adjust to actual marker text + adjust count if subsequent plans added more markers.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise preamble + populate `## Executive Summary` TOC table + populate `## Appendix A: Glossary`</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-09 complete; structural-check exits 0)
    - .planning/phases/02-design/02-CONTEXT.md §"Specifics" — "Reviewer experience" (read-in-order OR skip-to-contract)
    - .planning/phases/02-design/02-CONTEXT.md §"Cross-cutting authoring decisions" D-32 (citation), D-33 (markers), D-34 (AUDIT.md grounding), D-35 (echo)
    - .planning/phases/01-audit/01-09-SUMMARY.md — Phase 1 synthesis precedent (executive summary + glossary + Appendix B trace)
    - .planning/AUDIT.md §"Executive Summary" + §"Appendix A: Glossary" — Phase 1 reference for table shape + glossary entry style
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate three regions:

    **(A) Finalise preamble.** Replace the existing placeholder line `> (Preamble placeholder — finalised in synthesis plan 02-10)` with a full preamble paragraph (in blockquote form) that explains:
    - Document purpose: locks v2 architecture for v2.1+ rebuild without re-deciding.
    - Reading conventions: success-criteria echo per H2 (D-35); inline `[OPEN: Phase 4 — ...]` markers + closed list at end (D-27/D-33); citation format `file:line` (D-32); `AUDIT.md §X.Y` grounding (D-34).
    - Reviewer flow: read-in-order OR skip-to-contract via executive summary table.
    - Phase boundary: no skill files edited in this milestone (kickoff mandate); v2.1+ build phases per Phase 3 CHANGELIST.md execute against this design.
    - Approval gate: single approval at end (full DESIGN.md reviewed as a whole) per ROADMAP Phase 2 gate.

    Keep the preamble concise (1-2 short paragraphs in blockquote form). Other preamble lines (Design Date, Branch / commit, Phase 1 Audit ground truth) stay above; only the placeholder blockquote line is replaced.

    **(B) Populate `## Executive Summary` table.** Replace the placeholder body (`(Executive summary table placeholder...)`) with a TOC-shaped table per `<interfaces>` shape above. Required columns: `Section | Decision summary | Locking IDs`. Use the rows shown in `<interfaces>` (>= 15 rows; one per major H2 section in document order). Each Section cell uses the literal H2 anchor in backticks (e.g., `` `## Cross-cutting decisions` ``) so reviewers can grep-locate. Each Decision summary captures the contract in one line.

    Add a brief framing paragraph above the table: "This table summarises every major design contract for skip-to-contract navigation. Each row's Section column links to the full contract; Decision summary captures the one-line lock; Locking IDs cite the REQUIREMENTS DESIGN-NN AND CONTEXT D-NN that produced the contract."

    **(C) Populate `## Appendix A: Glossary`.** Replace placeholder body with >= 20 glossary entries. Entry format: `**term** — definition citing the locking section`. Suggested coverage:
    - Frontmatter fields: `frontmatter_version`, `based_on_*`, `status` enum, `client`, `project`, `platform`, `pipe_id`, `space_id`, `project_id`, `delivery`, `kickoff_branch`, `commercial_inputs_status`, `risk_multiplier_version`, `tech_spec_scope`, `delivery_filter`, `doc_type`, `doc_version`, `doc_published_at`, `last_diff_review_at`, `ingested_at`, `target_id`, `native_ai_path`, `tier_claims_last_verified`, `targets_artefact`, `last_passed_at`, `failure_class`, `cr_id`.
    - Status lifecycle terms: `draft`, `client_review`, `approved`, `archived`.
    - Stage-prefix conventions: stage-prefix scheme; substages (4a/4b/7a/7b/8a-8d).
    - Platform terms: Pipe / Phase / Card / Behaviors (Pipefy); Space / Folder / Task (Wrike); Project / Proof / ReviewAI / Checklist (Ziflow).
    - Test-bot terms: tier-1 / tier-2 / harness_drift / sandbox_lock.yaml / schema_drift_report.md / acknowledge / revert / quarantined / obsolete.
    - Plugin surfaces: `commands/`, `agents/`, `hooks/`, `tests/`, `references/`, `mcpServers`.
    - Doc + sign-off terms: doc-diff, brain-mirror, spoke, 00_Index.md, Field Notes, tone_lint.

    Each entry cites locking DESIGN-NN section (e.g., `**delivery** — per-requirement routing key `delivery: native-ai | api` set in Stage 4a fnspec; consumed by Stages 5/6/7b/10. Per DESIGN-20.`).

    **Implementing decisions:** D-19 (executive summary = matrix shape), D-32 (cite section anchors), D-34 (cite AUDIT.md where v0.3.0 lineage applies), D-35 (preamble explains echo convention).
  </action>
  <verify>
    <automated>EXEC_ROWS=$(awk '/^## Executive Summary/,/^---/' .planning/DESIGN.md | grep -cE '^\| \`?##') &amp;&amp; [ "$EXEC_ROWS" -ge "15" ] &amp;&amp; APPENDIX_A=$(awk '/^## Appendix A: Glossary/,/^## Appendix B:/' .planning/DESIGN.md | grep -cE '^\*\*[a-zA-Z]') &amp;&amp; [ "$APPENDIX_A" -ge "20" ] &amp;&amp; PLACEHOLDER=$(grep -cF '(Preamble placeholder' .planning/DESIGN.md) &amp;&amp; [ "$PLACEHOLDER" = "0" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh; CODE=$?; echo "STRUCTURAL_CHECK=$CODE"; [ "$CODE" = "0" ] &amp;&amp; echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Preamble placeholder removed: `grep -cF '(Preamble placeholder' .planning/DESIGN.md` returns `0`
    - Preamble final blockquote present: explains echo convention (D-35), inline + closed `[OPEN]` markers (D-27/D-33), `file:line` citations (D-32), `AUDIT.md §X.Y` grounding (D-34), reviewer skip-to-contract flow, phase boundary, single approval gate
    - Executive Summary table has >= 15 rows referencing major H2 sections (`awk '/^## Executive Summary/,/^---/' .planning/DESIGN.md | grep -cE '^\| \`?##'` >= 15)
    - Executive Summary table cell text references at least: Cross-cutting, Skill layout, 13-skill inventory, Stage-by-stage hand-off contract, Platform skills, Stages 1–11, Test bot architecture
    - Appendix A has >= 20 glossary entries (`awk '/^## Appendix A: Glossary/,/^## Appendix B:/' .planning/DESIGN.md | grep -cE '^\*\*[a-zA-Z]'` >= 20)
    - Appendix A entries cover: at least 10 frontmatter field terms; status lifecycle terms (`draft`, `client_review`, `approved`, `archived`); test-bot terms (`tier-1`, `tier-2`, `harness_drift`, `sandbox_lock`); plugin surfaces (`commands/`, `agents/`, `hooks/`, `tests/`, `references/`)
    - Each Appendix A entry cites the locking DESIGN-NN section (`grep -cE '\*\*[a-zA-Z][a-zA-Z_-]+\*\* — .* DESIGN-' .planning/DESIGN.md` >= 15)
    - **Structural-check still exits 0**
  </acceptance_criteria>
  <done>
    Preamble finalised. Executive Summary populated with >= 15-row TOC table. Appendix A populated with >= 20 glossary entries each citing locking DESIGN-NN. Structural-check exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate `## Appendix B: DESIGN-* → DESIGN.md section traceability` (30 rows) + finalise `## Deferred to Phase 4 OPEN-QUESTIONS` enumerated list + final structural-check pass</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1)
    - .planning/REQUIREMENTS.md §"Design (DESIGN.md)" — DESIGN-01..30 in numeric order
    - .planning/phases/02-design/02-CONTEXT.md §"Open-questions handling" D-27 (closed list at end enumerates every inline marker for traceability into Phase 4 register)
    - .planning/phases/02-design/scripts/design-structure-check.sh — final run at end to confirm exit 0
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate two regions and run final structural-check:

    **(A) `## Appendix B: DESIGN-* → DESIGN.md section traceability`** — replace placeholder body with a 30-row traceability table. Columns: `| DESIGN-NN | Section anchor | Locking decision IDs |`. One row per DESIGN-01..30 in numeric order. Each row maps:
    - Column 1: DESIGN-NN ID
    - Column 2: DESIGN.md section anchor where the contract lives (use `§<section name>` reference matching the H2/H3 anchor; for DESIGN-20 cite both 4a + 4b anchors; for DESIGN-23 cite both 7a + 7b anchors; for DESIGN-08 also cite the survey H3)
    - Column 3: Locking decision IDs from CONTEXT D-NN (comma-separated; "—" if none)

    Example rows:
    ```
    | DESIGN-01 | §Cross-cutting decisions → §DESIGN-01 — Canonical frontmatter scheme | D-25 (survey result) |
    | DESIGN-08 | §Cross-cutting decisions → §DESIGN-08 — Frontmatter migration co-existence; §Live status-lifecycle survey | D-25 |
    | DESIGN-20 | §Stage 4a: Functional spec — platform; §Stage 4b: Functional spec — integration | D-20 (decision-contract-only depth) |
    | DESIGN-22 | §Stage 6: Cost estimate | D-22 (structure-only; numerics deferred) |
    | DESIGN-23 | §Stage 7a: Build prompt — dev; §Stage 7b: Build prompt — implementation per platform | D-20 |
    | DESIGN-29 | §Test bot architecture → §DESIGN-29 client_state.yaml skeleton | D-30 |
    ```

    Add framing paragraph above table: "This appendix maps every REQUIREMENTS DESIGN-* ID to its DESIGN.md home section + locking CONTEXT decision IDs. Reviewer can verify 1:1 coverage against REQUIREMENTS.md §'Design (DESIGN.md)' DESIGN-01..30. Each DESIGN-* has exactly one home section (DESIGN-20 + DESIGN-23 span two substage sections each)."

    **(B) `## Deferred to Phase 4 OPEN-QUESTIONS`** — REPLACE the seed marker placeholder body with a complete enumerated list of EVERY inline `[OPEN: Phase 4 — ...]` marker present in DESIGN.md, in document order.

    Methodology:
    1. Run grep to find every inline marker: `grep -nE '\[OPEN: Phase 4' .planning/DESIGN.md`. Capture line numbers.
    2. For each marker found, walk backward to find the enclosing H2/H3 section (nearest preceding `^### ` or `^## `).
    3. Emit one bulleted list entry per marker. Format:
       `- **`[OPEN: Phase 4 — <subject>]`** — source: §<H2 / H3 section name> — owner: Phase <N> per CHANGE-04 (or "TBD" if no Phase owner cited).`

    Use the expected items list shown in `<interfaces>` above as the baseline (8 items expected from Plans 02-01 seed + 02-04 platform markers + 02-07 D-22 Stage 6 marker). The Plan 02-01 seed marker (the line in the Deferred section) is REPLACED by this enumerated list.

    Add framing paragraph above list: "This list enumerates every inline `[OPEN: Phase 4 — ...]` marker in DESIGN.md, in document order, as the canonical handoff to Phase 4 OPEN-QUESTIONS.md. Each item names the source section + recommended owning phase per CHANGE-04. Phase 4 register builds against this list mechanically — no inline markers should be missing from this enumeration."

    Add closing paragraph: "Verification (run by Phase 4 / final reviewer): inline marker count outside this section should equal list-item count inside this section. Mismatch → an inline marker was added without enumeration here OR an item here lacks corresponding inline marker. Both halt Phase 4 register build."

    **(C) Final structural-check pass.** Run `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 0 (all 9 assertions pass — Plan 02-09 already achieved this; this task adds synthesis content but does not break structure).

    Additionally run inline-marker-vs-list reconciliation:
    `INLINE_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4')`
    `LIST_OPEN=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4')`
    Expected: `INLINE_BODY == LIST_OPEN` (each inline marker has exactly one list entry — and the list entries quote the marker text, so each list entry counts as 1 `[OPEN: Phase 4` occurrence too). Final state: both counts >= 8.

    **Implementing decisions:** D-19 (Appendix B = traceability matrix), D-27 (closed list enumerates every inline marker), D-32 (cite §section anchors), D-35 (synthesis confirms per-DESIGN coverage).
  </action>
  <verify>
    <automated>APPENDIX_B=$(awk '/^## Appendix B:/,/^## /' .planning/DESIGN.md | grep -cE '^\| DESIGN-[0-9]{2} \|') &amp;&amp; [ "$APPENDIX_B" -ge "30" ] &amp;&amp; DEFERRED_LIST=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/DESIGN.md | grep -cE '^- \*\*') &amp;&amp; [ "$DEFERRED_LIST" -ge "1" ] &amp;&amp; INLINE_BODY=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/{exit} {print}' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4') &amp;&amp; LIST_OPEN=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4') &amp;&amp; echo "INLINE_BODY=$INLINE_BODY LIST_OPEN=$LIST_OPEN" &amp;&amp; [ "$INLINE_BODY" = "$LIST_OPEN" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh; CODE=$?; echo "FINAL_STRUCTURAL_CHECK=$CODE"; [ "$CODE" = "0" ] &amp;&amp; echo SYNTHESIS_COMPLETE</automated>
  </verify>
  <acceptance_criteria>
    - Appendix B has exactly >= 30 rows: `awk '/^## Appendix B:/,/^## /' .planning/DESIGN.md | grep -cE '^\| DESIGN-[0-9]{2} \|'` returns >= `30`
    - Appendix B contains every DESIGN-NN ID (DESIGN-01..30) — verifiable: for N in 01..30, `grep -qF "| DESIGN-$N |" .planning/DESIGN.md` succeeds (30 individual checks; can be condensed via loop in the verify script)
    - DESIGN-20 row references both 4a + 4b section anchors
    - DESIGN-23 row references both 7a + 7b section anchors
    - DESIGN-08 row references both cross-cutting + status-lifecycle survey anchors
    - Deferred list has >= 8 enumerated items (one per expected inline marker — Plan 02-01 seed + Plan 02-04 platform markers + Plan 02-07 D-22 marker = at least 8 items, possibly 9+ depending on actual marker emission across plans)
    - Inline-marker-vs-list reconciliation: `INLINE_BODY == LIST_OPEN` (every inline marker has exactly one entry quoting it; list entries quoting markers count toward LIST_OPEN total)
    - Each list entry cites source `§<H2 / H3>` AND owner `Phase <N>` (or "TBD")
    - **Final structural-check exits 0** — all 9 assertions pass; DESIGN.md is structurally complete
    - **Closing-paragraph reconciliation note present** in the Deferred section (instructs Phase 4 / final reviewer how to verify)
  </acceptance_criteria>
  <done>
    Appendix B has 30-row traceability table mapping DESIGN-01..30 to home sections + locking D-IDs. Deferred section has full enumerated list of every inline `[OPEN: Phase 4 — ...]` marker (>= 8 items). Inline-marker-vs-list counts reconcile. Final structural-check exits 0. **DESIGN.md is structurally complete and ready for Phase 2 approval gate review.**
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Closed `[OPEN]` list ↔ inline markers | Per D-27, the closed list MUST enumerate every inline marker. Drift (inline marker without list entry; list entry without inline marker) breaks the Phase 4 register build. |
| Appendix B traceability ↔ REQUIREMENTS DESIGN-01..30 | Each REQUIREMENTS DESIGN-* must appear in Appendix B exactly once. Missing rows mean reviewer cannot 1:1-verify coverage. |
| Executive Summary table ↔ actual H2 sections in DESIGN.md | If Executive Summary references a section that doesn't exist (or fails to reference an existing section), navigation breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | Closed `[OPEN]` list omits an inline marker | mitigate | Inline-marker-vs-list reconciliation check (Task 2 verify): `INLINE_BODY == LIST_OPEN`. Mismatch fails the gate. Methodology in action requires walking every `grep -nE '\[OPEN: Phase 4'` hit. |
| T-02-10-02 | Tampering | Appendix B has fewer than 30 rows or skips a DESIGN-NN | mitigate | Acceptance criteria assert >= 30 rows AND specifically check each DESIGN-NN literal substring present in a `| DESIGN-NN |` cell. Missing IDs fail. |
| T-02-10-03 | Tampering | Executive Summary table cell references wrong H2 anchor (e.g., `## Stage 4 fnspec split` doesn't exist; actual anchor is `## Stage 4a: Functional spec — platform`) | mitigate | Reviewer-attestation manual check; acceptance criteria check for canonical section names being referenced (`Cross-cutting`, `Skill layout`, etc.). Author copies anchor strings from existing DESIGN.md. |
| T-02-10-04 | Tampering | Glossary entry definitions invent terms not used in DESIGN.md | mitigate | Each glossary entry cites the locking DESIGN-NN section (per acceptance criteria — `grep -cE '\*\*[a-zA-Z][a-zA-Z_-]+\*\* — .* DESIGN-' .planning/DESIGN.md` >= 15). If a term has no DESIGN-NN home, it shouldn't be in the glossary. |
| T-02-10-05 | Tampering | Plan 02-10 accidentally REINTRODUCES the placeholder content from earlier plans (e.g., overwrites Stage 6 with new content) | mitigate | This plan ONLY edits 5 specific regions: preamble blockquote, Executive Summary, Appendix A, Appendix B, Deferred. All other H2/H3 bodies stay untouched. Acceptance criteria use awk-scoped queries against the 5 target regions; structural-check assertion #4 (echo count >= 30) catches accidental echo deletion. |
| T-02-10-06 | Tampering | Final structural-check exits non-zero due to synthesis content breaking an existing assertion | mitigate | Each Task ends with structural-check run. Task 1 confirms exit 0 after preamble + Executive Summary + Appendix A. Task 2 confirms exit 0 after Appendix B + Deferred list. Two checkpoints catch any structural break. |
</threat_model>

<verification>
End of Plan 02-10 success-state:
1. Preamble blockquote finalised (placeholder removed)
2. `## Executive Summary` populated with >= 15-row TOC + per-section decision-summary table
3. `## Appendix A: Glossary` populated with >= 20 entries each citing locking DESIGN-NN
4. `## Appendix B: DESIGN-* → DESIGN.md section traceability` populated with 30-row table covering DESIGN-01..30 (including substage-spanning rows for DESIGN-20 + DESIGN-23)
5. `## Deferred to Phase 4 OPEN-QUESTIONS` populated with full enumerated list (>= 8 items); inline-marker-vs-list counts reconcile
6. Final structural-check exits 0 — DESIGN.md structurally complete

Manual reviewer attestation:
- [ ] Executive Summary cell contents match actual contracts in their referenced sections (no drift)
- [ ] Glossary entries cover all unique frontmatter fields introduced across DESIGN-01..30 (frontmatter coverage check)
- [ ] Appendix B traceability is complete (DESIGN-01..30 each have a row; substage-spanning rows handled correctly)
- [ ] Deferred list reconciles 1:1 with inline `[OPEN]` markers — no orphans either way
- [ ] Final structural-check exits 0 — Plan 02-10 hands off a clean state for Phase 2 approval gate
</verification>

<success_criteria>
- [ ] Preamble blockquote finalised
- [ ] Executive Summary >= 15-row TOC table
- [ ] Appendix A >= 20 glossary entries
- [ ] Appendix B 30-row DESIGN-* traceability table (covers DESIGN-01..30)
- [ ] Deferred list enumerates every inline `[OPEN]` marker (>= 8 items); reconciliation passes
- [ ] Final structural-check exits 0
- [ ] No content under `dydx-delivery/` modified
- [ ] No new echo lines added (Plan 02-09 already brought count to 32; synthesis does not add to count)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-10-SUMMARY.md` documenting:
- Preamble finalised
- Executive Summary: N rows (one per major H2)
- Appendix A: N glossary entries (frontmatter / status / stage / platform / test-bot / surfaces vocabulary)
- Appendix B: 30-row DESIGN-* traceability — every REQUIREMENTS DESIGN-NN has a home section + locking D-NN
- Deferred list: N items (matches inline `[OPEN]` count outside the Deferred section); each cites source + owning phase per CHANGE-04
- **Final structural-check exits 0 — DESIGN.md structurally complete and ready for Phase 2 approval gate review per ROADMAP.**
- Pointer: "Phase 2 deliverable complete. Submit `.planning/DESIGN.md` for human approval per ROADMAP Phase 2 approval gate. After approval, Phase 3 (Change list) begins per ROADMAP sequencing — `/gsd-discuss-phase 3` next."
</output>
