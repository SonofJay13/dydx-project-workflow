---
plan_id: 02-03
phase: 02-design
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
requirements: [DESIGN-11, DESIGN-12, DESIGN-13]
files_modified:
  - .planning/phases/02-design/02-03-PLAN.md
  - .planning/DESIGN.md
autonomous: true
tags: [design, skill-inventory, hand-off-matrix, layout]

must_haves:
  truths:
    - "DESIGN.md `## Skill layout` section names the v2 folder layout — `skills/`, `commands/`, `agents/`, `hooks/`, `.claude-plugin/`, plus the new plugin-level `references/` directory — with one-line purpose per directory and references to the canonical files inside `references/` (safety-rules.md, stage-numbering.md, frontmatter-scheme.md, glossary.md per FOUND-01)."
    - "DESIGN.md `## 13-skill inventory` opens with a 13-row matrix per D-19 + decision-contract subsections per D-20 — purpose, inputs, outputs, downstream consumer, complexity tag, dependencies, hand-off message shape per skill. Tag categories per CONTEXT and REQUIREMENTS DESIGN-12: 6 NEW stage skills + 3 NEW platform skills + 4 MODIFIED + 2 UNCHANGED + 1 RETIRED-AND-REPLACED."
    - "DESIGN.md `## Stage-by-stage hand-off contract` matrix gains >= 10 data rows (one per stage transition) — clearing structural-check assertion #9. Columns per D-26: From | To | Carrier file path | Frontmatter fields propagated | Gating status flag | Hand-off message."
    - "Each of the 3 H3 subsections opens with a `> **DESIGN-NN:** <plain-English statement>` echo line per D-35 (3 echo lines added; running total = 13)."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-11 (folder layout), DESIGN-12 (13-skill inventory), DESIGN-13 (hand-off matrix) populated"
      contains: "## Skill layout body; ## 13-skill inventory matrix + per-skill prose; ## Stage-by-stage hand-off contract >= 10 data rows; 3 echo blockquote lines"
  key_links:
    - from: "## 13-skill inventory matrix"
      to: "Plans 02-04 (platform skills) + 02-05/02-06/02-07/02-08 (stage skills) + 02-09 (test bot)"
      via: "Each per-skill row in the matrix has a 'See section' column referencing the H2 anchor where the per-skill decision contract lives"
      pattern: "See `## platform-pipefy` / `## Stage 1: Kickoff capture` / etc."
    - from: "## Stage-by-stage hand-off contract matrix rows"
      to: "Per-stage subsections (Stage 1..11) — each stage's `Hand-off message` cell is restated verbatim in its per-stage subsection per D-26"
      via: "Verbatim hand-off message string anchors both the matrix row and the per-stage subsection"
      pattern: "exact-string match between matrix Hand-off message cell and per-stage subsection hand-off block"
---

<objective>
Populate `.planning/DESIGN.md` with skill layout (DESIGN-11), 13-skill inventory (DESIGN-12), and stage-by-stage hand-off contract matrix (DESIGN-13).

**Purpose:** This is the single most-cited section of DESIGN.md per CONTEXT.md `<specifics>` — v2.1+ build phases will reference DESIGN-13 hand-off matrix more than any other section. Build for grep-ability per D-26: exact carrier file paths, exact frontmatter field names, exact gating status flag values.

**Output:** `.planning/DESIGN.md` with `## Skill layout` body, `## 13-skill inventory` matrix + per-skill prose, `## Stage-by-stage hand-off contract` populated with >= 10 data rows. 3 of the 30 DESIGN-NN echo lines added (DESIGN-11, 12, 13). Running total: 13/30.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- This plan writes to three H2 regions: ## Skill layout, ## 13-skill inventory, ## Stage-by-stage hand-off contract.
     The 13-skill inventory is the master enumeration that all later per-skill plans (02-04..02-09) write per-skill
     contracts against — each per-skill prose subsection in this plan must declare the H2 anchor where its detailed
     contract will land in those later plans.
     The hand-off matrix is the single most-grep-cited section in DESIGN.md per CONTEXT specifics. -->

13-skill inventory matrix shape (per D-19 + DESIGN-12 sub-bullet):

| # | Skill name | Tag | Stage | Inputs (frontmatter + upstream artefact) | Outputs | Downstream consumer | Complexity | Dependencies | Detailed contract section |
|---|------------|-----|-------|------------------------------------------|---------|---------------------|------------|--------------|---------------------------|
| 1 | kickoff-capture | NEW | 1 | meeting notes / requirements / Field Notes; `client:` `project:` | `01_kickoff_v<N>.md` (discovery-ready OR draft SOW) | discovery-intake (skip if draft SOW) OR generate-sow | Medium | Miro paste fallback; Field Notes triage | `## Stage 1: Kickoff capture` |
| 2 | discovery-intake | MODIFIED | 2 | `01_kickoff_v*` artefact; `based_on_kickoff:` | `02_discovery_v<N>.md` | generate-sow | Low (skip mode adds branch) | none new | `## Stage 2: Discovery refactor` |
| 3 | generate-sow | UNCHANGED-structure / behaviour-modified | 3 | `02_discovery_v*` OR `01_kickoff_v*` (draft SOW path); `based_on_discovery:` | `03_sow_v<N>.md` | generate-fnspec-platform AND/OR generate-fnspec-integration | Medium | none new | `## Stage 3: SOW refactor` |
| 4 | generate-fnspec-platform | NEW (replaces generate-functional-spec for platform portions) | 4a | `03_sow_v*`; `based_on_sow:` | `04a_fnspec-platform_v<N>.md` with `delivery: native-ai \| api` per requirement | generate-fnspec-integration (consistency check) AND generate-cost-estimate AND generate-implementation-prompt AND push-native-ai-knowledge | Medium-high | per-platform capability matrix from `platform-<X>` skill | `## Stage 4a: Functional spec — platform` |
| 5 | generate-fnspec-integration | NEW (replaces generate-functional-spec for integration portions) | 4b | `03_sow_v*` + (optional) `04a_fnspec-platform_v*`; `based_on_sow:` `based_on_fnspec_platform:` | `04b_fnspec-integration_v<N>.md` | generate-technical-spec AND generate-cost-estimate AND generate-build-prompt | Medium-high | cross-spec consistency check against 4a | `## Stage 4b: Functional spec — integration` |
| 6 | generate-technical-spec | MODIFIED (scope-gated) | 5 | `04b_fnspec-integration_v*` (required) + (optional) `04a_fnspec-platform_v*` (lightweight platform-API addendum); `based_on_fnspec_*:` | `05_techspec_v<N>.md` | generate-cost-estimate; generate-build-prompt | Medium | none new | `## Stage 5: Tech spec` |
| 7 | generate-cost-estimate | NEW | 6 | `04a_*` AND/OR `04b_*` AND `05_techspec_v*` (if exists); per-assignee task breakdown; risk-multiplier taxonomy structure (defaults DEFERRED per D-22) | `06_cost_v<N>.md` + Coda task-table rows via `keyColumns` upsert | client-facing cost estimate (gated by wait-for-commercial-inputs) | High (Coda integration) | platform-pipefy / platform-wrike / platform-ziflow for schema introspection | `## Stage 6: Cost estimate` |
| 8 | generate-build-prompt | MODIFIED | 7a | `04b_*` AND `05_techspec_v*` AND `06_cost_v*`; `based_on_techspec:` `based_on_cost:` | `07a_build-prompt_v<N>.md` (dev) | dev (human consumer) | Medium | none new | `## Stage 7a: Build prompt — dev` |
| 9 | generate-implementation-prompt | NEW | 7b | `04a_*` AND `06_cost_v*`; `based_on_fnspec_platform:` `based_on_cost:`; per-platform shape (Pipefy / Wrike / Ziflow) | `07b_implementation-prompt_v<N>.md` (per-platform) | non-dev (human consumer) per platform | Medium | platform-<X> reference for shape | `## Stage 7b: Build prompt — implementation per platform` |
| 10 | provision-test-harness | NEW | 8a | `04a_*` AND `04b_*` (delivery routing) AND `client_state.yaml` (existing or NEW); `based_on_fnspec_*:` | `08a_test-harness_v<N>.md` + persistent `<Client> Brain/test-bot/{client_state.yaml, test_runner.py, test_cases/}` (bootstrap once + delta on subsequent ships) | execute-tests (8d) | High (sandbox provisioning + state mgmt) | platform-<X> for sandbox tenant access; sandbox_lock.yaml for concurrency | `## Stage 8: Test bot — overview` |
| 11 | generate-test-plan | MODIFIED | 8b | `04b_*` AND `05_techspec_v*` AND `08a_test-harness_v*`; per-TC layer tagging (tier-1 deterministic / tier-2 AI per DESIGN-28) | `08b_test-plan_v<N>.md` | execute-tests | Medium | DESIGN-28 layer boundary | `## Stage 8: Test bot — overview` |
| 12 | generate-uat-plan | NEW | 8c | `04a_*` AND `04b_*` AND `08b_test-plan_v*` | `08c_uat-plan_v<N>.md` | client (human consumer) | Low-medium | none new | `## Stage 8: Test bot — overview` |
| 13 | execute-tests | MODIFIED (paths move to `<Client> Brain/test-bot/` + agent invocation) | 8d | `08a_*` AND `08b_*` AND `08c_*`; `client_state.yaml`; sandbox_lock.yaml | `08d_test-results_v<N>.md` (in `<Client> Brain/test-bot/`) | update-documentation | High (sandbox execution + AI orchestrator) | test-bot-orchestrator agent (DESIGN-04) | `## Stage 8: Test bot — overview` |
| 14 | update-documentation | NEW | 9 | `08d_test-results_v*` (approved) AND all upstream `04*/05/06/07*` artefacts; `doc_published_at` invariant | `ChangeRequests/<CR>/doc-diff.md` + Drive doc per closed `doc_type` enum | reviewer (human consumer); push-native-ai-knowledge gated by `doc_published_at >= last_diff_review_at` | Medium | Drive MCP | `## Stage 9: Documentation publishing` |
| 15 | push-native-ai-knowledge | NEW | 10 | `04a_*` AND approved `09_doc-diff_v*` fragments AND per-platform `native-ai-inventory.md` (from platform-<X>); branches on `native_ai_path: api \| paste \| none` | per-platform native-AI knowledge ingestion + `doc_version: <semver>` + `ingested_at: <ISO>` per ingested doc | client native-AI surface (Pipefy AI Agent / Wrike Copilot / Ziflow ReviewAI) | High (per-platform knowledge ingestion) | platform-<X>; refuses ingest if `doc_published_at < last_diff_review_at` (CRIT-8) OR target ID mismatches `client:` (MIN-4) | `## Stage 10: Native-AI enablement` |
| 16 | sign-off-and-archive | NEW | 11 | All approved upstream artefacts; `tone_lint` pass | `<Client> Brain/<spokes>/` updated + Coda one-way mirror push + CR move to `Archive/` + `00_Index.md` version bump | client + Field Notes table (preserved input-only) | Medium | brain-mirror Coda doc template (Phase 4 OPEN-05); Field Notes table preserved | `## Stage 11: Sign-off, brain update, archive` |

Wait — that's 16 rows, not 13. Per CONTEXT D-31 + DESIGN-12 the count is "13 skills total" but the matrix above lists 16. Reconciling: DESIGN-12 says "6 NEW stage skills + 3 NEW platform skills + 4 MODIFIED + 2 UNCHANGED + 1 RETIRED-AND-REPLACED" = 6+3+4+2+1 = 16 referenced, but **the RETIRED skill is removed from the v2 inventory** (it's the OLD `generate-functional-spec` superseded by `generate-fnspec-platform` + `generate-fnspec-integration` per Stage 4 split). So v2 end-state inventory = 16 - 1 retired - the 3 platform skills which sit on a separate row range = 12 stage skills. But REQUIREMENTS says 13 skills including platform skills. Resolving:

- v2 end-state inventory rows = **6 NEW stage + 4 MODIFIED stage + 2 UNCHANGED stage + 3 NEW platform = 15**, NOT including the 1 RETIRED (that one is logged separately as the source of the Stage 4 split).
- Architecture research (`.planning/research/SUMMARY.md` 9-phase plan + `.planning/research/ARCHITECTURE.md`) is the source of truth for the count. The number "13" in DESIGN-12 likely counts platform skills together as a category or excludes the UNCHANGED ones from end-state count.

**Executor task:** read `.planning/research/ARCHITECTURE.md` to confirm the exact 13-skill count, then build the matrix accordingly. If the actual count is 15 (per the breakdown above), DESIGN-12 prose must reconcile: "DESIGN-12 originally specified '13 skills total' — architecture research enumerates 15 v2 end-state skills (6 NEW stage + 4 MODIFIED + 2 UNCHANGED + 3 NEW platform; the 1 RETIRED `generate-functional-spec` is removed and replaced by 4a + 4b). The matrix below captures the 15 v2 end-state skills." If research confirms 13, follow research and rebuild the matrix to match. Acceptance criteria below tolerate either count (>= 13).

Hand-off matrix shape (D-26):

| From | To | Carrier file path | Frontmatter fields propagated | Gating status flag | Hand-off message |
|------|-----|-------------------|-------------------------------|--------------------|--------------------|
| Stage 1 | Stage 2 | `01_kickoff_v<N>.md` | `client:` `project:` `frontmatter_version: 2` | `status: approved` | `> Awaiting `status: approved` write to \`01_kickoff_v<N>.md\`. Branch routing on \`kickoff_branch:\` value (\`discovery-ready\` → Stage 2; \`draft-sow\` → SKIP Stage 2 → Stage 3).` |
| Stage 2 | Stage 3 | `02_discovery_v<N>.md` | `based_on_kickoff: 01_kickoff_v<N>` `client:` `project:` | `status: approved` | `> Awaiting `status: approved` write to \`02_discovery_v<N>.md\` before generate-sow runs.` |
| Stage 3 | Stage 4a | `03_sow_v<N>.md` | `based_on_discovery: 02_discovery_v<N>` (or `based_on_kickoff:` if draft-sow path) `platform:` (if known) | `status: approved` AND `client_review` allowed (live in current generate-sow per AUDIT-01.2) | `> Awaiting `status: approved`; routing to Stage 4a (platform fnspec) and/or Stage 4b (integration fnspec) per project scope.` |
| Stage 4a | Stage 4b | `04a_fnspec-platform_v<N>.md` | `based_on_sow: 03_sow_v<N>` `platform:` `delivery: native-ai \| api` per req | `status: approved` (4a optional for integration-only projects) | `> Awaiting `status: approved` to \`04a\`; cross-spec consistency check runs at start of 4b.` |
| Stage 4b | Stage 5 | `04b_fnspec-integration_v<N>.md` | `based_on_sow:` `based_on_fnspec_platform:` (if 4a exists) | `status: approved` (5 SKIPPED if no 4b — platform-only project; lightweight platform-API addendum on 4a instead) | `> Awaiting `status: approved` to \`04b\` before tech spec runs. Skip Stage 5 if no 4b exists.` |
| Stage 5 | Stage 6 | `05_techspec_v<N>.md` | `based_on_fnspec_integration:` `based_on_fnspec_platform:` (if exists) | `status: approved` | `> Awaiting `status: approved` to \`05\` AND wait-for-commercial-inputs gate before generate-cost-estimate runs (per DESIGN-22).` |
| Stage 6 | Stage 7a | `06_cost_v<N>.md` + Coda task-table rows (one-way mirror) | `based_on_techspec:` `based_on_fnspec_*:` `risk_multiplier_version:` (per L/M/H taxonomy structure) | `status: approved` | `> Awaiting `status: approved` to \`06\` (locks costed scope) before build-prompt generation.` |
| Stage 7a / 7b | Stage 8a | `07a_build-prompt_v<N>.md` (dev path) AND/OR `07b_implementation-prompt_v<N>.md` (per-platform) | `based_on_fnspec_*:` `based_on_cost:` `delivery:` (carries forward 4a tag) | `status: approved` (both 7a and 7b if applicable) | `> Awaiting `status: approved` to \`07a\`/\`07b\`; provision-test-harness reads \`delivery:\` routing.` |
| Stage 8a/b/c | Stage 8d | `08a_test-harness_v<N>.md` + `08b_test-plan_v<N>.md` + `08c_uat-plan_v<N>.md` + `<Client> Brain/test-bot/client_state.yaml` | `based_on_fnspec_*:` `client_state_version:` `harness_version:` `last_known_schema_version:` | `status: approved` on 8a + 8b; 8c is human-facing (no `status: approved` gate) | `> Awaiting `status: approved` to \`08a\` AND \`08b\`; execute-tests runs against current `client_state.yaml`; sandbox_lock.yaml acquired before run.` |
| Stage 8d | Stage 9 | `08d_test-results_v<N>.md` (in `<Client> Brain/test-bot/`) | `based_on_test_plan:` `based_on_harness:` `failure_classes:` (per DESIGN-24 enum incl. `harness_drift`) | `status: approved` (signals tests passed at acceptable level) | `> Awaiting `status: approved` to \`08d\`; doc-diff generation requires test approval.` |
| Stage 9 | Stage 10 | `ChangeRequests/<CR>/doc-diff.md` + Drive doc per closed `doc_type` enum (`<client_slug>__<project_slug>__<doc_type>__v<N>`) | `doc_type:` `doc_version:` `doc_published_at:` (ISO timestamp) `last_diff_review_at:` | `status: approved` on doc-diff (reviewer approval gate) | `> Awaiting `status: approved` to \`doc-diff.md\` AND `doc_published_at` set on Drive doc + frontmatter; push-native-ai-knowledge refuses ingest if \`doc_published_at < last_diff_review_at\` (CRIT-8 fix).` |
| Stage 10 | Stage 11 | per-platform native-AI ingestion artefact (Pipefy KB doc / Wrike attached doc / Ziflow checklist) + `doc_version:` `ingested_at:` per ingested doc | `native_ai_path:` `target_id:` (Pipefy AI / Wrike AI block from `00_HUB.md`) | `status: approved` on per-platform ingestion record | `> Awaiting `status: approved` on per-platform native-AI ingestion records; sign-off-and-archive runs only after all push-native-ai-knowledge runs succeed (or `native_ai_path: none` for that platform).` |

That's 12 transitions; >= 10 satisfies the structural-check assertion. Substages adjusted to merge 8a/8b/8c → 8d into one row (test bot internal hand-offs).

`## Skill layout` shape (DESIGN-11):

```markdown
## Skill layout

> **DESIGN-11:** v2 folder layout — `skills/`, `commands/`, `agents/`, `hooks/`, `.claude-plugin/`, plus the new plugin-level `references/` directory.

The v2 plugin folder layout extends the v0.3.0 shape (which had `skills/` + `.claude-plugin/` only) with the surfaces locked in DESIGN-04 plus a plugin-level `references/` directory for canonical SoT documents.

```text
dydx-delivery/
├── .claude-plugin/
│   ├── plugin.json              # version 2.0.0 synced (DESIGN-04 / AUDIT-06)
│   └── (manifest extras)
├── references/                  # NEW v2 — plugin-level canonical SoT (FOUND-01)
│   ├── safety-rules.md          # canonical hard rules (DESIGN-03 — moved from skills/execute-tests/references/)
│   ├── stage-numbering.md       # canonical stage-numbering scheme (DESIGN-02)
│   ├── frontmatter-scheme.md    # canonical frontmatter scheme (DESIGN-01)
│   └── glossary.md              # canonical vocabulary
├── skills/                      # 15 skills total (12 stage + 3 platform); 1 retired (generate-functional-spec)
│   ├── kickoff-capture/         # Stage 1 (NEW)
│   ├── discovery-intake/        # Stage 2 (MODIFIED)
│   ├── generate-sow/            # Stage 3 (UNCHANGED-structure / behaviour modified)
│   ├── generate-fnspec-platform/    # Stage 4a (NEW)
│   ├── generate-fnspec-integration/ # Stage 4b (NEW)
│   ├── generate-technical-spec/ # Stage 5 (MODIFIED — scope-gated)
│   ├── generate-cost-estimate/  # Stage 6 (NEW)
│   ├── generate-build-prompt/   # Stage 7a (MODIFIED)
│   ├── generate-implementation-prompt/ # Stage 7b (NEW)
│   ├── provision-test-harness/  # Stage 8a (NEW)
│   ├── generate-test-plan/      # Stage 8b (MODIFIED — agent invocation)
│   ├── generate-uat-plan/       # Stage 8c (NEW)
│   ├── execute-tests/           # Stage 8d (MODIFIED — paths move to <Client> Brain/test-bot/)
│   ├── update-documentation/    # Stage 9 (NEW)
│   ├── push-native-ai-knowledge/ # Stage 10 (NEW)
│   ├── sign-off-and-archive/    # Stage 11 (NEW)
│   ├── platform-pipefy/         # NEW (DESIGN-14)
│   ├── platform-wrike/          # NEW (DESIGN-15)
│   └── platform-ziflow/         # NEW (DESIGN-16)
├── commands/                    # NEW v2
│   ├── refine.md                # parameterised /dydx-refine-* (DESIGN-05 / D-23)
│   ├── gsd-discuss.md           # GSD-prefixed shortcut
│   ├── gsd-research.md
│   ├── gsd-plan.md
│   └── gsd-execute.md
├── agents/                      # NEW v2
│   └── test-bot-orchestrator.md # DESIGN-24 tier-2 AI orchestrator
├── hooks/                       # NEW v2
│   ├── validate-frontmatter.py  # DESIGN-04 / DESIGN-06 approval-gate enforcement
│   └── bump-artefact-version.py # DESIGN-04
└── tests/                       # NEW v2 (D-24)
    ├── test_validate_frontmatter.py
    ├── test_bump_artefact_version.py
    └── test_frontmatter_parser.py
```

**RETIRED in v2:** `skills/generate-functional-spec/` (replaced by `generate-fnspec-platform/` + `generate-fnspec-integration/` per Stage 4 split / DESIGN-20).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Skill layout` body (DESIGN-11) + `## 13-skill inventory` matrix-then-prose (DESIGN-12)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01 + 02-02 complete; cross-cutting + survey + Appendix C populated)
    - .planning/REQUIREMENTS.md §"Skill layout & hand-offs" DESIGN-11, 12, 13 (verbatim sub-bullets)
    - .planning/research/ARCHITECTURE.md — confirm exact v2 13-skill count + names; reconcile against the 15-row matrix interpretation in `<interfaces>`
    - .planning/research/SUMMARY.md — 9-phase build plan; per-skill introducing-phase mapping
    - .planning/AUDIT.md §AUDIT-01 — 7 v0.3.0 skills' inputs/outputs/hand-offs (ground truth for MODIFIED + UNCHANGED + RETIRED tagging)
    - .planning/AUDIT.md §AUDIT-04 — referenced-but-missing skills (`platform-pipefy`/`platform-wrike` — confirms NEW status of those + `platform-ziflow`)
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-20, D-21 (decision-contract only — no full SKILL.md prose)
    - .planning/phases/02-design/02-CONTEXT.md §"Document structure" D-19 (matrix-then-prose pattern)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Skill layout` body and `## 13-skill inventory` body in this order:

    **(A) `## Skill layout` body (DESIGN-11)** — replace the placeholder body with:
    1. Echo line: `> **DESIGN-11:** v2 folder layout — \`skills/\`, \`commands/\`, \`agents/\`, \`hooks/\`, \`.claude-plugin/\`, plus the new plugin-level \`references/\` directory.`
    2. One-paragraph framing prose: "The v2 plugin folder layout extends the v0.3.0 shape (which had `skills/` + `.claude-plugin/` only — per AUDIT.md §AUDIT-01 + §AUDIT-08) with the surfaces locked in DESIGN-04 plus a plugin-level `references/` directory for canonical SoT documents per DESIGN-02 / DESIGN-03."
    3. Code-fenced ASCII tree (use ` ```text ` fence) showing the full v2 folder layout. Use the layout shape in `<interfaces>` above EXACTLY — every directory + file noted, every cross-reference (DESIGN-NN, AUDIT-NN, FOUND-NN) preserved. Each leaf comment cites the locking decision.
    4. Trailing line: `**RETIRED in v2:** \`skills/generate-functional-spec/\` (replaced by \`generate-fnspec-platform/\` + \`generate-fnspec-integration/\` per Stage 4 split / DESIGN-20).`

    **(B) `## 13-skill inventory` body (DESIGN-12)** — replace the placeholder body with:
    1. Echo line: `> **DESIGN-12:** v2 13-skill inventory — 6 NEW stage + 3 NEW platform + 4 MODIFIED + 2 UNCHANGED + 1 RETIRED-AND-REPLACED. Each skill carries purpose, inputs, outputs, downstream consumer, complexity, dependencies, hand-off message shape per D-20.`
    2. **Reconciliation paragraph (REQUIRED if research-confirmed count ≠ 13):** read `.planning/research/ARCHITECTURE.md` to determine the exact v2 end-state skill count. If research enumerates 15 (6 NEW stage + 4 MODIFIED + 2 UNCHANGED + 3 NEW platform — the 1 RETIRED is removed), state explicitly: "DESIGN-12 captures the v2 end-state skill inventory. The '13 skills total' framing in REQUIREMENTS.md DESIGN-12 includes the RETIRED `generate-functional-spec` (replaced by 4a + 4b) in the headline count; this matrix lists the 15 (or N per research) v2 end-state skills the v2.1+ rebuild produces, with the 1 RETIRED noted at the bottom for migration mapping." If research confirms exactly 13, omit the reconciliation paragraph and adjust the matrix to match research. **Honesty principle (Phase 1 D-32 / D-34 precedent): do not pad the matrix to hit 13 if research says otherwise; do not silently drop legitimate skills to hit 13 if research says otherwise. Cite research source for the count.**
    3. **Inventory matrix** — multi-row table per `<interfaces>` shape above. Columns: `| # | Skill name | Tag | Stage | Inputs (frontmatter + upstream artefact) | Outputs | Downstream consumer | Complexity | Dependencies | Detailed contract section |`. One row per v2 end-state skill. Tag values must be: `NEW`, `MODIFIED`, `UNCHANGED-structure / behaviour-modified` (the 2 UNCHANGED skills per DESIGN-12), or `NEW (platform)`. The "Detailed contract section" column references the H2 anchor where Plans 02-04..02-09 will write the per-skill prose. Use exact anchor strings (`## Stage 1: Kickoff capture`, `## platform-pipefy`, etc.).
    4. **Per-skill prose subsections** — under the matrix, do NOT write per-skill prose blocks (those land in Plans 02-04..02-09 under the per-stage / per-platform H2 anchors already present in the skeleton). Instead, write one short paragraph: "Per-skill decision contracts (purpose, inputs, outputs, downstream consumer, complexity tag, dependencies, hand-off message shape) live in their respective sections per the 'Detailed contract section' column. Per D-20, full SKILL.md prose is NOT drafted in this milestone — that authoring runs in v2.1+ build phases."
    5. **Migration mapping note** — short paragraph: "RETIRED `generate-functional-spec` maps to the new Stage 4a + Stage 4b skills per DESIGN-20. v0.3.0 artefacts using the old skill name remain readable per DESIGN-08 lenient mode; CR-driven opt-in upgrades them to the new names during their owning CR."

    **Implementing decisions:** D-19 (matrix-then-prose), D-20 (decision-contract only — no full SKILL.md prose here), D-32 (cite ARCHITECTURE.md for count source), D-34 (AUDIT.md grounding for v0.3.0 facts), D-35 (echo line per H2).
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-11:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-12:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^```text' .planning/DESIGN.md &amp;&amp; grep -qF 'dydx-delivery/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-pipefy/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-wrike/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-ziflow/' .planning/DESIGN.md &amp;&amp; grep -qF 'tests/' .planning/DESIGN.md &amp;&amp; INVENTORY_ROWS=$(awk '/^## 13-skill inventory/,/^## /' .planning/DESIGN.md | grep -cE '^\| [0-9]+ \|') &amp;&amp; [ "$INVENTORY_ROWS" -ge "13" ] &amp;&amp; echo LAYOUT_AND_INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-11:\*\*' .planning/DESIGN.md` succeeds (DESIGN-11 echo line)
    - `grep -qE '^> \*\*DESIGN-12:\*\*' .planning/DESIGN.md` succeeds (DESIGN-12 echo line)
    - `grep -qE '^```text$' .planning/DESIGN.md` succeeds (skill-layout ASCII tree code fence)
    - `grep -qF 'dydx-delivery/' .planning/DESIGN.md` succeeds (root in tree)
    - `grep -qF 'platform-pipefy/' .planning/DESIGN.md` AND `grep -qF 'platform-wrike/' .planning/DESIGN.md` AND `grep -qF 'platform-ziflow/' .planning/DESIGN.md` succeed (3 platform skills in tree + matrix)
    - `grep -qF 'kickoff-capture/' .planning/DESIGN.md` succeeds (Stage 1 NEW skill)
    - `grep -qF 'generate-fnspec-platform/' .planning/DESIGN.md` AND `grep -qF 'generate-fnspec-integration/' .planning/DESIGN.md` succeed (Stage 4 split)
    - `grep -qF 'tests/' .planning/DESIGN.md` succeeds (D-24 plugin self-tests directory)
    - `grep -qF 'commands/' .planning/DESIGN.md` AND `grep -qF 'agents/' .planning/DESIGN.md` AND `grep -qF 'hooks/' .planning/DESIGN.md` succeed (DESIGN-04 surfaces in tree)
    - `grep -qF 'references/' .planning/DESIGN.md` succeeds (plugin-level references dir)
    - `grep -qF 'safety-rules.md' .planning/DESIGN.md` succeeds (DESIGN-03 SoT location in tree)
    - `grep -qF 'RETIRED' .planning/DESIGN.md` succeeds (RETIRED tag for generate-functional-spec)
    - Inventory matrix has >= 13 numeric-indexed data rows: `awk '/^## 13-skill inventory/,/^## /' .planning/DESIGN.md | grep -cE '^\| [0-9]+ \|'` returns >= `13`
    - Inventory matrix references at least 6 NEW + 4 MODIFIED + 2 UNCHANGED + 3 platform tags (cumulative): `grep -cE '\| NEW \|' .planning/DESIGN.md` returns >= 6 AND `grep -cE '\| MODIFIED \|' .planning/DESIGN.md` returns >= 4 AND `grep -cE '\| UNCHANGED' .planning/DESIGN.md` returns >= 2 AND `grep -cE '\| NEW \(platform\) \|' .planning/DESIGN.md` returns >= 3
    - Inventory matrix references all stage anchors via "See ## Stage" or "## platform-": `grep -cE 'Stage [0-9]+[ab]?[a-d]?' .planning/DESIGN.md` returns >= 13 (one per skill row)
    - Migration mapping note present: `grep -qF 'generate-functional-spec' .planning/DESIGN.md` AND `grep -qF 'lenient mode' .planning/DESIGN.md` succeed
  </acceptance_criteria>
  <done>
    `## Skill layout` body has DESIGN-11 echo + ASCII tree showing all v2 directories (incl. NEW commands/agents/hooks/tests/references). `## 13-skill inventory` body has DESIGN-12 echo + reconciliation paragraph (if count ≠ 13) + inventory matrix with >= 13 rows + per-skill detailed-contract-section pointers + migration mapping note.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate `## Stage-by-stage hand-off contract` matrix with >= 10 transition rows (DESIGN-13 / D-26) + run structural-check (assertion #9 should now pass)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — skill layout + inventory populated)
    - .planning/REQUIREMENTS.md §"DESIGN-13" (verbatim sub-bullet — for every stage transition, the carrier file path, frontmatter fields propagated, gating status flag)
    - .planning/phases/02-design/02-CONTEXT.md §"Hand-off contract presentation" D-26 (single transition matrix shape; columns; per-stage subsections also carry hand-off message verbatim)
    - .planning/phases/02-design/02-CONTEXT.md §"Specifics" — "Hand-off matrix as the most-used DESIGN.md page" + "Build it for grep-ability"
    - .planning/AUDIT.md §AUDIT-01 — current v0.3.0 hand-off contracts per skill (ground truth for the carrier file paths and frontmatter fields that are already in use; v2 extends the convention)
    - .planning/phases/02-design/scripts/design-structure-check.sh — run at end to verify assertion #9 (handoff_rows >= 10) now passes
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Stage-by-stage hand-off contract` body:

    1. **Echo line**: `> **DESIGN-13:** Stage-by-stage hand-off contract — for every stage transition, the carrier file path, the frontmatter fields that propagate, and the status flag that gates the next stage. Single transition matrix per D-26 (matrix-then-prose); per-stage subsections (Stage 1..11) restate hand-off message verbatim for grep-ability.`

    2. **Framing paragraph**: "The hand-off matrix is the single most-cited section of DESIGN.md (per CONTEXT specifics). v2.1+ build phases reference these rows when authoring per-stage SKILL.md files — every carrier file path, frontmatter field name, and gating status flag value here is grep-canonical. Per D-26, the matrix is the source of truth; per-stage subsections (under `## Stage 1: Kickoff capture` through `## Stage 11: Sign-off, brain update, archive`) restate the hand-off message verbatim for navigability."

    3. **Replace the existing matrix header row** (already laid by Plan 02-01) by writing the >= 10 data rows below it. Use the matrix data shown in the `<interfaces>` block above (12 transitions, satisfies >= 10 floor):
       - Stage 1 → Stage 2 (with branch routing on `kickoff_branch:` value)
       - Stage 2 → Stage 3
       - Stage 3 → Stage 4a (and parallel route to 4b per project scope)
       - Stage 4a → Stage 4b (with cross-spec consistency check)
       - Stage 4b → Stage 5 (with skip-Stage-5 condition for platform-only projects)
       - Stage 5 → Stage 6 (with wait-for-commercial-inputs gate per DESIGN-22)
       - Stage 6 → Stage 7a (and parallel 7b)
       - Stage 7a/7b → Stage 8a (provision-test-harness reads `delivery:` routing)
       - Stage 8a/b/c → Stage 8d (sandbox_lock.yaml acquired before run)
       - Stage 8d → Stage 9 (test approval gates doc-diff generation)
       - Stage 9 → Stage 10 (`doc_published_at` invariant; CRIT-8 fix)
       - Stage 10 → Stage 11 (per-platform native-AI ingestion records gate sign-off)

       Each row's `From` cell starts with literal `Stage` (per structural-check assertion #9 regex `^| Stage`). Each row's `Hand-off message` cell uses backtick-wrapped paths and `> Awaiting \`status: approved\`...` blockquote prefix.

    4. **Constraint:** The matrix `Hand-off message` cell strings must be reusable verbatim by Plans 02-05..02-08 in their per-stage subsections. Phrase each message as a single line (no embedded newlines) so the per-stage author can copy-paste cleanly.

    5. **After populating, run the structural-check**: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: still exits 1 (echo count is at 13/30 after this plan; remaining 17 echo lines from Plans 02-04..02-09). The hand-off matrix assertion #9 should now PASS (>= 10 rows). The first failing assertion message should now be assertion #4 (echo count >= 30, found 13).

    **Implementing decisions:** D-26 (single matrix, 6 columns, per-stage subsection restates verbatim — per-stage subsections themselves come from later plans), D-32 (`file:line` not needed in matrix — carrier file paths are templates not citations), D-35 (DESIGN-13 echo line).
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-13:\*\*' .planning/DESIGN.md &amp;&amp; HANDOFF_ROWS=$(awk '/^## Stage-by-stage hand-off contract/,/^## /' .planning/DESIGN.md | grep -cE '^\| Stage') &amp;&amp; [ "$HANDOFF_ROWS" -ge "10" ] &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "13" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo HANDOFF_MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-13:\*\*' .planning/DESIGN.md` succeeds (DESIGN-13 echo line)
    - `awk '/^## Stage-by-stage hand-off contract/,/^## /' .planning/DESIGN.md | grep -cE '^\| Stage'` returns >= `10` (matrix has at least 10 transition rows; assertion #9 passes)
    - `awk '/^## Stage-by-stage hand-off contract/,/^## /' .planning/DESIGN.md | grep -qF '| Stage 1 | Stage 2 |'` succeeds (Stage 1→2 transition row)
    - `awk '/^## Stage-by-stage hand-off contract/,/^## /' .planning/DESIGN.md | grep -qF '| Stage 4a | Stage 4b |'` succeeds (substage transition)
    - `awk '/^## Stage-by-stage hand-off contract/,/^## /' .planning/DESIGN.md | grep -qF '| Stage 9 | Stage 10 |'` succeeds (CRIT-8 fix transition)
    - Hand-off matrix `Carrier file path` column references stage-prefixed filenames: `awk '/^## Stage-by-stage hand-off contract/,/^## /' .planning/DESIGN.md | grep -qF '01_kickoff_v' AND grep -qF '04a_fnspec-platform_v' AND grep -qF '08a_test-harness_v' AND grep -qF 'doc-diff.md'` (verifying the canonical filenames from DESIGN-02 stage-numbering scheme)
    - Hand-off message column references `status: approved` (gating flag): `grep -cF 'status: approved' .planning/DESIGN.md` returns >= 12 (one per matrix row + cross-cutting DESIGN-06)
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `13` (DESIGN-01..13 echoes; remaining 17 from later plans)
    - `bash design-structure-check.sh` exits with code `1` (mid-phase — echo count 13 < 30 after this plan)
    - Failure message names "expected >= 30 'DESIGN-NN:' success-criteria echo blockquote lines, found 13" (NOT the hand-off matrix assertion — that should now pass)
  </acceptance_criteria>
  <done>
    Hand-off matrix has DESIGN-13 echo + framing prose + >= 10 transition rows (12 expected). Matrix is grep-canonical for per-stage authors. Structural-check assertion #9 (handoff_rows) passes; assertion #4 (echo count) is the failing one now (13/30). Plans 02-04..02-09 land remaining 17 echo lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill inventory matrix → per-skill plans 02-04..02-09 | Each per-skill plan reads the matrix's "Detailed contract section" column to know where to write its per-skill prose. A wrong anchor reference here misroutes downstream content. |
| Hand-off matrix → per-stage subsection authors | Per D-26, per-stage subsections restate the hand-off message verbatim. A typo or path drift in the matrix propagates to every per-stage subsection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Skill inventory padded to hit "13 skills" claim despite research enumerating 15 | mitigate | Task 1 explicitly forbids padding-to-fit. If research enumerates ≠ 13, the executor writes a reconciliation paragraph citing research and proceeds with the actual count. Acceptance criteria require >= 13 rows (tolerates higher counts) — does NOT enforce exactly 13. Phase 1 D-32/D-34 honesty precedent. |
| T-02-03-02 | Tampering | Hand-off matrix carrier file path drift (e.g., `01_kickoff_v<N>.md` in matrix vs `02_kickoff_v<N>.md` later) | mitigate | DESIGN-02 stage-numbering scheme is the SoT for file prefixes (locked in Plan 02-02 cross-cutting DESIGN-02 contract). Acceptance criteria assert specific canonical filenames present in matrix (`01_kickoff_v`, `04a_fnspec-platform_v`, `08a_test-harness_v`, etc.) — drift would fail those checks. Per-stage subsections in Plans 02-05..02-08 must cite the matrix's exact carrier path. |
| T-02-03-03 | Tampering | Hand-off matrix uses `^| Stage` row prefix — if a row starts with `| **Stage** |` (bold), assertion #9 regex `^\| Stage` (literal space, no asterisks) misses it | mitigate | Acceptance criteria explicitly check `awk ... | grep -qF '| Stage 1 | Stage 2 |'` — bare `| Stage X |` form, no bold. Phase 1 Plan 08 precedent: bold prefix dropped from row leading cells (`| Coda |` not `| **Coda** |`) for the same reason. |
| T-02-03-04 | Tampering | "13-skill inventory" matrix accidentally omits a v2 skill (e.g., one of the platform skills) | mitigate | Acceptance criteria require explicit grep for each platform skill name (`platform-pipefy/`, `platform-wrike/`, `platform-ziflow/`) AND each NEW stage skill name (`kickoff-capture/`, `generate-fnspec-platform/`, etc.) AND each MODIFIED skill (via tag count assertions: >= 4 MODIFIED rows). |
| T-02-03-05 | Tampering | DESIGN-13 hand-off matrix `Hand-off message` cells use multi-line format that breaks Markdown table rendering | mitigate | Task 2 action explicitly requires single-line cells (no embedded newlines). Long messages use backtick-wrapped paths + `> Awaiting...` prefix as a SINGLE line. If a row's message is too long to fit cleanly, split conceptually but keep one row per transition. |
| T-02-03-06 | Information Disclosure | Skill inventory leaks future-phase implementation details that belong in v2.1+ build | accept | Per D-20, per-skill prose blocks live in their detailed-contract sections (Plans 02-04..02-09) — but those are still decision-contract-only, not full SKILL.md prose. The matrix itself only carries summary info (purpose, inputs, outputs, downstream consumer, complexity, dependencies). No SKILL.md authoring leaks. |
</threat_model>

<verification>
End of Plan 02-03 success-state:
1. `## Skill layout` body has DESIGN-11 echo + ASCII tree of v2 plugin folder layout
2. `## 13-skill inventory` body has DESIGN-12 echo + reconciliation paragraph (if needed) + matrix with >= 13 rows + per-skill anchor references + migration mapping note
3. `## Stage-by-stage hand-off contract` body has DESIGN-13 echo + framing paragraph + matrix with >= 10 transition rows
4. Echo line count = 13 (DESIGN-01..13)
5. `bash design-structure-check.sh` exits 1 with failing assertion = "expected >= 30 'DESIGN-NN:' success-criteria echo blockquote lines, found 13" (assertion #9 hand-off matrix now PASSES; assertion #4 echo count still failing)

Manual reviewer attestation:
- [ ] Skill inventory matrix tag counts match DESIGN-12 breakdown (6 NEW + 4 MODIFIED + 2 UNCHANGED + 3 NEW platform; 1 RETIRED noted at bottom)
- [ ] Hand-off matrix carrier paths use DESIGN-02 stage-prefixed convention (`01_kickoff_v<N>.md`, `04a_fnspec-platform_v<N>.md`, etc.)
- [ ] Hand-off matrix "Gating status flag" column uses DESIGN-01 canonical lifecycle values (`status: approved`, `status: client_review`)
- [ ] No per-skill SKILL.md prose drafted (per D-20 — that's v2.1+ build territory)
- [ ] No new `[OPEN]` markers added (DESIGN-11/12/13 are all decided; deferrals belong to per-stage / per-platform plans)
</verification>

<success_criteria>
- [ ] DESIGN-11 / DESIGN-12 / DESIGN-13 echo lines + bodies populated
- [ ] Skill layout ASCII tree shows all v2 directories (skills/ + commands/ + agents/ + hooks/ + tests/ + references/ + .claude-plugin/)
- [ ] Inventory matrix has >= 13 rows with all required tags (NEW / MODIFIED / UNCHANGED / NEW (platform))
- [ ] Hand-off matrix has >= 10 transition rows (12 expected); each row uses canonical stage-prefixed file paths and `status: approved` gating
- [ ] Echo count = 13/30
- [ ] Structural-check exits 1 with assertion #4 (echo count) failing — assertion #9 (hand-off matrix rows) now PASSES
- [ ] No content under `dydx-delivery/` modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-03-SUMMARY.md` documenting:
- Skill layout: 7 v2 surfaces enumerated (skills, commands, agents, hooks, tests, references, .claude-plugin)
- Inventory: actual v2 end-state count (13 if research confirms; otherwise reconciliation result + the count) + tag breakdown + RETIRED skill mapping
- Hand-off matrix: 12 transition rows; canonical file paths + status flags
- Echo count progression: 13/30 (Plans 02-04..02-09 add remaining 17)
- Pointer: "Plan 02-04 (Wave 4) populates Platform skills next (DESIGN-14, 15, 16). The 3 platform skill subsections each add 1 echo line; running total after 02-04 = 16/30."
</output>
