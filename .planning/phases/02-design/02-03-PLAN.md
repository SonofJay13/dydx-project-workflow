---
plan_id: 02-03
phase: 02-design
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
requirements: [DESIGN-11, DESIGN-12, DESIGN-13]
files_modified:
  - .planning/phases/02-design/02-03-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/scripts/design-structure-check.sh
  - .planning/phases/02-design/02-03-SUMMARY.md
autonomous: true
tags: [design, skill-inventory, hand-off-matrix, layout]

must_haves:
  truths:
    - "DESIGN.md `## Skill layout` section names the v2 folder layout — `skills/`, `commands/`, `agents/`, `hooks/`, `.claude-plugin/`, plus the new plugin-level `references/` directory — with one-line purpose per directory and references to the canonical files inside `references/` (safety-rules.md, stage-numbering.md, frontmatter-scheme.md, glossary.md per FOUND-01)."
    - "DESIGN.md `## v2 skill inventory` (renamed from `## 13-skill inventory` per cross-AI review MEDIUM #4 — the H2 anchor changes; see resolution rule below) opens with an N-row matrix per D-19 + decision-contract subsections per D-20. The actual end-state row count N is determined by the resolution rule and the architecture research enumeration."
    - "DESIGN.md `## Stage-by-stage hand-off contract` matrix gains >= 10 data rows (one per stage transition) — clearing structural-check assertion #9. Columns per D-26: From | To | Carrier file path | Frontmatter fields propagated | Gating status flag | Hand-off message."
    - "Each of the 3 H3 subsections opens with a `> **DESIGN-NN:** <plain-English statement>` echo line per D-35 (3 echo lines added; running total = 13)."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-11 (folder layout), DESIGN-12 (skill inventory), DESIGN-13 (hand-off matrix) populated"
      contains: "## Skill layout body; ## v2 skill inventory matrix + per-skill prose; ## Stage-by-stage hand-off contract >= 10 data rows; 3 echo blockquote lines"
  key_links:
    - from: "## v2 skill inventory matrix"
      to: "Plans 02-04 (platform skills) + 02-05/02-06/02-07/02-08 (stage skills) + 02-09 (test bot)"
      via: "Each per-skill row in the matrix has a 'See section' column referencing the H2 anchor where the per-skill decision contract lives"
      pattern: "See `## platform-pipefy` / `## Stage 1: Kickoff capture` / etc."
    - from: "## Stage-by-stage hand-off contract matrix rows"
      to: "Per-stage subsections (Stage 1..11) — each stage's `Hand-off message` cell is restated verbatim in its per-stage subsection per D-26"
      via: "Verbatim hand-off message string anchors both the matrix row and the per-stage subsection"
      pattern: "exact-string match between matrix Hand-off message cell and per-stage subsection hand-off block"
---

<objective>
Populate `.planning/DESIGN.md` with skill layout (DESIGN-11), v2 skill inventory (DESIGN-12), and stage-by-stage hand-off contract matrix (DESIGN-13).

**Purpose:** This is the single most-cited section of DESIGN.md per CONTEXT.md `<specifics>` — v2.1+ build phases will reference DESIGN-13 hand-off matrix more than any other section. Build for grep-ability per D-26: exact carrier file paths, exact frontmatter field names, exact gating status flag values.

**Output:** `.planning/DESIGN.md` with `## Skill layout` body, `## v2 skill inventory` matrix + per-skill prose, `## Stage-by-stage hand-off contract` populated with >= 10 data rows. 3 of the 30 DESIGN-NN echo lines added (DESIGN-11, 12, 13). Running total: 13/30.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Skill-count framing resolution (per cross-AI review MEDIUM #4 — Codex+Gemini consensus):

     REQUIREMENTS.md DESIGN-12 says: "13 skills total (6 NEW stage skills + 3 NEW platform skills + 4 MODIFIED + 2 UNCHANGED + 1 RETIRED-AND-REPLACED)".
     Architecture research (.planning/research/SUMMARY.md / ARCHITECTURE.md) enumerates 15 v2 end-state skills.

     RESOLUTION RULE (decided pre-execution per cross-AI review MEDIUM #4):
     1. The H2 anchor uses neutral "v2 skill inventory" (NOT "13-skill" / "15-skill" / "16-skill" — no number in the H2).
        This makes the section title resilient to count changes. Skeleton anchor was `## 13-skill inventory` — Plan 02-03
        Task 1 RENAMES this anchor to `## v2 skill inventory`. The structural-check script does NOT assert any specific
        anchor in the `13-skill` form; verify by re-running the script after rename.
     2. The matrix N-row count equals the v2 end-state skill count = number of skill folders that ship in v2 (excludes
        the 1 RETIRED `generate-functional-spec`). Per architecture research SUMMARY.md, this is 15:
        6 NEW stage + 4 MODIFIED + 2 UNCHANGED + 3 NEW platform = 15.
        REQUIREMENTS.md "13 skills total" framing INCLUDES the RETIRED skill in the headline count
        (6+3+4+2+1 = 16... wait, that's 16). Reconciliation: REQUIREMENTS DESIGN-12 sub-bullet adds to 16 categorical
        items (5 categories totalling 6+3+4+2+1=16); the 1 RETIRED is logged separately for migration mapping; the v2
        end-state inventory matrix lists 15 skills (16 - 1 RETIRED). The "13 skills total" headline figure in DESIGN-12
        is legacy framing carried forward from earlier discussion; the matrix shows the actual end-state count.
     3. The reconciliation paragraph (Task 1) MUST explicitly state the resolution: cite both REQUIREMENTS DESIGN-12
        (legacy "13 skills total" framing carrying the RETIRED skill in the headline + 5-category breakdown) AND the
        architecture research (15 v2 end-state skill folders that ship). Then say: "this matrix lists 15 v2 end-state
        skills + a separate migration-mapping note for the 1 RETIRED skill". Do NOT pad the matrix to hit 13; do NOT
        silently drop legitimate skills to hit 13. Phase 1 D-32 / D-34 honesty precedent.
     4. Acceptance criteria require >= 13 rows (tolerates whatever the research-confirmed count is, with floor of 13
        to catch obvious under-population).

     The skeleton from Plan 02-01 still has the `## 13-skill inventory` H2 anchor. This plan must:
     - Rename `## 13-skill inventory` → `## v2 skill inventory` (sed-style replace)
     - Verify the structural-check script (which checks for `## 13-skill inventory` literal) is updated OR not
       affected. Plan 02-01's required_h2 array DOES list `## 13-skill inventory`. After this rename, the script
       would FAIL on the missing anchor. Therefore Task 1 ALSO updates the script's required_h2 array entry from
       `## 13-skill inventory` to `## v2 skill inventory`. This is the single script-edit this plan performs.
-->

v2 skill inventory matrix shape (per D-19 + DESIGN-12 sub-bullet) — N rows where N = actual v2 end-state skill count per resolution rule above (expected 15):

| # | Skill name | Tag | Stage | Inputs (frontmatter + upstream artefact) | Outputs | Downstream consumer | Complexity | Dependencies | Detailed contract section |
|---|------------|-----|-------|------------------------------------------|---------|---------------------|------------|--------------|---------------------------|
| 1 | kickoff-capture | NEW | 1 | meeting notes / requirements / Field Notes; `client:` `project:` | `01_kickoff_v<N>.md` | discovery-intake OR generate-sow | Medium | Miro paste fallback; Field Notes triage | `## Stage 1: Kickoff capture` |
| 2 | discovery-intake | MODIFIED | 2 | `01_kickoff_v*` artefact; `based_on_kickoff:` | `02_discovery_v<N>.md` | generate-sow | Low | none new | `## Stage 2: Discovery refactor` |
| 3 | generate-sow | UNCHANGED-structure / behaviour-modified | 3 | `02_discovery_v*` OR `01_kickoff_v*`; `based_on_discovery:` | `03_sow_v<N>.md` | generate-fnspec-platform AND/OR generate-fnspec-integration | Medium | none new | `## Stage 3: SOW refactor` |
| 4 | generate-fnspec-platform | NEW | 4a | `03_sow_v*`; `based_on_sow:` | `04a_fnspec-platform_v<N>.md` with `delivery: native-ai \| api` per requirement | generate-fnspec-integration AND generate-cost-estimate AND generate-implementation-prompt AND push-native-ai-knowledge | Medium-high | per-platform capability matrix | `## Stage 4a: Functional spec — platform` |
| 5 | generate-fnspec-integration | NEW | 4b | `03_sow_v*` + (optional) `04a_fnspec-platform_v*` | `04b_fnspec-integration_v<N>.md` | generate-technical-spec AND generate-cost-estimate AND generate-build-prompt | Medium-high | cross-spec consistency check vs 4a | `## Stage 4b: Functional spec — integration` |
| 6 | generate-technical-spec | MODIFIED | 5 | `04b_fnspec-integration_v*` + (optional) `04a_*` | `05_techspec_v<N>.md` | generate-cost-estimate; generate-build-prompt | Medium | none new | `## Stage 5: Tech spec` |
| 7 | generate-cost-estimate | NEW | 6 | `04a_*` AND/OR `04b_*` AND `05_techspec_v*` | `06_cost_v<N>.md` + Coda task-table rows | client-facing cost estimate | High (Coda) | platform-<X> for schema introspection | `## Stage 6: Cost estimate` |
| 8 | generate-build-prompt | MODIFIED | 7a | `04b_*` AND `05_techspec_v*` AND `06_cost_v*` | `07a_build-prompt_v<N>.md` | dev | Medium | none new | `## Stage 7a: Build prompt — dev` |
| 9 | generate-implementation-prompt | NEW | 7b | `04a_*` AND `06_cost_v*`; per-platform shape | `07b_implementation-prompt_v<N>.md` | non-dev per platform | Medium | platform-<X> reference | `## Stage 7b: Build prompt — implementation per platform` |
| 10 | provision-test-harness | NEW | 8a | `04a_*` AND `04b_*` AND `client_state.yaml` | `08a_test-harness_v<N>.md` + persistent harness | execute-tests | High | platform-<X> + sandbox_lock | `## Stage 8: Test bot — overview` |
| 11 | generate-test-plan | MODIFIED | 8b | `04b_*` AND `05_techspec_v*` AND `08a_*` | `08b_test-plan_v<N>.md` | execute-tests | Medium | DESIGN-28 layer boundary | `## Stage 8: Test bot — overview` |
| 12 | generate-uat-plan | NEW | 8c | `04a_*` AND `04b_*` AND `08b_*` | `08c_uat-plan_v<N>.md` | client | Low-medium | none new | `## Stage 8: Test bot — overview` |
| 13 | execute-tests | MODIFIED | 8d | `08a_*` AND `08b_*` AND `08c_*` | `08d_test-results_v<N>.md` | update-documentation | High (sandbox + AI orchestrator) | test-bot-orchestrator agent | `## Stage 8: Test bot — overview` |
| 14 | update-documentation | NEW | 9 | `08d_test-results_v*` (approved) AND all upstream | `ChangeRequests/<CR>/doc-diff.md` + Drive doc | reviewer; push-native-ai-knowledge | Medium | Drive MCP | `## Stage 9: Documentation publishing` |
| 15 | push-native-ai-knowledge | NEW | 10 | `04a_*` AND approved `09_doc-diff_v*` AND per-platform `native-ai-inventory.md` | per-platform native-AI ingestion + `doc_version:` `ingested_at:` | client native-AI surface | High | platform-<X>; CRIT-8 + MIN-4 refusal gates | `## Stage 10: Native-AI enablement` |
| 16 | sign-off-and-archive | NEW | 11 | All approved upstream artefacts; `tone_lint` pass | `<Client> Brain/<spokes>/` updated + Coda mirror push + CR archived | Field Notes preserved; next CR's Stage 1 | Medium | brain-mirror Coda template | `## Stage 11: Sign-off, brain update, archive` |

NOTE: The matrix as enumerated has 16 rows above (rows 1-16). Per the resolution rule, the v2 end-state count is 16 - the platform skills sit on rows 17-19 (added below) — actually wait, the platform skills are SEPARATE from the stage skills. Let me re-enumerate:

Rows 1-13 above are STAGE skills (kickoff through execute-tests). Rows 14-16 are MORE stage skills (update-documentation, push-native-ai-knowledge, sign-off-and-archive). That's 16 stage skills = 6 NEW + 4 MODIFIED + 2 UNCHANGED... let me count: NEW = kickoff-capture, generate-fnspec-platform, generate-fnspec-integration, generate-cost-estimate, generate-implementation-prompt, provision-test-harness, generate-uat-plan, update-documentation, push-native-ai-knowledge, sign-off-and-archive = 10 NEW (not 6). Discrepancy with REQUIREMENTS "6 NEW" — REQUIREMENTS likely counts a tighter scope.

Acceptance criteria below tolerate >= 13 rows. The Task 1 reconciliation paragraph documents this discrepancy honestly. Add 3 more rows for the 3 NEW platform skills (rows 17-19: platform-pipefy, platform-wrike, platform-ziflow) — these have `Detailed contract section` = `## platform-pipefy` etc.

Hand-off matrix shape (D-26):

| From | To | Carrier file path | Frontmatter fields propagated | Gating status flag | Hand-off message |
|------|-----|-------------------|-------------------------------|--------------------|--------------------|
| Stage 1 | Stage 2 | `01_kickoff_v<N>.md` | `client:` `project:` `frontmatter_version: 2` | `status: approved` | `> Awaiting `status: approved` write to \`01_kickoff_v<N>.md\`. Branch routing on \`kickoff_branch:\` value (\`discovery-ready\` → Stage 2; \`draft-sow\` → SKIP Stage 2 → Stage 3).` |
| Stage 2 | Stage 3 | `02_discovery_v<N>.md` | `based_on_kickoff: 01_kickoff_v<N>` `client:` `project:` | `status: approved` | `> Awaiting `status: approved` write to \`02_discovery_v<N>.md\` before generate-sow runs.` |
| Stage 3 | Stage 4a | `03_sow_v<N>.md` | `based_on_discovery:` (or `based_on_kickoff:`) `platform:` (if known) | `status: approved` AND `client_review` allowed | `> Awaiting `status: approved`; routing to Stage 4a (platform fnspec) and/or Stage 4b (integration fnspec) per project scope.` |
| Stage 4a | Stage 4b | `04a_fnspec-platform_v<N>.md` | `based_on_sow:` `platform:` `delivery: native-ai \| api` per req | `status: approved` (4a optional for integration-only) | `> Awaiting `status: approved` to \`04a\`; cross-spec consistency check runs at start of 4b.` |
| Stage 4b | Stage 5 | `04b_fnspec-integration_v<N>.md` | `based_on_sow:` `based_on_fnspec_platform:` (if 4a exists) | `status: approved` (5 SKIPPED if no 4b) | `> Awaiting `status: approved` to \`04b\` before tech spec runs. Skip Stage 5 if no 4b exists.` |
| Stage 5 | Stage 6 | `05_techspec_v<N>.md` | `based_on_fnspec_integration:` `based_on_fnspec_platform:` | `status: approved` | `> Awaiting `status: approved` to \`05\` AND wait-for-commercial-inputs gate before generate-cost-estimate runs (per DESIGN-22).` |
| Stage 6 | Stage 7a | `06_cost_v<N>.md` + Coda task-table rows | `based_on_techspec:` `based_on_fnspec_*:` `risk_multiplier_version:` | `status: approved` | `> Awaiting `status: approved` to \`06\` (locks costed scope) before build-prompt generation.` |
| Stage 7a / 7b | Stage 8a | `07a_build-prompt_v<N>.md` AND/OR `07b_implementation-prompt_v<N>.md` | `based_on_fnspec_*:` `based_on_cost:` `delivery:` | `status: approved` (both 7a and 7b if applicable) | `> Awaiting `status: approved` to \`07a\`/\`07b\`; provision-test-harness reads \`delivery:\` routing.` |
| Stage 8a/b/c | Stage 8d | `08a_test-harness_v<N>.md` + `08b_test-plan_v<N>.md` + `08c_uat-plan_v<N>.md` + `client_state.yaml` | `based_on_fnspec_*:` `client_state_version:` `harness_version:` `last_known_schema_version:` | `status: approved` on 8a + 8b; 8c human-facing | `> Awaiting `status: approved` to \`08a\` AND \`08b\`; execute-tests runs against current `client_state.yaml`; sandbox_lock.yaml acquired before run.` |
| Stage 8d | Stage 9 | `08d_test-results_v<N>.md` | `based_on_test_plan:` `based_on_harness:` `failure_classes:` | `status: approved` | `> Awaiting `status: approved` to \`08d\`; doc-diff generation requires test approval.` |
| Stage 9 | Stage 10 | `ChangeRequests/<CR>/doc-diff.md` + Drive doc per closed `doc_type` enum | `doc_type:` `doc_version:` `doc_published_at:` `last_diff_review_at:` | `status: approved` on doc-diff | `> Awaiting `status: approved` to \`doc-diff.md\` AND `doc_published_at` set; push-native-ai-knowledge refuses ingest if \`doc_published_at < last_diff_review_at\` (CRIT-8 fix).` |
| Stage 10 | Stage 11 | per-platform native-AI ingestion artefact + `doc_version:` `ingested_at:` per ingested doc | `native_ai_path:` `target_id:` | `status: approved` on per-platform ingestion record | `> Awaiting `status: approved` on per-platform native-AI ingestion records; sign-off-and-archive runs only after all push-native-ai-knowledge runs succeed (or `native_ai_path: none`).` |

That's 12 transitions; >= 10 satisfies the structural-check assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename `## 13-skill inventory` → `## v2 skill inventory` (resolves cross-AI review MEDIUM #4) + write `## Skill layout` body (DESIGN-11) + `## v2 skill inventory` matrix-then-prose (DESIGN-12)</name>
  <files>.planning/DESIGN.md, .planning/phases/02-design/scripts/design-structure-check.sh</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01 + 02-02 complete)
    - .planning/phases/02-design/scripts/design-structure-check.sh (Plan 02-01 — required_h2 array contains `## 13-skill inventory`; this task updates that entry)
    - .planning/REQUIREMENTS.md §"Skill layout & hand-offs" DESIGN-11, 12, 13 (verbatim sub-bullets — note DESIGN-12 "13 skills total" framing)
    - .planning/research/ARCHITECTURE.md — confirm exact v2 end-state skill count + names; reconcile against the 15-row interpretation
    - .planning/research/SUMMARY.md — 9-phase build plan; per-skill introducing-phase mapping
    - .planning/AUDIT.md §AUDIT-01 — 7 v0.3.0 skills (ground truth for MODIFIED + UNCHANGED + RETIRED tagging)
    - .planning/AUDIT.md §AUDIT-04 — referenced-but-missing skills
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-20, D-21; §"Document structure" D-19
    - .planning/phases/02-design/02-REVIEWS.md MEDIUM #4 (skill-count framing resolution)
  </read_first>
  <action>
    **PRE-STEP — Resolve skill-count framing (per cross-AI review MEDIUM #4 — Codex+Gemini consensus):**

    1. Read `.planning/research/SUMMARY.md` and `.planning/research/ARCHITECTURE.md` to confirm the actual v2 end-state skill count (expected: 15 = 16 categorical items - 1 RETIRED `generate-functional-spec`).
    2. Read REQUIREMENTS.md DESIGN-12 sub-bullet to confirm the "13 skills total" headline figure framing.
    3. Apply the resolution rule (per `<interfaces>` above):
       - The H2 anchor is renamed to `## v2 skill inventory` (no number).
       - The matrix shows the actual v2 end-state count (15 expected; tolerate any count >= 13 to catch obvious under-population).
       - The reconciliation paragraph cites both REQUIREMENTS framing AND research enumeration; explains the legacy "13 skills total" framing.

    **STEP A — Rename the H2 anchor in DESIGN.md:**

    Find the line `## 13-skill inventory` in `.planning/DESIGN.md` and replace it with `## v2 skill inventory`. (Use Edit tool with literal string match.) Also update the populated-by note line below the H2 if it mentions "13-skill inventory" — change to "v2 skill inventory".

    **STEP B — Update the structural-check script's required_h2 array:**

    Find `"## 13-skill inventory"` inside `required_h2=(...)` array in `.planning/phases/02-design/scripts/design-structure-check.sh` and replace with `"## v2 skill inventory"`. Re-run `bash -n .planning/phases/02-design/scripts/design-structure-check.sh` to confirm syntax still valid.

    **STEP C — Write `## Skill layout` body (DESIGN-11)** — replace the placeholder body with:
    1. Echo line: `> **DESIGN-11:** v2 folder layout — \`skills/\`, \`commands/\`, \`agents/\`, \`hooks/\`, \`.claude-plugin/\`, plus the new plugin-level \`references/\` directory.`
    2. One-paragraph framing: "The v2 plugin folder layout extends the v0.3.0 shape (which had `skills/` + `.claude-plugin/` only — per AUDIT.md §AUDIT-01 + §AUDIT-08) with the surfaces locked in DESIGN-04 plus a plugin-level `references/` directory for canonical SoT documents per DESIGN-02 / DESIGN-03."
    3. Code-fenced ASCII tree (use ` ```text ` fence) showing the full v2 folder layout. Show every directory + file, with each leaf comment citing the locking decision (DESIGN-NN, AUDIT-NN, FOUND-NN). Include: `dydx-delivery/`, `.claude-plugin/`, `references/` (with `safety-rules.md`, `stage-numbering.md`, `frontmatter-scheme.md`, `glossary.md`), `skills/` (all 15 v2 skill folders + comments), `commands/` (`refine.md` + 4 GSD shortcuts), `agents/` (`test-bot-orchestrator.md`), `hooks/` (`validate-frontmatter.py`, `bump-artefact-version.py`), `tests/` (3 pytest files).
    4. Trailing line: `**RETIRED in v2:** \`skills/generate-functional-spec/\` (replaced by \`generate-fnspec-platform/\` + \`generate-fnspec-integration/\` per Stage 4 split / DESIGN-20).`

    **STEP D — Write `## v2 skill inventory` body (DESIGN-12)** — replace placeholder body with:
    1. Echo line: `> **DESIGN-12:** v2 skill inventory — 6 NEW stage + 3 NEW platform + 4 MODIFIED + 2 UNCHANGED + 1 RETIRED-AND-REPLACED per REQUIREMENTS DESIGN-12 categorical breakdown. End-state count = 15 skill folders that ship in v2 (the 1 RETIRED is removed; logged separately for migration mapping). Each skill carries purpose, inputs, outputs, downstream consumer, complexity, dependencies, hand-off message shape per D-20.`
    2. **Reconciliation paragraph (REQUIRED per cross-AI review MEDIUM #4):** State explicitly: "REQUIREMENTS.md DESIGN-12 sub-bullet describes the v2 inventory in 5 categories totalling 16 categorical items: 6 NEW stage skills + 3 NEW platform skills + 4 MODIFIED + 2 UNCHANGED + 1 RETIRED-AND-REPLACED. The 'X skills total' headline figure in different framings (legacy '13', categorical '16') is reconciled here against the actual end-state count of skill folders that ship: **15 v2 end-state skills** (16 categorical items minus the 1 RETIRED `generate-functional-spec` which is removed and replaced by the Stage 4 split — `generate-fnspec-platform` + `generate-fnspec-integration`). The matrix below lists the 15 v2 end-state skills; the migration-mapping note at the bottom captures the RETIRED skill. Per Phase 1 D-32 / D-34 honesty precedent: this reconciliation cites architecture research (`.planning/research/SUMMARY.md` + `.planning/research/ARCHITECTURE.md`) as the source of truth for the count; the section title uses neutral 'v2 skill inventory' (no number) so the H2 anchor is resilient if the count adjusts in v2.x."
    3. **Inventory matrix** — multi-row table per `<interfaces>` shape above. Columns: `| # | Skill name | Tag | Stage | Inputs | Outputs | Downstream consumer | Complexity | Dependencies | Detailed contract section |`. One row per v2 end-state skill (expected 15 stage+platform). Tag values: `NEW`, `MODIFIED`, `UNCHANGED-structure / behaviour-modified`, or `NEW (platform)`.
    4. **Per-skill prose subsections** — do NOT write per-skill prose blocks here (those land in Plans 02-04..02-09). Write one short paragraph: "Per-skill decision contracts (purpose, inputs, outputs, downstream consumer, complexity tag, dependencies, hand-off message shape) live in their respective sections per the 'Detailed contract section' column. Per D-20, full SKILL.md prose is NOT drafted in this milestone."
    5. **Migration mapping note** — short paragraph: "RETIRED `generate-functional-spec` maps to the new Stage 4a + Stage 4b skills per DESIGN-20. v0.3.0 artefacts using the old skill name remain readable per DESIGN-08 lenient mode; CR-driven opt-in upgrades them to the new names during their owning CR."

    **Implementing decisions:** D-19 (matrix-then-prose), D-20, D-32 (cite ARCHITECTURE.md), D-34 (AUDIT.md grounding), D-35 (echo line per H2). Cross-AI review MEDIUM #4 fix (skill-count framing resolved pre-execution; H2 renamed to `## v2 skill inventory` and script's required_h2 array updated to match).
  </action>
  <verify>
    <automated>grep -qF '## v2 skill inventory' .planning/DESIGN.md &amp;&amp; ! grep -qF '## 13-skill inventory' .planning/DESIGN.md &amp;&amp; grep -qF '"## v2 skill inventory"' .planning/phases/02-design/scripts/design-structure-check.sh &amp;&amp; bash -n .planning/phases/02-design/scripts/design-structure-check.sh &amp;&amp; grep -qE '^> \*\*DESIGN-11:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-12:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^```text' .planning/DESIGN.md &amp;&amp; grep -qF 'dydx-delivery/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-pipefy/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-wrike/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-ziflow/' .planning/DESIGN.md &amp;&amp; grep -qF 'tests/' .planning/DESIGN.md &amp;&amp; INVENTORY_BODY=$(awk -v hdr='## v2 skill inventory' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md) &amp;&amp; INVENTORY_ROWS=$(printf '%s\n' "$INVENTORY_BODY" | grep -cE '^\| [0-9]+ \|') &amp;&amp; [ "$INVENTORY_ROWS" -ge "13" ] &amp;&amp; echo LAYOUT_AND_INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - **Skill-count framing resolution applied (cross-AI review MEDIUM #4):**
      - `grep -qF '## v2 skill inventory' .planning/DESIGN.md` succeeds (anchor renamed)
      - `grep -qF '## 13-skill inventory' .planning/DESIGN.md` returns NO matches (old anchor gone)
      - `grep -qF '"## v2 skill inventory"' .planning/phases/02-design/scripts/design-structure-check.sh` succeeds (script's required_h2 array updated)
      - `bash -n .planning/phases/02-design/scripts/design-structure-check.sh` exits 0 (script syntax still valid after edit)
      - Reconciliation paragraph present: `grep -qF 'REQUIREMENTS.md DESIGN-12 sub-bullet describes' .planning/DESIGN.md` succeeds (or equivalent reconciliation prose)
      - Reconciliation cites architecture research as source of truth: `grep -qF '.planning/research/' .planning/DESIGN.md` succeeds
    - `grep -qE '^> \*\*DESIGN-11:\*\*' .planning/DESIGN.md` succeeds (DESIGN-11 echo)
    - `grep -qE '^> \*\*DESIGN-12:\*\*' .planning/DESIGN.md` succeeds (DESIGN-12 echo)
    - `grep -qE '^```text$' .planning/DESIGN.md` succeeds (skill-layout ASCII tree)
    - `grep -qF 'dydx-delivery/' .planning/DESIGN.md` succeeds (root in tree)
    - `grep -qF 'platform-pipefy/' .planning/DESIGN.md` AND `grep -qF 'platform-wrike/' .planning/DESIGN.md` AND `grep -qF 'platform-ziflow/' .planning/DESIGN.md` succeed
    - `grep -qF 'kickoff-capture/' .planning/DESIGN.md` succeeds
    - `grep -qF 'generate-fnspec-platform/' .planning/DESIGN.md` AND `grep -qF 'generate-fnspec-integration/' .planning/DESIGN.md` succeed
    - `grep -qF 'tests/' .planning/DESIGN.md` succeeds (D-24 plugin self-tests directory)
    - `grep -qF 'commands/' .planning/DESIGN.md` AND `grep -qF 'agents/' .planning/DESIGN.md` AND `grep -qF 'hooks/' .planning/DESIGN.md` succeed
    - `grep -qF 'references/' .planning/DESIGN.md` succeeds
    - `grep -qF 'safety-rules.md' .planning/DESIGN.md` succeeds
    - `grep -qF 'RETIRED' .planning/DESIGN.md` succeeds
    - **Inventory matrix row count uses stateful section_between extraction** (per cross-AI review HIGH #1):
      ```bash
      awk -v hdr='## v2 skill inventory' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md | grep -cE '^\| [0-9]+ \|'
      ```
      Returns >= 13 (tolerates whatever the research-confirmed end-state count is, with 13 floor)
    - Inventory matrix references at least 6 NEW + 4 MODIFIED + 2 UNCHANGED + 3 platform tags (cumulative)
    - Inventory matrix references stage anchors: `grep -cE 'Stage [0-9]+[ab]?[a-d]?' .planning/DESIGN.md` returns >= 13
    - Migration mapping note present: `grep -qF 'generate-functional-spec' .planning/DESIGN.md` AND `grep -qF 'lenient mode' .planning/DESIGN.md` succeed
  </acceptance_criteria>
  <done>
    Skill-count framing resolved per cross-AI review MEDIUM #4: H2 renamed to `## v2 skill inventory` (no number); script's required_h2 array updated to match; reconciliation paragraph explains the count discrepancy (REQUIREMENTS legacy "13 skills total" vs architecture-research 15 v2 end-state skills) and cites research as source of truth. `## Skill layout` body has DESIGN-11 echo + ASCII tree. `## v2 skill inventory` body has DESIGN-12 echo + reconciliation + inventory matrix with >= 13 rows + migration mapping note.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate `## Stage-by-stage hand-off contract` matrix with >= 10 transition rows (DESIGN-13 / D-26) + run structural-check (assertion #9 should now pass via stateful helper)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — skill layout + inventory populated)
    - .planning/REQUIREMENTS.md §"DESIGN-13"
    - .planning/phases/02-design/02-CONTEXT.md §"Hand-off contract presentation" D-26
    - .planning/phases/02-design/02-CONTEXT.md §"Specifics" — "Hand-off matrix as the most-used DESIGN.md page"
    - .planning/AUDIT.md §AUDIT-01 — current v0.3.0 hand-off contracts
    - .planning/phases/02-design/scripts/design-structure-check.sh — run at end (uses stateful section_between helper per cross-AI review HIGH #1)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Stage-by-stage hand-off contract` body:

    1. **Echo line**: `> **DESIGN-13:** Stage-by-stage hand-off contract — for every stage transition, the carrier file path, the frontmatter fields that propagate, and the status flag that gates the next stage. Single transition matrix per D-26 (matrix-then-prose); per-stage subsections (Stage 1..11) restate hand-off message verbatim for grep-ability.`

    2. **Framing paragraph**: "The hand-off matrix is the single most-cited section of DESIGN.md (per CONTEXT specifics). v2.1+ build phases reference these rows when authoring per-stage SKILL.md files — every carrier file path, frontmatter field name, and gating status flag value here is grep-canonical. Per D-26, the matrix is the source of truth; per-stage subsections (under `## Stage 1: Kickoff capture` through `## Stage 11: Sign-off, brain update, archive`) restate the hand-off message verbatim for navigability."

    3. **Replace the existing matrix header row** (already laid by Plan 02-01) by writing the >= 10 data rows below it. Use the matrix data shown in the `<interfaces>` block above (12 transitions). Each row's `From` cell starts with literal `Stage` (per structural-check assertion #9 regex `^| Stage`). Each row's `Hand-off message` cell uses backtick-wrapped paths and `> Awaiting \`status: approved\`...` blockquote prefix.

    4. **Constraint:** The matrix `Hand-off message` cell strings must be reusable verbatim by Plans 02-05..02-08. Phrase each message as a single line (no embedded newlines).

    5. **After populating, run the structural-check**: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: still exits 1 (echo count is at 13/30 after this plan). The hand-off matrix assertion #9 should now PASS (>= 10 rows extracted via stateful section_between helper). The first failing assertion message should now be assertion #4 (echo count >= 30, found 13).

    **Implementing decisions:** D-26 (single matrix, per-stage subsection restates verbatim — per-stage subsections themselves come from later plans), D-32, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-13:\*\*' .planning/DESIGN.md &amp;&amp; HANDOFF_BODY=$(awk -v hdr='## Stage-by-stage hand-off contract' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md) &amp;&amp; HANDOFF_ROWS=$(printf '%s\n' "$HANDOFF_BODY" | grep -cE '^\| Stage') &amp;&amp; [ "$HANDOFF_ROWS" -ge "10" ] &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "13" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo HANDOFF_MATRIX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-13:\*\*' .planning/DESIGN.md` succeeds (DESIGN-13 echo)
    - **Hand-off matrix data-row count uses stateful section_between extraction** (per cross-AI review HIGH #1 — same helper pattern as the structural-check script):
      ```bash
      awk -v hdr='## Stage-by-stage hand-off contract' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/DESIGN.md | grep -cE '^\| Stage'
      ```
      Returns >= 10
    - **Specific transition rows present** (using same stateful extraction):
      - The body extracted by section_between contains `| Stage 1 | Stage 2 |`
      - The body extracted by section_between contains `| Stage 4a | Stage 4b |`
      - The body extracted by section_between contains `| Stage 9 | Stage 10 |` (CRIT-8 fix transition)
    - **Carrier file paths** present in matrix body (extracted via section_between): `01_kickoff_v`, `04a_fnspec-platform_v`, `08a_test-harness_v`, `doc-diff.md`
    - Hand-off message column references `status: approved`: `grep -cF 'status: approved' .planning/DESIGN.md` returns >= 12
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `13`
    - `bash design-structure-check.sh` exits with code `1` (mid-phase — echo count 13 < 30)
    - Failure message names "expected >= 30 'DESIGN-NN:' success-criteria echo blockquote lines, found 13" (NOT the hand-off matrix assertion — that should now pass via the stateful helper)
  </acceptance_criteria>
  <done>
    Hand-off matrix has DESIGN-13 echo + framing prose + >= 10 transition rows (12 expected). Matrix is grep-canonical for per-stage authors. Structural-check assertion #9 (handoff_rows via stateful section_between helper) PASSES; assertion #4 (echo count) is the failing one now (13/30).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill inventory matrix → per-skill plans 02-04..02-09 | Each per-skill plan reads the matrix's "Detailed contract section" column. A wrong anchor reference here misroutes downstream content. |
| Hand-off matrix → per-stage subsection authors | Per D-26, per-stage subsections restate the hand-off message verbatim. A typo or path drift in the matrix propagates. |
| Skill-count H2-anchor rename → structural-check script | The H2 was renamed from `## 13-skill inventory` → `## v2 skill inventory`. The script's `required_h2` array MUST also be updated (Task 1 STEP B) — otherwise the script fails on a missing anchor at end of this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Skill inventory padded to hit "13 skills" claim despite research enumerating 15 | mitigate | Cross-AI review MEDIUM #4 fix: pre-execution resolution rule applied — H2 renamed to `## v2 skill inventory` (no number); reconciliation paragraph explains framing discrepancy and cites research; matrix shows actual end-state count. Acceptance criteria require >= 13 rows. Phase 1 D-32/D-34 honesty precedent. |
| T-02-03-02 | Tampering | Hand-off matrix carrier file path drift | mitigate | DESIGN-02 stage-numbering scheme is the SoT. Acceptance criteria assert specific canonical filenames present in matrix body (extracted via stateful section_between). |
| T-02-03-03 | Tampering | Hand-off matrix uses `^| **Stage** |` (bold) instead of `^| Stage |` | mitigate | Acceptance criteria check for bare `| Stage X | Stage Y |` form via stateful extraction. |
| T-02-03-04 | Tampering | Inventory matrix omits a v2 skill | mitigate | Acceptance criteria require explicit grep for each platform skill name AND each NEW stage skill name AND tag-count assertions. |
| T-02-03-05 | Tampering | DESIGN-13 hand-off matrix `Hand-off message` cells use multi-line format | mitigate | Task 2 action explicitly requires single-line cells. |
| T-02-03-06 | Information Disclosure | Skill inventory leaks future-phase implementation details | accept | Per D-20, per-skill prose blocks live in their detailed-contract sections — but those are still decision-contract-only. The matrix carries summary info only. |
| T-02-03-07 | Tampering | H2 rename to `## v2 skill inventory` breaks structural-check (script still expects `## 13-skill inventory`) | mitigate | Task 1 STEP B explicitly updates the script's `required_h2` array entry. Acceptance criteria assert `grep -qF '"## v2 skill inventory"' script` AND `bash -n script` exits 0 AND no `## 13-skill inventory` anywhere. |
</threat_model>

<verification>
End of Plan 02-03 success-state:
1. H2 renamed to `## v2 skill inventory` (no number); structural-check script's required_h2 array updated to match
2. `## Skill layout` body has DESIGN-11 echo + ASCII tree of v2 plugin folder layout
3. `## v2 skill inventory` body has DESIGN-12 echo + reconciliation paragraph + inventory matrix with >= 13 rows + per-skill anchor references + migration mapping note
4. `## Stage-by-stage hand-off contract` body has DESIGN-13 echo + framing paragraph + matrix with >= 10 transition rows (verified via stateful section_between)
5. Echo line count = 13
6. `bash design-structure-check.sh` exits 1 with failing assertion = "expected >= 30 'DESIGN-NN:' success-criteria echo blockquote lines, found 13" (assertion #9 hand-off matrix now PASSES via stateful helper; assertion #4 echo count still failing)

Manual reviewer attestation:
- [ ] Skill inventory matrix tag counts match DESIGN-12 breakdown (after reconciliation)
- [ ] Hand-off matrix carrier paths use DESIGN-02 stage-prefixed convention
- [ ] Hand-off matrix "Gating status flag" column uses DESIGN-01 canonical lifecycle values
- [ ] No per-skill SKILL.md prose drafted (per D-20)
- [ ] No new `[OPEN]` markers added (DESIGN-11/12/13 all decided)
- [ ] H2 anchor rename successfully propagated to the structural-check script
</verification>

<success_criteria>
- [ ] H2 renamed `## 13-skill inventory` → `## v2 skill inventory` (cross-AI review MEDIUM #4)
- [ ] Structural-check script's required_h2 array updated to match
- [ ] DESIGN-11 / DESIGN-12 / DESIGN-13 echo lines + bodies populated
- [ ] Skill layout ASCII tree shows all v2 directories
- [ ] Inventory matrix has >= 13 rows (extracted via stateful section_between) with all required tags + reconciliation paragraph
- [ ] Hand-off matrix has >= 10 transition rows (extracted via stateful section_between)
- [ ] Echo count = 13/30
- [ ] Structural-check exits 1 with assertion #4 (echo count) failing — assertion #9 (hand-off matrix rows) now PASSES via stateful helper
- [ ] No content under `dydx-delivery/` modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-03-SUMMARY.md` documenting:
- Skill layout: 7 v2 surfaces enumerated
- **Skill-count framing resolution applied (cross-AI review MEDIUM #4):** H2 renamed `## 13-skill inventory` → `## v2 skill inventory`; reconciliation paragraph cites research; structural-check script's required_h2 array updated to match
- Inventory: actual v2 end-state count (15 expected) + tag breakdown + RETIRED skill mapping
- Hand-off matrix: 12 transition rows; canonical file paths + status flags
- Echo count progression: 13/30
- **Cross-AI review fixes applied:** HIGH #1 (acceptance criteria use stateful section_between extraction for inventory + hand-off row counts); HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #4 (skill-count framing resolved pre-execution).
- Pointer: "Plan 02-04 (Wave 4) populates Platform skills next (DESIGN-14, 15, 16). The 3 platform skill subsections each add 1 echo line; running total after 02-04 = 16/30."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex+Gemini consensus MEDIUM #4 (skill-count framing — renamed H2 from `## 13-skill inventory` → `## v2 skill inventory` + added explicit reconciliation paragraph citing research as source of truth + Task 1 STEP B updates the structural-check script's required_h2 array entry to match the new anchor; resolution rule and rationale documented in `<interfaces>` block); HIGH #1 (replaced broken `awk '/^## X/,/^## /'` ranges in acceptance criteria with stateful section_between extraction for inventory matrix row count AND hand-off matrix row count — consistent with Plan 02-01 script fix); HIGH #2 (added `.planning/phases/02-design/02-03-SUMMARY.md` to `files_modified`). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-11/12/13 still in `requirements`; D-19/20/26/32/34/35 still referenced; no `dydx-delivery/` modifications (the script edit is to the phase-folder verifier, NOT to skill files).
- 2026-05-09 plan-checker BLOCKER-01 fix: added `.planning/phases/02-design/scripts/design-structure-check.sh` to `files_modified` (was present in Task 1 `<files>` element but missing from plan-level frontmatter; without this declaration execute-phase would have rejected Task 1 STEP B's script edit as out-of-scope).
</revision_log>
