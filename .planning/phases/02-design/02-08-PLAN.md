---
plan_id: 02-08
phase: 02-design
plan: 08
type: execute
wave: 8
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06, 02-07]
requirements: [DESIGN-24, DESIGN-25, DESIGN-26, DESIGN-27]
files_modified:
  - .planning/phases/02-design/02-08-PLAN.md
  - .planning/DESIGN.md
autonomous: true
tags: [design, stage-skills, test-bot-overview, documentation, native-ai, sign-off]

must_haves:
  truths:
    - "DESIGN.md `## Stage 8: Test bot — overview` (DESIGN-24) populated with stage-level test-bot architecture: `provision-test-harness` (8a) bootstraps once + delta-updates each ship; persistent harness lives at `<Client> Brain/test-bot/` (outside this repo); tier-1 deterministic Python + tier-2 AI orchestrator with hard layer-separation contract; `harness_drift` failure class added to enum; `sandbox_lock.yaml` for concurrency; sandbox allowlist extended to Coda (CRIT-5 fix); test-case lifecycle states `active | obsolete | quarantined`. **Detailed tier-1/tier-2 boundary + client_state.yaml schema + drift-detection contract live under `## Test bot architecture` section (DESIGN-28..30) populated by Plan 02-09; this section just declares the stage-level overview.**"
    - "DESIGN.md `## Stage 9: Documentation publishing` (DESIGN-25) populated: `update-documentation` skill writes `ChangeRequests/<CR>/doc-diff.md`; reviewer-approval gate before push; deterministic local→Drive folder/filename mapping; closed `doc_type` enum (`discovery | sow | platform_fnspec | integration_fnspec | tech_spec | test_plan | build_prompt | results | brain_spoke`); naming `<client_slug>__<project_slug>__<doc_type>__v<N>` (double-underscore separator); `doc_published_at` timestamp invariant; halt condition if `00_HUB.md` `Documentation:` link missing (graceful — does not halt other stages)."
    - "DESIGN.md `## Stage 10: Native-AI enablement` (DESIGN-26) populated: `push-native-ai-knowledge` skill reads Stage 4a + approved Stage 9 doc fragments + per-platform `native-ai-inventory.md`; branches on `native_ai_path: api | paste | none`; copy-paste fallback default; refuses ingest if `doc_published_at < last_diff_review_at` (CRIT-8 fix); per-client target ID from `00_HUB.md`; refuses ingest if target mismatches `client:` (MIN-4 fix); `doc_version: <semver>` + `ingested_at: <ISO>` per ingested doc."
    - "DESIGN.md `## Stage 11: Sign-off, brain update, archive` (DESIGN-27) populated: `sign-off-and-archive` skill updates local `<Client> Brain/<spokes>/`; one-way Coda mirror push with brain-mirror Coda doc template (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes); `tone_lint` pass before publish (MOD-9 prevention); CR move to `Archive/`; `00_Index.md` version bump; Field Notes table preserved (input-only); pre-archive sanity check (no orphan refs, no missing artefacts)."
    - "Echo lines: DESIGN-24, 25, 26, 27 — 4 NEW echoes. Running total: 29/30."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-24, 25, 26, 27 stage skill decision contracts populated"
      contains: "4 H2 stage subsections each with echo + decision contract per D-20; closed doc_type enum (DESIGN-25); `native_ai_path` branching (DESIGN-26); brain-mirror template sections (DESIGN-27); sandbox allowlist extended to Coda (DESIGN-24 CRIT-5 fix)"
  key_links:
    - from: "DESIGN-25 doc_type closed enum"
      to: "DESIGN-26 native-AI push (reads `doc_type:` to filter ingestable doc fragments)"
      via: "doc_type: enum value"
      pattern: "doc_type: (discovery|sow|platform_fnspec|integration_fnspec|tech_spec|test_plan|build_prompt|results|brain_spoke)"
    - from: "DESIGN-26 doc_published_at < last_diff_review_at refusal (CRIT-8 fix)"
      to: "DESIGN-25 doc_published_at + last_diff_review_at frontmatter contract"
      via: "Both fields set in Stage 9; Stage 10 reads + compares"
      pattern: "doc_published_at >= last_diff_review_at"
    - from: "DESIGN-27 brain-mirror Coda doc template"
      to: "Phase 4 OPEN-05 standard Coda templates"
      via: "Template content (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes) is the design contract; the template itself authored in Phase 4 / Phase 8 owner"
      pattern: "brain-mirror Coda doc template"
---

<objective>
Populate `.planning/DESIGN.md` Stage 8 overview (DESIGN-24), Stage 9 (DESIGN-25), Stage 10 (DESIGN-26), Stage 11 (DESIGN-27) decision contracts.

**Purpose:** Stage 8 is the test bot overview at stage level — detailed architecture (DESIGN-28..30) lives in `## Test bot architecture` populated by Plan 02-09. Stages 9–11 are the publish/native-AI/sign-off triple — each carries critical CRIT-/MOD-/MIN- pitfall fixes (CRIT-8 doc_published_at gate; MIN-4 target ID mismatch; MOD-9 tone_lint).

**Output:** `.planning/DESIGN.md` Stages 8, 9, 10, 11 sections populated. 4 echo lines added. Running total: 29/30. Plan 02-09 adds the final DESIGN-30 echo to land at 30/30.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/PITFALLS.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/phases/02-design/02-05-PLAN.md
@.planning/phases/02-design/02-06-PLAN.md
@.planning/phases/02-design/02-07-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Stage 8 overview here is stage-level (skill bootstrap pattern + persistent harness location + tier separation
     declared at high level + pitfall fixes). The detailed tier-1/tier-2 boundary + client_state.yaml schema +
     drift-detection algorithm live under ## Test bot architecture (DESIGN-28..30) populated by Plan 02-09.
     This plan provides the stage-level overview; Plan 02-09 provides the architectural detail. -->

DESIGN-25 closed `doc_type` enum (REQUIREMENTS canonical):
- discovery
- sow
- platform_fnspec
- integration_fnspec
- tech_spec
- test_plan
- build_prompt
- results
- brain_spoke

DESIGN-25 naming pattern: `<client_slug>__<project_slug>__<doc_type>__v<N>` — double-underscore separator (REQUIREMENTS canonical).

DESIGN-27 brain-mirror Coda doc template sections (REQUIREMENTS canonical):
- Overview
- Workflows
- Platforms
- Integrations
- Operating Model
- Change History
- Field Notes

DESIGN-24 stage 8 carrier files: `08a_test-harness_v<N>.md`, `08b_test-plan_v<N>.md`, `08c_uat-plan_v<N>.md`, `08d_test-results_v<N>.md` (note 8d in `<Client> Brain/test-bot/` per CONTEXT — paths move from current `dydx-delivery/skills/execute-tests/` location).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Stage 8: Test bot — overview` (DESIGN-24) + `## Stage 9: Documentation publishing` (DESIGN-25)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-07 complete)
    - .planning/REQUIREMENTS.md §"DESIGN-24" (full sub-bullet — provision-test-harness 8a; persistent harness location; tier-1/tier-2; harness_drift; sandbox_lock.yaml; CRIT-5 sandbox allowlist Coda fix; test-case lifecycle states; 8b/8c/8d follow same template structure as today, paths move to <Client> Brain/test-bot/)
    - .planning/REQUIREMENTS.md §"DESIGN-25" (full sub-bullet — update-documentation skill; doc-diff.md; reviewer-approval gate; deterministic mapping; closed doc_type enum; double-underscore naming; doc_published_at; halt condition for missing 00_HUB.md Documentation: link)
    - .planning/AUDIT.md §AUDIT-01.5 (v0.3.0 generate-test-plan brittleness — what becomes 8b in v2)
    - .planning/AUDIT.md §AUDIT-01.7 (v0.3.0 execute-tests brittleness — what becomes 8d in v2)
    - .planning/research/PITFALLS.md — CRIT-5 (sandbox allowlist missing Coda); CRIT-8 (doc_published_at gate); MOD-1 (graceful halt); MIN-4 (target ID mismatch)
    - .planning/phases/02-design/02-CONTEXT.md §"Test-bot architecture detail" D-30 (interface-contract + skeleton schema; detailed contract lives under ## Test bot architecture populated by Plan 02-09)
    - .planning/DESIGN.md hand-off matrix Stage 7a/7b → 8a, Stage 8a/b/c → 8d, Stage 8d → 9, Stage 9 → 10 rows (verbatim per D-26)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate Stage 8 + Stage 9 H2 sections.

    **(A) `## Stage 8: Test bot — overview` (DESIGN-24)** — replace placeholder body:
    1. Echo line: `> **DESIGN-24:** Stage 8 Test bot architecture (overview) — \`provision-test-harness\` skill (8a) bootstraps once + delta-updates each ship; persistent harness lives at \`<Client> Brain/test-bot/{client_state.yaml, test_runner.py, test_cases/}\` (outside this repo); tier-1 deterministic Python + tier-2 AI orchestrator with hard layer-separation contract; \`harness_drift\` failure class added to \`spec gap | implementation gap | environment issue | unknown\`; \`sandbox_lock.yaml\` for concurrency; sandbox allowlist extended to Coda (CRIT-5 fix); test-case lifecycle states \`active | obsolete | quarantined\`. Detailed tier-1/tier-2 boundary + \`client_state.yaml\` schema + drift-detection contract live under \`## Test bot architecture\` (DESIGN-28..30).`
    2. **Stage-level overview only** — note explicitly: "This section provides stage-level overview of the test bot. Detailed architecture (Python ↔ AI orchestrator boundary; client_state.yaml schema; drift-detection algorithm) lives under `## Test bot architecture` (DESIGN-28..30)."
    3. Skills enumerated under Stage 8: 8a `provision-test-harness/` (NEW), 8b `generate-test-plan/` (MODIFIED), 8c `generate-uat-plan/` (NEW), 8d `execute-tests/` (MODIFIED — paths move to `<Client> Brain/test-bot/`; agent invocation per DESIGN-04 test-bot-orchestrator).
    4. **Persistent harness location:** `<Client> Brain/test-bot/{client_state.yaml, test_runner.py, test_cases/}` — explicitly OUTSIDE this repo. Bootstrap once (8a first run); delta-updates each ship (8a subsequent runs read existing `client_state.yaml`, diff against current state, write delta only).
    5. **Sandbox allowlist (CRIT-5 fix):** v0.3.0 `safety-rules.md` allowlists Pipefy / Wrike / Ziflow sandbox tenants but OMITS Coda sandbox. v2 extends the allowlist to include Coda sandbox doc(s) per client. The canonical safety-rules at `dydx-delivery/references/safety-rules.md` (per DESIGN-03) carries the extended allowlist.
    6. **Tier separation (high-level — detail in DESIGN-28..30):** Python tier-1 (deterministic) + AI tier-2 (orchestrator) — hard separation: Python tests are HUMAN-AUTHORED, never AI-generated; AI does NOT write tier-1. Per `## Out of Scope` "Generating Python tier-1 tests from natural language alone" anti-feature.
    7. **Failure-class enum extension:** `harness_drift` added to `spec gap | implementation gap | environment issue | unknown` (5 classes total in v2). Drift detection per DESIGN-30.
    8. **Concurrency:** `sandbox_lock.yaml` per client at `<Client> Brain/test-bot/sandbox_lock.yaml`. Acquired at start of 8d run; released at end. Prevents two concurrent test runs against same sandbox.
    9. **Test-case lifecycle states:** `active` (default — runs in current ship test plan); `obsolete` (`targets_artefact:` no longer exists in current artefact set — auto-detected per `targets_artefact:` frontmatter on each TC); `quarantined` (manually flagged for skip — flaky / known-broken; reviewer must add justification).
    10. **Inputs / Outputs / Downstream / Status / Hand-off** subsections — span the 4 substages 8a/8b/8c/8d:
        - 8a Inputs: `04a_*` + `04b_*` (delivery routing); `client_state.yaml` (existing or NEW); upstream `based_on_fnspec_*:` frontmatter. 8a Outputs: `08a_test-harness_v<N>.md` + delta-updated `client_state.yaml` + populated `<Client> Brain/test-bot/test_cases/`.
        - 8b Inputs: `04b_*` + `05_techspec_v*` + `08a_*`. Outputs: `08b_test-plan_v<N>.md` with per-TC layer tagging (tier-1 / tier-2 per DESIGN-28).
        - 8c Inputs: `04a_*` + `04b_*` + `08b_*`. Outputs: `08c_uat-plan_v<N>.md` (human-facing).
        - 8d Inputs: `08a_*`, `08b_*`, `08c_*`, `client_state.yaml`, `sandbox_lock.yaml`. Outputs: `08d_test-results_v<N>.md` in `<Client> Brain/test-bot/`. Invokes `test-bot-orchestrator` agent (DESIGN-04) for tier-2 AI orchestration.
    11. **Hand-off message(s)** — verbatim from matrix Stage 7a/7b → 8a + Stage 8a/b/c → 8d + Stage 8d → 9 rows.
    12. **Cross-references:** DESIGN-04 (test-bot-orchestrator agent + sandbox_lock); DESIGN-28/29/30 (detailed architecture); DESIGN-25 (downstream); AUDIT.md §AUDIT-01.5 + §AUDIT-01.7; PITFALLS CRIT-5.

    **(B) `## Stage 9: Documentation publishing` (DESIGN-25)** — replace placeholder body:
    1. Echo line: `> **DESIGN-25:** Stage 9 Documentation publishing — \`update-documentation\` skill writes \`ChangeRequests/<CR>/doc-diff.md\`; reviewer-approval gate before push; deterministic local→Drive folder/filename mapping; closed \`doc_type\` enum; naming \`<client_slug>__<project_slug>__<doc_type>__v<N>\` (double-underscore separator); \`doc_published_at\` timestamp invariant; halt condition if \`00_HUB.md\` \`Documentation:\` link missing (graceful — does not halt other stages per MOD-1).`
    2. Skill / Stage / Complexity: `update-documentation/` (NEW) / Stage 9 / Medium.
    3. **Inputs:** Frontmatter (`based_on_test_results: 08d_test-results_v<N>` REQUIRED + all upstream `04*/05/06/07*` artefacts referenced); upstream artefacts (8d approved + all upstream); external (Drive MCP for publish; client `00_HUB.md` for `Documentation:` Drive folder link).
    4. **Outputs:** `ChangeRequests/<CR>/doc-diff.md` (intermediate diff for reviewer); per-doc Drive uploads named `<client_slug>__<project_slug>__<doc_type>__v<N>`. Frontmatter on each Drive doc: `doc_type: <enum value>`; `doc_version: <semver>`; `doc_published_at: <ISO timestamp>`; `last_diff_review_at: <ISO timestamp>` (set when reviewer approves doc-diff.md).
    5. **Closed `doc_type` enum (DESIGN-25 contract — exactly 9 values):** `discovery | sow | platform_fnspec | integration_fnspec | tech_spec | test_plan | build_prompt | results | brain_spoke`. Values not in this enum → reject in skill quality gate. (Note: `build_prompt` covers BOTH 7a dev and 7b implementation — distinguish by `delivery_filter:` frontmatter on the source artefact, not `doc_type:`.)
    6. **Naming convention (DESIGN-25 contract):** `<client_slug>__<project_slug>__<doc_type>__v<N>` — DOUBLE-UNDERSCORE separator (per REQUIREMENTS verbatim). Reject single-underscore, hyphen, or mixed separators in skill quality gate. Example: `acme-inc__widget-redesign__platform_fnspec__v3`.
    7. **doc-diff workflow:** Skill computes diff against previously-published Drive doc (if any). Writes `doc-diff.md` to `ChangeRequests/<CR>/`. Reviewer-approval gate: skill HALTS until reviewer writes `status: approved` to `doc-diff.md` AND sets `last_diff_review_at: <ISO>` on the diff. Only then does skill push to Drive (sets `doc_published_at: <ISO>` on Drive doc + frontmatter — invariant).
    8. **Halt condition (MOD-1 graceful):** If `<Client> Brain/00_HUB.md` `Documentation:` link is missing, skill halts with explicit error in `doc-diff.md` ("Cannot publish — `Documentation:` Drive folder link missing in `00_HUB.md`. Add link and retry.") The halt is GRACEFUL: only Stage 9 halts; other stages run normally. Per MOD-1 prevention.
    9. **Downstream consumer:** push-native-ai-knowledge (Stage 10 — gated by `doc_published_at >= last_diff_review_at` per CRIT-8 fix); reviewer (human consumer for doc-diff approval).
    10. **Status flag:** `status: approved` on `doc-diff.md` (not on the Drive doc — Drive is one-way mirror per DESIGN-09).
    11. **Hand-off message:** verbatim from matrix Stage 9 → 10 row (incl. CRIT-8 reference).
    12. **Key v2 decisions** (5 numbered): closed doc_type enum (rejection in quality gate); double-underscore naming convention (rejection in quality gate); doc-diff before publish (reviewer-approval gate); doc_published_at invariant (CRIT-8 prerequisite for Stage 10); MOD-1 graceful halt on missing Documentation link.
    13. **Dependencies:** DESIGN-04 (Drive MCP); DESIGN-09 (Drive one-way mirror direction); DESIGN-26 (downstream — CRIT-8 contract).
    14. **Cross-references:** DESIGN-26 (CRIT-8); AUDIT.md §AUDIT-04 (`<Client> Brain/00_HUB.md` `Documentation:` link backfill — Phase 4 OPEN-04 backfill coordination); PITFALLS MOD-1, CRIT-8.

    **Implementing decisions:** D-20 (decision-contract-only), D-26 (hand-off messages verbatim), D-32/D-34 (cite AUDIT.md + PITFALLS), D-35 (echo per H2).
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-24:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-25:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'provision-test-harness' .planning/DESIGN.md &amp;&amp; grep -qF '<Client> Brain/test-bot/' .planning/DESIGN.md &amp;&amp; grep -qF 'harness_drift' .planning/DESIGN.md &amp;&amp; grep -qF 'sandbox_lock.yaml' .planning/DESIGN.md &amp;&amp; grep -qF 'sandbox allowlist extended to Coda' .planning/DESIGN.md &amp;&amp; grep -qF 'CRIT-5' .planning/DESIGN.md &amp;&amp; grep -qF 'doc_type' .planning/DESIGN.md &amp;&amp; grep -qF '__v' .planning/DESIGN.md &amp;&amp; grep -qF 'doc_published_at' .planning/DESIGN.md &amp;&amp; grep -qF 'last_diff_review_at' .planning/DESIGN.md &amp;&amp; grep -qF 'platform_fnspec' .planning/DESIGN.md &amp;&amp; grep -qF 'brain_spoke' .planning/DESIGN.md &amp;&amp; grep -qF 'MOD-1' .planning/DESIGN.md &amp;&amp; echo STAGES_8_9_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-24:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qE '^> \*\*DESIGN-25:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qF 'provision-test-harness' .planning/DESIGN.md` succeeds (Stage 8a skill)
    - `grep -qF '<Client> Brain/test-bot/' .planning/DESIGN.md` succeeds (persistent harness location outside repo)
    - `grep -qF 'harness_drift' .planning/DESIGN.md` succeeds (failure-class enum extension)
    - `grep -qF 'sandbox_lock.yaml' .planning/DESIGN.md` succeeds (concurrency mechanism)
    - `grep -qF 'sandbox allowlist extended to Coda' .planning/DESIGN.md` succeeds (CRIT-5 fix wording)
    - `grep -qF 'CRIT-5' .planning/DESIGN.md` succeeds (pitfall ID cited)
    - `grep -qF 'active | obsolete | quarantined' .planning/DESIGN.md` OR all 3 lifecycle words present individually (`active`, `obsolete`, `quarantined`)
    - **DESIGN-25 closed doc_type enum — all 9 values present:** `discovery`, `sow`, `platform_fnspec`, `integration_fnspec`, `tech_spec`, `test_plan`, `build_prompt`, `results`, `brain_spoke` (each as literal substring)
    - `grep -qF '__v' .planning/DESIGN.md` succeeds (double-underscore version separator pattern)
    - `grep -qF '<client_slug>__<project_slug>__<doc_type>__v<N>' .planning/DESIGN.md` OR equivalent literal naming-convention example present
    - `grep -qF 'doc_published_at' .planning/DESIGN.md` AND `grep -qF 'last_diff_review_at' .planning/DESIGN.md` succeed (CRIT-8 fix paired fields)
    - `grep -qF 'MOD-1' .planning/DESIGN.md` succeeds (graceful-halt pitfall ID)
    - `grep -qF 'Documentation:' .planning/DESIGN.md` succeeds (00_HUB.md link reference)
    - Hand-off messages verbatim from matrix: `grep -qF 'doc_published_at < last_diff_review_at' .planning/DESIGN.md` succeeds (matrix Stage 9 → 10 referenced text containing CRIT-8 fix)
  </acceptance_criteria>
  <done>
    Stage 8 overview (DESIGN-24) populated with skills enumeration + persistent harness location + sandbox allowlist Coda extension (CRIT-5) + failure-class enum + sandbox_lock + lifecycle states + tier-separation high-level. Stage 9 (DESIGN-25) populated with skill + closed doc_type enum (9 values) + double-underscore naming + doc-diff workflow + reviewer-approval gate + doc_published_at invariant + MOD-1 graceful halt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `## Stage 10: Native-AI enablement` (DESIGN-26) + `## Stage 11: Sign-off, brain update, archive` (DESIGN-27) + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — Stages 8 + 9 populated)
    - .planning/REQUIREMENTS.md §"DESIGN-26" (push-native-ai-knowledge skill; reads 4a + 9 fragments + per-platform native-ai-inventory.md; branches on native_ai_path; copy-paste fallback default; refuses ingest if doc_published_at < last_diff_review_at — CRIT-8; per-client target ID in 00_HUB.md; refuses ingest if target mismatches client: — MIN-4; doc_version + ingested_at per ingested doc)
    - .planning/REQUIREMENTS.md §"DESIGN-27" (sign-off-and-archive; updates local <Client> Brain/<spokes>/; one-way Coda mirror push with brain-mirror Coda doc template — Overview/Workflows/Platforms/Integrations/Operating Model/Change History/Field Notes; tone_lint pass — MOD-9; CR move to Archive/; 00_Index.md version bump; Field Notes preserved input-only; pre-archive sanity check)
    - .planning/research/PITFALLS.md — CRIT-8 (doc_published_at gate); MIN-4 (target ID mismatch); MOD-9 (tone_lint)
    - .planning/AUDIT.md §AUDIT-04.4 (Field Notes + brain mirror references; Phase 4 OPEN-05 brain-mirror template)
    - .planning/DESIGN.md hand-off matrix Stage 10 → 11 row + Stage 11 final position
    - .planning/phases/02-design/scripts/design-structure-check.sh — run at end to verify echo count = 29/30
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate Stage 10 + Stage 11 H2 sections.

    **(A) `## Stage 10: Native-AI enablement` (DESIGN-26)** — replace placeholder body:
    1. Echo line: `> **DESIGN-26:** Stage 10 Native-AI enablement — \`push-native-ai-knowledge\` skill reads Stage 4a + approved Stage 9 doc fragments + per-platform \`native-ai-inventory.md\`; branches on \`native_ai_path: api | paste | none\`; copy-paste fallback is the default; refuses to ingest if \`doc_published_at < last_diff_review_at\` (CRIT-8 fix); per-client target ID in \`00_HUB.md\` \`Pipefy AI:\` / \`Wrike AI:\` / \`Ziflow AI:\` blocks; refuses ingest if target mismatches \`client:\` frontmatter (MIN-4 fix); \`doc_version: <semver>\` + \`ingested_at: <ISO>\` per ingested doc.`
    2. Skill / Stage / Complexity: `push-native-ai-knowledge/` (NEW) / Stage 10 / High (per-platform knowledge ingestion + 2 refusal gates).
    3. **Inputs:** Frontmatter (`based_on_fnspec_platform: 04a_*` + `based_on_doc_diff: ChangeRequests/<CR>/doc-diff.md` (approved) + `client:` + `platform:` (per-platform routing)); upstream (`04a` + Stage 9 doc fragments approved per `last_diff_review_at:`); external (per-platform `native-ai-inventory.md` from `platform-<X>` skill — drives the `native_ai_path` branching).
    4. **Outputs:** Per-platform native-AI knowledge ingestion records: Pipefy AI KB upload OR Wrike AI attached doc OR Ziflow ReviewAI checklist (per `native_ai_path` value). Each ingested doc carries `doc_version: <semver>` + `ingested_at: <ISO>` + `target_id: <platform-specific>`. Local artefact `10_native-ai-push_v<N>.md` records what was ingested where.
    5. **Branching on `native_ai_path` (DESIGN-26 contract):**
       - `api`: Direct API ingestion via per-platform mechanism (Pipefy KB upload IF `[OPEN]` resolves; Wrike attach-doc-via-MCP; Ziflow ReviewAI checklist API IF available). HIGH-confidence path.
       - `paste`: Copy-paste fallback — skill EMITS the doc fragment formatted for paste (per platform-specific format), HALTS, prompts human to paste into target surface, resumes when human confirms.
       - `none`: Skip — no native-AI surface for this doc fragment. Recorded in `10_native-ai-push_v<N>.md` for traceability.
    6. **CRIT-8 refusal (DESIGN-26 contract):** Skill REFUSES to ingest any doc fragment whose source doc carries `doc_published_at < last_diff_review_at` (i.e., Drive doc was published BEFORE the diff was reviewed — meaning the published doc may not match the reviewed diff). Refusal is hard halt with explicit error; no fallback.
    7. **MIN-4 refusal (DESIGN-26 contract):** Skill REFUSES to ingest if the per-platform target ID (from `<Client> Brain/00_HUB.md` `Pipefy AI:` / `Wrike AI:` / `Ziflow AI:` block) does not match the artefact's `client:` frontmatter value. Prevents cross-client native-AI contamination. Hard halt; no fallback.
    8. **Default: copy-paste fallback** — Per `## Out of Scope` "Promising direct-API ingestion when only copy-paste works" — skill defaults to `paste` mode unless platform's `native-ai-inventory.md` confirms HIGH-confidence direct ingestion (i.e., the relevant `[OPEN]` markers from DESIGN-14/15/16 have been resolved). LOW-confidence native-AI capability → `paste` mode.
    9. **Downstream consumer:** sign-off-and-archive (Stage 11 — runs after all native-AI ingestion records are `status: approved`).
    10. **Status flag:** `status: approved` on `10_native-ai-push_v<N>.md`.
    11. **Hand-off message:** verbatim from matrix Stage 10 → 11 row.
    12. **Key v2 decisions** (5 numbered): native_ai_path branching (3 paths); CRIT-8 refusal (doc_published_at gate); MIN-4 refusal (target ID match); copy-paste default (no optimistic API claims); per-doc traceability (doc_version + ingested_at + target_id).
    13. **Dependencies:** DESIGN-14/15/16 (per-platform native-ai-inventory.md as routing input); DESIGN-25 (Stage 9 doc_published_at + last_diff_review_at fields); platform-pipefy/-wrike/-ziflow (`references/knowledge-ingestion.md`).
    14. **Cross-references:** DESIGN-25 (CRIT-8 paired contract); DESIGN-27 (downstream); PITFALLS CRIT-8 + MIN-4; AUDIT.md §AUDIT-04 (00_HUB.md target ID blocks — Phase 4 OPEN-05 default schema).

    **(B) `## Stage 11: Sign-off, brain update, archive` (DESIGN-27)** — replace placeholder body:
    1. Echo line: `> **DESIGN-27:** Stage 11 Sign-off, brain update, archive — \`sign-off-and-archive\` skill updates local \`<Client> Brain/<spokes>/\`; one-way push to Coda mirror with brain-mirror Coda doc template (Overview / Workflows / Platforms / Integrations / Operating Model / Change History / Field Notes); \`tone_lint\` pass before publish (MOD-9 prevention); CR move to \`Archive/\`; \`00_Index.md\` version bump; Field Notes table preserved (input-only, never overwritten); pre-archive sanity check (no orphan refs, no missing artefacts).`
    2. Skill / Stage / Complexity: `sign-off-and-archive/` (NEW) / Stage 11 / Medium.
    3. **Inputs:** Frontmatter (all upstream artefacts approved — `01_kickoff_*` through `10_native-ai-push_*` all `status: approved`; `cr_id:` for the CR being signed off); upstream artefacts (the entire CR's artefact set); external (Coda MCP for one-way mirror push; brain-mirror Coda doc template per Phase 4 OPEN-05).
    4. **Outputs:** Updated `<Client> Brain/<spokes>/` (per-spoke markdown updates: Overview, Workflows, Platforms, Integrations, Operating Model, Change History, Field Notes preserved); Coda mirror doc updated (one-way per DESIGN-09); CR moved to `<Client> Brain/Archive/<CR>/`; `00_Index.md` version bump; `11_signoff_v<N>.md` carrier in `<Client> Brain/Archive/<CR>/`. Frontmatter on `11_signoff_v<N>.md`: standard + `archived_at: <ISO>` + `tone_lint_status: passed | failed`.
    5. **Brain-mirror Coda doc template (DESIGN-27 contract — 7 canonical sections):** Overview, Workflows, Platforms, Integrations, Operating Model, Change History, Field Notes. Each section has its own template body (per Phase 4 OPEN-05 — the actual template authoring is Phase 8 owner per Phase 4 success criteria; DESIGN.md commits to the section list). Push direction: ONE-WAY (local → Coda) per DESIGN-09. Field Notes section is INPUT-ONLY in Coda — Stage 11 NEVER overwrites the Field Notes table; it pulls Field Notes for record only via the Stage 1 triage-filter side effect (Stage 1 already updated `processed_at:` on triaged notes per DESIGN-17).
    6. **`tone_lint` pass (MOD-9 prevention):** Before publishing brain-spoke updates to Coda, skill runs `tone_lint` against EACH spoke (per DESIGN-10 forbidden-phrasings list + voice principles). Failure halts; reviewer must fix flagged passages and rerun.
    7. **CR move to Archive/:** Source CR folder `<Client> Brain/<Project>/ChangeRequests/<CR>/` moved to `<Client> Brain/<Project>/Archive/<CR>/`. Sign-off carrier `11_signoff_v<N>.md` written into `Archive/<CR>/`.
    8. **`00_Index.md` version bump:** `<Client> Brain/<Project>/00_Index.md` carries a per-CR version log (one entry per archived CR). Sign-off appends a new entry.
    9. **Pre-archive sanity check (DESIGN-27 contract):** Before move-to-Archive, skill verifies: no orphan references in `<Client> Brain/<spokes>/` to artefacts NOT in the CR's artefact set; no `based_on_*:` frontmatter pointing at missing files; all 11_signoff_v<N>.md upstream references resolve. Failure halts.
    10. **Field Notes preserved input-only:** Coda Field Notes table is NEVER overwritten by Stage 11. Per DESIGN-09 directional boundary: Field Notes is read-only triage queue. Stage 11 only WRITES the brain-spoke sections + Change History; Field Notes stays as-is.
    11. **Downstream consumer:** None within the v2 pipeline (Stage 11 is terminal). Outputs feed back into next CR's Stage 1 (next kickoff reads from updated `<Client> Brain/<spokes>/`).
    12. **Status flag:** `status: approved` on `11_signoff_v<N>.md`.
    13. **Hand-off message:** Pipeline-end message — `> Pipeline complete for CR <cr_id>. Brain updated; Coda mirror published; CR archived. Next CR's Stage 1 reads from updated <Client> Brain/.`
    14. **Key v2 decisions** (6 numbered): brain-mirror 7-section template (per Phase 4 OPEN-05); one-way Coda mirror per DESIGN-09; tone_lint MOD-9 prevention; CR move to Archive/ + 00_Index.md version bump; pre-archive sanity check (orphan refs + missing artefacts); Field Notes preserved input-only.
    15. **Dependencies:** DESIGN-09 (one-way Coda mirror direction); DESIGN-10 (tone_lint principles + forbidden list); DESIGN-26 (upstream — Stage 10 native-AI ingestion approved); Phase 4 OPEN-05 (brain-mirror Coda doc template authoring deferred to Phase 8 owner per Phase 4 success criteria).
    16. **Cross-references:** DESIGN-09, DESIGN-10, DESIGN-25 (Change History entries reference Drive doc versions); PITFALLS MOD-9; Phase 4 OPEN-05 + OPEN-04 (hub-link backfill coordination).

    **(C) Run structural-check** at end: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 1 (echo count = 29/30 after this plan; Plan 02-09 adds DESIGN-30 echo to land at 30/30 — but DESIGN-28 + 29 echoes also added by 02-09 land in `## Test bot architecture` H3 subsections, so total at end of Plan 02-09 = 32 echoes).

    **Wait — recount.** Echo count tally:
    - Plan 02-02: DESIGN-01..10 = 10
    - Plan 02-03: DESIGN-11, 12, 13 = 3 → 13
    - Plan 02-04: DESIGN-14, 15, 16 = 3 → 16
    - Plan 02-05: DESIGN-17, 18, 19 = 3 → 19
    - Plan 02-06: DESIGN-20 (×2 — under 4a + 4b), DESIGN-21 = 3 → 22
    - Plan 02-07: DESIGN-22, DESIGN-23 (×2 — under 7a + 7b) = 3 → 25
    - Plan 02-08: DESIGN-24, 25, 26, 27 = 4 → 29
    - Plan 02-09: DESIGN-28, 29, 30 = 3 → 32

    Total at end of Plan 02-09 = 32 echo lines. Structural-check assertion #4 requires >= 30 → passes with margin. Plan 02-08 lands at 29; assertion #4 still failing (29 < 30) by 1.

    **Implementing decisions:** D-20 (decision-contract-only), D-26 (hand-off message verbatim), D-32/D-34 (cite AUDIT.md + PITFALLS), D-35 (echo per H2).
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-26:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-27:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'push-native-ai-knowledge' .planning/DESIGN.md &amp;&amp; grep -qF 'sign-off-and-archive' .planning/DESIGN.md &amp;&amp; grep -qF 'native_ai_path: api | paste | none' .planning/DESIGN.md &amp;&amp; grep -qF 'CRIT-8' .planning/DESIGN.md &amp;&amp; grep -qF 'MIN-4' .planning/DESIGN.md &amp;&amp; grep -qF 'MOD-9' .planning/DESIGN.md &amp;&amp; grep -qF 'tone_lint' .planning/DESIGN.md &amp;&amp; grep -qF 'brain-mirror' .planning/DESIGN.md &amp;&amp; grep -qF 'Operating Model' .planning/DESIGN.md &amp;&amp; grep -qF 'Change History' .planning/DESIGN.md &amp;&amp; grep -qF 'Field Notes' .planning/DESIGN.md &amp;&amp; grep -qF '00_Index.md' .planning/DESIGN.md &amp;&amp; grep -qF '10_native-ai-push_v' .planning/DESIGN.md &amp;&amp; grep -qF '11_signoff_v' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "29" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo STAGES_10_11_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-26:\*\*' .planning/DESIGN.md` AND `grep -qE '^> \*\*DESIGN-27:\*\*' .planning/DESIGN.md` succeed
    - `grep -qF 'push-native-ai-knowledge' .planning/DESIGN.md` AND `grep -qF 'sign-off-and-archive' .planning/DESIGN.md` succeed (skill names per DESIGN-12 inventory)
    - `grep -qF 'native_ai_path: api | paste | none' .planning/DESIGN.md` succeeds (DESIGN-26 branching contract verbatim from REQUIREMENTS)
    - `grep -qF 'CRIT-8' .planning/DESIGN.md` succeeds (DESIGN-26 doc_published_at refusal pitfall ID)
    - `grep -qF 'MIN-4' .planning/DESIGN.md` succeeds (DESIGN-26 target ID mismatch refusal)
    - `grep -qF 'MOD-9' .planning/DESIGN.md` succeeds (DESIGN-27 tone_lint pitfall ID)
    - `grep -qF 'tone_lint' .planning/DESIGN.md` succeeds (DESIGN-27 contract)
    - **DESIGN-27 brain-mirror template — all 7 sections present:** `Overview`, `Workflows`, `Platforms`, `Integrations`, `Operating Model`, `Change History`, `Field Notes` (each as literal substring near the brain-mirror reference)
    - `grep -qF '00_Index.md' .planning/DESIGN.md` succeeds (DESIGN-27 version bump target)
    - `grep -qF '10_native-ai-push_v' .planning/DESIGN.md` succeeds (Stage 10 carrier per DESIGN-02)
    - `grep -qF '11_signoff_v' .planning/DESIGN.md` succeeds (Stage 11 carrier per DESIGN-02)
    - `grep -qF 'doc_version' .planning/DESIGN.md` AND `grep -qF 'ingested_at' .planning/DESIGN.md` succeed (DESIGN-26 per-doc tracking)
    - `grep -qF 'pre-archive sanity check' .planning/DESIGN.md` succeeds (DESIGN-27 contract)
    - `grep -qF 'Field Notes preserved' .planning/DESIGN.md` OR `grep -qF 'Field Notes table is NEVER overwritten' .planning/DESIGN.md` succeeds (DESIGN-27 + DESIGN-09 cross-reference)
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `29` (cumulative from all previous plans)
    - `bash design-structure-check.sh` exits with code `1` (mid-phase — echo count 29 < 30; Plan 02-09 + 02-10 land remaining)
    - No new `[OPEN]` markers added in Stages 8–11 sections (counter from end of Plan 02-07 unchanged at >= 9)
  </acceptance_criteria>
  <done>
    Stages 8 (overview), 9, 10, 11 decision contracts populated. Stage 8 declares overview only — detailed architecture lives under `## Test bot architecture` (Plan 02-09). Stage 9 has closed doc_type enum (9 values) + double-underscore naming + reviewer-approval gate + doc_published_at invariant + MOD-1 graceful halt. Stage 10 has native_ai_path branching + CRIT-8 + MIN-4 refusal gates + copy-paste default. Stage 11 has brain-mirror 7-section template + tone_lint MOD-9 + CR archive + 00_Index version bump + pre-archive sanity + Field Notes preservation. Echo count = 29/30.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage 9 doc_published_at + last_diff_review_at fields → Stage 10 CRIT-8 refusal | The two fields must be set in Stage 9 BEFORE Stage 10 reads them. Drift in field naming or timestamp format breaks the CRIT-8 fix. |
| Stage 10 per-platform target ID → MIN-4 cross-client contamination prevention | If `00_HUB.md` `Pipefy AI:` / `Wrike AI:` / `Ziflow AI:` blocks are missing or formatted differently than the skill expects, MIN-4 refusal misfires. |
| Stage 11 Coda one-way mirror → Field Notes preservation | If Stage 11 accidentally writes to the Field Notes table, the canonical input-only contract per DESIGN-09 is broken. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | DESIGN-25 doc_type enum has fewer or extra values vs REQUIREMENTS canonical 9 | mitigate | Acceptance criteria assert all 9 enum values as literal substrings (`discovery`, `sow`, `platform_fnspec`, `integration_fnspec`, `tech_spec`, `test_plan`, `build_prompt`, `results`, `brain_spoke`). Wrong count fails the gate. |
| T-02-08-02 | Tampering | DESIGN-25 naming convention drifts to single underscore or hyphen | mitigate | Acceptance criteria assert literal `__v` substring (the version separator) AND the full convention string `<client_slug>__<project_slug>__<doc_type>__v<N>` near the naming-convention callout. |
| T-02-08-03 | Tampering | DESIGN-26 native_ai_path uses `paste | api | none` (reverse order) | mitigate | Acceptance criteria assert literal `native_ai_path: api | paste | none` (canonical order from REQUIREMENTS). Wrong order fails. |
| T-02-08-04 | Tampering | DESIGN-26 omits the `api` HIGH-confidence default and ships `paste` everywhere → optimistic-API anti-pattern violation | mitigate | DESIGN-26 contract explicitly states "copy-paste fallback IS the default" + "Per `## Out of Scope` 'Promising direct-API ingestion when only copy-paste works' — skill defaults to `paste` mode unless platform's `native-ai-inventory.md` confirms HIGH-confidence direct ingestion." This is the CORRECT direction (default-to-paste, not default-to-api). Acceptance criteria assert the `## Out of Scope` reference. |
| T-02-08-05 | Tampering | DESIGN-27 brain-mirror template missing one of the 7 sections | mitigate | Acceptance criteria assert all 7 section names (`Overview`, `Workflows`, `Platforms`, `Integrations`, `Operating Model`, `Change History`, `Field Notes`) as literal substrings. |
| T-02-08-06 | Tampering | DESIGN-27 Field Notes preservation contract gets reversed (skill writes to Field Notes table) | mitigate | Acceptance criteria assert `Field Notes preserved` or `Field Notes table is NEVER overwritten`. DESIGN-09 cross-reference reinforces. Per DESIGN-09 directional boundary, Field Notes is INPUT-ONLY — any future v2.x author reverses this at their own peril. |
| T-02-08-07 | Tampering | Stage 8 overview drifts into Plan 02-09's DESIGN-28..30 territory (writes detailed tier-1/tier-2 boundary or full client_state.yaml schema) | mitigate | Action explicitly states "Stage-level overview only — Detailed architecture lives under `## Test bot architecture` (DESIGN-28..30)." If Plan 02-08 writes the detailed architecture, Plan 02-09's work would duplicate it. Manual reviewer attestation in Verification step checks no overlap. |
</threat_model>

<verification>
End of Plan 02-08 success-state:
1. Stages 8 (overview) + 9 + 10 + 11 H2 sections populated with full decision contracts per D-20
2. DESIGN-24, 25, 26, 27 echo lines added (4 new)
3. Echo count = 29/30
4. `bash design-structure-check.sh` exits 1 with assertion #4 (echo count, found 29) failing — only 1 echo line short of the 30 floor
5. Inline `[OPEN]` count unchanged from end of Plan 02-07 (no new markers in Stages 8–11; all contracts LOCKED)
6. Stage 8 overview is overview-only — no DESIGN-28..30 detail leaked from Plan 02-09's territory

Manual reviewer attestation:
- [ ] DESIGN-25 closed `doc_type` enum has exactly 9 values matching REQUIREMENTS canonical order
- [ ] DESIGN-25 naming convention uses double-underscore separator (verbatim per REQUIREMENTS)
- [ ] DESIGN-26 native_ai_path enum order matches REQUIREMENTS (`api | paste | none`)
- [ ] DESIGN-26 default mode is `paste` (per Out of Scope anti-optimistic-API contract)
- [ ] DESIGN-27 brain-mirror template enumerates all 7 canonical sections
- [ ] DESIGN-27 Field Notes preservation contract maintained (input-only)
- [ ] Stage 8 overview does NOT contain detailed tier-1/tier-2 boundary, full client_state.yaml schema, or drift-detection algorithm (those live in Plan 02-09's `## Test bot architecture`)
</verification>

<success_criteria>
- [ ] DESIGN-24/25/26/27 echo lines + decision contracts populated
- [ ] DESIGN-25: closed doc_type enum (9 values) + double-underscore naming + reviewer-approval gate + doc_published_at + MOD-1
- [ ] DESIGN-26: native_ai_path branching + CRIT-8 + MIN-4 refusals + copy-paste default
- [ ] DESIGN-27: brain-mirror 7-section template + tone_lint MOD-9 + CR archive + 00_Index bump + Field Notes preservation
- [ ] Echo count = 29/30
- [ ] No new `[OPEN]` markers in Stages 8–11
- [ ] Structural-check exits 1 (1 echo short of 30)
- [ ] No content under `dydx-delivery/` modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-08-SUMMARY.md` documenting:
- Stage 8 overview (skills + persistent harness location + sandbox-allowlist Coda extension CRIT-5 + failure-class enum + sandbox_lock + lifecycle states + tier-separation high-level)
- Stage 9: 9-value doc_type enum + double-underscore naming + reviewer-approval gate + doc_published_at + MOD-1 graceful halt
- Stage 10: native_ai_path branching + CRIT-8 doc_published_at refusal + MIN-4 target ID refusal + copy-paste default
- Stage 11: brain-mirror 7 sections + tone_lint MOD-9 + Field Notes preservation + 00_Index bump + pre-archive sanity
- Echo count: 29/30
- Pointer: "Plan 02-09 (Wave 9) populates `## Test bot architecture` (DESIGN-28, 29, 30) — interface contract + skeleton client_state.yaml + drift-detection contract per D-30. Each adds 1 echo line. Running total after 02-09 = 32/30 (assertion #4 passes with margin). Plan 02-10 then handles synthesis (executive summary, glossary, Appendix B traceability, closed [OPEN] list)."
</output>
