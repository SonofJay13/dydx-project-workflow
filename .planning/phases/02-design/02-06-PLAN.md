---
plan_id: 02-06
phase: 02-design
plan: 06
type: execute
wave: 6
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
requirements: [DESIGN-20, DESIGN-21]
files_modified:
  - .planning/phases/02-design/02-06-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-06-SUMMARY.md
autonomous: true
tags: [design, stage-skills, fnspec-split, tech-spec]

must_haves:
  truths:
    - "DESIGN.md `## Stage 4a: Functional spec — platform` (DESIGN-20 first half) and `## Stage 4b: Functional spec — integration` (DESIGN-20 second half) populated with decision contracts per D-20: per-requirement `delivery: native-ai | api` tagging (the routing key for downstream stages); per-platform capability matrix as classifier input; cross-spec consistency check; either spec optional for single-track projects; legacy single fnspec retired."
    - "DESIGN.md `## Stage 5: Tech spec` (DESIGN-21) populated: scope-gated to Stage 4b existence; lightweight platform-API addendum on Stage 4a when API-required portions exist on platform-only build; covers error handling + observability + retries + idempotency for API portions; never hand-waves error paths."
    - "Stage 4a and Stage 4b each get their own `> **DESIGN-NN:**` echo line — DESIGN-20 echoes appear under BOTH 4a AND 4b H2s (since DESIGN-20 covers both substages); DESIGN-21 echo under Stage 5. Total NEW echoes = 3 (1 each for 4a, 4b, 5). Running total: 22/30."
    - "DESIGN-20 4a output schema includes `delivery: native-ai | api` per requirement — this is the routing key that Stage 5 (DESIGN-21 — scope gate), Stage 6 (DESIGN-22 — cost categorisation), Stage 7b (DESIGN-23 — implementation prompt routing), Stage 10 (DESIGN-26 — native-AI push) all consume."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-20 (Stage 4a + 4b) and DESIGN-21 (Stage 5) decision contracts populated"
      contains: "3 H2 stage subsections (4a, 4b, 5) each with echo + decision contract per D-20; routing-key contract `delivery: native-ai | api`; cross-spec consistency check; scope gate"
  key_links:
    - from: "DESIGN-20 `delivery: native-ai | api` per-requirement tagging"
      to: "DESIGN-21 (Stage 5 scope gate); DESIGN-22 (Stage 6 cost routing — forward); DESIGN-23 (Stage 7b implementation prompt routing — forward); DESIGN-26 (Stage 10 native-AI push routing — forward)"
      via: "`delivery:` field is the single routing key consumed by 4 downstream stages"
      pattern: "delivery: (native-ai|api)"
    - from: "DESIGN-21 scope gate"
      to: "Stage 5 SKIPPED if no Stage 4b exists; lightweight platform-API addendum on Stage 4a instead"
      via: "Stage 4b existence is the routing decision"
      pattern: "exists(`04b_fnspec-integration_v*`)"
---

<objective>
Populate `.planning/DESIGN.md` Stage 4a (DESIGN-20 platform fnspec), Stage 4b (DESIGN-20 integration fnspec), Stage 5 (DESIGN-21 tech spec scope gate) decision contracts.

**Purpose:** Stage 4 split (4a + 4b) is the single biggest v2 architectural change for stage skills (per CONTEXT specifics — "Stage 4 (fnspec split — DESIGN-20)... carries the most decision weight"). Routes per-requirement `delivery: native-ai | api` tag is the key consumed by 4 downstream stages. Stage 5 introduces scope-gating against Stage 4b existence, with platform-API addendum for platform-only builds.

**Output:** `.planning/DESIGN.md` Stages 4a, 4b, 5 sections populated. 3 of 30 echo lines added. Running total: 22/30.

**Forward-reference guardrails (per cross-AI review MEDIUM #6):** Cross-references to DESIGN-22 (Stage 6), DESIGN-23 (Stage 7), DESIGN-26 (Stage 10) from this plan's content are FORWARD references — those sections do not exist yet at end of Wave 6. Cross-references cite the DESIGN-NN ID only as anchor placeholders; acceptance criteria do NOT assert that the cross-referenced body content exists. Plan 02-10 (synthesis) verifies Appendix B traceability covers all 30 IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/phases/02-design/02-05-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- DESIGN-20 covers 4a + 4b. The echo line `> **DESIGN-20:**` appears under EACH H2:
     under 4a H2 with text scoped to platform fnspec; under 4b H2 with text scoped to integration fnspec.
     This satisfies D-35 per-section echo (each H2 gets an echo) AND keeps DESIGN-20 traceability (both echoes
     reference the same DESIGN-20 ID).

     Section-range extraction in acceptance criteria uses awk ranges with DISTINCT start/end anchors
     (e.g., `awk '/^## Stage 4a:/,/^## Stage 4b:/'`) — these are SAFE because the start regex does NOT
     match the end regex on the same line. The broken `awk '/^## X/,/^## /'` pattern (cross-AI review
     HIGH #1) is forbidden; this plan does not use it. -->

Stage 4a subsection key elements:

```markdown
## Stage 4a: Functional spec — platform

> **DESIGN-20:** Stage 4a Fnspec — platform — `generate-fnspec-platform` skill; per-requirement `delivery: native-ai | api` tagging (the routing key for downstream Stages 5 / 6 / 7b / 10); per-platform capability matrix as classifier input; cross-spec consistency check against 4b; optional for integration-only projects.

**Skill:** `generate-fnspec-platform/` (NEW per inventory; replaces `generate-functional-spec` for platform portions).
**Stage:** 4a (file prefix `04a_fnspec-platform_*` per DESIGN-02).
**Complexity:** Medium-high.

**Inputs.**
- Frontmatter: `based_on_sow: 03_sow_v<N>` + `client:` + `project:` + `platform: pipefy | wrike | ziflow` (REQUIRED for this skill to run; activates platform-gated identifiers per DESIGN-01).
- Upstream artefact: `03_sow_v<N>.md` (Stage 3 output).
- External inputs: per-platform capability matrix from `platform-<X>` skill's `references/native-ai-inventory.md` (per DESIGN-14/15/16) — used as classifier input for `delivery:` tagging.

**Outputs.**
- Carrier: `04a_fnspec-platform_v<N>.md`.
- **Per-requirement `delivery:` tagging (DESIGN-20 contract):** Each requirement row in the fnspec carries an explicit `delivery: native-ai | api` field. Classifier rule: if the requirement maps cleanly to a platform native-AI capability (per the platform's `native-ai-inventory.md` matrix with HIGH/MEDIUM confidence), `delivery: native-ai`; otherwise `delivery: api`. LOW-confidence native-AI capabilities default to `delivery: api` to avoid promising direct ingestion when only copy-paste works (per `## Out of Scope`).
- Frontmatter: `frontmatter_version: 2`; `platform: <X>`; `pipe_id` / `space_id` / `project_id` (whichever matches platform per DESIGN-01); `status: draft → client_review → approved`.

**Downstream consumer.**
- generate-fnspec-integration (Stage 4b — for cross-spec consistency check)
- generate-cost-estimate (Stage 6 — uses `delivery:` for cost categorisation) — FORWARD reference per cross-AI review MEDIUM #6
- generate-implementation-prompt (Stage 7b — uses `delivery:` for routing) — FORWARD reference
- push-native-ai-knowledge (Stage 10 — reads `delivery: native-ai` rows for ingest selection) — FORWARD reference

**Status flag.**
- `status: approved`; 4a is OPTIONAL for integration-only projects (project skips to 4b directly).

**Hand-off message** (verbatim from matrix Stage 4a → Stage 4b row).

> Awaiting `status: approved` to `04a`; cross-spec consistency check runs at start of 4b.

**Key v2 decisions for this stage.**

1. **Per-requirement `delivery:` tagging** — single routing key `delivery: native-ai | api` per requirement row. Drives 4 downstream stages (5 / 6 / 7b / 10) without re-classifying.
2. **Per-platform capability matrix as classifier input** — Stage 4a reads the platform skill's `native-ai-inventory.md` (DESIGN-14/15/16) and uses HIGH/MEDIUM confidence rows to suggest `delivery: native-ai`; LOW confidence (i.e., `[OPEN]` flagged in platform skill) defaults to `delivery: api` to avoid optimistic claims.
3. **Cross-spec consistency check (runs at start of 4b)** — when 4a + 4b both exist, a check at start of 4b verifies: no requirement appears in both specs with conflicting `delivery:` tags; integration touchpoints in 4b reference platform requirements in 4a by ID; no orphan API endpoints.
4. **Optional for integration-only projects** — if project has no platform-side work, 4a does not run. SOW (Stage 3) `platform:` field absence signals this.
5. **Legacy single fnspec retired** — `generate-functional-spec` (v0.3.0) is RETIRED per DESIGN-12 inventory; v0.3.0 artefacts using the old skill remain readable per DESIGN-08 lenient mode.

**Dependencies.**
- DESIGN-14/15/16 (per-platform capability matrices as classifier input)
- DESIGN-01 (platform-gated identifier rules)
- DESIGN-08 (lenient mode for v0.3.0 single-fnspec artefacts)

**Cross-references.** DESIGN-19 (Stage 3 SOW upstream); DESIGN-21 (Stage 5 platform-API addendum — same wave); DESIGN-22 (Stage 6 reads `delivery:` — FORWARD); DESIGN-23 (Stage 7b reads `delivery:` — FORWARD); DESIGN-26 (Stage 10 reads `delivery: native-ai` — FORWARD); AUDIT.md §AUDIT-01.3 (v0.3.0 generate-functional-spec brittleness — single-spec-for-everything anti-pattern — backward, populated).
```

Stage 4b shape mirrors Stage 4a but for integration scope; DESIGN-20 echo appears with text scoped to "Stage 4b Fnspec — integration".
Stage 5 shape carries the scope-gate logic (REQUIRED only when 4b exists; lightweight platform-API addendum on 4a when platform-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Stage 4a: Functional spec — platform` + `## Stage 4b: Functional spec — integration` decision contracts (DESIGN-20)</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-05 complete)
    - .planning/REQUIREMENTS.md §"DESIGN-20" (verbatim sub-bullet — Stage 4 fnspec split; per-requirement `delivery: native-ai | api` tagging; per-platform capability matrix as classifier input; cross-spec consistency check; optional for single-track projects; legacy single fnspec retired)
    - .planning/AUDIT.md §AUDIT-01.3 (v0.3.0 generate-functional-spec brittleness — single-spec-for-everything anti-pattern; v2 split addresses this)
    - .planning/research/ARCHITECTURE.md — Stage 4 split rationale + cross-spec consistency check shape
    - .planning/phases/02-design/02-CONTEXT.md §"Specifics" — Stage 4 carries the most decision weight; subsection length proportional
    - .planning/DESIGN.md hand-off matrix Stage 3 → 4a, Stage 4a → 4b, Stage 4b → 5 rows (copy verbatim per D-26)
    - .planning/DESIGN.md `### platform-pipefy` / `### platform-wrike` / `### platform-ziflow` capability matrices (Plan 02-04 output — used as classifier input per DESIGN-20)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Stage 4a: Functional spec — platform` and `## Stage 4b: Functional spec — integration` H2s.

    **(A) Stage 4a (DESIGN-20 platform half)** — replace placeholder body using the per-stage shape from `<interfaces>` above:
    1. Echo line: `> **DESIGN-20:** Stage 4a Fnspec — platform — \`generate-fnspec-platform\` skill; per-requirement \`delivery: native-ai | api\` tagging (the routing key for downstream Stages 5 / 6 / 7b / 10); per-platform capability matrix as classifier input; cross-spec consistency check against 4b; optional for integration-only projects.`
    2. Skill / Stage / Complexity: `generate-fnspec-platform/` (NEW) / Stage 4a / Medium-high.
    3. **Inputs**: Frontmatter consumed (`based_on_sow:`, `client:`, `project:`, `platform: pipefy | wrike | ziflow` REQUIRED); upstream artefact (`03_sow_v<N>.md`); external inputs (per-platform `native-ai-inventory.md` from DESIGN-14/15/16 — classifier input).
    4. **Outputs**: carrier `04a_fnspec-platform_v<N>.md`; **per-requirement `delivery: native-ai | api` tagging (THE DESIGN-20 contract)**; frontmatter set incl. platform-gated identifier per DESIGN-01.
    5. **Downstream consumer**: 4 stages enumerate (4b consistency check, 6 cost — FORWARD, 7b implementation prompt — FORWARD, 10 native-AI push — FORWARD).
    6. **Status flag**: `status: approved`; OPTIONAL for integration-only projects.
    7. **Hand-off message**: verbatim from Stage 4a → 4b matrix row.
    8. **Key v2 decisions** (5 numbered points): per-requirement `delivery:` tagging; per-platform capability matrix as classifier input; cross-spec consistency check (runs at start of 4b); optional for integration-only; legacy single fnspec retired.
    9. **Dependencies**: DESIGN-14/15/16, DESIGN-01, DESIGN-08.
    10. **Cross-references**: DESIGN-19 (backward), 21 (same wave), 22, 23, 26 (forward); AUDIT.md §AUDIT-01.3 (backward).

    **(B) Stage 4b (DESIGN-20 integration half)** — replace placeholder body using same shape:
    1. Echo line: `> **DESIGN-20:** Stage 4b Fnspec — integration — \`generate-fnspec-integration\` skill; per-requirement \`delivery: native-ai | api\` tagging continues (per DESIGN-20 same routing key); cross-spec consistency check against 4a runs at start; optional for platform-only projects (Stage 5 SKIPPED with platform-API addendum on 4a instead).`
    2. Skill / Stage / Complexity: `generate-fnspec-integration/` (NEW) / Stage 4b / Medium-high.
    3. **Inputs**: Frontmatter consumed (`based_on_sow:` + optional `based_on_fnspec_platform: 04a_fnspec-platform_v<N>` if 4a exists); upstream (`03_sow_v<N>.md` + optional `04a`).
    4. **Outputs**: carrier `04b_fnspec-integration_v<N>.md`; same per-requirement `delivery:` tagging continues.
    5. **Cross-spec consistency check** (THE 4b key responsibility): runs FIRST. Checks: (a) no requirement ID in both 4a and 4b with conflicting `delivery:` tags; (b) every integration touchpoint in 4b that references a platform requirement cites the requirement ID from 4a; (c) no orphan API endpoints. Failure halts before fnspec write; emits `04b_consistency_check_v<N>.md`.
    6. **Downstream consumer**: generate-technical-spec (Stage 5 — same wave); generate-cost-estimate (Stage 6 — FORWARD); generate-build-prompt (Stage 7a — FORWARD).
    7. **Status flag**: `status: approved`; OPTIONAL for platform-only projects.
    8. **Hand-off message**: verbatim from Stage 4b → Stage 5 matrix row.
    9. **Key v2 decisions** (4 numbered points): cross-spec consistency check (THIS skill owns it); same `delivery:` tagging contract; optional for platform-only; legacy single fnspec retired.
    10. **Dependencies**: DESIGN-20 4a (consistency check); DESIGN-01.
    11. **Cross-references**: Stage 4a, 5 (same wave), 6, 7a (FORWARD); AUDIT.md §AUDIT-01.3.

    **Implementing decisions:** D-20, D-26, D-32/D-34, D-35.
  </action>
  <verify>
    <automated>STAGE_4A_ECHO=$(awk '/^## Stage 4a:/,/^## Stage 4b:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-20:\*\*') &amp;&amp; [ "$STAGE_4A_ECHO" = "1" ] &amp;&amp; STAGE_4B_ECHO=$(awk '/^## Stage 4b:/,/^## Stage 5:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-20:\*\*') &amp;&amp; [ "$STAGE_4B_ECHO" = "1" ] &amp;&amp; grep -qF 'delivery: native-ai | api' .planning/DESIGN.md &amp;&amp; grep -qF 'cross-spec consistency check' .planning/DESIGN.md &amp;&amp; grep -qF '04a_fnspec-platform_v' .planning/DESIGN.md &amp;&amp; grep -qF '04b_fnspec-integration_v' .planning/DESIGN.md &amp;&amp; grep -qF 'generate-fnspec-platform' .planning/DESIGN.md &amp;&amp; grep -qF 'generate-fnspec-integration' .planning/DESIGN.md &amp;&amp; grep -qF 'legacy single fnspec retired' .planning/DESIGN.md &amp;&amp; echo STAGE_4_OK</automated>
  </verify>
  <acceptance_criteria>
    - Stage 4a subsection contains exactly 1 `> **DESIGN-20:**` echo line — SAFE awk range with distinct start/end anchors: `awk '/^## Stage 4a:/,/^## Stage 4b:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-20:\*\*'` = 1
    - Stage 4b subsection contains exactly 1 `> **DESIGN-20:**` echo line — SAFE awk range: `awk '/^## Stage 4b:/,/^## Stage 5:/' .planning/DESIGN.md | grep -cE '^> \*\*DESIGN-20:\*\*'` = 1
    - `grep -qF 'delivery: native-ai | api' .planning/DESIGN.md` succeeds (THE routing key contract)
    - `grep -qF 'cross-spec consistency check' .planning/DESIGN.md` succeeds
    - `grep -qF '04a_fnspec-platform_v' .planning/DESIGN.md` succeeds
    - `grep -qF '04b_fnspec-integration_v' .planning/DESIGN.md` succeeds
    - `grep -qF 'generate-fnspec-platform' .planning/DESIGN.md` AND `grep -qF 'generate-fnspec-integration' .planning/DESIGN.md` succeed
    - `grep -qF 'legacy single fnspec retired' .planning/DESIGN.md` succeeds
    - `grep -qF 'platform: pipefy | wrike | ziflow' .planning/DESIGN.md` OR `grep -qF '`platform: pipefy | wrike | ziflow`' .planning/DESIGN.md` succeeds
    - Hand-off messages literal-match: `grep -qF '> Awaiting `status: approved` to `04a`' .planning/DESIGN.md` AND `grep -qF '> Awaiting `status: approved` to `04b`' .planning/DESIGN.md` succeed
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** Cross-references to DESIGN-22/23/26 (Stages 6/7b/10) are anchor placeholders only — acceptance criteria do NOT assert that those sections' body content exists at end of Wave 6.
  </acceptance_criteria>
  <done>
    Stage 4a + Stage 4b decision contracts populated with DESIGN-20 echo lines under each H2, the `delivery: native-ai | api` routing-key contract, cross-spec consistency check (in 4b), optional-substage rules. Both hand-off messages verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `## Stage 5: Tech spec` (DESIGN-21) decision contract + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — Stage 4a + 4b populated)
    - .planning/REQUIREMENTS.md §"DESIGN-21"
    - .planning/AUDIT.md §AUDIT-01.4
    - .planning/DESIGN.md hand-off matrix Stage 5 → Stage 6 row (copy verbatim per D-26)
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-20
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — replace placeholder body of `## Stage 5: Tech spec` H2.

    Required elements:
    1. Echo line: `> **DESIGN-21:** Stage 5 Tech spec scope gate — REQUIRED only when Stage 4b exists; lightweight platform-API addendum on Stage 4a when API-required portions exist on platform-only build; covers error handling + observability + retries + idempotency for API portions; never hand-waves error paths.`
    2. Skill / Stage / Complexity: `generate-technical-spec/` (MODIFIED) / Stage 5 / Medium.
    3. **Inputs**: Frontmatter (`based_on_fnspec_integration: 04b_fnspec-integration_v<N>` REQUIRED + optional `based_on_fnspec_platform: 04a_fnspec-platform_v<N>`); upstream artefact (`04b` REQUIRED, optional `04a`).
    4. **Outputs**: carrier `05_techspec_v<N>.md`. Frontmatter: `tech_spec_scope: full | platform-api-addendum-only`.
    5. **Scope gate (DESIGN-21 contract)**:
       - **Default path:** Stage 4b exists → full tech spec runs.
       - **Skip path:** Stage 4b does NOT exist → tech spec SKIPS. BUT if 4a has any requirement with `delivery: api`, a **platform-API addendum** runs INSIDE 4a artefact as a `## Platform-API Addendum` H2 section; 4a frontmatter gains `has_platform_api_addendum: true`.
       - **Edge case:** No API portions anywhere → tech spec SKIPS entirely.
    6. **Error-paths discipline**: every API endpoint enumerates (a) failure modes; (b) retry policy; (c) idempotency; (d) observability. Hand-waving forbidden.
    7. **Downstream consumer**: generate-cost-estimate (Stage 6 — FORWARD); generate-build-prompt (Stage 7a — FORWARD).
    8. **Status flag**: `status: approved`.
    9. **Hand-off message**: verbatim from matrix Stage 5 → Stage 6 row.
    10. **Key v2 decisions** (3 numbered): scope-gate; lightweight platform-API addendum; error-paths discipline.
    11. **Dependencies**: DESIGN-20 (4a / 4b existence + `delivery:` routing); DESIGN-22 (FORWARD — wait-for-commercial-inputs gate downstream).
    12. **Cross-references**: Stage 4a, 4b (same wave), 6 (FORWARD); AUDIT.md §AUDIT-01.4.

    **Run structural-check** at end. Expected: exits 1 (echo count = 22/30).

    **Implementing decisions:** D-20, D-26, D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-21:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'tech_spec_scope:' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-API addendum' .planning/DESIGN.md &amp;&amp; grep -qF 'has_platform_api_addendum:' .planning/DESIGN.md &amp;&amp; grep -qF '05_techspec_v' .planning/DESIGN.md &amp;&amp; grep -qF 'idempotency' .planning/DESIGN.md &amp;&amp; grep -qF 'observability' .planning/DESIGN.md &amp;&amp; grep -qF 'retry policy' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "22" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo STAGE_5_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-21:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qF 'tech_spec_scope:' .planning/DESIGN.md` succeeds
    - `grep -qF 'platform-api-addendum-only' .planning/DESIGN.md` OR `grep -qF '`platform-api-addendum-only`' .planning/DESIGN.md` succeeds
    - `grep -qF 'platform-API addendum' .planning/DESIGN.md` succeeds
    - `grep -qF 'has_platform_api_addendum:' .planning/DESIGN.md` succeeds
    - `grep -qF '05_techspec_v' .planning/DESIGN.md` succeeds
    - All four error-paths discipline elements: `grep -qF 'idempotency'` AND `grep -qF 'observability'` AND `grep -qF 'retry policy'` AND `grep -qF 'failure modes'` succeed
    - `grep -qF 'hand-waving' .planning/DESIGN.md` succeeds
    - Hand-off message contains verbatim Stage 5 → 6 string (incl. `wait-for-commercial-inputs` gate per DESIGN-22 — already in matrix from Plan 02-03)
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `22`
    - `bash design-structure-check.sh` exits with code `1` (mid-phase)
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** Cross-references to DESIGN-22 (Stage 6) cited as anchor placeholder only.
  </acceptance_criteria>
  <done>
    Stage 5 Tech spec decision contract populated with DESIGN-21 echo + scope gate (full vs platform-API addendum vs skip) + error-paths discipline + hand-off message verbatim. Echo count = 22/30.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `delivery:` routing key in 4a output → 4 downstream stages | Undefined value → undefined behaviour. |
| Stage 4b cross-spec consistency check | Inconsistencies MUST halt before 4b write. |
| Stage 5 scope-gate decision | Three branches; misrouting drops error-paths discipline. |
| Forward references to DESIGN-22 (Stage 6 — same plan as DESIGN-23 in Plan 02-07) — populated in Plan 02-07 (cross-AI review MEDIUM #6) | Cross-references cite the ID only; body verification deferred to Plan 02-10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | DESIGN-20 contract uses `delivery: api | native-ai` (reverse order) | mitigate | Acceptance criteria assert literal `delivery: native-ai | api`. |
| T-02-06-02 | Tampering | Cross-spec consistency check ownership ambiguous | mitigate | Two-place declaration (4a + 4b). |
| T-02-06-03 | Tampering | Stage 5 scope gate omits "no API portions anywhere" edge case | mitigate | Action explicitly enumerates 3 branches. |
| T-02-06-04 | Tampering | Stage 5 error-paths drifts to checklist-style | mitigate | Acceptance criteria assert all 4 elements + `hand-waving` callout. |
| T-02-06-05 | Tampering | DESIGN-20 echo count mismatch | mitigate | Per-stage scoped checks via SAFE awk ranges (distinct start/end). |
| T-02-06-06 | Tampering | Stage 5 hand-off message omits wait-for-commercial-inputs | mitigate | Verbatim from matrix. |
| T-02-06-07 | Tampering | Acceptance criteria assert content from forward-referenced DESIGN-22/23/26 that don't exist yet — false-fail at end of Wave 6 | mitigate | Cross-AI review MEDIUM #6 fix: forward references are anchor placeholders only; body verification deferred to Plan 02-10. |
</threat_model>

<verification>
End of Plan 02-06 success-state:
1. Stage 4a, 4b, 5 H2 sections populated
2. DESIGN-20 echo line appears exactly once under each of Stage 4a + Stage 4b H2s (2 occurrences total)
3. DESIGN-21 echo under Stage 5 H2
4. `delivery: native-ai | api` routing key contract present
5. Cross-spec consistency check named (4a + 4b — assigned to 4b)
6. Stage 5 scope gate has 3 branches
7. Echo count = 22/30
8. `bash design-structure-check.sh` exits 1 with assertion #4 (echo count) failing
9. Forward references to DESIGN-22/23/26 are anchor placeholders only

Manual reviewer attestation:
- [ ] `delivery:` routing key matches REQUIREMENTS verbatim
- [ ] Cross-spec consistency check enumerates 3 specific checks
- [ ] Stage 5 scope gate branches mapped 1:1 to fnspec-existence states
- [ ] Error-paths discipline lists 4 specific concerns per endpoint
- [ ] No full SKILL.md prose drafted
</verification>

<success_criteria>
- [ ] DESIGN-20 echoes under 4a + 4b H2s (2 instances counted via SAFE awk ranges)
- [ ] DESIGN-21 echo under Stage 5
- [ ] `delivery: native-ai | api` routing-key contract present
- [ ] Cross-spec consistency check: 3 specific checks enumerated
- [ ] Stage 5: 3 scope-gate branches
- [ ] Error-paths discipline: 4 elements per endpoint
- [ ] Echo count = 22/30
- [ ] Structural-check exits 1 (mid-phase)
- [ ] No content under `dydx-delivery/` modified
- [ ] Forward-reference guardrails applied (cross-AI review MEDIUM #6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-06-SUMMARY.md` documenting:
- Stage 4 split: 4a + 4b decision contracts + `delivery: native-ai | api` routing-key contract
- Cross-spec consistency check (4 → 4b assignment + 3 specific checks)
- Stage 5 scope gate: 3 branches + error-paths discipline (4 elements)
- Echo count: 22/30 (DESIGN-20 contributes 2 echoes for 4a + 4b)
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #6 (forward-reference guardrails — cross-references to DESIGN-22/23/26 are anchor placeholders only).
- Pointer: "Plan 02-07 (Wave 7) populates Stage 6 + Stage 7a/7b next."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-06-SUMMARY.md` to `files_modified`); MEDIUM #6 (forward-reference guardrails — explicit `<objective>` callout + threat T-02-06-07 added; cross-references to DESIGN-22/23/26 are anchor placeholders only at end of Wave 6). Existing safe `awk '/^## Stage 4a:/,/^## Stage 4b:/'` and `awk '/^## Stage 4b:/,/^## Stage 5:/'` patterns kept (distinct start/end anchors — not the broken `/^## X/,/^## /` pattern). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-20/21 still in `requirements`; D-20/26/32/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
