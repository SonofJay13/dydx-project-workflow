---
plan_id: 02-04
phase: 02-design
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
requirements: [DESIGN-14, DESIGN-15, DESIGN-16]
files_modified:
  - .planning/phases/02-design/02-04-PLAN.md
  - .planning/DESIGN.md
autonomous: true
tags: [design, platform-skills, native-ai, internalisation]

must_haves:
  truths:
    - "DESIGN.md `## Platform skills` section opens with a 3-row platform-comparison matrix per D-19 (rows: pipefy / wrike / ziflow; columns: native-AI surface / API protocol / sandbox access pattern / native_ai_path default / known gaps)."
    - "Each of `### platform-pipefy`, `### platform-wrike`, `### platform-ziflow` H3 subsections opens with `> **DESIGN-NN:**` echo (D-35) + 5-file `references/` shape declaration + 2026-grounded native-AI capability matrix + API surface for the gap + sandbox access pattern + `native_ai_path: api | paste | none` flag with confidence + `tier_claims_last_verified` frontmatter contract."
    - "Per D-21, ONLY decision contracts (purpose, 5-file shape, capability matrix, API surface, sandbox pattern, ingestion path, frontmatter) — NO full SKILL.md or references/*.md prose drafted."
    - "Inline `[OPEN: Phase 4 — ...]` markers added for the 5 research-blocked items per CONTEXT Deferred Ideas: Pipefy AI KB content-upload endpoint (DESIGN-14); Wrike AI Studio knowledge-ingestion API (DESIGN-15); Ziflow ReviewAI knowledge-ingestion API (DESIGN-16); Pipefy GraphQL pagination cursor field names (DESIGN-14); Ziflow read-after-create consistency window (DESIGN-16); Pipefy/Wrike 2026 rate-limit currency (DESIGN-14 + DESIGN-15)."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-14, 15, 16 platform skill decision contracts populated"
      contains: "platform-comparison matrix; 3 H3 subsections each with echo + 5-file references shape + native-AI matrix + API surface + sandbox pattern + native_ai_path flag + tier_claims_last_verified frontmatter; >= 5 inline [OPEN: Phase 4] markers for research-blocked items"
  key_links:
    - from: "## Platform skills section"
      to: "Stage 6 (DESIGN-22 cost estimate uses platform-<X> for schema introspection); Stage 7b (DESIGN-23 implementation prompt uses platform-<X> for shape); Stage 8a (DESIGN-24 provision-test-harness uses platform-<X> for sandbox tenant access); Stage 10 (DESIGN-26 push-native-ai-knowledge reads per-platform native-ai-inventory.md)"
      via: "Each platform skill exposes a contract surface that downstream stage skills consume"
      pattern: "see `## platform-<X>` from Stage <N> sections"
    - from: "Inline [OPEN: Phase 4 — ...] markers in platform sections"
      to: "Phase 4 OPEN-QUESTIONS register (per CHANGE-04 research-blocked Phase 7 native-AI ingestion paths)"
      via: "Closed list at end of DESIGN.md (added by Plan 02-10) enumerates every inline marker"
      pattern: "[OPEN: Phase 4 — <one-line summary>]"
---

<objective>
Populate `.planning/DESIGN.md` Platform skills section (DESIGN-14, 15, 16): platform-pipefy / platform-wrike / platform-ziflow decision contracts.

**Purpose:** Internalises the 3 platform skills with identical 5-file `references/` shape per DESIGN-14/15/16. Each skill exposes the contract surface that Stage 6 (cost), Stage 7b (implementation prompt), Stage 8a (test harness), and Stage 10 (native-AI push) consume. Per D-21, ONLY decision contracts — NO full SKILL.md or references/*.md prose drafted (that runs in v2.2 per CHANGELIST.md PLAT-01..03).

**Output:** `.planning/DESIGN.md` `## Platform skills` body populated with comparison matrix + 3 H3 subsections. 3 of 30 echo lines added (DESIGN-14, 15, 16). Running total: 16/30. >= 5 inline `[OPEN: Phase 4]` markers added for research-blocked items per CONTEXT Deferred Ideas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- This plan writes to ## Platform skills H2 region.
     Three H3 subsections (### platform-pipefy / -wrike / -ziflow) already anchored by Plan 02-01.
     Per D-21, decision-contract-only — NO full SKILL.md or 5-file references/*.md prose. -->

Platform-comparison matrix shape (D-19 — opens the section):

| Platform | Native-AI surface (2026) | API protocol | Sandbox access | `native_ai_path` default | Known research-blocked items |
|----------|--------------------------|--------------|-----------------|--------------------------|------------------------------|
| pipefy | AI Agents (KB + Skills + MCP + IDP + Web Search + BYO-LLM) | GraphQL | Sandbox tenant per client (sandbox-only enforcement per safety-rules) | `api` (HIGH-confidence on Behaviors/Skills; LOW-confidence on KB content-upload) | KB content-upload endpoint; GraphQL pagination cursor field names; 2026 rate-limit currency |
| wrike | Copilot + 16 MCP tools | REST | Sandbox space per client; `host` field from OAuth token persisted (NOT hardcoded `www.wrike.com`) | `api` (MEDIUM-confidence; depends on Copilot knowledge-ingestion API surface) | AI Studio knowledge-ingestion API; 2026 rate-limit currency |
| ziflow | ReviewAI Checklists Public Preview (Change Verification + Brand Standards Coming Soon) | REST | Sandbox project per client | `paste` (Checklists Public Preview is the documented path; copy-paste fallback default until knowledge-ingestion API ships) | ReviewAI knowledge-ingestion API; read-after-create consistency window |

Per-platform H3 subsection shape:

```markdown
### platform-pipefy

> **DESIGN-14:** `platform-pipefy/` skill structure — 5-file `references/` shape (`api-contract.md`, `native-ai-inventory.md`, `knowledge-ingestion.md`, `client-shape-gotchas.md`, `vocabulary.md`); 2026-grounded native-AI capability matrix; API surface for the gap; sandbox access pattern; `native_ai_path: api | paste | none` flag with confidence; `tier_claims_last_verified` frontmatter.

**Skill folder layout (5-file references/ shape per DESIGN-14):**

```text
dydx-delivery/skills/platform-pipefy/
├── SKILL.md
└── references/
    ├── api-contract.md           # GraphQL endpoints, paginate_all helper, rate-limit handling
    ├── native-ai-inventory.md    # AI Agents capability matrix (KB + Skills + MCP + IDP + Web Search + BYO-LLM)
    ├── knowledge-ingestion.md    # KB content-upload path (LOW-confidence — see [OPEN] below)
    ├── client-shape-gotchas.md   # per-client pipe shape variations, custom field naming patterns
    └── vocabulary.md             # Pipefy-specific terms (Pipe, Phase, Card, Behaviors, etc.)
```

**Native-AI capability matrix (2026-grounded per RESEARCH.md / FEATURES.md):**

| Capability | Available? | Surface | Confidence |
|------------|------------|---------|------------|
| Knowledge base | yes | Pipefy AI Agents → KB | HIGH (documented) |
| Skills | yes | AI Agents → Skills | HIGH (documented) |
| MCP integration | yes | AI Agents → MCP | HIGH (documented) |
| Intelligent Document Processing (IDP) | yes | AI Agents → IDP | HIGH |
| Web Search | yes | AI Agents → Web Search | HIGH |
| BYO-LLM | yes | AI Agents config | HIGH |
| KB content-upload via API | unknown | `[OPEN: Phase 4 — Pipefy AI KB content-upload endpoint not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` | LOW |

**API surface for the gap:**

- Protocol: GraphQL via Pipefy public API.
- Helper: `paginate_all(query, cursor_field)` for cursor pagination — wraps the standard GraphQL `pageInfo { endCursor, hasNextPage }` pattern.
- Cursor field names: `[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]`.
- Rate limit: `[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]`
- Auth: Bearer token from Pipefy app installation; sandbox token distinct from production token.

**Sandbox access pattern:**

Sandbox tenant per client; production tenant access OUT OF SCOPE per `## Out of Scope`. `client_state.yaml` (DESIGN-29) carries `pipefy_sandbox_pipe_id:` per client.

**`native_ai_path` flag (DESIGN-26 routing):**

`api` for Behaviors instructions + Skills config (HIGH-confidence — direct API documented). `paste` fallback for KB content-upload UNTIL the `[OPEN]` above is resolved (LOW-confidence on direct ingestion). Per-doc routing happens at Stage 10 push-native-ai-knowledge.

**Frontmatter contract:**

- `tier_claims_last_verified: <ISO date>` — when this skill's native-AI capability matrix was last verified against live Pipefy docs. v2.2 build sets initial value; refresh required when rate-limits / endpoints change (PLAT-01 includes a "verify capability matrix" step per ship).
- `platform: pipefy` (DESIGN-01 platform-gating activates `pipe_id`).

**Cross-references:** DESIGN-22 (Stage 6 cost estimate uses platform-pipefy for schema introspection); DESIGN-23 (Stage 7b implementation prompt uses Behaviors + KB upload list); DESIGN-24 (Stage 8a provision-test-harness reads `pipefy_sandbox_pipe_id` from `client_state.yaml`); DESIGN-26 (Stage 10 push-native-ai-knowledge branches on `native_ai_path`); AUDIT.md §AUDIT-04.1 (this skill addresses the v0.3.0 referenced-but-missing `platform-pipefy` finding).
```

Equivalent shape for **platform-wrike** (DESIGN-15) and **platform-ziflow** (DESIGN-16) — same 5-file references/ structure, native-AI matrix grounded in Copilot + 16 MCP tools / ReviewAI Checklists Public Preview respectively, with per-platform `[OPEN]` markers for the research-blocked items.

Per CONTEXT Deferred Ideas, the inline `[OPEN: Phase 4 — ...]` markers added by this plan (count: >= 5):
1. **DESIGN-14:** Pipefy AI KB content-upload endpoint (Phase 7 owner)
2. **DESIGN-14:** Pipefy GraphQL pagination cursor field names (Phase 2 owner)
3. **DESIGN-14:** Pipefy 2026 rate-limit currency (Phase 1/Phase 2 owner)
4. **DESIGN-15:** Wrike AI Studio knowledge-ingestion API (Phase 7 owner)
5. **DESIGN-15:** Wrike 2026 rate-limit currency (Phase 1/Phase 2 owner)
6. **DESIGN-16:** Ziflow ReviewAI knowledge-ingestion API (Phase 7 owner)
7. **DESIGN-16:** Ziflow read-after-create consistency window (Phase 2 owner)

That's 7 markers; satisfies >= 5 floor with margin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Platform skills` opening matrix + DESIGN-14 platform-pipefy subsection</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-03 complete)
    - .planning/REQUIREMENTS.md §"Platform skills (internalisation)" DESIGN-14 (verbatim sub-bullet — Pipefy AI Agents capabilities; paginate_all helper; sandbox; native_ai_path flag; tier_claims_last_verified)
    - .planning/research/FEATURES.md — read sections on Pipefy AI Agents (KB, Skills, MCP, IDP, Web Search, BYO-LLM) for 2026-grounded matrix
    - .planning/research/STACK.md — Pipefy GraphQL surface notes
    - .planning/research/PITFALLS.md — Pipefy CRIT/MOD/MIN entries (cursor pagination, rate limits)
    - .planning/AUDIT.md §AUDIT-04.1 (referenced-but-missing platform-pipefy finding)
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-21 (decision-contract-only); §"Open-questions handling" D-27, D-33 (inline marker syntax)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `## Platform skills` body in two parts:

    **(A) Section opening + 3-row platform-comparison matrix.** Under the existing `## Platform skills` H2, before the H3 subsections, add:
    1. One-paragraph framing: "The three platform skills (`platform-pipefy/`, `platform-wrike/`, `platform-ziflow/`) internalise per-platform knowledge that v0.3.0 referenced as missing artefacts (per AUDIT.md §AUDIT-04.1). Each carries an identical 5-file `references/` shape: `api-contract.md` + `native-ai-inventory.md` + `knowledge-ingestion.md` + `client-shape-gotchas.md` + `vocabulary.md`. The native-AI capability matrices are 2026-grounded per RESEARCH.md / FEATURES.md. Per D-21, this section ships decision contracts only — full SKILL.md and references/*.md prose authoring runs in v2.2 per CHANGELIST.md PLAT-01..03."
    2. Platform-comparison matrix (3 rows × 6 columns) per `<interfaces>` shape above.

    **(B) `### platform-pipefy` H3 subsection (DESIGN-14).** Replace the placeholder under existing `### platform-pipefy` H3 with:
    1. Echo line: `> **DESIGN-14:** \`platform-pipefy/\` skill structure — 5-file \`references/\` shape; 2026-grounded native-AI capability matrix; API surface for the gap; sandbox access pattern; \`native_ai_path: api | paste | none\` flag with confidence; \`tier_claims_last_verified\` frontmatter.`
    2. **Skill folder layout** (` ```text ` fence) showing the 5-file references/ structure per `<interfaces>` above.
    3. **Native-AI capability matrix** (7 rows × 4 columns: Capability / Available? / Surface / Confidence). Last row is the `[OPEN]` marker for KB content-upload.
    4. **API surface for the gap** subsection with bullets:
       - Protocol: GraphQL.
       - Helper: `paginate_all(query, cursor_field)` for cursor pagination.
       - Cursor field names: `[OPEN: Phase 4 — Pipefy GraphQL pagination cursor field names need verification against current 2026 schema per OPEN-01]`.
       - Rate limit: `[OPEN: Phase 4 — Pipefy 2026 rate-limit currency unverified; Phase 1/Phase 2 owner per CHANGE-04. Documented historic ceiling: ~5 req/sec per token.]`
       - Auth: Bearer token from Pipefy app installation; sandbox token distinct from production token.
    5. **Sandbox access pattern** subsection: Sandbox tenant per client; production OUT OF SCOPE per `## Out of Scope`. `client_state.yaml` (DESIGN-29) carries `pipefy_sandbox_pipe_id:` per client.
    6. **`native_ai_path` flag** subsection per `<interfaces>` above.
    7. **Frontmatter contract** subsection: `tier_claims_last_verified: <ISO date>` + `platform: pipefy`.
    8. **Cross-references** line citing DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 / AUDIT.md §AUDIT-04.1.

    **Implementing decisions:** D-19 (matrix-then-prose), D-21 (decision-contract-only), D-27 + D-33 (inline `[OPEN: Phase 4 — ...]` markers + sparing use), D-32 (`file:line` for AUDIT.md cite), D-34 (AUDIT.md grounding), D-35 (echo line per H3).
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-14:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-pipefy/' .planning/DESIGN.md &amp;&amp; grep -qF 'api-contract.md' .planning/DESIGN.md &amp;&amp; grep -qF 'native-ai-inventory.md' .planning/DESIGN.md &amp;&amp; grep -qF 'knowledge-ingestion.md' .planning/DESIGN.md &amp;&amp; grep -qF 'client-shape-gotchas.md' .planning/DESIGN.md &amp;&amp; grep -qF 'vocabulary.md' .planning/DESIGN.md &amp;&amp; grep -qF 'paginate_all' .planning/DESIGN.md &amp;&amp; grep -qF 'tier_claims_last_verified' .planning/DESIGN.md &amp;&amp; PIPEFY_OPEN=$(awk '/^### platform-pipefy/,/^### platform-wrike/' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4') &amp;&amp; [ "$PIPEFY_OPEN" -ge "3" ] &amp;&amp; PLATFORM_TABLE=$(awk '/^## Platform skills/,/^### platform-pipefy/' .planning/DESIGN.md | grep -cE '^\| pipefy \|') &amp;&amp; [ "$PLATFORM_TABLE" -ge "1" ] &amp;&amp; echo PIPEFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-14:\*\*' .planning/DESIGN.md` succeeds (DESIGN-14 echo line)
    - Platform-comparison matrix exists between `## Platform skills` and `### platform-pipefy`: `awk '/^## Platform skills/,/^### platform-pipefy/' .planning/DESIGN.md | grep -cE '^\| (pipefy|wrike|ziflow) \|'` returns >= 3 (3 platform rows)
    - Pipefy 5-file `references/` shape present: all of `api-contract.md`, `native-ai-inventory.md`, `knowledge-ingestion.md`, `client-shape-gotchas.md`, `vocabulary.md` referenced under `### platform-pipefy`
    - Native-AI capability matrix has >= 6 capability rows (KB + Skills + MCP + IDP + Web Search + BYO-LLM) under platform-pipefy
    - `grep -qF 'paginate_all' .planning/DESIGN.md` succeeds (DESIGN-14 helper named)
    - `grep -qF 'tier_claims_last_verified' .planning/DESIGN.md` succeeds (DESIGN-14 frontmatter contract)
    - Pipefy section carries >= 3 `[OPEN: Phase 4` markers (KB content-upload + cursor field names + rate-limit currency): `awk '/^### platform-pipefy/,/^### platform-wrike/' .planning/DESIGN.md | grep -cE '\[OPEN: Phase 4'` returns >= 3
    - Sandbox access pattern subsection mentions `pipefy_sandbox_pipe_id:` (forward link to DESIGN-29 client_state.yaml)
    - Cross-references line cites at least DESIGN-22, DESIGN-23, DESIGN-24, DESIGN-26, AUDIT.md §AUDIT-04
    - `grep -qF 'native_ai_path' .planning/DESIGN.md` succeeds (DESIGN-14 routing flag named)
  </acceptance_criteria>
  <done>
    `## Platform skills` opening matrix + framing prose populated. `### platform-pipefy` H3 subsection populated with all DESIGN-14 sub-bullet contracts (5-file shape; native-AI matrix; API surface with `[OPEN]` markers for cursor + rate-limit; sandbox; native_ai_path flag; tier_claims_last_verified frontmatter; cross-references).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `### platform-wrike` (DESIGN-15) + `### platform-ziflow` (DESIGN-16) subsections + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — pipefy populated)
    - .planning/REQUIREMENTS.md §"Platform skills" DESIGN-15 (Wrike Copilot + 16 MCP tools; REST; `host` field from OAuth token; sandbox; attach-doc-via-MCP) and DESIGN-16 (Ziflow ReviewAI Checklists Public Preview; REST; `wait_for_proof` helper; sandbox; Checklist generation primarily + copy-paste fallback)
    - .planning/research/FEATURES.md — Wrike Copilot + 16 MCP tools; Ziflow ReviewAI sections
    - .planning/research/PITFALLS.md — Wrike `host` field hardcode bug; Ziflow read-after-create consistency
    - .planning/AUDIT.md §AUDIT-04.1 (platform-wrike referenced-but-missing finding); §AUDIT-04 (any platform-ziflow references)
    - .planning/phases/02-design/02-CONTEXT.md §"Open-questions handling" D-27 (sparingly!)
    - .planning/phases/02-design/scripts/design-structure-check.sh — run at end to verify echo count = 16/30 and inline `[OPEN]` count >= 5
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — populate `### platform-wrike` and `### platform-ziflow` H3 subsections using the SAME shape as platform-pipefy (Task 1) but with platform-specific contracts:

    **(A) `### platform-wrike` (DESIGN-15)** — replace placeholder with:
    1. Echo line: `> **DESIGN-15:** \`platform-wrike/\` skill structure — 5-file \`references/\` shape; 2026-grounded native-AI capability matrix (Copilot + 16 MCP tools); API surface for the gap (REST; \`host\` from OAuth token persisted, NOT hardcoded \`www.wrike.com\`); sandbox access; knowledge-ingestion via attach-doc-via-MCP.`
    2. Skill folder layout (` ```text ` fence) — same 5-file shape but path `dydx-delivery/skills/platform-wrike/`.
    3. Native-AI capability matrix: rows for **Copilot** (yes; HIGH); **MCP tools** (yes — 16 tools available; HIGH); **Knowledge ingestion via attach-doc-via-MCP** (yes documented; MEDIUM-confidence — needs verification of attach mechanism details); **AI Studio knowledge-ingestion API** — `[OPEN: Phase 4 — Wrike AI Studio knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` (LOW-confidence).
    4. API surface for the gap:
       - Protocol: REST.
       - **CRITICAL:** `host` field MUST be persisted from OAuth token response (e.g., `https://app-us2.wrike.com/api/v4/...`). Hardcoding `www.wrike.com` is the v0.3.0 bug per RESEARCH.md / PITFALLS.md — clients on different Wrike data centers fail silently with 401.
       - Rate limit: `[OPEN: Phase 4 — Wrike 2026 rate-limit currency unverified per OPEN-01; Phase 1/Phase 2 owner per CHANGE-04. Documented historic: ~100 req/min per user.]`
       - Auth: OAuth token (refresh per standard); sandbox space distinct from production.
    5. Sandbox access: Sandbox space per client; production OUT OF SCOPE; `client_state.yaml` (DESIGN-29) carries `wrike_sandbox_space_id:` AND `wrike_host:` (per the `host` persistence rule above).
    6. `native_ai_path` flag: `api` for Copilot workflow narrative + 16 MCP tools (HIGH-confidence). `paste` fallback for AI Studio knowledge ingestion UNTIL `[OPEN]` resolves.
    7. Frontmatter contract: `tier_claims_last_verified: <ISO date>` + `platform: wrike` (activates `space_id` per DESIGN-01).
    8. Cross-references: DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 / DESIGN-29 (`wrike_host` field) / AUDIT.md §AUDIT-04.1.

    **(B) `### platform-ziflow` (DESIGN-16)** — replace placeholder with:
    1. Echo line: `> **DESIGN-16:** \`platform-ziflow/\` skill structure — 5-file \`references/\` shape; 2026-grounded native-AI capability matrix (ReviewAI Checklists Public Preview; Change Verification + Brand Standards Coming Soon); API surface for the gap (REST; \`wait_for_proof\` helper for eventual consistency); sandbox access; knowledge-ingestion path = Checklist generation primarily + copy-paste fallback.`
    2. Skill folder layout — same 5-file shape, path `dydx-delivery/skills/platform-ziflow/`.
    3. Native-AI capability matrix: rows for **Checklists Public Preview** (yes; HIGH); **Change Verification** (Coming Soon; MEDIUM); **Brand Standards** (Coming Soon; MEDIUM); **ReviewAI knowledge-ingestion API** — `[OPEN: Phase 4 — Ziflow ReviewAI knowledge-ingestion API not externally verified per OPEN-01 — Phase 7 owner per CHANGE-04]` (LOW-confidence).
    4. API surface for the gap:
       - Protocol: REST.
       - Helper: `wait_for_proof(proof_id, max_wait_s)` for eventual consistency on proof creation — polls `GET /proofs/{id}` until status reaches a stable state.
       - Read-after-create consistency window: `[OPEN: Phase 4 — Ziflow read-after-create consistency window unverified per OPEN-01; Phase 2 owner per CHANGE-04. Conservative default in helper: 30 second poll with 2s interval.]`
       - Auth: API key in header (per Ziflow standard); sandbox project distinct from production.
    5. Sandbox access: Sandbox project per client; `client_state.yaml` (DESIGN-29) carries `ziflow_sandbox_project_id:`.
    6. `native_ai_path` flag: `paste` (DEFAULT — Checklists Public Preview is the documented authoring path; copy-paste fallback for content the API does not support yet). `api` for checklist/criteria generation IF the resolution of the knowledge-ingestion `[OPEN]` enables it.
    7. Frontmatter contract: `tier_claims_last_verified: <ISO date>` + `platform: ziflow` (activates `project_id` per DESIGN-01).
    8. Cross-references: DESIGN-22 / DESIGN-23 / DESIGN-24 / DESIGN-26 / AUDIT.md §AUDIT-04.

    **(C) After both subsections, run the structural-check**: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: still exits 1 (echo count is at 16/30 after this plan). Failing assertion: assertion #4 (echo count). Inline `[OPEN]` count assertion (#6) should now PASS with margin (>= 7 markers across the 3 platform sections, plus the seed marker from Plan 02-01 = 8 total).

    **Implementing decisions:** D-19 (per-platform matrix), D-21 (decision-contract-only — no full SKILL.md or references/*.md prose), D-27 + D-33 (inline markers, sparingly used — only for genuinely research-blocked items), D-32 (`file:line` for cross-refs), D-34 (AUDIT.md grounding), D-35 (echo lines).
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-15:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-16:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-wrike/' .planning/DESIGN.md &amp;&amp; grep -qF 'platform-ziflow/' .planning/DESIGN.md &amp;&amp; grep -qF 'wrike_host' .planning/DESIGN.md &amp;&amp; grep -qF 'wait_for_proof' .planning/DESIGN.md &amp;&amp; grep -qF 'OAuth' .planning/DESIGN.md &amp;&amp; grep -qF 'Checklists Public Preview' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "16" ] &amp;&amp; OPEN_COUNT=$(grep -cE '\[OPEN: Phase 4' .planning/DESIGN.md) &amp;&amp; [ "$OPEN_COUNT" -ge "5" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo PLATFORM_SKILLS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-15:\*\*' .planning/DESIGN.md` succeeds (DESIGN-15 echo)
    - `grep -qE '^> \*\*DESIGN-16:\*\*' .planning/DESIGN.md` succeeds (DESIGN-16 echo)
    - Wrike 5-file references/ shape present under `### platform-wrike` (5 file names listed)
    - Ziflow 5-file references/ shape present under `### platform-ziflow` (5 file names listed)
    - `grep -qF 'wrike_host' .planning/DESIGN.md` succeeds (DESIGN-15 critical bug-prevention contract)
    - `grep -qF 'OAuth' .planning/DESIGN.md` succeeds (DESIGN-15 auth)
    - `grep -qF 'wait_for_proof' .planning/DESIGN.md` succeeds (DESIGN-16 helper)
    - `grep -qF 'Checklists Public Preview' .planning/DESIGN.md` succeeds (DESIGN-16 ReviewAI surface)
    - `grep -qF 'Change Verification' .planning/DESIGN.md` AND `grep -qF 'Brand Standards' .planning/DESIGN.md` succeed (DESIGN-16 Coming Soon capabilities)
    - Wrike section has >= 2 inline `[OPEN: Phase 4` markers (knowledge-ingestion + rate-limit)
    - Ziflow section has >= 2 inline `[OPEN: Phase 4` markers (knowledge-ingestion + read-after-create consistency)
    - Total inline `[OPEN: Phase 4` count across DESIGN.md: `grep -cE '\[OPEN: Phase 4' .planning/DESIGN.md` returns >= 8 (1 seed from Plan 02-01 + >= 3 pipefy + >= 2 wrike + >= 2 ziflow)
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `16` (DESIGN-01..16 echoes)
    - `bash design-structure-check.sh` exits with code `1` (mid-phase — echo count 16 < 30)
  </acceptance_criteria>
  <done>
    `### platform-wrike` and `### platform-ziflow` populated with full DESIGN-15/16 contracts (5-file shape, native-AI matrices grounded in Copilot+MCP / ReviewAI, API surface with `host`/`wait_for_proof` helpers, `[OPEN]` markers for the 4 research-blocked items, native_ai_path flags, frontmatter, cross-references). Echo count = 16/30. >= 8 inline `[OPEN]` markers across DESIGN.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native-AI capability matrices → 2026 platform reality | These claims are 2026-grounded but research-derived; if a capability shipped a breaking change after the research date, the matrix is stale. `tier_claims_last_verified` frontmatter exists exactly to surface this risk; v2.2 PLAT-01..03 build phases must verify before shipping each platform skill. |
| `[OPEN: Phase 4 — ...]` markers → Phase 4 OPEN-QUESTIONS register | Every inline marker must be enumerated in Plan 02-10's closed list. Marker text format must be parseable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Native-AI matrix claims a capability that does not actually exist on the 2026 platform | mitigate | Each capability row carries explicit Confidence column (HIGH / MEDIUM / LOW). LOW-confidence items are tagged with `[OPEN: Phase 4 — ...]` markers. `tier_claims_last_verified` frontmatter forces v2.2 build to re-verify. CONTEXT D-21 explicit: capability matrices are 2026-grounded per research at time of writing. |
| T-02-04-02 | Tampering | Wrike `host` hardcode bug-prevention contract is buried, downstream Plan 02-04 author for v2.2 misses it | mitigate | `host` field rule appears in 3 places per Task 2: API surface bullets (CRITICAL callout), `client_state.yaml` `wrike_host:` field reference, cross-references to DESIGN-29. Triple-mention ensures grep visibility. |
| T-02-04-03 | Tampering | Plan author writes full SKILL.md prose instead of decision contract (violates D-21) | mitigate | Acceptance criteria check for specific contract elements (5-file shape, capability matrix rows, helper names, frontmatter contract) — these match the decision-contract scope. Manual reviewer attestation in Verification step explicitly checks "no full SKILL.md prose drafted". |
| T-02-04-04 | Tampering | `[OPEN]` marker text format inconsistent across the 3 platforms (some say "per OPEN-01", some omit) — Plan 02-10 closed list builder cannot enumerate uniformly | mitigate | Action specifies exact marker template: `[OPEN: Phase 4 — <subject> not externally verified per OPEN-01 — Phase <N> owner per CHANGE-04]`. Acceptance criteria require >= 5 markers; Plan 02-10 walks `grep -cE '\[OPEN: Phase 4'` for the closed list — uniform `[OPEN: Phase 4 —` prefix is sufficient. |
| T-02-04-05 | Repudiation | Confidence column values not justified | mitigate | Action specifies Confidence values per row with rationale: HIGH = documented in 2026 research; MEDIUM = inferred from related docs; LOW = `[OPEN]` flagged. Reviewer can spot-check by reading the cited capability + comparing to research notes. |
| T-02-04-06 | Tampering | DESIGN-15 omits the OAuth `host` persistence rule (a v0.3.0 bug per research) and the v2.2 build re-introduces the bug | mitigate | The rule appears in API surface bullets as "**CRITICAL:** `host` field MUST be persisted from OAuth token response... Hardcoding `www.wrike.com` is the v0.3.0 bug per RESEARCH.md / PITFALLS.md — clients on different Wrike data centers fail silently with 401." Acceptance criteria assert `grep -qF 'wrike_host'`. |
</threat_model>

<verification>
End of Plan 02-04 success-state:
1. `## Platform skills` opens with framing paragraph + 3-row platform-comparison matrix
2. `### platform-pipefy`, `### platform-wrike`, `### platform-ziflow` H3 subsections populated with: echo line + 5-file references/ shape + native-AI matrix + API surface + sandbox pattern + native_ai_path flag + tier_claims_last_verified frontmatter + cross-references
3. >= 8 total inline `[OPEN: Phase 4]` markers in DESIGN.md (1 seed + 3 pipefy + 2 wrike + 2 ziflow minimum)
4. Echo count = 16/30
5. `bash design-structure-check.sh` exits 1 with assertion #4 (echo count) failing; assertion #6 (inline `[OPEN]` count >= 1) passes; assertion #9 (hand-off matrix) passes

Manual reviewer attestation:
- [ ] Per-platform native-AI matrix capability rows match 2026-grounded RESEARCH.md / FEATURES.md content (no fabricated capabilities)
- [ ] All Confidence column values are HIGH / MEDIUM / LOW with consistent application (HIGH = documented; MEDIUM = inferred; LOW = `[OPEN]`)
- [ ] No full SKILL.md or references/*.md prose drafted (D-21 decision-contract-only honoured)
- [ ] All `[OPEN]` markers cite OPEN-01 OR Phase number per CHANGE-04 (uniform format for Plan 02-10 closed-list builder)
- [ ] Wrike `host` persistence rule appears in DESIGN-15 (CRITICAL callout) AND in DESIGN-29 cross-reference (Task 2 final bullet)
</verification>

<success_criteria>
- [ ] Platform-comparison matrix (3 rows × 6 columns) opens `## Platform skills`
- [ ] DESIGN-14 / 15 / 16 echo lines + per-platform contracts populated
- [ ] >= 8 total inline `[OPEN: Phase 4]` markers across DESIGN.md (1 seed + per-platform research-blocked items)
- [ ] Echo count = 16/30
- [ ] Structural-check exits 1 (mid-phase) with assertion #4 (echo count) failing
- [ ] No content under `dydx-delivery/` modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-04-SUMMARY.md` documenting:
- Platform-comparison matrix: 3 platforms × 6 columns
- Per-platform contracts: 5-file references/ shape + native-AI matrix + API surface + sandbox + native_ai_path + frontmatter (each platform)
- Inline `[OPEN]` markers added: 7 (3 pipefy + 2 wrike + 2 ziflow); cumulative = 8 with seed
- Echo count: 16/30
- Pointer: "Plan 02-05 (Wave 5) populates Stage 1-3 skills next (DESIGN-17, 18, 19). Each adds 1 echo line; running total after 02-05 = 19/30."
</output>
