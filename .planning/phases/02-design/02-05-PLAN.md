---
plan_id: 02-05
phase: 02-design
plan: 05
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04]
requirements: [DESIGN-17, DESIGN-18, DESIGN-19]
files_modified:
  - .planning/phases/02-design/02-05-PLAN.md
  - .planning/DESIGN.md
  - .planning/phases/02-design/02-05-SUMMARY.md
autonomous: true
tags: [design, stage-skills, kickoff, discovery, sow]

must_haves:
  truths:
    - "DESIGN.md `## Stage 1: Kickoff capture` (DESIGN-17), `## Stage 2: Discovery refactor` (DESIGN-18), `## Stage 3: SOW refactor` (DESIGN-19) populated with decision contracts per D-20: purpose, inputs (frontmatter + upstream artefact paths), outputs, downstream consumer, status flag(s), hand-off message shape (verbatim from DESIGN-13 matrix per D-26), complexity tag, dependencies, key v2 design decisions."
    - "Each subsection opens with `> **DESIGN-NN:**` echo (D-35) — 3 echo lines added; running total 19/30."
    - "DESIGN-17 Stage 1 carries the dual artefact branching (discovery-ready vs draft SOW), Field Notes triage filter (`processed_at IS NULL`, never auto-merges per DESIGN-09), Miro paste fallback when API ingest unavailable, auto-classification with explicit 'unknown' markers."
    - "DESIGN-18 Stage 2 carries the consume-`01_kickoff_v*`-artefact mode + skip-when-draft-SOW-produced behaviour."
    - "DESIGN-19 Stage 3 carries the single-SOW-covering-platform-AND-integration scope."
  artifacts:
    - path: ".planning/DESIGN.md"
      provides: "DESIGN-17, 18, 19 stage skill decision contracts populated"
      contains: "3 H2 stage subsections each with echo + decision contract per D-20; hand-off messages restated verbatim per D-26"
  key_links:
    - from: "DESIGN-17 Stage 1 hand-off message"
      to: "Hand-off matrix row 'Stage 1 → Stage 2' in `## Stage-by-stage hand-off contract`"
      via: "Verbatim hand-off message string (per D-26)"
      pattern: "exact-string match between matrix cell and per-stage subsection"
    - from: "DESIGN-17 dual-branch routing"
      to: "DESIGN-18 Stage 2 skip behaviour"
      via: "`kickoff_branch:` frontmatter field"
      pattern: "kickoff_branch: (discovery-ready|draft-sow)"
---

<objective>
Populate `.planning/DESIGN.md` Stage 1 (DESIGN-17), Stage 2 (DESIGN-18), Stage 3 (DESIGN-19) decision contracts.

**Purpose:** Stage 1 introduces the kickoff capture skill (NEW). Stage 2 refactors discovery-intake. Stage 3 SOW refactors to single-spec covering platform AND integration. Per D-20, decision-contract-only.

**Output:** `.planning/DESIGN.md` Stages 1, 2, 3 sections populated. 3 of 30 echo lines added. Running total: 19/30.

**Forward-reference guardrails (per cross-AI review MEDIUM #6):** Cross-references to DESIGN-20 (Stage 4 split), DESIGN-22 (Stage 6), DESIGN-23 (Stage 7), DESIGN-24 (Stage 8), DESIGN-26 (Stage 10) etc. from this plan's content are FORWARD references — those sections do not exist yet at end of Wave 5. Cross-references cite the DESIGN-NN ID only as anchor placeholders; acceptance criteria in this plan do NOT assert that the cross-referenced body content exists. Plan 02-10 (synthesis) verifies Appendix B traceability covers all 30 IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/research/SUMMARY.md
@.planning/research/FEATURES.md
@.planning/phases/02-design/02-CONTEXT.md
@.planning/phases/02-design/02-01-PLAN.md
@.planning/phases/02-design/02-02-PLAN.md
@.planning/phases/02-design/02-03-PLAN.md
@.planning/phases/02-design/02-04-PLAN.md
@.planning/DESIGN.md

<interfaces>
<!-- Per-stage subsection shape (D-20 decision-contract; D-26 hand-off message restated verbatim from matrix).

     Section-range extraction in acceptance criteria uses awk ranges with DISTINCT start/end anchors
     (e.g., `awk '/^## Stage 1:/,/^## Stage 2:/'`) — these are SAFE because Stage 1 ≠ Stage 2 on the
     same line. The broken `awk '/^## X/,/^## /'` pattern (cross-AI review HIGH #1) is forbidden.

     Forward-reference guardrails (cross-AI review MEDIUM #6): References to DESIGN-20/22/23/24/26
     are anchor placeholders only. Acceptance criteria do not assert their body content. -->

```markdown
## Stage 1: Kickoff capture

> **DESIGN-17:** Stage 1 Kickoff capture — inputs (meeting notes / client requirements / internal feedback / Miro workflow map); dual artefact branching (discovery-ready vs draft SOW); Field Notes triage from Coda brain doc (defaults to `processed_at IS NULL`, never auto-merges per DESIGN-09); Miro paste fallback when API ingest unavailable; auto-classification into kickoff template sections with explicit "unknown" markers.

**Skill:** `kickoff-capture/` (NEW per DESIGN-12 inventory).
**Stage:** 1 (file prefix `01_kickoff_*` per DESIGN-02).
**Complexity:** Medium.

**Inputs.**
- **Frontmatter consumed:** `client:` + `project:` + (optional) `frontmatter_version: 2`. New artefact (no `based_on_*` upstream).
- **Upstream artefact paths:** N/A (Stage 1 is the entry point).
- **External inputs:** meeting notes (paste), client requirements docs, internal feedback, Miro workflow map (paste — see fallback below); Field Notes from `<Client> Brain` Coda doc (read-only via Coda MCP per AUDIT.md §AUDIT-08).

**Outputs.**
- **Carrier file:** `01_kickoff_v<N>.md` in `<Client> Brain/<Project>/`.
- **Frontmatter set:** `client:`, `project:`, `frontmatter_version: 2`, `kickoff_branch: discovery-ready | draft-sow`, `field_notes_processed_count: <N>`, `status: draft`.
- **Branch routing (DESIGN-17 dual-branch contract):**
  - `kickoff_branch: discovery-ready` → Stage 2 (Discovery) consumes.
  - `kickoff_branch: draft-sow` → Stage 2 SKIPS; Stage 3 (SOW) consumes directly.

**Downstream consumer.** discovery-intake (Stage 2) IF `kickoff_branch: discovery-ready`; OR generate-sow (Stage 3) IF `kickoff_branch: draft-sow` (skips Stage 2).

**Status flag(s).** `status: approved` on `01_kickoff_v<N>.md` gates either downstream stage.

**Hand-off message (verbatim from DESIGN-13 matrix Stage 1 → Stage 2 row).**

> Awaiting `status: approved` write to `01_kickoff_v<N>.md`. Branch routing on `kickoff_branch:` value (`discovery-ready` → Stage 2; `draft-sow` → SKIP Stage 2 → Stage 3).

**Key v2 decisions for this stage.**

1. **Dual-branch artefact.**
2. **Field Notes triage filter** — `processed_at IS NULL` (per DESIGN-09 directional boundary).
3. **Miro paste fallback** (per DESIGN-07 connector probe — Miro MCP currently MISSING per AUDIT.md §AUDIT-08).
4. **Auto-classification with `[unknown — needs human classification]` inline markers**.

**Dependencies.** DESIGN-09 (Field Notes never auto-merged); DESIGN-07 (Miro probe — paste fallback); DESIGN-01 (frontmatter scheme).

**Cross-references.** DESIGN-18 (forward — Stage 2 reads `kickoff_branch:` to skip when `draft-sow`); DESIGN-19 (forward — Stage 3 reads same field); AUDIT.md §AUDIT-08; AUDIT.md §AUDIT-01.1.
```

Same shape applies to Stage 2 + Stage 3 with their REQUIREMENTS.md sub-bullet contents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `## Stage 1: Kickoff capture` (DESIGN-17) decision contract</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state — Plans 02-01..02-04 complete)
    - .planning/REQUIREMENTS.md §"DESIGN-17"
    - .planning/AUDIT.md §AUDIT-01.1 + §AUDIT-08
    - .planning/research/FEATURES.md (Stage 1 dual-branch + Field Notes triage)
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-20; §"Hand-off contract presentation" D-26
    - .planning/DESIGN.md hand-off matrix Stage 1 → Stage 2 row (already written by Plan 02-03 — copy verbatim)
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — replace placeholder body of `## Stage 1: Kickoff capture` H2 with the full DESIGN-17 decision contract per the per-stage shape in `<interfaces>` above.

    Required elements (in order):
    1. Echo line: `> **DESIGN-17:** Stage 1 Kickoff capture — inputs (meeting notes / client requirements / internal feedback / Miro workflow map); dual artefact branching (discovery-ready vs draft SOW); Field Notes triage from Coda brain doc (defaults to \`processed_at IS NULL\`, never auto-merges per DESIGN-09); Miro paste fallback when API ingest unavailable; auto-classification into kickoff template sections with explicit "unknown" markers.`
    2. Skill / Stage / Complexity lines.
    3. Inputs subsection.
    4. Outputs subsection (carrier + frontmatter + branch routing rules).
    5. Downstream consumer subsection.
    6. Status flag(s) subsection.
    7. Hand-off message subsection — copy VERBATIM from the Stage 1 → Stage 2 row of the hand-off matrix (Plan 02-03).
    8. Key v2 decisions: 4 numbered points.
    9. Dependencies subsection.
    10. Cross-references subsection (cite DESIGN-18, DESIGN-19 as forward references; AUDIT.md §AUDIT-08, §AUDIT-01.1 as backward references — actually populated).

    **Implementing decisions:** D-20, D-26, D-32, D-34, D-35.

    **No new `[OPEN]` markers expected** — DESIGN-17 contracts are LOCKED.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-17:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'kickoff_branch:' .planning/DESIGN.md &amp;&amp; grep -qF 'discovery-ready' .planning/DESIGN.md &amp;&amp; grep -qF 'draft-sow' .planning/DESIGN.md &amp;&amp; grep -qF 'processed_at IS NULL' .planning/DESIGN.md &amp;&amp; grep -qF '01_kickoff_v' .planning/DESIGN.md &amp;&amp; grep -qF '[unknown' .planning/DESIGN.md &amp;&amp; STAGE1_BODY=$(awk '/^## Stage 1: Kickoff capture/,/^## Stage 2:/' .planning/DESIGN.md) &amp;&amp; STAGE1_LINES=$(printf '%s\n' "$STAGE1_BODY" | wc -l) &amp;&amp; [ "$STAGE1_LINES" -ge "30" ] &amp;&amp; echo STAGE1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-17:\*\*' .planning/DESIGN.md` succeeds
    - `grep -qF 'kickoff_branch:' .planning/DESIGN.md` succeeds
    - `grep -qF 'discovery-ready' .planning/DESIGN.md` AND `grep -qF 'draft-sow' .planning/DESIGN.md` succeed
    - `grep -qF 'processed_at IS NULL' .planning/DESIGN.md` succeeds (Field Notes triage filter)
    - `grep -qF '01_kickoff_v' .planning/DESIGN.md` succeeds
    - `grep -qF '[unknown' .planning/DESIGN.md` succeeds
    - `grep -qF 'Miro paste fallback' .planning/DESIGN.md` succeeds
    - **Stage 1 subsection length >= 30 lines** (SAFE awk range — distinct start/end anchors): `awk '/^## Stage 1: Kickoff capture/,/^## Stage 2:/' .planning/DESIGN.md | wc -l` >= 30
    - Hand-off message subsection contains verbatim string from hand-off matrix Stage 1 → Stage 2 row
    - Cross-references subsection cites at least DESIGN-18 + DESIGN-19 (forward) + AUDIT.md §AUDIT-08 + AUDIT.md §AUDIT-01 (backward references — actually populated)
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** Acceptance criteria do NOT assert that DESIGN-18 or DESIGN-19 body content exists in DESIGN.md at end of this task — only that the cross-references cite the IDs. Stage 2 and Stage 3 bodies populated by Task 2.
  </acceptance_criteria>
  <done>
    Stage 1 Kickoff capture decision contract written. No full SKILL.md prose; no template draft.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `## Stage 2: Discovery refactor` (DESIGN-18) + `## Stage 3: SOW refactor` (DESIGN-19) decision contracts + run structural-check</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - .planning/DESIGN.md (current state after Task 1 — Stage 1 populated)
    - .planning/REQUIREMENTS.md §"DESIGN-18" + §"DESIGN-19"
    - .planning/AUDIT.md §AUDIT-01.1 + §AUDIT-01.2
    - .planning/DESIGN.md hand-off matrix Stage 2 → Stage 3 + Stage 3 → Stage 4a rows (copy verbatim)
    - .planning/phases/02-design/02-CONTEXT.md §"Per-stage skill depth" D-20
    - .planning/phases/02-design/scripts/design-structure-check.sh
  </read_first>
  <action>
    Edit `.planning/DESIGN.md` — replace placeholder bodies of `## Stage 2: Discovery refactor` and `## Stage 3: SOW refactor` H2s.

    **(A) `## Stage 2: Discovery refactor` (DESIGN-18)** — same per-stage subsection shape as Task 1:
    1. Echo line: `> **DESIGN-18:** Stage 2 Discovery intake refactor — consume \`01_kickoff_v*\` artefact (skip raw-notes mode); skip entire stage when kickoff produced a draft SOW; same template structure as v0.3.0 otherwise.`
    2. Skill / Stage / Complexity: `discovery-intake/` (MODIFIED) / Stage 2 / Low (skip mode adds branch).
    3. **Inputs:** Frontmatter consumed: `based_on_kickoff: 01_kickoff_v<N>` + `client:` + `project:` + `frontmatter_version: 2`. Upstream artefact: `01_kickoff_v<N>.md`. External inputs: none (raw-notes mode RETIRED).
    4. **Outputs:** `02_discovery_v<N>.md`. Frontmatter: `status: draft → client_review → approved` per DESIGN-01.
    5. **Skip behaviour:** IF `01_kickoff_v<N>.md` carries `kickoff_branch: draft-sow`, this stage SKIPS entirely.
    6. **Downstream consumer:** generate-sow (Stage 3).
    7. **Status flag:** `status: approved`.
    8. **Hand-off message** — verbatim from matrix Stage 2 → Stage 3 row.
    9. **Key v2 decisions:** raw-notes mode RETIRED; kickoff-as-input forces explicit `based_on_kickoff:`; skip-when-draft-SOW reads `kickoff_branch:`.
    10. **Dependencies:** DESIGN-17 (forward — Plan 02-05 Task 1 just populated); DESIGN-01.
    11. **Cross-references:** DESIGN-17, DESIGN-19 (forward — populated next); AUDIT.md §AUDIT-01.1.

    **(B) `## Stage 3: SOW refactor` (DESIGN-19)** — same shape:
    1. Echo line: `> **DESIGN-19:** Stage 3 SOW refactor — single SOW covering platform AND integration; status lifecycle locked to canonical scheme (DESIGN-01); structurally unchanged from v0.3.0 otherwise.`
    2. Skill / Stage / Complexity: `generate-sow/` (UNCHANGED-structure / behaviour-modified) / Stage 3 / Medium.
    3. **Inputs:** Frontmatter: `based_on_discovery: 02_discovery_v<N>` (normal) OR `based_on_kickoff: 01_kickoff_v<N>` (draft-SOW path).
    4. **Outputs:** `03_sow_v<N>.md`. Frontmatter: `client_review` retained per AUDIT.md §AUDIT-01.2 + DESIGN-08.
    5. **Single-spec scope:** ONE SOW covers BOTH platform and integration. Stage 4 split downstream (per DESIGN-20 — forward reference).
    6. **Downstream consumer:** generate-fnspec-platform (Stage 4a — forward); generate-fnspec-integration (Stage 4b — forward).
    7. **Status flag:** `status: approved` (canonical) OR `status: client_review` (per DESIGN-08).
    8. **Hand-off message** — verbatim from matrix Stage 3 → Stage 4a row.
    9. **Key v2 decisions:** canonical 4-stage lifecycle; `client_review` retained; single SOW covers BOTH; structurally unchanged from v0.3.0.
    10. **Dependencies:** DESIGN-17/18; DESIGN-01; DESIGN-08.
    11. **Cross-references:** DESIGN-18, DESIGN-20 (forward — Stage 4 split downstream); AUDIT.md §AUDIT-01.2.

    **(C) Run structural-check**: `bash .planning/phases/02-design/scripts/design-structure-check.sh; echo "EXIT_CODE=$?"`. Expected: exits 1 (echo count = 19/30).

    **Implementing decisions:** D-20, D-26, D-34, D-35.
  </action>
  <verify>
    <automated>grep -qE '^> \*\*DESIGN-18:\*\*' .planning/DESIGN.md &amp;&amp; grep -qE '^> \*\*DESIGN-19:\*\*' .planning/DESIGN.md &amp;&amp; grep -qF 'based_on_kickoff:' .planning/DESIGN.md &amp;&amp; grep -qF 'based_on_discovery:' .planning/DESIGN.md &amp;&amp; grep -qF 'raw-notes mode RETIRED' .planning/DESIGN.md &amp;&amp; grep -qF '02_discovery_v' .planning/DESIGN.md &amp;&amp; grep -qF '03_sow_v' .planning/DESIGN.md &amp;&amp; grep -qF 'single SOW' .planning/DESIGN.md &amp;&amp; ECHO_COUNT=$(grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md) &amp;&amp; [ "$ECHO_COUNT" = "19" ] &amp;&amp; bash .planning/phases/02-design/scripts/design-structure-check.sh 2>&amp;1; CODE=$?; echo "STRUCTURAL_CHECK_EXIT=$CODE"; [ "$CODE" = "1" ] &amp;&amp; echo STAGES_2_3_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^> \*\*DESIGN-18:\*\*' .planning/DESIGN.md` AND `grep -qE '^> \*\*DESIGN-19:\*\*' .planning/DESIGN.md` succeed
    - `grep -qF 'based_on_kickoff:' .planning/DESIGN.md` succeeds
    - `grep -qF 'based_on_discovery:' .planning/DESIGN.md` succeeds
    - `grep -qF 'raw-notes mode RETIRED' .planning/DESIGN.md` succeeds (DESIGN-18 contract)
    - `grep -qF '02_discovery_v' .planning/DESIGN.md` succeeds
    - `grep -qF '03_sow_v' .planning/DESIGN.md` succeeds
    - `grep -qF 'single SOW' .planning/DESIGN.md` succeeds
    - `grep -qF 'client_review retained' .planning/DESIGN.md` OR `grep -qF '`client_review` retained' .planning/DESIGN.md` succeeds
    - Hand-off message subsections contain verbatim Stage 2 → Stage 3 and Stage 3 → Stage 4a strings from the matrix
    - `grep -cE '^> \*\*DESIGN-[0-9]{2}:\*\*' .planning/DESIGN.md` returns exactly `19`
    - `bash design-structure-check.sh` exits with code `1` (mid-phase)
    - No new `[OPEN]` markers added
    - **Forward-reference guardrails (cross-AI review MEDIUM #6):** DESIGN-20 (forward — Stage 4 split) cited as anchor in Stage 3 cross-references; acceptance criteria do NOT assert DESIGN-20 body content exists. Plan 02-06 populates DESIGN-20.
  </acceptance_criteria>
  <done>
    Stage 2 + Stage 3 decision contracts populated. Echo count = 19/30. Forward references handled per cross-AI review MEDIUM #6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-stage hand-off message → matrix Stage X → Stage Y row | Per D-26, verbatim restate. |
| Stage 2 skip behaviour → Stage 1 `kickoff_branch:` value | Undefined value → undefined-skip. |
| Forward references to DESIGN-20 (Stage 4 split) — populated in Plan 02-06 (cross-AI review MEDIUM #6) | Cross-references cite the ID only; body verification deferred to Plan 02-10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Hand-off message in Stage 1 subsection drifts from matrix Stage 1 → Stage 2 cell | mitigate | Acceptance criteria require literal-substring match. |
| T-02-05-02 | Tampering | DESIGN-17 lists Field Notes triage filter as `processed_at = null` (SQL-style) | mitigate | Acceptance criteria assert `grep -qF 'processed_at IS NULL'`. |
| T-02-05-03 | Tampering | DESIGN-19 omits `client_review` retention rationale | mitigate | Acceptance criteria assert `grep -qF 'client_review retained'`. |
| T-02-05-04 | Tampering | Plan author drafts full SKILL.md prose | mitigate | Acceptance criteria check for specific contract elements. |
| T-02-05-05 | Tampering | DESIGN-17 silently introduces an `[OPEN]` marker for the Miro swimlane-reconstruction algorithm | mitigate | Action explicitly states "no new `[OPEN]` markers expected". |
| T-02-05-06 | Tampering | Acceptance criteria assert content from forward-referenced DESIGN-20 (Stage 4 split) that doesn't exist yet — would create false-fail at end of Wave 5 | mitigate | Cross-AI review MEDIUM #6 fix: forward references to DESIGN-20 are anchor placeholders only; body verification deferred to Plan 02-10. |
</threat_model>

<verification>
End of Plan 02-05 success-state:
1. Stage 1, 2, 3 H2 sections populated with full decision contracts per D-20
2. Each section opens with `> **DESIGN-NN:**` echo line per D-35
3. Each section's hand-off message subsection restates the matrix row verbatim per D-26
4. Echo count = 19/30
5. `bash design-structure-check.sh` exits 1 with assertion #4 (echo count, found 19) failing
6. Inline `[OPEN]` count unchanged from Plan 02-04
7. Forward references to DESIGN-20 cited as anchor placeholders only

Manual reviewer attestation:
- [ ] Stage 1 dual-branch routing is concrete (`kickoff_branch:` enum + per-value behaviour)
- [ ] Stage 2 skip behaviour reads `kickoff_branch:`
- [ ] Stage 3 single-SOW scope is explicit
- [ ] Hand-off messages literal-match matrix cells from Plan 02-03
- [ ] No full SKILL.md prose drafted
- [ ] Forward references handled as anchor placeholders only
</verification>

<success_criteria>
- [ ] DESIGN-17 / 18 / 19 echo lines + decision contracts populated
- [ ] Echo count = 19/30
- [ ] Hand-off messages restated verbatim from matrix
- [ ] No new `[OPEN]` markers
- [ ] Structural-check exits 1 (mid-phase)
- [ ] No content under `dydx-delivery/` modified
- [ ] Forward-reference guardrails applied (cross-AI review MEDIUM #6)
</success_criteria>

<output>
After completion, create `.planning/phases/02-design/02-05-SUMMARY.md` documenting:
- Stage 1 dual-branch contract + Field Notes triage filter + Miro paste fallback + auto-classification markers
- Stage 2 skip behaviour + raw-notes mode RETIRED
- Stage 3 single-SOW scope + `client_review` retention
- Echo count: 19/30
- **Cross-AI review fixes applied:** HIGH #2 (added SUMMARY.md to `files_modified`); MEDIUM #6 (forward-reference guardrails — DESIGN-20 cited as anchor placeholder only).
- Pointer: "Plan 02-06 (Wave 6) populates Stage 4a/4b + Stage 5 next (DESIGN-20, 21)."
</output>

<revision_log>
- 2026-05-09 reviews-mode revision: addressed Codex HIGH #2 (added `.planning/phases/02-design/02-05-SUMMARY.md` to `files_modified`); MEDIUM #6 (forward-reference guardrails — added explicit `<objective>` callout that cross-references to DESIGN-20 from Stages 1/2/3 are forward references; acceptance criteria do not assert their body content; T-02-05-06 added to threat register). Existing safe `awk '/^## Stage 1:/,/^## Stage 2:/'` patterns kept (distinct start/end anchors — not the broken `/^## X/,/^## /` pattern). No `???` artefacts found in original plan (LOW #9 N/A here). Coverage invariants preserved: DESIGN-17/18/19 still in `requirements`; D-20/26/34/35 still referenced; no `dydx-delivery/` modifications.
</revision_log>
