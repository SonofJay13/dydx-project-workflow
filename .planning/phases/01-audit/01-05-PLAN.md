---
phase: 01-audit
plan: 05
type: execute
wave: 5
depends_on: ["01-audit/04"]
files_modified:
  - .planning/AUDIT.md
autonomous: true
requirements: [AUDIT-05]
tags: [audit, duplicated-content, design-only]

must_haves:
  truths:
    - "AUDIT-05 catalogues all 4 duplicate-content categories in v0.3.0: hard-rules block (4 mentions), start-at-any-point block (6 SKILL.md copies), Cowork-vs-Claude-Code positioning (3 copies), pipeline diagram (2 copies)."
    - "Each category names the canonical source plus the duplicate copies with `file:line` citations."
    - "Each category has a canonical-source recommendation per AUDIT-05 requirement."
    - "Stage-N label collision (2 templates labelled 'Stage 6') is also recorded as a [NEW] dup-label finding."
  artifacts:
    - path: ".planning/AUDIT.md"
      provides: "AUDIT-05 section populated with 5 duplicate-content subsections (4 from CONCERNS.md + 1 [NEW])."
      contains: "## AUDIT-05: Duplicated Content Blocks; ### 5.1 Hard-rules block; ### 5.2 Start-at-any-point; ### 5.3 Cowork-vs-Claude-Code; ### 5.4 Pipeline diagram; ### 5.5 [NEW] Stage-N label collision; **Canonical source:**; **Closes via:** DESIGN-"
  key_links:
    - from: "AUDIT-05 dup-block citations"
      to: "v0.3.0 source files (READMEs + SKILL.md + reference templates)"
      via: "compact file:line citations"
      pattern: "`dydx-delivery/(README\\.md|skills/.*/(SKILL|references/.*)\\.md):[0-9]+"
    - from: "AUDIT-05 'Canonical source:' recommendations"
      to: "DESIGN-03 (single SoT for hard rules); DESIGN-10 (persona); DESIGN-11 (plugin-level references/); DESIGN-12 (skill inventory)"
      via: "**Closes via:** trailer line"
      pattern: "\\*\\*Closes via:\\*\\* DESIGN-(03|10|11|12)"
---

<objective>
Populate AUDIT-05 with the 4 confirmed duplicate-content categories (hard-rules; start-at-any-point; Cowork-vs-Claude-Code; pipeline diagram) plus the [NEW] Stage-N label collision finding. Each category lists the locations with citations and names the canonical source per AUDIT-05 requirement.

**Purpose:** AUDIT-05 satisfies ROADMAP success criterion 4 (duplicated content with canonical-source recommendation per duplicate). RESEARCH.md §6 already enumerates each category — this plan transcribes them with the canonical-source decisions and DESIGN-* closures.

**Output:** AUDIT-05 section of `.planning/AUDIT.md` populated with 5 H3 subsections (4 confirmed dups + 1 [NEW]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit/01-CONTEXT.md
@.planning/phases/01-audit/01-RESEARCH.md
@.planning/phases/01-audit/01-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- AUDIT-05 input data is FULLY pre-built in RESEARCH.md §6 (lines 327-396). -->
<!-- The executor TRANSCRIBES; do not re-grep the codebase. -->

The 5 duplicate-content subsections (per RESEARCH.md §6):

- `### 5.1 Hard-rules block` — RESEARCH.md §6.1 — 4 mentions (1 canonical + 3 duplicates) — Closes via DESIGN-03; MOD-16
- `### 5.2 Start-at-any-point triage block` — RESEARCH.md §6.2 — 6 SKILL.md copies, no canonical today — Closes via DESIGN-11 (plugin-level references/) → FOUND-02
- `### 5.3 Cowork-vs-Claude-Code positioning` — RESEARCH.md §6.3 — 3 copies, canonical = `when-to-open-claude-code.md` — Closes via DESIGN-10
- `### 5.4 Pipeline diagram` — RESEARCH.md §6.4 — 2 copies, two distinct purposes (high-level vs swimlane) — Closes via DESIGN-12
- `### 5.5 [NEW] Stage-N label collision (2 templates labelled "Stage 6")` — RESEARCH.md §6.5 — flagged as [NEW] dup-label — Closes via DESIGN-02

Per RESEARCH.md §12 Open Q6: hard-rules duplication framing resolves to "4 mentions; 3 are duplicates of 1 canonical" (correcting CONCERNS.md's "three places" wording).

Compact citation form per D-14: `` `dydx-delivery/skills/execute-tests/references/safety-rules.md` `` (file-only when whole file is the subject) or `` `dydx-delivery/README.md:99-105` `` (range).

D-13 banned-phrase ERE used by all gates: `'we should|the design will|recommend that v2|propose|v2 will'` (case-insensitive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUDIT-05 section opener + subsections 5.1–5.4 (CONCERNS.md confirmed duplicates)</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §6 (lines 327-396) — every duplicate enumerated with file:line
    - .planning/codebase/CONCERNS.md (lines 121-150) — original CONCERNS.md duplicate-content section
    - .planning/REQUIREMENTS.md AUDIT-05 (line 22); DESIGN-03, DESIGN-10, DESIGN-11, DESIGN-12
    - .planning/phases/01-audit/01-PATTERNS.md §"Pattern 6 — Tables" + §"Pattern 5 — Cross-reference style"
    - .planning/phases/01-audit/01-CONTEXT.md D-13, D-14, D-15
  </read_first>
  <action>
    Replace the placeholder body under `## AUDIT-05: Duplicated Content Blocks` in `.planning/AUDIT.md`.

    **Section opener** (1 paragraph, 2-3 sentences): "Catalogues every block of duplicated content in v0.3.0 along with the canonical-source recommendation per AUDIT-05 requirement. The recommendation names where canonical content should live; v2 design moves (single-SoT consolidation, plugin-level references/) are tracked under DESIGN-* requirements per D-13."

    Then write 4 H3 subsections per `<interfaces>`. Each subsection has this exact shape:

    ```markdown
    ### 5.N <Topic>

    <One-line context — what is duplicated and what diverges between copies.>

    | Location | Lines | Role |
    |---|---|---|
    | `<canonical-path>` | <range or "full file"> | **CANONICAL** |
    | `<duplicate-path>` | <range> | DUPLICATE |
    | ... | ... | ... |

    **Canonical source:** `<path>` — <one-line rationale>.

    **Severity:** **[STRUCTURAL]** — <one-line>.

    **Closes via:** DESIGN-NN (one-line description). [+ optional Pitfall ref: MOD-X]

    ---
    ```

    **Per-subsection content** (transcribe from RESEARCH.md §6):

    **§ 5.1 Hard-rules block (4 mentions; 3 duplicates of 1 canonical)** — Context: "Sandbox/no-deletions/read-write-only/audit-trail rules appear in 4 surfaces. The 4 copies do NOT match exactly — canonical lists 10 numbered rules; the others list 5–7 condensed. Per RESEARCH.md §12 Open Q6, framing is '4 mentions; 3 are duplicates of 1 canonical' (correcting CONCERNS.md's 'three places' wording)."
    Citation table (4 rows; from RESEARCH.md §6.1):
    - `dydx-delivery/skills/execute-tests/references/safety-rules.md` — full file (1-101) — **CANONICAL** — 10 numbered rules
    - `dydx-delivery/README.md:99-105` — 7 lines — DUPLICATE — 5 condensed bullets ("Test execution — safety rules (hard)")
    - `dydx-delivery/skills/execute-tests/SKILL.md:21-31` — 11 lines — DUPLICATE — 7 numbered rules condensed
    - `dydx-delivery/skills/generate-test-plan/references/test-plan-template.md:36-44` — 9 lines — DUPLICATE — 5 condensed bullets
    Canonical source: `dydx-delivery/skills/execute-tests/references/safety-rules.md` — most complete (10 numbered rules), already documented as the canonical reference in `execute-tests/SKILL.md:23`.
    Severity: **[STRUCTURAL]** — wording mismatch across 4 surfaces creates rule-divergence risk.
    Closes via: DESIGN-03 (single SoT for hard rules at `dydx-delivery/references/safety-rules.md` — note v2 elevates to plugin-level references/). **Pitfall ref:** MOD-16 (hard-rules duplicate-and-edit).

    **§ 5.2 Start-at-any-point triage block (6 SKILL.md copies)** — Context: "Near-identical 3-option prompt ('(a) Paste / (b) Walk through inline / (c) Cancel') appears in 6 SKILL.md files with small wording variations. No canonical source today."
    Citation table (6 rows; from RESEARCH.md §6.2):
    - `dydx-delivery/skills/generate-sow/SKILL.md:27-33` — DUPLICATE
    - `dydx-delivery/skills/generate-functional-spec/SKILL.md:28-32` — DUPLICATE
    - `dydx-delivery/skills/generate-technical-spec/SKILL.md:28-32` — DUPLICATE
    - `dydx-delivery/skills/generate-test-plan/SKILL.md:30-34` — DUPLICATE
    - `dydx-delivery/skills/generate-build-prompt/SKILL.md:28-32` — DUPLICATE
    - `dydx-delivery/skills/execute-tests/SKILL.md:40-44` — DUPLICATE
    Canonical source: **None today.** Audit observation: needs a v2 canonical reference (e.g. plugin-level `references/start-at-any-point.md`); audit names the gap, design proposes the location.
    Severity: **[STRUCTURAL]** — wording variations create per-skill divergence.
    Closes via: DESIGN-11 (plugin-level `references/` directory) implies a place; specific consolidation is part of v2.1 build phase per FOUND-02.

    **§ 5.3 Cowork-vs-Claude-Code positioning (3 copies)** — Context: "Cowork-as-strategy-seat / Claude-Code-as-build-seat framing appears in 3 surfaces. The decision-matrix `when-to-open-claude-code.md` is canonical; the other two repeat the framing one-liner without the matrix."
    Citation table (3 rows):
    - `dydx-delivery/README.md:5,9-29` — DUPLICATE — Pipeline-diagram framing + one-liner
    - `dydx-delivery/skills/generate-build-prompt/SKILL.md:8,163-171` — DUPLICATE — One-liner at 8; "Iterating between Cowork and Claude Code" section at 163-171
    - `dydx-delivery/skills/generate-build-prompt/references/when-to-open-claude-code.md` — full file — **CANONICAL** decision matrix (but the framing one-liner is duplicated above)
    Canonical source: `dydx-delivery/skills/generate-build-prompt/references/when-to-open-claude-code.md` — keeps the canonical decision matrix; the plugin README and `generate-build-prompt` SKILL.md should inline a one-line summary plus pointer (consistent with hard-rules pattern).
    Severity: **[STRUCTURAL]** — same framing duplicated; wording could drift.
    Closes via: DESIGN-10 (persona contract) — likely subsumes the positioning text; specific consolidation is v2.1 build.

    **§ 5.4 Pipeline diagram (2 copies)** — Context: "Two ASCII pipeline diagrams: high-level overview (plugin README) and tool-transition swimlanes (when-to-open-claude-code.md). Two copies serve different purposes — could be argued they're not strict duplicates. Audit calls out per CONCERNS.md classification but recommends both retained."
    Citation table (2 rows):
    - `dydx-delivery/README.md:9-29` — Pipeline-overview ASCII + 7-skill table — **CANONICAL (high-level overview)**
    - `dydx-delivery/skills/generate-build-prompt/references/when-to-open-claude-code.md:18-40` — Swimlane variant illustrating tool transitions — **CANONICAL (tool-transition view, distinct purpose)**
    Canonical source: Both retained; the swimlane variant is marked as "tool-transition view" not "pipeline view" — different lens, not strict duplicate.
    Severity: **[STRUCTURAL]** — visual drift risk if maintained separately; observation logged for v2 consideration.
    Closes via: DESIGN-12 (skill inventory) implies a redrawn 13-skill diagram in plugin-level docs.

    **Implementing decisions:** D-13 (observation-led), D-14 (citation form), D-15 (severity tags), D-08 (CONCERNS preservation), Open Q6 ("4 mentions; 3 duplicates of 1 canonical").
  </action>
  <verify>
    <automated>[ "$(grep -cE '^### 5\.[1-4] ' .planning/AUDIT.md)" = "4" ] &amp;&amp; [ "$(awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -c '\*\*Canonical source:\*\*')" -ge "4" ] &amp;&amp; [ "$(awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -c '\*\*Closes via:\*\*')" -ge "4" ] &amp;&amp; awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -q 'DESIGN-03' &amp;&amp; awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -q 'safety-rules.md' &amp;&amp; echo SUBSECTIONS_5_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 4 H3 subsections under AUDIT-05 in this task: `grep -cE '^### 5\.[1-4] ' .planning/AUDIT.md` returns `4`
    - Each subsection has `**Canonical source:**` line: `awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -c '\*\*Canonical source:\*\*'` >= `4`
    - Each subsection has `**Closes via:**` trailer: `awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -c '\*\*Closes via:\*\*'` >= `4`
    - DESIGN-03, DESIGN-10, DESIGN-11, DESIGN-12 all referenced: `awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -cE 'DESIGN-(03|10|11|12)'` >= `4`
    - All 6 start-at-any-point copies cited (5.2): `awk '/^### 5\.2 /,/^---$/' .planning/AUDIT.md | grep -cE '\`dydx-delivery/skills/.*/SKILL\.md:[0-9]+'` >= `6`
    - Hard-rules canonical (5.1) named: `awk '/^### 5\.1 /,/^---$/' .planning/AUDIT.md | grep -q 'safety-rules.md'` succeeds
    - All 4 hard-rules locations cited (5.1): `awk '/^### 5\.1 /,/^---$/' .planning/AUDIT.md | grep -cE '\`dydx-delivery'` >= `4`
    - At least 4 [STRUCTURAL] tags total: `awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -c '\*\*\[STRUCTURAL\]\*\*'` >= `4`
    - No design-move language — canonical D-13 banned-phrase ERE: `awk '/^## AUDIT-05:/,/^## AUDIT-06:/' .planning/AUDIT.md | grep -ciE 'we should|the design will|recommend that v2|propose|v2 will'` returns `0`
  </acceptance_criteria>
  <done>
    AUDIT-05 has 4 confirmed duplicate-content subsections; each names canonical source + DESIGN-* closure; observation-led tone preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add subsection 5.5 — [NEW] Stage-N label collision finding</name>
  <files>.planning/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-audit/01-RESEARCH.md §6.5 (lines 386-395) — Stage-N label collision finding
    - .planning/codebase/CONCERNS.md (lines 59-76) — original Stage numbering inconsistencies (line 71 specifically: "Two distinct templates are both labelled 'Stage 6'")
    - .planning/REQUIREMENTS.md DESIGN-02 (line 34) — canonical stage-numbering scheme
    - .planning/phases/01-audit/01-CONTEXT.md D-10 — [NEW] tag for net-new findings
  </read_first>
  <action>
    Append `### 5.5 [NEW] Stage-N label collision (2 templates labelled "Stage 6")` under AUDIT-05 (after § 5.4's `---`).

    Per RESEARCH.md §6.5, this is a [NEW] dup-label finding — already covered by CONCERNS.md:71 under Stage-numbering inconsistencies but worth re-flagging in AUDIT-05 since it's a literal duplicate label-string.

    Subsection shape:

    ```markdown
    ### 5.5 [NEW] Stage-N label collision (2 templates labelled "Stage 6")

    Two distinct templates self-label as "Stage 6" — a literal duplicate label-string conflict.

    | Template | Line | Self-label |
    |---|---|---|
    | build-prompt-template.md | 15 | "Stage 6 of the dydx-delivery pipeline" |
    | results-template.md | 18 | "Stage 6 of the dydx-delivery pipeline" |

    Citations: `` `dydx-delivery/skills/generate-build-prompt/references/build-prompt-template.md:15` ``; `` `dydx-delivery/skills/execute-tests/references/results-template.md:18` ``.

    **[NEW]** **[STRUCTURAL]** — already covered by `.planning/codebase/CONCERNS.md:71` under Stage-numbering inconsistencies, re-flagged here as a literal duplicate label.

    **Closes via:** DESIGN-02 (canonical stage-numbering scheme — substages `4a/4b/7a/7b/8a-8d` and old→new mapping table).

    ---
    ```

    Note: this subsection has the [NEW] tag per D-10 + the [STRUCTURAL] severity tag (RESEARCH.md §4 Net-new finding example shows [NEW] precedes severity).

    **Implementing decisions:** D-10 (NEW tag), D-13 (observation-led), D-14 (citation form), D-15 (severity).
  </action>
  <verify>
    <automated>grep -qE '^### 5\.5 \[NEW\] Stage-N label collision' .planning/AUDIT.md &amp;&amp; awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q 'build-prompt-template' &amp;&amp; awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q 'results-template' &amp;&amp; awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q 'DESIGN-02' &amp;&amp; awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q '\*\*\[NEW\]\*\*' &amp;&amp; echo NEW_FINDING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^### 5\.5 \[NEW\] Stage-N label collision' .planning/AUDIT.md` succeeds
    - Subsection contains both template citations: `awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -cE '\`dydx-delivery/skills/.*/(build-prompt-template|results-template)\.md:[0-9]+'` >= `2`
    - [NEW] tag present: `awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q '\*\*\[NEW\]\*\*'` succeeds
    - [STRUCTURAL] tag present: `awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q '\*\*\[STRUCTURAL\]\*\*'` succeeds
    - DESIGN-02 closure: `awk '/^### 5\.5 /,/^---$/' .planning/AUDIT.md | grep -q 'DESIGN-02'` succeeds
    - AUDIT-05 now has 5 H3 subsections total: `grep -cE '^### 5\.[1-5] ' .planning/AUDIT.md` returns `5`
  </acceptance_criteria>
  <done>
    AUDIT-05 has 5 H3 subsections total (4 confirmed dups + 1 [NEW] label collision); structural-check still asserts 8 H2 sections.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RESEARCH.md §6 → AUDIT-05 prose | Executor transcribes pre-built duplicate enumeration. If executor mis-counts copies (e.g. claiming 5 hard-rules copies instead of 4), reviewers can't trust the canonical-source recommendation. |
| Canonical-source recommendation → architectural impact | Naming the wrong canonical (e.g. claiming `dydx-delivery/README.md` is canonical for hard-rules instead of `safety-rules.md`) misdirects DESIGN-03's Phase 2 work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Executor names wrong canonical (e.g. claiming README is canonical for hard-rules) | mitigate | RESEARCH.md §6.1 explicitly identifies `safety-rules.md` as canonical based on rule-count + existing `execute-tests/SKILL.md:23` reference. Acceptance criterion checks `safety-rules.md` appears in 5.1. |
| T-01-05-02 | Repudiation | Hard-rules duplicate count drifts (3 vs 4 framing per Open Q6) | mitigate | `<interfaces>` section explicitly resolves to "4 mentions; 3 duplicates of 1 canonical" per Open Q6. Acceptance criterion checks 4 location citations in 5.1. |
| T-01-05-03 | Information Disclosure | [NEW] tag missing on 5.5 (silently drops the label-collision-as-dup framing) | mitigate | Acceptance criterion mandates `\*\*\[NEW\]\*\*` present in subsection 5.5. |
| T-01-05-04 | Repudiation | Section drifts into design proposal (violates D-13) | mitigate | Acceptance criterion runs canonical D-13 banned-phrase ERE `grep -ciE 'we should\|the design will\|recommend that v2\|propose\|v2 will' = 0` — same pattern enforced in every content plan. |
</threat_model>

<verification>
End of Plan 05 success-state:
1. AUDIT-05 has 5 H3 subsections (`### 5.1` through `### 5.5`).
2. Each of 5.1-5.4 has `**Canonical source:**` + `**Closes via:** DESIGN-NN`.
3. 5.5 carries [NEW] + [STRUCTURAL] + Closes via DESIGN-02.
4. All 6 start-at-any-point copies cited; all 4 hard-rules locations cited.
5. No design-move language (canonical D-13 ERE returns 0).
6. 8-H2-section count holds.
</verification>

<success_criteria>
- [ ] AUDIT-05 has 5 H3 subsections (4 confirmed + 1 [NEW])
- [ ] Each confirmed-dup subsection names canonical source
- [ ] All citations preserved with compact `file:line` form
- [ ] DESIGN-03, DESIGN-10, DESIGN-11, DESIGN-12, DESIGN-02 referenced as appropriate
- [ ] Severity tags applied; [NEW] tag on 5.5
- [ ] No design-move language (canonical D-13 banned-phrase ERE)
- [ ] No content under `dydx-delivery/` modified
- [ ] Structural-check script still recognises 8 H2 sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-05-SUMMARY.md` documenting:
- Subsection count, citation count, canonical-source declarations
- Pointer: "Plan 06 (Wave 6) populates AUDIT-06 next"
</output>
