---
plan: 07-02
phase: 7
slug: stage-1-kickoff-discovery-sow-upstream-wiring
wave: 2
depends_on: [07-01]
files_modified:
  - dydx-delivery/skills/discovery-intake/SKILL.md
files_created: []
autonomous: true
requirements: [STG2-01, STG2-02, STG2-03]
must_haves:
  truths:
    - "`discovery-intake/SKILL.md` consumes approved `01_kickoff_v<N>.md` as sole upstream artefact"
    - "Raw-notes entry path is RETIRED — `## Start-at-any-point handling` block deleted; `## What this skill does not do` documents the retirement"
    - "`based_on_kickoff:` field is MANDATORY in every NEW `02_discovery_v<N>.md` write-path (lenient read-path per DESIGN-08 + D-76)"
    - "On `kickoff_branch: draft-sow`, skill emits verbatim `Stage 2 SKIPPED — kickoff branch = draft-sow` and writes NO artefact (per D-74 / R-01 authoritative spelling)"
    - "`intake-template.md` body is UNCHANGED per STG2-03 (9-section dimension list preserved; mismatch with kickoff 8-section is INTENTIONAL per R-04)"
    - "`bash phase7-structure-check.sh --section discovery` exits 0 (D1, D2, D3 all PASS)"
  artifacts:
    - path: "dydx-delivery/skills/discovery-intake/SKILL.md"
      provides: "Stage 2 skill body — consumes kickoff as sole upstream; raw-notes RETIRED; verbatim skip-message on draft-sow branch"
      contains: "`based_on_kickoff:` MANDATORY language, raw-notes RETIRED documentation, verbatim `Stage 2 SKIPPED — kickoff branch = draft-sow` skip-message, DESIGN-18 line 612 handoff"
  key_links:
    - from: "dydx-delivery/skills/discovery-intake/SKILL.md"
      to: "dydx-delivery/skills/kickoff-capture/references/kickoff-template.md (via `01_kickoff_v<N>.md` artefact)"
      via: "Inputs section MANDATORY upstream reference + Step 1 reads `kickoff_branch:` frontmatter"
      pattern: "01_kickoff_v|based_on_kickoff|kickoff_branch"
---

<objective>
Surgical modifications to `dydx-delivery/skills/discovery-intake/SKILL.md` per STG2-01 / STG2-02 / STG2-03 + D-74 / D-76 / R-01:

1. Make kickoff (`01_kickoff_v<N>.md`) the sole upstream input.
2. Retire the raw-notes entry path.
3. Add the `kickoff_branch:` routing read at Step 1 — on `draft-sow`, emit the verbatim skip-message and exit without writing.
4. Add `based_on_kickoff:` MANDATORY field to the write-path frontmatter block (lenient read for legacy v0.3.0 artefacts per DESIGN-08 + D-76).
5. Confirm `intake-template.md` body is structurally unchanged (no-op verification task per STG2-03).
6. Gate on `bash phase7-structure-check.sh --section discovery` (D1, D2, D3 PASS).

Purpose: Light up the kickoff → discovery upstream contract so when 07-01's kickoff artefact carries `kickoff_branch: discovery-ready`, this skill consumes it; when `draft-sow`, this skill cleanly emits the verbatim skip and routes to Stage 3.

Output: 1 modified SKILL.md file. No other files touched. Disjoint from 07-03 (`generate-sow/SKILL.md` is touched by 07-03 only) → 07-02 and 07-03 run in parallel under Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md
@dydx-delivery/skills/discovery-intake/SKILL.md
@dydx-delivery/skills/discovery-intake/references/intake-template.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-01-SUMMARY.md

<interfaces>
<!-- Contract surface this plan must satisfy. -->

Upstream artefact contract (produced by 07-01 — DESIGN-17 line 562):
```yaml
kickoff_branch: <discovery-ready | draft-sow>
```

Discovery write-path frontmatter MUST add (DESIGN-18 line 595-598 + D-76):
```yaml
based_on_kickoff: 01_kickoff_v<N>.md   # MANDATORY on writes from v2.2+
```

Verbatim skip-message (D-74 / STG2-02 / Roadmap criterion #3 — AUTHORITATIVE per R-01; DO NOT lift DESIGN-18 line 604 alt spelling):
```
Stage 2 SKIPPED — kickoff branch = draft-sow
```

Verbatim handoff (DESIGN-18 line 612):
```
Awaiting status: approved write to 02_discovery_v<N>.md before generate-sow runs.
```

Lenient-read contract (DESIGN-08 + D-76):
- WRITE path: `based_on_kickoff:` MANDATORY (this plan documents the rule; `validate-frontmatter` hook implementation deferred to v2.6).
- READ path: lenient — legacy v0.3.0 `00_discovery_v*.md` / `02_discovery_v*.md` artefacts without `based_on_kickoff:` still parse.
</interfaces>
</context>

<tasks>

<task id="07-02-T1" type="auto">
  <name>Task 1: Surgical edits to discovery-intake/SKILL.md — Inputs, Step 1 kickoff routing, Step 5 frontmatter</name>
  <files>dydx-delivery/skills/discovery-intake/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/discovery-intake/SKILL.md` (149 lines — current v0.3.0 body; surgical-edit target)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §4 (line-numbered surgical-edit landmarks)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §6 (per-section edit table for discovery-intake)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md` D-74 (skip-message verbatim) + D-76 (lenient-read contract)
    - `.planning/DESIGN.md` lines 587-623 (DESIGN-18 — Inputs + skip behaviour + handoff — but USE STG2-02 spelling per R-01, NOT line 604 drift)
  </read_first>
  <action>
    Apply 5 surgical edits to `dydx-delivery/skills/discovery-intake/SKILL.md`. Preserve the v0.3.0 body shape (`### Step N` numbering, `## Inputs` / `## Output` / `## How to run` / `## What this skill does not do` / `## Quality bar` sections) per CONVENTIONS.md. The `intake-template.md` body is NOT touched (STG2-03 — verified in T2).

    **Edit 1 — Frontmatter `description:` field (line ~3):**
    Replace current description with text that drops "pasted meeting notes / transcript / brief" trigger phrasing and adds "consume approved kickoff artefact `01_kickoff_v<N>.md`". Description currently advertises raw-notes intake — RETIRED per STG2-01.

    **Edit 2 — `## Inputs` block (lines ~10-13):**
    REPLACE bullets entirely. Drop the "Free-form context: chat, pasted meeting notes, transcript, brief, email thread" line. Add bullet:
    - "Approved `01_kickoff_v<N>.md` (sole upstream artefact, MANDATORY) — carries `status: approved` + `kickoff_branch:` enum (`discovery-ready` → run; `draft-sow` → skip per Step 1)."

    Add brief paragraph documenting `based_on_kickoff:` MANDATORY on write-path per D-76:
    > **Write-path contract (D-76):** Every NEW `02_discovery_v<N>.md` artefact MUST carry `based_on_kickoff: 01_kickoff_v<N>.md` in frontmatter. The `validate-frontmatter` hook (deferred to v2.6 / SURF-01..03) will enforce this on disk; until then the SKILL body is the contract source. Read-path stays lenient per DESIGN-08 — legacy v0.3.0 `00_discovery_v*.md` artefacts without `based_on_kickoff:` still parse.

    **Edit 3 — `### Step 1 — Establish target location` (lines ~23-29):**
    Insert a new sub-step at the top of Step 1 (or rename to "Step 1 — Locate upstream kickoff and route by branch"):

    > **Locate upstream kickoff.** Read the latest `status: approved` `01_kickoff_v<N>.md` for this client+project. If no approved kickoff exists, emit an explicit error directing the reviewer to run `kickoff-capture` first (raw-notes entry is RETIRED — see "What this skill does not do").
    >
    > **Read `kickoff_branch:` from kickoff frontmatter:**
    > - If `kickoff_branch: discovery-ready` → proceed to Step 2 (run discovery interview).
    > - If `kickoff_branch: draft-sow` → emit the following message verbatim to stdout / handoff log and EXIT WITHOUT WRITING any `02_discovery_v<N>.md` artefact:
    >
    > ```
    > Stage 2 SKIPPED — kickoff branch = draft-sow
    > ```
    >
    > Audit trail lives in git (kickoff approval commit) and the handoff log — no marker file is written (per D-74). Stage 3 (`generate-sow`) reads the kickoff directly via `based_on_kickoff:`.

    USE THE D-74 / STG2-02 SPELLING VERBATIM. DO NOT lift DESIGN-18 line 604's alternative spelling per R-01 (RESEARCH §11). Structure-check D3 asserts the exact string `Stage 2 SKIPPED — kickoff branch = draft-sow` via `grep -qF`.

    **Edit 4 — `### Step 5 — Draft the artefact` frontmatter block (lines ~97-107):**
    Add `based_on_kickoff: 01_kickoff_v<N>.md` as a MANDATORY field in the YAML frontmatter example. Keep `captured_by` / `captured_at` (STG2-03 body-unchanged — name reform is out of scope). The frontmatter block becomes:
    ```yaml
    ---
    client: <CLIENT_NAME>
    platform: <pipefy | wrike | other>
    integrations: []
    version: <N>
    status: draft
    based_on_kickoff: 01_kickoff_v<N>.md   # MANDATORY per D-76 / STG2-01
    captured_by: <USER>
    captured_at: <YYYY-MM-DD>
    ---
    ```
    (Exact field order is planner-discretion; the verbatim `based_on_kickoff` literal substring is what D1 + X2 assertions check.)

    **Edit 5 — `### Step 6 — Write and hand off` (lines ~109-122):**
    REPLACE the existing multi-bullet review-steps block with the DESIGN-18 line 612 verbatim handoff:
    > Awaiting status: approved write to 02_discovery_v<N>.md before generate-sow runs.

    Keep this as a single block-quote line; remove the v0.3.0 review-procedure narrative around it.
  </action>
  <verify>
    <automated>grep -qF 'Stage 2 SKIPPED — kickoff branch = draft-sow' dydx-delivery/skills/discovery-intake/SKILL.md && grep -qF 'based_on_kickoff' dydx-delivery/skills/discovery-intake/SKILL.md && grep -qiE '(MANDATORY|required)' dydx-delivery/skills/discovery-intake/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'Stage 2 SKIPPED — kickoff branch = draft-sow' dydx-delivery/skills/discovery-intake/SKILL.md` succeeds (D3 — VERBATIM authoritative spelling per R-01)
    - `grep -qF 'based_on_kickoff' dydx-delivery/skills/discovery-intake/SKILL.md` succeeds (D1 + X2)
    - `grep -qiE '(MANDATORY|required)' dydx-delivery/skills/discovery-intake/SKILL.md` succeeds (D1 — write-path enforcement policy documented)
    - `grep -qF '01_kickoff_v<N>.md' dydx-delivery/skills/discovery-intake/SKILL.md` succeeds (upstream artefact reference)
    - `grep -qF 'Awaiting status: approved write to 02_discovery_v<N>.md' dydx-delivery/skills/discovery-intake/SKILL.md` succeeds (DESIGN-18 line 612 verbatim handoff)
    - SKILL.md does NOT contain `kickoff produced draft SOW; routing to Stage 3` (verifies R-01 — DESIGN-18 line 604 alt spelling NOT lifted)
  </acceptance_criteria>
  <done>5 surgical edits applied; D1 + D3 + X2 assertion targets present in SKILL.md.</done>
</task>

<task id="07-02-T2" type="auto">
  <name>Task 2: Retire raw-notes path — delete Start-at-any-point block, add RETIRED notice to What this skill does not do</name>
  <files>dydx-delivery/skills/discovery-intake/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/discovery-intake/SKILL.md` (post-T1 state)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §4 (lines 124-130 + lines 132-138 surgical-edit landmarks)
    - `.planning/REQUIREMENTS.md` STG2-01 (raw-notes RETIRED authority)
  </read_first>
  <action>
    Two more surgical edits to retire the raw-notes entry path:

    **Edit 6 — `## Start-at-any-point handling` block (lines ~124-130):**
    DELETE the entire section. This block documents the v0.3.0 shortcut-inputs ("I already have a brief", "Just structure these notes") — the raw-notes entry path RETIRED per STG2-01. Removing the section is the surgical signal that the path is gone.

    Alternative if the section also documented legitimate kickoff-based shortcuts (READ the section first): keep ONLY the kickoff-based content and remove every reference to raw-notes / brief paste. Default action is full deletion; reviewer judgement can preserve a subset if the section contains anything that survives STG2-01.

    **Edit 7 — `## What this skill does not do` (lines ~132-138):**
    APPEND a new bullet:
    > - **Does NOT accept raw notes.** The raw-notes entry path is RETIRED in v2.2 — discovery is a pure transform of an approved `01_kickoff_v<N>.md` artefact per STG2-01 + DESIGN-18. Pasted meeting notes / Miro / Field Notes content flows through `kickoff-capture` first (see `dydx-delivery/skills/kickoff-capture/SKILL.md`).

    The literal "RETIRED" token is what D2 asserts via `grep -qiE 'raw[- ]notes.*(RETIRED|removed|no longer)'`. Use the word "RETIRED" verbatim (uppercase) to maximise grep robustness.

    **No edits to Step 3 (discovery dimensions list):** STG2-03 + R-04 lock the 9-section discovery template body (including "Business outcome" as item 1). DO NOT "fix" the 9-vs-8 mismatch with kickoff's 8-list — the mismatch is INTENTIONAL.
  </action>
  <verify>
    <automated>grep -qiE 'raw[- ]notes.*(RETIRED|removed|no longer)' dydx-delivery/skills/discovery-intake/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qiE 'raw[- ]notes.*(RETIRED|removed|no longer)' SKILL.md` succeeds (D2 assertion)
    - `grep -qF 'Start-at-any-point handling' SKILL.md` returns FALSE (section removed) OR if any kickoff-based content was preserved, the section no longer references raw-notes / brief paste
    - `grep -qF 'pasted meeting notes' SKILL.md` returns FALSE in the Inputs / Steps context (RETIRED — only acceptable mention is the historical reference inside "What this skill does not do")
    - Step 3 discovery dimensions list still contains "Business outcome" (STG2-03 / R-04 — INTENTIONALLY unchanged; do NOT remove)
  </acceptance_criteria>
  <done>Raw-notes path explicitly retired in SKILL.md; D2 assertion target present; STG2-03 body-unchanged contract preserved for Step 3 dimensions.</done>
</task>

<task id="07-02-T3" type="auto">
  <name>Task 3: Verify intake-template.md body unchanged (STG2-03 no-op confirmation)</name>
  <files></files>
  <read_first>
    - `dydx-delivery/skills/discovery-intake/references/intake-template.md` (BEFORE state at start of Wave 2)
    - `.planning/REQUIREMENTS.md` STG2-03 ("Same discovery template structure as v0.3.0 ... Only upstream input contract and skip behaviour change; body unchanged.")
  </read_first>
  <action>
    Verification task — confirm `intake-template.md` body is structurally UNCHANGED per STG2-03. This is a no-op edit; the task gate is `git diff` empty.

    Command:
    ```bash
    git diff dydx-delivery/skills/discovery-intake/references/intake-template.md
    ```

    Expected: empty diff (no changes to the file across 07-02). If the diff is non-empty, the executor MUST revert any inadvertent edits to this file — STG2-03 forbids structural changes.

    Note: this captures R-04 from RESEARCH §11 — discovery template's 9-section dimension list (Business outcome + Users + Systems + Triggers + Data + Rules + Integrations + Exceptions + Open Questions) deliberately differs from kickoff template's 8-section list (system / users / triggers / data / rules / integrations / exceptions / failure-points). The mismatch is intentional; do NOT harmonise.
  </action>
  <verify>
    <automated>test -z "$(git diff dydx-delivery/skills/discovery-intake/references/intake-template.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff dydx-delivery/skills/discovery-intake/references/intake-template.md` returns empty output
    - File still exists at the canonical path
    - File still contains all 9 v0.3.0 dimension headings (Business outcome / Users / Systems / Triggers / Data / Rules / Integrations / Exceptions / Open Questions or equivalent — confirm via `grep -c '^## ' intake-template.md` returns the same value as before 07-02 started)
  </acceptance_criteria>
  <done>STG2-03 body-unchanged contract verified via git diff. R-04 intentional mismatch preserved.</done>
</task>

<task id="07-02-T4" type="auto">
  <name>Task 4: Run phase7-structure-check.sh --section discovery (plan gate)</name>
  <files></files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh` (shipped in 07-01)
    - Updated `dydx-delivery/skills/discovery-intake/SKILL.md` (post-T1, T2)
  </read_first>
  <action>
    Run the discovery-section gate. The kickoff section already passes (07-01 shipped); discovery section now expected green.

    Command:
    ```bash
    bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --section discovery
    ```

    Expected: D1, D2, D3 emit `PASS:` followed by `ALL ASSERTIONS PASSED` and exit 0.

    If any D-assertion fails, fix the source SKILL.md (not the script). Cross-section assertions (X1, X2) and the sow section (S1, S2) are still expected to fail at this point — 07-03 runs in parallel and the cross-section gate is checked at the synthesis task in 07-03 via `--all`.
  </action>
  <verify>
    <automated>bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --section discovery</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0
    - stdout contains `ALL ASSERTIONS PASSED`
    - stdout contains 3 `PASS:` lines covering D1, D2, D3
  </acceptance_criteria>
  <done>`--section discovery` exits 0 — STG2-01, STG2-02 verified structurally; STG2-03 verified via T3 git diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved kickoff → discovery-intake | Discovery reads `01_kickoff_v<N>.md` written by `kickoff-capture` (Phase 7 internal pipeline; both ends are reviewer-operated). |
| discovery write-path → filesystem | Discovery writes `02_discovery_v<N>.md` to client/project path (path components from kickoff frontmatter). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | Skip-message string drift (D-74 vs DESIGN-18 line 604 alt spelling) | mitigate | R-01 explicitly resolves: D3 structure-check `grep -qF` enforces the D-74 / STG2-02 / Roadmap spelling. Plan task 1 cites the authoritative spelling verbatim. Plan task 1 acceptance criterion explicitly forbids the DESIGN-18 drift spelling appearing in SKILL.md. |
| T-07-07 | Spoofing / Repudiation | Discovery artefact written without `based_on_kickoff:` field — provenance broken | mitigate | D1 structure-check asserts `based_on_kickoff` literal + (MANDATORY|required) keyword in SKILL.md body. Hook-level enforcement (`validate-frontmatter` v2.6 / SURF-01..03) is deferred but documented as the next-step contract per D-76. |
| T-07-08 | Information disclosure | Legacy v0.3.0 discovery artefacts without `based_on_kickoff:` fail to load | accept | DESIGN-08 + D-76 lenient-read contract — legacy artefacts parse without enforcement. Documented in SKILL.md Inputs section per Edit 2. |
| T-07-09 | Tampering | Reviewer accidentally edits `intake-template.md` body during the surgical SKILL.md edits | mitigate | T3 explicit `git diff` gate; non-empty diff blocks plan completion. STG2-03 body-unchanged contract preserved. |
| T-07-10 | Denial of service | Missing `01_kickoff_v<N>.md` upstream → SKILL fails to run | accept | Documented in Step 1: emit explicit error directing reviewer to run `kickoff-capture` first. Fail-loud behaviour preserves auditability. |

ASVS L1 alignment: write-path provenance via `based_on_kickoff:` MANDATORY rule + structure-check enforcement; doc-only changes (no execution surface). Threat surface is small.
</threat_model>

<verification>
- Plan-local: `bash phase7-structure-check.sh --section discovery` exits 0 (D1, D2, D3 PASS).
- Manual: read the modified Step 1 sub-step end-to-end; confirm the verbatim skip-message appears in a code block / block-quote with the literal text from D-74.
- Manual: `git diff dydx-delivery/skills/discovery-intake/references/intake-template.md` returns empty (STG2-03 verification).
- Cross-section: full `--all` gate runs in 07-03 synthesis; X1 + X2 must pass after both 07-02 and 07-03 ship.
</verification>

<success_criteria>
- `dydx-delivery/skills/discovery-intake/SKILL.md` modified per surgical-edit table (T1 + T2).
- `dydx-delivery/skills/discovery-intake/references/intake-template.md` unchanged (T3 — STG2-03).
- `bash phase7-structure-check.sh --section discovery` exits 0.
- D-74 / STG2-02 verbatim spelling used; DESIGN-18 line 604 drift spelling NOT present.
- R-04 9-vs-8 intentional mismatch preserved (Step 3 dimensions untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-02-SUMMARY.md` recording:
- Surgical edits applied (7 — count matches Edits 1-7 in T1 + T2)
- Final `--section discovery` exit code (0 expected)
- Whether the `## Start-at-any-point handling` block was fully deleted or partially preserved (record rationale if partial)
- Git diff for `intake-template.md` (expect empty)
</output>
