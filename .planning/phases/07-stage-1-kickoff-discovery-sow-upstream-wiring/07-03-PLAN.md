---
plan: 07-03
phase: 7
slug: stage-1-kickoff-discovery-sow-upstream-wiring
wave: 2
depends_on: [07-01]
files_modified:
  - dydx-delivery/skills/generate-sow/SKILL.md
  - dydx-delivery/skills/generate-sow/references/sow-template.md
  - .planning/DESIGN.md
  - .planning/REQUIREMENTS.md
files_created:
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-meeting-notes.md
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-miro-paste.md
  - .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-field-notes.md
autonomous: false
requirements: [STG3-01, STG3-02, STG1-01, STG1-02, STG1-03, STG1-04, STG1-05, STG2-01, STG2-02, STG2-03]
must_haves:
  truths:
    - "generate-sow/SKILL.md documents canonical 4-state lifecycle draft -> client_review -> approved -> archived"
    - "generate-sow/SKILL.md reads kickoff directly via based_on_kickoff on the draft-sow branch; reads 02_discovery on discovery-ready branch"
    - "sow-template.md carries ## Platform Scope + ## Integration Scope H2 sections (single artefact covers both per STG3-02 + D-75)"
    - "DESIGN.md glossary line ~1482 fixed to discovery-ready | draft-sow (R-02 — authoritative across 4 sources vs glossary outlier)"
    - "generate-sow handoff updated to Stage 4a/4b nomenclature (R-05 pre-emptive fix; avoids Phase 8 follow-up)"
    - "Sample-CR fixtures exist (3 files) for manual e2e smoke"
    - "Manual e2e smoke completes: reviewer runs kickoff -> discovery (per branch) -> sow against fixtures"
    - "REQUIREMENTS.md traceability table flipped — all 10 v2.2 Phase 7 requirement rows show Pending -> Satisfied"
    - "bash phase7-structure-check.sh (full --all run) exits 0 — K1-K7, D1-D3, S1-S2, X1-X2 all PASS"
  artifacts:
    - path: "dydx-delivery/skills/generate-sow/SKILL.md"
      provides: "Stage 3 SOW skill body — canonical 4-state lifecycle + dual upstream input contract + Stage 4a/4b handoff"
      contains: "draft -> client_review -> approved -> archived verbatim, based_on_kickoff + based_on_discovery dual-input policy, DESIGN-19 line 653 verbatim handoff"
    - path: "dydx-delivery/skills/generate-sow/references/sow-template.md"
      provides: "SOW artefact template with dual-scope H2 split"
      contains: "## Platform Scope + ## Integration Scope H2 headers, canonical lifecycle in frontmatter narrative"
    - path: ".planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/"
      provides: "Sample-CR fixtures for manual e2e smoke"
      contains: "3 files: meeting-notes (~30 lines) + Miro paste (~15 lines) + Field Notes (~20 lines, processed_at IS NULL rows)"
  key_links:
    - from: "dydx-delivery/skills/generate-sow/SKILL.md"
      to: "dydx-delivery/skills/kickoff-capture/references/kickoff-template.md (via 01_kickoff_v<N>.md on draft-sow path)"
      via: "Step 1 Inputs reads based_on_kickoff when discovery was skipped"
      pattern: "based_on_kickoff|01_kickoff_v"
    - from: "dydx-delivery/skills/generate-sow/references/sow-template.md"
      to: "Phase 8 Stage 4a/4b generate-fnspec-platform + generate-fnspec-integration"
      via: "## Platform Scope feeds Stage 4a; ## Integration Scope feeds Stage 4b"
      pattern: "## Platform Scope|## Integration Scope"
---

<objective>
Surgical modifications to `dydx-delivery/skills/generate-sow/SKILL.md` + `references/sow-template.md` per STG3-01 / STG3-02 + D-75 + R-05, PLUS phase-wide synthesis:

1. Lock canonical 4-state lifecycle `draft -> client_review -> approved -> archived` in SKILL.md (STG3-01) — using the unicode arrow character that the structure-check `grep -qF` asserts.
2. Split single `## Scope` narrative into `## Platform Scope` + `## Integration Scope` H2 sections in `sow-template.md` (STG3-02 + D-75).
3. Update Inputs / Step 1 / Step 5 to read kickoff directly via `based_on_kickoff:` on the `draft-sow` branch (DESIGN-19 lines 635-636 + 643).
4. R-05 pre-emptive fix: update handoff to Stage 4a/4b nomenclature (avoids Phase 8 follow-up).
5. R-02 synthesis fix: align DESIGN.md glossary line ~1482 `kickoff_branch:` enum to authoritative `discovery-ready | draft-sow` spelling.
6. Author 3 sample-CR fixtures for manual e2e smoke.
7. Run manual e2e smoke (reviewer task) — kickoff -> discovery (per branch) -> sow against fixtures.
8. Flip 10 REQUIREMENTS.md trace checkboxes (`STG1-01..05` + `STG2-01..03` + `STG3-01..02`) from Pending -> Satisfied.
9. Final phase gate: `bash phase7-structure-check.sh` (full `--all`) exits 0.

Purpose: Land the SOW upstream-wiring surgical edits AND the phase-level synthesis tasks (e2e smoke + REQUIREMENTS trace flips + glossary alignment + R-05 cleanup) in one plan per D-69 synthesis-folding rule (estimated ~110 LOC well below 400 threshold per RESEARCH §10).

Output: 2 modified skill files + 3 sample-CR fixtures + DESIGN.md glossary edit + REQUIREMENTS.md trace flips. Plan is autonomous=false — Task 7 is a manual e2e smoke requiring reviewer interaction.

Disjoint from 07-02 (`discovery-intake/SKILL.md` touched there only) -> 07-02 and 07-03 run in parallel under Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DESIGN.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md
@dydx-delivery/skills/generate-sow/SKILL.md
@dydx-delivery/skills/generate-sow/references/sow-template.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-01-SUMMARY.md
@.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-02-SUMMARY.md

<interfaces>
Canonical 4-state lifecycle (STG3-01 / DESIGN-19 lines 643 + 657 — VERBATIM, using the unicode rightwards arrow U+2192 character, NOT ASCII `->`):

    draft -> client_review -> approved -> archived

(The line above uses ASCII `->` for documentation readability; the actual file content must use the unicode arrow character. S1 asserts the unicode-arrow form in SKILL.md OR sow-template.md via `grep -qF`. Executors: copy the lifecycle string verbatim from `dydx-delivery/references/frontmatter-scheme.md` or from RESEARCH §3 K assertion block — both carry the correct unicode form.)

Dual upstream input policy (DESIGN-19 line 643):
- `based_on_discovery: 02_discovery_v<N>` (normal discovery-ready path)
- OR `based_on_kickoff: 01_kickoff_v<N>` (draft-sow path — Stage 2 skipped)
- One or the other, NOT both.

Dual-scope H2 headers (STG3-02 + D-75 — VERBATIM, anchored at line start):

    ## Platform Scope
    ## Integration Scope

S2 asserts both via `grep -qE` anchored to line start in sow-template.md.

Verbatim handoff (DESIGN-19 line 653):

    Awaiting status: approved on 03_sow_v<N>.md; routing to Stage 4a (platform fnspec) and/or Stage 4b (integration fnspec) per project scope.

R-02 glossary fix target (DESIGN.md line ~1482):
- BEFORE: `kickoff-direct | discovery-via`
- AFTER: `discovery-ready | draft-sow`

R-05 pre-emptive cleanup target (`generate-sow/SKILL.md` line ~95):
- BEFORE: reference to `generate-functional-spec` (about-to-retire skill)
- AFTER: reference to Stage 4a/4b (`generate-fnspec-platform` + `generate-fnspec-integration`) per Phase 8 nomenclature
</interfaces>
</context>

<tasks>

<task id="07-03-T1" type="auto">
  <name>Task 1: Surgical edits to generate-sow/SKILL.md (Inputs, Step 1, Step 5 + 4-state lifecycle + R-05 cleanup)</name>
  <files>dydx-delivery/skills/generate-sow/SKILL.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-sow/SKILL.md` (110 lines — surgical-edit target)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §4 second table (line-numbered landmarks for generate-sow)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §7 (per-section edit table)
    - `.planning/DESIGN.md` lines 627-664 (DESIGN-19)
    - `dydx-delivery/references/frontmatter-scheme.md` (canonical lifecycle string source — copy verbatim from there to preserve unicode arrows)
  </read_first>
  <action>
    Apply 5 surgical edits to `dydx-delivery/skills/generate-sow/SKILL.md`. Preserve v0.3.0 body shape per CONVENTIONS.md SKILL.md body structure.

    Edit 1 — `## Inputs` (lines ~11-14):
    UPDATE per DESIGN-19 lines 635-636. Replace single "latest 02_discovery_v*.md" requirement with dual policy. Add bullets explaining: (a) approved 02_discovery_v<N>.md on the discovery-ready path with based_on_discovery frontmatter, OR (b) approved 01_kickoff_v<N>.md on the draft-sow path with based_on_kickoff frontmatter. One or the other, never both per DESIGN-19 line 643. Note in this section: SOW body covers BOTH platform AND integration scope (single artefact per STG3-02 + D-75) — split lives at Stage 4 per DESIGN-20.

    Edit 2 — `### Step 1 — Locate upstream artefact` (lines ~22-34):
    UPDATE the triage prompt. New flow:
    1. Read the latest status: approved 01_kickoff_v<N>.md for this client+project.
    2. Read kickoff_branch: from kickoff frontmatter.
       - If kickoff_branch: discovery-ready -> look for the latest status: approved 02_discovery_v<N>.md. SOW frontmatter will carry based_on_discovery.
       - If kickoff_branch: draft-sow -> use kickoff directly (Stage 2 was skipped per STG2-02). SOW frontmatter will carry based_on_kickoff.
    3. If neither approved upstream is found, emit an explicit error directing the reviewer to run the upstream stage first.

    Edit 3 — `### Step 3 — Draft the SOW` (lines ~41-56):
    Light narrative update — note that deliverables now split into `## Platform Scope` items vs `## Integration Scope` items (per the template's H2 split landed in T2). Body narrative largely intact; the template carries the load-bearing change.

    Edit 4 — `### Step 5 — Write and hand off` (lines ~68-95):

    Frontmatter block (lines ~72-82) — UPDATE per DESIGN-19 line 643:
    - Add `frontmatter_version: 2`
    - Add `platform:` if known at SOW time
    - Replace single `based_on_discovery:` with dual policy comment-block (reviewer picks one of `based_on_discovery:` OR `based_on_kickoff:` — one or the other, not both)
    - Keep `status: draft` with an adjacent narrative line documenting the canonical 4-state lifecycle verbatim.

    Canonical lifecycle narrative (verbatim — copy the unicode arrow form from `dydx-delivery/references/frontmatter-scheme.md`):
    "Status lifecycle (canonical per STG3-01 + DESIGN-08 + AUDIT-01.2): draft -> client_review -> approved -> archived. generate-sow is the sole skill retaining client_review — the interim commercial-review state is a real workflow stage, not a v0.3.0 quirk."

    (In the actual file, replace each ASCII `->` above with the unicode rightwards arrow character U+2192. S1 asserts the unicode-arrow form via `grep -qF`.)

    Handoff message (lines ~84-95) — REPLACE with DESIGN-19 line 653 verbatim + R-05 cleanup:
    "Awaiting status: approved on 03_sow_v<N>.md; routing to Stage 4a (platform fnspec) and/or Stage 4b (integration fnspec) per project scope."

    Edit 5 — Hunt-and-replace any other `generate-functional-spec` references in SKILL.md body. Update each to Stage 4a/4b nomenclature (R-05 pre-emptive — avoids Phase 8 follow-up).

    Edit 6 — `## What this skill does not do` (lines ~97-101):
    APPEND bullet: "Does NOT split scope by platform vs integration at the artefact level — that split happens at Stage 4 (Fnspec). The single SOW artefact carries both as ## Platform Scope + ## Integration Scope H2 sections (per DESIGN-19 + STG3-02 + D-75)."
  </action>
  <verify>
    <automated>grep -qF "draft → client_review → approved → archived" dydx-delivery/skills/generate-sow/SKILL.md && grep -qF "based_on_kickoff" dydx-delivery/skills/generate-sow/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF "draft → client_review → approved → archived" generate-sow/SKILL.md` succeeds (S1 — canonical lifecycle verbatim with unicode arrows)
    - `grep -qF "based_on_kickoff" generate-sow/SKILL.md` succeeds (X2 — draft-sow upstream contract)
    - `grep -qF "based_on_discovery" generate-sow/SKILL.md` succeeds (discovery-ready upstream contract)
    - `grep -qF "Awaiting status: approved on 03_sow_v<N>.md; routing to Stage 4a" generate-sow/SKILL.md` succeeds (DESIGN-19 line 653 verbatim + R-05 cleanup)
    - `grep -c "generate-functional-spec" generate-sow/SKILL.md` returns 0 (R-05 — dangling reference fully removed)
  </acceptance_criteria>
  <done>SKILL.md surgical edits applied; S1 + X2 assertion targets present; R-05 pre-emptive cleanup done.</done>
</task>

<task id="07-03-T2" type="auto">
  <name>Task 2: Surgical edits to sow-template.md — frontmatter dual-input + Platform/Integration Scope H2 split</name>
  <files>dydx-delivery/skills/generate-sow/references/sow-template.md</files>
  <read_first>
    - `dydx-delivery/skills/generate-sow/references/sow-template.md` (155 lines — surgical-edit target)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §5 (line-numbered landmarks)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-PATTERNS.md` §8 (per-line edit table)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-CONTEXT.md` D-75
  </read_first>
  <action>
    Apply 3 surgical edits to `dydx-delivery/skills/generate-sow/references/sow-template.md`.

    Edit 1 — Frontmatter (lines ~6-7):
    - Line 6: keep `status: draft` value. Add adjacent comment / inline narrative documenting canonical lifecycle (`draft -> client_review -> approved -> archived` — use unicode arrows). S1 can satisfy on either SKILL.md OR sow-template.md; having it in both adds robustness.
    - Line 7: replace `based_on_discovery: 02_discovery_v<N>.md` with a dual-policy block — both fields present as YAML keys, one commented out, with a clear comment noting reviewer picks one at write time:
      ```
      # One or the other — never both per DESIGN-19 line 643:
      based_on_discovery: 02_discovery_v<N>.md   # normal path (Stage 2 ran)
      # based_on_kickoff: 01_kickoff_v<N>.md     # draft-sow path (Stage 2 skipped)
      ```

    Edit 2 — `## 2. In-scope deliverables` block (lines ~26-33):
    SPLIT into two top-level H2 sections per D-75. Replace the single `## 2. In-scope deliverables` table with two new H2 sections:

    `## Platform Scope` — intro paragraph: "What ships as platform configuration on Pipefy / Wrike / Ziflow (the primary delivery platform for this engagement). Stage 4a (`generate-fnspec-platform`) consumes this section." Followed by a 4-column table (#, Deliverable, Platform, Notes) with an empty placeholder row.

    `## Integration Scope` — intro paragraph: "What ships as inbound/outbound integration work (custom APIs, webhooks, ETL, external system handoffs). Stage 4b (`generate-fnspec-integration`) consumes this section. If the engagement has no integration work, leave the table empty and note 'no integration scope' — Stage 4b will then skip cleanly." Followed by a 4-column table (#, Deliverable, Integration target, Notes) with an empty placeholder row.

    S2 asserts presence of literal `^## Platform Scope` AND `^## Integration Scope` H2 headers (anchored regex via `grep -qE`).

    Section numbering of subsequent sections: at planner discretion. Recommended: keep original `## 1. Business outcome` heading at top; let `## Platform Scope` + `## Integration Scope` replace `## 2. In-scope deliverables`; renumber subsequent sections (Out-of-scope, etc.) down by one OR leave numbering gaps — reviewer discretion. Verifier is renumber-agnostic.

    Edit 3 — `## Handoff` block (lines ~147-155):
    Update any `generate-functional-spec` reference to Stage 4a/4b nomenclature (R-05 pre-emptive). Recommended verbatim text: "Awaiting status: approved on 03_sow_v<N>.md; routing to Stage 4a (platform fnspec) and/or Stage 4b (integration fnspec) per project scope."
  </action>
  <verify>
    <automated>grep -qE "^## Platform Scope" dydx-delivery/skills/generate-sow/references/sow-template.md && grep -qE "^## Integration Scope" dydx-delivery/skills/generate-sow/references/sow-template.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE "^## Platform Scope" sow-template.md` succeeds (S2a)
    - `grep -qE "^## Integration Scope" sow-template.md` succeeds (S2b)
    - `grep -qF "based_on_kickoff" sow-template.md` succeeds (dual upstream policy documented in frontmatter)
    - `grep -qF "based_on_discovery" sow-template.md` succeeds
    - `grep -c "generate-functional-spec" sow-template.md` returns 0 (R-05 — handoff cleanup)
    - `grep -qF "## 1. Business outcome" sow-template.md` succeeds (existing section preserved — reviewer did not destructively rewrite the template)
  </acceptance_criteria>
  <done>Template has dual-scope H2 split; S2 assertion target present; frontmatter documents dual upstream policy; R-05 handoff cleanup done.</done>
</task>

<task id="07-03-T3" type="auto">
  <name>Task 3: R-02 glossary fix — DESIGN.md line ~1482 kickoff_branch enum alignment</name>
  <files>.planning/DESIGN.md</files>
  <read_first>
    - `.planning/DESIGN.md` lines 1475-1490 (glossary entry context around line 1482)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §11 R-02 (4-vs-1 source vote, authoritative spelling)
  </read_first>
  <action>
    Surgical one-line edit to `.planning/DESIGN.md` glossary entry around line 1482. Locate the `kickoff_branch:` enum definition; replace the outlier spelling with the authoritative spelling.

    BEFORE (line ~1482): `kickoff-direct | discovery-via`
    AFTER: `discovery-ready | draft-sow`

    Surrounding context is preserved untouched. This is a 1-token swap — the glossary entry's description, definition, or other prose stays as-is.

    Rationale (recorded for SUMMARY): the DESIGN.md body at line 562 (DESIGN-17), REQUIREMENTS.md STG1-02, CONTEXT.md D-69/D-77, and ROADMAP.md Phase 7 criterion #1 all use `discovery-ready | draft-sow`. The glossary at line ~1482 was an outlier — likely a pre-CONTEXT.md drafting artefact per RESEARCH §11. Aligning the glossary closes R-02 (MEDIUM-severity drift).

    If the line number has drifted from RESEARCH §11's note of ~1482, search for `kickoff-direct` or `discovery-via` literal strings in DESIGN.md and fix every occurrence — there should be only ONE occurrence (the glossary entry).
  </action>
  <verify>
    <automated>! grep -qF "kickoff-direct" .planning/DESIGN.md && ! grep -qF "discovery-via" .planning/DESIGN.md && grep -qF "discovery-ready" .planning/DESIGN.md && grep -qF "draft-sow" .planning/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kickoff-direct" .planning/DESIGN.md` returns 0 (R-02 — outlier removed)
    - `grep -c "discovery-via" .planning/DESIGN.md` returns 0 (R-02 — outlier removed)
    - `grep -qF "discovery-ready" .planning/DESIGN.md` succeeds (authoritative spelling present)
    - `grep -qF "draft-sow" .planning/DESIGN.md` succeeds (authoritative spelling present)
  </acceptance_criteria>
  <done>DESIGN.md glossary entry aligned with the 4-source authoritative spelling. R-02 resolved.</done>
</task>

<task id="07-03-T4" type="auto">
  <name>Task 4: Author 3 sample-CR fixtures for manual e2e smoke</name>
  <files>
    .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-meeting-notes.md,
    .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-miro-paste.md,
    .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-field-notes.md
  </files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-RESEARCH.md` §10b (sample-CR fixture proposal — line counts + content shape)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md` (Manual-Only Verifications row for e2e smoke)
    - `.planning/REQUIREMENTS.md` STG1-05 (three capture paths)
  </read_first>
  <action>
    Author 3 minimal fixture files under `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/` for the manual e2e smoke task (T5). Each fixture represents one of the three STG1-05 capture paths. Content is illustrative — a small synthetic change request with enough operational signal to populate a kickoff artefact's 8 categories.

    Fixture 1 — `sample-cr-meeting-notes.md` (~30 lines):
    Freeform meeting-notes paste. Synthetic scenario: e.g., "VodafoneZiggo wants a new Pipefy phase for ticket auto-routing based on customer tier" or similar plausible CR for one of the established clients (Up & Up Group or VodafoneZiggo per D-65). Include enough signal to populate at least 5 of the 8 kickoff categories (system / users / triggers / data / rules); leave some categories under-evidenced so the `[unknown — needs human classification]` marker has reason to fire (validates STG1-04 rubric).

    Fixture 2 — `sample-cr-miro-paste.md` (~15 lines per DESIGN-07 / AUDIT-08 shape):
    Text describing a workflow diagram pasted from Miro. Use markdown to represent the diagram shape (boxes + arrows in prose, e.g., "Box: Ticket Created -> Box: Triage Phase -> [Decision: tier?] -> Box: Studio Lead Queue / Director Queue"). No actual image — narration is the canonical source per DESIGN-07.

    Fixture 3 — `sample-cr-field-notes.md` (~20 lines, 2-3 mock Field Notes rows):
    Mock Field Notes Coda paste — 2 or 3 rows in markdown table form, each with `processed_at: NULL` (verbatim — matches the K6 triage filter wording). Each row captures a single operational observation (e.g., "Ticket 12345 stuck in Triage for 3 days — no SLA on Triage Phase"). Include at least one row that should be DROPPED by the reviewer (MOD-8 prevention validation — the kickoff session asks keep/drop/edit-and-keep).

    All three fixtures begin with a YAML front-matter block declaring `fixture: true` + `fixture_for: phase-7-e2e-smoke` so the e2e smoke task in T5 can confirm fixtures are not mistaken for production CRs.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-meeting-notes.md && test -f .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-miro-paste.md && test -f .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/fixtures/sample-cr-field-notes.md</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixture files exist at the paths in `files_created`
    - `grep -qF "fixture_for: phase-7-e2e-smoke" sample-cr-meeting-notes.md` succeeds (and same for the other two fixtures)
    - `grep -qF "processed_at" sample-cr-field-notes.md` succeeds (Field Notes shape — verbatim filter column referenced)
    - `wc -l sample-cr-meeting-notes.md` returns approximately 25-40 lines (target ~30)
    - `wc -l sample-cr-miro-paste.md` returns approximately 12-20 lines (target ~15)
    - `wc -l sample-cr-field-notes.md` returns approximately 15-25 lines (target ~20)
  </acceptance_criteria>
  <done>3 fixtures authored; ready for manual e2e smoke in T5.</done>
</task>

<task id="07-03-T5" type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual e2e smoke against sample-CR fixtures (kickoff -> discovery -> sow)</name>
  <what-built>
    Phase 7 ships 3 skills (kickoff-capture NEW; discovery-intake MODIFIED; generate-sow MODIFIED) plus structure-check harness plus 3 sample-CR fixtures. The structure-check harness validates static structural properties of the skill bodies; it does NOT exercise the skills end-to-end. This checkpoint runs the skills against the fixtures from T4 to validate the actual pipeline behaviour.
  </what-built>
  <how-to-verify>
    Reviewer executes the following smoke sequence:

    1. Invoke `kickoff-capture` skill against `fixtures/sample-cr-meeting-notes.md`. Expected: produces a `01_kickoff_v1.md` artefact at the documented output path with a `kickoff_branch:` value chosen (`discovery-ready` based on the fixture's content depth) and `[unknown — needs human classification]` markers inline for under-evidenced categories.

    2. Mark the kickoff `status: approved` (manual approval gate per DESIGN-06).

    3. Invoke `discovery-intake` skill. Expected: reads the approved kickoff, sees `kickoff_branch: discovery-ready`, proceeds to run the discovery interview, and emits `02_discovery_v1.md` with `based_on_kickoff: 01_kickoff_v1.md` in frontmatter.

    4. Repeat T5.1 against `fixtures/sample-cr-miro-paste.md` — choose `kickoff_branch: draft-sow` this time (engagement is well-enough understood from the Miro flow). Mark approved.

    5. Invoke `discovery-intake` again on the draft-sow kickoff. Expected: skill emits the verbatim message `Stage 2 SKIPPED — kickoff branch = draft-sow` to stdout / handoff log AND writes NO `02_discovery_v*.md` artefact. Verify no marker file exists at `02_discovery_v*` for this fixture (per D-74).

    6. Invoke `generate-sow` on both kickoffs from steps 2 and 4. Expected: each produces a `03_sow_v1.md` artefact. The discovery-ready SOW has `based_on_discovery: 02_discovery_v1.md` in frontmatter; the draft-sow SOW has `based_on_kickoff: 01_kickoff_v1.md`. Each SOW has both `## Platform Scope` and `## Integration Scope` H2 sections present (even if Integration Scope is empty / "no integration scope" for the simpler fixture).

    7. Optional bonus run: invoke `kickoff-capture` against `fixtures/sample-cr-field-notes.md`. Validate that the MOD-8 prevention prompt asks `keep / drop / edit-and-keep` for each Field Notes row; mark the one row designated as "should be dropped" with `drop`; observe the artefact records the rejection inline (audit trail per DESIGN-09).

    Inspection points:
    - Artefacts produced at expected paths (`<Client> Brain/<Project>/01_kickoff_v1.md` etc., or the path scheme documented in `dydx-delivery/references/stage-numbering.md`).
    - Frontmatter contracts honoured (`kickoff_branch:`, `based_on_kickoff:`, `based_on_discovery:`, status enum).
    - Verbatim skip-message appears EXACTLY as `Stage 2 SKIPPED — kickoff branch = draft-sow`.
    - No `02_discovery_v*` marker file written on the draft-sow path.
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark the e2e smoke pass, or describe any failures (which fixture, which step, what was wrong) for a follow-up fix task.
  </resume-signal>
  <acceptance_criteria>
    - Reviewer types "approved" or equivalent confirmation
    - All 3 fixture-driven smoke runs produce expected artefacts (or expected skip behaviour) at expected paths
    - Verbatim skip-message string observed exactly on the draft-sow path
    - Both SOW artefacts contain `## Platform Scope` AND `## Integration Scope` H2 sections (even if one is empty / "no integration scope" placeholder)
  </acceptance_criteria>
  <done>Manual e2e smoke passes (Dimension 9 / end-to-end coverage per 07-VALIDATION.md).</done>
</task>

<task id="07-03-T6" type="auto">
  <name>Task 6: Flip 10 REQUIREMENTS.md traceability checkboxes (STG1-01..05 + STG2-01..03 + STG3-01..02)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 117-145 (Traceability table — current state shows all 10 v2.2 Phase 7 rows as Pending)
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-VALIDATION.md` (Requirement -> Assertion Mapping table)
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` Traceability table — flip 10 v2.2 Phase 7 requirement rows from `Pending` to `Satisfied` (or whatever status token the table convention uses — match the prior milestone's token if present).

    Rows to flip:
    1. STG1-01 — Phase 7 — Pending -> Satisfied
    2. STG1-02 — Phase 7 — Pending -> Satisfied
    3. STG1-03 — Phase 7 — Pending -> Satisfied
    4. STG1-04 — Phase 7 — Pending -> Satisfied
    5. STG1-05 — Phase 7 — Pending -> Satisfied
    6. STG2-01 — Phase 7 — Pending -> Satisfied
    7. STG2-02 — Phase 7 — Pending -> Satisfied
    8. STG2-03 — Phase 7 — Pending -> Satisfied
    9. STG3-01 — Phase 7 — Pending -> Satisfied
    10. STG3-02 — Phase 7 — Pending -> Satisfied

    Also flip the 10 requirement-line checkboxes earlier in REQUIREMENTS.md (lines 26-46 — the `- [ ] **STG1-01**: ...` entries) from `[ ]` to `[x]`.

    DO NOT flip any Phase 8 rows (STG4, ROUTE) — those are out of scope for Phase 7.

    Update the "Coverage" footer at lines 142-145 if the status token-change reflects in any cumulative counter (recommend leaving the count text alone — Phase 7 didn't change the requirement count, only the satisfaction status).
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*STG" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*STG[123]-" .planning/REQUIREMENTS.md` returns 10 (5 STG1 + 3 STG2 + 2 STG3 boxes checked)
    - `grep -c "^- \[ \] \*\*STG[123]-" .planning/REQUIREMENTS.md` returns 0 (no Phase 7 requirement still unchecked)
    - `grep -c "^- \[ \] \*\*STG4-" .planning/REQUIREMENTS.md` returns 6 (Phase 8 STG4 rows untouched)
    - `grep -c "^- \[ \] \*\*ROUTE-" .planning/REQUIREMENTS.md` returns 5 (Phase 8 ROUTE rows untouched)
    - The Traceability table at the bottom shows all 10 STG1/STG2/STG3 rows in a satisfied status (literal token chosen to match prior-milestone convention — `Satisfied` or `Complete` or `Satisfied ✓`)
  </acceptance_criteria>
  <done>REQUIREMENTS.md trace table reflects Phase 7 satisfaction; Phase 8 rows untouched.</done>
</task>

<task id="07-03-T7" type="auto">
  <name>Task 7: Final phase gate — bash phase7-structure-check.sh --all (K1-K7, D1-D3, S1-S2, X1-X2 all PASS)</name>
  <files></files>
  <read_first>
    - `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh` (shipped in 07-01)
    - All Phase 7 deliverables on disk (07-01 + 07-02 outputs + 07-03 T1-T6 outputs)
  </read_first>
  <action>
    Run the full Phase 7 structure-check gate. This is the phase-level final gate — all 4 runners (kickoff / discovery / sow / cross) must emit PASS for every assertion.

    Command:
    ```
    bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --all
    ```
    (or without the `--all` flag — same behaviour per dispatch case.)

    Expected output: every K1-K7, D1-D3, S1-S2, X1-X2 assertion emits `PASS:` followed by `ALL ASSERTIONS PASSED` and exit 0.

    If ANY assertion fails:
    - K-series fail -> 07-01 deliverable issue; check kickoff-capture skill body and references.
    - D-series fail -> 07-02 deliverable issue; check discovery-intake/SKILL.md.
    - S-series fail -> 07-03 T1/T2 issue; check generate-sow/SKILL.md or sow-template.md.
    - X-series fail -> cross-skill consistency issue; check that `kickoff_branch:` appears in all 3 skill files AND `based_on_kickoff:` appears in both discovery-intake and generate-sow SKILL.md.

    Fix the source file (NEVER the script — the script is the contract). Re-run until exit 0.
  </action>
  <verify>
    <automated>bash .planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/scripts/phase7-structure-check.sh --all</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0
    - stdout contains `ALL ASSERTIONS PASSED`
    - stdout contains PASS lines covering K1, K2, K3, K4, K5, K6, K7, D1, D2, D3, S1, S2, X1, X2 (14 assertions total per 07-VALIDATION.md Dimension 8 sensor model: 7 kickoff + 3 discovery + 2 sow + 2 cross)
  </acceptance_criteria>
  <done>Final phase gate green. Phase 7 ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved kickoff -> generate-sow (draft-sow path) | SOW reads `01_kickoff_v<N>.md` directly when Stage 2 is skipped. |
| approved 02_discovery -> generate-sow (discovery-ready path) | SOW reads `02_discovery_v<N>.md` when discovery ran. |
| reviewer -> e2e smoke (T5) | Reviewer-driven manual invocation against synthetic fixtures (low trust risk; not external attacker). |
| DESIGN.md / REQUIREMENTS.md write-path | Planning artefacts edited by reviewer/executor; version-controlled via git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | Canonical 4-state lifecycle string drift (unicode arrow vs ASCII `->`) | mitigate | S1 structure-check `grep -qF` enforces unicode-arrow form (`draft → client_review → approved → archived`). Executor instructed to copy verbatim from `dydx-delivery/references/frontmatter-scheme.md` source. R-02 alignment for glossary protects against secondary drift surface. |
| T-07-12 | Spoofing | SOW written without `based_on_kickoff:` OR `based_on_discovery:` (broken provenance) | mitigate | Step 5 frontmatter block in SKILL.md documents the dual-input policy; sow-template.md frontmatter carries both fields with comment-block guidance. Hook-level enforcement (`validate-frontmatter` v2.6) is deferred but the contract is documented. |
| T-07-13 | Tampering | DESIGN.md glossary edit (T3) accidentally introduces secondary drift | mitigate | T3 acceptance criteria explicitly assert `grep -c "kickoff-direct"` returns 0 AND both authoritative tokens are present. Edit is single-token swap, minimal scope. |
| T-07-14 | Information disclosure | Sample-CR fixtures (T4) accidentally contain real client PII | accept | Fixtures use established clients per D-65 (VodafoneZiggo / Up & Up Group) with synthetic scenarios; reviewer responsibility to keep content illustrative. fixture_for: phase-7-e2e-smoke marker distinguishes from production CRs. |
| T-07-15 | Repudiation | E2e smoke (T5) skipped or partially completed; manual gate falsely signed off | accept | Reviewer attestation via approved signal; git history records the commit. Plan is autonomous=false so the checkpoint cannot be auto-passed by an executor agent. |
| T-07-16 | Denial of service | Structure-check `--all` runs slow due to many `grep -qF` calls | accept | Total assertion count is 14; estimated runtime <1s per 07-VALIDATION.md. No DoS surface. |
| T-07-17 | Elevation of privilege | REQUIREMENTS.md trace flip applied to Phase 8 rows by accident | mitigate | T6 acceptance explicitly asserts `grep -c "^- \[ \] \*\*STG4-"` returns 6 AND `grep -c "^- \[ \] \*\*ROUTE-"` returns 5 — Phase 8 rows must remain unchecked. |

ASVS L1 alignment: doc-only changes + manual gate (T5) + structure-check enforcement across 14 assertions. Threat surface remains small; primary mitigations are grep-gate verifications of canonical strings.
</threat_model>

<verification>
- Plan-local: `bash phase7-structure-check.sh --section sow` exits 0 after T1+T2 (S1, S2 PASS).
- Plan-local: `bash phase7-structure-check.sh --all` exits 0 after all T1-T7 (T7 is the gate).
- Manual: T5 e2e smoke — reviewer attestation captured in checkpoint resume-signal.
- Cross-plan: structure-check `--all` validates X1 + X2 cross-skill assertions (verifies 07-02 discovery edits remain consistent with 07-03 sow edits).
- Source-audit: REQUIREMENTS.md Traceability table shows 10 v2.2 Phase 7 rows as Satisfied; Phase 8 rows untouched.
- R-02 closed: DESIGN.md glossary aligned to authoritative spelling (verifiable via `grep -c "kickoff-direct" .planning/DESIGN.md` returning 0).
- R-05 closed: zero `generate-functional-spec` references in generate-sow SKILL.md or sow-template.md.
</verification>

<success_criteria>
- `dydx-delivery/skills/generate-sow/SKILL.md` modified per surgical-edit list (T1).
- `dydx-delivery/skills/generate-sow/references/sow-template.md` modified per surgical-edit list (T2).
- `.planning/DESIGN.md` glossary entry aligned (T3 — R-02 closed).
- 3 sample-CR fixtures exist at `.planning/phases/07-.../fixtures/` (T4).
- Manual e2e smoke (T5) signed off by reviewer.
- REQUIREMENTS.md trace table shows all 10 v2.2 Phase 7 requirement rows satisfied (T6).
- `bash phase7-structure-check.sh --all` exits 0 with `ALL ASSERTIONS PASSED` (T7 — final phase gate).
- Phase 7 ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-stage-1-kickoff-discovery-sow-upstream-wiring/07-03-SUMMARY.md` recording:
- Surgical edits applied to generate-sow/SKILL.md (5 — Edits 1-5 in T1) and sow-template.md (3 — Edits 1-3 in T2)
- R-02 glossary fix outcome (DESIGN.md line ~1482 — record the actual line number found at edit time)
- R-05 outcome (count of `generate-functional-spec` references removed)
- 3 fixture file paths + final line counts
- E2e smoke outcome (T5 — reviewer attestation summary; any deviations recorded)
- REQUIREMENTS.md trace flip count (10) + confirmation Phase 8 rows untouched
- Final `bash phase7-structure-check.sh --all` exit code (0 expected) + PASS-line count (14 expected)
</output>
