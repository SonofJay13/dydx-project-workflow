---
plan_id: 04-02
phase: 04-open-questions
plan: 02
type: execute
wave: 2
depends_on: [04-01]
requirements: [OPEN-01, OPEN-02]
files_modified:
  - .planning/OPEN-QUESTIONS.md
  - .planning/phases/04-open-questions/04-02-SUMMARY.md
autonomous: true
tags: [open-questions, OPEN-01, OPEN-02, research-blocked, connector-availability, design-only]

must_haves:
  truths:
    - "OPEN-QUESTIONS.md `## OPEN-01: Research-flagged unverified items` populated with ~8 register rows in closed 9-field schema (D-47): 3x BLOCKER native-AI ingestion (Pipefy AI KB / Wrike AI Studio / Ziflow ReviewAI) all owner=Phase 7 + 5x GUARDRAIL/INFORMATIONAL research items (Pipefy GraphQL pagination / Ziflow read-after-create / Pipefy 2026 rate-limit / Wrike 2026 rate-limit / Miro export-whole-board / Claude in Chrome canonical product naming)."
    - "OPEN-QUESTIONS.md `## OPEN-02: Connector-availability uncertainties` populated with ~3-4 register rows: Coda MCP wired? / Google Workspace MCP wired (which server: taylorwilsdon vs piotr-agier vs Anthropic-maintained)? / Miro MCP wired (or paste-only)? — all owner=Phase 1; resolution=`/gsd-research-phase 1`."
    - "Every OPEN-01 + OPEN-02 register row carries closed 9-field schema per D-47: OPEN-QN / Question / Source citations / Owning phase / Verification owner / Severity / Resolution path / Proposed default / Status."
    - "All severity values exact-match D-48 closed enum (BLOCKER|GUARDRAIL|INFORMATIONAL); all resolution-path values exact-match D-49 closed enum; all status values exact-match D-47 closed enum (open|proposed); all owning-phase values match Phase [1-9] enum or literal TBD."
    - "D-37 OPEN-01 contingent fallback verbatim sentence (`slide P8/P9 → v2.7`) appears in the OPEN-01 section preamble OR within Phase 7 BLOCKER row Proposed-default field per D-37 carry-forward."
    - "Every register row has >= 1 backtick-wrapped `path:line` citation per D-14 carried (e.g., `` `.planning/REQUIREMENTS.md:90` ``, `` `.planning/CHANGELIST.md:284` ``, `` `.planning/AUDIT.md:NNN` ``)."
  artifacts:
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "OPEN-01 + OPEN-02 sections populated with closed 9-field register rows; OPEN-NN populated-by placeholder notes for these two sections REPLACED (no leftover `04-02` plan-ID strings under these H2s)"
      contains: "## OPEN-01 body: ~8 register rows; ## OPEN-02 body: ~3-4 register rows; D-37 contingent-fallback sentence; ~12 register rows total"
  key_links:
    - from: "OPEN-01 BLOCKER rows (Pipefy AI KB / Wrike AI Studio / Ziflow ReviewAI)"
      to: "CHANGELIST.md Phase 7 mini-table Research-blocked cell + Appendix C research-blocked phases"
      via: "Source citations field cites both `.planning/CHANGELIST.md:NNN` (Phase 7 mini-table) AND `.planning/CHANGELIST.md:284` (Appendix E bullet)"
      pattern: "multi-source citation per D-50 dedup discipline"
    - from: "OPEN-01 native-AI rows + Phase 7 owner"
      to: "D-37 contingent fallback sentence (verbatim — `slide P8/P9 → v2.7`)"
      via: "Phase 4 mirrors Phase 3's verbatim D-37 wording wherever Phase 7-blocker rows are described per D-37 carry-forward"
      pattern: "exact substring match in OPEN-01 section preamble or row body"
    - from: "OPEN-02 connector-availability rows"
      to: "AUDIT.md §AUDIT-08 live MCP probe results (Coda / Google Workspace / Miro / Slack — present-and-working / present-but-broken / missing)"
      via: "Source citations field cites `.planning/AUDIT.md:NNN` for each connector"
      pattern: "AUDIT-08 is the connector-availability source-of-truth per CONTEXT.md canonical_refs"
---

<objective>
Wave 2: populate `.planning/OPEN-QUESTIONS.md` `## OPEN-01: Research-flagged unverified items` AND `## OPEN-02: Connector-availability uncertainties` with closed 9-field register rows per D-47. These are the heaviest sections per CONTEXT.md Specifics (~12 rows combined). Mostly research-blocked → resolution-path = `/gsd-research-phase <N>` per D-49 + D-42 carried.

**Purpose:** Lands the bulk of the research-flagged backlog. After this wave, Plans 04-03 + 04-04 land the remaining design-deferred + policy rows; Plan 04-05 synthesises Executive Summary + Appendices.

**Heavy lift:** OPEN-01 has 3 BLOCKER rows that must mirror Phase 3's verbatim D-37 contingent-fallback wording (`slide P8/P9 → v2.7`). OPEN-02 has 3-4 connector rows grounded in AUDIT-08's live MCP probe + live AUDIT.md probe results.

**Output:** OPEN-01 + OPEN-02 sections of OPEN-QUESTIONS.md populated; ~12 register rows total in closed 9-field schema; structural-check still fails on remaining unpopulated sections (OPEN-03..07 + Appendices) — final pass at Plan 04-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/OPEN-QUESTIONS.md
@.planning/phases/04-open-questions/04-CONTEXT.md
@.planning/phases/04-open-questions/04-DISCUSSION-LOG.md
@.planning/phases/04-open-questions/04-01-PLAN.md
@.planning/phases/04-open-questions/04-01-SUMMARY.md
@.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
@.planning/phases/03-changelist/03-03-SUMMARY.md
@.planning/phases/03-changelist/03-06-SUMMARY.md

<interfaces>
<!-- Per-row schema (closed 9-field per D-47). Author chooses table-form OR per-row block; both satisfy A4..A9. -->
<!-- This plan uses **per-row block form** (markdown subsection per row) for OPEN-01 + OPEN-02 because rows -->
<!-- carry multi-source citations and longer Proposed-default verbiage that doesn't fit a table column cleanly. -->

**Per-row block format (D-47 9-field schema, per-row block form):**

```markdown
**OPEN-Q01** — Pipefy AI KB content-upload endpoint externally verifiable?

- **Question:** Is the Pipefy AI KB content-upload endpoint externally verifiable, and if so via which API call (REST vs GraphQL; sync vs async; what payload shape)?
- **Source citations:** `.planning/REQUIREMENTS.md:90`, `.planning/CHANGELIST.md:284`, `.planning/DESIGN.md:NNN` (DESIGN-14 platform-pipefy native_ai_path), `.planning/research/PITFALLS.md:NNN` (CRIT-N if applicable)
- **Owning phase:** Phase 7
- **Verification owner:** Phase 7 / dev
- **Severity:** BLOCKER
- **Resolution path:** /gsd-research-phase 7
- **Proposed default:** none (per REQUIREMENTS.md OPEN-01 — paths must be verified before Phase 7 can lock plans). If unresolved at v2.5 kickoff per D-37 contingent fallback: split P6 → v2.5 (Documentation only) and P7 → v2.6 (Native-AI when unblocked); slide P8/P9 → v2.7. Phase 7 is the only blocked-by-OPEN phase; isolating it preserves cadence on the rest.
- **Status:** open
```

The 12 expected rows (per CONTEXT.md Specifics):

**OPEN-01 ~8 rows:**
1. OPEN-Q01 — Pipefy AI KB content-upload endpoint (BLOCKER / Phase 7 / `/gsd-research-phase 7`)
2. OPEN-Q02 — Wrike AI Studio knowledge-ingestion API (BLOCKER / Phase 7 / `/gsd-research-phase 7`)
3. OPEN-Q03 — Ziflow ReviewAI knowledge-ingestion API (BLOCKER / Phase 7 / `/gsd-research-phase 7`)
4. OPEN-Q04 — Pipefy GraphQL pagination cursor field names (GUARDRAIL / Phase 7 / `/gsd-research-phase 7`) — note CHANGELIST.md Appendix E assigns this to "Phase 7" per the platform-pipefy build owner
5. OPEN-Q05 — Ziflow read-after-create consistency window (GUARDRAIL / Phase 2 / `/gsd-research-phase 2`)
6. OPEN-Q06 — Pipefy 2026 rate-limit currency (INFORMATIONAL / Phase 1/Phase 2 / `/gsd-research-phase 1`) — historic ceiling ~5 req/sec per token
7. OPEN-Q07 — Wrike 2026 rate-limit currency (INFORMATIONAL / Phase 1/Phase 2 / `/gsd-research-phase 1`) — historic ~100 req/min per user
8. OPEN-Q08 — Miro export-whole-board endpoint availability (INFORMATIONAL / Phase 3 / `/gsd-research-phase 3`)
9. OPEN-Q09 — Claude in Chrome canonical product naming (INFORMATIONAL / Phase 1 / `/gsd-research-phase 1`)

(OPEN-01 may run 8-9 rows; CONTEXT.md says "likely 8 rows".)

**OPEN-02 ~3-4 rows:**
10. OPEN-Q10 — Coda MCP wired in this workspace? Which version pin? (GUARDRAIL / Phase 1 / `/gsd-research-phase 1`)
11. OPEN-Q11 — Google Workspace MCP wired (which server: `taylorwilsdon` vs `piotr-agier` vs Anthropic-maintained)? (GUARDRAIL / Phase 1 / `/gsd-research-phase 1`)
12. OPEN-Q12 — Miro MCP wired (or paste-only)? (INFORMATIONAL / Phase 1 / `/gsd-research-phase 1`)
13. OPEN-Q13 — Wrike auth header / `host` field source-of-truth (INFORMATIONAL / Phase 1 / `/gsd-research-phase 1`)

(OPEN-02 may run 3-4 rows; CONTEXT.md says "3-4 rows".)

**D-37 contingent fallback verbatim sentence** must appear at least once — choices:
- (a) embedded in the Proposed-default field of all 3 BLOCKER native-AI rows (most explicit)
- (b) emitted as an OPEN-01 section preamble paragraph below the H2 (most concise)

This plan chooses option (b) — single section preamble paragraph — because the same sentence in 3 places duplicates verbiage. Add the verbatim sentence in the OPEN-01 section preamble (1 paragraph between the H2 and the first `**OPEN-Q01**` block).

**Citation discipline (D-14 + D-50):**
- Every row carries >= 1 backtick-wrapped `` `path:line` `` citation
- Multi-source rows (e.g., OPEN-Q01 sourced in BOTH REQUIREMENTS.md AND CHANGELIST.md AND DESIGN.md) list all citations comma-separated
- Cite real line numbers (use `grep -n` to find them); do not fabricate
- Examples of canonical lines to cite (verify with `grep -n` at execution):
  - `.planning/REQUIREMENTS.md:90` — OPEN-01 verbatim text
  - `.planning/REQUIREMENTS.md:91` — OPEN-02 verbatim text
  - `.planning/CHANGELIST.md:277..285` — Appendix E bullets (one bullet per inline marker)
  - `.planning/CHANGELIST.md:284` — Pipefy AI KB Phase 7 inline marker
  - `.planning/AUDIT.md:NNN` — AUDIT-08 live MCP probe table (Coda / Google Workspace / Miro / Slack rows)
  - `.planning/DESIGN.md:NNN` — DESIGN-14 platform-pipefy / DESIGN-15 platform-wrike / DESIGN-16 platform-ziflow native_ai_path fields

**Append-only invariant:** This plan ONLY modifies the bodies of `## OPEN-01` and `## OPEN-02`. It MUST NOT touch any other H2 section. Verification: `git diff` shows changes confined to those two sections.

**Replace populated-by note:** Each H2's existing populated-by note (from skeleton 04-01) is REPLACED — not appended to. After this plan, neither `## OPEN-01` nor `## OPEN-02` body should contain the substring `Populated by 04-02-PLAN.md` (no-placeholder mid-phase fail still happens elsewhere because OPEN-03..07 + Appendices remain unpopulated; that's expected per Wave 2 invariant).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve actual line numbers for citations + write OPEN-01 section body (preamble paragraph + ~8 register rows + D-37 verbatim fallback)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — skeleton from 04-01)
    - .planning/REQUIREMENTS.md (lines 88-96 — OPEN-01..07 verbatim text; resolve actual line numbers via `grep -n '^- \*\*OPEN-0' .planning/REQUIREMENTS.md`)
    - .planning/CHANGELIST.md (lines 269-287 — Appendix E 9 bullets; resolve actual line numbers via `grep -nF '[OPEN: Phase 4 —' .planning/CHANGELIST.md`)
    - .planning/DESIGN.md — DESIGN-14 / DESIGN-15 / DESIGN-16 platform-pipefy/-wrike/-ziflow native_ai_path field locations (resolve via `grep -nF 'native_ai_path' .planning/DESIGN.md` or `grep -nE '^### DESIGN-1[456]' .planning/DESIGN.md`)
    - .planning/AUDIT.md §AUDIT-08 live MCP probe (resolve via `grep -nF '## AUDIT-08' .planning/AUDIT.md` then read ~50 lines)
    - .planning/research/SUMMARY.md if present — Phase 7 native-AI block reference for D-37 contingent fallback context
    - .planning/phases/04-open-questions/04-CONTEXT.md §"Specifics" + §"D-37"
    - .planning/phases/03-changelist/03-03-SUMMARY.md (Phase 7 BLOCKER mini-table writer; D-37 verbatim wording precedent)
  </read_first>
  <action>
    Execute these steps in sequence:

    **Step 1 — Resolve canonical line numbers** for citations (run via Bash):
    ```bash
    grep -n '^- \*\*OPEN-0' .planning/REQUIREMENTS.md   # captures OPEN-01..07 line numbers
    grep -nF '[OPEN: Phase 4 —' .planning/CHANGELIST.md  # captures inline marker + Appendix E bullet line numbers
    grep -nE '^## AUDIT-0[3-9]' .planning/AUDIT.md       # captures AUDIT-08 + neighbouring section line numbers
    grep -nE '^### DESIGN-1[456]:' .planning/DESIGN.md   # captures platform-skill section line numbers
    ```
    Record the actual line numbers for each citation. If a line number cannot be located, use the section's H2 line number AND record fallback `.planning/<file>.md:<H2-line>` citation form. NEVER fabricate line numbers.

    **Step 2 — Edit `.planning/OPEN-QUESTIONS.md`** — REPLACE the populated-by note under `## OPEN-01: Research-flagged unverified items` with this exact body:

    1. **Section preamble paragraph (D-37 contingent fallback verbatim — CONTEXT MANDATORY):**

       ```
       This section catalogues every research-flagged "couldn't verify" item surfaced from `.planning/research/SUMMARY.md` § Phase Ordering Rationale + `.planning/CHANGELIST.md` Appendix C (research-blocked phases) + `.planning/DESIGN.md` § "Deferred to Phase 4 OPEN-QUESTIONS". Three rows are HARD BLOCKER (native-AI ingestion APIs per platform — Pipefy AI KB / Wrike AI Studio / Ziflow ReviewAI; all Phase 7 owner per CHANGE-04). The remaining items are GUARDRAIL or INFORMATIONAL (resolved during owning phase but do not halt plan-creation). **D-37 OPEN-01 contingent fallback (verbatim — carried from `.planning/CHANGELIST.md` § Phase 7 / Appendix E):** If OPEN-01 (native-AI ingestion paths per platform) remains unresolved at v2.5 kickoff, split P6 → v2.5 (Documentation only) and P7 → v2.6 (Native-AI when unblocked); slide P8/P9 → v2.7. Phase 7 is the only blocked-by-OPEN phase; isolating it preserves cadence on the rest.
       ```

    2. **Per-row blocks (~8 rows)** — emit each row in this exact 9-field shape (use the per-row block format shown in `<interfaces>`). Each row:

       a. **OPEN-Q01 — Pipefy AI KB content-upload endpoint externally verifiable?**
          - Question: Is the Pipefy AI KB content-upload endpoint externally verifiable, and if so via which API call (REST vs GraphQL; sync vs async; what payload shape)?
          - Source citations: `` `.planning/REQUIREMENTS.md:<resolved>` ``, `` `.planning/CHANGELIST.md:<resolved-Pipefy-KB-Appendix-E-line>` ``, `` `.planning/DESIGN.md:<DESIGN-14 line>` ``
          - Owning phase: Phase 7
          - Verification owner: Phase 7 / dev
          - Severity: BLOCKER
          - Resolution path: /gsd-research-phase 7
          - Proposed default: none (per REQUIREMENTS.md OPEN-01 — paths must be externally verified before Phase 7 can lock plans).
          - Status: open

       b. **OPEN-Q02 — Wrike AI Studio knowledge-ingestion API externally verifiable?**
          - Question: Is the Wrike AI Studio knowledge-ingestion API externally verifiable, and if so via which endpoint (sync vs async; KB-scope vs skill-scope; rate-limit per ingest job)?
          - Source citations: `` `.planning/REQUIREMENTS.md:<resolved>` ``, `` `.planning/CHANGELIST.md:<Wrike-AI-Studio-line>` ``, `` `.planning/DESIGN.md:<DESIGN-15 line>` ``
          - Owning phase: Phase 7
          - Verification owner: Phase 7 / dev
          - Severity: BLOCKER
          - Resolution path: /gsd-research-phase 7
          - Proposed default: none.
          - Status: open

       c. **OPEN-Q03 — Ziflow ReviewAI knowledge-ingestion API externally verifiable?**
          - Question: Is the Ziflow ReviewAI knowledge-ingestion API (Checklists Public Preview) externally verifiable, and if so via which endpoint?
          - Source citations: `` `.planning/REQUIREMENTS.md:<resolved>` ``, `` `.planning/CHANGELIST.md:<Ziflow-ReviewAI-line>` ``, `` `.planning/DESIGN.md:<DESIGN-16 line>` ``
          - Owning phase: Phase 7
          - Verification owner: Phase 7 / dev
          - Severity: BLOCKER
          - Resolution path: /gsd-research-phase 7
          - Proposed default: none.
          - Status: open

       d. **OPEN-Q04 — Pipefy GraphQL pagination cursor field names current?**
          - Question: Do Pipefy GraphQL pagination cursor field names in v0.3.0 helpers match the current 2026 schema (e.g., `pageInfo.hasNextPage` / `pageInfo.endCursor` vs renamed equivalents)?
          - Source citations: `` `.planning/CHANGELIST.md:<Pipefy-pagination-Appendix-E-line>` ``, `` `.planning/DESIGN.md:<DESIGN-14 paginate_all line>` ``
          - Owning phase: Phase 7
          - Verification owner: Phase 7 / dev
          - Severity: GUARDRAIL
          - Resolution path: /gsd-research-phase 7
          - Proposed default: assume documented historic field names; verify via live GraphQL introspection at Phase 7 kickoff (CHANGELIST.md Appendix E owner = Phase 7 per CHANGE-04 platform-pipefy build).
          - Status: open

       e. **OPEN-Q05 — Ziflow read-after-create consistency window**
          - Question: What is the actual read-after-create consistency window for Ziflow proof-create operations in 2026?
          - Source citations: `` `.planning/CHANGELIST.md:<Ziflow-read-after-create-Appendix-E-line>` ``, `` `.planning/DESIGN.md:<DESIGN-16 wait_for_proof line>` ``
          - Owning phase: Phase 2
          - Verification owner: Phase 2 / dev
          - Severity: GUARDRAIL
          - Resolution path: /gsd-research-phase 2
          - Proposed default: conservative — 30 second poll with 2s interval (per CHANGELIST.md Appendix E bullet verbatim recommendation).
          - Status: proposed

       f. **OPEN-Q06 — Pipefy 2026 rate-limit currency**
          - Question: Are Pipefy's 2026 rate limits still ~5 req/sec per token, or have they been revised?
          - Source citations: `` `.planning/CHANGELIST.md:<Pipefy-rate-limit-Appendix-E-line>` ``, `` `.planning/DESIGN.md:<DESIGN-14 rate-limit line>` ``
          - Owning phase: Phase 1/Phase 2
          - Verification owner: Phase 1 / dev
          - Severity: INFORMATIONAL
          - Resolution path: /gsd-research-phase 1
          - Proposed default: documented historic ceiling ~5 req/sec per token (per CHANGELIST.md Appendix E verbatim); helpers throttle at 4 req/sec (80%) — locked by DESIGN-22 carried.
          - Status: proposed

       g. **OPEN-Q07 — Wrike 2026 rate-limit currency**
          - Question: Are Wrike's 2026 rate limits still ~100 req/min per user, or have they been revised?
          - Source citations: `` `.planning/CHANGELIST.md:<Wrike-rate-limit-Appendix-E-line>` ``, `` `.planning/DESIGN.md:<DESIGN-15 rate-limit line>` ``
          - Owning phase: Phase 1/Phase 2
          - Verification owner: Phase 1 / dev
          - Severity: INFORMATIONAL
          - Resolution path: /gsd-research-phase 1
          - Proposed default: documented historic ~100 req/min per user (per CHANGELIST.md Appendix E verbatim).
          - Status: proposed

       h. **OPEN-Q08 — Miro export-whole-board endpoint availability**
          - Question: Does Miro 2026 expose an export-whole-board endpoint usable from Stage 3 SOW capture, or does the workflow remain paste-from-screenshot?
          - Source citations: `` `.planning/REQUIREMENTS.md:<OPEN-01 line>` ``, `` `.planning/AUDIT.md:<AUDIT-03 connector-table Miro row line>` ``
          - Owning phase: Phase 3
          - Verification owner: Phase 3 / dev
          - Severity: INFORMATIONAL
          - Resolution path: /gsd-research-phase 3
          - Proposed default: paste-from-screenshot remains the supported path; export-whole-board endpoint is opportunistic.
          - Status: proposed

       i. **OPEN-Q09 — Claude in Chrome canonical product naming**
          - Question: What is the canonical 2026 product name for Anthropic's browser-resident Claude (Claude in Chrome / Claude for Chrome / something else)?
          - Source citations: `` `.planning/REQUIREMENTS.md:<OPEN-01 line>` ``, `` `.planning/AUDIT.md:<AUDIT-04 referenced-but-missing artefacts line>` ``
          - Owning phase: Phase 1
          - Verification owner: Phase 1 / non-dev
          - Severity: INFORMATIONAL
          - Resolution path: /gsd-research-phase 1
          - Proposed default: use whichever name appears canonically on https://claude.ai or the official Anthropic site at Phase 1 kickoff; document the resolution in `dydx-delivery/references/glossary.md` (FOUND-01).
          - Status: proposed

    **Implementing decisions:** D-46 (primary by OPEN-NN), D-47 (closed 9-field row schema, per-row block form), D-48 (severity closed enum BLOCKER/GUARDRAIL/INFORMATIONAL), D-49 (resolution-path closed enum), D-50 (multi-source citation per row), D-37 carried (verbatim contingent fallback in section preamble), D-14 carried (citation backtick-pattern), D-42 carried (research-blocked rows get `/gsd-research-phase <N>` resolution).
  </action>
  <verify>
    <automated>OPEN1_BODY=$(awk -v hdr='## OPEN-01: Research-flagged unverified items' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN1_ROW_COUNT=$(printf '%s\n' "$OPEN1_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+\*\*') && [ "$OPEN1_ROW_COUNT" -ge 8 ] && BLOCKER_COUNT=$(printf '%s\n' "$OPEN1_BODY" | grep -cE 'Severity:[[:space:]]*BLOCKER') && [ "$BLOCKER_COUNT" -ge 3 ] && printf '%s\n' "$OPEN1_BODY" | grep -qF 'slide P8/P9 → v2.7' && CITATION_COUNT=$(printf '%s\n' "$OPEN1_BODY" | grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`') && [ "$CITATION_COUNT" -ge "$OPEN1_ROW_COUNT" ] && echo OPEN_01_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-01: Research-flagged unverified items` body has >= 8 register rows: extract section via section_between awk + count `^\*\*OPEN-Q[0-9]+\*\*` matches; expression returns >= 8
    - >= 3 BLOCKER rows in OPEN-01 (the 3 native-AI ingestion endpoints): `grep -cE 'Severity:[[:space:]]*BLOCKER'` against OPEN-01 body returns >= 3
    - D-37 OPEN-01 contingent fallback verbatim sentence present: `grep -qF 'slide P8/P9 → v2.7'` against OPEN-01 body succeeds
    - Every OPEN-01 row carries >= 1 backtick-wrapped `path:line` citation: `grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`'` against OPEN-01 body returns >= row_count
    - All 9 fields present per row: 9 individual `grep -qF` checks against OPEN-Q01 body block — `Question:`, `Source citations:`, `Owning phase:`, `Verification owner:`, `Severity:`, `Resolution path:`, `Proposed default:`, `Status:` (8 field-prefix labels — `OPEN-Q01` is the row ID)
    - All Severity values exact-match closed enum: `grep -nE 'Severity:[[:space:]]' .planning/OPEN-QUESTIONS.md | grep -vE 'Severity:[[:space:]]*(BLOCKER|GUARDRAIL|INFORMATIONAL)'` produces no output
    - All Resolution-path values exact-match closed enum: `grep -nE 'Resolution path:[[:space:]]' .planning/OPEN-QUESTIONS.md | grep -vE 'Resolution path:[[:space:]]*(/gsd-research-phase [0-9]+|decide-before-Phase-[0-9]+|Coda-template-authoring \(Phase 8\)|policy-pending-sign-off|live-workstream-pointer)'` produces no output
    - Section preamble paragraph cites `.planning/research/SUMMARY.md` AND `.planning/CHANGELIST.md` Appendix C AND `.planning/DESIGN.md` source-stream — 3 individual `grep -qF` checks against OPEN-01 body
    - No accidental modifications to other H2 sections (Executive Summary / How to read / OPEN-02..07 / Appendix A / B / C): inspect git diff confines to OPEN-01 region
  </acceptance_criteria>
  <done>
    `## OPEN-01: Research-flagged unverified items` body populated with section preamble paragraph (citing source streams + emitting D-37 verbatim contingent fallback) + 8-9 register rows in closed 9-field per-row block form. Every row carries closed-enum-conformant fields + >= 1 backtick-wrapped path:line citation. Populated-by skeleton note replaced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write OPEN-02 section body — connector-availability uncertainty rows grounded in AUDIT-08 live MCP probe</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01 populated by Task 1)
    - .planning/AUDIT.md §AUDIT-08 live MCP probe (resolve line range via `grep -nF '## AUDIT-08' .planning/AUDIT.md` then read 50-80 lines from there)
    - .planning/AUDIT.md §AUDIT-04 referenced-but-missing artefacts (resolve line range via `grep -nF '## AUDIT-04' .planning/AUDIT.md`)
    - .planning/REQUIREMENTS.md OPEN-02 verbatim text (line ~91)
    - .planning/phases/04-open-questions/04-CONTEXT.md §"Specifics" — "OPEN-02 connector-availability"
  </read_first>
  <action>
    Edit `.planning/OPEN-QUESTIONS.md` — REPLACE the populated-by note under `## OPEN-02: Connector-availability uncertainties` with this body:

    **Section preamble paragraph:**

    ```
    This section catalogues connector-availability uncertainties surfaced from `.planning/AUDIT.md` § AUDIT-08 (live MCP probe in this workspace) and `.planning/REQUIREMENTS.md` OPEN-02. Every row is owned by Phase 1 (Foundations + Connector Verification per CHANGELIST.md CHANGE-01) — Phase 1 connector probe is the resolution path for all rows in this section. Severity is GUARDRAIL when missing wiring blocks a downstream stage (Coda / Google Workspace) and INFORMATIONAL when a documented fallback exists (Miro paste-only).
    ```

    **Per-row blocks (~3-4 rows):**

    Each row uses the same per-row block format as OPEN-01 (9-field schema per D-47).

    **OPEN-Q10 — Coda MCP wired in this workspace at canonical version pin?**
    - Question: Is Coda MCP wired in this workspace, at what version pin, and does the wiring carry the live-edit + table-rows-manage capabilities Stage 6 + Stage 11 require?
    - Source citations: `` `.planning/REQUIREMENTS.md:<OPEN-02 line>` ``, `` `.planning/AUDIT.md:<AUDIT-08 Coda MCP row line>` ``, `` `.planning/DESIGN.md:<DESIGN-22 Coda mechanics line>` ``
    - Owning phase: Phase 1
    - Verification owner: Phase 1 / dev
    - Severity: GUARDRAIL
    - Resolution path: /gsd-research-phase 1
    - Proposed default: assume Coda MCP wired (AUDIT-08 records present-and-working as of probe date); Phase 1 connector probe re-confirms version pin + capability-set at v2.1 kickoff. If absent: Stage 6 cost estimate Coda mirror writes degrade to manual-paste fallback.
    - Status: proposed

    **OPEN-Q11 — Google Workspace MCP wired (which server)?**
    - Question: Is Google Workspace MCP wired in this workspace, and which specific server (`taylorwilsdon` vs `piotr-agier` vs Anthropic-maintained)? Different servers expose different tool surfaces (Drive read-vs-write; Gmail send vs read-only; Calendar event-create vs read).
    - Source citations: `` `.planning/REQUIREMENTS.md:<OPEN-02 line>` ``, `` `.planning/AUDIT.md:<AUDIT-08 Google Workspace MCP row line>` ``
    - Owning phase: Phase 1
    - Verification owner: Phase 1 / dev
    - Severity: GUARDRAIL
    - Resolution path: /gsd-research-phase 1
    - Proposed default: confirm via `gsd-tools.cjs mcp:status` at Phase 1 kickoff; document specific server choice in `dydx-delivery/references/connectors.md` (FOUND-06). If absent: Stage 9 doc-publish degrades to manual-upload to Drive (graceful per MOD-1 prevention).
    - Status: proposed

    **OPEN-Q12 — Miro MCP wired (or paste-only)?**
    - Question: Is Miro MCP wired in this workspace, or does the Stage 3 SOW capture flow rely on paste-from-screenshot?
    - Source citations: `` `.planning/REQUIREMENTS.md:<OPEN-02 line>` ``, `` `.planning/AUDIT.md:<AUDIT-08 Miro MCP row line>` ``
    - Owning phase: Phase 1
    - Verification owner: Phase 1 / dev
    - Severity: INFORMATIONAL
    - Resolution path: /gsd-research-phase 1
    - Proposed default: paste-from-screenshot remains the documented baseline (per AUDIT-08 Miro = "missing" or "deferred"). MCP wiring is opportunistic; Stage 3 design must function without it.
    - Status: proposed

    **OPEN-Q13 — Wrike auth header / `host` field source-of-truth across multi-tenant deployments**
    - Question: Where is the per-tenant Wrike `host` field (returned from OAuth token exchange) persisted in this workspace's pattern, and is it the same source-of-truth Stage 7b will read from for `platform-wrike` API calls?
    - Source citations: `` `.planning/REQUIREMENTS.md:<OPEN-02 line>` ``, `` `.planning/DESIGN.md:<DESIGN-15 host field line>` ``, `` `.planning/AUDIT.md:<AUDIT-03 Wrike connector dependency line>` ``
    - Owning phase: Phase 1
    - Verification owner: Phase 1 / dev
    - Severity: INFORMATIONAL
    - Resolution path: /gsd-research-phase 1
    - Proposed default: persist `wrike_host` in `<Client> Brain/00_HUB.md` Coda block alongside `coda_brain_doc:` cache (per DESIGN-15 carried).
    - Status: proposed

    **Implementing decisions:** D-46 (OPEN-02 H2), D-47 (closed 9-field row schema), D-48 (severity GUARDRAIL or INFORMATIONAL — no BLOCKER in OPEN-02 because connector probe is non-halting), D-49 (`/gsd-research-phase 1` resolution), D-50 (multi-source citation), D-14 carried.
  </action>
  <verify>
    <automated>OPEN2_BODY=$(awk -v hdr='## OPEN-02: Connector-availability uncertainties' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN2_ROW_COUNT=$(printf '%s\n' "$OPEN2_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+\*\*') && [ "$OPEN2_ROW_COUNT" -ge 3 ] && BAD_SEV=$(printf '%s\n' "$OPEN2_BODY" | grep -nE 'Severity:[[:space:]]' | grep -vE 'Severity:[[:space:]]*(BLOCKER|GUARDRAIL|INFORMATIONAL)' || true) && [ -z "$BAD_SEV" ] && OWNER_PHASE_1=$(printf '%s\n' "$OPEN2_BODY" | grep -cE 'Owning phase:[[:space:]]*Phase 1') && [ "$OWNER_PHASE_1" -ge 3 ] && CITATION_COUNT=$(printf '%s\n' "$OPEN2_BODY" | grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`') && [ "$CITATION_COUNT" -ge "$OPEN2_ROW_COUNT" ] && echo OPEN_02_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-02: Connector-availability uncertainties` body has >= 3 register rows: extract section via section_between awk + count `^\*\*OPEN-Q[0-9]+\*\*` matches; returns >= 3
    - All OPEN-02 rows have `Owning phase: Phase 1`: `grep -cE 'Owning phase:[[:space:]]*Phase 1'` against OPEN-02 body returns >= 3
    - All OPEN-02 rows have `Resolution path: /gsd-research-phase 1`: `grep -cF 'Resolution path: /gsd-research-phase 1'` against OPEN-02 body returns >= 3
    - All OPEN-02 Severity values are GUARDRAIL or INFORMATIONAL (no BLOCKER): `grep -cE 'Severity:[[:space:]]*BLOCKER'` against OPEN-02 body returns 0
    - Every OPEN-02 row cites `.planning/AUDIT.md` (AUDIT-08 grounding): `grep -cE '`\.planning/AUDIT\.md:[0-9]+`'` against OPEN-02 body returns >= row_count
    - Section preamble cites AUDIT-08 verbatim: `grep -qF 'AUDIT-08' .planning/OPEN-QUESTIONS.md` succeeds (within OPEN-02 body)
    - All 9 fields present per row in OPEN-Q10 (sample row): 8 individual `grep -qF` checks against OPEN-Q10 block — `Question:`, `Source citations:`, `Owning phase:`, `Verification owner:`, `Severity:`, `Resolution path:`, `Proposed default:`, `Status:`
    - OPEN-01 from Task 1 unchanged: spot-check via `grep -qF 'OPEN-Q01' .planning/OPEN-QUESTIONS.md && grep -qF 'OPEN-Q05' .planning/OPEN-QUESTIONS.md` succeeds
  </acceptance_criteria>
  <done>
    `## OPEN-02: Connector-availability uncertainties` body populated with section preamble paragraph (citing AUDIT-08 + OPEN-02 stream) + 3-4 register rows in closed 9-field per-row block form. All rows owner=Phase 1, resolution=`/gsd-research-phase 1`. Populated-by skeleton note replaced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run mid-phase structural-check + write Wave 2 SUMMARY.md</name>
  <files>.planning/phases/04-open-questions/04-02-SUMMARY.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01 + OPEN-02 populated by Tasks 1+2)
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
    - .planning/phases/03-changelist/03-03-SUMMARY.md (Phase 3 mid-phase SUMMARY precedent)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    **Step 1 — Mid-phase structural-check** (expected to FAIL but progress further than Wave 1):

    ```bash
    bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo "EXIT_CODE=$?"
    ```

    Expected first-fail: A4 (register-row count >= 22) — Wave 2 lands ~12 rows; floor is 22; will not yet pass. OR A10 (Appendix B 1:1 cardinality) — Appendix B body still empty. OR A11 (Appendix C reconciliation block missing). OR A13 (no-placeholder — populated-by notes still present in OPEN-03..07 + Appendices). Any of these is acceptable as the first-fail. Record the actual first-fail message verbatim in SUMMARY.

    **Step 2 — Spot-check counts** (run via Bash):
    ```bash
    grep -cE '^\*\*OPEN-Q[0-9]+\*\*' .planning/OPEN-QUESTIONS.md  # expect >= 12
    grep -cE 'Severity:[[:space:]]*BLOCKER' .planning/OPEN-QUESTIONS.md  # expect >= 3 (OPEN-01 native-AI rows)
    grep -cE 'Severity:[[:space:]]*GUARDRAIL' .planning/OPEN-QUESTIONS.md  # expect >= 3
    grep -cE 'Severity:[[:space:]]*INFORMATIONAL' .planning/OPEN-QUESTIONS.md  # expect >= 4
    grep -qF 'slide P8/P9 → v2.7' .planning/OPEN-QUESTIONS.md && echo D37_OK
    ```

    **Step 3 — Write `.planning/phases/04-open-questions/04-02-SUMMARY.md`** using template (see Phase 3 03-03-SUMMARY.md for direct precedent). Required content:

    1. Plan ID + execution date + outcome
    2. Sections populated (OPEN-01 ~8-9 rows; OPEN-02 ~3-4 rows; total ~12-13 rows)
    3. D-37 verbatim contingent fallback present (verbatim quote in SUMMARY)
    4. Closed-enum verification: all severity / resolution-path / status / owning-phase fields exact-match enum (numbers per the spot-check output)
    5. Mid-phase structural-check first-fail message verbatim
    6. Decisions implemented: D-46, D-47, D-48, D-49, D-50, D-37, D-42, D-14
    7. Hand-off to Wave 3 (Plan 04-03 owns OPEN-03 + OPEN-04 + OPEN-05)
  </action>
  <verify>
    <automated>EXIT_CODE=0; bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh > /tmp/wave2_check.out 2>&1 || EXIT_CODE=$?; echo "Wave 2 structural-check EXIT=$EXIT_CODE"; cat /tmp/wave2_check.out; ROWS=$(grep -cE '^\*\*OPEN-Q[0-9]+\*\*' .planning/OPEN-QUESTIONS.md); echo "ROWS=$ROWS"; [ "$ROWS" -ge 12 ] && BLOCKERS=$(grep -cE 'Severity:[[:space:]]*BLOCKER' .planning/OPEN-QUESTIONS.md) && [ "$BLOCKERS" -ge 3 ] && grep -qF 'slide P8/P9 → v2.7' .planning/OPEN-QUESTIONS.md && test -f .planning/phases/04-open-questions/04-02-SUMMARY.md && grep -qF 'OPEN-01' .planning/phases/04-open-questions/04-02-SUMMARY.md && grep -qF 'OPEN-02' .planning/phases/04-open-questions/04-02-SUMMARY.md && echo TASK_3_OK</automated>
  </verify>
  <acceptance_criteria>
    - structural-check ran (exits NON-ZERO mid-phase per Wave 2 invariant — full pass deferred to Plan 04-05 synthesis)
    - Total register rows now >= 12: `grep -cE '^\*\*OPEN-Q[0-9]+\*\*' .planning/OPEN-QUESTIONS.md` returns >= 12
    - >= 3 BLOCKER rows: `grep -cE 'Severity:[[:space:]]*BLOCKER' .planning/OPEN-QUESTIONS.md` returns >= 3
    - D-37 verbatim sentence present: `grep -qF 'slide P8/P9 → v2.7' .planning/OPEN-QUESTIONS.md` succeeds
    - SUMMARY.md exists at `.planning/phases/04-open-questions/04-02-SUMMARY.md`
    - SUMMARY.md mentions both OPEN-01 + OPEN-02: 2 individual `grep -qF` checks succeed
    - SUMMARY.md cites the structural-check first-fail message verbatim: `grep -qF 'EXIT_CODE' .planning/phases/04-open-questions/04-02-SUMMARY.md` OR `grep -qF 'FAIL:' .planning/phases/04-open-questions/04-02-SUMMARY.md` succeeds
    - SUMMARY.md names downstream consumer (Plan 04-03 / Wave 3): `grep -qF '04-03' .planning/phases/04-open-questions/04-02-SUMMARY.md` succeeds
  </acceptance_criteria>
  <done>
    Wave 2 lands ~12 register rows under OPEN-01 + OPEN-02. Structural-check still fails on later assertions (register-count floor 22 not yet met; Appendix B empty; Appendix C missing reconciliation block; populated-by notes still present in unwritten sections) — expected per Wave 2 invariant. SUMMARY.md committed with closed-enum verification + D-37 verbatim presence + hand-off to Plan 04-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Citation line numbers ↔ source files | Citations format `path:line` per D-14. Risk: line numbers drift if source file is edited after this plan but before approval. Mitigation: cite section H2 line numbers when possible (more stable than mid-section content lines); structural-check (A9) only verifies backtick-pattern presence, not line-number validity — the reconciliation algorithm in Plan 04-05 verifies via `grep -n` at synthesis time. |
| D-37 verbatim sentence ↔ Phase 7 BLOCKER row description | Tampering class. The verbatim sentence MUST appear once and exactly. Mitigation: Task 1 emits it once in OPEN-01 section preamble; structural-check assertion A12 grep -qF on substring `slide P8/P9 → v2.7`. |
| Closed-enum drift ↔ structural-check | Tampering class. Author could write `Severity: WARNING` or `Resolution path: research` (off-enum). Mitigation: structural-check A5/A6/A7/A8 disallow patterns reject any literal outside the enum — fails at script invocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | OPEN-01 / OPEN-02 row content | mitigate | Closed-enum disallow patterns (A5/A6/A7/A8) + per-row 9-field schema acceptance criteria + multi-source citation acceptance check |
| T-04-05 | Information disclosure | Source citations | accept | Citations are public planning docs; no secrets exposed |
| T-04-06 | Tampering | D-37 verbatim sentence | mitigate | Substring assertion A12 enforces exact match |
</threat_model>

<verification>
- OPEN-01 ~8-9 rows in closed 9-field schema, >= 3 BLOCKER, D-37 verbatim sentence in section preamble
- OPEN-02 ~3-4 rows in closed 9-field schema, all owner=Phase 1, all resolution=`/gsd-research-phase 1`
- Structural-check still fails (register-count floor not yet met); first-fail recorded in SUMMARY
- SUMMARY.md committed
</verification>

<success_criteria>
- `.planning/OPEN-QUESTIONS.md` `## OPEN-01` body populated; ~8-9 register rows; section preamble contains D-37 verbatim
- `.planning/OPEN-QUESTIONS.md` `## OPEN-02` body populated; ~3-4 register rows; all owner=Phase 1
- All severity / resolution-path / status / owning-phase fields exact-match closed enums (D-47/D-48/D-49)
- Every row has >= 1 backtick-wrapped `path:line` citation per D-14
- Mid-phase structural-check ran (exits NON-ZERO; first-fail recorded in SUMMARY)
- `.planning/phases/04-open-questions/04-02-SUMMARY.md` committed
</success_criteria>

<output>
After completion, create `.planning/phases/04-open-questions/04-02-SUMMARY.md` per template.
</output>