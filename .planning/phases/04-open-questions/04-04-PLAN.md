---
plan_id: 04-04
phase: 04-open-questions
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
requirements: [OPEN-06, OPEN-07]
files_modified:
  - .planning/OPEN-QUESTIONS.md
  - .planning/phases/04-open-questions/04-04-SUMMARY.md
autonomous: true
tags: [open-questions, OPEN-06, OPEN-07, policy, refine-resolution, plugin-self-test, design-only]

must_haves:
  truths:
    - "OPEN-QUESTIONS.md `## OPEN-06: /refine-<skill> resolution` populated with 1-2 register rows in closed 9-field schema (D-47): build-vs-orphan-delete decision (OPEN-Q21) + (if build) namespace decision OPEN-Q21.1. Both rows owner=Phase 1; all rows resolution=`policy-pending-sign-off` per D-49 + D-52."
    - "OPEN-QUESTIONS.md `## OPEN-07: Plugin self-test scope` populated with single canonical register row (OPEN-Q22 — explicitly numbered to honour OPEN-03 cross-reference from Plan 04-03): smoke tests for hooks + frontmatter validator vs defer. Owner=Phase 9 per CHANGELIST.md CHANGE-04 (Phase 9 = Surfaces) / decide-before-Phase-9 / resolution=`policy-pending-sign-off` / Severity=INFORMATIONAL / Status=proposed (recommended default in REQUIREMENTS.md OPEN-07 verbatim)."
    - "Both OPEN-06 + OPEN-07 rows carry the D-52 sub-fields embedded in row body (NOT extra columns — keeps closed 9-field schema): Decision deadline + Acceptance signal + Fallback-if-undecided. All three sub-fields present in Proposed-default field per D-52."
    - "OPEN-Q22 ID is assigned to the OPEN-07 row (NOT OPEN-Q21 or OPEN-Q23) so the OPEN-03 cross-reference from Plan 04-03 (`See OPEN-Q22`) resolves correctly."
    - "Every OPEN-06 + OPEN-07 register row carries closed 9-field schema; severity / resolution-path / status / owning-phase exact-match closed enums (single-owner-only per cross-AI C6)."
    - "OPEN-Q21.1 decimal-ID row, if emitted, is COUNTED by structural-check A4/A10/A14 per cross-AI C2 decimal-ID-aware regex; cumulative register total reaches >= 21 floor."
  artifacts:
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "OPEN-06 + OPEN-07 sections populated; populated-by notes for these two sections REPLACED. Cumulative register total now ~23-25 rows post-cross-AI-C6 split."
      contains: "## OPEN-06 body: 1-2 rows; ## OPEN-07 body: 1 row (OPEN-Q22). Total 2-3 new rows."
  key_links:
    - from: "OPEN-Q22 (OPEN-07 plugin self-test row)"
      to: "OPEN-03 cross-reference blockquote (Plan 04-03 Task 1)"
      via: "Forward reference: OPEN-03 says `See OPEN-Q22`; OPEN-07 row MUST be OPEN-Q22"
      pattern: "ID locked by upstream cross-reference; numbering is NOT planner discretion at this point"
    - from: "OPEN-Q21 (OPEN-06 first row)"
      to: "REQUIREMENTS.md OPEN-06 verbatim recommended default (`build single parameterised command`)"
      via: "Source citation field cites `.planning/REQUIREMENTS.md:95`"
      pattern: "Recommended default lifted verbatim per D-52"
    - from: "OPEN-06 + OPEN-07 (policy decisions)"
      to: "ROADMAP.md Phase 4 success criterion 5 (`with clear 'decide before Phase X' owners`)"
      via: "Decision deadline sub-field per D-52 names exact phase"
      pattern: "ROADMAP success criterion 5 satisfied by D-52 sub-field discipline"
---

<objective>
Wave 4: populate `.planning/OPEN-QUESTIONS.md` `## OPEN-06: /refine-<skill> resolution` AND `## OPEN-07: Plugin self-test scope` with closed 9-field register rows. These are the two policy-decision sections per ROADMAP success criterion 5.

**Purpose:** Lands the policy backlog. Both sections use `Resolution path: policy-pending-sign-off` per D-49 (Jason approves recommended default; no further work). Both rows carry D-52 sub-fields (Decision deadline + Acceptance signal + Fallback-if-undecided) embedded in the Proposed-default field body per D-52.

**Numbering directive (carried from Plan 04-03 SUMMARY.md):** OPEN-Q21 lands in OPEN-06 (first row); OPEN-Q22 lands in OPEN-07 (so OPEN-03's forward cross-reference `See OPEN-Q22` resolves correctly). If OPEN-06 carries a 2nd row (namespace sub-decision), it is OPEN-Q21.1 (decimal-ID; counted by cross-AI C2 decimal-ID-aware regex in A4/A10/A14). OPEN-07 stays OPEN-Q22 strictly.

**Cross-AI review concerns mitigated in this plan:**
- **C2 (HIGH carried)** — OPEN-Q21.1 decimal-ID row is correctly counted by A4/A10/A14 regex `OPEN-Q[0-9]+(\.[0-9]+)?` (Plan 04-01 fix). Decimal-ID is NOT silently dropped.
- **C5 prep** — Citation line numbers must be REAL (not fabricated) so A14 SAMPLE pass succeeds.
- **C6 (carried)** — Both rows single-owner only.
- **C7 (LOW)** — Action wording is platform-neutral ("edit file at path").
- **C8 (LOW)** — Proposed-default fields stay register-only. Recommendations track REQUIREMENTS.md verbatim text without prescribing destinations beyond what REQUIREMENTS already specifies.

**Output:** OPEN-06 + OPEN-07 sections populated; ~2-3 new register rows; cumulative register total now ~23-25 rows (post-cross-AI-C6 split). Final pass at Plan 04-05 synthesis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/OPEN-QUESTIONS.md
@.planning/phases/04-open-questions/04-CONTEXT.md
@.planning/phases/04-open-questions/04-DISCUSSION-LOG.md
@.planning/phases/04-open-questions/04-REVIEWS.md
@.planning/phases/04-open-questions/04-01-PLAN.md
@.planning/phases/04-open-questions/04-02-PLAN.md
@.planning/phases/04-open-questions/04-03-PLAN.md
@.planning/phases/04-open-questions/04-03-SUMMARY.md
@.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh

<interfaces>
**OPEN-06 body — 1-2 rows on `/refine-<skill>` resolution.** Per D-52 carried, recommended default verbatim from REQUIREMENTS.md OPEN-06 = "build single parameterised command" + namespace recommendation `/dydx-refine-<skill>`. Plan 04-04 emits as 2 rows for clarity (decision-A + sub-decision-B): OPEN-Q21 main + OPEN-Q21.1 namespace sub-decision; both carry the same Owning phase + Resolution path.

OPEN-Q21 — `/refine-<skill>` resolution: build-single-parameterised-command vs delete-orphan-references
- Question: Should the v0.3.0 `/refine-<skill>` orphan references be (a) collapsed into a single parameterised `refine.md` command per DESIGN-04 carried, OR (b) deleted entirely (no refine workflow shipped)?
- Citations: `.planning/REQUIREMENTS.md:95` (OPEN-06 verbatim), `.planning/DESIGN.md:<DESIGN-04 line>`, `.planning/AUDIT.md:<AUDIT-04 referenced-but-missing artefacts /refine-<skill> line>`
- Owning phase: Phase 1
- Verification owner: Phase 1 / Jason
- Severity: INFORMATIONAL
- Resolution path: policy-pending-sign-off
- Proposed default (verbatim recommendation per REQUIREMENTS.md OPEN-06; D-52 sub-fields embedded):
  - **Recommendation:** "build single parameterised command" (option a) per REQUIREMENTS.md OPEN-06 verbatim.
  - **Decision deadline:** before Phase 1 plan-phase locks Foundations build (v2.1).
  - **Acceptance signal:** Jason explicit go-ahead in chat or commit message ("approved" or equivalent — same pattern as Phase approval gates).
  - **Fallback-if-undecided:** v2.1 ships with `/refine-<skill>` orphan references intact but undocumented (option c — punt); revisit at v2.2 kickoff.
- Status: proposed

OPEN-Q21.1 — `/refine-<skill>` namespace decision (sub-row; only relevant if OPEN-Q21 = build)
- Question: If OPEN-Q21 = build (option a), what is the canonical command namespace — `/dydx-refine-<skill>` (plugin-prefixed; recommended) OR `/refine-<skill>` (unprefixed; risks clash with other plugins)?
- Citations: `.planning/REQUIREMENTS.md:95`, `.planning/DESIGN.md:<DESIGN-04 line>`
- Owning phase: Phase 1
- Verification owner: Phase 1 / Jason
- Severity: INFORMATIONAL
- Resolution path: policy-pending-sign-off
- Proposed default:
  - **Recommendation:** `/dydx-refine-<skill>` (plugin-prefixed; avoids clash with other plugins' `/refine-*` commands) per CONTEXT.md D-52.
  - **Decision deadline:** before Phase 1 plan-phase locks Foundations build (v2.1).
  - **Acceptance signal:** Jason explicit go-ahead in chat or commit message.
  - **Fallback-if-undecided:** unprefixed `/refine-<skill>`; revisit if clash detected post-ship.
  - **Conditional:** only relevant if OPEN-Q21 = build; if OPEN-Q21 = delete, this row is superseded.
- Status: proposed

**OPEN-07 body — 1 canonical row (OPEN-Q22 strictly per cross-reference).** Per D-52 + D-49 carried.

OPEN-Q22 — Plugin self-test scope: smoke tests vs defer
- Question: Should v2.1 ship with plugin self-tests (smoke tests for hooks + frontmatter validator via `pytest` on the plugin's own correctness, target `dydx-delivery/tests/`) OR defer (v2.1 ships without self-tests; revisit at v2.2)?
- Citations: `.planning/REQUIREMENTS.md:96` (OPEN-07 verbatim), `.planning/DESIGN.md:<DESIGN-04 plugin surfaces line>`, `.planning/CHANGELIST.md:<Phase 9 mini-table line>` (Phase 9 deliverable item g)
- Owning phase: Phase 9
- Verification owner: Phase 9 / dev
- Severity: INFORMATIONAL
- Resolution path: policy-pending-sign-off
- Proposed default (verbatim recommendation per REQUIREMENTS.md OPEN-07; D-52 sub-fields embedded):
  - **Recommendation:** "smoke tests for hooks + frontmatter validator via `pytest` on the plugin's own correctness" (recommended; pytest at `dydx-delivery/tests/` per DESIGN-04 carried). Owning phase = Phase 9 (Surfaces) per CHANGE-04.
  - **Decision deadline:** before Phase 9 plan-phase locks Surfaces build (v2.6).
  - **Acceptance signal:** Jason explicit go-ahead in chat or commit message.
  - **Fallback-if-undecided:** v2.1 ships without self-tests; revisit at v2.2.
- Status: proposed

**ID-numbering directive (carried from Plan 04-03 SUMMARY.md cross-reference):**
- Plan 04-03 emitted forward cross-reference: `See OPEN-Q22` (in OPEN-03 body)
- Plan 04-04 MUST therefore assign OPEN-Q22 to the OPEN-07 row (NOT to OPEN-06 rows)
- OPEN-06 first row = OPEN-Q21; OPEN-06 sub-row = OPEN-Q21.1 (decimal-ID — counted by cross-AI C2 decimal-ID-aware regex in A4/A10/A14)
- OPEN-07 row = OPEN-Q22 strictly

**Append-only invariant:** This plan modifies ONLY the bodies of `## OPEN-06` and `## OPEN-07`. Must NOT touch other H2 sections.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve canonical line numbers + write OPEN-06 section body — 2 rows on `/refine-<skill>` resolution (build vs orphan-delete + namespace sub-decision OPEN-Q21.1 decimal-ID)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01..05 populated by 04-02 + 04-03; OPEN-06/07 still carry placeholder notes)
    - .planning/REQUIREMENTS.md (line ~95 — OPEN-06 verbatim text; resolve via `grep -nF '**OPEN-06**' .planning/REQUIREMENTS.md`)
    - .planning/DESIGN.md — DESIGN-04 plugin surfaces (1 parameterised refine.md command); resolve via `grep -nE '^### DESIGN-04:' .planning/DESIGN.md`
    - .planning/AUDIT.md — AUDIT-04 referenced-but-missing artefacts (/refine-<skill> orphan references); resolve via `grep -nF '/refine-' .planning/AUDIT.md`
    - .planning/phases/04-open-questions/04-CONTEXT.md §"D-52 Policy-decisions" + §"Specifics" — "OPEN-06 + OPEN-07 policy"
    - .planning/phases/04-open-questions/04-03-SUMMARY.md (numbering directive: OPEN-Q22 reserved for OPEN-07)
    - .planning/phases/04-open-questions/04-REVIEWS.md (cross-AI review concerns)
  </read_first>
  <rationale>
    Cross-AI C2 (HIGH carried): OPEN-Q21.1 decimal-ID row is REQUIRED by namespace sub-decision; Plan 04-01 A4/A10/A14 regex `OPEN-Q[0-9]+(\.[0-9]+)?` ensures it's counted. Without C2 fix, this row would be silently dropped.
    Cross-AI C7: platform-neutral wording.
    Cross-AI C8: proposed-default recommendation tracks REQUIREMENTS.md OPEN-06 verbatim; no destination overreach.
  </rationale>
  <action>
    **Step 1 — Resolve canonical line numbers:**
    ```bash
    grep -nF '**OPEN-06**' .planning/REQUIREMENTS.md
    grep -nE '^### DESIGN-04:' .planning/DESIGN.md
    grep -nF '/refine-' .planning/AUDIT.md | head -5
    ```
    Substitute resolved line numbers into citation fields. NEVER fabricate.

    **Step 2 — Edit file at path `.planning/OPEN-QUESTIONS.md`** — REPLACE the populated-by note under `## OPEN-06: /refine-<skill> resolution` with:

    Section preamble:
    ```
    This section catalogues the policy decision on `/refine-<skill>` resolution per REQUIREMENTS.md OPEN-06. Per D-52 carried, both rows in this section use `Resolution path: policy-pending-sign-off` (Jason approves the recommended default; no further work needed beyond an acceptance signal). The recommended default is lifted verbatim from REQUIREMENTS.md OPEN-06: build single parameterised command (option a); namespace `/dydx-refine-<skill>` (plugin-prefixed; avoids clash). Each row's Proposed-default field embeds the D-52 sub-fields (Decision deadline / Acceptance signal / Fallback-if-undecided) in markdown-list form. The namespace sub-decision is emitted as decimal-ID OPEN-Q21.1 (per cross-AI C2 decimal-ID-aware regex in structural-check A4/A10/A14).
    ```

    Then emit OPEN-Q21 (main decision) + OPEN-Q21.1 (namespace sub-decision) using verbatim row content from `<interfaces>` block above. Each row uses per-row block format (D-47 9-field schema) with the Proposed-default field containing 4 nested sub-fields per D-52:
    - **Recommendation** (verbatim from REQUIREMENTS.md OPEN-06)
    - **Decision deadline** (named v2.x phase before which decision must be reached)
    - **Acceptance signal** (explicit signal pattern)
    - **Fallback-if-undecided** (what ships if deadline passes)

    The 2nd row (OPEN-Q21.1) additionally adds a **Conditional** sub-field marking it relevant only if OPEN-Q21 = build.

    **Implementing decisions:** D-46 (OPEN-06 H2), D-47 (closed 9-field schema), D-48 (severity INFORMATIONAL — recommended default is shippable), D-49 (resolution = `policy-pending-sign-off`), D-52 (sub-fields embedded in Proposed-default; recommended default verbatim), D-14 carried; cross-AI C2 (decimal-ID row counted), C5 prep, C6 carried (single-owner), C7 (platform-neutral), C8 (no overreach).
  </action>
  <verify>
    <automated>OPEN6_BODY=$(awk -v hdr='## OPEN-06: /refine-<skill> resolution' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN6_ROW_COUNT=$(printf '%s\n' "$OPEN6_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+(\.[0-9]+)?\*\*') && [ "$OPEN6_ROW_COUNT" -ge 1 ] && BAD_RES=$(printf '%s\n' "$OPEN6_BODY" | grep -nE 'Resolution path:[[:space:]]' | grep -vE 'Resolution path:[[:space:]]*policy-pending-sign-off' || true) && [ -z "$BAD_RES" ] && BAD_OWNER=$(printf '%s\n' "$OPEN6_BODY" | grep -nF 'Owning phase: Phase 1/Phase 2' || true) && [ -z "$BAD_OWNER" ] && printf '%s\n' "$OPEN6_BODY" | grep -qF 'build single parameterised command' && printf '%s\n' "$OPEN6_BODY" | grep -qF '/dydx-refine-' && printf '%s\n' "$OPEN6_BODY" | grep -qF 'Decision deadline' && printf '%s\n' "$OPEN6_BODY" | grep -qF 'Acceptance signal' && printf '%s\n' "$OPEN6_BODY" | grep -qF 'Fallback-if-undecided' && CITATION_COUNT=$(printf '%s\n' "$OPEN6_BODY" | grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`') && [ "$CITATION_COUNT" -ge "$OPEN6_ROW_COUNT" ] && echo OPEN_06_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-06: /refine-<skill> resolution` body has >= 1 register row (>= 2 if planner emits both main + namespace sub-decision)
    - All OPEN-06 Resolution-path values = `policy-pending-sign-off`
    - All OPEN-06 Severity values = INFORMATIONAL
    - **C6 hybrid-owner banned** — `grep -nF 'Owning phase: Phase 1/Phase 2'` against OPEN-06 body produces no output
    - Verbatim REQUIREMENTS.md OPEN-06 recommendation present: `grep -qF 'build single parameterised command'`
    - Namespace recommendation `/dydx-refine-` present
    - D-52 sub-fields all present (Decision deadline / Acceptance signal / Fallback-if-undecided): 3 individual `grep -qF` checks
    - **C2 decimal-ID counted** — if OPEN-Q21.1 sub-row emitted, it MUST appear in the OPEN-06 row count via the decimal-ID-aware regex `^\*\*OPEN-Q[0-9]+(\.[0-9]+)?\*\*`
    - Every OPEN-06 row carries >= 1 backtick-wrapped citation
    - All 9 fields present per row in OPEN-Q21
    - OPEN-01..05 unchanged: spot-check OPEN-Q01 + OPEN-Q06a + OPEN-Q14 + OPEN-Q17 + OPEN-Q18 still present
  </acceptance_criteria>
  <done>
    `## OPEN-06` body populated with section preamble + 2 register rows (OPEN-Q21 main decision + OPEN-Q21.1 namespace sub-decision). Both rows owner=Phase 1, resolution=`policy-pending-sign-off`, D-52 sub-fields embedded in Proposed-default. Verbatim REQUIREMENTS.md recommended default present. OPEN-Q21.1 decimal-ID counted by structural-check A4/A10/A14 per cross-AI C2 regex.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write OPEN-07 section body — single canonical row OPEN-Q22 (plugin self-test scope; cross-reference target from OPEN-03)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01..06 populated)
    - .planning/REQUIREMENTS.md (line ~96 — OPEN-07 verbatim text; resolve via `grep -nF '**OPEN-07**' .planning/REQUIREMENTS.md`)
    - .planning/DESIGN.md — DESIGN-04 plugin surfaces; resolve via `grep -nE '^### DESIGN-04:' .planning/DESIGN.md`
    - .planning/CHANGELIST.md — Phase 9 mini-table Deliverables cell (item g — optional plugin self-tests at `dydx-delivery/tests/`); resolve via `grep -nF '## Phase 9:' .planning/CHANGELIST.md` then read 30 lines
    - .planning/phases/04-open-questions/04-03-SUMMARY.md (cross-reference confirms OPEN-Q22 reserved for this row)
  </read_first>
  <rationale>
    Cross-AI C7: platform-neutral wording.
    Cross-AI C8: recommendation tracks REQUIREMENTS.md OPEN-07 verbatim; the `dydx-delivery/tests/` destination is named in REQUIREMENTS.md itself, so it stays in scope.
  </rationale>
  <action>
    Edit file at path `.planning/OPEN-QUESTIONS.md` — REPLACE the populated-by note under `## OPEN-07: Plugin self-test scope` with:

    Section preamble:
    ```
    This section catalogues the policy decision on plugin self-test scope per REQUIREMENTS.md OPEN-07. Per D-52 carried, the row uses `Resolution path: policy-pending-sign-off` (Jason approves recommended default). The recommended default is lifted verbatim from REQUIREMENTS.md OPEN-07: smoke tests for hooks + frontmatter validator via `pytest` on the plugin's own correctness; owning phase = Phase 9 (Surfaces) per CHANGE-04. The Proposed-default field embeds the D-52 sub-fields (Decision deadline / Acceptance signal / Fallback-if-undecided) in markdown-list form.

    **Cross-reference:** This row (OPEN-Q22) is the canonical home for plugin self-test scope. OPEN-03 (REQUIREMENTS.md OPEN-03 4th sub-item carried `plugin self-test scope (smoke tests for hooks + frontmatter validator vs none)` mention) cross-references here per D-50 source-merging discipline (single canonical row; multi-source citation).
    ```

    Then emit **OPEN-Q22** (the row ID is locked by the OPEN-03 cross-reference from Plan 04-03; the executor MUST use exactly `**OPEN-Q22**` for the row ID line):

    ```markdown
    **OPEN-Q22** — Plugin self-test scope: smoke tests vs defer

    - **Question:** Should v2.1 ship with plugin self-tests (smoke tests for hooks + frontmatter validator via `pytest` on the plugin's own correctness, target `dydx-delivery/tests/`) OR defer (v2.1 ships without self-tests; revisit at v2.2)?
    - **Source citations:** `.planning/REQUIREMENTS.md:<resolved-OPEN-07 line>`, `.planning/DESIGN.md:<resolved-DESIGN-04 line>`, `.planning/CHANGELIST.md:<resolved-Phase-9-mini-table line>`
    - **Owning phase:** Phase 9
    - **Verification owner:** Phase 9 / dev
    - **Severity:** INFORMATIONAL
    - **Resolution path:** policy-pending-sign-off
    - **Proposed default:**
      - **Recommendation:** "smoke tests for hooks + frontmatter validator via `pytest` on the plugin's own correctness" (recommended; pytest at `dydx-delivery/tests/` per DESIGN-04 carried). Owning phase = Phase 9 (Surfaces) per CHANGE-04.
      - **Decision deadline:** before Phase 9 plan-phase locks Surfaces build (v2.6).
      - **Acceptance signal:** Jason explicit go-ahead in chat or commit message ("approved" or equivalent — same pattern as Phase approval gates).
      - **Fallback-if-undecided:** v2.1 ships without self-tests; revisit at v2.2 kickoff.
    - **Status:** proposed
    ```

    **CRITICAL — verify the row ID is exactly `OPEN-Q22`** (not Q21, Q23, or Q22.1) so OPEN-03's `See OPEN-Q22` cross-reference resolves.

    **Implementing decisions:** D-46 (OPEN-07 H2), D-47 (closed 9-field schema), D-48 (severity INFORMATIONAL), D-49 (resolution = `policy-pending-sign-off`), D-50 (cross-reference target — single canonical row), D-52 (sub-fields embedded; recommended default verbatim from REQUIREMENTS.md), D-14 carried; cross-AI C5 prep, C6 carried (single-owner), C7 (platform-neutral), C8 (no overreach).
  </action>
  <verify>
    <automated>OPEN7_BODY=$(awk -v hdr='## OPEN-07: Plugin self-test scope' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN7_ROW_COUNT=$(printf '%s\n' "$OPEN7_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+\*\*') && [ "$OPEN7_ROW_COUNT" = "1" ] && printf '%s\n' "$OPEN7_BODY" | grep -qF '**OPEN-Q22**' && printf '%s\n' "$OPEN7_BODY" | grep -qF 'Resolution path: policy-pending-sign-off' && printf '%s\n' "$OPEN7_BODY" | grep -qF 'Owning phase: Phase 9' && printf '%s\n' "$OPEN7_BODY" | grep -qF 'pytest' && printf '%s\n' "$OPEN7_BODY" | grep -qF 'Decision deadline' && printf '%s\n' "$OPEN7_BODY" | grep -qF 'Acceptance signal' && printf '%s\n' "$OPEN7_BODY" | grep -qF 'Fallback-if-undecided' && CITATION_COUNT=$(printf '%s\n' "$OPEN7_BODY" | grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`') && [ "$CITATION_COUNT" -ge 1 ] && echo OPEN_07_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-07: Plugin self-test scope` body has exactly 1 register row
    - The row ID is exactly `OPEN-Q22`: `grep -qF '**OPEN-Q22**' .planning/OPEN-QUESTIONS.md` succeeds within OPEN-07 body
    - Owning phase = `Phase 9` (single-owner per cross-AI C6)
    - Resolution path = `policy-pending-sign-off`
    - Severity = INFORMATIONAL
    - Verbatim REQUIREMENTS.md recommendation substring present (`pytest` and `smoke tests for hooks + frontmatter validator`): 2 individual `grep -qF` checks
    - D-52 sub-fields all present
    - >= 1 backtick-wrapped citation
    - All 9 fields present per row
    - OPEN-03 cross-reference resolves: `OPEN-Q22` appears in BOTH OPEN-03 body AND OPEN-07 body — `grep -cF 'OPEN-Q22' .planning/OPEN-QUESTIONS.md` returns >= 2
  </acceptance_criteria>
  <done>
    `## OPEN-07: Plugin self-test scope` body populated with section preamble + single canonical row OPEN-Q22 (ID locked by OPEN-03 forward cross-reference). Owner=Phase 9 (single-owner), resolution=`policy-pending-sign-off`, D-52 sub-fields embedded. Verbatim REQUIREMENTS.md recommended default present. Cross-reference from OPEN-03 now resolves correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run mid-phase structural-check + write Wave 4 SUMMARY.md</name>
  <files>.planning/phases/04-open-questions/04-04-SUMMARY.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01..07 all populated; only Executive Summary + How-to-read + Appendix A/B/C remain unwritten)
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <rationale>
    Wave 4 mid-phase invariant: A4 floor `>= 21` (cross-AI C4) is now solidly met (~23-25 rows). A10 (Appendix B 1:1) still fails (Appendix B empty). A11 still fails (Appendix C missing). A13 narrowed regex still catches `Populated by 04-NN-PLAN.md` notes in Executive Summary + How-to-read + Appendix A/B/C. A14 SAMPLE pass should succeed (citations are real).
  </rationale>
  <action>
    **Step 1 — Run mid-phase structural-check:**
    ```bash
    bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo "EXIT_CODE=$?"
    ```

    Expected first-fail: A4 PASSES if cumulative register total >= 21 (~23-25 expected). A10 (Appendix B 1:1 cardinality) FAILS because Appendix B is still empty (0 rows != register_total). OR A11 (Appendix C reconciliation block missing). OR A13 (populated-by notes still in Executive Summary + How-to-read + Appendix A/B/C). Record verbatim. A14 SAMPLE pass MUST succeed.

    **Step 2 — Spot-check counts:**
    ```bash
    grep -cE '^\*\*OPEN-Q[0-9]+(\.[0-9]+)?\*\*' .planning/OPEN-QUESTIONS.md  # expect >= 23 (post-cross-AI-C6 split)
    grep -cE 'Owning phase:[[:space:]]*Phase 9' .planning/OPEN-QUESTIONS.md  # expect >= 1 (OPEN-Q22)
    grep -cF 'policy-pending-sign-off' .planning/OPEN-QUESTIONS.md  # expect >= 2 (OPEN-Q21 + OPEN-Q22 minimum)
    grep -cF 'OPEN-Q22' .planning/OPEN-QUESTIONS.md  # expect >= 2 (OPEN-03 cross-reference + OPEN-07 row ID)
    grep -cF 'OPEN-Q21.1' .planning/OPEN-QUESTIONS.md  # expect >= 1 (decimal-ID per cross-AI C2)
    grep -cF 'Decision deadline' .planning/OPEN-QUESTIONS.md  # expect >= 2
    grep -cF 'Acceptance signal' .planning/OPEN-QUESTIONS.md  # expect >= 2
    grep -cF 'Fallback-if-undecided' .planning/OPEN-QUESTIONS.md  # expect >= 2
    grep -nF 'Owning phase: Phase 1/Phase 2' .planning/OPEN-QUESTIONS.md  # MUST be empty (cross-AI C6)
    ```

    **Step 3 — Create file at path `.planning/phases/04-open-questions/04-04-SUMMARY.md`** using template. Required content:
    1. Plan ID + execution date + outcome (`Plan 04-04 — Wave 4 — executed 2026-05-10 — outcome: COMPLETE`)
    2. Sections populated: OPEN-06 (1-2 rows; verbatim REQUIREMENTS.md OPEN-06 recommendation; D-52 sub-fields embedded; OPEN-Q21.1 decimal-ID per cross-AI C2) + OPEN-07 (1 row OPEN-Q22; cross-reference target resolved). Total 2-3 new rows.
    3. Cumulative register total now ~23-25 rows (post-cross-AI-C6 split); structural-check A4 PASSES (>= 21 floor met); first-fail moves to Appendix B / Appendix C / placeholder assertions.
    4. Closed-enum verification: severity / resolution-path / status / owning-phase counts. NO hybrid-owner literal anywhere (cross-AI C6).
    5. Decimal-ID OPEN-Q21.1 confirmed counted by A4/A10/A14 (cross-AI C2 regex).
    6. Mid-phase structural-check first-fail message verbatim; A14 SAMPLE result.
    7. Decisions implemented: D-46, D-47, D-48, D-49, D-50, D-52, D-14; cross-AI C2 / C5 / C6 / C7 / C8.
    8. Hand-off to Wave 5 (Plan 04-05 owns synthesis: preamble + Executive Summary 3-table + How-to-read 5-paragraph + Appendix A per-phase rollup + Appendix B traceability + Appendix C REAL reconciliation per cross-AI C3 + ROADMAP success-criteria walk + final structural-check exits 0 + reviewer-ready terminal-state + Appendix B numerical sort per cross-AI G2).

    **Implementing decisions:** D-44 carry-equivalent (no new D-N introduced); D-50 cross-reference resolution verified; D-52 sub-field discipline applied to OPEN-Q21 + OPEN-Q22.
  </action>
  <verify>
    <automated>EXIT_CODE=0; bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh > /tmp/wave4_check.out 2>&1 || EXIT_CODE=$?; echo "Wave 4 structural-check EXIT=$EXIT_CODE"; cat /tmp/wave4_check.out; ROWS=$(grep -cE '^\*\*OPEN-Q[0-9]+(\.[0-9]+)?\*\*' .planning/OPEN-QUESTIONS.md); echo "ROWS=$ROWS"; [ "$ROWS" -ge 23 ] && PHASE_9_OWNER=$(grep -cE 'Owning phase:[[:space:]]*Phase 9' .planning/OPEN-QUESTIONS.md) && [ "$PHASE_9_OWNER" -ge 1 ] && POLICY_RES=$(grep -cF 'policy-pending-sign-off' .planning/OPEN-QUESTIONS.md) && [ "$POLICY_RES" -ge 2 ] && Q22_REFS=$(grep -cF 'OPEN-Q22' .planning/OPEN-QUESTIONS.md) && [ "$Q22_REFS" -ge 2 ] && Q21_DECIMAL=$(grep -cF 'OPEN-Q21.1' .planning/OPEN-QUESTIONS.md) && [ "$Q21_DECIMAL" -ge 1 ] && BAD_OWNER=$(grep -nF 'Owning phase: Phase 1/Phase 2' .planning/OPEN-QUESTIONS.md || true) && [ -z "$BAD_OWNER" ] && grep -qF 'Decision deadline' .planning/OPEN-QUESTIONS.md && grep -qF 'Acceptance signal' .planning/OPEN-QUESTIONS.md && grep -qF 'Fallback-if-undecided' .planning/OPEN-QUESTIONS.md && test -f .planning/phases/04-open-questions/04-04-SUMMARY.md && grep -qF 'OPEN-06' .planning/phases/04-open-questions/04-04-SUMMARY.md && grep -qF 'OPEN-07' .planning/phases/04-open-questions/04-04-SUMMARY.md && grep -qF '04-05' .planning/phases/04-open-questions/04-04-SUMMARY.md && grep -qF 'C2' .planning/phases/04-open-questions/04-04-SUMMARY.md && echo TASK_3_OK</automated>
  </verify>
  <acceptance_criteria>
    - structural-check ran (exits NON-ZERO mid-phase per Wave 4 invariant — full pass at 04-05)
    - Cumulative register total >= 23 (post-cross-AI-C6 split)
    - >= 1 row with `Owning phase: Phase 9` (OPEN-Q22)
    - >= 2 rows with `Resolution path: policy-pending-sign-off`
    - `OPEN-Q22` appears >= 2 times (OPEN-03 cross-reference + OPEN-07 row ID)
    - **C2 decimal-ID counted** — `OPEN-Q21.1` literal appears >= 1 time in document
    - **C6 hybrid-owner banned globally** — `grep -nF 'Owning phase: Phase 1/Phase 2'` document-wide produces no output
    - D-52 sub-fields all present (Decision deadline / Acceptance signal / Fallback-if-undecided)
    - SUMMARY.md exists at `.planning/phases/04-open-questions/04-04-SUMMARY.md`
    - SUMMARY.md mentions OPEN-06 + OPEN-07 + cross-AI C2: 3 individual `grep -qF` checks
    - SUMMARY.md cites first-fail verbatim
    - SUMMARY.md names downstream consumer (Plan 04-05 / Wave 5)
  </acceptance_criteria>
  <done>
    Wave 4 lands ~2-3 register rows under OPEN-06 + OPEN-07. Cumulative register total now ~23-25 rows post-cross-AI-C6 split (well above A4 >= 21 floor). Structural-check first-fail moves to Appendix B / Appendix C / placeholder assertions — full pass at Plan 04-05 synthesis. SUMMARY.md committed; OPEN-03 forward cross-reference confirmed resolved; OPEN-Q21.1 decimal-ID confirmed counted by cross-AI C2 regex.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OPEN-Q22 ID locked by OPEN-03 cross-reference | Tampering class — extreme. If Plan 04-04 author numbers OPEN-07 as Q21 / Q23 / Q22.1, the OPEN-03 forward cross-reference (`See OPEN-Q22`) breaks. Mitigation: Task 2 acceptance criteria assert the literal `**OPEN-Q22**` string within OPEN-07 body; cross-reference grep against full document `grep -cF 'OPEN-Q22' .planning/OPEN-QUESTIONS.md` returns >= 2. |
| Decimal-ID row OPEN-Q21.1 ↔ structural-check counting (cross-AI C2) | Tampering class. Without the C2 regex fix in Plan 04-01, OPEN-Q21.1 would be silently dropped. Mitigation: A4/A10/A14 use `OPEN-Q[0-9]+(\.[0-9]+)?`; acceptance criterion `grep -cF 'OPEN-Q21.1'` returns >= 1. |
| D-52 sub-field embedding ↔ closed 9-field schema | The 4 D-52 sub-fields (Recommendation / Decision deadline / Acceptance signal / Fallback-if-undecided) are embedded INSIDE the Proposed-default field — NOT as separate top-level row fields — to keep the 9-field schema closed. |
| Recommended default verbatim drift | Tampering class. Proposed-default field MUST quote REQUIREMENTS.md OPEN-06 + OPEN-07 verbatim. Mitigation: acceptance criteria require substring matches on the verbatim phrases. |
| Hybrid-owner reversion (cross-AI C6 carried) | Tampering class. Mitigation: A8 enum-tightened in Plan 04-01 + acceptance criterion `grep -nF 'Phase 1/Phase 2'` empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | OPEN-Q22 ID assignment | mitigate | Task 2 acceptance criteria explicit + cross-reference document-wide grep count |
| T-04-15 | Tampering | OPEN-Q21.1 decimal-ID counting (cross-AI C2) | mitigate | A4/A10/A14 regex `OPEN-Q[0-9]+(\.[0-9]+)?` + acceptance criterion |
| T-04-16 | Tampering | D-52 sub-fields embedding | mitigate | Tasks 1+2 emit nested markdown-list under Proposed-default; acceptance criteria check 3 sub-field labels |
| T-04-17 | Tampering | Verbatim recommended default | mitigate | Acceptance criteria require substring matches on REQUIREMENTS.md verbatim phrases |
| T-04-18 | Tampering | Hybrid-owner reversion (cross-AI C6) | mitigate | A8 enum-tightened + acceptance criterion empty |
</threat_model>

<verification>
- OPEN-06 has 1-2 rows (incl. decimal-ID OPEN-Q21.1); all `policy-pending-sign-off` resolution; D-52 sub-fields embedded; verbatim REQUIREMENTS.md recommendation
- OPEN-07 has 1 row with ID exactly `OPEN-Q22`; Phase 9 owner; D-52 sub-fields embedded; verbatim REQUIREMENTS.md recommendation
- OPEN-Q22 appears >= 2 times in document (cross-reference resolved)
- OPEN-Q21.1 decimal-ID appears >= 1 time (cross-AI C2 regex counts it)
- Cumulative register total >= 23 rows
- NO hybrid-owner literal anywhere
- Structural-check first-fail moved to Appendix-B/C/placeholder assertions
- SUMMARY.md committed
</verification>

<success_criteria>
- `## OPEN-06` populated with 1-2 rows; all `policy-pending-sign-off`; D-52 sub-fields present; OPEN-Q21.1 decimal-ID counted
- `## OPEN-07` populated with single canonical row `OPEN-Q22`; Phase 9 owner; D-52 sub-fields present
- OPEN-03 forward cross-reference (`See OPEN-Q22`) resolves correctly
- All severity / resolution-path / status / owning-phase fields exact-match closed enums (single-owner only)
- Mid-phase structural-check ran; first-fail recorded
- `.planning/phases/04-open-questions/04-04-SUMMARY.md` committed
</success_criteria>

<output>
After completion, create `.planning/phases/04-open-questions/04-04-SUMMARY.md` per template.
</output>
