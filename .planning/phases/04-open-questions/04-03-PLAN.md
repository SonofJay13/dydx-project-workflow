---
plan_id: 04-03
phase: 04-open-questions
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
requirements: [OPEN-03, OPEN-04, OPEN-05]
files_modified:
  - .planning/OPEN-QUESTIONS.md
  - .planning/phases/04-open-questions/04-03-SUMMARY.md
autonomous: true
tags: [open-questions, OPEN-03, OPEN-04, OPEN-05, design-deferred, hub-link, coda-templates, design-only]

must_haves:
  truths:
    - "OPEN-QUESTIONS.md `## OPEN-03: Design-decision-deferred items` populated with ~3 register rows in closed 9-field schema (D-47): risk-multiplier defaults pending dYdX-historical validation (Phase 4 / decide-before-Phase-4) + frontmatter migration cutover date (Phase 1 / decide-before-Phase-1) + status-lifecycle survey check (Phase 1 / decide-before-Phase-1)."
    - "OPEN-QUESTIONS.md `## OPEN-04: Hub-link backfill rollout coordination` populated with single canonical row per D-51 — Owning phase=Phase 1 / Verification owner=Phase 1 / Jason / Severity=INFORMATIONAL / Resolution path=live-workstream-pointer / Proposed default verbatim from REQUIREMENTS.md OPEN-04 / Status=proposed."
    - "OPEN-QUESTIONS.md `## OPEN-05: Standard Coda templates v2 must author` populated with 3 register rows: brain-mirror doc template + task-table template + 00_HUB.md Coda block schema. All Owning phase=Phase 8 / Resolution path=`Coda-template-authoring (Phase 8)` per D-49."
    - "Plugin self-test scope is dedup'd per D-50: section-canonicalised under OPEN-07; OPEN-03 cross-references OPEN-Q22 (assigned at Wave 4)."
    - "Every OPEN-03/OPEN-04/OPEN-05 register row carries closed 9-field schema per D-47; severity / resolution-path / status / owning-phase exact-match closed enums."
  artifacts:
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "OPEN-03 + OPEN-04 + OPEN-05 sections populated; populated-by notes for these three sections REPLACED"
      contains: "## OPEN-03 body: ~3 rows; ## OPEN-04 body: 1 canonical row; ## OPEN-05 body: 3 rows; total 7 new rows; cumulative register total ~19-20 rows"
  key_links:
    - from: "OPEN-Q14 (risk-multiplier defaults)"
      to: "CHANGELIST.md Phase 4 mini-table Research-blocked cell + Appendix E bullet 7"
      via: "Multi-source citation per D-50"
      pattern: "CHANGELIST.md is closed list authority"
    - from: "OPEN-Q17 (OPEN-04 hub-link backfill)"
      to: "Jason's parallel workstream tracker (location TBD per D-51)"
      via: "Resolution path = live-workstream-pointer"
      pattern: "single canonical row per D-51; sub-row deferred"
    - from: "OPEN-Q18..Q20 (OPEN-05 Coda templates)"
      to: "Phase 8 + DESIGN-26/27 carried"
      via: "Citations to DESIGN.md + REQUIREMENTS.md OPEN-05"
      pattern: "Phase 8 owner; Coda-template-authoring resolution"
---

<objective>
Wave 3: populate `.planning/OPEN-QUESTIONS.md` `## OPEN-03: Design-decision-deferred items` AND `## OPEN-04: Hub-link backfill rollout coordination` AND `## OPEN-05: Standard Coda templates v2 must author` with closed 9-field register rows.

**Purpose:** Lands the design-deferred backlog (OPEN-03) + the live-workstream-pointer row (OPEN-04 — single canonical row per D-51) + the Coda-template-authoring rows (OPEN-05 — Phase 8 owner). Cross-cutting concern: plugin self-test scope appears in REQUIREMENTS.md OPEN-03 4th sub-item AND REQUIREMENTS.md OPEN-07. Per D-50 dedup discipline this plan section-canonicalises the row under OPEN-07 (its primary home) and emits a forward cross-reference under OPEN-03 to OPEN-Q22 (assigned at Wave 4).

**Output:** OPEN-03 / OPEN-04 / OPEN-05 sections populated; ~7 new register rows; cumulative register total now ~19-20 rows. Structural-check still fails. Final pass at Plan 04-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/OPEN-QUESTIONS.md
@.planning/phases/04-open-questions/04-CONTEXT.md
@.planning/phases/04-open-questions/04-01-PLAN.md
@.planning/phases/04-open-questions/04-02-PLAN.md
@.planning/phases/04-open-questions/04-02-SUMMARY.md
@.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh

<interfaces>
This plan emits OPEN-Q14..Q20 (7 new rows). Cumulative register total expected ~19-20 (Wave 2: 12-13 + Wave 3: 7).

**OPEN-03 body (3 rows + cross-ref to OPEN-07):**

OPEN-Q14 — risk-multiplier defaults / Phase 4 / decide-before-Phase-4 / INFORMATIONAL / proposed
- Question: Are default risk multipliers (L=1.1 / M=1.3 / H=1.6 per DESIGN-22) validated against dYdX historical project costs?
- Citations: REQUIREMENTS.md:92 + CHANGELIST.md:283 + DESIGN.md:DESIGN-22 line
- Proposed default: 1.1/1.3/1.6 per DESIGN-22 carried; decision-deadline before Phase 4 plan-phase locks Stage 6.

OPEN-Q15 — Frontmatter migration cutover date / Phase 1 / decide-before-Phase-1 / INFORMATIONAL / proposed
- Question: When is migration v0.3.0 → v2 frontmatter triggered? Tied to specific v2.x ship date OR purely opt-in per CR (per DESIGN-08)?
- Citations: REQUIREMENTS.md:92 + DESIGN.md:DESIGN-08 line + CHANGELIST.md:Appendix D rule line
- Proposed default: opt-in per CR (no global cutover); DESIGN-08 + CHANGELIST.md Appendix D rule 2; v2 readers tolerate v0.3.0 lenient mode permanently.

OPEN-Q16 — Status-lifecycle survey orphan check / Phase 1 / decide-before-Phase-1 / INFORMATIONAL / proposed
- Question: Status-lifecycle survey confirmed no live `status:` value gets orphaned by v2 lifecycle (`draft → client_review → approved → archived`)?
- Citations: REQUIREMENTS.md:92 + DESIGN.md:DESIGN-08 status-survey line + AUDIT.md:AUDIT-01.2 generate-sow line
- Proposed default: per DESIGN-08 — sampled v0.3.0 sources show only `{draft, client_review, approved}`; `archived` additive; no orphans. Phase 1 re-runs survey at v2.1 kickoff.

Cross-reference blockquote (after the 3 rows):
> **Plugin self-test scope** (REQUIREMENTS.md OPEN-03 4th sub-item) is section-canonicalised under `## OPEN-07: Plugin self-test scope` per D-50 source-merging discipline. See OPEN-Q22 (assigned at Wave 4 / Plan 04-04).

**OPEN-04 body (1 canonical row per D-51):**

OPEN-Q17 — Hub-link backfill rollout coordination / Phase 1 / live-workstream-pointer / INFORMATIONAL / proposed
- Question: Which clients have `<Client> Brain/00_HUB.md` carrying the `Documentation:` Drive link, and which need backfill before v2.x ship gates? Where is the live workstream tracker?
- Citations: REQUIREMENTS.md:93 + DESIGN.md:DESIGN-19 SOW hub-link line + research/PITFALLS.md:MOD-1 line
- Verification owner: Phase 1 / Jason
- Proposed default: graceful halt at Stage 9 only — does not halt other stages, per MOD-1 prevention (verbatim from REQUIREMENTS.md OPEN-04). Pointer source-of-truth: live `00_HUB.md` files + Jason's parallel workstream tracker (location TBD; sub-row to be added when tracker materialises during v2.1 Foundations build kickoff per D-51).

Closing paragraph: "Sub-row reservation: a second row may be added during v2.1 kickoff to name the live workstream tracker source-of-truth..."

**OPEN-05 body (3 rows; all Phase 8 owner):**

OPEN-Q18 — brain-mirror doc Coda template / Phase 8 / Coda-template-authoring (Phase 8) / INFORMATIONAL / proposed
- Question: Canonical Coda doc template structure for per-client brain mirror — section-per-spoke layout + Field Notes table schema + approval-watermark field shape?
- Citations: REQUIREMENTS.md:94 + DESIGN.md:DESIGN-26 line + DESIGN.md:DESIGN-27 line
- Proposed default: author-as-built during Phase 8; section-per-spoke per DESIGN-26; Field Notes input-only per DESIGN-27.

OPEN-Q19 — task-table Coda template / Phase 8 / Coda-template-authoring (Phase 8) / INFORMATIONAL / proposed
- Question: Canonical Coda task-table schema (column names + types + `keyColumns` upsert key)? How does plugin auto-introspect existing schema?
- Citations: REQUIREMENTS.md:94 + DESIGN.md:DESIGN-22 keyColumns line
- Proposed default: author-as-built; keyColumns = (client_slug, project_slug, task_slug) triple per DESIGN-22; introspection cached in 00_HUB.md (see OPEN-Q20).

OPEN-Q20 — Default `00_HUB.md` Coda block schema / Phase 8 / Coda-template-authoring (Phase 8) / INFORMATIONAL / proposed
- Question: Canonical 00_HUB.md Coda block YAML schema (`coda_brain_doc:` / `coda_tasks_table:` / `coda_tasks_schema:` cache)?
- Citations: REQUIREMENTS.md:94 + DESIGN.md:DESIGN-22 Coda block schema line
- Proposed default: author-as-built; YAML keys per DESIGN-22 carried; defines the persistent Stage 6 ↔ Stage 11 contract.

**Append-only invariant:** This plan modifies ONLY the bodies of `## OPEN-03`, `## OPEN-04`, `## OPEN-05`. Must NOT touch other H2 sections.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve canonical line numbers + write OPEN-03 section body — 3 design-deferred rows + cross-reference to OPEN-07</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01 + OPEN-02 populated by 04-02)
    - .planning/REQUIREMENTS.md (line ~92 — OPEN-03 verbatim text)
    - .planning/CHANGELIST.md (line ~283 — Appendix E bullet 7 risk-multiplier; resolve via `grep -nF 'risk-multiplier defaults pending dYdX-historical' .planning/CHANGELIST.md`)
    - .planning/DESIGN.md — DESIGN-08 + DESIGN-22 line numbers (resolve via `grep -nE '^### DESIGN-(08|22):' .planning/DESIGN.md`)
    - .planning/AUDIT.md — generate-sow `client_review` reference (resolve via `grep -nF 'client_review' .planning/AUDIT.md`)
    - .planning/phases/04-open-questions/04-CONTEXT.md §"Specifics" — "OPEN-03 design-deferred"; §"D-50 Source merging"
  </read_first>
  <action>
    **Step 1 — Resolve canonical line numbers:**
    ```bash
    grep -nF '**OPEN-03**' .planning/REQUIREMENTS.md
    grep -nF 'risk-multiplier defaults pending dYdX-historical' .planning/CHANGELIST.md
    grep -nE '^### DESIGN-(08|22):' .planning/DESIGN.md
    grep -nF 'client_review' .planning/AUDIT.md | head -3
    ```
    Substitute resolved line numbers into the citation fields below.

    **Step 2 — Edit `.planning/OPEN-QUESTIONS.md`** — REPLACE the populated-by note under `## OPEN-03: Design-decision-deferred items` with:

    Section preamble:
    ```
    This section catalogues design-decision-deferred items needing human input before the owning phase locks plans. Severity is INFORMATIONAL across all rows because each carries a Proposed-default that is shippable; resolution requires Jason's explicit acceptance signal (per D-49 `decide-before-Phase-<N>` enum value) within the named phase deadline. Plugin self-test scope (4th sub-item in REQUIREMENTS.md OPEN-03) is section-canonicalised under `## OPEN-07: Plugin self-test scope` per D-50 dedup discipline; cross-reference at end of this section.
    ```

    Then emit OPEN-Q14, OPEN-Q15, OPEN-Q16 in per-row block form (D-47 9-field schema). Each row contains 8 labeled fields after the row ID line:

    ```markdown
    **OPEN-Q14** — risk-multiplier defaults validated against dYdX historicals?

    - **Question:** Are the default risk multipliers (L=1.1 / M=1.3 / H=1.6 — locked structure per DESIGN-22 carried) validated against dYdX historical project costs, or imported from generic delivery-estimate research?
    - **Source citations:** `.planning/REQUIREMENTS.md:<resolved>`, `.planning/CHANGELIST.md:<resolved-Appendix-E-line>`, `.planning/DESIGN.md:<DESIGN-22 line>`
    - **Owning phase:** Phase 4
    - **Verification owner:** Phase 4 / Jason
    - **Severity:** INFORMATIONAL
    - **Resolution path:** decide-before-Phase-4
    - **Proposed default:** 1.1 / 1.3 / 1.6 (per DESIGN-22 carried structure-locked defaults). Decision-deadline: before Phase 4 plan-phase locks Stage 6 cost estimate plans.
    - **Status:** proposed
    ```

    Repeat with OPEN-Q15 (frontmatter migration cutover date — Phase 1 / decide-before-Phase-1 / INFORMATIONAL — proposed default: opt-in per CR per DESIGN-08 + CHANGELIST.md Appendix D rule 2) and OPEN-Q16 (status-lifecycle survey orphan check — Phase 1 / decide-before-Phase-1 / INFORMATIONAL — proposed default: per DESIGN-08 sampled v0.3.0 sources show `{draft, client_review, approved}`, `archived` additive only, no orphans).

    After all three rows, emit cross-reference blockquote:
    ```
    > **Plugin self-test scope** (REQUIREMENTS.md OPEN-03 4th sub-item) is section-canonicalised under `## OPEN-07: Plugin self-test scope` per D-50 source-merging discipline. See OPEN-Q22 (assigned at Wave 4 / Plan 04-04).
    ```

    Note: OPEN-Q21 lands in OPEN-06; OPEN-Q22 lands in OPEN-07 (both Wave 4). The cross-reference points forward to OPEN-Q22 — Wave 4 author MUST honour this number.

    **Implementing decisions:** D-46, D-47, D-48 (INFORMATIONAL only), D-49 (`decide-before-Phase-<N>`), D-50 (dedup → cross-reference instead of duplicate row), D-14 carried.
  </action>
  <verify>
    <automated>OPEN3_BODY=$(awk -v hdr='## OPEN-03: Design-decision-deferred items' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN3_ROW_COUNT=$(printf '%s\n' "$OPEN3_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+\*\*') && [ "$OPEN3_ROW_COUNT" -ge 3 ] && BAD_RES=$(printf '%s\n' "$OPEN3_BODY" | grep -nE 'Resolution path:[[:space:]]' | grep -vE 'Resolution path:[[:space:]]*(decide-before-Phase-[0-9]+)' || true) && [ -z "$BAD_RES" ] && printf '%s\n' "$OPEN3_BODY" | grep -qF 'OPEN-Q22' && printf '%s\n' "$OPEN3_BODY" | grep -qF 'D-50' && CITATION_COUNT=$(printf '%s\n' "$OPEN3_BODY" | grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`') && [ "$CITATION_COUNT" -ge "$OPEN3_ROW_COUNT" ] && echo OPEN_03_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-03` body has >= 3 register rows: extract section + count `^\*\*OPEN-Q[0-9]+\*\*` matches; returns >= 3
    - All OPEN-03 Resolution-path values are `decide-before-Phase-<N>` form: `grep -nE 'Resolution path:' OPEN-03 body | grep -vE 'decide-before-Phase-'` produces no output
    - All OPEN-03 Severity values are INFORMATIONAL: count returns >= 3
    - OPEN-Q14 cites both REQUIREMENTS.md AND CHANGELIST.md (multi-source per D-50): 2 individual `grep -qF` checks
    - Cross-reference present + names D-50 + names OPEN-Q22: 3 individual `grep -qF` checks against OPEN-03 body
    - OPEN-01 + OPEN-02 from prior plans unchanged: spot-check OPEN-Q01 + OPEN-Q13 still present
    - All 9 fields present per row in OPEN-Q14: 8 individual `grep -qF` checks for `Question:`, `Source citations:`, `Owning phase:`, `Verification owner:`, `Severity:`, `Resolution path:`, `Proposed default:`, `Status:`
  </acceptance_criteria>
  <done>
    `## OPEN-03` body populated with section preamble + 3 register rows in closed 9-field per-row block form + cross-reference blockquote pointing forward to OPEN-Q22 (Wave 4). Plugin-self-test scope dedup'd per D-50.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write OPEN-04 section body — single canonical row per D-51 (pointer-only + invariant; verbatim REQUIREMENTS.md proposed default)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01..03 populated)
    - .planning/REQUIREMENTS.md (line ~93 — OPEN-04 verbatim text including `graceful halt at Stage 9 only` proposed default)
    - .planning/DESIGN.md — DESIGN-19 SOW hub-link contract (resolve via `grep -nE '^### DESIGN-19:' .planning/DESIGN.md`)
    - .planning/research/PITFALLS.md — MOD-1 graceful-halt invariant (resolve via `grep -nF 'MOD-1' .planning/research/PITFALLS.md`)
    - .planning/phases/04-open-questions/04-CONTEXT.md §"D-51 Hub-link backfill" + §"Specifics" — "OPEN-04 hub-link"
  </read_first>
  <action>
    Edit `.planning/OPEN-QUESTIONS.md` — REPLACE the populated-by note under `## OPEN-04: Hub-link backfill rollout coordination` with:

    Section preamble:
    ```
    This section catalogues the rollout coordination invariant for the per-client hub-link backfill (the `Documentation:` Drive link in each `<Client> Brain/00_HUB.md`). Per D-51 (CONTEXT.md), Phase 4 does NOT enumerate clients in this register — that is a moving target on Jason's parallel workstream. The single canonical row records the invariant + pointer source-of-truth + proposed default verbatim from `.planning/REQUIREMENTS.md` OPEN-04. A sub-row may be added later when the live tracker source-of-truth materialises during v2.1 Foundations build kickoff.
    ```

    OPEN-Q17 row in per-row block form:

    ```markdown
    **OPEN-Q17** — Hub-link backfill rollout coordination

    - **Question:** Which clients have `<Client> Brain/00_HUB.md` carrying the `Documentation:` Drive link, and which need backfill before v2.x ship gates? Where is the live workstream tracker (Coda doc / shared spreadsheet / GitHub issue / TBD)?
    - **Source citations:** `.planning/REQUIREMENTS.md:<resolved-OPEN-04 line>`, `.planning/DESIGN.md:<resolved-DESIGN-19 line>`, `.planning/research/PITFALLS.md:<resolved-MOD-1 line>`
    - **Owning phase:** Phase 1
    - **Verification owner:** Phase 1 / Jason
    - **Severity:** INFORMATIONAL
    - **Resolution path:** live-workstream-pointer
    - **Proposed default:** graceful halt at Stage 9 only — does not halt other stages, per MOD-1 prevention (verbatim from `.planning/REQUIREMENTS.md` OPEN-04). Pointer source-of-truth: live `00_HUB.md` files in client folders + Jason's parallel workstream tracker (location TBD; sub-row to be added when tracker materialises during v2.1 Foundations build kickoff per D-51).
    - **Status:** proposed
    ```

    Closing paragraph:
    ```
    **Sub-row reservation:** A second row (OPEN-Q17.1 or similar) may be added during v2.1 Foundations build kickoff to name the live workstream tracker source-of-truth (Coda doc / shared spreadsheet / GitHub issue / external URL). Phase 4 explicitly does NOT snapshot this because it is a moving target; D-51 carry-forward applies (pointer-only + invariant). Reconciliation algorithm in Plan 04-05 accommodates 1-row OR 2-row OPEN-04 cardinality cleanly.
    ```

    **Implementing decisions:** D-46, D-47, D-48 (INFORMATIONAL — no halt), D-49 (`live-workstream-pointer`), D-51 (single canonical row + sub-row reservation), D-14 carried.
  </action>
  <verify>
    <automated>OPEN4_BODY=$(awk -v hdr='## OPEN-04: Hub-link backfill rollout coordination' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN4_ROW_COUNT=$(printf '%s\n' "$OPEN4_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+\*\*') && [ "$OPEN4_ROW_COUNT" = "1" ] && printf '%s\n' "$OPEN4_BODY" | grep -qF 'graceful halt at Stage 9 only' && printf '%s\n' "$OPEN4_BODY" | grep -qF 'Resolution path: live-workstream-pointer' && printf '%s\n' "$OPEN4_BODY" | grep -qF 'D-51' && printf '%s\n' "$OPEN4_BODY" | grep -qF 'Phase 1 / Jason' && CITATION_COUNT=$(printf '%s\n' "$OPEN4_BODY" | grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`') && [ "$CITATION_COUNT" -ge 1 ] && printf '%s\n' "$OPEN4_BODY" | grep -qF 'Sub-row reservation' && echo OPEN_04_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-04` body has exactly 1 register row (D-51 pointer-only): count returns 1
    - Verbatim REQUIREMENTS.md proposed default substring present: `grep -qF 'graceful halt at Stage 9 only'` succeeds
    - Resolution path = `live-workstream-pointer`: `grep -qF 'Resolution path: live-workstream-pointer'` succeeds within OPEN-04 body
    - Verification owner = `Phase 1 / Jason`: succeeds
    - Severity = INFORMATIONAL: succeeds
    - D-51 cited in body: succeeds
    - Citation count >= 1
    - Closing paragraph reserves sub-row: `grep -qF 'Sub-row reservation'` succeeds
    - OPEN-01..03 unchanged: spot-check OPEN-Q01 + OPEN-Q14 still present
  </acceptance_criteria>
  <done>
    `## OPEN-04` body populated with section preamble + single canonical row OPEN-Q17 (pointer-only per D-51) + sub-row reservation closing paragraph. Verbatim REQUIREMENTS.md proposed default present. Verification owner = `Phase 1 / Jason`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write OPEN-05 section body (3 Coda template rows; Phase 8 owner) + run mid-phase structural-check + write Wave 3 SUMMARY.md</name>
  <files>.planning/OPEN-QUESTIONS.md, .planning/phases/04-open-questions/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — OPEN-01..04 populated)
    - .planning/REQUIREMENTS.md (line ~94 — OPEN-05 verbatim text)
    - .planning/DESIGN.md — DESIGN-22 + DESIGN-26 + DESIGN-27 line numbers (resolve via `grep -nE '^### DESIGN-(22|26|27):' .planning/DESIGN.md`)
    - .planning/CHANGELIST.md — Phase 8 mini-table (resolve via `grep -nF '## Phase 8:' .planning/CHANGELIST.md`)
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    **Step 1 — Edit `.planning/OPEN-QUESTIONS.md`** — REPLACE the populated-by note under `## OPEN-05: Standard Coda templates v2 must author` with:

    Section preamble:
    ```
    This section catalogues the standard Coda templates v2 must author during Phase 8 (Sign-off + Coda mirror per CHANGELIST.md CHANGE-01). All rows owner=Phase 8; resolution=`Coda-template-authoring (Phase 8)` per D-49 enum. Severity is INFORMATIONAL across — defaults are author-as-built; no row halts plan creation in any earlier phase. Templates are emitted into `dydx-delivery/skills/sign-off-and-archive/references/` per DESIGN-26/27 carried. Templates do not exist in v0.3.0; this is a NEW Phase 8 deliverable.
    ```

    Then emit OPEN-Q18, OPEN-Q19, OPEN-Q20 in per-row block form. Each row's full content (per `<interfaces>` block above):

    ```markdown
    **OPEN-Q18** — brain-mirror doc Coda template (section-per-spoke + Field Notes table)

    - **Question:** What is the canonical Coda doc template structure for the per-client brain mirror — section-per-spoke layout (one section per stage spoke), Field Notes table schema (input-only per DESIGN-27), and approval-watermark field shape?
    - **Source citations:** `.planning/REQUIREMENTS.md:<OPEN-05 line>`, `.planning/DESIGN.md:<DESIGN-26 line>`, `.planning/DESIGN.md:<DESIGN-27 line>`
    - **Owning phase:** Phase 8
    - **Verification owner:** Phase 8 / non-dev
    - **Severity:** INFORMATIONAL
    - **Resolution path:** Coda-template-authoring (Phase 8)
    - **Proposed default:** author-as-built during Phase 8 Sign-off + Coda mirror skill build; section-per-spoke layout per DESIGN-26 carried; Field Notes table is input-only (read-by-skill, never written-by-skill) per DESIGN-27 carried.
    - **Status:** proposed
    ```

    Repeat with OPEN-Q19 (task-table Coda template — Phase 8 / Coda-template-authoring (Phase 8) — proposed default keyColumns triple per DESIGN-22) and OPEN-Q20 (default 00_HUB.md Coda block schema — Phase 8 / Coda-template-authoring (Phase 8) — proposed default canonical YAML keys per DESIGN-22).

    **Step 2 — Run mid-phase structural-check:**
    ```bash
    bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo "EXIT_CODE=$?"
    ```
    Expected first-fail: still A4 (register total ~19-20 < 22 floor) OR A10 (Appendix B empty) OR A11 (Appendix C missing) OR A13 (populated-by notes still in OPEN-06/07 + Appendices). Record verbatim.

    **Step 3 — Spot-check counts:**
    ```bash
    grep -cE '^\*\*OPEN-Q[0-9]+\*\*' .planning/OPEN-QUESTIONS.md  # expect >= 19
    grep -cE 'Owning phase:[[:space:]]*Phase 8' .planning/OPEN-QUESTIONS.md  # expect >= 3
    grep -cF 'Coda-template-authoring (Phase 8)' .planning/OPEN-QUESTIONS.md  # expect >= 3
    grep -cF 'live-workstream-pointer' .planning/OPEN-QUESTIONS.md  # expect >= 1
    grep -cF 'decide-before-Phase-' .planning/OPEN-QUESTIONS.md  # expect >= 3
    ```

    **Step 4 — Write `.planning/phases/04-open-questions/04-03-SUMMARY.md`** using template. Required content:
    1. Plan ID + execution date + outcome (`Plan 04-03 — Wave 3 — executed 2026-05-10 — outcome: COMPLETE`)
    2. Sections populated: OPEN-03 (3 rows + cross-reference) + OPEN-04 (1 row + sub-row reservation) + OPEN-05 (3 rows). Total 7 new rows.
    3. Cross-reference for OPEN-07 plugin self-test (D-50 dedup) noted; Plan 04-04 MUST assign OPEN-Q22 to that row to honour the cross-reference.
    4. Closed-enum verification: severity / resolution-path / status / owning-phase counts (numbers per spot-check output).
    5. Mid-phase structural-check first-fail message verbatim.
    6. Decisions implemented: D-46, D-47, D-48, D-49, D-50, D-51, D-14.
    7. Hand-off to Wave 4 (Plan 04-04 owns OPEN-06 + OPEN-07; MUST assign OPEN-Q21 to OPEN-06 first row + OPEN-Q22 to OPEN-07 row to honour OPEN-03 cross-reference).

    **Implementing decisions:** D-46 (OPEN-05 H2), D-47 (closed 9-field schema), D-48 (severity INFORMATIONAL across), D-49 (resolution = `Coda-template-authoring (Phase 8)`), D-14 carried.
  </action>
  <verify>
    <automated>OPEN5_BODY=$(awk -v hdr='## OPEN-05: Standard Coda templates v2 must author' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && OPEN5_ROW_COUNT=$(printf '%s\n' "$OPEN5_BODY" | grep -cE '^\*\*OPEN-Q[0-9]+\*\*') && [ "$OPEN5_ROW_COUNT" -ge 3 ] && PHASE_8_OWNER=$(printf '%s\n' "$OPEN5_BODY" | grep -cE 'Owning phase:[[:space:]]*Phase 8') && [ "$PHASE_8_OWNER" = "$OPEN5_ROW_COUNT" ] && CODA_RES=$(printf '%s\n' "$OPEN5_BODY" | grep -cF 'Coda-template-authoring (Phase 8)') && [ "$CODA_RES" -ge 3 ] && TOTAL_ROWS=$(grep -cE '^\*\*OPEN-Q[0-9]+\*\*' .planning/OPEN-QUESTIONS.md) && [ "$TOTAL_ROWS" -ge 19 ] && EXIT_CODE=0; bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh > /tmp/wave3_check.out 2>&1 || EXIT_CODE=$?; echo "Wave 3 structural-check EXIT=$EXIT_CODE"; cat /tmp/wave3_check.out; test -f .planning/phases/04-open-questions/04-03-SUMMARY.md && grep -qF 'OPEN-03' .planning/phases/04-open-questions/04-03-SUMMARY.md && grep -qF 'OPEN-04' .planning/phases/04-open-questions/04-03-SUMMARY.md && grep -qF 'OPEN-05' .planning/phases/04-open-questions/04-03-SUMMARY.md && grep -qF 'OPEN-Q22' .planning/phases/04-open-questions/04-03-SUMMARY.md && echo TASK_3_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## OPEN-05` body has >= 3 register rows; all carry `Owning phase: Phase 8`
    - All OPEN-05 Resolution-path = `Coda-template-authoring (Phase 8)`: count >= 3
    - All OPEN-05 Severity = INFORMATIONAL: count = OPEN5_ROW_COUNT
    - Cumulative register total >= 19 (Wave 2: 12-13 + Wave 3: 7)
    - structural-check ran (exits NON-ZERO mid-phase per Wave 3 invariant)
    - SUMMARY.md exists at `.planning/phases/04-open-questions/04-03-SUMMARY.md`
    - SUMMARY.md mentions OPEN-03 + OPEN-04 + OPEN-05 + OPEN-Q22: 4 individual `grep -qF` checks succeed
    - SUMMARY.md cites the structural-check first-fail verbatim
    - SUMMARY.md names downstream consumer (Plan 04-04 / Wave 4): `grep -qF '04-04'` succeeds
    - SUMMARY.md instructs Wave 4 to assign OPEN-Q22 to OPEN-07 plugin self-test row (so cross-reference holds)
    - All 9 fields present per row in OPEN-Q18 (sample): 8 individual `grep -qF` checks for the 8 field labels
  </acceptance_criteria>
  <done>
    `## OPEN-05` body populated with section preamble + 3 register rows in closed 9-field per-row block form. All rows owner=Phase 8; all resolution=`Coda-template-authoring (Phase 8)`. Cumulative register total now ~19-20. Structural-check still fails (register-count floor 22 not yet met; Appendix B empty; Appendix C missing) — expected per Wave 3 invariant. SUMMARY.md committed with closed-enum verification + hand-off to Plan 04-04 carrying explicit OPEN-Q22 numbering directive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OPEN-Q numbering continuity ↔ cross-reference forward to Q22 | Tampering class. Plan 04-03 emits a forward cross-reference to OPEN-Q22 (which Plan 04-04 will assign). If Plan 04-04 numbers differently, the cross-reference is broken. Mitigation: Plan 04-03 SUMMARY.md instructs Plan 04-04 explicitly to assign OPEN-Q21 to OPEN-06 first row + OPEN-Q22 to OPEN-07 plugin self-test row. |
| D-51 single-canonical-row invariant ↔ structural-check | Repudiation class. If a future author adds a sub-row to OPEN-04 without updating the reconciliation algorithm, cardinality breaks. Mitigation: closing paragraph reserves sub-row explicitly; reconciliation algorithm in Plan 04-05 accommodates 1-row OR 2-row cardinality. |
| Phase 8 owner / Coda-template-authoring resolution coupling | All OPEN-05 rows must carry both. Mitigation: structural-check A6 + A8 enforce closed enums; acceptance criteria assert Phase-8-owner count == OPEN5_ROW_COUNT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | OPEN-Q numbering continuity | mitigate | SUMMARY.md instructs Plan 04-04 explicitly; structural-check enforces closed-enum compliance |
| T-04-08 | Repudiation | D-51 single-canonical-row | accept | Sub-row reservation paragraph documents future-row provision; reconciliation accommodates |
| T-04-09 | Tampering | OPEN-05 Phase 8 owner / Coda resolution coupling | mitigate | Acceptance criteria assert Phase-8-owner count == row count |
</threat_model>

<verification>
- OPEN-03 has 3 rows + cross-reference to OPEN-Q22
- OPEN-04 has 1 row + sub-row reservation paragraph
- OPEN-05 has 3 rows, all Phase 8 owner + Coda resolution
- Cumulative register total >= 19
- Structural-check still fails (register floor 22 not yet met); first-fail recorded
- SUMMARY.md committed
</verification>

<success_criteria>
- `## OPEN-03` populated with 3 design-deferred rows + cross-reference blockquote
- `## OPEN-04` populated with single canonical row per D-51 + sub-row reservation
- `## OPEN-05` populated with 3 Coda-template rows; all Phase 8 owner; all Coda-template-authoring resolution
- All severity / resolution-path / status / owning-phase fields exact-match closed enums
- Mid-phase structural-check ran (exits NON-ZERO; recorded in SUMMARY)
- `.planning/phases/04-open-questions/04-03-SUMMARY.md` committed; instructs Plan 04-04 on OPEN-Q22 numbering
</success_criteria>

<output>
After completion, create `.planning/phases/04-open-questions/04-03-SUMMARY.md` per template.
</output>
