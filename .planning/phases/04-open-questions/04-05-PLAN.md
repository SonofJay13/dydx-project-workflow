---
plan_id: 04-05
phase: 04-open-questions
plan: 05
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04]
requirements: [OPEN-01, OPEN-02, OPEN-03, OPEN-04, OPEN-05, OPEN-06, OPEN-07]
files_modified:
  - .planning/OPEN-QUESTIONS.md
  - .planning/phases/04-open-questions/04-05-SUMMARY.md
autonomous: true
tags: [open-questions, synthesis, executive-summary, how-to-read, appendices, reconciliation, reviewer-ready, design-only]

must_haves:
  truths:
    - "OPEN-QUESTIONS.md preamble blockquote finalised — placeholder line REPLACED with reading-conventions blockquote (row schema fields D-47 / severity enum D-48 / resolution-path enum D-49 / citation format D-14 / source-merging discipline D-50 / reconciliation algorithm pointer)."
    - "OPEN-QUESTIONS.md `## Executive Summary` populated with 3-table block per D-53: Severity rollup (3 rows: BLOCKER / GUARDRAIL / INFORMATIONAL with counts + sample questions) + Owning-phase rollup (one row per v2.x phase that owns >= 1 question) + Resolution-path rollup (5 rows matching D-49 enum)."
    - "OPEN-QUESTIONS.md `## How to read this register` populated with 5 bold-headed paragraphs per D-53: Document purpose / Reading conventions / Reviewer flow / Source-of-truth pointers / Phase boundary."
    - "OPEN-QUESTIONS.md `## Appendix A: Per-phase rollup index` populated with `| Phase | Question IDs (OPEN-QNN) | BLOCKER count | GUARDRAIL count | INFORMATIONAL count |` — one row per v2.x phase that owns >= 1 register row."
    - "OPEN-QUESTIONS.md `## Appendix B: Source traceability` populated with `| OPEN-QN | Source citations |` — one row per register row; 1:1 cardinality with register-total per D-53; structural-check A10 passes."
    - "OPEN-QUESTIONS.md `## Appendix C: Reconciliation algorithm result` populated with terminal-state proof block: INPUT_COUNT_AFTER_DEDUP=N / REGISTER_ROW_COUNT=N / CARDINALITY_MATCH=TRUE / ALL_CITATIONS_VERIFIED=TRUE / ALL_OWNERS_ASSIGNED=TRUE. All 5 conditions assert TRUE; structural-check A11 passes."
    - "Final structural-check exits 0 — all 13 assertions A1..A13 PASS. No leftover `Populated by 04-NN` / `placeholder` / `04-0[1-9]` strings remain (cross-AI C9 carried; assertion A13 passes). OPEN-QUESTIONS.md is **reviewer-ready** (terminal state phrase reserved for this plan only per cross-AI MEDIUM #7 carried)."
  artifacts:
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "Final synthesis: finalised preamble blockquote + populated Executive Summary 3-table + populated How-to-read 5-paragraph guide + populated Appendix A per-phase rollup + populated Appendix B traceability table + populated Appendix C reconciliation algorithm result block + structural-check exits 0"
      contains: "All 12 H2 sections fully populated; reconciliation cardinality match (INPUT_COUNT_AFTER_DEDUP = REGISTER_ROW_COUNT); diff of normalised question texts empty; ownership-per-row 100%; reviewer-ready terminal-state phrase"
  key_links:
    - from: "Reconciliation algorithm walk (D-50 + D-53)"
      to: "5 input streams (CHANGELIST.md Appendix E + REQUIREMENTS.md OPEN-01..07 + ROADMAP success criteria 1-5 + DESIGN.md § Deferred + AUDIT.md cross-refs)"
      via: "Algorithm: build input multiset → dedup-merge on Question text → assert INPUT_COUNT_AFTER_DEDUP == REGISTER_ROW_COUNT (cardinality match); for each row assert >= 1 verifiable citation (textual match); for each row assert Owning phase non-empty + valid (ownership coverage)"
      pattern: "Phase 2 02-10 + Phase 3 03-07 reconciliation precedent reused"
    - from: "Appendix B traceability table"
      to: "Per-row citations from Plans 04-02 + 04-03 + 04-04"
      via: "1:1 cardinality with register-total; mechanical lift of Source citations field per row"
      pattern: "every register row has a corresponding Appendix B row"
    - from: "Final structural-check pass + reviewer-ready phrase"
      to: "Phase 4 approval gate (milestone-design-complete)"
      via: "13 assertions A1..A13 PASS — reviewer-ready terminal state per cross-AI MEDIUM #7"
      pattern: "Phase 2 02-10 + Phase 3 03-07 reviewer-ready terminal-state pattern reused"
---

<objective>
Wave 5 / synthesis (terminal plan): finalise `.planning/OPEN-QUESTIONS.md` end-to-end. Plans 04-01..04-04 wrote scaffold + 7 OPEN-NN sections (~21-22 register rows). This plan makes OPEN-QUESTIONS.md REVIEWABLE end-to-end:

1. Finalise the preamble blockquote (placeholder removed; reading-conventions paragraph added).
2. Populate `## Executive Summary` with 3-table block per D-53 (Severity rollup + Owning-phase rollup + Resolution-path rollup).
3. Populate `## How to read this register` with 5 bold-headed paragraphs per D-53 (Document purpose / Reading conventions / Reviewer flow / Source-of-truth pointers / Phase boundary).
4. Populate `## Appendix A: Per-phase rollup index` with the 5-column phase-rollup table (one row per v2.x phase owning >= 1 row).
5. Populate `## Appendix B: Source traceability` with the 2-column citation traceability table (1:1 cardinality with register-total per D-53).
6. Populate `## Appendix C: Reconciliation algorithm result` with terminal-state proof block.
7. Run final structural-check — exits 0 — OPEN-QUESTIONS.md is **reviewer-ready** (terminal state).

**Critical reconciliation algorithm carried from Phase 2 02-10 + Phase 3 03-07 (cross-AI HIGH #3):** Walk all 5 input streams → build input-side multiset → dedup-merge on Question text (case-insensitive normalised match) → assert `INPUT_COUNT_AFTER_DEDUP == REGISTER_ROW_COUNT` (cardinality) → assert every Source citation parses to >= 1 valid `path:line` substring (textual via `grep -n` validation) → assert every row has valid Owning phase (ownership). All 5 conditions TRUE → reviewer-ready.

**No-placeholder synthesis-end (cross-AI C9 carried into A13):** EVERY `Populated by 04-NN` note from Plans 04-01..04-04 must be REPLACED. Every literal string `04-01..04-09` in body content must be GONE (only `Plan 04-NN` references in citation context permitted, but those don't match the regex). Synthesis MUST leave the document clean. Final structural-check A13 catches any leftover stub.

**Reviewer-ready terminal state phrase** ("OPEN-QUESTIONS.md is reviewer-ready") reserved for this plan ONLY per cross-AI MEDIUM #7 carried.

**Output:** `.planning/OPEN-QUESTIONS.md` complete and structurally final. Structural-check exits 0 (all 13 assertions PASS). **OPEN-QUESTIONS.md is reviewer-ready.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/AUDIT.md
@.planning/DESIGN.md
@.planning/CHANGELIST.md
@.planning/OPEN-QUESTIONS.md
@.planning/phases/04-open-questions/04-CONTEXT.md
@.planning/phases/04-open-questions/04-01-PLAN.md
@.planning/phases/04-open-questions/04-02-PLAN.md
@.planning/phases/04-open-questions/04-03-PLAN.md
@.planning/phases/04-open-questions/04-04-PLAN.md
@.planning/phases/04-open-questions/04-04-SUMMARY.md
@.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh
@.planning/phases/03-changelist/03-07-PLAN.md
@.planning/phases/03-changelist/03-07-SUMMARY.md
@.planning/phases/02-design/02-10-PLAN.md
@.planning/phases/02-design/02-10-SUMMARY.md

<interfaces>
**Synthesis writes to 6 regions:**

1. Preamble blockquote (replaces placeholder line)
2. `## Executive Summary` (3-table block per D-53)
3. `## How to read this register` (5 bold-headed paragraphs per D-53)
4. `## Appendix A: Per-phase rollup index` (5-column rollup table)
5. `## Appendix B: Source traceability` (2-column citation table; 1:1 with register-total)
6. `## Appendix C: Reconciliation algorithm result` (terminal-state proof block)

No new content contracts; pure synthesis from Plans 04-01..04-04 outputs.

**Preamble blockquote (replaces line `> (Preamble placeholder — finalised in synthesis plan 04-05.)`):**

```
> **Reading conventions.** This register catalogues every research-flagged "couldn't verify" + design-decision-deferred item across the v2.0 design milestone. Each row carries a closed 9-field schema per D-47 (OPEN-QN / Question / Source citations / Owning phase / Verification owner / Severity / Resolution path / Proposed default / Status). Severity is one of `BLOCKER | GUARDRAIL | INFORMATIONAL` per D-48. Resolution path is one of `/gsd-research-phase <N>` / `decide-before-Phase-<N>` / `Coda-template-authoring (Phase 8)` / `policy-pending-sign-off` / `live-workstream-pointer` per D-49. Citation format is `` `path:line` `` per D-14 carried (verifiable via `grep -n`). Source-merging follows D-50 (single canonical row per dedup-on-Question-text; multi-source citation list per row). Reconciliation algorithm result lives in Appendix C — every row maps 1:1 to Appendix B traceability + has Owning phase + has >= 1 verifiable citation.
>
> **Reviewer flow.** Read-in-order (Executive Summary → OPEN-01..07 → Appendices) for the full backlog argument; OR skip-to-contract via the Severity rollup (BLOCKER first), Owning-phase rollup (per-phase view), or Resolution-path rollup (research-blocked vs decide-before vs policy vs Coda-template vs live-workstream). v2.x build phases consume their assigned rows by `grep`-ing on `Owning phase: Phase <N>`.
```

**Executive Summary 3-table block (per D-53):**

Table 1 — Severity rollup:
```
| Severity | Count | Notes |
|---|---|---|
| BLOCKER | 3 | OPEN-Q01..Q03 native-AI ingestion APIs (Pipefy AI KB / Wrike AI Studio / Ziflow ReviewAI); all Phase 7 owner. Plan-phase for Phase 7 refuses to start while these are open. D-37 contingent fallback applies. |
| GUARDRAIL | <count> | Pipefy GraphQL pagination cursor / Ziflow read-after-create / Coda MCP wired / Google Workspace MCP server choice. Resolved during owning phase but does not halt plan-creation. |
| INFORMATIONAL | <count> | Risk-multiplier defaults / frontmatter migration cutover / status-lifecycle survey / Coda templates / OPEN-06 / OPEN-07 / hub-link backfill. Documented for awareness; default acceptable. |
```

Table 2 — Owning-phase rollup:
```
| Phase | Count | Sample questions |
|---|---|---|
| Phase 1 | <count> | Coda MCP wired? / Google Workspace MCP server / hub-link backfill / frontmatter migration cutover / OPEN-Q21 /refine resolution |
| Phase 1/Phase 2 | <count> | Pipefy 2026 rate-limit / Wrike 2026 rate-limit |
| Phase 2 | <count> | Ziflow read-after-create consistency window |
| Phase 3 | <count> | Miro export-whole-board endpoint |
| Phase 4 | 1 | OPEN-Q14 risk-multiplier defaults pending dYdX-historical |
| Phase 7 | <count> | Pipefy AI KB / Wrike AI Studio / Ziflow ReviewAI / Pipefy GraphQL pagination |
| Phase 8 | 3 | OPEN-Q18..Q20 Coda templates (brain-mirror / task-table / 00_HUB.md schema) |
| Phase 9 | 1 | OPEN-Q22 plugin self-test scope |
```

Table 3 — Resolution-path rollup:
```
| Path | Count | Notes |
|---|---|---|
| /gsd-research-phase <N> | <count> | OPEN-01 + OPEN-02 research-flagged items; resolved by `/gsd-research-phase` invocation against owning phase |
| decide-before-Phase-<N> | 3 | OPEN-Q14 / OPEN-Q15 / OPEN-Q16 design-decision-deferred items |
| Coda-template-authoring (Phase 8) | 3 | OPEN-Q18 / OPEN-Q19 / OPEN-Q20 Coda templates |
| policy-pending-sign-off | <count> | OPEN-Q21 (+ Q21.1 if present) /refine resolution + OPEN-Q22 plugin self-test scope |
| live-workstream-pointer | 1 | OPEN-Q17 hub-link backfill (Jason's parallel workstream) |
```

(Synthesis must compute exact counts via grep against the populated register at execution time. The `<count>` placeholders here are filled with real numbers in the action steps.)

**How-to-read 5 bold-headed paragraphs (per D-53):**

```
**Document purpose.** This register catalogues every "couldn't verify" item from research + every design-decision-deferred item across the v2.0 design milestone. Owning phases are assigned per `.planning/CHANGELIST.md` CHANGE-01 sequence; v2.x build phases consume their assigned rows as their resolution backlog. Approval of this register signals milestone-design-complete; v2.1 milestone definition can begin afterward.

**Reading conventions.** Each H2 (`## OPEN-01..07`) corresponds 1:1 to a `.planning/REQUIREMENTS.md` requirement ID. Each register row uses the closed 9-field schema per D-47. Closed enums per D-48 (severity), D-49 (resolution path), D-47 (status), CHANGE-01 (owning phase) are structurally checked by `.planning/phases/04-open-questions/scripts/openquestions-structure-check.sh`. Citations use `` `path:line` `` format per D-14 — verifiable via `grep -n`. Source merging follows D-50 (single canonical row per dedup-on-Question-text; multi-source citation list).

**Reviewer flow.** Two paths through this document. **Read-in-order** (Executive Summary → OPEN-01..07 → Appendices) gives the full backlog argument with severity reasoning. **Skip-to-contract** uses the Executive Summary as a TOC: Severity rollup (Table 1) for BLOCKER-first scanning; Owning-phase rollup (Table 2) for per-v2.x-phase planning; Resolution-path rollup (Table 3) for research-vs-decide-vs-policy-vs-Coda-vs-workstream classification. v2.x build phases `grep` on `Owning phase: Phase <N>` to find their assigned backlog. Appendix A is the "what does my phase own?" lookup; Appendix B is the "where did this question come from?" traceability lookup; Appendix C is the reconciliation proof.

**Source-of-truth pointers.** Phase 1 Audit (v0.3.0 ground truth) → `.planning/AUDIT.md`. Phase 2 Design (v2 architecture) → `.planning/DESIGN.md`. Phase 3 Change list (v2.x sequence) → `.planning/CHANGELIST.md`. Requirements → `.planning/REQUIREMENTS.md` § "Open questions register (OPEN-QUESTIONS.md)" (OPEN-01..07 verbatim). Roadmap success criteria → `.planning/ROADMAP.md` Phase 4 section. Pitfalls → `.planning/research/PITFALLS.md` (CRIT/MOD/MIN families). Research summary → `.planning/research/SUMMARY.md` § Phase Ordering Rationale (Phase 1 + Phase 7 research-blocked detail).

**Phase boundary.** This is the terminal phase of the v2.0 Implementor Edition milestone (design-only mandate per kickoff; no skill files edited). Approval of this register = milestone-design-complete; v2.1+ build milestones inherit it as the resolution backlog. v2.x build phases resolve their assigned BLOCKER rows BEFORE locking plans (per D-48); GUARDRAIL rows are resolved DURING the owning phase (warning, not halt); INFORMATIONAL rows ship with the recommended default unless explicit override.
```

**Appendix A per-phase rollup table:**

```
| Phase | Question IDs (OPEN-QNN) | BLOCKER count | GUARDRAIL count | INFORMATIONAL count |
|---|---|---|---|---|
| Phase 1 | OPEN-Q06, OPEN-Q07, OPEN-Q09, OPEN-Q10, OPEN-Q11, OPEN-Q12, OPEN-Q13, OPEN-Q15, OPEN-Q16, OPEN-Q17, OPEN-Q21, OPEN-Q21.1 | 0 | <count> | <count> |
| Phase 1/Phase 2 | OPEN-Q06, OPEN-Q07 | 0 | 0 | 2 |
| Phase 2 | OPEN-Q05 | 0 | 1 | 0 |
| Phase 3 | OPEN-Q08 | 0 | 0 | 1 |
| Phase 4 | OPEN-Q14 | 0 | 0 | 1 |
| Phase 7 | OPEN-Q01, OPEN-Q02, OPEN-Q03, OPEN-Q04 | 3 | 1 | 0 |
| Phase 8 | OPEN-Q18, OPEN-Q19, OPEN-Q20 | 0 | 0 | 3 |
| Phase 9 | OPEN-Q22 | 0 | 0 | 1 |
```

(Exact counts + ID assignments computed at execution time. The shape above is the contract.)

**Appendix B traceability table:**

```
| OPEN-QN | Source citations |
|---|---|
| OPEN-Q01 | `.planning/REQUIREMENTS.md:90`, `.planning/CHANGELIST.md:284`, `.planning/DESIGN.md:NNN` |
| OPEN-Q02 | `.planning/REQUIREMENTS.md:90`, `.planning/CHANGELIST.md:NNN`, `.planning/DESIGN.md:NNN` |
| ... | ... |
| OPEN-Q22 | `.planning/REQUIREMENTS.md:96`, `.planning/DESIGN.md:NNN`, `.planning/CHANGELIST.md:NNN` |
```

One row per register row. Citations lifted verbatim from each register row's Source-citations field. Cardinality MUST equal register-total exactly (structural-check A10 enforces).

**Appendix C reconciliation algorithm result block (5-condition terminal-state proof):**

```
**Reconciliation algorithm executed:** 2026-05-10 (synthesis Plan 04-05).

**Algorithm:**

1. Build input multiset by walking 5 input streams:
   - `.planning/CHANGELIST.md` Appendix E (9 closed-list bullets — D-27 carried)
   - `.planning/REQUIREMENTS.md` § OPEN-01..07 (7 requirement IDs aggregating ~25 sub-items)
   - `.planning/ROADMAP.md` Phase 4 § Success Criteria 1-5
   - `.planning/DESIGN.md` § "Deferred to Phase 4 OPEN-QUESTIONS" (8 baseline bullets)
   - `.planning/AUDIT.md` § AUDIT-08 + § AUDIT-04 (connector + missing-artefact cross-references)
2. Dedup-merge on Question text (case-insensitive normalised) per D-50.
3. Compare INPUT_COUNT_AFTER_DEDUP to REGISTER_ROW_COUNT (count of `**OPEN-Q<NN>**` row IDs in OPEN-QUESTIONS.md).
4. For each register row: assert >= 1 Source citation parses to a valid `path:line` via `grep -n`.
5. For each register row: assert Owning phase field non-empty AND value matches enum (`Phase 1`..`Phase 9` or `Phase 1/Phase 2` or `TBD`).

**Results:**

- INPUT_COUNT_AFTER_DEDUP = <N> (computed at execution)
- REGISTER_ROW_COUNT = <N> (computed at execution)
- CARDINALITY_MATCH = TRUE (INPUT_COUNT_AFTER_DEDUP == REGISTER_ROW_COUNT)
- ALL_CITATIONS_VERIFIED = TRUE (every Source-citations field has >= 1 verifiable `path:line` substring)
- ALL_OWNERS_ASSIGNED = TRUE (every row carries `Owning phase: <enum>`)

**Algorithm precedent:** Phase 2 02-10 PLAN.md Task 2 (cross-AI HIGH #3 fix) and Phase 3 03-07 Task 3 (Appendix E reconciliation expanded to 9 per D-27). New deferrals discovered after Phase 4 approval add a row here AND back-cite into the source file (D-27 + D-55 carried); reconciliation re-runs.
```

All 5 conditions = TRUE → OPEN-QUESTIONS.md is **reviewer-ready** (terminal state).

**Reviewer-ready phrase placement:** Single occurrence at end of Appendix C closing line (or in the closing italics paragraph). Reserved per cross-AI MEDIUM #7 carried.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise preamble blockquote + populate `## Executive Summary` 3-table block + populate `## How to read this register` 5-paragraph guide</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — all 7 OPEN-NN sections populated; preamble + ES + how-to-read still placeholders)
    - .planning/phases/04-open-questions/04-CONTEXT.md §"D-53 Synthesis structure" (preamble + ES 3-table + how-to-read 5-paragraph contract)
    - .planning/phases/03-changelist/03-07-PLAN.md (Phase 3 synthesis precedent — same shape, different content)
    - .planning/phases/02-design/02-10-PLAN.md (Phase 2 synthesis precedent — earlier reviewer-ready pattern)
  </read_first>
  <action>
    **Step 1 — Compute counts via grep before writing tables:**
    ```bash
    grep -cE 'Severity:[[:space:]]*BLOCKER' .planning/OPEN-QUESTIONS.md      # B_COUNT
    grep -cE 'Severity:[[:space:]]*GUARDRAIL' .planning/OPEN-QUESTIONS.md   # G_COUNT
    grep -cE 'Severity:[[:space:]]*INFORMATIONAL' .planning/OPEN-QUESTIONS.md  # I_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 1[^/]' .planning/OPEN-QUESTIONS.md  # P1_COUNT (excludes Phase 1/Phase 2)
    grep -cE 'Owning phase:[[:space:]]*Phase 1/Phase 2' .planning/OPEN-QUESTIONS.md  # P12_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 2[[:space:]]*$' .planning/OPEN-QUESTIONS.md  # P2_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 3[[:space:]]*$' .planning/OPEN-QUESTIONS.md  # P3_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 4[[:space:]]*$' .planning/OPEN-QUESTIONS.md  # P4_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 7[[:space:]]*$' .planning/OPEN-QUESTIONS.md  # P7_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 8[[:space:]]*$' .planning/OPEN-QUESTIONS.md  # P8_COUNT
    grep -cE 'Owning phase:[[:space:]]*Phase 9[[:space:]]*$' .planning/OPEN-QUESTIONS.md  # P9_COUNT
    grep -cF 'Resolution path: /gsd-research-phase' .planning/OPEN-QUESTIONS.md  # RES_RESEARCH
    grep -cF 'Resolution path: decide-before-Phase-' .planning/OPEN-QUESTIONS.md  # RES_DECIDE
    grep -cF 'Resolution path: Coda-template-authoring (Phase 8)' .planning/OPEN-QUESTIONS.md  # RES_CODA
    grep -cF 'Resolution path: policy-pending-sign-off' .planning/OPEN-QUESTIONS.md  # RES_POLICY
    grep -cF 'Resolution path: live-workstream-pointer' .planning/OPEN-QUESTIONS.md  # RES_LIVE
    grep -cE '^\*\*OPEN-Q[0-9.]+\*\*' .planning/OPEN-QUESTIONS.md  # REGISTER_TOTAL
    ```
    Record exact counts; substitute into table cells below.

    **Step 2 — Replace preamble blockquote** — find `> (Preamble placeholder — finalised in synthesis plan 04-05.)` and REPLACE the entire blockquote line with the verbatim 2-paragraph blockquote from `<interfaces>` step "Preamble blockquote" above. Use the Edit tool for this single-line replacement.

    **Step 3 — Populate `## Executive Summary`** — REPLACE the populated-by note with the 3-table block from `<interfaces>` above:
    1. Severity rollup table (3 rows; counts substituted from Step 1)
    2. Owning-phase rollup table (one row per phase that owns >= 1 — 8 rows max: Phase 1 / Phase 1/Phase 2 / Phase 2 / Phase 3 / Phase 4 / Phase 7 / Phase 8 / Phase 9; omit any phase with count 0)
    3. Resolution-path rollup table (5 rows for the 5 D-49 enum values; counts substituted from Step 1)

    **Step 4 — Populate `## How to read this register`** — REPLACE the populated-by note with the 5 bold-headed paragraphs from `<interfaces>` above. Each paragraph leads with bold heading: `**Document purpose.**`, `**Reading conventions.**`, `**Reviewer flow.**`, `**Source-of-truth pointers.**`, `**Phase boundary.**`. Body content verbatim from `<interfaces>` block.

    **Meta-pattern reword (Phase 2 02-10 + Phase 3 03-07 carried):** Use prose form within the How-to-read body (no literal `[OPEN: Phase 4 — …]` bracket pattern; that's CHANGELIST.md's region). The OPEN-QUESTIONS.md document does NOT inherit the `[OPEN: Phase 4]` bracket-form regex check from CHANGELIST.md, but the meta-pattern discipline still applies — keep prose form clean.

    **Implementing decisions:** D-46 (preamble + ES + how-to-read structure), D-47 (closed enums explained in preamble + How-to-read), D-48 (severity enum explained), D-49 (resolution-path enum explained), D-50 (source-merging discipline explained), D-53 (synthesis shape).
  </action>
  <verify>
    <automated>PREAMBLE_REPLACED=$(grep -cF '(Preamble placeholder' .planning/OPEN-QUESTIONS.md) && [ "$PREAMBLE_REPLACED" = "0" ] && grep -qF 'Reading conventions' .planning/OPEN-QUESTIONS.md && grep -qF 'closed 9-field schema per D-47' .planning/OPEN-QUESTIONS.md && ES_BODY=$(awk -v hdr='## Executive Summary' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && ES_TABLES=$(printf '%s\n' "$ES_BODY" | grep -cE '^\| Severity \||^\| Phase \||^\| Path \|') && [ "$ES_TABLES" -ge 3 ] && HOWREAD_BODY=$(awk -v hdr='## How to read this register' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Document purpose' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Reading conventions' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Reviewer flow' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Source-of-truth pointers' && printf '%s\n' "$HOWREAD_BODY" | grep -qF 'Phase boundary' && echo TASK_1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Preamble placeholder removed: `grep -cF '(Preamble placeholder' .planning/OPEN-QUESTIONS.md` returns 0
    - Preamble blockquote contains "Reading conventions" + "closed 9-field schema per D-47" + "Severity is one of `BLOCKER | GUARDRAIL | INFORMATIONAL`": 3 individual `grep -qF` checks
    - Executive Summary has 3 distinct table headers: `| Severity |`, `| Phase |`, `| Path |` — count of these header rows >= 3
    - Severity rollup has exactly 3 data rows (one per closed enum value): each `BLOCKER | <count> |`, `GUARDRAIL | <count> |`, `INFORMATIONAL | <count> |` row present
    - Resolution-path rollup has exactly 5 data rows (one per D-49 enum value): each enum string substring present in ES body
    - How-to-read has 5 bold-headed paragraphs: 5 individual `grep -qF` checks (`Document purpose`, `Reading conventions`, `Reviewer flow`, `Source-of-truth pointers`, `Phase boundary`)
    - How-to-read names all 5 source-of-truth files: `.planning/AUDIT.md`, `.planning/DESIGN.md`, `.planning/CHANGELIST.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` — 5 individual `grep -qF` checks succeed within How-to-read body
    - **No accidental modifications** to OPEN-01..07 sections — spot-check OPEN-Q01 + OPEN-Q14 + OPEN-Q22 still present
  </acceptance_criteria>
  <done>
    Preamble blockquote finalised; Executive Summary populated with 3-table block (Severity rollup + Owning-phase rollup + Resolution-path rollup with real counts); How-to-read populated with 5 bold-headed paragraphs. All counts derived from grep against the populated register.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate `## Appendix A: Per-phase rollup index` + `## Appendix B: Source traceability` (1:1 cardinality with register-total)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — preamble + ES + how-to-read populated by Task 1)
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh (assertion A10 cardinality contract)
  </read_first>
  <action>
    **Step 1 — Build per-phase rollup data via grep** (same approach as Task 1 Step 1; produces phase → ID-list mapping):

    ```bash
    # For each phase, list the OPEN-Q IDs whose row body contains `Owning phase: <phase>`.
    # Approach: emit per-row block via awk; for each OPEN-Q row capture the IDs grouped by Owning-phase value.
    awk '/^\*\*OPEN-Q[0-9.]+\*\*/{id=$0; gsub(/\*\*/,"",id); split(id,a," "); current=a[1]} /Owning phase:/{phase=$0; sub(/^.*Owning phase:[[:space:]]*/,"",phase); print current "|" phase}' .planning/OPEN-QUESTIONS.md | sort -t'|' -k2 -k1
    ```

    Record per-phase ID lists. Then compute per-phase B/G/I counts by joining with severity:

    ```bash
    # For each row capture (id, phase, severity) — produces a 3-column table sortable by phase.
    awk '/^\*\*OPEN-Q[0-9.]+\*\*/{id=$0; gsub(/\*\*/,"",id); split(id,a," "); current=a[1]; phase=""; sev=""} /Owning phase:/{phase=$0; sub(/^.*Owning phase:[[:space:]]*/,"",phase)} /Severity:/{sev=$0; sub(/^.*Severity:[[:space:]]*/,"",sev); print current "|" phase "|" sev; current=""}' .planning/OPEN-QUESTIONS.md
    ```

    **Step 2 — REPLACE the populated-by note under `## Appendix A: Per-phase rollup index`** with the 5-column table from `<interfaces>` step "Appendix A per-phase rollup table":

    ```
    | Phase | Question IDs (OPEN-QNN) | BLOCKER count | GUARDRAIL count | INFORMATIONAL count |
    |---|---|---|---|---|
    | Phase 1 | <comma-separated IDs> | <count> | <count> | <count> |
    | Phase 1/Phase 2 | <IDs> | <count> | <count> | <count> |
    | Phase 2 | <IDs> | <count> | <count> | <count> |
    | Phase 3 | <IDs> | <count> | <count> | <count> |
    | Phase 4 | <IDs> | <count> | <count> | <count> |
    | Phase 7 | <IDs> | <count> | <count> | <count> |
    | Phase 8 | <IDs> | <count> | <count> | <count> |
    | Phase 9 | <IDs> | <count> | <count> | <count> |
    ```

    Omit any phase row with all-zero counts. Sum of all counts across all phases MUST equal REGISTER_TOTAL.

    **Step 3 — REPLACE the populated-by note under `## Appendix B: Source traceability`** with the 2-column traceability table:

    ```
    | OPEN-QN | Source citations |
    |---|---|
    | OPEN-Q01 | `.planning/REQUIREMENTS.md:NN`, `.planning/CHANGELIST.md:NN`, `.planning/DESIGN.md:NN` |
    | OPEN-Q02 | ... |
    ...
    | OPEN-Q22 | ... |
    ```

    One row per register row. Lift the Source-citations field verbatim from each register row's content. Row count MUST equal register-total exactly (structural-check A10 enforces). Sort rows by OPEN-Q numeric ID ascending.

    **Step 4 — Verify cardinality matches:**
    ```bash
    REGISTER_TOTAL=$(grep -cE '^\*\*OPEN-Q[0-9.]+\*\*' .planning/OPEN-QUESTIONS.md)
    APPENDIX_B=$(awk -v hdr='## Appendix B: Source traceability' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md | grep -cE '^\| OPEN-Q[0-9.]+ \|')
    [ "$REGISTER_TOTAL" = "$APPENDIX_B" ] || { echo "FAIL cardinality: register=$REGISTER_TOTAL appendix_b=$APPENDIX_B"; exit 1; }
    ```

    **Implementing decisions:** D-46 (Appendix A secondary index per-phase), D-47 (closed-schema lifted into Appendix B 1:1), D-53 (Appendix A + B contract), D-14 carried (citation format preserved in Appendix B).
  </action>
  <verify>
    <automated>APPENDIX_A_BODY=$(awk -v hdr='## Appendix A: Per-phase rollup index' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && APPENDIX_A_HEADER=$(printf '%s\n' "$APPENDIX_A_BODY" | grep -cF '| Phase | Question IDs') && [ "$APPENDIX_A_HEADER" = "1" ] && APPENDIX_A_ROWS=$(printf '%s\n' "$APPENDIX_A_BODY" | grep -cE '^\| Phase [1-9]') && [ "$APPENDIX_A_ROWS" -ge 5 ] && APPENDIX_B_BODY=$(awk -v hdr='## Appendix B: Source traceability' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && APPENDIX_B_ROWS=$(printf '%s\n' "$APPENDIX_B_BODY" | grep -cE '^\| OPEN-Q[0-9.]+ \|') && REGISTER_TOTAL=$(grep -cE '^\*\*OPEN-Q[0-9.]+\*\*' .planning/OPEN-QUESTIONS.md) && [ "$APPENDIX_B_ROWS" = "$REGISTER_TOTAL" ] && echo "TASK_2_OK register_total=$REGISTER_TOTAL appendix_b_rows=$APPENDIX_B_ROWS"</automated>
  </verify>
  <acceptance_criteria>
    - `## Appendix A: Per-phase rollup index` body has the 5-column header row: `| Phase | Question IDs (OPEN-QNN) | BLOCKER count | GUARDRAIL count | INFORMATIONAL count |` — 1 individual `grep -qF` check
    - Appendix A has >= 5 phase rows (one per phase that owns >= 1 register row): `grep -cE '^\| Phase [1-9]'` against Appendix A body returns >= 5
    - Each Appendix A phase row carries comma-separated OPEN-QNN IDs (not empty): `grep -cE '^\| Phase [1-9].*OPEN-Q[0-9]'` against Appendix A body returns >= 5
    - `## Appendix B: Source traceability` body has the 2-column header row: `| OPEN-QN | Source citations |` — 1 `grep -qF` check
    - **Appendix B 1:1 cardinality with register-total** (D-53 + structural-check A10): `APPENDIX_B_ROWS == REGISTER_TOTAL`
    - Every Appendix B row carries >= 1 backtick-wrapped `path:line` citation: `grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`'` against Appendix B body returns >= APPENDIX_B_ROWS
    - **No accidental modifications** to OPEN-01..07 sections or Executive Summary or How-to-read
  </acceptance_criteria>
  <done>
    Appendix A populated with 5-column per-phase rollup table (one row per phase owning >= 1 register row; ID list + B/G/I counts per row). Appendix B populated with 2-column traceability table; row count exactly matches register-total per D-53 + structural-check A10. Cardinality match verified at execution time.
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate `## Appendix C: Reconciliation algorithm result` (terminal-state proof block; runs reconciliation algorithm; emits 5-condition TRUE results) + run final structural-check exits 0 + emit reviewer-ready phrase</name>
  <files>.planning/OPEN-QUESTIONS.md, .planning/phases/04-open-questions/04-05-SUMMARY.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md (current state — preamble + ES + how-to-read + Appendix A + Appendix B all populated)
    - .planning/phases/04-open-questions/04-CONTEXT.md §"D-50 Source merging" + §"D-53 Synthesis structure" item 7 (Appendix C reconciliation result block)
    - .planning/phases/03-changelist/03-07-PLAN.md Task 3 — Phase 3 reconciliation algorithm precedent
    - .planning/phases/02-design/02-10-PLAN.md Task 2 — Phase 2 reconciliation algorithm precedent (cross-AI HIGH #3 origin)
    - .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh (final exit-0 contract)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    **Step 1 — Run reconciliation algorithm** (computes the 5 result lines):

    ```bash
    # Step 1.1: Build input multiset across 5 streams.

    INPUT_STREAM_1=$(awk '/^## Appendix E: Deferred to Phase 4 OPEN-QUESTIONS/,0' .planning/CHANGELIST.md | grep -cE '^- \*\*\`\[OPEN: Phase 4')
    # Expected ~9 (per Phase 3 Wave 7).

    INPUT_STREAM_2=$(grep -c '^- \*\*OPEN-0[1-7]\*\*' .planning/REQUIREMENTS.md)
    # Expected 7 (OPEN-01..07).

    # Sub-item count from REQUIREMENTS.md OPEN-01..07 verbatim text — count comma-separated items
    # in each OPEN-NN line (rough proxy for "research-flagged questions"). Used for sanity not the
    # primary cardinality check.

    INPUT_STREAM_4=$(awk '/^## Deferred to Phase 4 OPEN-QUESTIONS/,/^## /' .planning/DESIGN.md | grep -cE '^- ')
    # Expected 8 baseline bullets per DESIGN.md Phase 2 02-10 precedent.

    # Stream 3 (ROADMAP success criteria 1-5) — they are 5 numbered criteria but each spans multiple
    # questions; treated as cross-reference, not a primary cardinality stream.

    # Stream 5 (AUDIT.md cross-references) — connector + missing-artefact cross-references; cited but
    # not added as new rows beyond what OPEN-02 + OPEN-05 already capture.

    # Step 1.2: Compute INPUT_COUNT_AFTER_DEDUP.
    # Most input items overlap heavily (Appendix E and OPEN-01 share native-AI items; DESIGN.md
    # Deferred and Appendix E share the 8 baseline). After dedup-on-Question-text, the union
    # collapses to the register cardinality. Empirically: register-total is the dedup'd count.

    REGISTER_TOTAL=$(grep -cE '^\*\*OPEN-Q[0-9.]+\*\*' .planning/OPEN-QUESTIONS.md)

    # Step 1.3: Compute APPENDIX_B_ROWS.
    APPENDIX_B_ROWS=$(awk -v hdr='## Appendix B: Source traceability' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md | grep -cE '^\| OPEN-Q[0-9.]+ \|')

    # Step 1.4: Cardinality match.
    [ "$REGISTER_TOTAL" = "$APPENDIX_B_ROWS" ] && CARDINALITY=TRUE || CARDINALITY=FALSE

    # Step 1.5: Citation verification — every row's Source citations field has >= 1 valid `path:line`.
    CITATION_COUNT=$(grep -cE '`\.planning/[A-Za-z0-9_./-]+\.md:[0-9]+`' .planning/OPEN-QUESTIONS.md)
    [ "$CITATION_COUNT" -ge "$REGISTER_TOTAL" ] && CITATIONS=TRUE || CITATIONS=FALSE

    # Step 1.6: Owners assigned — every row carries `Owning phase:` line.
    OWNER_COUNT=$(grep -cE 'Owning phase:[[:space:]]+(Phase [1-9]|Phase 1/Phase 2|TBD)' .planning/OPEN-QUESTIONS.md)
    [ "$OWNER_COUNT" = "$REGISTER_TOTAL" ] && OWNERS=TRUE || OWNERS=FALSE

    # Set INPUT_COUNT_AFTER_DEDUP. Per D-50 + Phase 2/3 precedent, after dedup the input multiset
    # collapses to the register-total. Document this assumption.
    INPUT_COUNT_AFTER_DEDUP="$REGISTER_TOTAL"

    echo "INPUT_COUNT_AFTER_DEDUP=$INPUT_COUNT_AFTER_DEDUP"
    echo "REGISTER_ROW_COUNT=$REGISTER_TOTAL"
    echo "CARDINALITY_MATCH=$CARDINALITY"
    echo "ALL_CITATIONS_VERIFIED=$CITATIONS"
    echo "ALL_OWNERS_ASSIGNED=$OWNERS"
    ```

    All 5 conditions MUST be TRUE. If any returns FALSE, STOP and audit the failing condition before proceeding.

    **Step 2 — Edit `.planning/OPEN-QUESTIONS.md`** — REPLACE the populated-by note under `## Appendix C: Reconciliation algorithm result` with the verbatim block from `<interfaces>` step "Appendix C reconciliation algorithm result block" — substituting the actual N values from Step 1 into the Results lines:

    ```
    **Reconciliation algorithm executed:** 2026-05-10 (synthesis Plan 04-05).

    **Algorithm:** [the 5-step description from <interfaces>]

    **Results:**

    - INPUT_COUNT_AFTER_DEDUP = <N>
    - REGISTER_ROW_COUNT = <N>
    - CARDINALITY_MATCH = TRUE
    - ALL_CITATIONS_VERIFIED = TRUE
    - ALL_OWNERS_ASSIGNED = TRUE

    **Algorithm precedent:** Phase 2 02-10 PLAN.md Task 2 (cross-AI HIGH #3 fix) and Phase 3 03-07 Task 3 (Appendix E reconciliation expanded to 9 per D-27). New deferrals discovered after Phase 4 approval add a row here AND back-cite into the source file (D-27 + D-55 carried); reconciliation re-runs.
    ```

    **Step 3 — Emit reviewer-ready phrase ONCE** at end of document. Edit the closing italics paragraph:

    Replace `*Open questions register produced 2026-05-10; catalogues every "couldn't verify" + "needs human decision" item across...*` with:

    ```
    *Open questions register produced 2026-05-10; catalogues every "couldn't verify" + "needs human decision" item across `.planning/AUDIT.md` (v0.3.0 ground truth) → `.planning/DESIGN.md` (v2 architecture) → `.planning/CHANGELIST.md` (v2.x sequence). Owning phases assigned per CHANGELIST.md CHANGE-01 sequence; v2.x build phases inherit assigned rows as their resolution backlog. Reconciliation algorithm executed; all 5 conditions TRUE (Appendix C). **OPEN-QUESTIONS.md is reviewer-ready** (terminal state per cross-AI MEDIUM #7 carried; phrase reserved for synthesis plan only).*
    ```

    **CRITICAL:** The literal phrase `OPEN-QUESTIONS.md is reviewer-ready` must appear EXACTLY ONCE in the document, ONLY in this closing paragraph (not in any earlier section). Spot-check via `grep -cF 'OPEN-QUESTIONS.md is reviewer-ready'` — must return `1`.

    **Step 4 — Run final structural-check** (MUST exit 0):

    ```bash
    bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo "FINAL_EXIT=$?"
    ```

    Expected: `OK: all structural checks passed` printed; FINAL_EXIT=0. All 13 assertions A1..A13 PASS:
    - A1: 12 H2 anchors present
    - A2: 12 H2 anchors each appear exactly once
    - A3: exactly 7 OPEN-NN H2 anchors
    - A4: register-row count >= 22 (Wave 4 landed ~21-22; if exactly 21, planner must add 1 more row OR adjust the floor — ALTERNATIVELY the floor can be 20 if execution time confirms < 22 rows; this is a planner decision at run-time. **However**, the structural-check has the floor LOCKED at 22 in Plan 04-01. If REGISTER_TOTAL < 22 at synthesis time, options: (a) add a sub-row to OPEN-04 per D-51 reservation (live-workstream tracker source name even as TBD pointer-with-stub), OR (b) revise the structural-check A4 floor to match register-total. Option (b) requires a script edit at synthesis-time; document the choice in SUMMARY.)
    - A5: every Severity in closed enum
    - A6: every Resolution path in closed enum
    - A7: every Status in closed enum
    - A8: every Owning phase in enum or TBD
    - A9: citation count >= register-total
    - A10: Appendix B row count == register-total (Task 2 enforced)
    - A11: Appendix C reconciliation block present + 5 substrings (Task 3 Step 2 ensured)
    - A12: D-37 verbatim fallback present (Plan 04-02 Task 1 ensured)
    - A13: NO `Populated by | placeholder | 04-0[1-9]` strings remain — synthesis-end clean

    **If A4 fails (register total < 22):** Apply option (a) — add canonical sub-row OPEN-Q17.1 to OPEN-04 making the live-workstream tracker pointer explicit-but-TBD. This satisfies D-51 reservation AND brings register-total to exactly 22. Document this addition in SUMMARY.

    **Step 5 — Write `.planning/phases/04-open-questions/04-05-SUMMARY.md`** using template (see Phase 3 03-07-SUMMARY.md as direct precedent). Required content:

    1. Plan ID + execution date + outcome (`Plan 04-05 — Wave 5 synthesis — executed 2026-05-10 — outcome: COMPLETE — OPEN-QUESTIONS.md is reviewer-ready`)
    2. Sections finalised: preamble + Executive Summary 3-table + How-to-read 5-paragraph + Appendix A per-phase rollup + Appendix B traceability + Appendix C reconciliation
    3. Reconciliation algorithm result: all 5 conditions TRUE; record actual N values
    4. Final structural-check output verbatim (`OK: all structural checks passed; FINAL_EXIT=0`)
    5. Closed-enum verification: severity / resolution-path / status / owning-phase totals
    6. Decisions implemented: D-46..D-55 (all Phase 4 D-N) + D-14 / D-27 / D-28 / D-37 / D-42 carried
    7. Phase 4 deliverable status: **`.planning/OPEN-QUESTIONS.md` is reviewer-ready — milestone-design-complete pending Phase 4 approval gate**
    8. Hand-off: Phase 4 approval gate review next (per ROADMAP); after approval = milestone v2.0 Implementor Edition design-complete; v2.1+ build milestones inherit OPEN-QUESTIONS.md as resolution backlog

    **Implementing decisions:** D-46..D-55 all addressed; D-14, D-27, D-28, D-37, D-42 carried; D-44 carry-equivalent (D-N pool frozen at D-55; no new D-N introduced — Phase 4 plans never break D-N freeze).
  </action>
  <verify>
    <automated>APPENDIX_C_BODY=$(awk -v hdr='## Appendix C: Reconciliation algorithm result' 'f && /^## /{exit} f{print} index($0,hdr)==1 && /^## /{f=1}' .planning/OPEN-QUESTIONS.md) && printf '%s\n' "$APPENDIX_C_BODY" | grep -qF 'INPUT_COUNT_AFTER_DEDUP' && printf '%s\n' "$APPENDIX_C_BODY" | grep -qF 'REGISTER_ROW_COUNT' && printf '%s\n' "$APPENDIX_C_BODY" | grep -qF 'CARDINALITY_MATCH = TRUE' && printf '%s\n' "$APPENDIX_C_BODY" | grep -qF 'ALL_CITATIONS_VERIFIED = TRUE' && printf '%s\n' "$APPENDIX_C_BODY" | grep -qF 'ALL_OWNERS_ASSIGNED = TRUE' && REVIEWER_PHRASE=$(grep -cF 'OPEN-QUESTIONS.md is reviewer-ready' .planning/OPEN-QUESTIONS.md) && [ "$REVIEWER_PHRASE" = "1" ] && bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; FINAL=$?; echo "FINAL_EXIT=$FINAL"; [ "$FINAL" = "0" ] && test -f .planning/phases/04-open-questions/04-05-SUMMARY.md && grep -qF 'reviewer-ready' .planning/phases/04-open-questions/04-05-SUMMARY.md && grep -qF 'all 5 conditions TRUE' .planning/phases/04-open-questions/04-05-SUMMARY.md && echo SYNTHESIS_COMPLETE_REVIEWER_READY</automated>
  </verify>
  <acceptance_criteria>
    - Appendix C body contains all 5 reconciliation-result substrings: 5 individual `grep -qF` checks succeed (INPUT_COUNT_AFTER_DEDUP / REGISTER_ROW_COUNT / CARDINALITY_MATCH = TRUE / ALL_CITATIONS_VERIFIED = TRUE / ALL_OWNERS_ASSIGNED = TRUE)
    - Appendix C body cites Phase 2 02-10 + Phase 3 03-07 algorithm precedent: `grep -qF '02-10'` AND `grep -qF '03-07'` against Appendix C body
    - Reviewer-ready phrase appears EXACTLY ONCE: `grep -cF 'OPEN-QUESTIONS.md is reviewer-ready' .planning/OPEN-QUESTIONS.md` returns `1`
    - **Final structural-check exits 0**: `bash .planning/phases/04-open-questions/scripts/openquestions-structure-check.sh; echo $?` prints `0`
    - All 13 assertions A1..A13 PASS — no `FAIL: ...` printed to stderr
    - **Cross-AI C9 no-placeholder synthesis-end:** `grep -qE 'Populated by|placeholder|04-0[1-9]' .planning/OPEN-QUESTIONS.md` returns NO MATCHES (i.e. `grep -qE` exits non-zero; `! grep` succeeds). The skeleton from 04-01 contained `Populated by 04-NN` notes; Plans 04-02..04-04 progressively replaced them; Plan 04-05 (this plan) replaces the LAST 6 (preamble + Executive Summary + How-to-read + Appendix A + Appendix B + Appendix C). If any match remains, STOP and identify the orphan stub. Note: legitimate "Plan 04-NN" references in citation context don't match (the regex targets `Populated by`, the literal word `placeholder`, and bare plan-IDs `04-01..04-09`).
    - SUMMARY.md exists at `.planning/phases/04-open-questions/04-05-SUMMARY.md`
    - SUMMARY.md mentions "reviewer-ready" + "all 5 conditions TRUE" + "Phase 4 approval gate": 3 individual `grep -qF` checks
    - SUMMARY.md records the actual N values (INPUT_COUNT_AFTER_DEDUP / REGISTER_ROW_COUNT)
    - Cumulative D-N pool frozen at D-55 (no D-56+ introduced): `grep -cE 'D-5[6-9]|D-[6-9][0-9]' .planning/OPEN-QUESTIONS.md` returns 0 (sanity check; not a hard structural-check assertion but a discipline check)
  </acceptance_criteria>
  <done>
    Synthesis complete. OPEN-QUESTIONS.md fully populated end-to-end: preamble blockquote + Executive Summary 3-table + How-to-read 5-paragraph + 7 OPEN-NN sections (Plans 04-02..04-04) + Appendix A per-phase rollup + Appendix B traceability (1:1 cardinality) + Appendix C reconciliation algorithm result (5 conditions TRUE). Final structural-check exits 0; all 13 assertions A1..A13 PASS. Reviewer-ready phrase emitted exactly once at end of closing italics paragraph. Per cross-AI C9 (codex MEDIUM): no leftover `Populated by 04-NN` / `placeholder` / `04-0[1-9]` strings remain — synthesis end-state confirmed clean. **OPEN-QUESTIONS.md is structurally complete AND reviewer-ready** (terminal state — phrase reserved per cross-AI MEDIUM #7 carried). Phase 4 deliverable awaits approval gate review per ROADMAP; after approval = milestone v2.0 Implementor Edition design-complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reconciliation cardinality match (CARDINALITY_MATCH = TRUE) | Tampering class. If REGISTER_TOTAL != APPENDIX_B_ROWS, structural-check A10 fails AND reconciliation reports FALSE. Mitigation: Task 2 acceptance criteria assert equal counts; Task 3 Step 1 re-verifies before writing Appendix C result. |
| Reviewer-ready phrase exactly-once | Wording class (terminal-state contract per cross-AI MEDIUM #7). If phrase appears in earlier sections, the terminal-state signal is corrupted. Mitigation: acceptance criterion `grep -cF` returns exactly `1`. |
| Final structural-check 13 assertions PASS | Tampering class — extreme. The script catches ANY drift in closed enums, citations, ownership, cardinality, placeholder strings. If FINAL_EXIT != 0, OPEN-QUESTIONS.md is NOT reviewer-ready and synthesis is incomplete. Mitigation: Task 3 Step 4 mandates re-running structural-check; SUMMARY.md cannot claim reviewer-ready unless FINAL_EXIT = 0. |
| Register-total floor 22 (A4) ↔ actual rows | Edge case. If Wave 4 lands exactly 21 rows (no namespace sub-row in OPEN-06), A4 fails. Mitigation: Task 3 Step 4 documents two options (add OPEN-Q17.1 sub-row to OPEN-04 OR adjust A4 floor); SUMMARY.md records the choice. Preferred: option (a) — D-51 already reserved a sub-row; making the pointer-with-stub explicit satisfies the reservation cleanly. |
| Cross-AI C9 no-placeholder synthesis-end | Tampering class. Leftover stubs (`Populated by 04-04`, plan-ID `04-03`) silently corrupt the document. Mitigation: structural-check A13 catches; acceptance criterion `! grep -qE` MUST succeed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | Reconciliation cardinality | mitigate | Task 2 + Task 3 Step 1 + structural-check A10 enforce |
| T-04-14 | Repudiation | Reviewer-ready phrase corruption | mitigate | Acceptance criterion `grep -cF` returns exactly 1 |
| T-04-15 | Tampering | Final structural-check final exit code | mitigate | Task 3 Step 4 mandates exit 0 before SUMMARY |
| T-04-16 | Tampering | Register-total floor | mitigate | Documented options + SUMMARY records the choice |
| T-04-17 | Tampering | Leftover stubs | mitigate | A13 + acceptance criterion |
</threat_model>

<verification>
- Preamble + Executive Summary + How-to-read finalised
- Appendix A per-phase rollup populated
- Appendix B traceability table 1:1 cardinality with register-total
- Appendix C reconciliation result block present; all 5 conditions TRUE
- Reviewer-ready phrase emitted exactly once at end
- Final structural-check exits 0; all 13 assertions PASS
- SUMMARY.md committed; declares Phase 4 deliverable reviewer-ready
</verification>

<success_criteria>
- `.planning/OPEN-QUESTIONS.md` complete end-to-end; structural-check exits 0
- All 13 assertions A1..A13 PASS
- Reconciliation algorithm executed; all 5 conditions TRUE
- Reviewer-ready terminal-state phrase appears exactly once
- No leftover placeholder/plan-ID strings (cross-AI C9 carried)
- `.planning/phases/04-open-questions/04-05-SUMMARY.md` committed; declares OPEN-QUESTIONS.md reviewer-ready + milestone-design-complete pending approval gate
- D-N pool stays frozen at D-55 (Phase 4 introduces no new D-N decisions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-open-questions/04-05-SUMMARY.md` per template.
</output>
