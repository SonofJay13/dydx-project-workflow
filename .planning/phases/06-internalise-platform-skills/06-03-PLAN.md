---
phase: 06-internalise-platform-skills
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - dydx-delivery/skills/platform-ziflow/SKILL.md
  - dydx-delivery/skills/platform-ziflow/references/api-contract.md
  - dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md
  - dydx-delivery/skills/platform-ziflow/references/knowledge-ingestion.md
  - dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md
  - dydx-delivery/skills/platform-ziflow/references/vocabulary.md
  - dydx-delivery/skills/platform-ziflow/.gitkeep
  - dydx-delivery/skills/platform-ziflow/references/.gitkeep
autonomous: true
requirements: [PLAT-03, PLAT-04, PLAT-05, PLAT-06]
tags: [platform-skills, ziflow, wait_for_proof, webhook-primary, q05, reviewai]

must_haves:
  truths:
    - "Per D-63, plan 06-03 ships the full platform-ziflow/ skill tree end-to-end (SKILL.md + 5 references/) in a single atomic unit; reviewers grade Ziflow as a whole."
    - "Per Q05 vendor research (2026-05-11), api-contract.md documents webhook-PRIMARY framing at the top (vendor explicitly recommends webhooks over polling per help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks); wait_for_proof is the FALLBACK only."
    - "Per DESIGN-16 / MOD-6, wait_for_proof helper contract uses 7-part D-64 structure with defaults max_wait_s=30 / interval_s=2; eventual-consistency window documented."
    - "Per UAT-6.1 and PLAT-05, SKILL.md frontmatter carries native_ai_path: paste; the literal token 'native_ai_path: api' appears nowhere in the platform-ziflow/ tree (Threat T-06-02 mitigation)."
    - "Per MOD-7 / D-68, SKILL.md frontmatter carries tier_claims_last_verified: 2026-05-09 baseline (Ziflow ReviewAI is high-churn — Change Verification + Brand Standards 'Coming Soon' per DESIGN-16 — so this date will likely move first among the three platforms)."
    - "Per D-67, OPEN-Q05 (Ziflow read-after-create consistency window) resolves inline as webhook-PRIMARY + 30s/2s polling fallback defaults landing in api-contract.md; OPEN-QUESTIONS.md row flip lives in synthesis plan 06-04."
    - "Per D-65, client-shape-gotchas.md seeded with generic Acme placeholder + TBD-at-first-engagement markers for Up & Up Group + VodafoneZiggo (per .claude/memory/reference_client_brain_coda_docs.md — those are the 2 documented Coda-brain clients)."
    - "Per DESIGN-16, native-ai-inventory.md documents 4-row Ziflow ReviewAI matrix (Checklists Public Preview HIGH + Change Verification 'Coming Soon' MEDIUM + Brand Standards 'Coming Soon' MEDIUM + Checklist Templates API GA April 2026)."
  artifacts:
    - path: "dydx-delivery/skills/platform-ziflow/SKILL.md"
      provides: "Ziflow platform reference skill (PLAT-03) with D-59 hard-rules pointer + D-68 re-verification trigger + locked frontmatter (PLAT-04, PLAT-05)"
      contains: "tier_claims_last_verified"
    - path: "dydx-delivery/skills/platform-ziflow/references/api-contract.md"
      provides: "Ziflow REST API contract — webhook-PRIMARY framing + wait_for_proof FALLBACK helper (MOD-6, Q05 resolution)"
      contains: "wait_for_proof"
    - path: "dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md"
      provides: "Ziflow ReviewAI capability matrix (Checklists Public Preview + Change Verification + Brand Standards) per DESIGN-16"
      contains: "ReviewAI"
    - path: "dydx-delivery/skills/platform-ziflow/references/knowledge-ingestion.md"
      provides: "Ziflow paste-only ingestion contract (UAT-6.1) — ReviewAI Checklist criteria + copy-paste fallback"
      contains: "Checklist"
    - path: "dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md"
      provides: "Ziflow per-client shape variant taxonomy seeded with Acme placeholder + TBD markers for Up & Up Group + VodafoneZiggo (D-65)"
      contains: "Acme"
    - path: "dydx-delivery/skills/platform-ziflow/references/vocabulary.md"
      provides: "Ziflow-specific terms only (D-66); pointer to dydx-delivery/references/glossary.md"
      contains: "proof"
  key_links:
    - from: "dydx-delivery/skills/platform-ziflow/SKILL.md"
      to: "dydx-delivery/references/safety-rules.md"
      via: "D-59 uniform hard-rules pointer (verbatim block)"
      pattern: "dydx-delivery/references/safety-rules\\.md"
    - from: "dydx-delivery/skills/platform-ziflow/references/api-contract.md"
      to: "help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks"
      via: "Q05 resolution — webhook-PRIMARY framing cites vendor doc URL"
      pattern: "help\\.ziflow\\.com"
    - from: "dydx-delivery/skills/platform-ziflow/references/vocabulary.md"
      to: "dydx-delivery/references/glossary.md"
      via: "D-66 pointer to project-wide glossary"
      pattern: "glossary\\.md"
---

<objective>
Wave 2 of Phase 6: ship the complete `skills/platform-ziflow/` tree end-to-end (SKILL.md + all 5 references/ files) in one atomic plan per D-63 per-platform slicing. Distinct directory ownership from 06-01 + 06-02 — `/gsd-execute-phase 6` can run 06-03 in parallel with 06-02 once 06-01's Wave 0 scaffold lands.

Purpose: Ziflow is the eventual-consistency-window platform (DESIGN-16 / MOD-6 — proof-create returns success before the proof is fully readable). Q05 vendor research (2026-05-11) discovered that Ziflow officially recommends webhooks over polling, so api-contract.md frames webhook subscription as PRIMARY and `wait_for_proof` as FALLBACK only. Ziflow ReviewAI is also the highest-churn 2026 capability surface (Change Verification + Brand Standards "Coming Soon") — D-68 per-platform distinct `tier_claims_last_verified:` dates will likely show Ziflow drifting earliest among the three platforms.

Output: 1 SKILL.md + 5 references/ files for Ziflow, all committed atomically per task. Replaces 06-01's Ziflow .gitkeep placeholders with real files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-internalise-platform-skills/06-CONTEXT.md
@.planning/phases/06-internalise-platform-skills/06-RESEARCH.md
@.planning/phases/06-internalise-platform-skills/06-PATTERNS.md
@.planning/phases/06-internalise-platform-skills/06-VALIDATION.md
@.planning/phases/06-internalise-platform-skills/06-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/DESIGN.md
@.planning/OPEN-QUESTIONS.md
@dydx-delivery/references/safety-rules.md
@dydx-delivery/references/glossary.md
@dydx-delivery/references/connector-matrix.md
@dydx-delivery/skills/platform-pipefy/SKILL.md
@dydx-delivery/skills/platform-pipefy/references/api-contract.md
@.claude/memory/reference_client_brain_coda_docs.md

<interfaces>
<!-- Ziflow SKILL.md frontmatter (verbatim from RESEARCH.md:704-713): -->
```yaml
---
name: platform-ziflow
description: Provide Ziflow REST API contract + 2026 ReviewAI capability matrix + per-client shape gotchas + vocabulary for any stage skill that targets a Ziflow tenant. Use when artefact frontmatter carries `platform: ziflow`. Documents the webhook-PRIMARY pattern for read-after-create signalling (vendor-recommended) + `wait_for_proof` fallback helper for environments without webhook delivery; ReviewAI Checklists Public Preview + Change Verification + Brand Standards capability tracking; paste-only native-AI ingestion per UAT-6.1. Does NOT execute API calls — pure reference content for skills + Stage 8 test bot.
frontmatter_version: 2
platform: ziflow
tier_claims_last_verified: 2026-05-09
native_ai_path: paste
---
```

<!-- D-59 hard-rules pointer (COPY VERBATIM — same as 06-01 + 06-02): -->
```markdown
## Hard rules

> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

<!-- wait_for_proof helper contract (FALLBACK ONLY) — verbatim 7-part from RESEARCH.md:399-447: -->

**IMPORTANT — Documentation framing:** Per 2026-05-11 vendor research, **Ziflow officially recommends webhooks over polling** (per `help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks` + `help.ziflow.com/hc/en-us/articles/30721919730836-Embedded-Review-Approval`). The canonical pattern is to subscribe to the `processed` event webhook BEFORE proof creation, so the platform notifies the caller when the proof is ready. `wait_for_proof` is the documented **FALLBACK** for environments where webhook delivery is unavailable (e.g., local dev, sandbox tests without exposed webhook URLs, harness runs).

**Signature:** `wait_for_proof(proof_id: str, max_wait_s: int = 30, interval_s: int = 2) -> dict`

**Behaviour:** Polls Ziflow's proof-get endpoint until the proof becomes readable (HTTP 200 + valid proof payload) or `max_wait_s` is exceeded. Handles the read-after-create eventual-consistency window (MOD-6) where Ziflow's proof-create call returns success before the proof is fully readable; subsequent reads within the consistency window may 404.

**Retry / poll budget:** `max_wait_s=30` total / `interval_s=2` between polls = up to ~15 polls per call. Defaults match DESIGN-16 conservative baseline. Q05 resolution: defaults **CONFIRMED** at 30s/2s for fallback path; webhook-primary path has no polling budget (event-driven).

**Failure modes:**
- HTTP 404 on each poll until `max_wait_s` exceeded → raise `ZiflowProofNotReady(proof_id, polls_attempted, total_wait_s)`.
- HTTP 401 / 403 on poll → raise `ZiflowAuthFailed` (NOT eventual-consistency; auth issue).
- HTTP 5xx on poll → 1 retry with `interval_s` backoff; subsequent 5xx → raise `ZiflowServerError`.
- Webhook delivery available + subscribed → caller should NOT use this helper; document the webhook path at the top of `api-contract.md`.

**Return shape:** `dict` — proof payload (id, status, version, project_id, urls, etc. per Ziflow proof-get response shape).

**Pseudocode** (verbatim from RESEARCH.md:419-443):
```python
def wait_for_proof(proof_id: str, max_wait_s: int = 30, interval_s: int = 2) -> dict:
    """
    FALLBACK ONLY — primary path is webhook subscription to 'processed' event.
    Use this helper when webhook delivery is unavailable (local dev / harness / sandbox).
    """
    deadline = time.time() + max_wait_s
    polls = 0
    while time.time() < deadline:
        polls += 1
        response = _get(f"/proofs/{proof_id}")
        if response.status_code == 200:
            return response.json()
        if response.status_code == 404:
            time.sleep(interval_s)
            continue
        if response.status_code in (401, 403):
            raise ZiflowAuthFailed(f"Auth failed at poll {polls}")
        if response.status_code >= 500:
            time.sleep(interval_s)
            response = _get(f"/proofs/{proof_id}")
            if response.status_code == 200:
                return response.json()
            raise ZiflowServerError(f"5xx persisted at poll {polls}")
    raise ZiflowProofNotReady(proof_id, polls, max_wait_s)
```

**Worked example:** Proof created at t=0; first read at t=2s → 404 (eventual consistency); read at t=4s → 404; read at t=6s → 200 + proof payload. Helper returns at poll #3 (total wait ~6s, well within 30s budget). Test-bot tier-1 verifies the wait completed within budget.

<!-- Ziflow Native-AI capability matrix (DESIGN-16 baseline; high-churn surface — Coming Soon items expected to flip): -->
| Capability | Available? | Surface | Confidence |
|---|---|---|---|
| ReviewAI Checklists | yes (Public Preview, Enterprise plan) | Ziflow ReviewAI → Checklists | HIGH — confirmed Public Preview per DESIGN-16 |
| Change Verification | **Coming Soon** | Ziflow ReviewAI → Change Verification | MEDIUM — vendor-announced future; PLAT-03 execution MAY check current GA status |
| Brand Standards | **Coming Soon** | Ziflow ReviewAI → Brand Standards | MEDIUM — vendor-announced future; PLAT-03 execution MAY check current GA status |
| Checklist Templates API | GA (April 2026 — Ziflow release notes 26.08) | Ziflow API → checklist-templates | HIGH — confirmed GA per 2026-05-11 vendor docs |
| ReviewAI knowledge-ingestion API | **CLOSED (UAT-6.1)** | n/a | UAT-6.1 — OPEN-Q03 closed; no API ingestion path through v2.6 |

<!-- D-65 client-shape-gotchas seed (Ziflow = Acme placeholder + TBD for Up & Up Group + VodafoneZiggo per .claude/memory/reference_client_brain_coda_docs.md): -->
```markdown
### Acme (DESIGN-16 worked example — placeholder, NOT a live tenant)

- **project_id:** `<acme-project-id>` (placeholder — replaced at first-engagement)
- **Source artefact:** DESIGN-16 at `.planning/DESIGN.md:502-543`
- **Note:** This row exists as a structural placeholder. The first real client engagement populates a verified row above this one and demotes Acme to "example" status.

### Up & Up Group (TBD at first Ziflow engagement)

- **project_id:** `<TBD at first engagement>`
- **review_workflow_stages:** `<TBD>`
- **Source artefact:** `<TBD — populate from first engagement>`

### VodafoneZiggo (TBD at first Ziflow engagement)

- **project_id:** `<TBD at first engagement>`
- **review_workflow_stages:** `<TBD>`
- **Source artefact:** `<TBD — populate from first engagement>`
```

<!-- Per-platform doc URL anchors (D-68): -->
- Ziflow: `https://api-docs.ziflow.com/` + `https://www.ziflow.com/reviewai`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer ↔ canonical references | Phase 6 doc-only; Ziflow ReviewAI is high-churn (Change Verification + Brand Standards "Coming Soon") — stale tier claims most likely here first |
| platform-ziflow/ tree ↔ downstream consumer skills | Stage 4a/7b/8/10 read SKILL.md frontmatter + references/* at session start; polling-first documentation without webhook-primary framing pollutes Stage 8 test-bot design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `tier_claims_last_verified:` ISO date — Ziflow most likely to drift first | mitigate | A5 assertion enforces ISO-date format; manual re-verify documented per VALIDATION § Manual-Only Verifications |
| T-06-02 | Information disclosure (banned value leak) | `native_ai_path: api` reappearing via copy-paste | mitigate | A7 grep gate exits 1 on any hit across platform-ziflow/ |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: platform-ziflow/SKILL.md — frontmatter + intro + hard-rules pointer (D-59) + re-verification trigger (D-68) + what-this-provides</name>
  <files>dydx-delivery/skills/platform-ziflow/SKILL.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/SKILL.md (06-01 sibling — shape to mirror)
    - dydx-delivery/skills/platform-wrike/SKILL.md (06-02 sibling — same shape)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:704-713 (Ziflow frontmatter verbatim template)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:717-735 (D-68 re-verification trigger; Ziflow doc URLs)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md spec_lock DESIGN-16 + UAT-6.1
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-ziflow/SKILL.md` mirroring the platform-pipefy + platform-wrike SKILL.md shape exactly. After Write, delete the `.gitkeep` placeholder at `dydx-delivery/skills/platform-ziflow/.gitkeep`.

**1. YAML frontmatter (verbatim from interfaces block):**
```yaml
---
name: platform-ziflow
description: Provide Ziflow REST API contract + 2026 ReviewAI capability matrix + per-client shape gotchas + vocabulary for any stage skill that targets a Ziflow tenant. Use when artefact frontmatter carries `platform: ziflow`. Documents the webhook-PRIMARY pattern for read-after-create signalling (vendor-recommended) + `wait_for_proof` fallback helper for environments without webhook delivery; ReviewAI Checklists Public Preview + Change Verification + Brand Standards capability tracking; paste-only native-AI ingestion per UAT-6.1. Does NOT execute API calls — pure reference content for skills + Stage 8 test bot.
frontmatter_version: 2
platform: ziflow
tier_claims_last_verified: 2026-05-09
native_ai_path: paste
---
```

**2. Title + intro:**
```markdown
# platform-ziflow

Ziflow platform reference for the dydx-delivery plugin. Any stage skill operating
against a Ziflow tenant loads this skill alongside the stage-specific skill;
artefact frontmatter `platform: ziflow` is the routing key.
```

**3. `## Inputs` + `## Output`:**
```markdown
## Inputs

- Artefact frontmatter carrying `platform: ziflow` and a populated `sandbox:` block (loaded by the calling stage skill)
- Phase 5 canonical references at `dydx-delivery/references/`

## Output

This skill produces NO artefacts. It is a reference skill — the calling stage skill produces the versioned output.
```

**4. `## Hard rules` (D-59 — VERBATIM):**
```markdown
## Hard rules

> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

**5. `## What this skill provides`:**
```markdown
## What this skill provides

- `references/api-contract.md` — REST endpoint (per-tenant base URL), auth header shape, webhook-PRIMARY framing (Ziflow officially recommends webhooks over polling per Q05 vendor research 2026-05-11), `wait_for_proof` FALLBACK helper contract (MOD-6) with `max_wait_s=30` / `interval_s=2` defaults.
- `references/native-ai-inventory.md` — 2026 Ziflow ReviewAI capability matrix (Checklists Public Preview + Change Verification + Brand Standards + Checklist Templates API) per DESIGN-16.
- `references/knowledge-ingestion.md` — Paste-only Stage 10 path (UAT-6.1): ReviewAI Checklist criteria + copy-paste fallback.
- `references/client-shape-gotchas.md` — Per-client tenant variations (Acme placeholder + TBD markers for Up & Up Group + VodafoneZiggo; append-only as new clients onboard).
- `references/vocabulary.md` — Ziflow-specific terms (proof / review / decision / stage / version / project_id / ReviewAI / Checklist / Change Verification / Brand Standards).
```

**6. `## Re-verification trigger` (D-68 — Ziflow doc URLs):**
```markdown
## Re-verification trigger

Re-verify `references/native-ai-inventory.md` against current Ziflow ReviewAI
documentation BEFORE any v2.x phase that consumes the capability matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — ReviewAI Checklist criteria)

Update `tier_claims_last_verified:` on this SKILL.md frontmatter after each
re-verification, citing the source doc URL + date in commit message.

Doc URL anchors:
- Ziflow: https://api-docs.ziflow.com/ + https://www.ziflow.com/reviewai

**High-churn note:** Ziflow ReviewAI is in active expansion — Change Verification +
Brand Standards are listed as "Coming Soon" per DESIGN-16. This `tier_claims_last_verified:`
date will likely drift forward earliest among the three platform skills; PLAT-03
execution MAY bump it forward if the Coming Soon items have shipped.
```

Delete `.gitkeep` placeholder: `rm dydx-delivery/skills/platform-ziflow/.gitkeep`.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-ziflow/SKILL.md && grep -qE '^tier_claims_last_verified: 2026-05-09$' dydx-delivery/skills/platform-ziflow/SKILL.md && grep -qE '^native_ai_path: paste$' dydx-delivery/skills/platform-ziflow/SKILL.md && grep -qE '^platform: ziflow$' dydx-delivery/skills/platform-ziflow/SKILL.md && grep -qF 'dydx-delivery/references/safety-rules.md' dydx-delivery/skills/platform-ziflow/SKILL.md && grep -qE '^## Re-verification trigger$' dydx-delivery/skills/platform-ziflow/SKILL.md && grep -qF 'api-docs.ziflow.com' dydx-delivery/skills/platform-ziflow/SKILL.md && [ ! -f dydx-delivery/skills/platform-ziflow/.gitkeep ] && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A3'</automated>
  </verify>
  <done>SKILL.md exists with valid YAML frontmatter (platform: ziflow, tier_claims_last_verified: 2026-05-09, native_ai_path: paste, frontmatter_version: 2); D-59 hard-rules pointer present verbatim; D-68 re-verification trigger present with Ziflow doc URLs and high-churn note; A3 PASS.</done>
  <acceptance_criteria>
    - Satisfies A3: `[ -f dydx-delivery/skills/platform-ziflow/SKILL.md ]` exits 0
    - Satisfies A5 (Ziflow partition): `grep -qE '^tier_claims_last_verified: [0-9]{4}-[0-9]{2}-[0-9]{2}$' <file>` exits 0
    - Satisfies A6 (Ziflow partition): `grep -qE '^native_ai_path: (paste|none)$' <file>` exits 0
    - Satisfies A7 partial: `grep -q 'native_ai_path: api' <file>` exits 1
    - `platform: ziflow` exact-token frontmatter: `grep -qE '^platform: ziflow$' <file>` exits 0
    - D-59 pointer verbatim: `grep -qF 'See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.' <file>` exits 0
    - D-68 trigger H2 + Ziflow doc URLs: `grep -qE '^## Re-verification trigger$' <file>` AND `grep -qF 'api-docs.ziflow.com' <file>` AND `grep -qF 'ziflow.com/reviewai' <file>` all exit 0
    - High-churn note present: `grep -qF 'Coming Soon' <file>` exits 0
    - `.gitkeep` placeholder removed: `[ ! -f dydx-delivery/skills/platform-ziflow/.gitkeep ]` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: platform-ziflow/references/api-contract.md — webhook-PRIMARY framing (Q05 resolution) + wait_for_proof FALLBACK contract (MOD-6) + auth + parked-ref</name>
  <files>dydx-delivery/skills/platform-ziflow/references/api-contract.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/references/api-contract.md (06-01 sibling — shape to mirror; webhook-PRIMARY block goes ABOVE the helper-contract block to enforce framing per Q05)
    - dydx-delivery/skills/platform-wrike/references/api-contract.md (06-02 sibling)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:399-447 (wait_for_proof 7-part helper contract verbatim)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:454-494 (api-contract.md outline)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:624-640 (Q05 vendor research resolution — webhook-PRIMARY + vendor doc URLs)
    - .planning/DESIGN.md:502-543 (DESIGN-16 — wait_for_proof + Ziflow ReviewAI capability matrix)
    - dydx-delivery/phases/06-internalise-platform-skills/06-PATTERNS.md:204-217 (Ziflow-specific helper-contract + MCP availability "No MCP")
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-ziflow/references/api-contract.md`. CRITICAL ordering note: the `## Webhook-PRIMARY (canonical pattern)` H2 MUST appear BEFORE the `## wait_for_proof` helper-contract H2 — A13 grep gate enforces webhook framing visibility per Q05 resolution.

**1. Title + scope-lock preamble:**
```markdown
# Ziflow — API contract

> Canonical SoT for Ziflow API contracts per DESIGN-16 (`.planning/DESIGN.md:502-543`). Documents the per-tenant REST base URL, auth shape, webhook-PRIMARY pattern (vendor-recommended per Q05 vendor research 2026-05-11), `wait_for_proof` FALLBACK helper, multi-tenant auth-concurrency, and MCP availability (none — direct REST only).
>
> **Scope locks (UAT 2026-05-10):**
> - **Q05 resolution (2026-05-11):** Ziflow officially recommends webhooks over polling. The canonical pattern is to subscribe to the `processed` event webhook BEFORE proof creation. `wait_for_proof` is the documented FALLBACK for environments where webhook delivery is unavailable.
> - **UAT-3.5:** Ziflow has no MCP — direct REST only.
> - **UAT-6.1:** Native-AI ingestion APIs OUT-OF-SCOPE — `native_ai_path: paste | none` only. See `references/knowledge-ingestion.md`.
> - **Q25 (Phase 5 resolution):** Auth-concurrency class default = `exclusive`; live tenant test deferred to PLAT-03 kickoff verification.
>
> **Cross-refs:**
> - Sandbox enforcement: `dydx-delivery/references/safety-rules.md` § "1. Sandbox enforcement"
> - Connector probe: `dydx-delivery/references/connector-matrix.md` (Phase 5 canonical)
> - Glossary: `dydx-delivery/skills/platform-ziflow/references/vocabulary.md` (Ziflow-specific terms)
```

**2. Auth-shape secret-handling note** (lift verbatim from platform-pipefy/wrike api-contract.md):
```markdown
> **Auth header & secret-handling note:** All helper contracts below document the auth header NAME + value SHAPE only (e.g., `Authorization: Bearer <token>`). NEVER paste real tokens, API keys, OAuth refresh tokens, or session cookies into this file. Tokens live in per-tenant `client_state.yaml` (gitignored) or env vars — never in the canonical reference set.
```

**3. `## Endpoint(s) + auth shape`:**
```markdown
## Endpoint(s) + auth shape

- **Base URL (per-tenant — populated from OAuth token-exchange or per-tenant configuration):** `https://<tenant-host>/api/v2` (final pattern at PLAT-03 kickoff verification per Q25)
- **Auth header:** `Authorization: Bearer <token>` (header NAME only — see secret-handling note above)
- **Per-tenant variants:** tenant-host (populated at first engagement); `project_id` per workspace
- Sandbox tenant only — see `dydx-delivery/references/safety-rules.md` Rule 1.
```

**4. `## Webhook-PRIMARY (canonical pattern) — Q05 resolution`:**
```markdown
## Webhook-PRIMARY (canonical pattern) — Q05 resolution

**Ziflow officially recommends webhooks over polling for proof-ready signalling.** Per vendor guidance (2026-05-11 research; sources: `help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks` + `help.ziflow.com/hc/en-us/articles/30721919730836-Embedded-Review-Approval`):

> "Use webhooks instead of polling the Ziflow API for updates." — Ziflow Help Center

**Canonical flow:**
1. Subscribe to the `processed` event webhook for the target project BEFORE creating a proof.
2. Create the proof. The proof-create call returns success before the proof is fully readable (eventual consistency window per MOD-6).
3. Ziflow delivers the `processed` event to the subscribed webhook endpoint when the proof is ready.
4. The caller reads the proof via `GET /proofs/{proof_id}` after webhook delivery — no polling needed.

**When to use `wait_for_proof` instead:** Local dev, sandbox harness runs, environments without an exposed webhook URL. See § `wait_for_proof` (FALLBACK) below.

**Vendor doc anchors:**
- `help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks` — webhook subscription mechanics
- `help.ziflow.com/hc/en-us/articles/30721919730836-Embedded-Review-Approval` — embedded review-approval flow (calls out webhook-primary)
```

**5. `## Rate limit + throttle`** (no per-call calibration needed; polling-bounded by `max_wait_s`):
```markdown
## Rate limit + throttle

- **Published ceiling:** Phase 5 / Phase 6 did not surface a Ziflow per-second/per-minute rate-limit publication. The bounded budget for `wait_for_proof` is `max_wait_s=30` total with `interval_s=2` between polls = up to ~15 polls per call.
- **Webhook-primary path:** event-driven; no per-call rate-limit applies (single webhook delivery per proof-ready transition).
- **429 handling (helper-internal):** If Ziflow returns 429 on a poll inside `wait_for_proof`, retry once with `interval_s` backoff; subsequent 429 → propagate via the FAILURE MODES `ZiflowServerError` path (5xx-equivalent treatment).
- **Re-verify at PLAT-03 kickoff:** if the published rate limit is surfaced during execution, record it here AND in `dydx-delivery/references/connector-matrix.md` for future calibration.
```

**6. `## wait_for_proof` (FALLBACK ONLY — D-64 7-part contract):**

Embed verbatim from the interfaces block: the IMPORTANT framing block (Ziflow officially recommends webhooks), then signature, behaviour, retry/poll budget, failure modes, return shape, pseudocode (the 24-line Python block), worked example. Use H2 `## wait_for_proof`. Make the first sentence of the section: `**FALLBACK ONLY** — primary path is webhook subscription to the \`processed\` event (see § Webhook-PRIMARY above).`

Add a single-line source citation at the end: `**Source:** DESIGN-16 helper spec at \`.planning/DESIGN.md:531\` + Q05 vendor research 2026-05-11 (\`help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks\` webhook-PRIMARY guidance + \`help.ziflow.com/hc/en-us/articles/30721919730836-Embedded-Review-Approval\` "use webhooks instead of polling" canonical statement).`

**7. `## Multi-tenant auth concurrency`:**
```markdown
## Multi-tenant auth concurrency

- **Class (default):** `exclusive` per Phase 5 Q25 resolution (Pipefy precedent under UAT-4.2). LOW confidence — live tenant test deferred to PLAT-03 kickoff verification.
- **`client_state.yaml` field:** `ziflow.auth_concurrency_class: exclusive` (per DESIGN-29; populated by Phase 6 PLAT-03 probe).
```

**8. `## MCP availability (UAT-3.5 — PARKED REFERENCE)`:**
```markdown
## MCP availability (UAT-3.5 — PARKED REFERENCE)

Ziflow has **no MCP** — direct REST is the only access path through v2.6 (and likely beyond). MCP adoption is not currently a vendor offering.
```
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qF 'max_wait_s=30' dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qF 'interval_s=2' dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qiF 'webhook' dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qE '^## wait_for_proof' dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qF 'help.ziflow.com' dydx-delivery/skills/platform-ziflow/references/api-contract.md && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A13'</automated>
  </verify>
  <done>api-contract.md exists with webhook-PRIMARY framing ABOVE the helper-contract block (Q05 resolution); wait_for_proof 7-part contract with max_wait_s=30 / interval_s=2 defaults (MOD-6); A13 assertion PASS.</done>
  <acceptance_criteria>
    - Satisfies A13: `grep -qF 'max_wait_s=30' <file> && grep -qF 'interval_s=2' <file> && grep -qF 'webhook' <file>` all conditions met
    - Webhook-PRIMARY H2 present and appears BEFORE wait_for_proof H2: `awk '/^## Webhook-PRIMARY/ {primary=NR} /^## wait_for_proof/ {fallback=NR} END {exit !(primary < fallback)}' <file>` exits 0
    - Q05 vendor doc URLs cited: `grep -qF 'help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks' <file>` exits 0
    - wait_for_proof signature present: `grep -qF 'Signature:' <file>` exits 0
    - FALLBACK ONLY label present in section body: `grep -qF 'FALLBACK ONLY' <file>` exits 0
    - Auth-shape secret-handling note present: `grep -qF 'NEVER paste real tokens' <file>` exits 0
    - MCP availability "No MCP" parked-ref present: `grep -qF 'Ziflow has **no MCP**' <file>` exits 0
    - Satisfies A7 partial (T-06-02 file-level): `grep -qF 'native_ai_path: api' <file>` exits 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: platform-ziflow/references/native-ai-inventory.md — 2026 Ziflow ReviewAI capability matrix (DESIGN-16)</name>
  <files>dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md (06-01 sibling — shape to mirror)
    - dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md (06-02 sibling — shape)
    - .planning/DESIGN.md:502-543 (DESIGN-16 Ziflow ReviewAI capability matrix)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:499-526 (outline)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:615 (Ziflow specifics row — Checklists Public Preview + Coming Soon items + Checklist Templates API GA April 2026)
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md` mirroring the platform-pipefy / platform-wrike native-ai-inventory.md shape.

**1. Title + frontmatter-sync note:**
```markdown
# Ziflow — Native-AI inventory

> `tier_claims_last_verified:` matches SKILL.md frontmatter value (2026-05-09 baseline per D-68; updated to execution date if fresh re-verification happens during this plan).
>
> **High-churn note:** Ziflow ReviewAI is in active expansion — Change Verification + Brand Standards are listed as "Coming Soon". Expect this date to drift forward earlier than the Pipefy / Wrike matrices.
```

**2. `## Re-verification trigger`:**
```markdown
## Re-verification trigger

Re-verify this capability matrix against current Ziflow ReviewAI documentation
(`https://api-docs.ziflow.com/` + `https://www.ziflow.com/reviewai`) BEFORE any
v2.x phase that consumes the matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — ReviewAI Checklist criteria)

Update `tier_claims_last_verified:` on `dydx-delivery/skills/platform-ziflow/SKILL.md`
frontmatter after each re-verification, citing the source doc URL + date in commit
message.
```

**3. `## Capability matrix (2026-grounded)` (5-row matrix from interfaces block):**
```markdown
## Capability matrix (2026-grounded)

| Capability | Available? | Surface | Confidence |
|---|---|---|---|
| ReviewAI Checklists | yes (Public Preview, Enterprise plan) | Ziflow ReviewAI → Checklists | HIGH — confirmed Public Preview per DESIGN-16 |
| Change Verification | **Coming Soon** | Ziflow ReviewAI → Change Verification | MEDIUM — vendor-announced future; PLAT-03 execution MAY check current GA status |
| Brand Standards | **Coming Soon** | Ziflow ReviewAI → Brand Standards | MEDIUM — vendor-announced future; PLAT-03 execution MAY check current GA status |
| Checklist Templates API | GA (April 2026 — Ziflow release notes 26.08) | Ziflow API → checklist-templates | HIGH — confirmed GA per 2026-05-11 vendor docs |
| ReviewAI knowledge-ingestion API | **CLOSED (UAT-6.1)** | n/a | UAT-6.1 — OPEN-Q03 closed; no API ingestion path through v2.6 |
```

**4. `## Paste-only delivery (UAT-6.1)`:**
```markdown
## Paste-only delivery (UAT-6.1)

`native_ai_path: paste | none` ONLY — `api` branch is FORBIDDEN. The Stage 10 paste bundle is the only supported ingestion path through v2.6; humans upload ReviewAI Checklist criteria manually via the Ziflow UI. See `references/knowledge-ingestion.md` for the paste-bundle shape.
```
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md && grep -qE '^## Capability matrix' dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md && grep -qF 'ReviewAI' dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md && grep -qF 'Checklists Public Preview' dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md && grep -qF 'Coming Soon' dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md && grep -qF 'UAT-6.1' dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md && ! grep -qF 'native_ai_path: api' dydx-delivery/skills/platform-ziflow/references/native-ai-inventory.md</automated>
  </verify>
  <done>native-ai-inventory.md exists with 5-row 2026 Ziflow ReviewAI capability matrix (Checklists Public Preview + Change Verification + Brand Standards + Checklist Templates API GA + knowledge-ingestion API CLOSED); re-verification trigger present; paste-only delivery section present; A17 PASS for ziflow.</done>
  <acceptance_criteria>
    - Satisfies A17 (ziflow partition): `grep -qE '^## Capability matrix' <file>` exits 0 AND `grep -cE '^\| (ReviewAI Checklists|Change Verification|Brand Standards|Checklist Templates|ReviewAI knowledge-ingestion)' <file>` returns >= 5
    - 2026 vendor docs grounded: `grep -qF 'ReviewAI' <file>` AND `grep -qF 'Checklists Public Preview' <file>` both exit 0
    - Coming Soon items tracked: `grep -qF 'Coming Soon' <file>` exits 0
    - UAT-6.1 paste-only section present: `grep -qF 'UAT-6.1' <file>` exits 0
    - High-churn note present: `grep -qF 'high-churn' <file>` OR `grep -qF 'High-churn' <file>` exits 0
    - Satisfies A7 partial (T-06-02): `grep -qF 'native_ai_path: api' <file>` exits 1
    - Re-verification trigger H2 present: `grep -qE '^## Re-verification trigger$' <file>` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: platform-ziflow/references/{knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md} — paste-only path + Acme + TBD seeds + Ziflow-specific terms</name>
  <files>dydx-delivery/skills/platform-ziflow/references/knowledge-ingestion.md, dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md, dydx-delivery/skills/platform-ziflow/references/vocabulary.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/references/{knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md} (06-01 siblings — shape to mirror)
    - dydx-delivery/skills/platform-wrike/references/{knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md} (06-02 siblings)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:528-606 (per-file outlines)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:259-354 (Ziflow-specific seed content)
    - .planning/DESIGN.md:502-543 (DESIGN-16 — Acme worked example + Ziflow terms)
    - .claude/memory/reference_client_brain_coda_docs.md (Up & Up Group + VodafoneZiggo are the 2 documented Coda-brain clients — D-65 TBD-stub targets)
    - dydx-delivery/references/glossary.md (D-66 dedup gate target)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md D-65 + D-66
  </read_first>
  <action>
Create three files together (single atomic task — 5-file shape parity per D-63). After Write, delete the `.gitkeep` placeholder at `dydx-delivery/skills/platform-ziflow/references/.gitkeep`.

**FILE A: `dydx-delivery/skills/platform-ziflow/references/knowledge-ingestion.md`**

```markdown
# Ziflow — Knowledge ingestion

## Primary path (paste-only per UAT-6.1)

`native_ai_path: paste`. Stage 10 (`push-native-ai-knowledge` skill, v2.5 / Phase 7) emits a paste bundle containing:

1. **ReviewAI Checklist criteria** — the Checklist criteria block authored from approved Stage 9 documentation fragments (see DESIGN-26 paste-bundle contract). Includes per-criterion description + expected-behaviour + pass/fail conditions per the ReviewAI Checklists Public Preview surface.
2. **Per-client target ID** — Ziflow ReviewAI target reference from `<Client> Brain/00_HUB.md` `Ziflow AI:` block (DESIGN-26 / MIN-4 fix).

The paste bundle is human-upload-only via the Ziflow UI. No API ingestion path is exercised through v2.6 (UAT-6.1; OPEN-Q03 closed).

## Audit log shape

Stage 10 records each ingestion in `<Client> Brain/changelog/cr-<id>/native-ai-audit.md` with these fields:

- `ingested_at:` ISO-8601 timestamp
- `doc_version:` semver from the approved Stage 9 doc
- `target_id:` Ziflow ReviewAI target reference (matches `00_HUB.md` `Ziflow AI:` block)
- `paste_bundle_path:` local path to the bundle artefact emitted by Stage 10
- `human_upload_acknowledged_at:` ISO-8601 timestamp recorded after the human confirms the upload in Ziflow UI

Refuses ingest if `doc_published_at < last_diff_review_at` (CRIT-8 fix per DESIGN-26) or `target_id` mismatches the artefact's `client:` frontmatter (MIN-4 fix).

## Fallback (manual paste via UI)

Paste-only IS the fallback. UAT-6.1 forecloses an API ingestion path through v2.6; humans manually upload Checklist criteria via the Ziflow ReviewAI UI. Copy-paste is the canonical path per DESIGN-16. The Stage 10 paste-bundle artefact + audit-log entry are the canonical record that ingestion happened.
```

**FILE B: `dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md`**

```markdown
# Ziflow — Client shape gotchas

> Append-only — new client shapes added per first-engagement; existing rows
> preserved for audit trail.

## Known shapes (verified)

### Acme (DESIGN-16 worked example — placeholder, NOT a live tenant)

- **project_id:** `<acme-project-id>` (placeholder — replaced at first-engagement)
- **Source artefact:** DESIGN-16 at `.planning/DESIGN.md:502-543`
- **Note:** This row exists as a structural placeholder. The first real client engagement populates a verified row above this one and demotes Acme to "example" status.

### Up & Up Group (TBD at first Ziflow engagement)

- **project_id:** `<TBD at first engagement>`
- **review_workflow_stages:** `<TBD>`
- **review_decision_labels:** `<TBD>`
- **Source artefact:** `<TBD — populate from first engagement>`
- **Per `.claude/memory/reference_client_brain_coda_docs.md`:** Up & Up Group is one of the two documented Coda-brain clients; first Ziflow engagement may not coincide with first plugin engagement.

### VodafoneZiggo (TBD at first Ziflow engagement)

- **project_id:** `<TBD at first engagement>`
- **review_workflow_stages:** `<TBD>`
- **review_decision_labels:** `<TBD>`
- **Source artefact:** `<TBD — populate from first engagement>`
- **Per `.claude/memory/reference_client_brain_coda_docs.md`:** VodafoneZiggo is the second documented Coda-brain client (Wrike-verified at `app-eu.wrike.com`; Ziflow tenant TBD).

## Pattern slots (variant taxonomy)

### Workflow stage names per client
- Ziflow workflow stages are customisable per tenant (e.g., "In Review", "Approved", "Rejected", "On Hold")
- Per-client mapping lives in `<Client> Brain/00_HUB.md` `ziflow_workflow_stages:` block
- Stage 4a fnspec MUST cite per-tenant stage names explicitly

### Project naming conventions
- Tenant-defined naming patterns (e.g., `<Client> - <Campaign> - <Asset Type>` or `<Brand> / <Year> / <Quarter>`)
- Captured per-client in `<Client> Brain/00_HUB.md` for consistency across Stage 4a / Stage 8 / Stage 10

### Review-decision label customisations
- Default Ziflow labels: "Approve", "Approve with changes", "Send for revisions", "Not relevant"
- Tenants may customise label text and add per-stage labels
- Test bot tier-1 assertions consume the canonical label set from `<Client> Brain/00_HUB.md`

## How to add a new shape

When onboarding a new client, append a verified row to `## Known shapes (verified)`
citing the engagement date + source artefact. Pattern slots are append-only;
existing rows are preserved for audit trail.
```

**FILE C: `dydx-delivery/skills/platform-ziflow/references/vocabulary.md`**

```markdown
# Ziflow — Vocabulary

> For project-wide terms see `dydx-delivery/references/glossary.md`.

## Platform-specific terms

**proof** — A Ziflow review unit — a single asset (or a related set of assets) submitted for stakeholder review. Identified by `proof_id`. Subject to read-after-create eventual consistency (MOD-6 / Q05 — use webhook-PRIMARY framing per `references/api-contract.md`).

**review** — The process of reviewing a proof — comments, annotations, decisions captured against the proof.

**decision** — A reviewer's verdict on a proof at a particular stage (e.g., "Approve", "Approve with changes", "Send for revisions", "Not relevant"). Per-tenant label customisations live in `<Client> Brain/00_HUB.md`.

**stage** — A workflow step in a Ziflow review (distinct from dydx-delivery 11-stage pipeline; "stage" in Ziflow vocab is project-internal). Maps to ReviewAI Checklist application per project.

**version** — A revision of a proof. Each new uploaded version receives an incrementing version number; reviewers can decision against a specific version.

**project_id** — Identifier for a Ziflow project (per-tenant container for proofs).

**ReviewAI** — Ziflow's 2026 AI capability surface. Includes Checklists (Public Preview), Change Verification ("Coming Soon"), Brand Standards ("Coming Soon"), Checklist Templates API (GA April 2026). Capability matrix tracked in `references/native-ai-inventory.md`.

**Checklist** — A pre-authored set of pass/fail criteria applied to a proof during review. ReviewAI Checklists (Public Preview) provide AI-assisted evaluation; Stage 10 paste bundle authors per-client Checklist criteria.

**Change Verification** — A "Coming Soon" ReviewAI capability for verifying that a new proof version applies the requested changes from prior decisions.

**Brand Standards** — A "Coming Soon" ReviewAI capability for verifying that a proof meets per-client brand-standard rules.

> Verification gate: synthesis-plan grep dedup confirms zero of these terms
> are ALSO defined in `dydx-delivery/references/glossary.md`. Cross-cutting
> terms (frontmatter / sandbox / native_ai_path / status: lifecycle) live in
> glossary.md, NOT here.
```

After Write, delete `dydx-delivery/skills/platform-ziflow/references/.gitkeep`.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-ziflow/references/knowledge-ingestion.md && test -f dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md && test -f dydx-delivery/skills/platform-ziflow/references/vocabulary.md && grep -qF 'ReviewAI Checklist criteria' dydx-delivery/skills/platform-ziflow/references/knowledge-ingestion.md && grep -qF 'Acme' dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md && grep -qF 'Up & Up Group' dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md && grep -qF 'VodafoneZiggo' dydx-delivery/skills/platform-ziflow/references/client-shape-gotchas.md && grep -qF 'proof' dydx-delivery/skills/platform-ziflow/references/vocabulary.md && grep -qF 'dydx-delivery/references/glossary.md' dydx-delivery/skills/platform-ziflow/references/vocabulary.md && [ ! -f dydx-delivery/skills/platform-ziflow/references/.gitkeep ] && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section ziflow 2>&1 | grep -qE '^PASS: A4'</automated>
  </verify>
  <done>All 3 files exist; UAT-6.1 paste-only contract in knowledge-ingestion.md; D-65 Acme + Up & Up Group + VodafoneZiggo seeds in client-shape-gotchas.md; D-66 vocabulary opens with glossary pointer + Ziflow-specific terms only; .gitkeep removed; A4-ziflow PASS.</done>
  <acceptance_criteria>
    - Satisfies A4 (ziflow partition): all 5 of `dydx-delivery/skills/platform-ziflow/references/{api-contract.md, native-ai-inventory.md, knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md}` exist
    - knowledge-ingestion.md (UAT-6.1): `grep -qF 'ReviewAI Checklist criteria' <file> && grep -qF 'UAT-6.1' <file> && grep -qF 'paste-only' <file>` all conditions met
    - client-shape-gotchas.md (D-65 seed): `grep -qF 'Acme' <file> && grep -qF 'Up & Up Group' <file> && grep -qF 'VodafoneZiggo' <file> && grep -qF 'TBD at first engagement' <file> && grep -qF 'Append-only' <file>` all conditions met
    - vocabulary.md (D-66 pointer): `grep -qF 'For project-wide terms see' <file>` exits 0 AND `grep -cE '^\*\*(proof|review|decision|stage|version|project_id|ReviewAI|Checklist|Change Verification|Brand Standards)\*\*' <file>` returns >= 10
    - vocabulary.md D-66 dedup pre-check: `grep -qE '^\*\*(frontmatter|sandbox|native_ai_path)\*\* ' <file>` exits 1 (those terms NOT defined here)
    - Satisfies A7 partial (T-06-02 file-level): `grep -qF 'native_ai_path: api' <file>` exits 1 for each of the 3 files
    - .gitkeep placeholder removed: `[ ! -f dydx-delivery/skills/platform-ziflow/references/.gitkeep ]` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all 4 tasks land:

1. `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section ziflow` exits 0 (Ziflow-specific assertions A3, A4-ziflow, A5-ziflow, A6-ziflow, A13, A17-ziflow PASS).
2. Full suite still FAILS at A16 (OPEN-QUESTIONS row flips not yet applied — that lives in 06-04 synthesis plan; expected mid-phase).
3. `grep -r 'native_ai_path: api' dydx-delivery/skills/platform-ziflow/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'` returns zero hits (T-06-02 mitigation).
4. Webhook-PRIMARY framing verified to appear BEFORE the wait_for_proof H2 in api-contract.md (ordering check; vendor-recommended pattern surfaces first).
5. All 6 Ziflow tree files (1 SKILL.md + 5 references/) committed atomically per task; both `.gitkeep` placeholders deleted.
</verification>

<success_criteria>
Plan 06-03 is complete when:
- [ ] `platform-ziflow/SKILL.md` exists with canonical frontmatter (`platform: ziflow`, `tier_claims_last_verified: 2026-05-09`, `native_ai_path: paste`, `frontmatter_version: 2`), D-59 hard-rules pointer, D-68 re-verification trigger with high-churn note
- [ ] `platform-ziflow/references/api-contract.md` documents webhook-PRIMARY framing (Q05 resolution) ABOVE the wait_for_proof FALLBACK helper (MOD-6) with max_wait_s=30 / interval_s=2 defaults; "No MCP" parked-ref
- [ ] `platform-ziflow/references/native-ai-inventory.md` documents 5-row ReviewAI capability matrix (Checklists Public Preview + Change Verification + Brand Standards + Checklist Templates API + knowledge-ingestion API CLOSED) with high-churn note
- [ ] `platform-ziflow/references/knowledge-ingestion.md` documents UAT-6.1 paste-only contract (ReviewAI Checklist criteria)
- [ ] `platform-ziflow/references/client-shape-gotchas.md` carries Acme placeholder + TBD stubs for Up & Up Group + VodafoneZiggo (D-65) + 3 pattern slots
- [ ] `platform-ziflow/references/vocabulary.md` opens with glossary pointer (D-66) and lists 10 Ziflow-specific terms only
- [ ] Both `.gitkeep` placeholders for platform-ziflow/ + platform-ziflow/references/ deleted
- [ ] Phase-6 structure-check assertions PASSING after plan completion: A3, A4-ziflow, A5-ziflow, A6-ziflow, A7-partial-ziflow, A13, A17-ziflow
- [ ] Zero hits for `native_ai_path: api` in platform-ziflow/ tree (T-06-02 mitigation)
- [ ] All deliverables committed; each task = 1 atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-internalise-platform-skills/06-03-SUMMARY.md` capturing:
- Ziflow SKILL.md + 5 references/ files landed end-to-end with all per-platform assertions PASSING
- Q05 read-after-create consistency window resolved inline — webhook-PRIMARY framing per vendor guidance + 30s/2s wait_for_proof fallback defaults landed in api-contract.md; OPEN-QUESTIONS.md row flip deferred to 06-04 synthesis plan
- Any execution-time re-verification of Ziflow ReviewAI capability matrix (manual; documents whether Change Verification + Brand Standards have shipped post-Coming-Soon, and whether tier_claims_last_verified: was bumped forward from 2026-05-09 baseline)
- D-59 + D-68 pattern carry-forward confirmed verbatim from 06-01 + 06-02 siblings
- All 3 platform skills now landed (Pipefy + Wrike + Ziflow); 06-04 synthesis can now flip Q05/Q06.2/Q07.2 rows + run vocabulary dedup gate + confirm structure-check exits 0
</output>
