---
phase: 06-internalise-platform-skills
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - dydx-delivery/skills/platform-wrike/SKILL.md
  - dydx-delivery/skills/platform-wrike/references/api-contract.md
  - dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md
  - dydx-delivery/skills/platform-wrike/references/knowledge-ingestion.md
  - dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md
  - dydx-delivery/skills/platform-wrike/references/vocabulary.md
  - dydx-delivery/skills/platform-wrike/.gitkeep
  - dydx-delivery/skills/platform-wrike/references/.gitkeep
autonomous: true
requirements: [PLAT-02, PLAT-04, PLAT-05, PLAT-06]
tags: [platform-skills, wrike, oauth-host, q07-2, copilot, mcp]

must_haves:
  truths:
    - "Per D-63, plan 06-02 ships the full platform-wrike/ skill tree end-to-end (SKILL.md + 5 references/) in a single atomic unit; reviewers grade Wrike as a whole."
    - "Per DESIGN-15 / MOD-5, api-contract.md documents the OAuth-host persistence 3-step pattern: extract host from OAuth token response → persist to client_state.yaml wrike.host: → use as base URL for every API call."
    - "Per MOD-5, api-contract.md carries the literal prohibition phrase against hardcoding www.wrike.com — Threat T-06-03 mitigation; A12 grep gate enforces."
    - "Per UAT-6.1 and PLAT-05, SKILL.md frontmatter carries native_ai_path: paste; the literal token 'native_ai_path: api' appears nowhere in the platform-wrike/ tree (Threat T-06-02 mitigation)."
    - "Per MOD-7 / D-68, SKILL.md frontmatter carries tier_claims_last_verified: 2026-05-09 baseline (per-platform distinct from Pipefy + Ziflow)."
    - "Per D-67, OPEN-Q07.2 (Wrike throttle calibration) resolves inline as 320 req/min per user (80% of 400 ceiling) landing in api-contract.md § Rate limit + throttle; OPEN-QUESTIONS.md row flip lives in synthesis plan 06-04."
    - "Per DESIGN-15, api-contract.md documents 16 Copilot MCP tools baseline; PLAT-02 execution MAY re-verify against current vendor count (47 per stackone.com 2026-05-11) and record in native-ai-inventory.md as a manual-only verification per VALIDATION § Manual-Only Verifications row 2."
    - "Per D-65, client-shape-gotchas.md seeded with VodafoneZiggo EU tenant (app-eu.wrike.com, account 5996999) — verbatim DESIGN-15 lift."
  artifacts:
    - path: "dydx-delivery/skills/platform-wrike/SKILL.md"
      provides: "Wrike platform reference skill (PLAT-02) with D-59 hard-rules pointer + D-68 re-verification trigger + locked frontmatter (PLAT-04, PLAT-05)"
      contains: "tier_claims_last_verified"
    - path: "dydx-delivery/skills/platform-wrike/references/api-contract.md"
      provides: "Wrike REST API contract — OAuth-host persistence pattern (MOD-5), 320 req/min/user throttle (Q07.2 resolution), 16-tool Copilot MCP baseline"
      contains: "OAuth token response"
    - path: "dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md"
      provides: "Wrike Copilot + 16 MCP tools capability matrix per DESIGN-15"
      contains: "Wrike Copilot"
    - path: "dydx-delivery/skills/platform-wrike/references/knowledge-ingestion.md"
      provides: "Wrike paste-only ingestion contract (UAT-6.1) — Copilot workflow narrative + MCP tool config + attach-doc-via-MCP path"
      contains: "Copilot workflow narrative"
    - path: "dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md"
      provides: "Wrike per-client shape variant taxonomy seeded with VodafoneZiggo EU tenant (D-65)"
      contains: "VodafoneZiggo"
    - path: "dydx-delivery/skills/platform-wrike/references/vocabulary.md"
      provides: "Wrike-specific terms only (D-66); pointer to dydx-delivery/references/glossary.md"
      contains: "space"
  key_links:
    - from: "dydx-delivery/skills/platform-wrike/SKILL.md"
      to: "dydx-delivery/references/safety-rules.md"
      via: "D-59 uniform hard-rules pointer (verbatim block)"
      pattern: "dydx-delivery/references/safety-rules\\.md"
    - from: "dydx-delivery/skills/platform-wrike/references/api-contract.md"
      to: "dydx-delivery/references/connector-matrix.md:73"
      via: "Q07.2 throttle derivation cites Phase 5 Q07.1 baseline (320 req/min = 80% of 400 ceiling)"
      pattern: "connector-matrix\\.md"
    - from: "dydx-delivery/skills/platform-wrike/references/api-contract.md"
      to: "<Client> Brain/00_HUB.md Coda block (per DESIGN-29 wrike.host: persistence destination)"
      via: "OAuth-host persistence — host field extracted from token response → client_state.yaml wrike.host: → base URL"
      pattern: "wrike\\.host"
    - from: "dydx-delivery/skills/platform-wrike/references/vocabulary.md"
      to: "dydx-delivery/references/glossary.md"
      via: "D-66 pointer to project-wide glossary"
      pattern: "glossary\\.md"
---

<objective>
Wave 2 of Phase 6: ship the complete `skills/platform-wrike/` tree end-to-end (SKILL.md + all 5 references/ files) in one atomic plan per D-63 per-platform slicing. Distinct directory ownership from 06-01 (Pipefy) and 06-03 (Ziflow) — `/gsd-execute-phase 6` can run 06-02 in parallel with 06-03 once 06-01's Wave 0 scaffold lands.

Purpose: Wrike is the OAuth-host-persistence platform (DESIGN-15 / MOD-5 — `host` field from OAuth token response is base-URL; hardcoding `www.wrike.com` breaks multi-tenant). Landing the persistence pattern as a documented contract here prevents Stage 7b implementation prompt + Stage 8 test bot from re-deriving the rule per-consumer.

Output: 1 SKILL.md + 5 references/ files for Wrike, all committed atomically per task. Replaces 06-01's Wrike .gitkeep placeholders with real files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-internalise-platform-skills/06-CONTEXT.md
@.planning/phases/06-internalise-platform-skills/06-RESEARCH.md
@.planning/phases/06-internalise-platform-skills/06-PATTERNS.md
@.planning/phases/06-internalise-platform-skills/06-VALIDATION.md
@.planning/phases/06-internalise-platform-skills/06-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/DESIGN.md
@.planning/OPEN-QUESTIONS.md
@dydx-delivery/references/safety-rules.md
@dydx-delivery/references/glossary.md
@dydx-delivery/references/connector-matrix.md
@dydx-delivery/skills/execute-tests/SKILL.md
@dydx-delivery/skills/platform-pipefy/SKILL.md
@dydx-delivery/skills/platform-pipefy/references/api-contract.md

<interfaces>
<!-- All concrete values reproduced here. Executor does not need to re-derive from research. -->

<!-- Wrike SKILL.md frontmatter (verbatim from RESEARCH.md:691-700): -->
```yaml
---
name: platform-wrike
description: Provide Wrike REST API contract + 2026 Copilot + MCP Server capability matrix + per-client shape gotchas + vocabulary for any stage skill that targets a Wrike tenant. Use when artefact frontmatter carries `platform: wrike`. Documents the OAuth-host persistence pattern (the `host` field MUST be persisted from the OAuth token response; hardcoding `www.wrike.com` breaks multi-tenant); the attach-doc-via-MCP knowledge-ingestion path; paste-only native-AI ingestion per UAT-6.1. Does NOT execute API calls — pure reference content for skills + Stage 8 test bot.
frontmatter_version: 2
platform: wrike
tier_claims_last_verified: 2026-05-09
native_ai_path: paste
---
```

<!-- D-59 hard-rules pointer (COPY VERBATIM — same as 06-01 Task 2): -->
```markdown
## Hard rules

> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

<!-- Wrike OAuth-host persistence pattern — verbatim from RESEARCH.md:353-397 (3-step pseudocode pattern, NOT a single helper): -->

**Signature:** This is a **pattern**, not a single-callable helper. The pattern has three parts: (1) extract `host` from OAuth token response; (2) persist to `client_state.yaml` `wrike.host:`; (3) use as base URL for every subsequent API call.

**Behaviour:** Replaces the hardcoded `www.wrike.com` base URL with the per-tenant host value carried in the OAuth token response. Wrike's OAuth handshake returns a tenant-specific `host` field (e.g., `app-us2.wrike.com` for US-2 region, `app-eu.wrike.com` for EU region). Hardcoding `www.wrike.com` breaks multi-tenant per MOD-5.

**Persistence:** `client_state.yaml` `wrike.host:` field (per DESIGN-29 schema). Read at session start; written on initial OAuth + on host-change events (rare; typically only on regional migration).

**Retry / poll budget:** N/A (pattern, not a network call). Per-call throttle ceiling: **320 req/min per user (80% of 400 ceiling; resolves Q07.2 per Phase 5 connector-matrix.md `:73`)**.

**Failure modes:**
- OAuth token response missing `host` field → raise `WrikeContractDrift("OAuth response missing host field — multi-tenant guarantee broken")`.
- `wrike.host:` absent from `client_state.yaml` at runtime → refuse to operate; surface "Wrike host not provisioned — re-run OAuth handshake" to caller.
- Hardcoded `www.wrike.com` detected in any helper module → CI-level lint failure (Stage 8 test-bot tier-1 assertion).

**Pseudocode** (verbatim from RESEARCH.md:369-391):
```python
# Step 1 — Extract on OAuth completion
def on_oauth_complete(token_response: dict, client_state_path: Path) -> None:
    host = token_response.get("host")
    if not host:
        raise WrikeContractDrift("OAuth response missing host field")
    cs = load_client_state(client_state_path)
    cs["wrike"]["host"] = host
    save_client_state(client_state_path, cs)

# Step 2 — Read at every API call
def wrike_base_url(client_state: dict) -> str:
    host = client_state.get("wrike", {}).get("host")
    if not host:
        raise RuntimeError("wrike.host not provisioned — re-run OAuth handshake")
    return f"https://{host}/api/v4"

# Step 3 — Use as base URL
def list_tasks(client_state: dict, params: dict) -> dict:
    url = f"{wrike_base_url(client_state)}/tasks"
    return _execute_with_throttle(url, params=params,
                                   throttle_req_per_min=320)  # 80% of 400 ceiling per user
```

**Worked examples:**
- US-2 region tenant: `token_response.host == "app-us2.wrike.com"` → base URL `https://app-us2.wrike.com/api/v4`.
- VodafoneZiggo EU tenant (account `5996999`): `token_response.host == "app-eu.wrike.com"` → base URL `https://app-eu.wrike.com/api/v4`; entry URL pattern `https://app-eu.wrike.com/workspace.htm?acc=5996999`.

<!-- Wrike Native-AI capability matrix (DESIGN-15 baseline; PLAT-02 MAY re-verify per VALIDATION manual-only row 2): -->
| Capability | Available? | Surface | Confidence |
|---|---|---|---|
| Wrike Copilot (real-time AI in workspace) | yes | Wrike workspace UI / Copilot panel | HIGH |
| MCP Server (16-tool baseline) | yes | `developers.wrike.com/wrike-mcp/` | **DESIGN-15 = 16 tools; current secondary count = 47 (stackone.com 2026-05-11)** — PLAT-02 execution MAY re-verify against `developers.wrike.com/wrike-mcp/` + `www.wrike.com/ai/mcp/` and record the authoritative count |
| Attach-doc-via-MCP knowledge-ingestion path | yes | MCP `attach_doc` tool surface | MEDIUM — requires MCP adoption (UAT-3.5 parks MCP through v2.6; paste-only fallback per UAT-6.1) |
| AI Studio knowledge-ingestion API | **CLOSED (UAT-6.1)** | n/a | UAT-6.1 — OPEN-Q02 closed; no API ingestion path through v2.6 |

<!-- D-65 client-shape-gotchas seed (Wrike = VodafoneZiggo EU verbatim from DESIGN-15 at .planning/DESIGN.md:459-500 + RESEARCH.md:1105-1156): -->
```markdown
### VodafoneZiggo (EU region — verified per DESIGN-15, 2026-05-10)

- **host:** `app-eu.wrike.com` (persisted from OAuth token response per DESIGN-15 / MOD-5)
- **account_id:** `5996999`
- **Entry URL pattern:** `https://app-eu.wrike.com/workspace.htm?acc=5996999`
- **Base URL for API calls:** `https://app-eu.wrike.com/api/v4`
- **Source artefact:** DESIGN-15 at `.planning/DESIGN.md:459-500`
- **Persistence:** `<Client> Brain/00_HUB.md` Coda block carries the host (per DESIGN-29 schema)
```

<!-- Per-platform doc URL anchors (D-68): -->
- Wrike: `https://developers.wrike.com/wrike-mcp/` + `https://www.wrike.com/ai/mcp/`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer ↔ canonical references | Phase 6 doc-only; threats are stale claims and banned-token leakage (hardcoded host string) |
| platform-wrike/ tree ↔ downstream consumer skills | Stage 4a/7b/8/10 read SKILL.md frontmatter + references/* at session start; hardcoded `www.wrike.com` propagates silently to every consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `tier_claims_last_verified:` ISO date | mitigate | A5 assertion enforces ISO-date format; manual MCP-count re-verify documented per VALIDATION § Manual-Only Verifications row 2 |
| T-06-02 | Information disclosure (banned value leak) | `native_ai_path: api` reappearing via copy-paste | mitigate | A7 grep gate exits 1 on any hit across platform-wrike/ |
| T-06-03 | Tampering | Hardcoded `www.wrike.com` slipping into examples | mitigate | A12 second-half assertion checks for the literal prohibition phrase; api-contract.md carries explicit "NEVER hardcode `www.wrike.com`" callout |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: platform-wrike/SKILL.md — frontmatter + intro + hard-rules pointer (D-59) + re-verification trigger (D-68) + what-this-provides</name>
  <files>dydx-delivery/skills/platform-wrike/SKILL.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/SKILL.md (Wave 1 sibling — exact shape to mirror; built in 06-01 Task 2)
    - dydx-delivery/skills/execute-tests/SKILL.md:1-22 (analog SKILL.md frontmatter + intro + D-59 pointer pattern at lines 20-22)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:691-700 (Wrike frontmatter verbatim template)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:717-735 (D-68 re-verification trigger boilerplate)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:430-462 (D-68 re-verification trigger Wrike-substituted)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md spec_lock DESIGN-15 + UAT-6.1
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-wrike/SKILL.md`. Delete the `.gitkeep` placeholder at `dydx-delivery/skills/platform-wrike/.gitkeep` AFTER the SKILL.md exists (placeholder is no longer needed once real content lands). Use the platform-pipefy/SKILL.md (built in 06-01 Task 2) as a sibling-mirror reference for structure; substitute Wrike-specific content.

**1. YAML frontmatter (verbatim from interfaces block):**
```yaml
---
name: platform-wrike
description: Provide Wrike REST API contract + 2026 Copilot + MCP Server capability matrix + per-client shape gotchas + vocabulary for any stage skill that targets a Wrike tenant. Use when artefact frontmatter carries `platform: wrike`. Documents the OAuth-host persistence pattern (the `host` field MUST be persisted from the OAuth token response; hardcoding `www.wrike.com` breaks multi-tenant); the attach-doc-via-MCP knowledge-ingestion path; paste-only native-AI ingestion per UAT-6.1. Does NOT execute API calls — pure reference content for skills + Stage 8 test bot.
frontmatter_version: 2
platform: wrike
tier_claims_last_verified: 2026-05-09
native_ai_path: paste
---
```

**2. Title + intro:**
```markdown
# platform-wrike

Wrike platform reference for the dydx-delivery plugin. Any stage skill operating
against a Wrike tenant loads this skill alongside the stage-specific skill;
artefact frontmatter `platform: wrike` is the routing key.
```

**3. `## Inputs` + `## Output`:**
```markdown
## Inputs

- Artefact frontmatter carrying `platform: wrike` and a populated `sandbox:` block (loaded by the calling stage skill)
- `client_state.yaml` `wrike.host:` field (persisted from OAuth token response — see `references/api-contract.md` § OAuth-host persistence)
- Phase 5 canonical references at `dydx-delivery/references/`

## Output

This skill produces NO artefacts. It is a reference skill — the calling stage skill produces the versioned output.
```

**4. `## Hard rules` (D-59 — VERBATIM):**
```markdown
## Hard rules

> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

**5. `## What this skill provides`** (mirror Pipefy SKILL.md shape, swap content):
```markdown
## What this skill provides

- `references/api-contract.md` — REST endpoint (per-tenant base URL from OAuth `host` field per MOD-5), auth header shape, rate limit (400 req/min/user ceiling; 320 req/min/user throttle per Q07.2), OAuth-host persistence 3-step pattern, hardcoded `www.wrike.com` prohibition.
- `references/native-ai-inventory.md` — 2026 Wrike Copilot + 16-tool MCP baseline capability matrix per DESIGN-15.
- `references/knowledge-ingestion.md` — Paste-only Stage 10 path (UAT-6.1): Copilot workflow narrative + MCP tool config + attach-doc-via-MCP instructions.
- `references/client-shape-gotchas.md` — Per-client tenant variations (VodafoneZiggo EU verified seed; append-only as new clients onboard).
- `references/vocabulary.md` — Wrike-specific terms (space / folder / project / task / custom field / host / account_id / space_id / Wrike Copilot / MCP Server).
```

**6. `## Re-verification trigger` (D-68 — adapt the boilerplate; Wrike doc URLs):**
```markdown
## Re-verification trigger

Re-verify `references/native-ai-inventory.md` against current Wrike Copilot
documentation BEFORE any v2.x phase that consumes the capability matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — Copilot workflow narratives + MCP tool config)

Update `tier_claims_last_verified:` on this SKILL.md frontmatter after each
re-verification, citing the source doc URL + date in commit message.

Doc URL anchors:
- Wrike: https://developers.wrike.com/wrike-mcp/ + https://www.wrike.com/ai/mcp/
```

Delete `.gitkeep` placeholder: `rm dydx-delivery/skills/platform-wrike/.gitkeep` (placeholder no longer needed).
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-wrike/SKILL.md && grep -qE '^tier_claims_last_verified: 2026-05-09$' dydx-delivery/skills/platform-wrike/SKILL.md && grep -qE '^native_ai_path: paste$' dydx-delivery/skills/platform-wrike/SKILL.md && grep -qE '^platform: wrike$' dydx-delivery/skills/platform-wrike/SKILL.md && grep -qF 'dydx-delivery/references/safety-rules.md' dydx-delivery/skills/platform-wrike/SKILL.md && grep -qE '^## Re-verification trigger$' dydx-delivery/skills/platform-wrike/SKILL.md && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A2'</automated>
  </verify>
  <done>SKILL.md exists with valid YAML frontmatter (platform: wrike, tier_claims_last_verified: 2026-05-09, native_ai_path: paste, frontmatter_version: 2); D-59 hard-rules pointer present verbatim; D-68 re-verification trigger present with Wrike doc URLs; A2, A5-wrike, A6-wrike PASS.</done>
  <acceptance_criteria>
    - Satisfies A2: `[ -f dydx-delivery/skills/platform-wrike/SKILL.md ]` exits 0
    - Satisfies A5 (Wrike partition): `grep -qE '^tier_claims_last_verified: [0-9]{4}-[0-9]{2}-[0-9]{2}$' dydx-delivery/skills/platform-wrike/SKILL.md` exits 0
    - Satisfies A6 (Wrike partition): `grep -qE '^native_ai_path: (paste|none)$' dydx-delivery/skills/platform-wrike/SKILL.md` exits 0
    - Satisfies A7 partial (T-06-02): `grep -q 'native_ai_path: api' dydx-delivery/skills/platform-wrike/SKILL.md` exits 1 (zero hits)
    - `platform: wrike` exact-token frontmatter: `grep -qE '^platform: wrike$' <file>` exits 0
    - D-59 pointer verbatim: `grep -qF 'See \`dydx-delivery/references/safety-rules.md\` for the canonical ruleset.' dydx-delivery/skills/platform-wrike/SKILL.md` exits 0
    - D-68 trigger H2 + Wrike doc URL: `grep -qE '^## Re-verification trigger$' <file>` AND `grep -qF 'developers.wrike.com/wrike-mcp/' <file>` both exit 0
    - `.gitkeep` placeholder removed: `[ ! -f dydx-delivery/skills/platform-wrike/.gitkeep ]` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: platform-wrike/references/api-contract.md — OAuth-host 3-step persistence (MOD-5) + www.wrike.com prohibition + Q07.2 throttle + MCP availability parked-ref</name>
  <files>dydx-delivery/skills/platform-wrike/references/api-contract.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/references/api-contract.md (06-01 sibling — exact shape to mirror for top preamble + auth-shape note + section ordering)
    - dydx-delivery/references/connector-matrix.md:1-31 (analog scope-lock callout + auth-shape note pattern)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:353-397 (OAuth-host 3-step pseudocode verbatim)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:454-494 (api-contract.md outline)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:658-672 (Q07.2 throttle resolution)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:204-217 (Wrike-specific helper-contract + parked-ref + rate-limit)
    - .planning/DESIGN.md:459-500 (DESIGN-15 — OAuth-host rule + VodafoneZiggo EU worked example)
    - dydx-delivery/references/connector-matrix.md:73 (Q07.1 Wrike rate-limit baseline)
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-wrike/references/api-contract.md` mirroring the platform-pipefy/references/api-contract.md shape exactly:

**1. Title + scope-lock preamble:**
```markdown
# Wrike — API contract

> Canonical SoT for Wrike API contracts per DESIGN-15 (`.planning/DESIGN.md:459-500`). Documents the per-tenant base URL from OAuth `host` field, auth shape, rate-limit + throttle, OAuth-host persistence 3-step pattern, multi-tenant auth-concurrency, and MCP availability (parked reference).
>
> **Scope locks (UAT 2026-05-10):**
> - **MOD-5 / DESIGN-15 (CRITICAL):** Base URL is parsed PER-TENANT from the OAuth token response `host` field. Hardcoding `www.wrike.com` breaks multi-tenant — NEVER hardcode `www.wrike.com`. Tenants like VodafoneZiggo run on `app-eu.wrike.com`; US-2 tenants on `app-us2.wrike.com`.
> - **UAT-3.5:** Wrike MCP Server is parked reference only — not adopted through v2.6.
> - **UAT-6.1:** Native-AI ingestion APIs OUT-OF-SCOPE — `native_ai_path: paste | none` only. See `references/knowledge-ingestion.md`.
> - **Q25 (Phase 5 resolution):** Auth-concurrency class default = `exclusive`; live tenant test deferred to PLAT-02 kickoff verification.
>
> **Cross-refs:**
> - Sandbox enforcement: `dydx-delivery/references/safety-rules.md` § "1. Sandbox enforcement"
> - Connector probe: `dydx-delivery/references/connector-matrix.md` (Phase 5 canonical; Q07.1 baseline at `:73`)
> - Glossary: `dydx-delivery/skills/platform-wrike/references/vocabulary.md` (Wrike-specific terms)
```

**2. Auth-shape secret-handling note** (lift verbatim from platform-pipefy/references/api-contract.md):
```markdown
> **Auth header & secret-handling note:** All helper contracts below document the auth header NAME + value SHAPE only (e.g., `Authorization: Bearer <token>`). NEVER paste real tokens, API keys, OAuth refresh tokens, or session cookies into this file. Tokens live in per-tenant `client_state.yaml` (gitignored) or env vars — never in the canonical reference set.
```

**3. `## Endpoint(s) + auth shape`:**
```markdown
## Endpoint(s) + auth shape

- **Base URL (per-tenant from OAuth `host` field per MOD-5 / DESIGN-15):** `https://<host>/api/v4` where `<host>` is the value returned by the Wrike OAuth token-exchange response (e.g., `app-us2.wrike.com` for US-2 region, `app-eu.wrike.com` for EU region).
- **Auth header:** `Authorization: Bearer <token>` (header NAME only — see secret-handling note above).
- **CRITICAL — DO NOT hardcode the host:** The `host` field MUST be persisted from the OAuth token response. NEVER hardcode `www.wrike.com` — that string is the marketing site, NOT a valid API base URL for any production tenant. See § OAuth-host persistence below for the 3-step pattern.
- Sandbox tenant only — see `dydx-delivery/references/safety-rules.md` Rule 1.
```

**4. `## Rate limit + throttle` (resolves Q07.2 inline):**
```markdown
## Rate limit + throttle

- **Published ceiling:** 400 req/min per user + 5000 req/min per IP (Wrike Help Center, re-confirmed 2026-05-11; Phase 5 Q07.1 baseline at `dydx-delivery/references/connector-matrix.md:73`).
- **Helper throttle (Q07.2 resolution, PLAT-06):** **320 req/min per user (80% of 400 ceiling, DESIGN-22 carried throttle pattern at `.planning/DESIGN.md:795`)**.
- **IP-level ceiling (5000/min):** Not a per-helper constraint; multiple users may share the IP. IP-level rate-limit-aware behaviour is a Stage 8 test-bot concern.
- **429 handling:** Max 3 retries per call with exponential backoff `[1s, 2s, 4s]`.
- **`X-RateLimit-*` headers:** Wrike returns these on every response; helpers SHOULD parse them to dynamically adjust throttle when the API surfaces server-side remaining-quota. Per-call adaptive-throttle is a P2 enhancement; static 320 req/min/user is the P1 baseline.
```

**5. `## OAuth-host persistence pattern (3-step) — MOD-5`** — embed verbatim from interfaces block in this PLAN: signature explanation, behaviour, persistence note, retry/poll budget, failure modes, the 3-step Python pseudocode (Step 1 / Step 2 / Step 3), and the 2 worked examples (US-2 + VodafoneZiggo EU). Use H2 `## OAuth-host persistence pattern (3-step) — MOD-5`. Add explicit prohibition phrase verbatim in the section body:

```markdown
**CRITICAL anti-pattern (MOD-5 — A12 verification gate):** NEVER hardcode `www.wrike.com` anywhere in helper modules, configuration, or example code. The OAuth token response carries the customer's regional host as part of its base-URL contract; hardcoding breaks multi-tenant.
```

Add a single-line source citation at the end: `**Source:** DESIGN-15 OAuth-host rule at \`.planning/DESIGN.md:488\` + Phase 5 OPEN-Q13 resolution at \`dydx-delivery/references/connector-matrix.md:78\` + Phase 5 connector-matrix.md \`:73\` throttle ceiling.`

**6. `## Multi-tenant auth concurrency`:**
```markdown
## Multi-tenant auth concurrency

- **Class (default):** `exclusive` per Phase 5 Q25 resolution (Pipefy precedent under UAT-4.2). LOW confidence — live tenant test deferred to PLAT-02 kickoff verification per Q25 resolution.
- **`client_state.yaml` field:** `wrike.auth_concurrency_class: exclusive` (per DESIGN-29; populated by Phase 6 PLAT-02 probe).
```

**7. `## MCP availability (UAT-3.5 — PARKED REFERENCE)`:**
```markdown
## MCP availability (UAT-3.5 — PARKED REFERENCE)

Wrike MCP Server is available (`developers.wrike.com/wrike-mcp/` + `www.wrike.com/ai/mcp/`) but **not adopted through v2.6** per UAT-3.5. REST API is the canonical Wrike access path for v2.1–v2.6.

- **DESIGN-15 baseline:** 16 MCP tools documented.
- **Current vendor count (manual re-verify; per VALIDATION § Manual-Only Verifications row 2):** 47 tools per stackone.com (2026-05-11) — DELTA from DESIGN-15 baseline. PLAT-02 execution MAY record the authoritative count in `references/native-ai-inventory.md` after manual check against `developers.wrike.com/wrike-mcp/`.
- MCP adoption re-evaluated as a post-v2.6 milestone gated on first-real-client-engagement-practice-run (per `.claude/memory/feedback_platform_skills_api_first.md`).
```
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF 'OAuth token response' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF 'NEVER hardcode `www.wrike.com`' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF '320 req/min' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF '400' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF 'app-eu.wrike.com' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF '5996999' dydx-delivery/skills/platform-wrike/references/api-contract.md && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A12' && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A15'</automated>
  </verify>
  <done>api-contract.md exists with OAuth-host 3-step persistence pattern (MOD-5); literal `NEVER hardcode \`www.wrike.com\`` prohibition phrase; Q07.2 throttle resolution inline (320 req/min/user); A12 + A15 assertions PASS.</done>
  <acceptance_criteria>
    - Satisfies A12 (T-06-03 mitigation): `grep -qF 'OAuth token response' <file> && grep -qF 'NEVER hardcode `www.wrike.com`' <file>` both exit 0
    - Satisfies A15 (Q07.2 / PLAT-06): `grep -qF '320 req/min' <file> && grep -qF '400' <file>` both exit 0
    - 3-step pseudocode landmarks present: `grep -qF 'on_oauth_complete' <file> && grep -qF 'wrike_base_url' <file>` both exit 0
    - VodafoneZiggo worked example present: `grep -qF 'app-eu.wrike.com' <file> && grep -qF '5996999' <file>` both exit 0
    - Auth-shape secret-handling note present: `grep -qF 'NEVER paste real tokens' <file>` exits 0
    - MCP availability parked-ref present: `grep -qF 'UAT-3.5' <file> && grep -qF 'not adopted through v2.6' <file>` both exit 0
    - Satisfies A7 partial (T-06-02 file-level): `grep -qF 'native_ai_path: api' <file>` exits 1 (zero hits)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: platform-wrike/references/native-ai-inventory.md — 2026 Wrike Copilot + 16-tool MCP capability matrix (DESIGN-15)</name>
  <files>dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md (06-01 sibling — exact shape to mirror)
    - .planning/DESIGN.md:459-500 (DESIGN-15 Wrike Copilot + 16 MCP tools baseline)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:499-526 (outline)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:615 (cross-platform parity row — 47-tool current count, DESIGN-15 baseline=16)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:222-256 (table shape + section outline)
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md` mirroring the platform-pipefy native-ai-inventory.md shape exactly.

**1. Title + frontmatter-sync note:**
```markdown
# Wrike — Native-AI inventory

> `tier_claims_last_verified:` matches SKILL.md frontmatter value (2026-05-09 baseline per D-68; updated to execution date if fresh re-verification happens during this plan).
```

**2. `## Re-verification trigger`:**
```markdown
## Re-verification trigger

Re-verify this capability matrix against current Wrike Copilot + MCP Server
documentation (`https://developers.wrike.com/wrike-mcp/` + `https://www.wrike.com/ai/mcp/`)
BEFORE any v2.x phase that consumes the matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — Copilot workflow narratives + MCP tool config)

Update `tier_claims_last_verified:` on `dydx-delivery/skills/platform-wrike/SKILL.md`
frontmatter after each re-verification, citing the source doc URL + date in commit
message.
```

**3. `## Capability matrix (2026-grounded)` (verbatim from interfaces block, 4-row matrix):**
```markdown
## Capability matrix (2026-grounded)

| Capability | Available? | Surface | Confidence |
|---|---|---|---|
| Wrike Copilot (real-time AI in workspace) | yes | Wrike workspace UI / Copilot panel | HIGH |
| MCP Server (16-tool baseline) | yes | `developers.wrike.com/wrike-mcp/` | **DESIGN-15 = 16 tools; current secondary count = 47 (stackone.com 2026-05-11)** — PLAT-02 execution MAY re-verify against `developers.wrike.com/wrike-mcp/` + `www.wrike.com/ai/mcp/` and record the authoritative count |
| Attach-doc-via-MCP knowledge-ingestion path | yes | MCP `attach_doc` tool surface | MEDIUM — requires MCP adoption (UAT-3.5 parks MCP through v2.6; paste-only fallback per UAT-6.1) |
| AI Studio knowledge-ingestion API | **CLOSED (UAT-6.1)** | n/a | UAT-6.1 — OPEN-Q02 closed; no API ingestion path through v2.6 |
```

**4. `## Paste-only delivery (UAT-6.1)`:**
```markdown
## Paste-only delivery (UAT-6.1)

`native_ai_path: paste | none` ONLY — `api` branch is FORBIDDEN. The Stage 10 paste bundle is the only supported ingestion path through v2.6; humans upload manually via the Wrike UI. See `references/knowledge-ingestion.md` for the paste-bundle shape.
```

**Optional execution-time re-verification (manual per VALIDATION § Manual-Only Verifications row 2):** If PLAT-02 executor wishes to re-verify the MCP tool count against current vendor docs (`developers.wrike.com/wrike-mcp/`) before committing, they MAY update the MCP row AND `dydx-delivery/skills/platform-wrike/SKILL.md` `tier_claims_last_verified:` to the execution date. Default: leave 2026-05-09 baseline as-is and surface the 16-vs-47 delta as a flag for downstream PLAT-02 review.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md && grep -qE '^## Capability matrix' dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md && ! grep -qF 'native_ai_path: api' dydx-delivery/skills/platform-wrike/references/native-ai-inventory.md</automated>
  </verify>
  <done>native-ai-inventory.md exists with 4-row 2026 Wrike Copilot + MCP Server capability matrix; re-verification trigger present; paste-only delivery section present; A17 PASS for wrike.</done>
  <acceptance_criteria>
    - Satisfies A17 (wrike partition): grep -qE '^## Capability matrix' on file exits 0 AND grep -cE '^\| (Wrike Copilot|MCP Server|Attach-doc-via-MCP|AI Studio)' returns at least 4
    - 2026 vendor-grounded: grep -qF 'Wrike Copilot' AND grep -qF 'MCP Server' both exit 0
    - Satisfies A7 partial (T-06-02): grep -qF 'native_ai_path: api' exits 1 (zero hits)
    - UAT-6.1 paste-only section present: grep -qF 'UAT-6.1' exits 0
    - Re-verification trigger H2 present: grep -qE '^## Re-verification trigger$' exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: platform-wrike/references/{knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md} — paste-only path + VodafoneZiggo seed + Wrike-specific terms</name>
  <files>dydx-delivery/skills/platform-wrike/references/knowledge-ingestion.md, dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md, dydx-delivery/skills/platform-wrike/references/vocabulary.md</files>
  <read_first>
    - dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md (06-01 sibling — exact shape to mirror)
    - dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md (06-01 sibling — exact shape to mirror)
    - dydx-delivery/skills/platform-pipefy/references/vocabulary.md (06-01 sibling — exact shape to mirror)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:528-606 (per-file outlines)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:259-354 (Wrike-specific seed content)
    - .planning/DESIGN.md:459-500 (DESIGN-15 — VodafoneZiggo EU verified seed)
    - dydx-delivery/references/glossary.md (D-66 dedup gate target)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md D-65 + D-66
  </read_first>
  <action>
Create three files together (single atomic task per D-63 — 5-file shape parity commitment). After Write, delete the `.gitkeep` placeholder at `dydx-delivery/skills/platform-wrike/references/.gitkeep` (`rm` it once all 5 references files exist).

**FILE A: `dydx-delivery/skills/platform-wrike/references/knowledge-ingestion.md`**

```markdown
# Wrike — Knowledge ingestion

## Primary path (paste-only per UAT-6.1)

`native_ai_path: paste`. Stage 10 (`push-native-ai-knowledge` skill, v2.5 / Phase 7) emits a paste bundle containing:

1. **Wrike Copilot workflow narrative** — a structured prose block describing the workflow steps Copilot should support, authored from approved Stage 9 documentation fragments (see DESIGN-26 paste-bundle contract).
2. **MCP tool config** — the MCP tool-list configuration (DESIGN-15 baseline = 16 tools; current vendor count = 47 per `developers.wrike.com/wrike-mcp/`) the human will manually configure in the Wrike workspace.
3. **Attach-doc-via-MCP instructions** — paste-only instructions for the human to attach approved Stage 9 documents to the Wrike workspace via the MCP `attach_doc` tool surface (manual UI workflow).
4. **Per-client target ID** — Wrike AI target reference from `<Client> Brain/00_HUB.md` `Wrike AI:` block (DESIGN-26 / MIN-4 fix).

The paste bundle is human-upload-only via the Wrike UI. No API ingestion path is exercised through v2.6 (UAT-6.1; OPEN-Q02 closed).

## Audit log shape

Stage 10 records each ingestion in `<Client> Brain/changelog/cr-<id>/native-ai-audit.md` with these fields:

- `ingested_at:` ISO-8601 timestamp
- `doc_version:` semver from the approved Stage 9 doc
- `target_id:` Wrike AI target reference (matches `00_HUB.md` `Wrike AI:` block)
- `paste_bundle_path:` local path to the bundle artefact emitted by Stage 10
- `human_upload_acknowledged_at:` ISO-8601 timestamp recorded after the human confirms the upload in Wrike UI

Refuses ingest if `doc_published_at < last_diff_review_at` (CRIT-8 fix per DESIGN-26) or `target_id` mismatches the artefact's `client:` frontmatter (MIN-4 fix).

## Fallback (manual paste via UI)

Paste-only IS the fallback. UAT-6.1 forecloses an API ingestion path through v2.6; humans manually attach documents via the Wrike workspace UI (or the MCP `attach_doc` tool surface, treated as a manual workflow for UAT-3.5 / paste-only purposes). The Stage 10 paste-bundle artefact + audit-log entry are the canonical record that ingestion happened.
```

**FILE B: `dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md`**

```markdown
# Wrike — Client shape gotchas

> Append-only — new client shapes added per first-engagement; existing rows
> preserved for audit trail.

## Known shapes (verified)

### VodafoneZiggo (EU region — verified per DESIGN-15, 2026-05-10)

- **host:** `app-eu.wrike.com` (persisted from OAuth token response per DESIGN-15 / MOD-5)
- **account_id:** `5996999`
- **Entry URL pattern:** `https://app-eu.wrike.com/workspace.htm?acc=5996999`
- **Base URL for API calls:** `https://app-eu.wrike.com/api/v4`
- **Source artefact:** DESIGN-15 at `.planning/DESIGN.md:459-500`
- **Persistence:** `<Client> Brain/00_HUB.md` Coda block carries the host (per DESIGN-29 schema)

## Pattern slots (variant taxonomy)

### Regional host variants
- **US default:** `app.wrike.com` (US tenants without regional sharding)
- **US-2:** `app-us2.wrike.com` (US-2 region tenants)
- **EU:** `app-eu.wrike.com` (EU region tenants — e.g., VodafoneZiggo)
- All three are PERSISTED from OAuth token response — NEVER hardcoded.

### account_id per tenant
- Per-tenant integer identifier surfaced in entry URLs (`?acc=<account_id>`)
- Used for tenant disambiguation in multi-account Wrike workspaces
- Stored in `<Client> Brain/00_HUB.md` Wrike block

### Space + project nesting
- `space_id` — top-level workspace partition (per-tenant; may have multiple spaces)
- `folder_id` — nested folder within a space
- `project_id` — a project within a space or folder (carries tasks)
- Nesting depth varies per client — Pattern slot for "shallow vs deep nesting" awaits first-engagement data

### Custom field IDs per tenant
- Wrike custom fields are tenant-scoped — IDs do NOT match across clients
- Per-client mapping lives in `<Client> Brain/00_HUB.md` `wrike_custom_fields:` block
- Stage 4a fnspec MUST cite per-tenant custom field IDs explicitly, never assume cross-tenant consistency

## How to add a new shape

When onboarding a new client, append a verified row to `## Known shapes (verified)`
citing the engagement date + source artefact. Pattern slots are append-only;
existing rows are preserved for audit trail.
```

**FILE C: `dydx-delivery/skills/platform-wrike/references/vocabulary.md`**

```markdown
# Wrike — Vocabulary

> For project-wide terms see `dydx-delivery/references/glossary.md`.

## Platform-specific terms

**space** — Top-level workspace partition in Wrike. A tenant may have one or many spaces; spaces are the outermost container for folders, projects, and tasks. Maps to `space_id` in REST queries.

**folder** — Nested container within a space (or within another folder). Holds projects and tasks. Maps to `folder_id`.

**project** — A Wrike project within a space or folder. Carries tasks, custom fields, and a workflow state. Maps to `project_id`.

**task** — Individual work-item record. Carries title, description, assignees, custom field values, attachments, comments, and transition history. The lowest-grain Wrike entity.

**custom field** — Per-tenant typed field attached to projects or tasks. IDs do NOT match across tenants — per-client mapping lives in `<Client> Brain/00_HUB.md`.

**host** — Per-tenant Wrike regional host (e.g., `app-us2.wrike.com`, `app-eu.wrike.com`). MUST be persisted from the OAuth token response — NEVER hardcoded (per MOD-5 / DESIGN-15). Read at session start from `client_state.yaml` `wrike.host:`.

**account_id** — Per-tenant integer identifier surfaced in entry URLs (`?acc=<account_id>`). Used for tenant disambiguation.

**space_id** — Identifier for a Wrike space. Required in REST queries targeting space-scoped operations.

**Wrike Copilot** — Wrike's 2026 real-time AI capability surface in the workspace UI. Capability matrix tracked in `references/native-ai-inventory.md`.

**MCP Server** — Wrike MCP Server (`developers.wrike.com/wrike-mcp/`) providing tool-based programmatic access. Available but parked through v2.6 (UAT-3.5).

> Verification gate: synthesis-plan grep dedup confirms zero of these terms
> are ALSO defined in `dydx-delivery/references/glossary.md`. Cross-cutting
> terms (frontmatter / sandbox / native_ai_path / status: lifecycle) live in
> glossary.md, NOT here.
```

After Write, delete `dydx-delivery/skills/platform-wrike/references/.gitkeep` (placeholder no longer needed).
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-wrike/references/knowledge-ingestion.md && test -f dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md && test -f dydx-delivery/skills/platform-wrike/references/vocabulary.md && grep -qF 'Copilot workflow narrative' dydx-delivery/skills/platform-wrike/references/knowledge-ingestion.md && grep -qF 'VodafoneZiggo' dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md && grep -qF 'app-eu.wrike.com' dydx-delivery/skills/platform-wrike/references/client-shape-gotchas.md && grep -qF 'space' dydx-delivery/skills/platform-wrike/references/vocabulary.md && grep -qF 'dydx-delivery/references/glossary.md' dydx-delivery/skills/platform-wrike/references/vocabulary.md && [ ! -f dydx-delivery/skills/platform-wrike/references/.gitkeep ] && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section wrike 2>&1 | grep -qE '^PASS: A4'</automated>
  </verify>
  <done>All 3 files exist; UAT-6.1 paste-only contract documented in knowledge-ingestion.md; D-65 VodafoneZiggo EU seed + 4 pattern slots in client-shape-gotchas.md; D-66 vocabulary opens with glossary pointer + Wrike-specific terms only; .gitkeep placeholder removed; A4-wrike PASS.</done>
  <acceptance_criteria>
    - Satisfies A4 (wrike partition): all 5 of `dydx-delivery/skills/platform-wrike/references/{api-contract.md, native-ai-inventory.md, knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md}` exist
    - knowledge-ingestion.md (UAT-6.1): `grep -qF 'Copilot workflow narrative' <file> && grep -qF 'UAT-6.1' <file> && grep -qF 'paste-only' <file>`
    - client-shape-gotchas.md (D-65 seed): `grep -qF 'VodafoneZiggo' <file> && grep -qF 'app-eu.wrike.com' <file> && grep -qF '5996999' <file> && grep -qF 'Append-only' <file>`
    - vocabulary.md (D-66 pointer): `grep -qF 'For project-wide terms see' <file>` exits 0 AND `grep -cE '^\*\*(space|folder|project|task|custom field|host|account_id|space_id|Wrike Copilot|MCP Server)\*\*' <file>` returns >= 10
    - vocabulary.md D-66 dedup pre-check: `grep -qE '^\*\*(frontmatter|sandbox|native_ai_path)\*\* ' <file>` exits 1 (those terms NOT defined here)
    - Satisfies A7 partial (T-06-02 file-level): `grep -qF 'native_ai_path: api' <file>` exits 1 for each of the 3 files
    - .gitkeep placeholder removed: `[ ! -f dydx-delivery/skills/platform-wrike/references/.gitkeep ]` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all 4 tasks land:

1. `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section wrike` exits 0 (Wrike-specific assertions A2, A4-wrike, A5-wrike, A6-wrike, A12, A15, A17-wrike PASS).
2. Full suite continues to FAIL at A3 (Ziflow SKILL.md not yet created — expected; resolved in 06-03).
3. `grep -r 'native_ai_path: api' dydx-delivery/skills/platform-wrike/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'` returns zero hits (T-06-02 mitigation).
4. `grep -rn 'NEVER hardcode' dydx-delivery/skills/platform-wrike/references/api-contract.md` returns at least one hit (T-06-03 prohibition phrase present).
5. All 6 Wrike tree files (1 SKILL.md + 5 references/) committed atomically per task; both `.gitkeep` placeholders deleted.
</verification>

<success_criteria>
Plan 06-02 is complete when:
- [ ] `platform-wrike/SKILL.md` exists with canonical frontmatter (`platform: wrike`, `tier_claims_last_verified: 2026-05-09`, `native_ai_path: paste`, `frontmatter_version: 2`), D-59 hard-rules pointer block, D-68 re-verification trigger
- [ ] `platform-wrike/references/api-contract.md` documents OAuth-host 3-step persistence (MOD-5) + literal `NEVER hardcode www.wrike.com` prohibition (T-06-03) + 320 req/min/user throttle (Q07.2) + 16-tool baseline note + VodafoneZiggo worked example
- [ ] `platform-wrike/references/native-ai-inventory.md` documents 4-row Wrike Copilot + 16-tool MCP capability matrix
- [ ] `platform-wrike/references/knowledge-ingestion.md` documents UAT-6.1 paste-only contract (Copilot workflow narrative + MCP tool config + attach-doc-via-MCP)
- [ ] `platform-wrike/references/client-shape-gotchas.md` carries VodafoneZiggo verified seed (D-65) + 4 pattern slots
- [ ] `platform-wrike/references/vocabulary.md` opens with glossary pointer (D-66) and lists 10 Wrike-specific terms only
- [ ] Both `.gitkeep` placeholders for platform-wrike/ + platform-wrike/references/ deleted
- [ ] Phase-6 structure-check assertions PASSING after plan completion: A2, A4-wrike, A5-wrike, A6-wrike, A7-partial-wrike, A12, A15, A17-wrike
- [ ] Zero hits for `native_ai_path: api` in platform-wrike/ tree (T-06-02 mitigation)
- [ ] All deliverables committed; each task = 1 atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-internalise-platform-skills/06-02-SUMMARY.md` capturing:
- Wrike SKILL.md + 5 references/ files landed end-to-end with all per-platform assertions PASSING
- Q07.2 throttle calibration resolved inline (320 req/min/user) — landed in api-contract.md § Rate limit + throttle; OPEN-QUESTIONS.md row flip deferred to 06-04 synthesis plan
- Any execution-time re-verification of MCP tool count vs vendor docs (manual; documents whether tier_claims_last_verified: was bumped forward + whether the 16-vs-47 delta resolves to an authoritative count)
- D-59 + D-68 pattern carry-forward confirmed verbatim from 06-01 Pipefy sibling
- Carry-forward notes for 06-03 (Ziflow) — e.g., 3-step pseudocode pattern works for non-helper "pattern" shapes; A12 prohibition-phrase grep gate proves mid-phase
</output>
