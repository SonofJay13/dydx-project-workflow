---
phase: 06-internalise-platform-skills
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - .planning/OPEN-QUESTIONS.md
autonomous: true
requirements: [PLAT-06]
tags: [synthesis, open-questions, vocabulary-dedup, structure-check-final, q05, q06-2, q07-2]

must_haves:
  truths:
    - "Per D-67, all 3 Phase-6-owned OPEN-Q row flips (Q05, Q06.2, Q07.2) happen in this synthesis plan — single-owner write to .planning/OPEN-QUESTIONS.md avoids inter-plan file-ownership conflict (Phase 5 D-57 precedent)."
    - "Per D-66 verification gate, vocabulary dedup grep confirms zero project-wide glossary terms (frontmatter / sandbox / native_ai_path / status: lifecycle) are duplicated across the 3 platform vocabulary.md files."
    - "Per VALIDATION.md A1..A17, phase6-structure-check.sh must exit 0 (all assertions PASS) by end of this plan — that's the Phase 6 reviewer-ready signal."
    - "Per PLAT-06, all 3 OPEN-Q rows show Status: decided with resolution text citing the specific api-contract.md path where the resolved value landed."
    - "Per D-63 + orchestrator note, this plan is the synthesis split-off (06-04) per planner discretion when 06-03 would have exceeded ~400 LOC — 06-03 ships the Ziflow platform skill; 06-04 ships only cross-platform verification gates + OPEN-QUESTIONS row flips."
    - "Per REVIEWS C8, A16 row-flip verification uses awk-between-OPEN-Q-headings extraction (NOT `grep -A 11`) — inserted decision bullets could shift the Status: line beyond 11 lines and false-negative under the old shape."
  artifacts:
    - path: ".planning/OPEN-QUESTIONS.md"
      provides: "Updated OPEN-QUESTIONS register with Q05 / Q06.2 / Q07.2 Status flipped from proposed → decided (PLAT-06 verification)"
      contains: "Status: decided"
  key_links:
    - from: ".planning/OPEN-QUESTIONS.md (Q05 row)"
      to: "dydx-delivery/skills/platform-ziflow/references/api-contract.md § wait_for_proof"
      via: "Q05 resolution one-liner cites where the resolved value landed"
      pattern: "platform-ziflow/references/api-contract\\.md"
    - from: ".planning/OPEN-QUESTIONS.md (Q06.2 row)"
      to: "dydx-delivery/skills/platform-pipefy/references/api-contract.md § Rate limit + throttle"
      via: "Q06.2 resolution one-liner"
      pattern: "platform-pipefy/references/api-contract\\.md"
    - from: ".planning/OPEN-QUESTIONS.md (Q07.2 row)"
      to: "dydx-delivery/skills/platform-wrike/references/api-contract.md § Rate limit + throttle"
      via: "Q07.2 resolution one-liner"
      pattern: "platform-wrike/references/api-contract\\.md"
---

<objective>
Wave 3 of Phase 6: cross-platform synthesis — flip the 3 Phase-6-owned OPEN-Q rows (Q05 / Q06.2 / Q07.2) to Status: decided per D-67, run the D-66 vocabulary dedup grep gate (PLAT-05 / D-66 verification), and confirm `phase6-structure-check.sh` exits 0 with all 17 assertions A1..A17 PASS — the Phase 6 reviewer-ready signal.

Purpose: Single-owner write to `.planning/OPEN-QUESTIONS.md` (the only Phase-6 file outside the 3 platform-skill directories) prevents file-ownership conflict between 06-01 / 06-02 / 06-03. Vocabulary dedup and final structure-check exit-0 verification can only meaningfully run after all 3 platform skills are landed (Wave 2 complete).

Output: 1 modified `.planning/OPEN-QUESTIONS.md` + final structure-check PASS confirmation in the synthesis SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-internalise-platform-skills/06-CONTEXT.md
@.planning/phases/06-internalise-platform-skills/06-RESEARCH.md
@.planning/phases/06-internalise-platform-skills/06-PATTERNS.md
@.planning/phases/06-internalise-platform-skills/06-VALIDATION.md
@.planning/phases/06-internalise-platform-skills/06-REVIEWS.md
@.planning/phases/06-internalise-platform-skills/06-01-SUMMARY.md
@.planning/phases/06-internalise-platform-skills/06-02-SUMMARY.md
@.planning/phases/06-internalise-platform-skills/06-03-SUMMARY.md
@.planning/OPEN-QUESTIONS.md
@dydx-delivery/references/glossary.md
@dydx-delivery/skills/platform-pipefy/references/api-contract.md
@dydx-delivery/skills/platform-pipefy/references/vocabulary.md
@dydx-delivery/skills/platform-wrike/references/api-contract.md
@dydx-delivery/skills/platform-wrike/references/vocabulary.md
@dydx-delivery/skills/platform-ziflow/references/api-contract.md
@dydx-delivery/skills/platform-ziflow/references/vocabulary.md

<interfaces>
<!-- Q05 / Q06.2 / Q07.2 current OPEN-QUESTIONS.md row shapes (read from file at execution time; current snippet at lines 107-162 per Grep verification 2026-05-11): -->

**OPEN-Q05 current state** (`.planning/OPEN-QUESTIONS.md:107-116`):
```
**OPEN-Q05** — Ziflow read-after-create consistency window

- Question: What is the actual read-after-create consistency window for Ziflow proof-create operations in 2026?
- Source citations: `.planning/CHANGELIST.md:282`, `.planning/DESIGN.md:523`
- Owning phase: Phase 2
- Verification owner: Phase 2 / dev
- Severity: GUARDRAIL
- Resolution path: /gsd-research-phase 2
- Proposed default: conservative — 30 second poll with 2s interval (per CHANGELIST.md Appendix E bullet verbatim recommendation).
- Status: proposed
```

**OPEN-Q06.2 current state** (`.planning/OPEN-QUESTIONS.md:130-139`):
```
**OPEN-Q06.2** — Pipefy 2026 rate-limit consumer-throttle calibration (cross-AI C6 split: Phase 2 helper implementation)

- Question: Given the Phase 1 published-rate-limit answer (Q06.1), what is the calibrated consumer throttle value the platform-pipefy helpers should enforce?
- Source citations: `.planning/CHANGELIST.md:277`, `.planning/DESIGN.md:442`, `.planning/DESIGN.md:795`
- Owning phase: Phase 2
- Verification owner: Phase 2 / dev
- Severity: INFORMATIONAL
- Resolution path: /gsd-research-phase 2
- Proposed default: helpers throttle at 4 req/sec (80% of historic ceiling — locked by DESIGN-22 carried throttle pattern at `.planning/DESIGN.md:795`) pending Q06.1 confirmation. Cross-references Q06.1.
- Status: proposed
```

**OPEN-Q07.2 current state** (`.planning/OPEN-QUESTIONS.md:153-162`):
```
**OPEN-Q07.2** — Wrike 2026 rate-limit consumer-throttle calibration (cross-AI C6 split: Phase 2 helper implementation)

- Question: Given the Phase 1 published-rate-limit answer (Q07.1), what is the calibrated consumer throttle value the platform-wrike helpers should enforce?
- Source citations: `.planning/CHANGELIST.md:278`, `.planning/DESIGN.md:483`, `.planning/DESIGN.md:795`
- Owning phase: Phase 2
- Verification owner: Phase 2 / dev
- Severity: INFORMATIONAL
- Resolution path: /gsd-research-phase 2
- Proposed default: helpers throttle at 80% of confirmed published ceiling per DESIGN-22 carried throttle pattern (`.planning/DESIGN.md:795`); pending Q07.1 confirmation. Cross-references Q07.1.
- Status: proposed
```

<!-- Edit pattern for each row (Phase 5 D-57 precedent — visible in OPEN-Q06.1 / OPEN-Q07.1 already-flipped form at .planning/OPEN-QUESTIONS.md:127-128, 150-151): -->

For each of the 3 rows above, INSERT a `**Decision (2026-05-11 Phase 6 PLAT-06 inline closure):**` bullet IMMEDIATELY ABOVE the `- Status:` line, then FLIP `- Status: proposed` → `- Status: decided`. The Phase 5 model for these flips is the Q06.1 / Q07.1 rows already in the file (lines 127-128 and 150-151 currently) — same pattern (`- **Decision (2026-05-10 W4 OPEN-Q closure):** Resolution: ...`).

<!-- Per-row resolution one-liner text (verbatim from RESEARCH.md:640, 656, 672 + 06-01/02/03 actual landed values): -->

- **Q05:** `- **Decision (2026-05-11 Phase 6 PLAT-06 inline closure):** Resolution: Webhook-primary per vendor guidance (`help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks`); `wait_for_proof` fallback defaults confirmed at `max_wait_s=30` / `interval_s=2`. Cited at `dydx-delivery/skills/platform-ziflow/references/api-contract.md` § wait_for_proof.`

- **Q06.2:** `- **Decision (2026-05-11 Phase 6 PLAT-06 inline closure):** Resolution: Helpers throttle at 13 req/sec per token (80% of 16.67 req/sec ceiling per Phase 5 Q06.1 at `dydx-delivery/references/connector-matrix.md:72`). Cited at `dydx-delivery/skills/platform-pipefy/references/api-contract.md` § Rate limit + throttle.`

- **Q07.2:** `- **Decision (2026-05-11 Phase 6 PLAT-06 inline closure):** Resolution: Helpers throttle at 320 req/min per user (80% of 400 ceiling per Phase 5 Q07.1 at `dydx-delivery/references/connector-matrix.md:73`). Cited at `dydx-delivery/skills/platform-wrike/references/api-contract.md` § Rate limit + throttle.`

<!-- D-66 vocabulary dedup grep gate target terms (from glossary.md project-wide terms): -->

Forbidden terms in any platform `vocabulary.md` (they MUST live in `dydx-delivery/references/glossary.md` ONLY):
- `frontmatter`
- `sandbox`
- `native_ai_path`
- `status:` lifecycle terms (`draft`, `client_review`, `approved`, `archived`)
- `frontmatter_version`
- `approved_by`, `approved_at`
- `based_on_*` field names

The dedup gate searches for these as `**term**` definition markers in each platform's `vocabulary.md`. Zero hits required.

<!-- REVIEWS C8 — awk-between-OPEN-Q-headings extraction shape (replaces grep -A 11): -->

```bash
# Per-question awk extraction: print only the lines between the OPEN-Q<id> heading
# anchor and the next OPEN-Q heading anchor. Robust against decision-bullet
# insertions that shift the Status: line position.
awk -v q="Q05" '
  $0 ~ "^\\*\\*OPEN-"q"\\*\\* " {p=1; next}
  p && /^\*\*OPEN-Q/ {p=0}
  p
' .planning/OPEN-QUESTIONS.md | grep -q '^- Status: decided$'
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer ↔ OPEN-QUESTIONS.md register | Single audit-trail file for the project's pending decisions; row-flip integrity is the synthesis-plan gate |
| Synthesis writer ↔ 3 sibling plan outputs | This plan is the ONLY plan that writes to .planning/OPEN-QUESTIONS.md in Phase 6 (file-ownership concurrency control per D-67) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information disclosure (banned value leak — cross-platform) | the forbidden third enum value could leak via OPEN-QUESTIONS narrative copy-paste | mitigate | A7 grep gate (tightened to YAML-field-assignment regex per REVIEWS C3) runs across ALL Phase 6 outputs at end of this plan; zero hits required |
| T-06-04 | Tampering (cross-platform consistency) | the previously-considered per-tenant API-host override field name could leak across Phase 6 outputs | mitigate | Final cross-tree grep using tightened YAML-field-assignment regex per REVIEWS C2 exits 1 (zero hits) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Flip OPEN-QUESTIONS.md Q05 / Q06.2 / Q07.2 rows from proposed → decided (REVIEWS C8 awk-between-headings verification shape)</name>
  <files>.planning/OPEN-QUESTIONS.md</files>
  <read_first>
    - .planning/OPEN-QUESTIONS.md:107-162 (current Q05 / Q06.1 / Q06.2 / Q07.1 / Q07.2 row blocks — Q06.1 + Q07.1 already show the flipped form to mirror)
    - .planning/phases/06-internalise-platform-skills/06-REVIEWS.md (REVIEWS C8: A16 verification uses awk-between-OPEN-Q-headings extraction; REVIEWS C3: prose must not write forbidden YAML field assignment)
    - .planning/phases/06-internalise-platform-skills/06-01-SUMMARY.md (carries Q06.2 resolved-value confirmation from Pipefy api-contract.md landing)
    - .planning/phases/06-internalise-platform-skills/06-02-SUMMARY.md (carries Q07.2 resolved-value confirmation from Wrike api-contract.md landing)
    - .planning/phases/06-internalise-platform-skills/06-03-SUMMARY.md (carries Q05 resolved-value confirmation from Ziflow api-contract.md landing)
    - dydx-delivery/skills/platform-pipefy/references/api-contract.md § Rate limit + throttle (verify "13 req/sec" resolved value matches the Q06.2 decision string)
    - dydx-delivery/skills/platform-wrike/references/api-contract.md § Rate limit + throttle (verify "320 req/min" resolved value)
    - dydx-delivery/skills/platform-ziflow/references/api-contract.md § wait_for_proof + § Webhook-PRIMARY (verify "max_wait_s=30" + "interval_s=2" + "help.ziflow.com" anchors match Q05 decision string)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md D-67 (inline OPEN-Q closure pattern)
  </read_first>
  <action>
Edit `.planning/OPEN-QUESTIONS.md` to flip 3 OPEN-Q rows from `Status: proposed` to `Status: decided`. Mirror the existing Phase 5 W4 OPEN-Q closure pattern visible at OPEN-Q06.1 lines 127-128 and OPEN-Q07.1 lines 150-151. For each of the 3 rows, INSERT a `- **Decision (...):**` bullet IMMEDIATELY ABOVE the `- Status:` line, then UPDATE that `- Status: proposed` line to `- Status: decided`.

**Edit 1: Q05 row** (currently at approx. lines 107-116).

Find the block starting `**OPEN-Q05** — Ziflow read-after-create consistency window` and ending at the next `**OPEN-Q` marker. Within that block, ABOVE the `- Status: proposed` line, INSERT this exact bullet:

```
- **Decision (2026-05-11 Phase 6 PLAT-06 inline closure):** Resolution: Webhook-primary per vendor guidance (`help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks`); `wait_for_proof` fallback defaults confirmed at `max_wait_s=30` / `interval_s=2`. Cited at `dydx-delivery/skills/platform-ziflow/references/api-contract.md` § wait_for_proof.
```

Then CHANGE the `- Status: proposed` line in that same block to `- Status: decided`.

**Edit 2: Q06.2 row** (currently at approx. lines 130-139).

Find the block starting `**OPEN-Q06.2** — Pipefy 2026 rate-limit consumer-throttle calibration` and ending at the next `**OPEN-Q` marker. Within that block, ABOVE the `- Status: proposed` line, INSERT this exact bullet:

```
- **Decision (2026-05-11 Phase 6 PLAT-06 inline closure):** Resolution: Helpers throttle at 13 req/sec per token (80% of 16.67 req/sec ceiling per Phase 5 Q06.1 at `dydx-delivery/references/connector-matrix.md:72`). Cited at `dydx-delivery/skills/platform-pipefy/references/api-contract.md` § Rate limit + throttle.
```

Then CHANGE the `- Status: proposed` line in that same block to `- Status: decided`.

**Edit 3: Q07.2 row** (currently at approx. lines 153-162).

Find the block starting `**OPEN-Q07.2** — Wrike 2026 rate-limit consumer-throttle calibration` and ending at the next `**OPEN-Q` marker. Within that block, ABOVE the `- Status: proposed` line, INSERT this exact bullet:

```
- **Decision (2026-05-11 Phase 6 PLAT-06 inline closure):** Resolution: Helpers throttle at 320 req/min per user (80% of 400 ceiling per Phase 5 Q07.1 at `dydx-delivery/references/connector-matrix.md:73`). Cited at `dydx-delivery/skills/platform-wrike/references/api-contract.md` § Rate limit + throttle.
```

Then CHANGE the `- Status: proposed` line in that same block to `- Status: decided`.

**Pre-edit verification (sanity):** Run `grep -cE '^- Status: proposed$' .planning/OPEN-QUESTIONS.md` and record the baseline count. After all 3 edits, the count MUST decrease by exactly 3.

**Use targeted Edit tool calls** scoped to each block — the entire OPEN-QUESTIONS.md should NOT be re-written. Do NOT modify rollup tables at top of file (line 28 etc.) — only the 3 row bodies. The rollup tables already cite these Qs by ID and severity; flipping individual row Status is the canonical mutation per D-67.

**REVIEWS C8 — verification gate shape (NOT `grep -A 11`):** The post-edit verify step below uses awk-between-OPEN-Q-headings extraction (per REVIEWS C8) instead of `grep -A 11`. The new shape is robust against future decision-bullet insertions that would shift the `- Status:` line beyond a fixed-line count.
  </action>
  <verify>
    <automated>awk '$0 ~ /^\*\*OPEN-Q05\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qE '^- Status: decided$' && awk '$0 ~ /^\*\*OPEN-Q05\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qF 'help.ziflow.com/hc/en-us/articles/30725068740244-Webhooks' && awk '$0 ~ /^\*\*OPEN-Q06\.2\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qE '^- Status: decided$' && awk '$0 ~ /^\*\*OPEN-Q06\.2\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qF '13 req/sec per token' && awk '$0 ~ /^\*\*OPEN-Q07\.2\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qE '^- Status: decided$' && awk '$0 ~ /^\*\*OPEN-Q07\.2\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qF '320 req/min per user' && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A16'</automated>
  </verify>
  <done>All 3 OPEN-Q rows (Q05, Q06.2, Q07.2) now show `Status: decided` with resolution-bullet citing the api-contract.md path where the resolved value landed; A16 PASS verified via awk-between-OPEN-Q-headings extraction (REVIEWS C8 shape — no `grep -A 11`).</done>
  <acceptance_criteria>
    - Satisfies A16 (REVIEWS C8 awk shape): For each of Q05, Q06.2, Q07.2 — `awk '$0 ~ /^\*\*OPEN-Q<id>\*\* / {p=1; next} p && /^\*\*OPEN-Q/ {p=0} p' .planning/OPEN-QUESTIONS.md | grep -qE '^- Status: decided$'` exits 0
    - Q05 resolution citation present: awk-extract of OPEN-Q05 block | `grep -qF 'platform-ziflow/references/api-contract.md'` exits 0
    - Q06.2 resolution citation present: awk-extract of OPEN-Q06.2 block | `grep -qF 'platform-pipefy/references/api-contract.md'` exits 0
    - Q07.2 resolution citation present: awk-extract of OPEN-Q07.2 block | `grep -qF 'platform-wrike/references/api-contract.md'` exits 0
    - Net reduction of `proposed` count by exactly 3: pre-edit baseline N − post-edit count == 3 (record both in commit message)
    - No other rows mutated: `grep -cE '^- Status: ' .planning/OPEN-QUESTIONS.md` count unchanged from pre-edit (only Status values shifted, no rows added/removed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-66 vocabulary dedup gate + cross-tree T-06-02 / T-06-04 grep gates (tightened regex per REVIEWS C2 + C3) + final phase6-structure-check.sh exit 0</name>
  <files>.planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh (read-only — verify script exits 0; no edits)</files>
  <read_first>
    - .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh (Wave 0 scaffold from 06-01 Task 1; full assertion list A1..A17 with REVIEWS C1 per-section partition + REVIEWS C2/C3 tightened regex + REVIEWS C8 awk shape)
    - .planning/phases/06-internalise-platform-skills/06-VALIDATION.md:55-73 (master assertion list)
    - .planning/phases/06-internalise-platform-skills/06-REVIEWS.md (REVIEWS C2 + C3 tightened regex; REVIEWS C8 awk shape)
    - dydx-delivery/references/glossary.md (D-66 dedup target — list of project-wide terms)
    - dydx-delivery/skills/platform-pipefy/references/vocabulary.md (D-66 grep target — must contain zero project-wide terms)
    - dydx-delivery/skills/platform-wrike/references/vocabulary.md (D-66 grep target)
    - dydx-delivery/skills/platform-ziflow/references/vocabulary.md (D-66 grep target)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md D-66 (vocabulary dedup contract)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:344-353 (dedup gate enumeration of forbidden cross-cutting terms)
  </read_first>
  <action>
This task runs THREE verification gates and confirms structure-check exits 0. NO files are modified. If any gate fails, the executor reports the specific platform + term + line offset failing the gate and exits without committing — the failing platform's references/vocabulary.md (06-01 / 06-02 / 06-03 Task 4) is amended in a Rule-1 fix to the relevant sibling plan, NOT in this synthesis plan.

**Gate 1 — D-66 vocabulary dedup grep (per CONTEXT D-66 verification gate):**

For each of the forbidden cross-cutting terms (`frontmatter`, `sandbox`, `native_ai_path`, `status:`, `frontmatter_version`, `approved_by`, `approved_at`, `based_on_`), run:

```bash
grep -rE "^\\*\\*(frontmatter|sandbox|native_ai_path|frontmatter_version|approved_by|approved_at|based_on_[a-z_]+)\\*\\* " dydx-delivery/skills/platform-pipefy/references/vocabulary.md dydx-delivery/skills/platform-wrike/references/vocabulary.md dydx-delivery/skills/platform-ziflow/references/vocabulary.md
```

Required outcome: ZERO hits. If any term appears as a `**term** —` definition in any platform vocabulary.md, that's a D-66 violation — record the failing platform + line + term and surface to the user.

Also explicit check for `status:` lifecycle terms (`draft`, `client_review`, `approved`, `archived`) as definitions (NOT as status-value mentions in narrative — only as `**draft** —` style definition markers):

```bash
grep -rE "^\\*\\*(draft|client_review|approved|archived)\\*\\* " dydx-delivery/skills/platform-*/references/vocabulary.md
```

Required outcome: ZERO hits.

**Gate 2 — T-06-02 cross-tree forbidden-token grep (PLAT-05 / A7; REVIEWS C3 tightened regex):**

```bash
grep -rEn '^[[:space:]]*native_ai_path:[[:space:]]+api[[:space:]]*$' \
  dydx-delivery/skills/platform-pipefy/ \
  dydx-delivery/skills/platform-wrike/ \
  dydx-delivery/skills/platform-ziflow/ \
  --exclude-dir=scripts --exclude='*.sh' --exclude='*~'
```

Required outcome: ZERO hits (exit 1). This is A7's full-tree manifestation under the tightened YAML-field-assignment regex (REVIEWS C3); the per-file partial grep gates from 06-01/02/03 are PASS-confirmed individually — this run is the cross-platform integrative confirmation.

**Gate 3 — T-06-04 cross-tree `api_host` field-assignment grep (REVIEWS C2 tightened regex):**

```bash
grep -rEn '^[[:space:]]*api_host[[:space:]]*:' dydx-delivery/skills/platform-pipefy/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'
```

Required outcome: ZERO hits (exit 1). UAT-4.1 removed the per-tenant API-host override configuration field; A11 second-half assertion gates this for platform-pipefy/references/api-contract.md specifically — this gate widens to the whole platform-pipefy/ tree under the tightened YAML-field-assignment regex (REVIEWS C2).

**Final gate — phase6-structure-check.sh exit 0 (all 17 assertions A1..A17 PASS):**

```bash
bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh
echo "Exit code: $?"
```

Required outcome: exit code 0; all 17 `PASS: AN` lines printed; final line `ALL ASSERTIONS PASSED`.

**Documentation step:** Record the gate results (PASS/FAIL with line offsets if FAIL) in the synthesis SUMMARY (06-04-SUMMARY.md) as evidence that PLAT-05 + PLAT-06 are satisfied and that Phase 6 is reviewer-ready. No files are modified by this task — the SUMMARY artefact is created in the standard `<output>` step below.
  </action>
  <verify>
    <automated>! grep -rqE '^\*\*(frontmatter|sandbox|native_ai_path|frontmatter_version|approved_by|approved_at|based_on_[a-z_]+)\*\* ' dydx-delivery/skills/platform-pipefy/references/vocabulary.md dydx-delivery/skills/platform-wrike/references/vocabulary.md dydx-delivery/skills/platform-ziflow/references/vocabulary.md && ! grep -rqE '^\*\*(draft|client_review|approved|archived)\*\* ' dydx-delivery/skills/platform-pipefy/references/vocabulary.md dydx-delivery/skills/platform-wrike/references/vocabulary.md dydx-delivery/skills/platform-ziflow/references/vocabulary.md && ! grep -rqEn '^[[:space:]]*native_ai_path:[[:space:]]+api[[:space:]]*$' dydx-delivery/skills/platform-pipefy/ dydx-delivery/skills/platform-wrike/ dydx-delivery/skills/platform-ziflow/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~' && ! grep -rqEn '^[[:space:]]*api_host[[:space:]]*:' dydx-delivery/skills/platform-pipefy/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~' && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh</automated>
  </verify>
  <done>D-66 dedup gate: zero project-wide terms defined in any platform vocabulary.md; T-06-02 cross-tree gate (REVIEWS C3 tightened regex): zero YAML-field-assignment hits for the forbidden third enum value across all 3 platform trees; T-06-04 cross-tree gate (REVIEWS C2 tightened regex): zero YAML-field-assignment hits for the removed per-tenant API-host override configuration field in platform-pipefy/; phase6-structure-check.sh exits 0 with all A1..A17 PASS. Phase 6 is reviewer-ready.</done>
  <acceptance_criteria>
    - D-66 dedup gate (PLAT-05 / vocabulary): `grep -rqE '^\*\*(frontmatter|sandbox|native_ai_path|frontmatter_version|approved_by|approved_at|based_on_[a-z_]+)\*\* ' dydx-delivery/skills/platform-*/references/vocabulary.md` exits 1 (zero hits)
    - D-66 status-lifecycle terms gate: `grep -rqE '^\*\*(draft|client_review|approved|archived)\*\* ' dydx-delivery/skills/platform-*/references/vocabulary.md` exits 1 (zero hits)
    - T-06-02 / A7 cross-tree gate (REVIEWS C3 tightened regex): `grep -rqEn '^[[:space:]]*native_ai_path:[[:space:]]+api[[:space:]]*$' dydx-delivery/skills/platform-pipefy/ dydx-delivery/skills/platform-wrike/ dydx-delivery/skills/platform-ziflow/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'` exits 1 (zero hits)
    - T-06-04 cross-tree gate (REVIEWS C2 tightened regex): `grep -rqEn '^[[:space:]]*api_host[[:space:]]*:' dydx-delivery/skills/platform-pipefy/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'` exits 1 (zero hits)
    - Final structure-check exit 0: `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` exits 0
    - All 17 assertions PASS: `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -cE '^PASS: A[0-9]+'` returns >= 17
    - Final line confirms: `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | tail -1` matches `ALL ASSERTIONS PASSED`
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks land:

1. `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` exits 0 — all 17 assertions A1..A17 PASS. This is the Phase 6 reviewer-ready signal.
2. `.planning/OPEN-QUESTIONS.md` shows 3 newly-decided rows (Q05, Q06.2, Q07.2) with resolution-bullet citing the platform-skill api-contract.md path. Status counts: `proposed` decreased by 3, `decided` increased by 3. Verification uses awk-between-OPEN-Q-headings extraction per REVIEWS C8 (NOT `grep -A 11`).
3. Cross-tree T-06-02 + T-06-04 gates PASS (zero forbidden-YAML-field-assignment hits under REVIEWS C2 + C3 tightened regex).
4. D-66 vocabulary dedup gate PASSES — no project-wide glossary term is duplicated in any platform vocabulary.md.

**Phase 6 reviewer-ready criteria (per ROADMAP success criteria 1..5):**
- SC1 (Pipefy platform skill shipped with revised DESIGN-14 contract): 06-01 PASS
- SC2 (Wrike platform skill shipped with OAuth-host persistence): 06-02 PASS
- SC3 (Ziflow platform skill shipped with `wait_for_proof` helper): 06-03 PASS
- SC4 (Frontmatter contracts locked across all 3 platform skills): 06-01/02/03 PASS (A5 + A6 + A7 cross-tree)
- SC5 (3 throttle/consistency OPEN-QUESTIONS resolved): 06-04 Task 1 PASS (A16 via REVIEWS C8 awk shape)
</verification>

<success_criteria>
Plan 06-04 is complete when:
- [ ] `.planning/OPEN-QUESTIONS.md` Q05 row Status flipped to `decided` with resolution citing platform-ziflow/references/api-contract.md § wait_for_proof
- [ ] `.planning/OPEN-QUESTIONS.md` Q06.2 row Status flipped to `decided` with resolution citing platform-pipefy/references/api-contract.md § Rate limit + throttle (13 req/sec)
- [ ] `.planning/OPEN-QUESTIONS.md` Q07.2 row Status flipped to `decided` with resolution citing platform-wrike/references/api-contract.md § Rate limit + throttle (320 req/min/user)
- [ ] A16 verification uses awk-between-OPEN-Q-headings extraction (REVIEWS C8 shape) — NOT `grep -A 11`
- [ ] D-66 vocabulary dedup grep gate PASSES (zero project-wide glossary terms defined in any platform vocabulary.md)
- [ ] T-06-02 cross-tree gate PASSES under REVIEWS C3 tightened regex (zero YAML-field-assignment hits for the forbidden third enum value across all 3 platform trees)
- [ ] T-06-04 cross-tree gate PASSES under REVIEWS C2 tightened regex (zero YAML-field-assignment hits for the removed per-tenant API-host override configuration field in platform-pipefy/)
- [ ] `phase6-structure-check.sh` exits 0 with all 17 assertions A1..A17 PASS — Phase 6 reviewer-ready signal
- [ ] All 5 ROADMAP success criteria mapped: SC1 (06-01) / SC2 (06-02) / SC3 (06-03) / SC4 (06-01/02/03 cross-tree A5+A6+A7) / SC5 (06-04 Task 1 A16)
- [ ] Each task = 1 atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-internalise-platform-skills/06-04-SUMMARY.md` capturing:
- 3 OPEN-Q row flips landed (Q05 + Q06.2 + Q07.2 → `decided`) with the exact resolution-bullet text appended above each `- Status:` line; record before/after counts of `proposed` (decremented by 3) and `decided` (incremented by 3); verification done via awk-between-OPEN-Q-headings extraction per REVIEWS C8
- D-66 vocabulary dedup gate result: PASS (zero project-wide terms defined in 3 platform vocabulary.md files) with the actual `grep -r` invocation reproduced for audit
- T-06-02 cross-tree result: PASS (zero YAML-field-assignment hits for the forbidden third enum value across all 3 platform trees under REVIEWS C3 tightened regex) — confirms PLAT-05 / UAT-6.1 enforcement
- T-06-04 cross-tree result: PASS (zero YAML-field-assignment hits for the removed per-tenant API-host override configuration field in platform-pipefy/ tree under REVIEWS C2 tightened regex) — confirms UAT-4.1 enforcement
- Final phase6-structure-check.sh result: exit 0, all 17 assertions A1..A17 PASS; reproduce the full PASS-line output as evidence
- ROADMAP Phase 6 success criteria 1..5 → 3 platform plans + 1 synthesis plan mapping; declare Phase 6 reviewer-ready awaiting human approval gate
- Milestone v2.1 closure note: Phase 6 closes milestone v2.1 per CHANGELIST.md / ROADMAP — next milestone (v2.2 Stage 1 Kickoff + Stage 4 Fnspec split per CHANGE-01) begins after Phase 6 approval
</output>
</content>
</invoke>