---
phase: 06-internalise-platform-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh
  - dydx-delivery/skills/platform-pipefy/SKILL.md
  - dydx-delivery/skills/platform-pipefy/references/api-contract.md
  - dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md
  - dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md
  - dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md
  - dydx-delivery/skills/platform-pipefy/references/vocabulary.md
  - dydx-delivery/skills/platform-wrike/.gitkeep
  - dydx-delivery/skills/platform-wrike/references/.gitkeep
  - dydx-delivery/skills/platform-ziflow/.gitkeep
  - dydx-delivery/skills/platform-ziflow/references/.gitkeep
autonomous: true
requirements: [PLAT-01, PLAT-04, PLAT-05, PLAT-06]
tags: [platform-skills, pipefy, scaffold, phase6-structure-check, paginate_all, q06-2]

must_haves:
  truths:
    - "Per D-63, plan 06-01 ships the full platform-pipefy/ skill tree end-to-end (SKILL.md + 5 references/) in a single atomic unit; reviewers grade Pipefy as a whole."
    - "Per D-64 and CONTEXT §code_context, phase6-structure-check.sh exists at .planning/phases/06-internalise-platform-skills/scripts/ with assertions A1..A17 from VALIDATION.md, mirroring the Phase 5 structure-check shape, exits 0 ONLY when the full phase is complete (intermediate FAIL is expected mid-phase)."
    - "Per UAT-4.1 (Q24 verified 2026-05-10), api-contract.md documents api.pipefy.com/graphql as canonical-only for ALL tenants; web_host and org_id are the per-tenant variants; api_host field-name reference is absent (Threat T-06-04 mitigation)."
    - "Per UAT-4.1 bonus, api-contract.md carries the HTML-on-auth-failure detection subsection (Content-Type: text/html, Keycloak login redirect, NOT JSON 401)."
    - "Per MOD-4 / D-64, api-contract.md documents the paginate_all helper using the 7-part contract structure (signature + behaviour + retry budget + failure modes + return shape + pseudocode + worked example)."
    - "Per UAT-6.1 and PLAT-05, SKILL.md frontmatter carries native_ai_path: paste; the literal token 'native_ai_path: api' appears nowhere in the platform-pipefy/ tree (Threat T-06-02 mitigation)."
    - "Per MOD-7 / D-68, SKILL.md frontmatter carries tier_claims_last_verified: with an ISO date (default 2026-05-09 unless re-verification bumps it forward); date is per-platform distinct."
    - "Per D-67, OPEN-Q06.2 (Pipefy throttle calibration) resolves inline as 13 req/sec per token (80% of 16.67 ceiling) landing in api-contract.md § Rate limit + throttle; the OPEN-QUESTIONS.md row flip itself lives in synthesis plan 06-04."
  artifacts:
    - path: ".planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh"
      provides: "Phase 6 structural verifier (Wave 0 scaffold; assertions A1..A17 from VALIDATION.md)"
      contains: "PASS:"
    - path: "dydx-delivery/skills/platform-pipefy/SKILL.md"
      provides: "Pipefy platform reference skill (PLAT-01) with D-59 hard-rules pointer + D-68 re-verification trigger + locked frontmatter (PLAT-04, PLAT-05)"
      contains: "tier_claims_last_verified"
    - path: "dydx-delivery/skills/platform-pipefy/references/api-contract.md"
      provides: "Pipefy GraphQL API contract — canonical-only api.pipefy.com/graphql endpoint (UAT-4.1), HTML-on-auth-failure detection, paginate_all helper (MOD-4), 13 req/sec throttle (Q06.2 resolution)"
      contains: "api.pipefy.com/graphql"
    - path: "dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md"
      provides: "Pipefy AI Agents 2.0 capability matrix (KB + Skills + MCP + IDP + Web Search + BYO-LLM) per DESIGN-14"
      contains: "Pipefy AI Agents"
    - path: "dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md"
      provides: "Pipefy paste-only ingestion contract (UAT-6.1) — Behaviors instructions + KB upload list"
      contains: "Behaviors"
    - path: "dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md"
      provides: "Pipefy per-client shape variant taxonomy seeded with Vodacom custom-subdomain (D-65)"
      contains: "Vodacom"
    - path: "dydx-delivery/skills/platform-pipefy/references/vocabulary.md"
      provides: "Pipefy-specific terms only (D-66); pointer to dydx-delivery/references/glossary.md"
      contains: "pipe"
  key_links:
    - from: "dydx-delivery/skills/platform-pipefy/SKILL.md"
      to: "dydx-delivery/references/safety-rules.md"
      via: "D-59 uniform hard-rules pointer (verbatim block)"
      pattern: "dydx-delivery/references/safety-rules\\.md"
    - from: "dydx-delivery/skills/platform-pipefy/references/api-contract.md"
      to: "dydx-delivery/references/connector-matrix.md:72"
      via: "Q06.2 throttle derivation cites Phase 5 Q06.1 baseline (13 req/sec = 80% of 16.67 ceiling)"
      pattern: "connector-matrix\\.md"
    - from: "dydx-delivery/skills/platform-pipefy/references/vocabulary.md"
      to: "dydx-delivery/references/glossary.md"
      via: "D-66 pointer to project-wide glossary"
      pattern: "glossary\\.md"
---

<objective>
Wave 1 of Phase 6: ship the complete `skills/platform-pipefy/` tree end-to-end (SKILL.md + all 5 references/ files) in one atomic plan per D-63 per-platform slicing. Plan also lands the Wave 0 scaffold work — `phase6-structure-check.sh` with assertions A1..A17 from VALIDATION.md, plus empty `platform-wrike/` and `platform-ziflow/` directories with `.gitkeep` placeholders so 06-02 and 06-03 can run in parallel against existing parent dirs.

Purpose: Pipefy is the canonical-endpoint-revised platform (UAT-4.1 / Q24 — `api.pipefy.com/graphql` for ALL tenants) and the helper-heaviest platform (paginate_all + HTML-on-auth-failure detection). Landing it first as a complete unit gives 06-02 / 06-03 a verified pattern to mirror; landing the scaffold inside this plan (rather than as a separate Wave 0 plan) avoids one wave of serial dependency without sacrificing parallel execution of Wrike + Ziflow.

Output: 1 verifier script + 1 SKILL.md + 5 references/ files for Pipefy + 2 placeholder dirs for Wrike/Ziflow, all committed atomically per task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-internalise-platform-skills/06-CONTEXT.md
@.planning/phases/06-internalise-platform-skills/06-RESEARCH.md
@.planning/phases/06-internalise-platform-skills/06-PATTERNS.md
@.planning/phases/06-internalise-platform-skills/06-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/DESIGN.md
@.planning/OPEN-QUESTIONS.md
@dydx-delivery/references/safety-rules.md
@dydx-delivery/references/glossary.md
@dydx-delivery/references/connector-matrix.md
@dydx-delivery/skills/execute-tests/SKILL.md
@dydx-delivery/skills/generate-build-prompt/SKILL.md
@.planning/phases/05-foundations/scripts/phase5-structure-check.sh

<interfaces>
<!-- Canonical artefacts and patterns the executor consumes. All values reproduced here so the executor needs no codebase exploration. -->

<!-- Phase 5 D-59 uniform hard-rules pointer (analog: dydx-delivery/skills/execute-tests/SKILL.md:20-22). COPY VERBATIM into platform-pipefy/SKILL.md: -->
```markdown
## Hard rules

> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

<!-- Pipefy SKILL.md frontmatter (verbatim from RESEARCH.md:678-687): -->
```yaml
---
name: platform-pipefy
description: Provide Pipefy GraphQL API contract + 2026 AI Agents capability matrix + per-client shape gotchas + vocabulary for any stage skill that targets a Pipefy tenant. Use when artefact frontmatter carries `platform: pipefy`. Documents the `paginate_all` helper contract for cursor pagination; the HTML-on-auth-failure detection rule (Pipefy returns Keycloak login HTML, not JSON 401); the canonical-only API endpoint (`api.pipefy.com/graphql` for ALL tenants); paste-only native-AI ingestion path per UAT-6.1. Does NOT execute API calls — pure reference content for skills + Stage 8 test bot.
frontmatter_version: 2
platform: pipefy
tier_claims_last_verified: 2026-05-09
native_ai_path: paste
---
```

<!-- D-68 Re-verification trigger block (verbatim from RESEARCH.md:717-735; platform-substituted for Pipefy): -->
```markdown
## Re-verification trigger

Re-verify `references/native-ai-inventory.md` against current Pipefy AI Agents
documentation BEFORE any v2.x phase that consumes the capability matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — Behaviors / Copilot
  workflow narratives / ReviewAI Checklist criteria)

Update `tier_claims_last_verified:` on this SKILL.md frontmatter after each
re-verification, citing the source doc URL + date in commit message.

Doc URL anchors:
- Pipefy: https://www.pipefy.com/ + Pipefy Help Center
```

<!-- Pipefy paginate_all helper contract — verbatim 7-part lift from RESEARCH.md:313-351 (cited into api-contract.md): -->
**Signature:** `paginate_all(query: str, cursor_field: str = "endCursor", page_size: int = 50) -> list[dict]`

**Behaviour:** Iterates Pipefy GraphQL cursor pagination across multi-page result sets until `pageInfo.hasNextPage == false`. Returns the accumulated flat list of records across all pages. Prevents the silent-truncation bug (MOD-4) that occurs when consumer skills assume a single-page response covers all records.

**Retry / poll budget:** Per-page request retries on HTTP 429 with exponential backoff. Throttle ceiling: **13 req/sec per token (80% of 16.67 ceiling = 500 req / 30s per token; resolves Q06.2 per Phase 5 connector-matrix.md `:72`)**. Max retries per page = 3; backoff curve `[1s, 2s, 4s]`. Total budget per page = ~7s before raising.

**Failure modes:**
- HTML response with `Content-Type: text/html` (Keycloak login HTML, UAT-4.1) → raise `PipefyAuthFailed` (NOT a JSON 401 — this is the Pipefy-specific auth-failure shape).
- HTTP 429 after max retries → raise `PipefyRateLimitExhausted`.
- Cursor field missing in response → raise `PipefyPaginationContractError` (signals API contract drift).
- Empty first page → return `[]` (not an error).

**Return shape:** `list[dict]` — flat accumulation of `result.edges[].node` payloads across all pages.

**Pseudocode** (verbatim from RESEARCH.md:330-347):
```python
def paginate_all(query: str, cursor_field: str = "endCursor", page_size: int = 50) -> list[dict]:
    results = []
    cursor = None
    while True:
        page = _execute_with_throttle(query, cursor=cursor, page_size=page_size,
                                       throttle_req_per_sec=13)  # 80% of 16.67 ceiling
        # Auth-failure detection (UAT-4.1 — non-standard Pipefy shape)
        if page.headers.get("Content-Type", "").startswith("text/html"):
            raise PipefyAuthFailed("Keycloak login HTML received; token invalid or expired")
        data = page.json()
        results.extend(node for node in data["result"]["edges"])
        page_info = data["result"]["pageInfo"]
        if not page_info["hasNextPage"]:
            break
        cursor = page_info[cursor_field]
    return results
```

**Worked example:** Querying all cards in a pipe with 247 cards (5 pages × 50 + final page × 47) → returns flat `list[dict]` of length 247. Single per-page HTTP 429 → 1s backoff → retry → succeeds; surface result identical to clean-path.

<!-- Auth-shape secret-handling note (lift near-verbatim from connector-matrix.md:31): -->
```markdown
> **Auth header & secret-handling note:** All helper contracts below document the auth header NAME + value SHAPE only (e.g., `Authorization: Bearer <token>`). NEVER paste real tokens, API keys, OAuth refresh tokens, or session cookies into this file. Tokens live in per-tenant `client_state.yaml` (gitignored) or env vars — never in the canonical reference set.
```

<!-- Pipefy native-AI capability matrix (verbatim lift from RESEARCH.md:741-748 / DESIGN-14): -->
| Capability | Available? | Surface | Confidence |
|---|---|---|---|
| Knowledge base | yes | Pipefy AI Agents → KB | HIGH |
| Skills | yes | AI Agents → Skills | HIGH |
| MCP integration | **flag for re-verify** | AI Agents → MCP | **DESIGN-14 = HIGH; current research = MEDIUM** — one secondary 2026 source contradicts ("Pipefy is not designed to ingest agents built on third-party frameworks through standard protocols like MCP"). PLAT-01 execution MUST re-verify against Pipefy.com / Pipefy AI Agents 2.0 launch docs (`globenewswire.com/news-release/2025/11/12/3186348`). |
| IDP (Intelligent Document Processing) | yes | AI Agents → IDP (Agents 2.0 native) | HIGH — confirmed via 2025-11 launch announcement |
| Web Search | yes | AI Agents → Web Search | HIGH |
| BYO-LLM | yes | AI Agents config (multi-LLM routing per workflow step) | HIGH — confirmed via 2025-11 launch announcement |
| KB content-upload via API | **CLOSED (UAT-6.1)** | n/a | UAT-6.1 — OPEN-Q01 closed; no API ingestion path through v2.6 |

<!-- D-65 client-shape-gotchas seed (Pipefy = Vodacom custom-subdomain verbatim from DESIGN-14 REVISED at .planning/DESIGN.md:408-457): -->
```markdown
### Vodacom (custom-subdomain — verified per DESIGN-14 REVISED, 2026-05-10)

- **web_host:** `vodacom.pipefy.com/{org_id}` (custom-subdomain pattern)
- **API host:** `api.pipefy.com/graphql` (canonical-only for ALL tenants per UAT-4.1 / Q24)
- **org_id:** required path segment for web URLs; NOT required in GraphQL API calls
- **Source artefact:** DESIGN-14 REVISED at `.planning/DESIGN.md:408-457`
```

<!-- Frontmatter scheme baseline that platform SKILL.md MUST conform to (cite: dydx-delivery/references/frontmatter-scheme.md): -->
- `frontmatter_version: 2` REQUIRED (FOUND-03 contract)
- `platform:` closed-enum value `pipefy` (Phase 6 NEW field)
- `native_ai_path:` closed-enum `paste | none` (UAT-6.1 — `api` FORBIDDEN per PLAT-05 grep gate)
- `tier_claims_last_verified:` ISO date (D-68 — per-platform distinct)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer ↔ canonical references | Phase 6 doc-only; threats are stale claims, banned-token leakage, removed-field regression — not runtime input |
| platform-pipefy/ tree ↔ downstream consumer skills | Stage 4a/7b/8/10 read SKILL.md frontmatter + references/* at session start; malformed contracts propagate silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `tier_claims_last_verified:` ISO date in SKILL.md | mitigate | A5 assertion enforces ISO-date format; manual re-verify documented per VALIDATION § Manual-Only Verifications row 1 |
| T-06-02 | Information disclosure (banned value leak) | `native_ai_path: api` reappearing via copy-paste from DESIGN-14 historical | mitigate | A7 grep gate exits 1 on any hit across platform-pipefy/; this plan's grep also runs explicitly during Task 6 |
| T-06-04 | Tampering | `api_host` field-name reference reappearing (UAT-4.1 removed it) | mitigate | A11 second-half grep gate — zero hits required for the literal token `api_host` in platform-pipefy/references/api-contract.md |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 scaffold — phase6-structure-check.sh + platform-wrike/ + platform-ziflow/ placeholder dirs</name>
  <files>.planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh, dydx-delivery/skills/platform-wrike/.gitkeep, dydx-delivery/skills/platform-wrike/references/.gitkeep, dydx-delivery/skills/platform-ziflow/.gitkeep, dydx-delivery/skills/platform-ziflow/references/.gitkeep</files>
  <read_first>
    - .planning/phases/05-foundations/scripts/phase5-structure-check.sh:1-60 (header / fail+pass helpers / arg parser / G-2 exclude convention) — mirror this exact shape
    - .planning/phases/06-internalise-platform-skills/06-VALIDATION.md:55-73 (master assertion list A1..A17)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md:194 (script path)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:357-372 (Phase 6 assertion set summary)
  </read_first>
  <action>
Create `.planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` as an executable bash script (`chmod +x` after Write). Mirror Phase 5 phase5-structure-check.sh shape verbatim:

1. Shebang `#!/usr/bin/env bash` + `set -euo pipefail`
2. Header comment block describing comment-vs-code policy (use `grep -qF` for sentinel-token literal matches case-sensitive; `grep -cE` / `grep -qE` for regex; NO `-i` flag to preserve Git-Bash-for-Windows portability)
3. G-2 convention comment: every absence-check (`! grep ...`) excludes `scripts/`, `*.sh`, `*~` to prevent script's own assertion text triggering false positives
4. Two helpers — `fail() { echo "FAIL: $1" >&2; exit 1; }` and `pass() { echo "PASS: $1"; }`
5. Arg parser supporting `--quick`, `--section <pipefy|wrike|ziflow>`, `--help|-h` (mirror Phase 5 arg parser)
6. Assertion functions per A1..A17 from VALIDATION.md:55-73 — emit each as `pass "AN: <one-line summary>"` on success, `fail "AN: <reason>"` on failure
7. Section gates — `if [ -z "$SECTION" ] || [ "$SECTION" = "pipefy" ]; then ...` wrappers around platform-specific assertions; `[ -z "$SECTION" ]` wraps cross-cutting assertions (A4, A6, A7, A16)
8. Final line: `echo "ALL ASSERTIONS PASSED"` and `exit 0`

Concrete assertion implementations (per VALIDATION.md A1..A17):
- A1: `[ -f dydx-delivery/skills/platform-pipefy/SKILL.md ] || fail "A1: ..."`
- A2: `[ -f dydx-delivery/skills/platform-wrike/SKILL.md ] || fail "A2: ..."`
- A3: `[ -f dydx-delivery/skills/platform-ziflow/SKILL.md ] || fail "A3: ..."`
- A4: For each platform in pipefy wrike ziflow, for each ref in api-contract.md native-ai-inventory.md knowledge-ingestion.md client-shape-gotchas.md vocabulary.md → `[ -f dydx-delivery/skills/platform-$platform/references/$ref ] || fail "A4: missing $platform/$ref"`
- A5: For each platform SKILL.md → `grep -qE '^tier_claims_last_verified: [0-9]{4}-[0-9]{2}-[0-9]{2}$' <file> || fail "A5: ..."`
- A6: For each platform SKILL.md → `grep -qE '^native_ai_path: (paste|none)$' <file> || fail "A6: ..."`
- A7: `if grep -rn 'native_ai_path: api' dydx-delivery/skills/platform-*/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~' >/dev/null 2>&1; then fail "A7: native_ai_path: api leaked"; else pass "A7: ..."; fi`
- A8: `grep -qF 'api.pipefy.com/graphql' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF '2026-05-10' dydx-delivery/skills/platform-pipefy/references/api-contract.md || fail "A8: ..."`
- A9: `grep -qF 'Content-Type: text/html' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'Keycloak' dydx-delivery/skills/platform-pipefy/references/api-contract.md || fail "A9: ..."`
- A10: `grep -qE '^## paginate_all' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'Signature:' dydx-delivery/skills/platform-pipefy/references/api-contract.md || fail "A10: ..."`
- A11: `grep -qF 'web_host' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'org_id' dydx-delivery/skills/platform-pipefy/references/api-contract.md && ! grep -qF 'api_host' dydx-delivery/skills/platform-pipefy/references/api-contract.md || fail "A11: ..."`
- A12: `grep -qF 'OAuth token response' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF 'NEVER hardcode `www.wrike.com`' dydx-delivery/skills/platform-wrike/references/api-contract.md || fail "A12: ..."`
- A13: `grep -qF 'max_wait_s=30' dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qF 'interval_s=2' dydx-delivery/skills/platform-ziflow/references/api-contract.md && grep -qF 'webhook' dydx-delivery/skills/platform-ziflow/references/api-contract.md || fail "A13: ..."`
- A14: `grep -qF '13 req/sec' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF '16.67' dydx-delivery/skills/platform-pipefy/references/api-contract.md || fail "A14: ..."`
- A15: `grep -qF '320 req/min' dydx-delivery/skills/platform-wrike/references/api-contract.md && grep -qF '400' dydx-delivery/skills/platform-wrike/references/api-contract.md || fail "A15: ..."`
- A16: For each Q in Q05 Q06.2 Q07.2 → parse the OPEN-QUESTIONS.md row block (between `**OPEN-Q$Q**` and the next `**OPEN-Q`) and `grep -qE '^- Status: decided$' || fail "A16: $Q not decided"` — use awk `/^\*\*OPEN-Q05\*\*/,/^\*\*OPEN-Q06\.1\*\*/` style range OR `sed -n` block extraction; mirror Phase 5 stateful section_between awk helper if convenient
- A17: For each platform → `grep -qE '^## Capability matrix' dydx-delivery/skills/platform-$platform/references/native-ai-inventory.md || fail "A17: $platform capability matrix missing"`

Also create the empty placeholder dirs for parallel-wave plan execution (06-02 Wrike + 06-03 Ziflow):
- `dydx-delivery/skills/platform-wrike/.gitkeep` (empty file)
- `dydx-delivery/skills/platform-wrike/references/.gitkeep` (empty file)
- `dydx-delivery/skills/platform-ziflow/.gitkeep` (empty file)
- `dydx-delivery/skills/platform-ziflow/references/.gitkeep` (empty file)

After Write, run `chmod +x .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh`. Expected exit at this point: 1 with `FAIL: A1: ...` (Pipefy SKILL.md not yet created) — that's the correct mid-phase state proving the script is live (Phase 1 Plan 01 + Phase 5 Plan 01 precedent).
  </action>
  <verify>
    <automated>test -x .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh && bash -n .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh && test -f dydx-delivery/skills/platform-wrike/.gitkeep && test -f dydx-delivery/skills/platform-ziflow/references/.gitkeep</automated>
  </verify>
  <done>phase6-structure-check.sh exists and is executable; `bash -n` reports zero syntax errors; all 4 `.gitkeep` placeholder files exist; running the script exits 1 with the expected `FAIL: A1: dydx-delivery/skills/platform-pipefy/SKILL.md missing` (proves the script is live end-to-end; satisfies Wave 0 scaffold contract per orchestrator deep-work rules).</done>
  <acceptance_criteria>
    - `test -x .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` (executable bit set)
    - `bash -n .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` exits 0 (valid bash syntax)
    - All 17 assertion implementations A1..A17 from VALIDATION.md present in the script as `pass`/`fail` callsites — grep `grep -cE 'pass "A[0-9]+:|fail "A[0-9]+:' phase6-structure-check.sh` returns ≥ 17 lines (Wave 0 scaffold artifact A0-baseline)
    - `dydx-delivery/skills/platform-wrike/.gitkeep` AND `dydx-delivery/skills/platform-wrike/references/.gitkeep` AND `dydx-delivery/skills/platform-ziflow/.gitkeep` AND `dydx-delivery/skills/platform-ziflow/references/.gitkeep` all exist as zero-byte files (`test -f` for each + `[ ! -s file ]` for zero-byte)
    - Script invoked with no args exits 1 with the FIRST failure being `FAIL: A1: ...` referring to platform-pipefy/SKILL.md (proves script is live and assertions wire to real filesystem)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: platform-pipefy/SKILL.md — frontmatter + intro + hard-rules pointer (D-59) + re-verification trigger (D-68) + what-this-provides</name>
  <files>dydx-delivery/skills/platform-pipefy/SKILL.md</files>
  <read_first>
    - dydx-delivery/skills/execute-tests/SKILL.md:1-22 (analog SKILL.md frontmatter + intro + D-59 pointer pattern at lines 20-22)
    - dydx-delivery/skills/generate-build-prompt/SKILL.md:1-8 (secondary analog — concise SKILL pattern)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:678-687 (Pipefy frontmatter verbatim template)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:717-735 (D-68 re-verification trigger boilerplate)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:96-130 (`## What this skill provides` template + re-verification trigger pattern)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md spec_lock UAT-4.1 / UAT-6.1 / D-68 (frontmatter contracts)
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-pipefy/SKILL.md` with the following sections in order. Per the orchestrator's deep-work rule, all values are concrete here — executor does not need to re-derive from research.

**1. YAML frontmatter (verbatim from interfaces block in `<context>`):**
```yaml
---
name: platform-pipefy
description: Provide Pipefy GraphQL API contract + 2026 AI Agents capability matrix + per-client shape gotchas + vocabulary for any stage skill that targets a Pipefy tenant. Use when artefact frontmatter carries `platform: pipefy`. Documents the `paginate_all` helper contract for cursor pagination; the HTML-on-auth-failure detection rule (Pipefy returns Keycloak login HTML, not JSON 401); the canonical-only API endpoint (`api.pipefy.com/graphql` for ALL tenants); paste-only native-AI ingestion path per UAT-6.1. Does NOT execute API calls — pure reference content for skills + Stage 8 test bot.
frontmatter_version: 2
platform: pipefy
tier_claims_last_verified: 2026-05-09
native_ai_path: paste
---
```

**2. Title + 3-sentence intro** (lift verbatim from RESEARCH.md:996-1000, adapted):
```markdown
# platform-pipefy

Pipefy platform reference for the dydx-delivery plugin. Any stage skill operating
against a Pipefy tenant loads this skill alongside the stage-specific skill;
artefact frontmatter `platform: pipefy` is the routing key.
```

**3. `## Inputs` + `## Output` section** (adapt execute-tests/SKILL.md:10-18 — platform skill produces NO artefact):
```markdown
## Inputs

- Artefact frontmatter carrying `platform: pipefy` and a populated `sandbox:` block (loaded by the calling stage skill)
- Phase 5 canonical references at `dydx-delivery/references/` (consumed transitively via this skill's pointers)

## Output

This skill produces NO artefacts. It is a reference skill — the calling stage skill produces the versioned output.
```

**4. `## Hard rules` section (D-59 — COPY THE BLOCK VERBATIM):**
```markdown
## Hard rules

> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See `dydx-delivery/references/safety-rules.md` for the canonical ruleset.
```

**5. `## What this skill provides` section (enumerate the 5 references/ files with one-line summaries each — lift from PATTERNS.md:96-109):**
```markdown
## What this skill provides

- `references/api-contract.md` — GraphQL endpoint (canonical-only `api.pipefy.com/graphql` for ALL tenants per UAT-4.1 / Q24), auth header shape, rate limit (500 req/30s ceiling; 13 req/sec throttle per Q06.2), HTML-on-auth-failure detection (UAT-4.1), `paginate_all` helper contract (MOD-4).
- `references/native-ai-inventory.md` — 2026 Pipefy AI Agents 2.0 capability matrix (KB / Skills / MCP / IDP / Web Search / BYO-LLM) per DESIGN-14.
- `references/knowledge-ingestion.md` — Paste-only Stage 10 path (UAT-6.1): Pipefy Behaviors instructions + KB upload list.
- `references/client-shape-gotchas.md` — Per-client pipe shape variations (Vodacom custom-subdomain verified seed; append-only as new clients onboard).
- `references/vocabulary.md` — Pipefy-specific terms (pipe / phase / card / connection / org_id / web_host / pipe_id / phase_id / card_id / Behaviors / Pipefy AI Agents).
```

**6. `## Re-verification trigger` section (D-68 — COPY VERBATIM from interfaces block, Pipefy-substituted):**
```markdown
## Re-verification trigger

Re-verify `references/native-ai-inventory.md` against current Pipefy AI Agents
documentation BEFORE any v2.x phase that consumes the capability matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — Behaviors / Copilot
  workflow narratives / ReviewAI Checklist criteria)

Update `tier_claims_last_verified:` on this SKILL.md frontmatter after each
re-verification, citing the source doc URL + date in commit message.

Doc URL anchors:
- Pipefy: https://www.pipefy.com/ + Pipefy Help Center
```

After Write, verify the file passes A1 + A5 + A6 of the structure-check. The script as a whole still exits 1 (A4 / A8 / A9 / A10 / A11 / A14 / A17 all gate on references/*.md files not yet created — those land in Tasks 3-7).
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-pipefy/SKILL.md && grep -qE '^tier_claims_last_verified: 2026-05-09$' dydx-delivery/skills/platform-pipefy/SKILL.md && grep -qE '^native_ai_path: paste$' dydx-delivery/skills/platform-pipefy/SKILL.md && grep -qF 'dydx-delivery/references/safety-rules.md' dydx-delivery/skills/platform-pipefy/SKILL.md && grep -qE '^## Re-verification trigger$' dydx-delivery/skills/platform-pipefy/SKILL.md && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section pipefy 2>&1 | grep -q '^PASS: A1' && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh 2>&1 | grep -q '^PASS: A5'</automated>
  </verify>
  <done>SKILL.md exists with valid YAML frontmatter (`frontmatter_version: 2`, `platform: pipefy`, `tier_claims_last_verified: 2026-05-09`, `native_ai_path: paste`); D-59 hard-rules pointer block present verbatim; D-68 re-verification trigger section present; A1 / A5 / A6 assertions PASS via structure-check.</done>
  <acceptance_criteria>
    - Satisfies A1: `[ -f dydx-delivery/skills/platform-pipefy/SKILL.md ]` exits 0
    - Satisfies A5: `grep -qE '^tier_claims_last_verified: [0-9]{4}-[0-9]{2}-[0-9]{2}$' dydx-delivery/skills/platform-pipefy/SKILL.md` exits 0
    - Satisfies A6: `grep -qE '^native_ai_path: (paste|none)$' dydx-delivery/skills/platform-pipefy/SKILL.md` exits 0
    - Satisfies A7 partial (T-06-02 mitigation): `grep -q 'native_ai_path: api' dydx-delivery/skills/platform-pipefy/SKILL.md` exits 1 (zero hits in this file)
    - D-59 pointer block grep: `grep -qF '> **Hard rules:** Sandbox-only operations. Read-write only against named sandbox tenants. Refuses destructive actions. See '` against `dydx-delivery/skills/platform-pipefy/SKILL.md` exits 0
    - D-68 re-verification trigger H2 present: `grep -qE '^## Re-verification trigger$' dydx-delivery/skills/platform-pipefy/SKILL.md` exits 0
    - YAML frontmatter parses without error (run `python -c "import yaml,sys; yaml.safe_load(open('dydx-delivery/skills/platform-pipefy/SKILL.md').read().split('---')[1])"` exits 0; or equivalent `awk` between `---` markers)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: platform-pipefy/references/api-contract.md — endpoints + UAT-4.1 canonical-only + HTML-on-auth-failure + paginate_all (MOD-4) + Q06.2 throttle</name>
  <files>dydx-delivery/skills/platform-pipefy/references/api-contract.md</files>
  <read_first>
    - dydx-delivery/references/connector-matrix.md:1-31 (analog scope-lock callout + auth-shape secret-handling note pattern — lines 1-10 + 31)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:313-351 (paginate_all 7-part helper contract verbatim)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:454-494 (api-contract.md outline + per-platform section content)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:642-656 (Q06.2 throttle resolution)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:142-217 (api-contract.md primary analog + helper-contract sub-structure + MCP-availability parked-reference + rate-limit content per platform)
    - .planning/DESIGN.md:408-457 (DESIGN-14 REVISED canonical-only endpoint + Q24 verified 2026-05-10 + HTML-on-auth-failure gotcha)
    - dydx-delivery/references/connector-matrix.md:72 (Q06.1 Pipefy rate-limit baseline that Q06.2 derives from)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md D-64 (helper code shape: doc-only + 7-part structure)
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-pipefy/references/api-contract.md` with the following sections in order. Per D-64 helper is doc-only; no executable code ships.

**1. Title + scope-lock preamble** (lift connector-matrix.md:1-10 pattern, adapt for Pipefy):
```markdown
# Pipefy — API contract

> Canonical SoT for Pipefy API contracts per DESIGN-14 REVISED (`.planning/DESIGN.md:408-457`). Documents the canonical-only GraphQL endpoint, auth shape, rate-limit + throttle, auth-failure detection, helper contracts, multi-tenant auth-concurrency, and MCP availability (parked reference).
>
> **Scope locks (UAT 2026-05-10):**
> - **UAT-4.1 / Q24 verified 2026-05-10:** API endpoint canonical-only — `api.pipefy.com/graphql` for ALL tenants. `web_host` and `org_id` vary per tenant; `api_host` field-name reference is REMOVED (DNS verified: `api.<subdomain>.pipefy.com` does not resolve for custom-subdomain tenants).
> - **UAT-3.5:** Pipedream Pipefy MCP is parked reference only — not adopted through v2.6.
> - **UAT-6.1:** Native-AI ingestion APIs OUT-OF-SCOPE — `native_ai_path: paste | none` only. See `references/knowledge-ingestion.md`.
> - **UAT-4.2:** Auth-concurrency = `exclusive` — cannot auth to two Pipefy tenants simultaneously.
>
> **Cross-refs:**
> - Sandbox enforcement: `dydx-delivery/references/safety-rules.md` § "1. Sandbox enforcement"
> - Connector probe: `dydx-delivery/references/connector-matrix.md` (Phase 5 canonical)
> - Glossary: `dydx-delivery/skills/platform-pipefy/references/vocabulary.md` (Pipefy-specific terms)
```

**2. Auth-shape secret-handling note** (lift near-verbatim from connector-matrix.md:31):
```markdown
> **Auth header & secret-handling note:** All helper contracts below document the auth header NAME + value SHAPE only (e.g., `Authorization: Bearer <token>`). NEVER paste real tokens, API keys, OAuth refresh tokens, or session cookies into this file. Tokens live in per-tenant `client_state.yaml` (gitignored) or env vars — never in the canonical reference set.
```

**3. `## Endpoint(s) + auth shape`**:
```markdown
## Endpoint(s) + auth shape

- **Base URL (canonical-only for ALL tenants per UAT-4.1 / Q24 verified 2026-05-10):** `https://api.pipefy.com/graphql`
- **Auth header:** `Authorization: Bearer <token>` (header NAME only — see secret-handling note above)
- **Per-tenant variants:** `web_host` (e.g., `app.pipefy.com` default OR `<subdomain>.pipefy.com` custom-subdomain) and `org_id` (path segment in web URLs; NOT required in GraphQL calls). These two fields are per-tenant; the API host is NOT.
- **Removed (UAT-4.1):** `api_host` field-name reference is NOT used anywhere — DNS verified `api.<subdomain>.pipefy.com` does not resolve for custom-subdomain tenants. Skills MUST NOT introduce `api_host` configuration.
- Sandbox tenant only — see `dydx-delivery/references/safety-rules.md` Rule 1.
```

**4. `## Rate limit + throttle` (resolves Q06.2 inline):**
```markdown
## Rate limit + throttle

- **Published ceiling:** 500 req / 30s per token ≈ 16.67 req/sec per token (Pipefy Community + Pipefy Help Center, re-confirmed 2026-05-11; Phase 5 Q06.1 baseline at `dydx-delivery/references/connector-matrix.md:72`).
- **Helper throttle (Q06.2 resolution, PLAT-06):** **13 req/sec per token (80% buffer, DESIGN-22 carried throttle pattern at `.planning/DESIGN.md:795`)**.
- **429 handling:** Max 3 retries per call with exponential backoff `[1s, 2s, 4s]`; total budget per page = ~7s before raising `PipefyRateLimitExhausted`.
- **Pipe Reports Export sub-limit:** 25 req / 24h per pipe — helpers consuming `paginate_all` against pipe-report endpoints carry a per-day budget tracker. Out-of-budget → return cached / partial result with warning, NOT raise.
```

**5. `## Auth-failure detection` (UAT-4.1 bonus — Threat T-06-03 / Pipefy non-standard auth-failure shape):**
```markdown
## Auth-failure detection

Pipefy GraphQL endpoint returns a **Keycloak login HTML page** with `Content-Type: text/html` on auth failure, NOT a JSON `401 Unauthorized`. This is the Pipefy-specific auth-failure shape (UAT-4.1 bonus finding).

**Detection rule (MUST appear in every Pipefy helper):**
- Check `Content-Type` header BEFORE parsing JSON.
- If `Content-Type` starts with `text/html` → raise `PipefyAuthFailed("Keycloak login HTML received; token invalid or expired")`.
- Do NOT use `try / except JSONDecodeError` — it masks the real signal.

This rule is enforced inside `paginate_all` pseudocode below; downstream helpers (Stage 8 test bot tier-1 assertions) MUST replicate it.
```

**6. `## paginate_all` helper contract (D-64 7-part structure — verbatim from RESEARCH.md:313-351):**

Embed exactly the helper contract block reproduced in the `<context>` `<interfaces>` block of this PLAN: signature, behaviour, retry/poll budget, failure modes, return shape, pseudocode (the 16-line Python block), worked example. Use H2 `## paginate_all`. Add a single-line source citation at the end: `**Source:** DESIGN-14 helper spec at \`.planning/DESIGN.md:440\` + UAT-4.1 HTML-auth gotcha at \`.planning/DESIGN.md:447\` + Phase 5 connector-matrix.md \`:72\` throttle ceiling.`

**7. `## Multi-tenant auth concurrency`:**
```markdown
## Multi-tenant auth concurrency

- **Class:** `exclusive` (locked per UAT-4.2). Cannot auth to two Pipefy tenants simultaneously.
- **Implication for Stage 8 test bot:** Tier-1 assertions serialize per-tenant ops + emit `auth_switch_required` retry signal at tenant-boundary crossings (per DESIGN-28).
- **`client_state.yaml` field:** `pipefy.auth_concurrency_class: exclusive` (per DESIGN-29).
```

**8. `## MCP availability (UAT-3.5 — PARKED REFERENCE)`:**
```markdown
## MCP availability (UAT-3.5 — PARKED REFERENCE)

Pipedream Pipefy MCP is available (`mcp.pipedream.net/v2`) but **not adopted through v2.6** per UAT-3.5. Native API (GraphQL) is the canonical Pipefy access path for v2.1–v2.6. MCP adoption re-evaluated as a post-v2.6 milestone gated on first-real-client-engagement-practice-run (per `.claude/memory/feedback_platform_skills_api_first.md`).
```

After Write, verify A8 / A9 / A10 / A11 / A14 all PASS via structure-check.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'api.pipefy.com/graphql' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF '2026-05-10' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'Content-Type: text/html' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'Keycloak' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qE '^## paginate_all$' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'Signature:' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'web_host' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF 'org_id' dydx-delivery/skills/platform-pipefy/references/api-contract.md && ! grep -qF 'api_host' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF '13 req/sec' dydx-delivery/skills/platform-pipefy/references/api-contract.md && grep -qF '16.67' dydx-delivery/skills/platform-pipefy/references/api-contract.md</automated>
  </verify>
  <done>api-contract.md exists with canonical endpoint (UAT-4.1); HTML-on-auth-failure detection subsection; paginate_all 7-part contract (MOD-4); Q06.2 throttle resolution inline (13 req/sec / 80% of 16.67); web_host + org_id documented; api_host field-name reference absent; A8 / A9 / A10 / A11 / A14 assertions PASS.</done>
  <acceptance_criteria>
    - Satisfies A8: `grep -qF 'api.pipefy.com/graphql' <file> && grep -qF '2026-05-10' <file>` both exit 0
    - Satisfies A9 (T-06-03 mitigation): `grep -qF 'Content-Type: text/html' <file> && grep -qF 'Keycloak' <file>` both exit 0
    - Satisfies A10 (MOD-4): `grep -qE '^## paginate_all$' <file> && grep -qF 'Signature:' <file>` both exit 0
    - Satisfies A11 (T-06-04 mitigation): `grep -qF 'web_host' <file> && grep -qF 'org_id' <file> && ! grep -qF 'api_host' <file>` all conditions met
    - Satisfies A14 (Q06.2 / PLAT-06): `grep -qF '13 req/sec' <file> && grep -qF '16.67' <file>` both exit 0
    - Pseudocode block contains the exact `throttle_req_per_sec=13` token (literal grep): `grep -qF 'throttle_req_per_sec=13' <file>` exits 0
    - Auth-shape secret-handling note present: `grep -qF 'NEVER paste real tokens' <file>` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: platform-pipefy/references/native-ai-inventory.md — 2026 AI Agents 2.0 capability matrix (DESIGN-14 verbatim lift)</name>
  <files>dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md</files>
  <read_first>
    - .planning/DESIGN.md:425-435 (DESIGN-14 Pipefy AI Agents 2.0 capability matrix — verbatim source)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:741-748 (re-lifted capability matrix with MCP re-verify flag)
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:499-526 (native-ai-inventory.md outline)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:222-256 (native-ai-inventory.md table shape + section outline)
    - dydx-delivery/references/connector-matrix.md:37-51 (analog table shape — Stage × connector grid)
  </read_first>
  <action>
Create `dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md` with three sections per RESEARCH.md:499-526:

**1. Title + frontmatter-sync note (verbatim):**
```markdown
# Pipefy — Native-AI inventory

> `tier_claims_last_verified:` matches SKILL.md frontmatter value (2026-05-09 baseline per D-68; updated to execution date if fresh re-verification happens during this plan).
```

**2. `## Re-verification trigger`** — mirror the SKILL.md `## Re-verification trigger` block but localised to this file's purpose (cite SKILL.md:tier_claims_last_verified as the update target):
```markdown
## Re-verification trigger

Re-verify this capability matrix against current Pipefy AI Agents documentation
(`https://www.pipefy.com/` + Pipefy Help Center + AI Agents 2.0 launch docs at
`globenewswire.com/news-release/2025/11/12/3186348`) BEFORE any v2.x phase that
consumes the matrix:
- Stage 4a delivery routing (consumes confidence ratings)
- Stage 7b implementation prompt (consumes capability surface list)
- Stage 10 native-AI paste bundle (consumes paste targets — Behaviors + KB upload list)

Update `tier_claims_last_verified:` on `dydx-delivery/skills/platform-pipefy/SKILL.md`
frontmatter after each re-verification, citing the source doc URL + date in commit
message.
```

**3. `## Capability matrix (2026-grounded)`** — verbatim lift from interfaces block in this PLAN's `<context>` (the 7-row table):
```markdown
## Capability matrix (2026-grounded)

| Capability | Available? | Surface | Confidence |
|---|---|---|---|
| Knowledge base | yes | Pipefy AI Agents → KB | HIGH |
| Skills | yes | AI Agents → Skills | HIGH |
| MCP integration | **flag for re-verify** | AI Agents → MCP | **DESIGN-14 = HIGH; current research = MEDIUM** — one secondary 2026 source contradicts ("Pipefy is not designed to ingest agents built on third-party frameworks through standard protocols like MCP"). PLAT-01 execution MAY re-verify against Pipefy.com / Pipefy AI Agents 2.0 launch docs (`globenewswire.com/news-release/2025/11/12/3186348`) and bump the date forward. |
| IDP (Intelligent Document Processing) | yes | AI Agents → IDP (Agents 2.0 native) | HIGH — confirmed via 2025-11 launch announcement |
| Web Search | yes | AI Agents → Web Search | HIGH |
| BYO-LLM | yes | AI Agents config (multi-LLM routing per workflow step) | HIGH — confirmed via 2025-11 launch announcement |
| KB content-upload via API | **CLOSED (UAT-6.1)** | n/a | UAT-6.1 — OPEN-Q01 closed; no API ingestion path through v2.6 |
```

**4. `## Paste-only delivery (UAT-6.1)`:**
```markdown
## Paste-only delivery (UAT-6.1)

`native_ai_path: paste | none` ONLY — `api` branch is FORBIDDEN. The Stage 10 paste bundle is the only supported ingestion path through v2.6; humans upload manually via the Pipefy UI. See `references/knowledge-ingestion.md` for the paste-bundle shape.
```

**Optional execution-time re-verification (manual per VALIDATION § Manual-Only Verifications row 3):** If PLAT-01 executor wishes to re-verify the MCP row against current Pipefy AI Agents 2.0 docs before committing, they MAY update both this matrix row AND `dydx-delivery/skills/platform-pipefy/SKILL.md` frontmatter `tier_claims_last_verified:` to the execution date. Default: leave 2026-05-09 baseline as-is.
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md && grep -qE '^## Capability matrix' dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md && grep -qF 'Pipefy AI Agents' dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md && grep -qF 'BYO-LLM' dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md && grep -qF 'UAT-6.1' dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md && ! grep -qF 'native_ai_path: api' dydx-delivery/skills/platform-pipefy/references/native-ai-inventory.md</automated>
  </verify>
  <done>native-ai-inventory.md exists with 7-row 2026 Pipefy AI Agents 2.0 capability matrix; re-verification trigger present; paste-only delivery section present; A17 PASS for pipefy.</done>
  <acceptance_criteria>
    - Satisfies A17 (pipefy partition): `grep -qE '^## Capability matrix' <file>` exits 0 AND grep `grep -cE '^\| (Knowledge base|Skills|MCP integration|IDP|Web Search|BYO-LLM|KB content-upload via API) \|' <file>` returns ≥ 7
    - 2026 vendor docs grounded: `grep -qF 'AI Agents' <file>` exits 0
    - Satisfies A7 partial (T-06-02): `grep -qF 'native_ai_path: api' <file>` exits 1 (zero hits)
    - UAT-6.1 paste-only section present: `grep -qF 'UAT-6.1' <file>` exits 0
    - Re-verification trigger H2 present: `grep -qE '^## Re-verification trigger$' <file>` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: platform-pipefy/references/{knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md} — paste-only path + Vodacom seed + Pipefy-specific terms</name>
  <files>dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md, dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md, dydx-delivery/skills/platform-pipefy/references/vocabulary.md</files>
  <read_first>
    - .planning/phases/06-internalise-platform-skills/06-RESEARCH.md:528-606 (knowledge-ingestion.md + client-shape-gotchas.md + vocabulary.md outlines)
    - .planning/phases/06-internalise-platform-skills/06-PATTERNS.md:259-354 (per-file analogs + Pipefy-specific seed content)
    - .planning/DESIGN.md:408-457 (DESIGN-14 REVISED — Vodacom custom-subdomain verified seed)
    - dydx-delivery/references/glossary.md (Phase 5 canonical SoT for project-wide terms — D-66 dedup gate target)
    - dydx-delivery/skills/discovery-intake/references/intake-template.md (partial analog for sectioned doc template; if file does not exist, use the RESEARCH.md outline as authoritative)
    - .planning/phases/06-internalise-platform-skills/06-CONTEXT.md D-65 (client-shape-gotchas seeding) and D-66 (vocabulary scope)
    - .claude/memory/reference_client_brain_coda_docs.md (Up & Up Group + VodafoneZiggo are the 2 documented Coda-brain clients)
  </read_first>
  <action>
Create three files together (single atomic task — they share the per-platform 5-file shape commitment per D-63):

**FILE A: `dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md`**

```markdown
# Pipefy — Knowledge ingestion

## Primary path (paste-only per UAT-6.1)

`native_ai_path: paste`. Stage 10 (`push-native-ai-knowledge` skill, v2.5 / Phase 7) emits a paste bundle containing:

1. **Pipefy Behaviors instructions** — the Behaviors prose block authored from approved Stage 9 documentation fragments (see DESIGN-26 paste-bundle contract).
2. **KB document upload list** — file paths + filenames for the documents the human will manually upload to the Pipefy AI Agents Knowledge Base via the Pipefy UI.
3. **Per-client target ID** — Pipefy AI target reference from `<Client> Brain/00_HUB.md` `Pipefy AI:` block (DESIGN-26 / MIN-4 fix — refuses ingest on target mismatch).

The paste bundle is human-upload-only via the Pipefy AI Agents UI. No API ingestion path is exercised through v2.6 (UAT-6.1; OPEN-Q01 closed).

## Audit log shape

Stage 10 records each ingestion in `<Client> Brain/changelog/cr-<id>/native-ai-audit.md` with these fields:

- `ingested_at:` ISO-8601 timestamp
- `doc_version:` semver from the approved Stage 9 doc
- `target_id:` Pipefy AI target reference (matches `00_HUB.md` `Pipefy AI:` block)
- `paste_bundle_path:` local path to the bundle artefact emitted by Stage 10
- `human_upload_acknowledged_at:` ISO-8601 timestamp recorded after the human confirms the upload in Pipefy UI

Refuses ingest if `doc_published_at < last_diff_review_at` (CRIT-8 fix per DESIGN-26) or `target_id` mismatches the artefact's `client:` frontmatter (MIN-4 fix).

## Fallback (manual paste via UI)

Paste-only IS the fallback. UAT-6.1 forecloses an API ingestion path through v2.6; humans manually upload via the Pipefy AI Agents Knowledge Base UI. The Stage 10 paste-bundle artefact + audit-log entry are the canonical record that ingestion happened.
```

**FILE B: `dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md`**

```markdown
# Pipefy — Client shape gotchas

> Append-only — new client shapes added per first-engagement; existing rows
> preserved for audit trail.

## Known shapes (verified)

### Vodacom (custom-subdomain — verified per DESIGN-14 REVISED, 2026-05-10)

- **web_host:** `vodacom.pipefy.com/{org_id}` (custom-subdomain pattern)
- **API host:** `api.pipefy.com/graphql` (canonical-only for ALL tenants per UAT-4.1 / Q24 — verified via DNS test 2026-05-10; `api.vodacom.pipefy.com` does NOT resolve)
- **org_id:** required path segment for web URLs; NOT required in GraphQL API calls
- **Source artefact:** DESIGN-14 REVISED at `.planning/DESIGN.md:408-457`

## Pattern slots (variant taxonomy)

### Web host variants
- **Default:** `app.pipefy.com/{org_id}` (most tenants)
- **Custom-subdomain:** `<subdomain>.pipefy.com/{org_id}` (Vodacom and similar — enterprise white-label)

### org_id semantics
- Required path segment in web URLs
- NOT required in GraphQL API calls (API endpoint is canonical-only)
- Per-tenant — stored in `<Client> Brain/00_HUB.md` Pipefy block

### Identifier distinction
- `pipe_id` — a Pipefy "pipe" (workflow); pipes contain phases
- `phase_id` — a phase within a pipe; phases contain cards
- `card_id` — an individual card (work-item) within a phase
- These three identifiers MUST NOT be conflated — every GraphQL query targets one of the three explicitly.

## How to add a new shape

When onboarding a new client, append a verified row to `## Known shapes (verified)`
citing the engagement date + source artefact. Pattern slots are append-only;
existing rows are preserved for audit trail.
```

**FILE C: `dydx-delivery/skills/platform-pipefy/references/vocabulary.md`**

```markdown
# Pipefy — Vocabulary

> For project-wide terms see `dydx-delivery/references/glossary.md`.

## Platform-specific terms

**pipe** — A Pipefy workflow container. Each pipe holds one or more phases and a set of cards moving through those phases. Maps to `pipe_id` in GraphQL queries.

**phase** — A stage within a pipe (e.g., "Backlog", "In progress", "Review", "Done"). Cards transition between phases via API or UI actions. Maps to `phase_id`.

**card** — An individual work-item record within a phase. Carries field values, attachments, comments, and transition history. Maps to `card_id`.

**connection** — A Pipefy concept linking two records (e.g., a card to a database record). Connections are typed and bidirectional.

**org_id** — Per-tenant organisation identifier; required path segment in web URLs (`<host>/<org_id>/...`) but NOT required in GraphQL API calls (API endpoint is canonical-only per UAT-4.1).

**web_host** — Per-tenant web host (e.g., `app.pipefy.com`, `vodacom.pipefy.com`). Used for web URLs only — distinct from the canonical API host `api.pipefy.com/graphql`.

**pipe_id** — Identifier for a pipe (workflow container). Required in GraphQL queries targeting pipe-scoped operations.

**phase_id** — Identifier for a phase within a pipe. Required in GraphQL queries targeting phase-scoped operations.

**card_id** — Identifier for an individual card. Required in GraphQL queries targeting card-scoped operations.

**Behaviors** — Pipefy AI Agents capability: pre-authored instruction blocks that steer agent behaviour in workflows. Stage 10 paste bundle includes a Behaviors block per UAT-6.1.

**Pipefy AI Agents** — Pipefy's 2026 AI capability surface (launched Nov 2025). Includes KB, Skills, MCP integration, IDP, Web Search, BYO-LLM. Capability matrix tracked in `references/native-ai-inventory.md`.

> Verification gate: synthesis-plan grep dedup confirms zero of these terms
> are ALSO defined in `dydx-delivery/references/glossary.md`. Cross-cutting
> terms (frontmatter / sandbox / native_ai_path / status: lifecycle) live in
> glossary.md, NOT here.
```
  </action>
  <verify>
    <automated>test -f dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md && test -f dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md && test -f dydx-delivery/skills/platform-pipefy/references/vocabulary.md && grep -qF 'Behaviors' dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md && grep -qF 'Vodacom' dydx-delivery/skills/platform-pipefy/references/client-shape-gotchas.md && grep -qF 'pipe' dydx-delivery/skills/platform-pipefy/references/vocabulary.md && grep -qF 'dydx-delivery/references/glossary.md' dydx-delivery/skills/platform-pipefy/references/vocabulary.md && ! grep -qF 'native_ai_path: api' dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md && bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section pipefy 2>&1 | tee /tmp/pipefy-check.log | grep -qE '^PASS: A4'</automated>
  </verify>
  <done>All 3 files exist; UAT-6.1 paste-only contract documented in knowledge-ingestion.md; D-65 Vodacom seed + pattern slots in client-shape-gotchas.md; D-66 vocabulary opens with glossary pointer + Pipefy-specific terms only; A4 (Pipefy partition) PASSES — 5 references files present for platform-pipefy.</done>
  <acceptance_criteria>
    - Satisfies A4 (Pipefy partition): all 5 of `dydx-delivery/skills/platform-pipefy/references/{api-contract.md, native-ai-inventory.md, knowledge-ingestion.md, client-shape-gotchas.md, vocabulary.md}` exist (`test -f` for each exits 0)
    - knowledge-ingestion.md: `grep -qF 'Behaviors' <file> && grep -qF 'UAT-6.1' <file> && grep -qF 'paste-only' <file>` all conditions met
    - client-shape-gotchas.md: `grep -qF 'Vodacom' <file> && grep -qF 'vodacom.pipefy.com' <file> && grep -qF 'Append-only' <file>` all conditions met (D-65 seed)
    - vocabulary.md (D-66 contract): `grep -qF '> For project-wide terms see '` against `dydx-delivery/skills/platform-pipefy/references/vocabulary.md` exits 0 AND `grep -cE '^\*\*(pipe|phase|card|connection|org_id|web_host|pipe_id|phase_id|card_id|Behaviors|Pipefy AI Agents)\*\*' <file>` returns ≥ 11 (Pipefy-specific terms only)
    - vocabulary.md D-66 dedup pre-check: `grep -qE '^\*\*(frontmatter|sandbox|native_ai_path)\*\* ' dydx-delivery/skills/platform-pipefy/references/vocabulary.md` exits 1 (those terms are NOT defined here — they live in glossary.md; 06-04 synthesis plan re-runs the canonical dedup grep)
    - Satisfies A7 partial (T-06-02 file-level): `grep -qF 'native_ai_path: api' dydx-delivery/skills/platform-pipefy/references/knowledge-ingestion.md` exits 1 (zero hits)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all 5 tasks land:

1. `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh --section pipefy` exits 0 (all Pipefy-specific assertions A1, A4-pipefy-partition, A5-pipefy, A6-pipefy, A8, A9, A10, A11, A14, A17-pipefy PASS).
2. Full suite `bash .planning/phases/06-internalise-platform-skills/scripts/phase6-structure-check.sh` continues to FAIL at A2 or A3 (Wrike/Ziflow SKILL.md not yet created — expected; resolved in 06-02 + 06-03).
3. `grep -r 'native_ai_path: api' dydx-delivery/skills/platform-pipefy/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'` returns zero hits (T-06-02 mitigation; A7 partial).
4. `grep -r 'api_host' dydx-delivery/skills/platform-pipefy/ --exclude-dir=scripts --exclude='*.sh' --exclude='*~'` returns zero hits (T-06-04 mitigation; A11 second-half).
5. All 6 Pipefy tree files (1 SKILL.md + 5 references/) plus the Wave 0 scaffold (script + 4 .gitkeep placeholders) committed atomically per task.
</verification>

<success_criteria>
Plan 06-01 is complete when:
- [ ] Wave 0 scaffold: `phase6-structure-check.sh` exists and is executable with all 17 assertion implementations present
- [ ] Wave 0 scaffold: `platform-wrike/.gitkeep`, `platform-wrike/references/.gitkeep`, `platform-ziflow/.gitkeep`, `platform-ziflow/references/.gitkeep` all exist (so 06-02 + 06-03 can run in parallel)
- [ ] `platform-pipefy/SKILL.md` exists with canonical frontmatter (`platform: pipefy`, `tier_claims_last_verified: 2026-05-09`, `native_ai_path: paste`, `frontmatter_version: 2`), D-59 hard-rules pointer block, D-68 re-verification trigger section
- [ ] `platform-pipefy/references/api-contract.md` documents canonical-only `api.pipefy.com/graphql` (UAT-4.1) + Q24 verification date 2026-05-10 + HTML-on-auth-failure detection + paginate_all 7-part contract (MOD-4) + 13 req/sec throttle (Q06.2) + web_host/org_id with zero `api_host` references (T-06-04)
- [ ] `platform-pipefy/references/native-ai-inventory.md` documents 7-row 2026 Pipefy AI Agents 2.0 capability matrix
- [ ] `platform-pipefy/references/knowledge-ingestion.md` documents UAT-6.1 paste-only contract (Behaviors + KB upload list)
- [ ] `platform-pipefy/references/client-shape-gotchas.md` carries Vodacom verified seed (D-65) + pattern slots
- [ ] `platform-pipefy/references/vocabulary.md` opens with glossary pointer (D-66) and lists 11 Pipefy-specific terms only
- [ ] Phase-6 structure-check assertions PASSING after plan completion: A1, A4-pipefy, A5-pipefy, A6-pipefy, A7-partial-pipefy, A8, A9, A10, A11, A14, A17-pipefy
- [ ] Zero hits for `native_ai_path: api` in platform-pipefy/ tree (T-06-02 mitigation)
- [ ] All deliverables committed; each task = 1 atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-internalise-platform-skills/06-01-SUMMARY.md` capturing:
- Wave 0 scaffold landed (phase6-structure-check.sh + 4 placeholder .gitkeep files); structure-check exits 1 with expected FAIL: A2 (Wrike SKILL.md missing) or A3 (Ziflow SKILL.md missing) — confirms parallel-wave plans are unblocked
- Pipefy SKILL.md + 5 references/ files landed end-to-end with all per-platform assertions PASSING
- Q06.2 throttle calibration resolved inline (13 req/sec) — landed in api-contract.md § Rate limit + throttle; OPEN-QUESTIONS.md row flip deferred to 06-04 synthesis plan
- Any execution-time re-verification of Pipefy AI Agents 2.0 capability matrix (manual; documents whether tier_claims_last_verified: was bumped forward from 2026-05-09 baseline)
- Carry-forward notes for 06-02 (Wrike) and 06-03 (Ziflow) — e.g., D-59 pointer block confirmed verbatim from execute-tests/SKILL.md:20-22; auth-shape secret-handling note lifted near-verbatim from connector-matrix.md:31
</output>